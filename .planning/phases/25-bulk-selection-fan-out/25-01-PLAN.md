---
phase: 25-bulk-selection-fan-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/outbound/bulk_envelope.ex
  - priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs
  - lib/cairnloop/governance.ex
  - test/cairnloop/outbound/bulk_envelope_test.exs
  - test/cairnloop/governance_test.exs
autonomous: false
requirements: [BULK-01, BULK-03]

must_haves:
  truths:
    - "A `Cairnloop.Outbound.BulkEnvelope` schema exists with snapshotted recipient + rendered body fields (D-13)."
    - "A migration creates `cairnloop_outbound_bulk_envelopes` and `mix ecto.migrate` has been run on a Postgres-available host (D-15, schema_migration_note)."
    - "Cohort eligibility reads for InboxLive go through the narrow `Cairnloop.Governance` facade — no direct Ecto query from the web layer (D-14)."
    - "Refusal attempts (oversized cohort) persist on `BulkEnvelope` with `status: :refused_cap_exceeded` (research Open Question 5, mirrors `Governance.propose_blocked`)."
  artifacts:
    - path: "lib/cairnloop/outbound/bulk_envelope.ex"
      provides: "Ecto schema for `cairnloop_outbound_bulk_envelopes` table, with `changeset/2` validating required fields and `count > 0`."
      contains: "defmodule Cairnloop.Outbound.BulkEnvelope"
    - path: "priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs"
      provides: "Postgres migration creating the bulk-envelope table with indexes on `:requested_at` and `:template_id`."
      contains: "create table(:cairnloop_outbound_bulk_envelopes"
    - path: "lib/cairnloop/governance.ex"
      provides: "Two new public functions: `list_eligible_conversation_ids_for_bulk_recovery/1` and `preview_bulk_recovery_cohort/1` (cohort + first-5 sample by updated_at desc, +N more tail)."
      contains: "def list_eligible_conversation_ids_for_bulk_recovery"
    - path: "test/cairnloop/outbound/bulk_envelope_test.exs"
      provides: "Headless schema tests: required fields, `count > 0` invariant, status enum values."
    - path: "test/cairnloop/governance_test.exs"
      provides: "Extended tests for the two new cohort-read functions (against process-dictionary MockRepo)."
  key_links:
    - from: "lib/cairnloop/governance.ex (preview_bulk_recovery_cohort/1)"
      to: "lib/cairnloop/conversation.ex (status: :resolved enum)"
      via: "Ecto where clause filtering candidates to `status == :resolved`"
      pattern: "where\\(\\[c\\], c\\.id in \\^candidate_ids and c\\.status == :resolved\\)"
    - from: "lib/cairnloop/outbound/bulk_envelope.ex"
      to: "priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs"
      via: "Ecto schema/table-name correspondence"
      pattern: "schema \"cairnloop_outbound_bulk_envelopes\""
---

<objective>
Lay the durable substrate Phase 25 fans out on top of: a new `Cairnloop.Outbound.BulkEnvelope` schema (the audit truth row per D-13), its migration, and two narrow read functions on `Cairnloop.Governance` (D-14) that InboxLive will consume in later plans for the cohort-eligibility preview.

Purpose: This plan satisfies the "snapshot at decision time" and "narrow facade reads" project invariants (CLAUDE.md) before any caller is wired up. It also lands the persisted refusal lane so OBS-02 (Phase 26) can read both submitted and refused bulk attempts from one table.

Output: Schema + migration + two facade functions + two test files. No LiveView changes, no `Outbound.bulk_trigger/2` yet — that arrives in Plan 02 once this substrate is in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md
@.planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md
@.planning/phases/25-bulk-selection-fan-out/25-VALIDATION.md
@CLAUDE.md
@lib/cairnloop/conversation.ex
@lib/cairnloop/governance.ex
@lib/cairnloop/outbound.ex
@lib/cairnloop/chat.ex
@test/cairnloop/governance_test.exs

<interfaces>
<!-- Contracts the executor needs. Extracted from in-repo code. -->

From lib/cairnloop/conversation.ex:
```
defmodule Cairnloop.Conversation do
  schema "cairnloop_conversations" do
    field(:status, Ecto.Enum, values: [:open, :resolved, :archived], default: :open)
    field(:subject, :string)
    field(:host_user_id, :string)
    # ... timestamps include :updated_at
  end
end
```

From lib/cairnloop/governance.ex (private repo accessor pattern to mirror):
```
defp repo do
  Application.fetch_env!(:cairnloop, :repo)
end
```

Migration timestamp convention: 14-digit UTC `YYYYMMDDHHMMSS` (see priv/repo/migrations/20260526084518_create_cairnloop_mcp_tokens.exs). Use `20260527063000` for this migration — strictly later than the most recent existing migration.

`Cairnloop.Governance` module already has `import Ecto.Query` at the top (line 60), so `where/3`, `order_by/3`, `select/3` are available. The `repo().all/1` indirection is the project-wide convention (CLAUDE.md narrow facade + D-30 from milestone 11).

Test pattern (process-dictionary MockRepo, no live DB):
- See `test/cairnloop/governance_test.exs:12-52` for the existing pattern (`Process.put(:tool_proposals, ...)`, `MockRepo.get_by/2`, `MockRepo.all/1`).
- The new `preview_bulk_recovery_cohort/1` test will seed `Process.put(:conversations, [...])` and provide a `MockRepo.all/1` clause that filters by `id in candidate_ids` and `status == :resolved`, then orders by `updated_at` desc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BulkEnvelope schema + headless schema test</name>
  <files>lib/cairnloop/outbound/bulk_envelope.ex, test/cairnloop/outbound/bulk_envelope_test.exs</files>
  <read_first>
    - lib/cairnloop/outbound.ex (sealed `trigger/2` shape, `repo()` indirection)
    - lib/cairnloop/governance/tool_proposal.ex IF PRESENT, else lib/cairnloop/conversation.ex (existing schema styling)
    - .planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md § "Code Examples" → "BulkEnvelope schema" (lines 484–522) — the executor MUST follow this shape; the schema's column list and `validate_number(:count, greater_than: 0)` invariant are load-bearing
    - .planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md § "Architecture & Re-use" (D-13)
    - test/cairnloop/governance_test.exs:1-60 (MockRepo / process-dictionary test pattern)
  </read_first>
  <behavior>
    - Test 1: `BulkEnvelope.changeset/2` with all required fields present is `valid?`.
    - Test 2: Missing any of [`:id`, `:template_id`, `:rendered_body`, `:recipient_conversation_ids`, `:count`, `:requested_at`] makes the changeset invalid (assert error on each field individually).
    - Test 3: `count: 0` is invalid (validate_number greater_than: 0).
    - Test 4: `status` defaults to `:submitted`; setting `status: :refused_cap_exceeded` with a `refused_reason` string is valid (research Open Question 5 — persisted refusals).
    - Test 5: `recipient_conversation_ids` accepts a list of integers; passing a non-list / non-integer-element list is rejected by the changeset cast.
  </behavior>
  <action>
    Create `lib/cairnloop/outbound/bulk_envelope.ex` defining `Cairnloop.Outbound.BulkEnvelope` per the research § "Code Examples" → "BulkEnvelope schema" (lines 484–522). Key shape:

    - `@primary_key {:id, :binary_id, autogenerate: false}` (envelope generates UUID at `bulk_trigger/2` time per A2; we keep `autogenerate: false` so callers always provide an explicit `Ecto.UUID.generate/0` value — easier to assert in tests).
    - schema table name: `"cairnloop_outbound_bulk_envelopes"`.
    - fields: `template_id :string`, `rendered_body :string`, `recipient_conversation_ids {:array, :integer}`, `count :integer`, `requested_by :string`, `requested_at :utc_datetime_usec`, `status Ecto.Enum, values: [:submitted, :refused_cap_exceeded], default: :submitted`, `refused_reason :string`, plus `timestamps()`.
    - `changeset/2` casts all of the above, validates_required `[:id, :template_id, :rendered_body, :recipient_conversation_ids, :count, :requested_at]`, validates `count > 0`.
    - Module docstring must state: "Durable audit envelope for a bulk outbound action (D-13, OBS-02-shaped). Snapshots template_id, rendered_body, and recipient cohort at confirmation time. Per-recipient delivery flows through `Outbound.trigger/2` (sealed) carrying the envelope id. Refused attempts persist with `status: :refused_cap_exceeded`."

    Create `test/cairnloop/outbound/bulk_envelope_test.exs`:
    - `use ExUnit.Case, async: true` (pure schema, no DB needed).
    - `alias Cairnloop.Outbound.BulkEnvelope`.
    - One `describe "changeset/2"` block holding the five behavior tests above.
    - Build a `valid_attrs/0` helper that returns a map with all required fields populated (use `Ecto.UUID.generate()` for `:id` and `DateTime.utc_now()` for `:requested_at`).
    - For "invalid when missing field" tests, iterate over the required-field list and drop one at a time (concise, avoids a Cartesian explosion — Nyquist).

    Module compiles warnings-clean (D-15).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tail -20 &amp;&amp; mix test test/cairnloop/outbound/bulk_envelope_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/outbound/bulk_envelope_test.exs` exits 0 with all five tests green.
    - `grep -c "defmodule Cairnloop.Outbound.BulkEnvelope" lib/cairnloop/outbound/bulk_envelope.ex` returns 1.
    - `grep -c "schema \"cairnloop_outbound_bulk_envelopes\"" lib/cairnloop/outbound/bulk_envelope.ex` returns 1.
    - `grep -c "validate_number(:count, greater_than: 0)" lib/cairnloop/outbound/bulk_envelope.ex` returns 1.
    - `grep -c "values: \[:submitted, :refused_cap_exceeded\]" lib/cairnloop/outbound/bulk_envelope.ex` returns 1.
    - `mix compile --warnings-as-errors` exits 0 (D-15).
  </acceptance_criteria>
  <done>Schema module exists at the declared path with the documented shape; headless tests cover required-field, `count > 0`, status-enum, and refused-reason cases; build is warnings-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migration for cairnloop_outbound_bulk_envelopes</name>
  <files>priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs</files>
  <read_first>
    - priv/repo/migrations/20260526084518_create_cairnloop_mcp_tokens.exs (most recent migration; copy its module/file-naming conventions)
    - .planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md § "Code Examples" → "Migration" (lines 525–549) — load-bearing column types
    - lib/cairnloop/outbound/bulk_envelope.ex (the schema this migration creates the table for; field types MUST match)
  </read_first>
  <action>
    Create the migration file at the exact path `priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs` with module name `Cairnloop.Repo.Migrations.AddOutboundBulkEnvelopes`.

    Mirror the research § "Migration" example:
    - `create table(:cairnloop_outbound_bulk_envelopes, primary_key: false)` with `add :id, :binary_id, primary_key: true`.
    - Columns: `:template_id :string null: false`, `:rendered_body :text null: false`, `:recipient_conversation_ids {:array, :bigint} null: false` (use `:bigint` to match the conversations PK), `:count :integer null: false`, `:requested_by :string` (nullable — actor may be "system"), `:requested_at :utc_datetime_usec null: false`, `:status :string null: false, default: "submitted"`, `:refused_reason :string` (nullable — only set on refusal).
    - `timestamps()`.
    - `create index(:cairnloop_outbound_bulk_envelopes, [:requested_at])`.
    - `create index(:cairnloop_outbound_bulk_envelopes, [:template_id])`.

    Do NOT add a FK from `recipient_conversation_ids` to `cairnloop_conversations` (it's an integer array, not a single id — A6 in research; array FKs are awkward; the join is purely audit-time, not a runtime read).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tail -5 &amp;&amp; test -f priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs &amp;&amp; grep -c "create table(:cairnloop_outbound_bulk_envelopes" priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the exact declared path.
    - `grep -c "defmodule Cairnloop.Repo.Migrations.AddOutboundBulkEnvelopes" priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs` returns 1.
    - `grep -c "create table(:cairnloop_outbound_bulk_envelopes" ...` returns 1.
    - `grep -c "create index(:cairnloop_outbound_bulk_envelopes" ...` returns 2 (requested_at + template_id).
    - `grep -c "recipient_conversation_ids, {:array, :bigint}, null: false" ...` returns 1.
    - `mix compile --warnings-as-errors` exits 0 (the migration is `.exs` and is compiled by Ecto at run time, not via `mix compile`, but this ensures no surrounding code regressed).
  </acceptance_criteria>
  <done>Migration file exists with the documented columns and indexes; warnings-clean build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend Cairnloop.Governance with cohort-eligibility reads</name>
  <files>lib/cairnloop/governance.ex, test/cairnloop/governance_test.exs</files>
  <read_first>
    - lib/cairnloop/governance.ex (full file — `import Ecto.Query` is at line 60; existing public-API docstrings at lines 233–245 set the style; `repo()` accessor at lines 78–80; `list_proposals_for_conversation/1` at lines 969–988 is the closest analog for a narrow read with order_by + preload semantics)
    - lib/cairnloop/conversation.ex (status enum + updated_at)
    - lib/cairnloop/chat.ex (`list_conversations/0` at lines 10–14 — confirms `updated_at desc` is the project ordering convention for conversation lists)
    - test/cairnloop/governance_test.exs:1-60 (MockRepo `get_by/2` and `all/1` patterns — the executor will extend MockRepo to handle conversation queries)
    - .planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md § "Code Examples" → "Governance facade extension" (lines 551–599) — load-bearing function signatures and return shape
    - .planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md § "Architecture & Re-use" (D-14)
  </read_first>
  <behavior>
    - Test 1: `Governance.list_eligible_conversation_ids_for_bulk_recovery([])` returns `[]` without touching the repo (or, equivalently, returns `[]` via the trivial query).
    - Test 2: Given a MockRepo seeded with three conversations (one `:resolved`, one `:open`, one `:resolved`), `list_eligible_conversation_ids_for_bulk_recovery/1` with all three ids returns ONLY the two `:resolved` ids — order is irrelevant for this function.
    - Test 3: Given the same seed, `preview_bulk_recovery_cohort/1` returns a map `%{eligible_ids: [..], sample: [..], more: 0, total: 2}` with the two resolved conversations, sample ordered `updated_at desc`, labels formatted as `"<subject> (#<id>)"` when subject is a binary and `"Conversation #<id>"` when subject is nil.
    - Test 4: With 7 resolved conversations seeded, `preview_bulk_recovery_cohort/1` returns `total: 7`, `more: 2`, and `sample` has exactly 5 elements — the 5 most-recently-updated.
    - Test 5: A conversation with a candidate id but `status: :open` is NEVER included in either function's result (D-01 / D-14 invariant).
  </behavior>
  <action>
    In `lib/cairnloop/governance.ex` add two PUBLIC functions, mirroring the research § "Governance facade extension" (lines 551–599):

    1. `list_eligible_conversation_ids_for_bulk_recovery/1`:
       - Takes `candidate_ids :: [integer()]`.
       - Returns the subset that have `status == :resolved`.
       - Uses `alias Cairnloop.Conversation` inside the function body or at the top of the module (the top-of-module alias list around line 62 is the right place — keep additions next to existing aliases).
       - Query shape: `Conversation |> where([c], c.id in ^candidate_ids and c.status == :resolved) |> select([c], c.id) |> repo().all()`.
       - Docstring: cites D-01 (resolved-only), D-02 (visible-rows-only — the caller MUST pass the visible cohort), D-14 (narrow facade), and explicitly states "v1 eligibility: status == :resolved".

    2. `preview_bulk_recovery_cohort/1`:
       - Takes `candidate_ids :: [integer()]`.
       - Returns `%{eligible_ids: [..], sample: [String.t()], more: non_neg_integer(), total: non_neg_integer()}`.
       - Query shape: same filter as #1 but selects `%{id, subject, host_user_id, updated_at}`, orders by `updated_at` desc (research Open Question 6 — locked rationale).
       - Builds `sample` from `Enum.take(rows, 5)` mapped through a private `label_for/1` helper: `%{subject: s, id: id} when is_binary(s) -> "#{s} (##{id})"; %{id: id} -> "Conversation ##{id}"`.
       - `more = max(total - 5, 0)`.
       - Docstring: cites D-07 (mandatory modal preview), D-02 (candidate set = visible filtered cohort), D-14, and explicitly states ordering is `updated_at desc`.

    Both functions go through `repo().all()` — never `Cairnloop.Repo` directly (CLAUDE.md narrow facade + the existing D-30 carry).

    Extend `test/cairnloop/governance_test.exs` (additive — do not delete existing tests):
    - Add a new `describe "list_eligible_conversation_ids_for_bulk_recovery/1"` block.
    - Add a new `describe "preview_bulk_recovery_cohort/1"` block.
    - The existing MockRepo must be extended so that its `all/1` clause can also handle `Cairnloop.Conversation`-shaped queries. Two reasonable approaches:
      (a) parse the `Ecto.Query` struct's `from` and `wheres` (mirrors lines ~55–60 in the existing MockRepo for `list_proposals_for_conversation/1`), OR
      (b) add a `select/2` clause via `Process.get(:conversations, [])` and apply the `in candidate_ids` + `status == :resolved` filter + `order_by` in Elixir.
      Option (b) is simpler and matches the project's "pure / total-function tests" preference under D-16; pick it unless the existing MockRepo already supports approach (a) cleanly.
    - For the two new describes, seed `Process.put(:conversations, [...])` in the `setup` block and `Process.delete(:conversations)` in `on_exit`.
    - Use realistic timestamps so the `updated_at desc` ordering test is deterministic (e.g., `~U[2026-05-27 12:00:00.000000Z]`, `~U[2026-05-27 13:00:00.000000Z]`, etc.).

    All tests headless per D-16; no `# REPO-UNAVAILABLE` tag needed — these run against the MockRepo, not Postgres.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tail -10 &amp;&amp; mix test test/cairnloop/governance_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/governance_test.exs` exits 0 with the five new tests green (in addition to all previously-existing tests still green — no regression).
    - `grep -c "def list_eligible_conversation_ids_for_bulk_recovery" lib/cairnloop/governance.ex` returns 1.
    - `grep -c "def preview_bulk_recovery_cohort" lib/cairnloop/governance.ex` returns 1.
    - `grep -c "order_by(\\[c\\], desc: c.updated_at)" lib/cairnloop/governance.ex` returns at least 1 (the `preview_bulk_recovery_cohort/1` ordering).
    - `grep -c "c.status == :resolved" lib/cairnloop/governance.ex` returns at least 2 (one per new function).
    - The two new functions' docstrings each cite at least one of `D-01`, `D-02`, `D-07`, or `D-14` literally (string match): `grep -c "D-1[447]" lib/cairnloop/governance.ex` returns ≥ 2.
    - `mix compile --warnings-as-errors` exits 0 (D-15).
  </acceptance_criteria>
  <done>Two new public reads exist on the Governance facade with documented shapes and ordering; tests green; warnings clean; no direct `Conversation`-table query exists or is added in any web-layer file (D-14).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Run `mix ecto.migrate` on a Postgres-available host</name>
  <files>priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs (run, do not modify)</files>
  <what-built>The migration file `priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs` was added in Task 2. Claude cannot run `mix ecto.migrate` in this workspace because `Cairnloop.Repo` is REPO-UNAVAILABLE here (per CLAUDE.md / D-16).</what-built>
  <action>
    HUMAN ACTION REQUIRED — Claude cannot perform this step in the current workspace because `Cairnloop.Repo` is unavailable here (CLAUDE.md / D-16).

    Run `mix ecto.migrate` on a Postgres-available host (the project's primary dev/CI environment) so the `cairnloop_outbound_bulk_envelopes` table exists. Subsequent plans (02 and 03) build on this table; their headless tests pass against a MockRepo, but their integration assertions (Oban uniqueness, FK integrity, multi atomicity) cannot run without the real table in place. See the `<how-to-verify>` block below for the exact sequence to run and the column list to confirm.
  </action>
  <how-to-verify>
    On a host with Postgres available (the project's primary dev/CI environment):

    1. `cd` into the cairnloop project root.
    2. Run `mix ecto.migrate` and confirm the output includes:
       `== Running Cairnloop.Repo.Migrations.AddOutboundBulkEnvelopes.change/0`
       `create table cairnloop_outbound_bulk_envelopes`
       `create index cairnloop_outbound_bulk_envelopes_requested_at_index`
       `create index cairnloop_outbound_bulk_envelopes_template_id_index`
    3. Confirm the table exists by running (psql or `mix run`):
       `Cairnloop.Repo.query!("SELECT column_name FROM information_schema.columns WHERE table_name = 'cairnloop_outbound_bulk_envelopes' ORDER BY ordinal_position").rows`
       and verify the column list contains `id`, `template_id`, `rendered_body`, `recipient_conversation_ids`, `count`, `requested_by`, `requested_at`, `status`, `refused_reason`, `inserted_at`, `updated_at`.
    4. If `mix test.integration` exists and is runnable, run it; otherwise just confirm `mix test` still passes.

    If the migration fails (e.g., naming collision with an existing table, or `recipient_conversation_ids` array-of-bigint not supported), DO NOT just "fix it forward" — pause and surface the error so the planner can revise Task 2 cleanly.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms `mix ecto.migrate` output matches the expected table + index creation lines listed in &lt;how-to-verify&gt;, the column list query returns the documented column set, and subsequent `mix test` runs are green on the migrated host.</human-check>
  </verify>
  <resume-signal>Type "migrated" once `mix ecto.migrate` has run successfully and the table is present. Type "blocked: &lt;reason&gt;" if the migration failed or cannot be run yet.</resume-signal>
  <done>Migration has been applied on a Postgres-available host; `cairnloop_outbound_bulk_envelopes` table exists with the documented column list (id, template_id, rendered_body, recipient_conversation_ids, count, requested_by, requested_at, status, refused_reason, inserted_at, updated_at); operator has signaled "migrated".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web layer → Governance facade | Cohort-eligibility reads MUST go through the narrow facade per D-14. Direct Ecto queries from `InboxLive` would bypass this boundary and are forbidden. |
| Confirmation modal → Worker | The rendered template body MUST be snapshotted on `BulkEnvelope.rendered_body` at confirmation time, not re-resolved at worker run time (CLAUDE.md "snapshot at decision time"). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Tampering | `BulkEnvelope.rendered_body` column | mitigate | Schema persists `rendered_body :string` (Task 1). Plan 02 will write to it from `bulk_trigger/2` at confirmation; Plan 02 worker must NOT re-render. This plan's contribution: ensure the column exists and is non-nullable in the migration (Task 2) so callers cannot omit it. |
| T-25-04 | Tampering | Web layer running direct `Ecto` queries for eligibility | mitigate | Task 3 adds `Governance.list_eligible_conversation_ids_for_bulk_recovery/1` and `Governance.preview_bulk_recovery_cohort/1`. Plan 03 will be required to call ONLY these — no direct `Conversation |> where(...)` in `InboxLive`. The acceptance criterion `grep -c "Conversation |> where" lib/cairnloop/web/inbox_live.ex == 0` will be checked in Plan 03. |
| T-25-SC | Tampering | New package installs | accept | This plan adds ZERO new packages (research § "Package Legitimacy Audit"). All used libraries (`ecto_sql`, `phoenix_live_view`) are already in `mix.exs` and were vetted in earlier milestones. No slopcheck needed. |
</threat_model>

<verification>
**Plan-wide checks before declaring done:**

1. `mix compile --warnings-as-errors` exits 0 (D-15 — mandatory).
2. `mix test test/cairnloop/outbound/bulk_envelope_test.exs test/cairnloop/governance_test.exs` exits 0 (both schema and facade tests green).
3. `mix test` (full headless suite) still exits 0 — no regressions in the rest of the codebase. Baseline `Automation.DraftTest` failure (memory note: cairnloop-baseline-draft-test-failure) is pre-existing M005 drift and does NOT count as a regression introduced by this plan.
4. `git diff --stat` shows changes ONLY to the five `files_modified` paths declared above — no scope creep into `lib/cairnloop/outbound.ex`, `lib/cairnloop/web/inbox_live.ex`, or any other file.
5. Task 4 (`mix ecto.migrate`) has been resolved with "migrated" before declaring Plan 01 done — the BulkEnvelope schema is meaningless without its table on a Postgres-available host.
</verification>

<success_criteria>
- BulkEnvelope schema module exists, compiles warnings-clean, and its headless tests are green.
- Migration file exists at the exact path and creates the table + two indexes.
- `Cairnloop.Governance` exposes the two new cohort-read functions; their docstrings cite the relevant decision IDs; their tests are green.
- The migration has been run on a Postgres-available host (Task 4 resume signal "migrated") — this is the substrate Plan 02's `bulk_trigger/2` will write to.
- Full headless `mix test` suite is green (except the pre-existing `Automation.DraftTest` baseline noise per memory note).
- No scope creep: only the five declared files were modified.
</success_criteria>

<output>
Create `.planning/phases/25-bulk-selection-fan-out/25-01-SUMMARY.md` when done, recording:
- The exact migration filename (with timestamp) chosen, so Plan 02 can reference it for the `bulk_envelope_id` FK / column shape.
- Confirmation that the schema's column list matches the migration's column list.
- Confirmation that Task 4 was resolved (migrated / blocked) — and if blocked, what blocked it.
- The five new function/test counts that downstream plans can rely on (e.g., "`Governance` gained 2 public functions; `BulkEnvelope` test file has 5 tests").
- Any deviations from the research § "Code Examples" shapes (there should be none for v1, but record any if they were necessary).
</output>
