---
phase: 25-bulk-selection-fan-out
plan: 03
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - lib/cairnloop/web/inbox_live.ex
  - test/cairnloop/web/inbox_live_test.exs
autonomous: false
requirements: [BULK-01, BULK-02, BULK-03, UI-03]

must_haves:
  truths:
    - "Operators can multi-select resolved-only conversations in `InboxLive` via explicit checkboxes per row plus a `select all visible` toggle in the column header (D-01, D-02, D-03)."
    - "Selection state lives in a LiveView assign `@selected_ids :: MapSet.t/0`, is NOT persisted across reloads, and is cleared on navigate-away (D-04)."
    - "A sticky bulk action bar appears at the BOTTOM of the inbox panel when `MapSet.size > 0`, shows the selection count, a `Clear selection` affordance, and a primary `Send recovery follow-up to N` button (D-05, research Open Question 4 — bottom-anchored)."
    - "Clicking the primary button opens a confirmation modal that renders the recipient count, the first-5 sample (ordered `updated_at desc` per research Open Question 6) with `+N more` tail, and the SINGLE rendered template body (D-06, D-07)."
    - "The modal uses `<.focus_wrap>` for keyboard a11y; `Confirm send` invokes `Outbound.bulk_trigger/2`; `Cancel` closes the modal and PRESERVES `@selected_ids` (D-08, research Pitfall 6)."
    - "When `MapSet.size > max_batch_size`, the modal renders a calm, brand-aligned refusal banner (icon + text, never color-alone — D-10 / brand §7.5) and the `Confirm send` button is disabled."
    - "Cohort eligibility reads go through `Cairnloop.Governance.preview_bulk_recovery_cohort/1` and `Cairnloop.Governance.list_eligible_conversation_ids_for_bulk_recovery/1` ONLY — no direct `Conversation |> where(...)` query exists in `InboxLive` (D-14)."
    - "All buttons / styled elements use brand tokens (`var(--cl-primary, #A94F30)` for the primary action; `var(--cl-danger, #B54C36)` for the refusal accent); no hardcoded hex outside the token fallback (D-D / brand book)."
  artifacts:
    - path: "lib/cairnloop/web/inbox_live.ex"
      provides: "Full bulk-selection LiveView surface: `@selected_ids` assign, checkbox handlers, sticky bar, confirmation modal, refusal banner, and submit handler that calls `Cairnloop.Outbound.bulk_trigger/2`."
      contains: "selected_ids: MapSet.new()"
    - path: "test/cairnloop/web/inbox_live_test.exs"
      provides: "Headless `Phoenix.LiveViewTest` tests covering selection toggle, select-all-visible, sticky-bar visibility, modal render with sample + body, cancel preserves selection, oversized refusal copy + icon, brand-token presence, and `<.focus_wrap>` markup."
  key_links:
    - from: "lib/cairnloop/web/inbox_live.ex (handle_event \"open_bulk_confirm\")"
      to: "lib/cairnloop/governance.ex (preview_bulk_recovery_cohort/1)"
      via: "Function call inside the LiveView event handler"
      pattern: "Cairnloop\\.Governance\\.preview_bulk_recovery_cohort"
    - from: "lib/cairnloop/web/inbox_live.ex (handle_event \"confirm_bulk_send\")"
      to: "lib/cairnloop/outbound.ex (bulk_trigger/2)"
      via: "Function call (through outbound_module() indirection mirroring conversation_live.ex)"
      pattern: "outbound_module\\(\\)\\.bulk_trigger\\("
    - from: "lib/cairnloop/web/inbox_live.ex (sticky bar primary button)"
      to: "CSS brand token --cl-primary"
      via: "Inline style attribute"
      pattern: "var\\(--cl-primary"
---

<objective>
Wire `InboxLive` end-to-end to the durable + delivery substrate landed in Plans 01 and 02: explicit per-row checkboxes (D-03), a `select all visible` header toggle, a `MapSet`-based selection assign (D-04), a sticky bottom bulk action bar (D-05 + research OQ4), a mandatory confirmation modal with `<.focus_wrap>` (D-07 + D-08), a calm fail-closed refusal banner (D-10), and a submit handler that calls `Outbound.bulk_trigger/2` (D-13).

Purpose: This is the operator-visible surface for Phase 25. It completes BULK-01 / BULK-02 / BULK-03 / UI-03 by turning the 44-line inbox into a bulk-action cockpit aligned with the recovery-card affordance the conversation surface already exposes (lib/cairnloop/web/conversation_live.ex:825-846 — visual analog).

Output: One updated LiveView module and its test file. No new modules; no schema changes; no `Outbound` changes (those landed in Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md
@.planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md
@.planning/phases/25-bulk-selection-fan-out/25-VALIDATION.md
@.planning/phases/25-bulk-selection-fan-out/25-01-SUMMARY.md
@.planning/phases/25-bulk-selection-fan-out/25-02-SUMMARY.md
@CLAUDE.md
@prompts/cairnloop_brand_book.md
@lib/cairnloop/web/inbox_live.ex
@lib/cairnloop/web/conversation_live.ex
@lib/cairnloop/outbound.ex
@lib/cairnloop/governance.ex
@lib/cairnloop/conversation.ex
@test/cairnloop/web/inbox_live_test.exs

<interfaces>
<!-- Contracts the executor needs. Extracted from in-repo code and from Plans 01/02. -->

From lib/cairnloop/web/conversation_live.ex (Phase 24 recovery card, lines 823–847 — VISUAL ANALOG):
```
attr(:conversation, :map, required: true)
def outbound_recovery_card(assigns) do
  ~H"""
  <%= if @conversation.status == :resolved do %>
    <section class="rail-card outbound-action-card" aria-label="Outbound recovery">
      <div class="outbound-action-eyebrow">Outbound recovery</div>
      <h3>Send Recovery Follow-up</h3>
      <p class="outbound-action-summary">...</p>
      <button type="button" class="outbound-action-button" phx-click="trigger_recovery_follow_up">
        Send Recovery Follow-up
      </button>
      <p class="outbound-action-hint">...</p>
    </section>
  <% end %>
  """
end
```
The bulk surface MUST feel structurally parallel to this card. Reuse the `outbound-action-button` / `outbound-action-card` CSS class vocabulary so the styling lands "of a piece" (no new CSS file in this plan — class names are referenced; styles already live in `lib/cairnloop/web/conversation_live.ex` CSS region — additive style overrides are inline via the brand-token style attribute).

From lib/cairnloop/web/conversation_live.ex (Phase 24 template + outbound module indirection — lines 1739–1745):
```
defp outbound_module do
  Application.get_env(:cairnloop, :outbound_module, Outbound)
end

defp recovery_follow_up_template_id do
  Application.get_env(:cairnloop, :outbound_recovery_template_id)
end
```
Mirror this indirection in `InboxLive` so tests can stub `Outbound`. (D-06 — reuse the same `:outbound_recovery_template_id` config knob, no new template wiring.)

From Plan 02's `Cairnloop.Outbound.bulk_trigger/2` (caller responsibility):
- Renders the template body ONCE at confirmation time (D-13 snapshot-at-decision-time).
- Passes the rendered body as `rendered_body: body_string`.
- v1 has no per-recipient personalization (D-07), so the body is a constant string per envelope — for v1 the render is simply: `body = "Outbound message using template: #{template_id}"` (mirroring `lib/cairnloop/outbound.ex:23` default) UNLESS a Phase 24 host has wired a richer notifier — in which case the body is still snapshotted as the literal recovery-template string the host configured. The exact rendering call is the executor's choice but MUST be a pure function of `template_id` (no per-recipient inputs) and MUST happen INSIDE `InboxLive`, not inside `bulk_trigger/2`.

From Plan 01's `Cairnloop.Governance.preview_bulk_recovery_cohort/1` shape:
```
%{
  eligible_ids: [integer()],
  sample: [String.t()],   # first 5 labels, updated_at desc
  more: non_neg_integer(),
  total: non_neg_integer()
}
```

From mix.exs:89 — `phoenix_live_view ~> 1.0` is installed; `<.focus_wrap id={"..."}>` from `Phoenix.Component` is available.

Brand book references (prompts/cairnloop_brand_book.md):
- §5.6: calm fail-closed error pattern — "reason-forward, honest, never raw Elixir terms".
- §7.4 / §7.5: brand color tokens; never state-by-color-alone (icon + text required).
- §10.5: micro-interaction principles.
- §10.6: state chips.
- Primary token: `var(--cl-primary, #A94F30)`. Danger token: `var(--cl-danger, #B54C36)` (or whichever is the current token name in `lib/cairnloop/web/conversation_live.ex` CSS).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Selection state + sticky bottom bulk action bar in InboxLive</name>
  <files>lib/cairnloop/web/inbox_live.ex, test/cairnloop/web/inbox_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/inbox_live.ex (current 44-line file)
    - lib/cairnloop/web/conversation_live.ex lines 823–847 (visual analog — recovery card)
    - lib/cairnloop/conversation.ex (`status: :resolved` enum)
    - prompts/cairnloop_brand_book.md (entire — calm copy, brand tokens, icon+text)
    - .planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md § "Code Examples" → "Selection toggle in InboxLive" (lines 387–425) and "Render checkbox + sticky bar" (lines 427–459) — load-bearing
    - .planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md § "Selection Model" (D-03, D-04, D-05)
    - test/cairnloop/web/inbox_live_test.exs (existing 27-line file — the existing test stays green; new tests are additive)
  </read_first>
  <behavior>
    - Test 1 (initial assign): `mount/3` populates `@selected_ids` with `MapSet.new()`.
    - Test 2 (toggle single): `handle_event("toggle_select", %{"id" => "5"}, socket)` adds `5` (integer) to `@selected_ids`; calling again removes it.
    - Test 3 (toggle ignores non-resolved): When the visible cohort has both `:resolved` and `:open` conversations, a checkbox is rendered ONLY for resolved rows. Open rows have no checkbox (the LiveView side is the second line of defense — Plan 01's `Governance` facade is the first).
    - Test 4 (select all visible): `handle_event("toggle_select_all_visible", _, socket)` — when no eligible conversation is currently selected, the assign becomes `MapSet.new([all_eligible_ids])`; when all are already selected, the assign becomes `MapSet.new()` (toggle).
    - Test 5 (clear selection): `handle_event("clear_selection", _, socket)` resets `@selected_ids` to `MapSet.new()` without affecting any other assign.
    - Test 6 (sticky bar visibility): rendered HTML contains the bulk action bar markup (e.g., element with `aria-label="Bulk actions"` or `role="region"`) ONLY when `MapSet.size(@selected_ids) > 0`. When the size is 0, the bar markup is absent.
    - Test 7 (sticky bar copy + tokens): with 3 selected, the bar renders the text `3 selected`, a `Clear selection` button, and a primary button labeled `Send recovery follow-up to 3` whose style attribute (or CSS class binding to a token-aware class) contains the substring `var(--cl-primary` (brand token assertion, D-D).
    - Test 8 (bottom-anchored): the bulk action bar markup contains `position: sticky` and `bottom: 0` (research OQ4 — bottom-anchored placement). This is asserted via a substring grep on the rendered HTML; if the executor opts for a CSS class instead of inline style, the test must assert on the class name and confirm in the docstring that the class resolves to `position: sticky; bottom: 0;` somewhere in the project's CSS.
  </behavior>
  <action>
    Modify `lib/cairnloop/web/inbox_live.ex` to ADD selection state and the sticky bar (do NOT remove existing functionality — the search modal mount on lines 35–41 stays).

    1. `mount/3`: add `selected_ids: MapSet.new()` to the assigns. Document with a comment citing D-04 (LiveView-local, no persistence; cleared on navigate-away — handled because each remount restarts at `MapSet.new()`).

    2. Add `handle_event/3` clauses (mirror the research § "Code Examples" → "Selection toggle in InboxLive" shape):
       - `"toggle_select"` — MapSet.put/delete on the given conv id (parsed as integer).
       - `"toggle_select_all_visible"` — tristate-aware: if all visible eligible IDs are in the set, remove them; else add all of them. Use a private helper `visible_eligible_ids/1` that filters `@conversations` to `status == :resolved` and maps to `id`.
       - `"clear_selection"` — reset to `MapSet.new()`.

    3. `render/1`:
       - Each `<li>` now branches on `conv.status == :resolved` to render a checkbox; checkbox `phx-click="toggle_select"`, `phx-value-id={conv.id}`, `checked={MapSet.member?(@selected_ids, conv.id)}`, `aria-label={"Select conversation: #{conv.subject || "No subject"}"}` (a11y).
       - Append the sticky bar markup AFTER the `<ul>` (still inside the wrapping div). Wrapped in `<%= if MapSet.size(@selected_ids) > 0 do %>` so it's only rendered when there's a selection. Use `role="region" aria-label="Bulk actions"`. Style attribute MUST include `position: sticky; bottom: 0; background: var(--cl-surface-raised, #FFFFFF); border-top: 1px solid var(--cl-border, #D8D0BF);` (research § "Render checkbox + sticky bar" lines 444–457).
       - The primary button MUST use `style="background: var(--cl-primary, #A94F30); color: #fffdf8;"` (or a CSS class that resolves to those tokens — executor's call; the brand-token grep test below pins one or the other). Label: `Send recovery follow-up to <%= MapSet.size(@selected_ids) %>`. `phx-click="open_bulk_confirm"` (handled in Task 2 — for this task it just exists in markup but the handler may not yet do anything beyond `{:noreply, socket}` placeholder).
       - The `Clear selection` button uses `phx-click="clear_selection"`.
       - The "select all visible" affordance is a header-row checkbox above the `<ul>` (or as the first `<li>` — research is silent on exact placement; the brand book §10.6 suggests state chips at the top of lists, so a header checkbox at the top of the `<ul>` is the closer match). `phx-click="toggle_select_all_visible"`; `checked={...}` reflects the tristate result.

    4. Add a private helper `defp visible_eligible_ids(conversations), do: conversations |> Enum.filter(&(&1.status == :resolved)) |> Enum.map(& &1.id)`.

    5. The existing `SearchModalComponent` mount at lines 35–41 MUST remain intact. No changes to `Chat.list_conversations/0` (Plan 03 stays out of the data-source path — that would be a separate refactor).

    Extend `test/cairnloop/web/inbox_live_test.exs`:
    - The existing `test "renders the inbox and mounts the search modal..."` stays green.
    - Add eight new tests covering behaviors 1–8 above.
    - For the LiveView event-handler tests (toggle, select all, clear): the existing test uses `render_component/2` for static rendering. For event handlers a `live/2`-style test is more idiomatic but requires more setup. The cheapest headless approach: directly call `InboxLive.handle_event/3` with a `%Phoenix.LiveView.Socket{assigns: ...}` and assert on the returned socket — same pattern as conversation_live tests. If the executor prefers, use `Phoenix.LiveViewTest`'s `render_change/3` and `render_click/2` against a live test session — both are acceptable.
    - For the sticky-bar HTML assertions: `render_component/2` with `selected_ids: MapSet.new([1, 2, 3])` and `conversations: [%Conversation{id: 1, status: :resolved}, ...]` and assert via `String.contains?/2` on the rendered HTML.
    - Headless per D-16.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tail -10 &amp;&amp; mix test test/cairnloop/web/inbox_live_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/web/inbox_live_test.exs` exits 0 with the existing test PLUS eight new tests green.
    - `grep -c "selected_ids: MapSet.new()" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "def handle_event(\"toggle_select\"" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "def handle_event(\"toggle_select_all_visible\"" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "def handle_event(\"clear_selection\"" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "var(--cl-primary" lib/cairnloop/web/inbox_live.ex` returns at least 1.
    - `grep -c "position: sticky" lib/cairnloop/web/inbox_live.ex` returns at least 1.
    - `grep -c "bottom: 0" lib/cairnloop/web/inbox_live.ex` returns at least 1.
    - `grep -c "aria-label=\"Bulk actions\"" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "D-03\\|D-04\\|D-05" lib/cairnloop/web/inbox_live.ex` returns at least 1 (decision-ID traceability in a docstring or comment).
    - **D-14 invariant gate:** `grep -c "Conversation |> where" lib/cairnloop/web/inbox_live.ex` returns 0 (no direct Ecto query — this task doesn't yet add the Governance call, but the negative invariant must hold).
    - `mix compile --warnings-as-errors` exits 0 (D-15).
  </acceptance_criteria>
  <done>Selection assign, three event handlers, sticky bottom bar with brand-token primary button, and select-all-visible header checkbox are wired and tested. No Outbound or Governance calls yet — those land in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirmation modal + refusal banner + bulk_trigger submit</name>
  <files>lib/cairnloop/web/inbox_live.ex, test/cairnloop/web/inbox_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/inbox_live.ex (as just modified by Task 1)
    - lib/cairnloop/web/conversation_live.ex lines 194–230 (Phase 24 `trigger_recovery_follow_up` handler — error / `put_flash` shape to mirror)
    - lib/cairnloop/web/conversation_live.ex lines 1739–1745 (`outbound_module()` and `recovery_follow_up_template_id()` indirection helpers — MIRROR these in InboxLive)
    - lib/cairnloop/web/search_modal_component.ex (existing modal styling vocabulary; the bulk confirm modal should feel of-a-piece)
    - lib/cairnloop/governance.ex (the two functions added in Plan 01)
    - lib/cairnloop/outbound.ex (the `bulk_trigger/2` landed in Plan 02 — call signature)
    - prompts/cairnloop_brand_book.md §5.6 (calm fail-closed error pattern), §7.5 (no state-by-color-alone)
    - .planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md § "Pattern 4: `<.focus_wrap>` + `JS.push_focus`/`JS.pop_focus` Modal" (lines 254–303) and § "Code Examples" → "Calm refusal banner" (lines 461–482) — load-bearing
    - .planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md § "Preview + Message Contract" (D-06, D-07, D-08) and § "Batch Safety Rails" (D-09, D-10, D-11) and § "Architecture & Re-use" (D-13, D-14)
  </read_first>
  <behavior>
    - Test 1 (open opens modal, sample populated): clicking the primary button (or directly dispatching `"open_bulk_confirm"`) with `@selected_ids = MapSet.new([1, 2, 3])` calls `Cairnloop.Governance.preview_bulk_recovery_cohort/1` (use a `Mox`-stubbed `Governance` or a process-dictionary stub — the executor's call) and assigns `@bulk_preview` to a map with the documented shape. The rendered HTML contains the count `3`, the rendered template body, and the first-5-or-fewer sample labels. No `+N more` tail appears when `more == 0`.
    - Test 2 (+N more): with `@selected_ids = MapSet.new(1..8)` (assume all resolved), the rendered modal contains the exact string `+ 3 more` (or `+3 more` — the executor's call; the test must pin one of the two literal forms).
    - Test 3 (oversized refusal — D-10 calm copy + icon, never color-alone): with `MapSet.size > max_batch_size()` (use `Application.put_env(:cairnloop, :max_batch_size, 3)` + `on_exit` cleanup), clicking the primary button does NOT call `Outbound.bulk_trigger/2`. Instead, the rendered modal (or an inline refusal banner — the executor's call) contains the substring `This batch exceeds the safe send limit of 3` AND contains an `<svg` element (icon, not color-alone) AND uses `var(--cl-danger` somewhere in the markup. The `Confirm send` button is either absent or has the `disabled` attribute.
    - Test 4 (cancel preserves selection — D-08): with the modal open and `@selected_ids = MapSet.new([1, 2, 3])`, dispatching `"cancel_bulk_confirm"` closes the modal (next render does NOT contain `aria-modal="true"`) AND `@selected_ids` is STILL `MapSet.new([1, 2, 3])`. This is the load-bearing regression test for Pitfall 6.
    - Test 5 (focus_wrap rendered — UI-03 accessibility): with the modal open, the rendered HTML contains an element matching the `<.focus_wrap>` markup (Phoenix LiveView 1.0 renders it with a stable `id` and `phx-hook="Phoenix.FocusWrap"` attribute — the executor's call on which to assert; the simplest grep is for the string `focus-wrap` somewhere in the rendered HTML, since `Phoenix.Component.focus_wrap/1` always emits that token).
    - Test 6 (confirm calls bulk_trigger with snapshotted body + actor): dispatching `"confirm_bulk_send"` with a non-oversized selection calls `outbound_module().bulk_trigger(ids, opts)` where `opts` includes `:template_id` (read from `Application.get_env(:cairnloop, :outbound_recovery_template_id)`), `:rendered_body` (a string — the snapshotted body), and `:actor` (from `@host_user_id` per CLAUDE.md "session-derived actor"). Use a stub `Outbound` (e.g., `Application.put_env(:cairnloop, :outbound_module, StubOutbound)` + an `on_exit` cleanup) and assert that the stub received exactly one call with the expected args.
    - Test 7 (success flash): when `bulk_trigger/2` returns `{:ok, _}`, the LiveView sets a `put_flash(:info, ...)` with calm operator copy (e.g., `"Bulk recovery queued for N conversations."`). `@selected_ids` is RESET to `MapSet.new()` (this is a sane post-send default — narrowing scope: cancel preserves selection, success clears it).
    - Test 8 (error flash — fail-closed): when `bulk_trigger/2` returns `{:error, :batch_too_large}` (defensive — also covered by the Task-1 LiveView cap check, but the backend may have a tighter cap), the LiveView shows a fail-closed `put_flash(:error, ...)` with calm copy that does NOT include any raw Elixir terms (no `inspect/1` output). `@selected_ids` is PRESERVED so the operator can narrow the selection.
    - Test 9 (template missing): when `Application.get_env(:cairnloop, :outbound_recovery_template_id)` is `nil`, `confirm_bulk_send` does NOT call `bulk_trigger/2` and instead shows a calm operator message ("Recovery follow-up template is not configured.") — mirrors `conversation_live.ex:207`.
  </behavior>
  <action>
    Modify `lib/cairnloop/web/inbox_live.ex` to ADD modal + handlers (Task 1 markup stays):

    1. Module-private indirection helpers (mirroring `lib/cairnloop/web/conversation_live.ex:1739-1745`):
       - `defp outbound_module, do: Application.get_env(:cairnloop, :outbound_module, Cairnloop.Outbound)`.
       - `defp recovery_follow_up_template_id, do: Application.get_env(:cairnloop, :outbound_recovery_template_id)`.
       - `defp governance_module, do: Application.get_env(:cairnloop, :governance_module, Cairnloop.Governance)`.
       - `defp max_batch_size, do: Application.get_env(:cairnloop, :max_batch_size, 25)`.

    2. Add `bulk_modal_open: false`, `bulk_preview: nil`, `bulk_refusal: nil` to mount assigns.

    3. Add `handle_event/3` clauses:
       - `"open_bulk_confirm"`:
         - Convert `@selected_ids` to a sorted list.
         - If `length(ids) > max_batch_size()`: assign `bulk_refusal: %{count: length(ids), max: max_batch_size()}` and `bulk_modal_open: true` (the modal opens but with the refusal banner, not the preview). DO NOT call `Outbound.bulk_trigger/2`.
         - Else: call `governance_module().preview_bulk_recovery_cohort(ids)`. Snapshot the rendered body (v1: literal string `"Outbound message using template: #{template_id}"` — pure function of `template_id`, matching `lib/cairnloop/outbound.ex:23`). Assign `bulk_preview: %{count, sample, more, rendered_body, template_id}`, `bulk_modal_open: true`, `bulk_refusal: nil`.
       - `"cancel_bulk_confirm"`: assign `bulk_modal_open: false`, `bulk_preview: nil`, `bulk_refusal: nil`. DO NOT touch `selected_ids` (D-08 — Pitfall 6).
       - `"confirm_bulk_send"`:
         - Guard: if `recovery_follow_up_template_id()` is nil → `put_flash(:error, "Recovery follow-up template is not configured.")` (mirror conversation_live.ex:207) and close the modal.
         - Guard: if `@bulk_refusal != nil` → ignore (defensive, the button should be disabled in markup); close modal.
         - Else: call `outbound_module().bulk_trigger(ids, template_id: ..., rendered_body: ..., actor: @host_user_id)`.
         - On `{:ok, _}`: `put_flash(:info, "Bulk recovery queued for #{length(ids)} conversations.")`, reset `selected_ids` to `MapSet.new()`, close modal.
         - On `{:error, :batch_too_large}`: `put_flash(:error, "This batch exceeds the safe send limit. Narrow your selection and try again.")` — DO NOT show a raw Elixir term; preserve `selected_ids`; close modal. (Reuse the same calm copy vocabulary the inline banner uses.)
         - On any other `{:error, _}`: a generic calm message (mirror conversation_live.ex:225 — "Recovery follow-up could not be queued right now. Please try again."). Preserve `selected_ids`; close modal.

    4. `render/1` — append the modal markup at the bottom of the wrapping div, gated on `@bulk_modal_open`. Use the research § "Pattern 4" shape:
       - `<div role="dialog" aria-modal="true" aria-labelledby="bulk-confirm-title" phx-window-keydown="cancel_bulk_confirm" phx-key="Escape">`.
       - `<.focus_wrap id="bulk-confirm-wrap">` — load-bearing for Test 5.
       - Inside: branch on `@bulk_refusal`:
         - If `@bulk_refusal != nil`: render the refusal banner (research § "Calm refusal banner" lines 461–482):
           - `role="alert"`, `border: 1px solid var(--cl-danger, #B54C36)`, inline SVG icon (icon + text — never color-alone, brand §7.5 / D-10).
           - Copy: `Batch too large.` heading + `This batch exceeds the safe send limit of <%= @bulk_refusal.max %>. Narrow your selection and try again.`
           - A `Cancel` button (`phx-click="cancel_bulk_confirm"`).
           - The `Confirm send` button MUST be either absent or `disabled` here — Test 3 pins this.
         - Else (preview):
           - `<h2 id="bulk-confirm-title">Send recovery follow-up</h2>`.
           - `You're about to send to <strong><%= @bulk_preview.count %></strong> conversation(s).`
           - `<ul aria-label="First 5 recipients">` listing `@bulk_preview.sample`.
           - `<%= if @bulk_preview.more > 0 do %>+ <%= @bulk_preview.more %> more<% end %>`.
           - `<section aria-label="Message body">` containing `@bulk_preview.rendered_body`.
           - Buttons: `Cancel` (`phx-click="cancel_bulk_confirm"`) and `Confirm send` (`phx-click="confirm_bulk_send"`, `style="background: var(--cl-primary, #A94F30); color: #fffdf8;"`).

    5. The primary sticky-bar button's `phx-click` value MUST be updated to `"open_bulk_confirm"` so Task 1's placeholder handler is now connected end-to-end. (If Task 1 already wired `phx-click="open_bulk_confirm"`, this step is a no-op.)

    6. Update the module docstring at the top of `lib/cairnloop/web/inbox_live.ex` to enumerate the new responsibilities and cite D-01 through D-14 in a single short bullet list — decision-ID traceability for the post-planning Decision Coverage Gate.

    Extend `test/cairnloop/web/inbox_live_test.exs` with the nine behavior tests:
    - Use a `setup` block that puts a `StubOutbound` module under `Application.put_env(:cairnloop, :outbound_module, StubOutbound)` and a `StubGovernance` under `Application.put_env(:cairnloop, :governance_module, StubGovernance)`, with `on_exit` cleanup. Define both stubs inside the test file (mirrors the existing `MockNotifier` pattern in `test/cairnloop/workers/outbound_worker_test.exs`).
    - `StubGovernance.preview_bulk_recovery_cohort(ids)` returns a deterministic map shape derived from `ids` (e.g., labels `"Conversation ##{id}"`, sample = first 5, more = max(length(ids) - 5, 0), total = length(ids)).
    - `StubOutbound.bulk_trigger(ids, opts)` records the call via `send(self(), {:bulk_trigger, ids, opts})` and returns `{:ok, %{envelope: %{id: "stub-uuid"}}}`. A second variant (or a `Process.put(:stub_outbound_response, ...)`-driven switch) returns `{:error, :batch_too_large}` for the fail-closed test.
    - For each test that touches `Application.put_env(:cairnloop, :outbound_recovery_template_id, ...)`, make sure `on_exit` cleans it up (research Pitfall 7).
    - Headless per D-16.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tail -15 &amp;&amp; mix test test/cairnloop/web/inbox_live_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/web/inbox_live_test.exs` exits 0 with all Task 1 tests still green PLUS the nine new modal/refusal/submit tests green.
    - `grep -c "def handle_event(\"open_bulk_confirm\"" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "def handle_event(\"cancel_bulk_confirm\"" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "def handle_event(\"confirm_bulk_send\"" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "<.focus_wrap" lib/cairnloop/web/inbox_live.ex` returns at least 1.
    - `grep -c "outbound_module().bulk_trigger(" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "governance_module().preview_bulk_recovery_cohort" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "aria-modal=\"true\"" lib/cairnloop/web/inbox_live.ex` returns 1.
    - `grep -c "var(--cl-danger" lib/cairnloop/web/inbox_live.ex` returns at least 1 (refusal banner — D-10).
    - `grep -c "<svg" lib/cairnloop/web/inbox_live.ex` returns at least 1 (icon — never color-alone per brand §7.5 / D-10).
    - **D-14 invariant gate (final):** `grep -c "Conversation |> where" lib/cairnloop/web/inbox_live.ex` returns 0. (No direct Ecto query for eligibility — all reads go through `Cairnloop.Governance`.)
    - **No raw-Elixir-term operator copy gate (D-D):** `grep -E "inspect\\(" lib/cairnloop/web/inbox_live.ex | grep -v '^#' | wc -l` returns 0 (no `inspect/1` used in operator-visible strings).
    - `grep -c "D-0[78]\\|D-1[04]" lib/cairnloop/web/inbox_live.ex` returns at least 1 (decision-ID traceability for D-07/D-08/D-10/D-14).
    - `mix compile --warnings-as-errors` exits 0 (D-15).
  </acceptance_criteria>
  <done>Modal + refusal banner + submit handler are wired end-to-end; cohort eligibility goes through the Governance facade; rendered body is snapshotted at confirmation time; cancel preserves selection; success clears selection; brand tokens + icon-plus-text refusal copy verified; warnings-clean build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — bulk send produces N system_outbound cards (REPO-UNAVAILABLE coverage)</name>
  <files>lib/cairnloop/web/inbox_live.ex (observed in browser, not modified), lib/cairnloop/outbound.ex (exercised via UI, not modified), priv/repo/migrations/20260527063000_add_outbound_bulk_envelopes.exs (applied)</files>
  <what-built>
    Plans 01 + 02 + 03 together deliver an end-to-end bulk send:
    - InboxLive checkbox selection → preview modal → `Outbound.bulk_trigger/2` → one `BulkEnvelope` row + N `OutboundWorker` jobs → N `system_outbound` cards on the affected conversation timelines (D-A).
    The DB-touching assertions (BulkEnvelope row persisted, Oban uniqueness rejecting duplicate enqueues, FK integrity) CANNOT be exercised in this workspace (REPO-UNAVAILABLE per CLAUDE.md / D-16). They are headless-tested at the MockRepo level but only a Postgres-available host can confirm the integrated behavior.
  </what-built>
  <action>
    HUMAN VERIFICATION REQUIRED — Claude cannot perform the in-browser checks here.

    All preceding tasks across Plans 01–03 have been completed automatically. This task asks the operator to validate the integrated bulk-send flow on a Postgres-available host: end-to-end happy path (selection → modal → confirm → N system_outbound cards), the keyboard a11y of the `<.focus_wrap>` modal, the brand-token visual treatment, and the calm icon-plus-text refusal banner when the cap is exceeded. See `<how-to-verify>` for the exact sequence. The DB-touching assertions (BulkEnvelope row, Oban uniqueness, FK integrity) also gate here because `Cairnloop.Repo` is unavailable in the planner/executor workspace.
  </action>
  <how-to-verify>
    On a host with `Cairnloop.Repo` available:

    1. Ensure Plan 01 Task 4 completed (`mix ecto.migrate` ran; `cairnloop_outbound_bulk_envelopes` table exists). If not, run it now.
    2. `mix test` (full headless suite) — must be green (excluding the pre-existing `Automation.DraftTest` baseline failure documented in memory note `cairnloop-baseline-draft-test-failure`).
    3. `mix test.integration` — must be green. Specifically the integration tests tagged `# REPO-UNAVAILABLE` from Plans 01–02 must now run and pass:
       - `bulk_trigger/2` writes ONE `BulkEnvelope` row + N `Message` rows with `role: :system_outbound` and `metadata["bulk_envelope_id"]` matching the envelope.
       - Two `OutboundWorker` jobs for the same `(conversation_id, template_id, bulk_envelope_id)` — the second is rejected by Oban's unique constraint.
    4. Boot the dev server (`iex -S mix phx.server` from a host app, or whichever launch path the project uses). Visit the inbox.
    5. Seed at least three resolved conversations (subjects: "A", "B", "C" — anything distinct).
    6. Confirm:
       - Each resolved row shows a checkbox; non-resolved rows do not.
       - Clicking checkboxes adds them to the selection; the sticky bar appears at the BOTTOM of the panel and shows "3 selected" + "Send recovery follow-up to 3".
       - The primary button uses the brand primary color (visually warm rust orange) — confirms `var(--cl-primary, #A94F30)` resolves correctly in the host CSS.
       - Clicking "Send recovery follow-up to 3" opens a modal listing 3 conversation labels, the rendered template body, and "Confirm send" / "Cancel" buttons.
       - Tab key cycles focus WITHIN the modal (Cancel ↔ Confirm send) and does not escape — `<.focus_wrap>` works.
       - Pressing `Esc` cancels the modal AND preserves the 3-row selection (D-08).
       - Reopening the modal and clicking "Confirm send" produces a flash "Bulk recovery queued for 3 conversations." and clears the selection.
       - Opening each of the three conversations confirms a single `system_outbound` card appended to each timeline (D-A — bulk produces N such cards, not a new card type).
    7. Refusal path: set `Application.put_env(:cairnloop, :max_batch_size, 2)` in an `iex` session (or temporarily reduce in `config/dev.exs`). Reload the inbox. Select 3 resolved conversations. Click the primary button. Confirm:
       - The modal opens with a refusal banner (NOT a confirm preview).
       - The banner shows an SVG icon next to the heading "Batch too large." and the body text "This batch exceeds the safe send limit of 2. Narrow your selection and try again."
       - The accent uses `var(--cl-danger, #B54C36)` (verifies the brand token + the icon-not-color-alone invariant per D-10 / brand §7.5).
       - No `Confirm send` button is present (or it is `disabled`).
    8. Restore `:max_batch_size` to 25.

    If ANY of the above checks fail, document the failure in the resume signal so the planner can revise the failing plan cleanly (rather than fixing forward).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms: (a) `mix test` and `mix test.integration` are green on a Postgres-available host; (b) the in-browser checks 4–7 in &lt;how-to-verify&gt; all hold (checkboxes only on resolved rows, sticky bottom bar with brand-primary button, `<.focus_wrap>` traps focus, Esc/cancel preserves selection, confirm clears it, each affected conversation gets exactly one `system_outbound` card); (c) the oversized-cohort refusal banner renders with SVG icon + calm copy + `var(--cl-danger)` accent and no Confirm send button.</human-check>
  </verify>
  <resume-signal>Type "verified" when all checks pass on a Postgres-available host. Type "failed: &lt;summary&gt;" if any check failed.</resume-signal>
  <done>End-to-end bulk send has been observed on a Postgres-available host; happy path, focus trap, escape/cancel preservation, success clearing, and oversized-refusal copy all behave as documented; operator has signaled "verified".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → LiveView | `phx-click` events carry only `id` (string) — no recipient lists, no template content. The server reconstructs the cohort from `@selected_ids` (an assign), never from request params. |
| LiveView → `Outbound.bulk_trigger/2` | The LiveView is responsible for rendering the template body and passing it as `:rendered_body` — this is the snapshot-at-decision-time boundary. |
| LiveView → `Cairnloop.Governance` | All cohort-eligibility reads go through the facade (D-14). The acceptance criterion gates against any direct `Conversation \| where` in `inbox_live.ex`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering / DoS | LiveView submit handler | mitigate | Task 2 enforces `length(ids) > max_batch_size()` BEFORE calling `Outbound.bulk_trigger/2`; the modal renders a refusal banner instead of the preview. The envelope (Plan 02) is the second line of defense — defense-in-depth (research Pitfall 4). |
| T-25-02 | Repudiation / Tampering | (Oban uniqueness — owned by Plan 02) | accept (owned by Plan 02) | The LiveView's submit handler always passes a fresh envelope id (generated by `bulk_trigger/2`); the operator double-clicking submit produces ONE call to `bulk_trigger/2`, which produces ONE envelope row, whose id is the dedup key. |
| T-25-03 | Tampering | `rendered_body` snapshot | mitigate | Task 2 has the LiveView render the body ONCE inside `"open_bulk_confirm"` and assign it on `@bulk_preview.rendered_body`; the same string is passed to `bulk_trigger/2` on `"confirm_bulk_send"`. The user's confirmed body is exactly what gets sent — no template re-resolution between confirm and send. |
| T-25-04 | Tampering / Information Disclosure | `InboxLive` reading conversations | mitigate | Cohort eligibility goes through `Cairnloop.Governance.preview_bulk_recovery_cohort/1`. The acceptance criterion `grep -c "Conversation |> where" lib/cairnloop/web/inbox_live.ex == 0` is a hard gate (D-14). |
| T-25-05 | Information Disclosure | LiveView event payloads | mitigate | `phx-value-id` carries a single conv id (the browser already knows about); no recipient PII flows through the wire. The actor is `@host_user_id` from the session, NOT from request params. |
| T-25-06 | Repudiation | Operator copy carrying raw Elixir terms | mitigate | Acceptance criterion: `grep -E "inspect\\(" lib/cairnloop/web/inbox_live.ex | grep -v '^#' | wc -l == 0`. All `put_flash` strings are pre-baked calm copy (mirror conversation_live.ex:201/207/226). |
| T-25-SC | Tampering | New package installs | accept | This plan adds ZERO new packages. `Phoenix.LiveView`, `Phoenix.Component`, and `<.focus_wrap>` are all in `phoenix_live_view ~> 1.0` already in `mix.exs`. |
</threat_model>

<verification>
**Plan-wide checks before declaring done:**

1. `mix compile --warnings-as-errors` exits 0 (D-15).
2. `mix test test/cairnloop/web/inbox_live_test.exs` exits 0 with the existing test + Task 1 tests + Task 2 tests all green.
3. `mix test` (full headless suite) is green except the documented baseline `Automation.DraftTest` failure (memory note: cairnloop-baseline-draft-test-failure).
4. `git diff --stat` shows only `lib/cairnloop/web/inbox_live.ex` and `test/cairnloop/web/inbox_live_test.exs` modified — no scope creep into `Outbound`, `Governance`, schema, or migration.
5. Task 3 (human verification on a Postgres-available host) has been resolved with "verified". If it returns "failed: ...", the planner revises the failing plan (do NOT fix forward).
6. **Decision Coverage cross-check** (manual, per the planning quality gate):
   - D-01 covered by Task 1 (resolved-only checkboxes) + Plan 01 (`Governance.list_eligible_conversation_ids_for_bulk_recovery/1`).
   - D-02 covered by Task 1 (selection is over `@conversations`, the visible cohort — caller of the facade passes visible ids only).
   - D-03 covered by Task 1 (per-row checkbox + header `select all visible`).
   - D-04 covered by Task 1 (`MapSet` assign, `mount/3` reset).
   - D-05 + research OQ4 covered by Task 1 (sticky `position: sticky; bottom: 0`).
   - D-06 covered by Task 2 (uses `Application.get_env(:cairnloop, :outbound_recovery_template_id)`, same as Phase 24).
   - D-07 covered by Task 2 (modal renders count + first-5 sample + `+N more` + rendered body).
   - D-08 covered by Task 2 (cancel preserves selection — Test 4).
   - D-09 covered by Task 2 (cap check refuses with calm copy) + Plan 02 (envelope-level `{:error, :batch_too_large}`).
   - D-10 covered by Task 2 (refusal banner: SVG icon + brand-token danger color + calm copy).
   - D-11 covered by Plan 02 (Oban `unique: keys:`).
   - D-12 covered by Plan 02 (sealed `trigger/2`).
   - D-13 covered by Plan 02 + Task 2 (`rendered_body` snapshot persisted on envelope; LiveView passes the body).
   - D-14 covered by Plans 01 + 03 (facade reads; negative grep gate on InboxLive).
   - D-15 covered by every task's `mix compile --warnings-as-errors` gate.
   - D-16 covered by the headless-test posture across all plans + Task 3 human-verify checkpoint for DB-touching assertions.
   - D-A covered by Plan 02 (`Outbound.trigger/2` still produces `:system_outbound` messages — one per recipient).
   - D-B covered by Plan 02 (telemetry enum-only labels) + Task 2 (no `template_id` in `:telemetry` span calls).
   - D-C covered by Plans 01 + 02 (`BulkEnvelope` + Oban jobs are workflow truth; telemetry is observability only).
   - D-D covered by Task 1 + Task 2 (brand tokens, icon+text refusal, no `inspect/1` in operator copy).
</verification>

<success_criteria>
- `InboxLive` exposes per-row checkboxes for resolved conversations, a select-all-visible header toggle, and a sticky bottom action bar with calm operator copy and the `var(--cl-primary)` token.
- A `<.focus_wrap>`-based confirmation modal renders count, first-5 sample (ordered `updated_at desc`), `+N more` tail, and the single rendered template body.
- Cancel preserves `@selected_ids`; Confirm send calls `Outbound.bulk_trigger/2` with the snapshotted body and clears the selection on success.
- Oversized cohorts hit a calm refusal banner with icon + text, brand-token danger accent, and DO NOT call `bulk_trigger/2`.
- `mix compile --warnings-as-errors` clean; `mix test` headless suite green (baseline failure excluded); Task 3 (Postgres host verification) returns "verified".
- All sixteen decisions D-01..D-16 plus D-A..D-D are traceable from at least one acceptance criterion or must-have in this plan or a predecessor plan.
- Scope: only the two declared files were modified.
</success_criteria>

<output>
Create `.planning/phases/25-bulk-selection-fan-out/25-03-SUMMARY.md` when done, recording:
- The chosen sticky-bar markup approach (inline style vs CSS class + where the CSS lives).
- The chosen refusal-banner SVG icon (inline path data or external `<img>` ref — research recommends inline for FOUC-free a11y).
- The chosen `+N more` literal form (`+ 3 more` or `+3 more`) — Test 2 in Task 2 pins one of these.
- The body-rendering approach taken inside `"open_bulk_confirm"` (v1 should be the trivial `"Outbound message using template: #{template_id}"`).
- Confirmation that Task 3 human-verify returned "verified" — and if it returned "failed", what failed and how the next planning iteration should address it.
- Final decision coverage cross-check table (D-01..D-16, D-A..D-D) mapped to specific acceptance criteria across Plans 01–03.
</output>
