---
phase: 37-component-primitives
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/web/components.ex
  - test/cairnloop/web/components_test.exs
autonomous: true
requirements: [UIC-01, UIC-02]
must_haves:
  truths:
    - "An operator screen can render a full page via <.cl_page title=...> with breadcrumb/actions/subnav slots and :wide/:reading width variants producing different inner framing classes"
    - "cl_stat accepts only a numeric count (count :integer, required) — the count :any semantics bug is gone"
    - "cl_hero renders a copper Fraunces count with a job label, an optional :detail sub-line slot, and a primary CTA (slot or cta/href attrs)"
  artifacts:
    - path: "lib/cairnloop/web/components.ex"
      provides: "cl_page/1, cl_hero/1 components + cl_stat count narrowing"
      contains: "def cl_page"
    - path: "test/cairnloop/web/components_test.exs"
      provides: "Headless render tests for cl_page + cl_hero + cl_stat numeric contract"
      exports: []
  key_links:
    - from: "lib/cairnloop/web/components.ex (cl_page)"
      to: ".cl-page--wide / .cl-page--reading"
      via: "class string emitted per @width enum"
      pattern: "cl-page--#\\{@width\\}"
    - from: "lib/cairnloop/web/components.ex (cl_hero)"
      to: ".cl-hero__count"
      via: "copper count numeral class"
      pattern: "cl-hero__count"
---

<objective>
Add the page-shell and primary-count primitives to `Cairnloop.Web.Components`: `cl_page/1`
(UIC-01 / D-08, the inner page frame with title/subtitle/width-enum + actions/breadcrumb/subnav
slots), `cl_hero/1` (UIC-02 / D-02, the separate ~2-3×-weight copper hero count with a `:detail`
slot and a primary CTA), and the one-line `cl_stat` de-polymorphization (UIC-02 / D-01,
`count :any` → `count :integer`). This plan owns `components.ex` + `components_test.exs` for Wave 1;
it runs in parallel with Plan 01 (which owns `cairnloop.css`).

Purpose: These are the framing/primacy primitives every later vM016 screen (P38 shell migration,
P39 Home redesign) depends on. Built and unit-tested here; adopted later.
Output: `cl_page/1`, `cl_hero/1`, narrowed `cl_stat` + their headless render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-component-primitives/37-UI-SPEC.md
@.planning/phases/37-component-primitives/37-PATTERNS.md
@.planning/phases/37-component-primitives/37-RESEARCH.md

<interfaces>
<!-- Existing analogs in components.ex the executor builds against. No exploration needed. -->
From lib/cairnloop/web/components.ex (current):
- `cl_card/1` (line 56) — multi-slot container with `:if={@header != []}` slot-emptiness guards.
- `cl_shell/1` (line 161) — page chrome; `cl_page` renders INSIDE its `.cl-main` (line 182). Do not wrap/replace `.cl-main`.
- `cl_breadcrumb/1` (line 194) — already exists with `aria-label="Breadcrumb"`; pass it INTO the `:breadcrumb` slot, do not re-author breadcrumb markup.
- `cl_button/1` (line 41) — `variant` in default/primary/danger/ghost; used for the `cl_hero` CTA.
- `cl_stat/1` (line 137) — `<.link navigate={@href} class="cl-stat cl-focusable">` with `.cl-stat__job` / `.cl-stat__count` (copper) / `.cl-stat__count--calm` / `.cl-stat__meta`. `attr(:count, :any, required: true)` is at line 128 — THIS is the one line to narrow.
- The moduledoc rule (lines 16-24): components emit `.cl-*` classes ONLY; all color/spacing lives in cairnloop.css as bare var(--cl-*). NEVER inline style/hex in this file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: De-polymorphize cl_stat to numeric-only (count :integer)</name>
  <files>lib/cairnloop/web/components.ex, test/cairnloop/web/components_test.exs</files>
  <read_first>
    - lib/cairnloop/web/components.ex — `cl_stat/1` (line 137) and its attr block, specifically `attr(:count, :any, required: true)` at line 128 (the exact line to change). Confirm the other attrs (`job`, `meta`, `href`, `cta`, `icon`, `calm?`, `:rest`, `inner_block`) and read how `@count` is rendered.
    - test/cairnloop/web/components_test.exs — the existing `rendered_to_string(~H...)` idiom + assertion style (lines 1-40).
    - .planning/phases/37-component-primitives/37-PATTERNS.md — "cl_stat/1 narrowing" section (D-01, single-line attr change; no migration task needed because no P37-scope caller passes a non-integer — home_live passes integers).
  </read_first>
  <behavior>
    - Test 1: `cl_stat` rendered with an integer `count` (e.g. `count={42}`) renders `42` inside the `.cl-stat__count` span and the rendered HTML contains no `#hex`.
    - Test 2: `cl_stat` still renders its `job` label and navigates via `href` (existing behavior preserved — the narrowing changes only the attr type, not markup).
  </behavior>
  <action>
    Change the single line `attr(:count, :any, required: true)` (components.ex:128) to `attr(:count, :integer, required: true)` per D-01. Leave every other `cl_stat` attr, the markup, and the `.cl-stat` CSS untouched. Phoenix validates attr types at compile time for literal assigns (a literal non-integer becomes a `--warnings-as-errors` failure); runtime assign values are not strictly enforced, so this is an API-contract narrowing with no migration task (verified: no P37-scope caller passes a non-integer). Add the two render tests above to `components_test.exs` extending the existing file.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'attr(:count, :integer, required: true)' lib/cairnloop/web/components.ex` matches; `attr(:count, :any' no longer matches.
    - `mix compile --warnings-as-errors` exits 0.
    - A `cl_stat` test renders an integer count into `.cl-stat__count` and asserts `refute html =~ ~r/#[0-9a-fA-F]{3,6}/`.
    - `mix test test/cairnloop/web/components_test.exs` exits 0.
  </acceptance_criteria>
  <done>`cl_stat` declares `count :integer, required: true`; build is warnings-clean; render tests confirm the numeric tile and token-purity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cl_page/1 shell component (UIC-01 / D-08)</name>
  <files>lib/cairnloop/web/components.ex, test/cairnloop/web/components_test.exs</files>
  <read_first>
    - lib/cairnloop/web/components.ex — `cl_card/1` (line 56-62, the `slot` + `:if={@slot != []}` guard idiom to copy), `cl_shell/1` (line 161, the outer chrome `cl_page` renders inside), `cl_breadcrumb/1` (line 194, hosted in the `:breadcrumb` slot).
    - .planning/phases/37-component-primitives/37-PATTERNS.md — "cl_page/1" section (the full slot/attr API + container/slot-guard markup skeleton, reusing `.cl-row`/`.cl-row--between`).
    - .planning/phases/37-component-primitives/37-UI-SPEC.md — §1 cl_page (structure, attributes, width contract, CSS classes introduced).
  </read_first>
  <behavior>
    - Test 1: `cl_page` with `title="Audit Log"` and default width renders `<h1 class="cl-page__title">Audit Log</h1>` and the container class `cl-page--wide`.
    - Test 2: `cl_page` with `width="reading"` renders `cl-page--reading`.
    - Test 3: `cl_page` with `:breadcrumb`, `:actions`, and `:subnav` slots provided renders each slot's content; with a `subtitle` provided renders `.cl-page__subtitle`.
    - Test 4: rendered HTML contains no `#hex`.
  </behavior>
  <action>
    Add `cl_page/1` to `components.ex` following the PATTERNS skeleton: attrs `title :string, required: true`, `subtitle :string, default: nil`, `width :string, values: ~w(wide reading), default: "wide"` (a SINGLE enum attr per D-08, not two booleans); slots `:actions`, `:breadcrumb`, `:subnav` (all optional, guard each with `:if={@slot != []}`), and `slot(:inner_block, required: true)`. Emit `<div class={["cl-page", "cl-page--#{@width}"]}>` containing a `.cl-page__header` with the optional breadcrumb above an `.cl-row.cl-row--between` row holding `<h1 class="cl-page__title">{@title}</h1>` + optional `<p :if={@subtitle} class="cl-page__subtitle">` on the left and the optional actions slot on the right; then an optional `.cl-page__subnav`; then `<div class="cl-page__body">{render_slot(@inner_block)}</div>`. Render all caller text via auto-escaped `{...}` interpolation — never `raw/1`. Emit `.cl-*` class names only; no inline style/hex. Add the four tests above to `components_test.exs`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def cl_page(' lib/cairnloop/web/components.ex` matches.
    - The component declares `attr(:width, :string, values: ~w(wide reading), default: "wide")` (single enum attr).
    - A test asserts `html =~ "cl-page--wide"`, another asserts `html =~ "cl-page--reading"`, the title `<h1>` text renders, and the three slots + subtitle render when provided.
    - Every cl_page test asserts `refute html =~ ~r/#[0-9a-fA-F]{3,6}/`.
    - `mix compile --warnings-as-errors` and `mix test test/cairnloop/web/components_test.exs` both exit 0.
  </acceptance_criteria>
  <done>`cl_page/1` exists with the locked single-enum width attr + all named slots + required title/inner_block, renders distinct `:wide`/`:reading` framing classes, and is token-pure under test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add cl_hero/1 primary-count component (UIC-02 / D-02)</name>
  <files>lib/cairnloop/web/components.ex, test/cairnloop/web/components_test.exs</files>
  <read_first>
    - lib/cairnloop/web/components.ex — `cl_stat/1` (line 137-149, the count-tile anatomy `cl_hero` is a heavier sibling of), `cl_button/1` (line 41, the primary CTA to render from `cta`/`href`), `cl_card/1` slot-guard idiom (line 59).
    - .planning/phases/37-component-primitives/37-PATTERNS.md — "cl_hero/1" section (mirrors `cl_stat` structure; CTA precedence: explicit CTA slot wins, else render `<.cl_button variant="primary">` from `cta`/`href`).
    - .planning/phases/37-component-primitives/37-UI-SPEC.md — §2 cl_hero (structure, attributes, visual contract, CSS classes introduced) and the RESEARCH Open-Q1 resolution (slot wins; attrs fallback).
  </read_first>
  <behavior>
    - Test 1: `cl_hero` with `count={128}` and `job="Work the queue"` renders `128` inside `.cl-hero__count` and the `.cl-hero__job` label text.
    - Test 2: `cl_hero` with `calm?={true}` renders `.cl-hero__count--calm`.
    - Test 3: `cl_hero` with `cta="Open inbox"` and `href="/inbox"` (no CTA slot) renders a `<.cl_button variant="primary">` with that label/destination; a `:detail` slot, when provided, renders `.cl-hero__detail` content.
    - Test 4: rendered HTML contains no `#hex`.
  </behavior>
  <action>
    Add `cl_hero/1` to `components.ex` per D-02 (a SEPARATE component, NOT a `cl_stat variant="hero"` — do not re-polymorphize the just-narrowed `cl_stat`): attrs `count :integer, required: true` (copper Fraunces), `job :string, required: true`, `href :string, default: nil`, `cta :string, default: nil`, `calm? :boolean, default: false`; slots `:detail` (optional quiet sub-line — designed generic enough to host the P39 "Recover-resolved" sub-line without baking Home semantics in) and an optional `:inner_block` CTA slot. Emit `<section class="cl-hero">` with `<span class="cl-hero__job">{@job}</span>`, `<span class={["cl-hero__count", @calm? && "cl-hero__count--calm"]}>{@count}</span>`, an optional `<div :if={@detail != []} class="cl-hero__detail">{render_slot(@detail)}</div>`, and the CTA. CTA precedence (planner-pinned): when an inner_block/CTA slot is provided it wins (`:if={@inner_block != []}` → `render_slot`); otherwise when `@cta` is set, render `<.cl_button variant="primary" navigate={@href}>{@cta}</.cl_button>`. Token-only; render caller text via auto-escaped `{...}`, never `raw/1`. Add the four tests above.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def cl_hero(' lib/cairnloop/web/components.ex` matches; no `variant="hero"` path is added to `cl_stat`.
    - The component declares `attr(:count, :integer, required: true)`.
    - Tests assert `html =~ "cl-hero__count"` with the integer rendered, `html =~ "cl-hero__count--calm"` when `calm?`, the `cl-hero__job` label, the `cl_button` CTA from `cta`/`href`, and the `:detail` slot content.
    - Every cl_hero test asserts `refute html =~ ~r/#[0-9a-fA-F]{3,6}/`.
    - `mix compile --warnings-as-errors` and `mix test test/cairnloop/web/components_test.exs` both exit 0.
  </acceptance_criteria>
  <done>`cl_hero/1` exists as a distinct copper-count primitive with a `:detail` slot and slot-or-attrs CTA precedence; build is warnings-clean and tests confirm token-purity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller assigns → rendered HTML | A caller LiveView (P38+, not this phase) supplies `title`/`subtitle`/`job`/`count`/slot content. The only control is output-encoding of that text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Tampering / Elevation (XSS) | `cl_page`, `cl_hero` caller text | mitigate | All caller-supplied strings rendered via HEEx auto-escaped `{...}` interpolation; NO caller input ever reaches `Phoenix.HTML.raw/1` (V5 output-encoding). `raw/1` stays only on `cl_icon`'s fixed internal icon-path allowlist (not used by these two components). |
| T-37-04 | Tampering (style drift) | emitted markup | mitigate | Components emit `.cl-*` class names only — no inline `style=`/hex; enforced by `brand_token_gate_test.exs` (D-13) + per-test `refute html =~ ~r/#hex/`. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: not applicable). |

No HIGH-severity threats — pure presentational components, no auth/session/input-validation/crypto/data-access surface.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 (the `count :integer` narrowing must not surface a literal-non-integer caller).
- `mix test test/cairnloop/web/components_test.exs` green (existing + new tests).
- `mix test test/cairnloop/web/brand_token_gate_test.exs` green (no `.ex` hex introduced).
</verification>

<success_criteria>
- `cl_page/1` renders title + `:wide`/`:reading` framing classes + breadcrumb/actions/subnav slots (UIC-01).
- `cl_stat` accepts only `count :integer`; `cl_hero/1` renders a copper count + `:detail` slot + primary CTA (UIC-02).
- Build warnings-clean; all render tests token-pure.
</success_criteria>

<output>
Create `.planning/phases/37-component-primitives/37-02-SUMMARY.md` when done.
</output>
