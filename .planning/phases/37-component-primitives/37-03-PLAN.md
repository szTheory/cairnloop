---
phase: 37-component-primitives
plan: 03
type: execute
wave: 2
depends_on: ["37-02"]
files_modified:
  - lib/cairnloop/web/components.ex
  - test/cairnloop/web/components_test.exs
autonomous: true
requirements: [UIC-03, UIC-04]
must_haves:
  truths:
    - "cl_disclosure wraps native <details> with phx-update=ignore + a required stable id and a static-at-mount open attribute, so a LiveView PubSub re-render cannot snap the panel shut (no server assign owns open state)"
    - "cl_fact_list renders a <dl> of label/value pairs (and an optional inner_block for custom rows) with dedicated .cl-fact-list styling"
  artifacts:
    - path: "lib/cairnloop/web/components.ex"
      provides: "cl_disclosure/1 + cl_fact_list/1 components"
      contains: "def cl_disclosure"
    - path: "test/cairnloop/web/components_test.exs"
      provides: "Patch-safety structural-marker test + fact-list render test"
      exports: []
  key_links:
    - from: "lib/cairnloop/web/components.ex (cl_disclosure)"
      to: "browser-owned <details> open state"
      via: "phx-update=ignore + required stable id"
      pattern: "phx-update=\"ignore\""
---

<objective>
Add the two container/list primitives to `Cairnloop.Web.Components`: `cl_disclosure/1`
(UIC-03 / D-03 — the patch-safe native `<details>` wrapper whose open state is browser-owned and
NEVER bound to a server assign), and `cl_fact_list/1` (UIC-04 / D-05 — a `<dl>` of label/value
pairs with a dedicated `.cl-fact-list` style and an optional custom-row slot). This plan depends on
Plan 02 only because it shares `components.ex` + `components_test.exs` (sequential file ownership),
not for any API dependency.

Purpose: `cl_disclosure` is the load-bearing primitive for P41's rail progressive disclosure;
`cl_fact_list` is the highest-reuse label/value primitive for context panels. Built + unit-tested
here; adopted later.
Output: `cl_disclosure/1`, `cl_fact_list/1` + their headless render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-component-primitives/37-UI-SPEC.md
@.planning/phases/37-component-primitives/37-PATTERNS.md
@.planning/phases/37-component-primitives/37-RESEARCH.md

<interfaces>
<!-- Existing analogs the executor builds against. -->
From lib/cairnloop/web/components.ex:
- `cl_card/1` (line 56) — the `slot(:summary, required: true)` + `:if={@slot != []}` slot idiom.
- `cl_shell/1` (line 171) — the `:for` comprehension idiom for iterating list assigns.
From priv/static/cairnloop.css (added by Plan 01, but `.cl-details` is PRE-EXISTING):
- `.cl-details` + `.cl-details > summary` + `.cl-details dl/dt/dd` (cairnloop.css:477-485) — cl_disclosure REUSES this verbatim; emit NO new disclosure CSS.
- `.cl-fact-list*` classes — added by Plan 01; cl_fact_list emits these class names.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cl_disclosure/1 patch-safe native disclosure (UIC-03 / D-03)</name>
  <files>lib/cairnloop/web/components.ex, test/cairnloop/web/components_test.exs</files>
  <read_first>
    - lib/cairnloop/web/components.ex — `cl_card/1` (line 56-62) for the required-slot + inner_block idiom; the moduledoc token-only rule (lines 16-24).
    - priv/static/cairnloop.css — `.cl-details` rules (cairnloop.css:477-485) which cl_disclosure reuses; emit NO new disclosure CSS.
    - .planning/phases/37-component-primitives/37-PATTERNS.md — "cl_disclosure/1" section (THE critical pattern: the exact `<details ... phx-update="ignore" open={@open}>` skeleton) and the forward-compat guardrail for P41.
    - .planning/phases/37-component-primitives/37-RESEARCH.md — Pattern 3 + Pitfall 1 + Pitfall 2 (phx-update="ignore" survives diffing; the ignored subtree freezes child content — record this guardrail in the SUMMARY for P41).
    - .planning/phases/37-component-primitives/37-UI-SPEC.md — §4 cl_disclosure (structure, attributes, patch-safety mechanism).
  </read_first>
  <behavior>
    - Test 1: `cl_disclosure id="inputs-scope" open={true}` with a `:summary` slot renders `<details`, the literal `phx-update="ignore"`, `id="inputs-scope"`, the static `open` attribute, the `cl-details cl-disclosure` classes, and the summary text.
    - Test 2: `cl_disclosure id="x"` with default `open` (false) renders `<details` WITHOUT the `open` attribute (HEEx boolean-attr semantics) but still carries `phx-update="ignore"` and the stable id.
    - Test 3: rendered HTML contains no `#hex`.
  </behavior>
  <action>
    Add `cl_disclosure/1` to `components.ex` exactly per the PATTERNS skeleton (D-03, locked): attrs `id :string, required: true` (MANDATORY — `phx-update="ignore"` requires a unique DOM id), `open :boolean, default: false` (rendered ONLY as the static HTML `open` attribute at initial mount — NEVER an assign-driven toggle, NO `phx-click`); slots `:summary, required: true` and `:inner_block, required: true`. Emit `<details class="cl-details cl-disclosure" id={@id} phx-update="ignore" open={@open}>` containing `<summary class="cl-details__summary">{render_slot(@summary)}</summary>` then `{render_slot(@inner_block)}`. HEEx renders `open={@open}` as a boolean attribute (present when true, absent when false) — exactly correct for `<details open>`. Do NOT bind `open` to any server assign, add `phx-click`, or use `Phoenix.LiveView.JS` (reserved for P41 rail-level controls). Reuse the existing `.cl-details` CSS — add no new disclosure CSS. In the SUMMARY, record the forward-compat guardrail (RESEARCH Pitfall 2): the ignored subtree freezes child content from server diffs, so P41 adopters must keep live-updating content OUTSIDE the `<details>`. Add the three tests above.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def cl_disclosure(' lib/cairnloop/web/components.ex` matches.
    - The emitted markup contains the literal `phx-update="ignore"` and `class="cl-details cl-disclosure"`; the `open` attr is `open={@open}` (static, assign-free toggle).
    - `grep -F 'phx-click' lib/cairnloop/web/components.ex` does NOT match inside `cl_disclosure` (no JS/assign toggle).
    - A test asserts `html =~ "<details"`, `html =~ ~s(phx-update="ignore")`, `html =~ ~s(id="inputs-scope")`, and (for open=true) `html =~ "open"`; another asserts open=false omits the `open` attribute.
    - Every cl_disclosure test asserts `refute html =~ ~r/#[0-9a-fA-F]{3,6}/`.
    - `mix compile --warnings-as-errors` and `mix test test/cairnloop/web/components_test.exs` both exit 0.
  </acceptance_criteria>
  <done>`cl_disclosure/1` wraps native `<details>` with `phx-update="ignore"` + required stable id + static-at-mount `open`, no server assign owns open state; the forward-compat freeze guardrail is recorded for P41; tests confirm the structural markers and token-purity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cl_fact_list/1 label/value list (UIC-04 / D-05)</name>
  <files>lib/cairnloop/web/components.ex, test/cairnloop/web/components_test.exs</files>
  <read_first>
    - lib/cairnloop/web/components.ex — the `:for` comprehension idiom (cl_shell line 171) for iterating the `facts` list; `cl_card/1` (line 59) for the optional inner_block guard.
    - .planning/phases/37-component-primitives/37-PATTERNS.md — "cl_fact_list/1" section (the `<dl class="cl-fact-list">` + `:for={fact <- @facts}` row markup; dedicated `.cl-fact-list` CSS, NOT `.cl-details dl` reuse).
    - .planning/phases/37-component-primitives/37-UI-SPEC.md — §5 cl_fact_list (structure, attributes, dedicated-class rationale, CSS classes introduced).
  </read_first>
  <behavior>
    - Test 1: `cl_fact_list facts={[%{label: "Plan", value: "Pro"}, %{label: "Risk tier", value: "Low"}]}` renders a `<dl class="cl-fact-list">` containing `<dt class="cl-fact-list__label">Plan</dt>` / `<dd class="cl-fact-list__value">Pro</dd>` pairs for each fact.
    - Test 2: `cl_fact_list` with an `inner_block` (custom rows) renders that block inside the same `<dl>`.
    - Test 3: rendered HTML contains no `#hex`.
  </behavior>
  <action>
    Add `cl_fact_list/1` to `components.ex` per D-05 / PATTERNS: attr `facts :list, default: []` (a list of `%{label: string, value: string}` maps) and an optional `slot(:inner_block)`. Emit `<dl class="cl-fact-list">` with `<div :for={fact <- @facts} class="cl-fact-list__row"><dt class="cl-fact-list__label">{fact.label}</dt><dd class="cl-fact-list__value">{fact.value}</dd></div>` followed by `{render_slot(@inner_block)}` for custom rows. Render `fact.label`/`fact.value` via auto-escaped `{...}` interpolation (never `raw/1`). Emit the dedicated `.cl-fact-list*` class names only (the CSS is added by Plan 01); no inline style/hex. Add the three tests above.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def cl_fact_list(' lib/cairnloop/web/components.ex` matches.
    - The component emits `<dl class="cl-fact-list">` with `.cl-fact-list__label` / `.cl-fact-list__value` per fact, and renders an optional `inner_block`.
    - A test asserts both the dt label and dd value text from a 2-element `facts` list render, plus an inner_block custom row.
    - Every cl_fact_list test asserts `refute html =~ ~r/#[0-9a-fA-F]{3,6}/`.
    - `mix compile --warnings-as-errors` and `mix test test/cairnloop/web/components_test.exs` both exit 0.
  </acceptance_criteria>
  <done>`cl_fact_list/1` renders a dedicated-class `<dl>` of label/value pairs plus an optional custom-row slot; tests confirm structure and token-purity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller assigns → rendered HTML | Caller (P41+) supplies summary/fact text and disclosure `id`/`open`. Output-encoding is the only control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Tampering / Elevation (XSS) | `cl_disclosure` summary, `cl_fact_list` label/value text | mitigate | All caller text rendered via HEEx auto-escaped `{...}`; NO caller input reaches `Phoenix.HTML.raw/1` (V5 output-encoding). |
| T-37-06 | Repudiation (stale UI) | `cl_disclosure` `phx-update="ignore"` subtree | accept | The ignored subtree freezes child content from server diffs by design (D-03 invariant). Not a P37 defect — recorded as a forward-compat guardrail so P41 adopters keep live-updating content outside the `<details>`. |
| T-37-07 | Tampering (style drift) | emitted markup | mitigate | `.cl-*` classes only, no inline style/hex; `brand_token_gate_test.exs` (D-13) + `refute html =~ ~r/#hex/` enforce token-purity. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: not applicable). |

No HIGH-severity threats.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/cairnloop/web/components_test.exs` green (existing + new).
- `mix test test/cairnloop/web/brand_token_gate_test.exs` green.
- The `cl_disclosure` open state is structurally proven browser-owned via `phx-update="ignore"` + stable id markers (a behavioral PubSub-survival test is P41 territory).
</verification>

<success_criteria>
- `cl_disclosure/1` emits `<details phx-update="ignore" id=... open?>` with no server-assigns open state (UIC-03).
- `cl_fact_list/1` renders a dedicated `<dl>` of facts + optional custom rows, token-pure (UIC-04).
- Build warnings-clean; all render tests token-pure.
</success_criteria>

<output>
Create `.planning/phases/37-component-primitives/37-03-SUMMARY.md` when done. Record the
`phx-update="ignore"` child-content-freeze forward-compat guardrail for P41 adopters.
</output>
