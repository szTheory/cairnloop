---
phase: 33-security-domain-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/knowledge_automation.ex
  - test/cairnloop/knowledge_automation_test.exs
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "System creates a new draft instead of reusing a published article ID for authoring targets."
    - "Gap candidate suggestion grounding completely ignores caller-supplied evidence bypasses."
    - "Stale revision checks rely exclusively on database events and fresh canonical grounding."
  artifacts:
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "Strict domain-layer constraints for suggestions and revisions"
  key_links: []
---

<objective>
Enforce immutable evidence and state for knowledge automation by closing three domain-layer security threats (T-10-10, T-10-12, T-10-13).

Purpose: Guarantee that external callers cannot spoof authoring targets, gap-candidate grounding, or stale-gate revision inputs, maintaining the strict trust boundaries of the domain layer.
Output: Hardened domain-layer functions and comprehensive test coverage proving bypasses are rejected.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SECURITY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEC-01 (T-10-10) Enforce Non-Published Authoring Targets</name>
  <files>lib/cairnloop/knowledge_automation.ex, test/cairnloop/knowledge_automation_test.exs</files>
  <behavior>
    - Test 1: Given a suggestion with a published authoring_article_id, it rejects reuse and safely creates a new draft article.
    - Test 2: Given a suggestion with a valid draft authoring_article_id, it reuses the target.
  </behavior>
  <action>
    Review `create_or_reuse_authoring_article_for_suggestion/2`. Ensure `reusable_authoring_article?` strictly rejects `[:published, "published"]` statuses and falls through to creating a safe `:draft` article instead of crashing or overwriting. Write explicit tests proving that a published target ID in `grounding_metadata` is gracefully rejected to close the authoring-target seam threat.
  </action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation_test.exs</automated>
  </verify>
  <done>A published authoring target ID results in a new draft article being safely created instead of an unsafe overwrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SEC-02 (T-10-12) Remove Gap Candidate Grounding Bypasses</name>
  <files>lib/cairnloop/knowledge_automation.ex, test/cairnloop/knowledge_automation_test.exs</files>
  <behavior>
    - Test 1: `suggest_article/2` with a `gap_candidate_id` strips any caller-supplied `evidence`, `grounding_metadata`, or `grounding_bundle` from attrs/opts.
    - Test 2: Gap candidate suggestions exclusively build their bundle from `gap_candidate_grounding_bundle/3`, disregarding caller injection.
  </behavior>
  <action>
    Update `suggest_article/2` and `hydrate_gap_candidate_request/2` to eliminate gap-candidate grounding bypasses. Strip caller-provided evidence fields before falling back to `fallback_attrs_grounding_bundle`, or ensure candidate hydration unconditionally overwrites caller opts. Add tests asserting that caller-supplied evidence or grounding opts are completely ignored on the gap candidate path.
  </action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation_test.exs</automated>
  </verify>
  <done>Gap candidate suggestions derive their grounding exclusively from the hydrated candidate, disregarding caller evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SEC-03 (T-10-13) Enforce Stale Gate Inputs</name>
  <files>lib/cairnloop/knowledge_automation.ex, test/cairnloop/knowledge_automation_test.exs</files>
  <behavior>
    - Test 1: `suggest_revision/2` completely ignores `gap_events` and `grounding_bundle` injected via opts by a caller.
    - Test 2: Stale gate inputs strictly derive from `article_linked_gap_events/3` and `fresh_revision_grounding_bundle/4`.
  </behavior>
  <action>
    Update `suggest_revision/2` and `build_revision_gate_inputs/3` to categorically ignore spoofed `gap_events` or `grounding_bundle` that might be passed in `opts`. Enforce that `stale_article_signal_module(opts).build_revision_gate` receives exclusively internally-generated inputs. Write tests proving injected opts are ignored for revision gates.
  </action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation_test.exs</automated>
  </verify>
  <done>Stale gate logic relies solely on database-backed events and domain-hydrated canonical grounding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain Interface | External callers (web, API, workers) interacting with KnowledgeAutomation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | `knowledge_automation.ex` | mitigate | (SEC-01) Enforce reuse of only non-published authoring targets, creating new drafts otherwise. |
| T-10-12 | Tampering | `knowledge_automation.ex` | mitigate | (SEC-02) Remove caller bypasses for gap-candidate grounding, strictly hydrating from candidate evidence. |
| T-10-13 | Spoofing | `knowledge_automation.ex` | mitigate | (SEC-03) Ignore caller-injected `gap_events` or `grounding_bundle`; rely on fresh canonical grounding and repo-backed events. |
</threat_model>

<verification>
- `mix test test/cairnloop/knowledge_automation_test.exs` passes with all bypass and spoofing attempts rejected.
- All three `SECURITY.md` threats (T-10-10, T-10-12, T-10-13) are definitively closed by code and proven by tests.
</verification>

<success_criteria>
- System rejects creating new suggestions targeting already published articles.
- Gap-candidate suggestions strictly derive from immutable system evidence, ignoring external caller payloads.
- Stale revision checks rely solely on database events and canonical grounding, rejecting spoofed inputs.
</success_criteria>

<output>
Create `.planning/phases/33-security-domain-closure/33-01-SUMMARY.md` when done
</output>