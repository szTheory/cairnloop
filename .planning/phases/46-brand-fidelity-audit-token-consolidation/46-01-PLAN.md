---
phase: 46-brand-fidelity-audit-token-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md
  - .planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md
autonomous: true
requirements: [FIDELITY-01, FIDELITY-02, FIDELITY-03]

must_haves:
  truths:
    - "A discrepancy ledger records every canonical --cl-* token (15 primitives + ~20 semantic + 6 status triads + scale groups, both light and dark) with a per-derivative cell vs tokens.json, app.css @theme, and brand-book prose (SC1, FIDELITY-01, D-01)"
    - "The ledger states priv/static/cairnloop.css :root is the single canonical token source and gives each of the three derivatives a provenance note, citing the existing app.css lines 4-7 'keep in sync' comment (SC2, FIDELITY-02, D-02/D-03)"
    - "A contrast baseline table scores every enumerated fg/bg pairing (RESEARCH rows 1-29 + brand-book §7.5 pairings) in BOTH light and dark themes with hex, ratio, threshold, and AA/AA-large/FAIL verdict (SC3, FIDELITY-03, D-05/D-06)"
    - "The copper route-marker --cl-path-copper #A94F30 is scored at both 3.0 (UI/large) and 4.5 (text) with its role annotated in the verdict; the text-muted #677066 -> bg 4.52:1 near-miss is flagged (D-06, RESEARCH headline items)"
    - "Every failure or near-miss carries a remediation note routed to Phase 47/48 as explicit input (D-08)"
    - "The 46-CONTRAST-BASELINE.md table is self-contained Markdown reusable verbatim by Phase 51 and re-checkable by Phase 48 SC4 (D-04)"
  artifacts:
    - path: ".planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md"
      provides: "Per-token canonical-vs-derivative drift ledger + canonical-source designation with provenance notes"
      contains: "canonical"
      min_lines: 60
    - path: ".planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md"
      provides: "Self-contained WCAG-AA contrast table, both themes, every shipped pairing, Phase-51-liftable"
      contains: "Verdict"
      min_lines: 60
  key_links:
    - from: ".planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md"
      to: "priv/static/cairnloop.css"
      via: "every canonical :root and [data-theme=dark] --cl-* token transcribed as a ledger row (read-only, no edit)"
      pattern: "--cl-"
    - from: ".planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md"
      to: "priv/static/cairnloop.css"
      via: "fg/bg pairings read off component rules lines 216-479, scored per theme"
      pattern: "FG token"
  prohibitions:
    - "No --cl-* value changed in any file (D-01)"
    - "No derivative file edited: priv/static/cairnloop.css, prompts/cairnloop.tokens.json, examples/cairnloop_example/assets/css/app.css, prompts/cairnloop_brand_book.md all unchanged (D-01)"
    - "No throwaway luminance script committed to the repo tree (D-07)"
    - "No mix.exs change, no new mix/npm/pip dependency, no new runtime code, no drift-guard test, no brand-token gate change"
---

<objective>
Produce the three Phase 46 brand-fidelity audit deliverables — all Markdown, all read-only over the brand sources — that pressure-test the shipped brand system and establish a single canonical token source so Phase 48 can evolve tokens once and right.

Purpose: Close FIDELITY-01/02/03. The discrepancy ledger becomes Phase 48's drift worklist; the canonical-source designation makes derivative provenance explicit; the contrast baseline table is lifted verbatim into the Phase 51 brand book and re-verified against the evolved palette in Phase 48 SC4.

Output:
- `46-DISCREPANCY-LEDGER.md` (FIDELITY-01 + FIDELITY-02) — per-canonical-token drift rows across all three derivatives, plus the canonical-source designation and per-derivative provenance notes.
- `46-CONTRAST-BASELINE.md` (FIDELITY-03) — a self-contained WCAG-AA table scoring every shipped fg/bg pairing in both light and dark themes.
- A throwaway, stdlib-only relative-luminance script: run once in the scratchpad, results pasted into the contrast table, then DELETED. It is NOT a produced artifact (D-07).

This is PURE ANALYSIS / DOCUMENTATION. No `--cl-*` value is changed, no derivative file is edited, no script is committed, no `mix.exs` change, no drift-guard test, no brand-token-gate change (D-01, D-07).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTEXT.md
@.planning/phases/46-brand-fidelity-audit-token-consolidation/46-RESEARCH.md
@.planning/phases/46-brand-fidelity-audit-token-consolidation/46-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This phase creates exactly the following files (both committed to git):

1. `.planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md` — FIDELITY-01 (drift ledger) + FIDELITY-02 (canonical-source designation + provenance notes). Per planner discretion, the canonical-source designation lives as a top section of THIS ledger file (not a separate file) — chosen because the designation and the per-derivative drift rows are read together and Phase 48 consumes them as one worklist.
2. `.planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md` — FIDELITY-03 (WCAG-AA contrast table).

NOT a produced artifact (explicitly transient): the throwaway relative-luminance script. It is written to the session scratchpad, run once, its output pasted into the contrast table, and DELETED before commit (D-07). `git status` must show no script in the repo tree.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write throwaway luminance script + discrepancy ledger with canonical-source designation</name>
  <files>.planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md</files>
  <read_first>
    - priv/static/cairnloop.css :root light block (lines ~18-157), [data-theme="dark"] block (lines ~160-194), and the non-color scale blocks (typography/spacing/radius/shadow/z/motion/control/layout, lines ~70-156) — the CANONICAL source (D-02)
    - prompts/cairnloop.tokens.json (full, 127 lines) — color.primitive + semantic_light + semantic_dark + typography + voice derivative
    - examples/cairnloop_example/assets/css/app.css lines 1-199 — the @theme block, @layer base :root/dark partial mirror, the existing "keep both in sync with priv/static/cairnloop.css" provenance comment (lines 4-7), and the daisyUI @plugin noise to exclude
    - prompts/cairnloop_brand_book.md §7 color system (lines ~435-532): §7.2 primitive table, §7.4 semantic table, §7.5 accessibility rules
    - 46-RESEARCH.md "Token Inventory", "Derivative Cross-Reference", and "WCAG Ratio Method" + "Code Examples" sections — the raw material is fully enumerated there; this task is transcription + verdicts, not discovery
  </read_first>
  <action>
    First, author the throwaway relative-luminance script in the SESSION SCRATCHPAD ONLY (never under the repo tree) using the WCAG 2.x sRGB algorithm from RESEARCH "Code Examples" (Python 3 stdlib, ~25 lines, zero deps per D-07). This script's output feeds Task 2; create it now so both tasks share it. Do NOT add it to git, mix.exs, package.json, or any lockfile.

    Then create `46-DISCREPANCY-LEDGER.md` with two parts:

    PART A — Canonical-source designation (FIDELITY-02, per D-02/D-03): a short section stating priv/static/cairnloop.css :root is the single canonical token source (it ships, carries the live ~470 --cl-* references, is what the operator UI consumes). Document each of the three derivatives as an expression-of-canonical with a provenance note: (1) prompts/cairnloop.tokens.json (color+type+voice derivative, no explicit provenance marker), (2) examples/cairnloop_example/assets/css/app.css @theme — cite its existing lines 4-7 "keep both in sync with priv/static/cairnloop.css" comment as the existing informal provenance marker, and note it also @imports canonical at line 7 so canonical wins at runtime, (3) prompts/cairnloop_brand_book.md §7 prose (the seed). State explicitly that NOTHING is deleted or restructured in this phase per D-03.

    PART B — Discrepancy ledger (FIDELITY-01, per D-01): group rows by layer — primitives, then semantic-light, then semantic-dark, then status triads, then non-color scales. For each canonical token, one row of the form `Token | Canonical value | tokens.json | app.css @theme | brand-book prose | Drift?`. Use "—" when a derivative does not express the token, and label that as a COVERAGE gap (not a value drift). Capture the two RESEARCH-confirmed app.css drifts completely: (1) the VALUE drift on `--cl-shadow-raised` (app.css `0 1px 2px rgba(24,33,31,0.08), 0 8px 24px rgba(24,33,31,0.06)` vs canonical `--cl-shadow-1` `0 1px 2px rgba(24,33,31,0.06)`), and (2) the COVERAGE gaps (app.css omits surface-sunken, text-soft, border-strong, primary-hover, overlay, the status triads, and all spacing/z/motion/control/layout scales + most radius/font scales). Record that tokens.json is CLEAN on its 15 primitives + the 14 semantic keys it carries (note its coverage scope; do not flag absent spacing/z tokens as value drift). Spot-confirm brand-book §7.2/§7.4 hex match canonical primitive-for-primitive and semantic-for-semantic; record any hex that has drifted. Note the legacy aliases (`--cl-warning-bg`->warning-surface, `--cl-danger-soft`->danger-surface) as aliases, not drift. Flag the dark-theme `--cl-warning == --cl-primary` (#D98A4A) as an open question for Phase 47 (RESEARCH A3) — do not assume bug vs intentional. Exclude the example-app daisyUI `@plugin` oklch blocks as out-of-scope (not --cl-* brand tokens). EDIT NOTHING in any source file — record drift only (D-01). The drift list is Phase 48's worklist; state that explicitly.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md && grep -qi 'canonical' .planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md && grep -q 'shadow-raised' .planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md && grep -q 'keep both in sync' .planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md && [ "$(git status --porcelain priv/static/cairnloop.css prompts/cairnloop.tokens.json examples/cairnloop_example/assets/css/app.css prompts/cairnloop_brand_book.md | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    `46-DISCREPANCY-LEDGER.md` exists with (a) a canonical-source designation naming priv/static/cairnloop.css :root canonical and giving each of the 3 derivatives a provenance note (citing the app.css lines 4-7 comment), and (b) a per-token drift ledger covering every canonical --cl-* token across both themes with per-derivative cells, including the two confirmed app.css drifts (shadow-raised value + coverage gaps) and tokens.json clean status. No brand source file is modified (git status clean for all four). The throwaway script exists in scratchpad only, not in the repo tree.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the WCAG-AA contrast baseline table (both themes, every pairing)</name>
  <files>.planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md</files>
  <read_first>
    - 46-RESEARCH.md "Operator-UI fg/bg Pairing Enumeration" (rows 1-29, with FG/BG tokens, thresholds, and source lines), the "Brand-book accessible-pairings (§7.5)" note, and the "Precomputed sample ratios" block (14 anchor values to validate the script output against)
    - priv/static/cairnloop.css component rules (lines ~216-479) for the shipped pairings and both theme token-value blocks (light :root + [data-theme="dark"]) to resolve every fg/bg hex per theme
    - prompts/cairnloop_brand_book.md §7.5 accessible-pairings (lines ~500-521) — the additional brand-book pairings to fold in (Moss Ink on Trailpaper, Trailpaper on Basalt, Waypoint Blue on Trailpaper) so the table is a Phase-51 superset
    - The throwaway luminance script created in Task 1 (in scratchpad)
  </read_first>
  <action>
    Resolve every enumerated pairing (RESEARCH rows 1-29 PLUS the brand-book §7.5 pairings) to concrete fg/bg hex values for BOTH light and dark themes (D-05), run them through the throwaway script, and write `46-CONTRAST-BASELINE.md` as a self-contained Markdown table designed for verbatim reuse in Phase 51 (D-04). Use the column set from CONTEXT specifics: `Pairing | FG token (hex) | BG token (hex) | Theme | Ratio | Threshold | Verdict`. Apply D-06 thresholds: 4.5 normal text, 3.0 large text (>=24px regular or >=18.66px bold), 3.0 non-text UI components (borders, focus rings, the route marker as a semantic indicator). Verdict values: AA / AA-large / FAIL.

    Score the copper route-marker `--cl-path-copper` (`#A94F30` light, `#D98A4A` dark) at BOTH 3.0 (as UI/large marker) and 4.5 (as if behind text), as separate rows, annotating the role in the Verdict cell so Phase 51 reuse is unambiguous. Explicitly flag the `--cl-text-muted #677066 -> --cl-bg #F5F0E6` 4.52:1 near-miss as a fragile pass (any Phase 47 palette shift risks pushing it below 4.5). Before finalizing, run the open-edge completeness check (RESEARCH A1 / Open Question 1): `grep -rn 'text-cl-\|bg-cl-' examples/cairnloop_example/lib` to find any Tailwind-utility fg/bg pairing in the example-app HEEx not already present in the lib CSS component rules; fold any new pairing into the table, or record "none found — A1 closes clean" if the grep is empty.

    For every FAIL or near-miss, add a remediation note and route it as explicit input to Phase 47 palette exploration / Phase 48 SC4 re-verify (D-08). Do NOT adjust the palette to fix anything (D-01/D-08). Cross-check the script output against the 14 precomputed sample ratios in RESEARCH; they must match (regression anchor for Phase 48 SC4). Keep raw hex confined to this .md artifact (documentation, not render code — the brand-token gate is not implicated). Use plain honest verdict language — no euphemism for failures.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md && grep -q 'Verdict' .planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md && grep -qi 'light' .planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md && grep -qi 'dark' .planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md && grep -q '4.52' .planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md && grep -q 'A94F30' .planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md && [ "$(grep -c '|' .planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md)" -ge 50 ]</automated>
  </verify>
  <done>
    `46-CONTRAST-BASELINE.md` exists as a self-contained table with the 7 specified columns, scoring every enumerated pairing (RESEARCH rows 1-29 + brand-book §7.5 pairings) in BOTH light and dark themes. The copper route-marker is scored at both 3.0 and 4.5 with role annotated; the text-muted 4.52:1 near-miss is flagged; failures/near-misses carry remediation notes routed to Phase 47/48. Script ratios match the 14 RESEARCH anchor values. The A1 grep over example-app HEEx was run and its result recorded.
  </done>
</task>

<task type="auto">
  <name>Task 3: Hygiene gate — delete throwaway script, confirm zero library mutation</name>
  <files>.planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md</files>
  <read_first>
    - 46-CONTEXT.md D-01 and D-07 (the prohibitions this gate enforces)
    - The session scratchpad path holding the throwaway script
  </read_first>
  <action>
    Delete the throwaway luminance script from the scratchpad (D-07 — it must not survive anywhere as a committed or tracked artifact). Then confirm the phase touched nothing it must not: no `--cl-*` value changed, no derivative file edited, no script committed, no `mix.exs` change. Run `git status --porcelain` and confirm the ONLY changed/added paths are the two phase Markdown artifacts (`46-DISCREPANCY-LEDGER.md`, `46-CONTRAST-BASELINE.md`) plus expected planning-doc bookkeeping (ROADMAP/STATE/manifest if the orchestrator updates them) — and specifically that none of `priv/static/cairnloop.css`, `prompts/cairnloop.tokens.json`, `examples/cairnloop_example/assets/css/app.css`, `prompts/cairnloop_brand_book.md`, or `mix.exs` appear as modified, and no `.py`/`.js` script appears anywhere in the tree. Record the hygiene result (the verbatim relevant `git status` lines) in a short "Phase hygiene attestation" section appended to `46-DISCREPANCY-LEDGER.md`. This is a documentation phase: do NOT run `mix test` (no library code changed); a `mix compile --warnings-as-errors` sanity run is optional and only to confirm the library is untouched.
  </action>
  <verify>
    <automated>! git ls-files --error-unmatch '*luminance*' 2>/dev/null && [ "$(git status --porcelain priv/static/cairnloop.css prompts/cairnloop.tokens.json examples/cairnloop_example/assets/css/app.css prompts/cairnloop_brand_book.md mix.exs | wc -l | tr -d ' ')" = "0" ] && [ -z "$(git ls-files '*.py' '*.js' :!:examples 2>/dev/null | grep -i contrast)" ] && grep -qi 'hygiene' .planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md</automated>
  </verify>
  <done>
    The throwaway script no longer exists in the scratchpad or anywhere in the repo tree. `git status --porcelain` confirms none of the four brand source files nor `mix.exs` are modified, and no contrast/luminance script is tracked. A "Phase hygiene attestation" section in `46-DISCREPANCY-LEDGER.md` records the clean git status. D-01 and D-07 are satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase introduces no runtime, network, input-handling, auth, or persistence surface. Deliverables are local Markdown audit artifacts; the only tooling is a throwaway, uncommitted, stdlib-only script that is deleted before commit (D-07). |

## STRIDE Threat Register

ASVS level: 1. Block on: high-severity threats. Honest disposition: no high- or medium-severity threats are introduced by this phase. The only realistic risk is a repo-hygiene mistake, covered by an explicit acceptance check (Task 3).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | Accidental edit/mutation of a brand source file (`cairnloop.css`, `tokens.json`, `app.css`, brand book) during a "document-only" phase | mitigate | Task 1 and Task 3 verify `git status --porcelain` is clean for all four brand sources; D-01 forbids any value change. |
| T-46-02 | Tampering | Accidental commit of the throwaway luminance script (new untracked tool entering the repo / hex package) | mitigate | Task 3 deletes the script from scratchpad and asserts no contrast/luminance script is tracked and `mix.exs` is unchanged (D-07 repo hygiene). |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; RESEARCH "Package Legitimacy Audit" confirms zero registry dependencies. Python 3 / Node are pre-existing local runtimes; the script is stdlib-only and uncommitted. No supply-chain surface. |
</threat_model>

<verification>
Phase-level artifact-completeness gates (no ExUnit — this is a documentation phase, per 46-VALIDATION.md):

- FIDELITY-01: every canonical `--cl-*` token (15 primitives + ~20 semantic + 6 status triads + scale groups, both themes) appears as a ledger row with per-derivative cells; the two known app.css drifts (shadow-raised value; coverage gaps) are present; tokens.json clean status recorded.
- FIDELITY-02: ledger states `priv/static/cairnloop.css :root` is canonical and gives each derivative a provenance note, citing the app.css lines 4-7 "keep in sync" comment.
- FIDELITY-03: every enumerated pairing (rows 1-29 + brand-book §7.5) is scored in both light and dark themes with hex/ratio/threshold/verdict; copper route-marker scored at both 3.0 and 4.5; text-muted 4.52:1 near-miss flagged; failures carry remediation notes; script ratios match the 14 RESEARCH anchors.
- Hygiene (D-01/D-07): `git status --porcelain` shows no modification to the four brand source files nor `mix.exs`; no luminance/contrast script tracked anywhere.
- Forward-reuse: `46-CONTRAST-BASELINE.md` reads as a self-contained table in isolation (Phase 51 liftable, Phase 48 SC4 re-checkable).
</verification>

<success_criteria>
- [ ] `46-DISCREPANCY-LEDGER.md` exists with canonical-source designation + per-token drift ledger (SC1, SC2)
- [ ] `46-CONTRAST-BASELINE.md` exists scoring every pairing in both themes with verdicts + remediation notes (SC3)
- [ ] Two confirmed app.css drifts captured; tokens.json clean status recorded; brand-book hex spot-confirmed
- [ ] Copper route-marker scored at both 3.0 and 4.5 (role annotated); text-muted 4.52:1 near-miss flagged
- [ ] A1 grep over example-app HEEx run and result recorded
- [ ] Throwaway script deleted; no brand source file nor mix.exs modified (D-01/D-07); hygiene attestation recorded
</success_criteria>

<output>
Create `.planning/phases/46-brand-fidelity-audit-token-consolidation/46-01-SUMMARY.md` when done.
</output>
