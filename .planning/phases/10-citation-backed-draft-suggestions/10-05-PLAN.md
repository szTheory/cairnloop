---
phase: 10
plan: "05"
type: execute
wave: 5
depends_on:
  - 10-04
files_modified:
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_automation/stale_article_signal.ex
  - lib/cairnloop/web/knowledge_base_live/index.ex
  - test/cairnloop/knowledge_automation/article_suggestion_test.exs
  - test/cairnloop/web/knowledge_base_live/gaps_test.exs
autonomous: true
gap_closure: true
requirements:
  - DRAFT-01
  - DRAFT-02
  - DRAFT-03
must_haves:
  truths:
    - "A gap-dashboard article suggestion is grounded from the selected candidate's durable retrieval and manual-handling evidence, not from a generic fallback query."
    - "A stale-article revision suggestion loads article-linked stale evidence and a fresh canonical grounding bundle inside the domain before the stale gate runs."
    - "The shipped `Gaps` and `Index` entrypoints prove the UI-to-domain-to-suggestion-review path and still fail closed when canonical grounding is missing."
  artifacts:
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "Gap-candidate evidence hydration, revision gate input loading, and removal of generic entrypoint fallback behavior"
    - path: "lib/cairnloop/knowledge_automation/stale_article_signal.ex"
      provides: "Strict revision gate over durable article-linked events plus fresh canonical snapshot"
    - path: "lib/cairnloop/web/knowledge_base_live/index.ex"
      provides: "Thin stale-revision entrypoint that passes article identity and scope while leaving evidence loading inside the domain"
    - path: "test/cairnloop/knowledge_automation/article_suggestion_test.exs"
      provides: "Domain proof for candidate-specific grounding and repo-backed stale-revision preparation"
    - path: "test/cairnloop/web/knowledge_base_live/gaps_test.exs"
      provides: "Entry-point proof for the shipped `Gaps` and `Index` flows"
  key_links:
    - from: "lib/cairnloop/web/knowledge_base_live/gaps.ex"
      to: "lib/cairnloop/knowledge_automation.ex"
      via: "selected candidate id plus scope trigger domain-side candidate hydration"
      pattern: "handle_event\\(\"suggest_article\"|get_gap_candidate!"
    - from: "lib/cairnloop/web/knowledge_base_live/index.ex"
      to: "lib/cairnloop/knowledge_automation.ex"
      via: "article id plus scope trigger internal stale-evidence loading and fresh grounding"
      pattern: "handle_event\\(\"suggest_revision\"|suggest_revision"
    - from: "lib/cairnloop/knowledge_automation.ex"
      to: "lib/cairnloop/knowledge_automation/stale_article_signal.ex"
      via: "repo-backed gap events and fresh canonical grounding bundle feed `build_revision_gate/3`"
      pattern: "build_revision_gate|gap_events|grounding_bundle"
---

<objective>
Close the two Phase 10 verification blockers at the real shipped entrypoints without reopening already-verified review and editor work.

Purpose: restore the phase goal by making both suggestion entrypoints load their own durable evidence before queueing generation.
Output: one additive gap-closure implementation plan that repairs gap-candidate grounding, stale-revision grounding, and the UI-to-domain proof that failed verification.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-citation-backed-draft-suggestions/10-CONTEXT.md
@.planning/phases/10-citation-backed-draft-suggestions/10-RESEARCH.md
@.planning/phases/10-citation-backed-draft-suggestions/10-PATTERNS.md
@.planning/phases/10-citation-backed-draft-suggestions/10-VERIFICATION.md
@.planning/phases/10-citation-backed-draft-suggestions/10-02-SUMMARY.md
@.planning/phases/10-citation-backed-draft-suggestions/10-04-SUMMARY.md
@lib/cairnloop/knowledge_automation.ex
@lib/cairnloop/knowledge_automation/stale_article_signal.ex
@lib/cairnloop/web/knowledge_base_live/gaps.ex
@lib/cairnloop/web/knowledge_base_live/index.ex
@test/cairnloop/knowledge_automation/article_suggestion_test.exs
@test/cairnloop/web/knowledge_base_live/gaps_test.exs

<interfaces>
From `lib/cairnloop/web/knowledge_base_live/gaps.ex`:

```elixir
def handle_event("suggest_article", %{"candidate_id" => candidate_id}, socket) do
  attrs = %{
    entrypoint_id: String.to_integer(candidate_id),
    gap_candidate_id: String.to_integer(candidate_id),
    title: socket.assigns.selected_candidate && socket.assigns.selected_candidate.title,
    tenant_scope: Keyword.get(socket.assigns.scope_filters, :tenant_scope, :system_unscoped),
    host_user_id: Keyword.get(socket.assigns.scope_filters, :host_user_id)
  }

  case knowledge_automation().suggest_article(attrs) do
```

From `lib/cairnloop/web/knowledge_base_live/index.ex`:

```elixir
def handle_event("suggest_revision", %{"article_id" => article_id}, socket) do
  case knowledge_automation().suggest_revision(%{article_id: String.to_integer(article_id)}) do
```

From `lib/cairnloop/knowledge_automation.ex`:

```elixir
def suggest_article(attrs, opts \\ []) do
  attrs = Map.new(attrs)

  case prepare_request(:article, attrs, opts) do
```

```elixir
def suggest_revision(attrs, opts \\ []) do
  attrs = Map.new(attrs)
  latest_revision_fn =
    Keyword.get(opts, :latest_revision_fn, &KnowledgeBase.get_latest_active_revision/1)
```

```elixir
defp fallback_attrs_grounding_bundle(attrs, opts) do
  ...
  query = Map.get(attrs, :query) || Map.get(attrs, "query") || "Knowledge Base maintenance"
  retrieval_module.ground_for_draft(%{query: query, host_user_id: host_user_id, clarification_attempts: 0}, ...)
end
```

From `lib/cairnloop/knowledge_automation/stale_article_signal.ex`:

```elixir
def build_revision_gate(article_id, base_revision_id, opts \\ []) do
  gap_events = Keyword.get(opts, :gap_events, [])
  grounding_bundle = Keyword.get(opts, :grounding_bundle, %{})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace gap-candidate generic fallback with hydrated candidate evidence</name>
  <files>lib/cairnloop/knowledge_automation.ex, test/cairnloop/knowledge_automation/article_suggestion_test.exs, test/cairnloop/web/knowledge_base_live/gaps_test.exs</files>
  <read_first>
    - lib/cairnloop/knowledge_automation.ex
    - lib/cairnloop/web/knowledge_base_live/gaps.ex
    - test/cairnloop/knowledge_automation/article_suggestion_test.exs
    - test/cairnloop/web/knowledge_base_live/gaps_test.exs
    - .planning/phases/10-citation-backed-draft-suggestions/10-CONTEXT.md
    - .planning/phases/10-citation-backed-draft-suggestions/10-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
    - `lib/cairnloop/knowledge_automation.ex` contains `get_gap_candidate!`
    - `lib/cairnloop/knowledge_automation.ex` contains `retrieval_gap_events`
    - `lib/cairnloop/knowledge_automation.ex` does not contain the exact fallback query string `"Knowledge Base maintenance"` on the gap-candidate path
    - `test/cairnloop/knowledge_automation/article_suggestion_test.exs` contains the exact test name `suggest_article uses hydrated gap-candidate evidence instead of generic retrieval fallback`
    - `test/cairnloop/web/knowledge_base_live/gaps_test.exs` contains the exact test name `selected gap candidates queue article suggestion generation with candidate-scoped evidence`
  </acceptance_criteria>
  <behavior>
    - Test 1: `suggest_article/2` loads the selected gap candidate in-domain per D-01, D-02, and D-03, hydrates its retrieval and manual-handling evidence, and derives the grounding bundle from that candidate-specific signal before inserting the suggestion row.
    - Test 2: if the selected candidate has no citation-ready canonical evidence, the inserted suggestion is persisted as failed per D-13 through D-17 rather than silently using a generic retrieval query.
    - Test 3: the shipped `Gaps.handle_event("suggest_article", ...)` path still redirects into `/knowledge-base/suggestions?...`, but the inserted suggestion now carries candidate-derived query, evidence snapshot, and digest.
  </behavior>
  <action>Refactor `suggest_article/2` so the gap-candidate flow does not depend on caller-supplied evidence and never falls back to `Retrieval.ground_for_draft/2` with the generic query `Knowledge Base maintenance`. Inside `KnowledgeAutomation`, load the selected candidate with `get_gap_candidate!/2` using `tenant_scope` and `host_user_id` from attrs, hydrate `retrieval_gap_events` plus `manual_handling_evidence`, and build a deterministic candidate grounding context from that durable signal. Use the newest non-empty `sanitized_query_excerpt` as the draft query; if none exists, fall back to the candidate title, then persist that query, the candidate-derived evidence snapshot, and the resulting digest on the suggestion row. Extend `article_suggestion_test.exs` to prove the retrieval module is called with candidate-derived context and that a candidate with no citation-ready canonical evidence fails closed instead of generating from a generic query. Extend `gaps_test.exs` so the real `Gaps` event path proves the inserted suggestion or suggestion-review redirect comes from candidate-scoped evidence, not just captured attrs.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/article_suggestion_test.exs test/cairnloop/web/knowledge_base_live/gaps_test.exs</automated>
  </verify>
  <done>The gap-dashboard entrypoint produces suggestions only from the selected candidate's durable evidence, preserves the shared review-lane redirect, and no longer treats a generic retrieval query as acceptable input for DRAFT-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Load repo-backed stale evidence and fresh canonical grounding inside the revision path</name>
  <files>lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_automation/stale_article_signal.ex, lib/cairnloop/web/knowledge_base_live/index.ex, test/cairnloop/knowledge_automation/article_suggestion_test.exs, test/cairnloop/web/knowledge_base_live/gaps_test.exs</files>
  <read_first>
    - lib/cairnloop/knowledge_automation.ex
    - lib/cairnloop/knowledge_automation/stale_article_signal.ex
    - lib/cairnloop/web/knowledge_base_live/index.ex
    - lib/cairnloop/knowledge_base.ex
    - lib/cairnloop/retrieval.ex
    - test/cairnloop/knowledge_automation/article_suggestion_test.exs
    - test/cairnloop/web/knowledge_base_live/gaps_test.exs
    - .planning/phases/10-citation-backed-draft-suggestions/10-CONTEXT.md
    - .planning/phases/10-citation-backed-draft-suggestions/10-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
    - `lib/cairnloop/knowledge_automation.ex` contains `build_revision_gate`
    - `lib/cairnloop/knowledge_automation.ex` contains `GapEvent`
    - `lib/cairnloop/knowledge_automation.ex` contains `ground_for_draft`
    - `lib/cairnloop/web/knowledge_base_live/index.ex` contains `host_user_id`
    - `test/cairnloop/knowledge_automation/article_suggestion_test.exs` contains the exact test name `suggest_revision loads article-linked gap evidence and fresh canonical grounding on the shipped path`
    - `test/cairnloop/web/knowledge_base_live/gaps_test.exs` contains the exact test name `knowledge base index queues revision suggestions from domain-loaded stale evidence`
  </acceptance_criteria>
  <behavior>
    - Test 1: `suggest_revision/2` keeps the UI contract thin per D-03 and D-04, but internally loads recent article-linked `GapEvent` rows for the latest published revision and builds a fresh canonical grounding bundle before calling `StaleArticleSignal.build_revision_gate/3`.
    - Test 2: the stale gate remains strict per D-05 through D-08: fewer than two eligible recent signals or a missing fresh canonical snapshot returns `{:error, {:stale_gate_blocked, stale_signal}}` and no generation job is queued.
    - Test 3: the shipped `Index.handle_event("suggest_revision", ...)` path proves the inserted suggestion is anchored to the current published revision with internally loaded stale evidence and redirects into the shared review lane.
  </behavior>
  <action>Repair the stale-revision flow at the domain boundary instead of pushing more work into the UI. Update `KnowledgeBaseLive.Index` to keep the same article button but also preserve `tenant_scope` and `host_user_id` in assigns so the real event path passes article identity plus scope into `suggest_revision/2`. In `KnowledgeAutomation`, replace the injected-only `revision_gate_opts/2` approach with internal loading: resolve the latest published revision, query recent durable `GapEvent` rows whose `attempted_evidence_snapshots` link to `{article_id, base_revision_id}`, and build a fresh canonical grounding bundle by calling `Retrieval.ground_for_draft/2` before evaluating `StaleArticleSignal.build_revision_gate/3`. Keep the gate deterministic and composite per D-05 through D-08: no age-only shortcut, no empty `gap_events`, and no queueing without a fresh canonical snapshot anchored to the current revision. Expand `article_suggestion_test.exs` to prove the shipped revision path works without injected `gap_events` or `grounding_bundle`, and replace the shallow `Index` mock proof in `gaps_test.exs` with a real UI-to-domain test that asserts the suggestion-review redirect and the inserted suggestion's `base_revision_id`, `grounding_metadata[\"stale_signal\"]`, and canonical evidence counts.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/article_suggestion_test.exs test/cairnloop/web/knowledge_base_live/gaps_test.exs</automated>
  </verify>
  <done>The stale-article entrypoint can generate a revision suggestion only when durable article-linked failure signals and a fresh canonical snapshot are loaded inside the domain, and the shipped UI path now proves DRAFT-02 plus DRAFT-03 end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gap dashboard -> suggestion domain | Selected candidate identity crosses from UI into durable evidence loading and generation prep |
| KB index -> stale gate | Thin article entrypoint crosses into repo-backed stale evidence and fresh canonical grounding |
| suggestion domain -> generation worker | Persisted evidence snapshot and grounding metadata determine whether generation is queued or fail-closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | T | `suggest_article/2` gap-candidate prep | mitigate | Build the grounding bundle only from hydrated candidate evidence and remove the generic `Knowledge Base maintenance` fallback on the gap-candidate path so unrelated retrieval data cannot masquerade as selected-candidate proof. |
| T-10-13 | S | `suggest_revision/2` stale gate inputs | mitigate | Load article-linked `GapEvent` rows and a fresh canonical snapshot inside the domain before `build_revision_gate/3`; reject queueing when callers provide only `article_id` without qualifying durable evidence. |
| T-10-14 | I | review-lane suggestion metadata | mitigate | Persist candidate-specific query, evidence digest, canonical evidence counts, and stale-signal metadata so operators can audit why the suggestion was queued or blocked. |
</threat_model>

<verification>
Close both blockers with focused proof:

`mix test test/cairnloop/knowledge_automation/article_suggestion_test.exs test/cairnloop/web/knowledge_base_live/gaps_test.exs`
</verification>

<success_criteria>
- Gap-driven article suggestions derive grounding from the selected candidate's durable evidence rather than a generic retrieval query.
- Revision suggestions load article-linked stale evidence and a fresh canonical snapshot inside the domain before generation can be queued.
- The shipped `Gaps` and `Index` entrypoints prove the UI-to-domain-to-review-lane path for DRAFT-01 and DRAFT-02 while preserving DRAFT-03 fail-closed behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/10-citation-backed-draft-suggestions/10-05-SUMMARY.md`
</output>
