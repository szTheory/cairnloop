---
phase: 34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/cairnloop/web/settings_live.ex"
  - "lib/cairnloop/retrieval.ex"
autonomous: true
requirements:
  - SET-02
  - SET-03
  - SET-04
must_haves:
  truths:
    - "Operator sees a stacked card or cockpit layout for Settings."
    - "Operator can toggle Dark Mode via a button, immediately updating UI."
    - "Operator sees Notifier health status."
    - "Operator sees Retrieval system (pgvector) health status."
  artifacts:
    - path: "lib/cairnloop/web/settings_live.ex"
      provides: "Layout shell, health checking in mount, and JS theme toggle in template"
    - path: "lib/cairnloop/retrieval.ex"
      provides: "system_health function to delegate health checks"
  key_links:
    - from: "lib/cairnloop/web/settings_live.ex"
      to: "lib/cairnloop/retrieval.ex"
      via: "Cairnloop.Retrieval.system_health"
      pattern: "Retrieval\\.system_health"
---

<objective>
Transform `SettingsLive` into an operator cockpit layout and implement health metrics and dark mode toggle.

Purpose: Provide a robust operator surface for monitoring system capabilities and customizing the interface ergonomics.
Output: Restructured `SettingsLive` with dark mode toggle and system health metrics.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/34-operator-settings-surface/RESEARCH.md
@.planning/phases/34-operator-settings-surface/34-UI-SPEC.md
@.planning/phases/34-operator-settings-surface/34-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: System Health Checks</name>
  <files>lib/cairnloop/web/settings_live.ex, lib/cairnloop/retrieval.ex</files>
  <action>
    Update `Cairnloop.Retrieval` and `mount/3` in `SettingsLive` to gather system health metrics. 
    1. Create `Cairnloop.Retrieval.system_health/0` (or `/1` taking repo) to encapsulate Retrieval health. This must include checking if the `pgvector` extension is installed, checking the health/status of the vector index itself, and verifying Oban queue status for failed indexing attempts. Return standard state descriptors like `{:ok, "Healthy"}` or `{:error, "Unreachable / Degraded"}`.
    2. In `SettingsLive.mount/3`, check Notifier health using module reflection (e.g., read `:notifier` from config, use `Code.ensure_loaded?` and verify `on_conversation_resolved/2` via `function_exported?`).
    3. In `SettingsLive.mount/3`, check Retrieval health by delegating to `Cairnloop.Retrieval.system_health/0`. Avoid executing raw SQL directly in the LiveView.
    Assign these metrics to the socket. For bad health states, use the correct UI copy: "Unreachable / Degraded". Ensure nil configurations do not crash the server.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/settings_live_test.exs test/cairnloop/retrieval_test.exs</automated>
  </verify>
  <done>Socket assigns include `notifier_health` and `retrieval_health` checks delegated to Context layers, using "Unreachable / Degraded" for error states.</done>
</task>

<task type="auto">
  <name>Task 2: Cockpit Layout & Dark Mode Toggle</name>
  <files>lib/cairnloop/web/settings_live.ex</files>
  <action>
    Restructure the `render/1` template into an operator cockpit layout (e.g., stacked `.cl-card` panels).
    1. Wrap the existing SLA Policies in a card/panel.
    2. Add a new "System Health" card to display the Notifier and Retrieval health metrics. Use visual dot/badge motifs (`--cl-success` for green/healthy, `--cl-danger` for red/error, `--cl-warning` for amber/pending). Display "Unreachable / Degraded" for any bad health states as per UI spec.
    3. Add a "Toggle Dark Mode" button (native HTML) to the top or side of the layout. Per `RESEARCH.md`, use pure JS in the `onclick` handler to toggle the `data-theme` attribute on `document.documentElement`, update `localStorage.setItem('phx:theme', ...)`, and dispatch a `phx:set-theme` custom event.
    Avoid creating LiveView JS Hooks for this library-level toggle.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/settings_live_test.exs</automated>
  </verify>
  <done>Settings Live template renders the new layout, system health indicators (with "Unreachable / Degraded" for errors), and standalone JS dark mode toggle without relying on external hook definitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to LiveView | Settings page is internal but operator-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | `SettingsLive` Dark Mode | accept | Theme toggle modifies only local state and DOM attributes harmlessly. |
</threat_model>
