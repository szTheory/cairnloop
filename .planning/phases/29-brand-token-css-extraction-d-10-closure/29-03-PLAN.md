---
phase: 29-brand-token-css-extraction-d-10-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 29-02
files_modified:
  - test/integration/approval_footer_live_test.exs
  - test/integration/tool_execution_outcome_live_test.exs
  - test/integration/bulk_recovery_live_test.exs
autonomous: true
requirements:
  - BRAND-03
tags:
  - brand-tokens
  - integration-tests
  - test-pin

must_haves:
  truths:
    - "All 6 hex-fallback assertions in the 3 integration test files are re-pinned to the bare `var(--cl-<token>)` form."
    - "Each re-pinned assertion keeps the closing parenthesis (per Pitfall 8 — `\"var(--cl-primary)\"` not `\"var(--cl-primary\"`) so the test stays strict against future regression."
    - "Surrounding assertion messages and never-color-alone context comments are preserved byte-for-byte."
    - "Both `mix compile --warnings-as-errors` and `mix test.integration test/integration/approval_footer_live_test.exs test/integration/tool_execution_outcome_live_test.exs test/integration/bulk_recovery_live_test.exs` pass after the re-pin."
  artifacts:
    - path: "test/integration/approval_footer_live_test.exs"
      provides: "Approval footer brand-token assertion — re-pinned to bare form (1 assertion at line 52)"
    - path: "test/integration/tool_execution_outcome_live_test.exs"
      provides: "Tool execution outcome brand-token assertions — re-pinned to bare form (3 assertions at lines 316, 394, 443)"
    - path: "test/integration/bulk_recovery_live_test.exs"
      provides: "Bulk recovery brand-token assertions — re-pinned to bare form (2 assertions: --cl-primary at line 99, --cl-danger at line 267)"
  key_links:
    - from: "integration test assertions (6 sites)"
      to: "rendered HTML output from InboxLive / ConversationLive / SearchModalComponent (after Plan 02 hex drops)"
      via: "assert html =~ \"var(--cl-<token>)\" substring match"
      pattern: "assert html =~ \"var(--cl-primary)\""
---

<objective>
BRAND-03 closure: re-pin all 6 hex-fallback assertions across 3 integration test
files to the bare `var(--cl-<token>)` form. These are the assertions that
previously asserted the rendered HTML contained the literal string
`"var(--cl-primary, #A94F30)"` or `"var(--cl-danger, #B54C36)"`. After Plan 02
dropped those suffixes from the render code, the rendered HTML now contains
`"var(--cl-primary)"` and `"var(--cl-danger)"` instead — the assertions must
be re-pinned to match.

This plan must run AFTER Plan 02 (Wave 2). Running it before would leave the
build red: the integration assertions would fail because the rendered HTML no
longer contains the hex form.

Purpose: Close BRAND-03. The 6 assertions are the "headless-token contract"
mentioned in REQUIREMENTS.md and the ROADMAP phase goal — keeping them strict
(closing paren preserved per Pitfall 8) ensures the bare-token contract is
asserted positively, not just negatively via the BRAND-04 gate.

Output: 3 modified integration test files (6 assertion lines total).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-brand-token-css-extraction-d-10-closure/29-CONTEXT.md
@.planning/phases/29-brand-token-css-extraction-d-10-closure/29-RESEARCH.md
@.planning/phases/29-brand-token-css-extraction-d-10-closure/29-PATTERNS.md
@.planning/phases/29-brand-token-css-extraction-d-10-closure/29-02-SUMMARY.md
@test/integration/approval_footer_live_test.exs
@test/integration/tool_execution_outcome_live_test.exs
@test/integration/bulk_recovery_live_test.exs

<interfaces>
<!-- Exact line locations verified via grep on 2026-05-27. Executor must NOT search; the locations are fixed. -->

Re-pin table (line numbers may shift slightly after Plan 02 commits, but the string content is exact):

| File                                                       | Line | Before                              | After                       |
| ---------------------------------------------------------- | ---- | ----------------------------------- | --------------------------- |
| test/integration/approval_footer_live_test.exs             | 52   | `"var(--cl-primary, #A94F30)"`     | `"var(--cl-primary)"`       |
| test/integration/tool_execution_outcome_live_test.exs      | 316  | `"var(--cl-primary, #A94F30)"`     | `"var(--cl-primary)"`       |
| test/integration/tool_execution_outcome_live_test.exs      | 394  | `"var(--cl-primary, #A94F30)"`     | `"var(--cl-primary)"`       |
| test/integration/tool_execution_outcome_live_test.exs      | 443  | `"var(--cl-primary, #A94F30)"`     | `"var(--cl-primary)"`       |
| test/integration/bulk_recovery_live_test.exs               | 99   | `"var(--cl-primary, #A94F30)"`     | `"var(--cl-primary)"`       |
| test/integration/bulk_recovery_live_test.exs               | 267  | `"var(--cl-danger, #B54C36)"`      | `"var(--cl-danger)"`        |

Strictness rule (Pitfall 8):
- Re-pin form MUST include the closing parenthesis: `"var(--cl-primary)"` not `"var(--cl-primary"`.
- Substring containment with the closing paren asserts the bare-token form IS present AND excludes any future hex-fallback regression. Dropping the paren would silently admit `"var(--cl-primary, #foo)"` back into the corpus.

Files NOT to touch (per CONTEXT.md D-03 explicit correction of REQUIREMENTS.md):
- test/cairnloop/web/inbox_live_test.exs — uses prefix-only form `"var(--cl-primary"` already; no hex-fallback assertions.
- test/cairnloop/web/conversation_live_test.exs — same; no hex-fallback assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-pin 1 assertion in test/integration/approval_footer_live_test.exs (line 52)</name>
  <files>test/integration/approval_footer_live_test.exs</files>
  <read_first>
    - test/integration/approval_footer_live_test.exs (full file — focus on lines 48-58 containing the assertion at line 52; note the surrounding never-color-alone context and the `assert has_element?(view, "button[phx-click='approve_action']")` follow-on)
    - .planning/phases/29-brand-token-css-extraction-d-10-closure/29-PATTERNS.md (the "test/integration/approval_footer_live_test.exs" section shows the exact assertion form and the closing-paren strictness rule)
    - .planning/phases/29-brand-token-css-extraction-d-10-closure/29-RESEARCH.md (Pitfall 8 — keep the closing paren)
  </read_first>
  <action>
    Locate the assertion at line 52 of `test/integration/approval_footer_live_test.exs`:
    `assert html =~ "var(--cl-primary, #A94F30)"`

    Replace the literal substring `"var(--cl-primary, #A94F30)"` with `"var(--cl-primary)"`.

    The closing parenthesis MUST be preserved in the replacement — `"var(--cl-primary)"` not `"var(--cl-primary"`. Substring containment with the closing paren asserts the bare-token form IS present AND excludes any future hex-fallback regression (Pitfall 8). Without the paren, a regression like `"var(--cl-primary, #foo)"` would silently match again.

    Do NOT touch any other line in this file. Surrounding context — the `live(ctx.conn, "/governance/#{ctx.conversation.id}")` setup, the never-color-alone comment at line 51, the `assert has_element?` follow-up at line 53 — stays byte-for-byte unchanged.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && grep -q 'assert html =~ "var(--cl-primary)"' test/integration/approval_footer_live_test.exs && ! grep -q '"var(--cl-primary, #A94F30)"' test/integration/approval_footer_live_test.exs && ! grep -q '"var(--cl-primary"' test/integration/approval_footer_live_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - File contains `assert html =~ "var(--cl-primary)"` (bare-token form with closing paren, exact literal).
    - File does NOT contain `"var(--cl-primary, #A94F30)"` (old hex-fallback form gone).
    - File does NOT contain `"var(--cl-primary"` as a standalone substring without closing paren (Pitfall 8 strictness check).
    - File still contains `live(ctx.conn, "/governance/#{ctx.conversation.id}")` (test setup unchanged).
    - File still contains `assert has_element?(view, "button[phx-click='approve_action']")` (follow-up assertion unchanged).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The single assertion at line 52 is re-pinned to `"var(--cl-primary)"` with closing paren; surrounding test setup and assertions unchanged; file compiles warnings-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin 3 assertions in test/integration/tool_execution_outcome_live_test.exs (lines 316, 394, 443)</name>
  <files>test/integration/tool_execution_outcome_live_test.exs</files>
  <read_first>
    - test/integration/tool_execution_outcome_live_test.exs (full file — 3 assertions to re-pin at lines 316, 394, 443; each has a trailing message string e.g. `"Status chip must use brand token (never hardcoded hex)"` which MUST be preserved)
    - .planning/phases/29-brand-token-css-extraction-d-10-closure/29-PATTERNS.md (the "test/integration/tool_execution_outcome_live_test.exs" section shows the multi-line `assert html =~ ..., "message"` form and confirms the assertion message strings stay byte-for-byte unchanged)
    - .planning/phases/29-brand-token-css-extraction-d-10-closure/29-RESEARCH.md (Pitfall 8 — keep the closing paren)
  </read_first>
  <action>
    Re-pin 3 assertions at lines 316, 394, 443 of `test/integration/tool_execution_outcome_live_test.exs`. Each assertion has the form:

    ```
    assert html =~ "var(--cl-primary, #A94F30)",
           "<some message string>"
    ```

    For each of the 3 sites: replace the literal substring `"var(--cl-primary, #A94F30)"` with `"var(--cl-primary)"`. The trailing assertion-message string (the second argument to `assert/2`) stays byte-for-byte unchanged at every site. The line break between the `=~ "..."` and the message string stays unchanged.

    Pitfall 8: keep the closing parenthesis in the re-pinned literal — `"var(--cl-primary)"` not `"var(--cl-primary"`. Strictness check rejects future hex-fallback regression.

    All 3 sites use the same token (`--cl-primary`) so the replacement is identical at each site. Do NOT touch any other line in this file.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && [ "$(grep -c 'assert html =~ "var(--cl-primary)"' test/integration/tool_execution_outcome_live_test.exs)" -ge 3 ] && ! grep -q '"var(--cl-primary, #A94F30)"' test/integration/tool_execution_outcome_live_test.exs && grep -q 'Status chip must use brand token' test/integration/tool_execution_outcome_live_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'assert html =~ "var(--cl-primary)"' test/integration/tool_execution_outcome_live_test.exs` returns >= 3 (all three sites re-pinned).
    - File does NOT contain `"var(--cl-primary, #A94F30)"` (old hex-fallback form fully gone).
    - File does NOT contain any standalone `"var(--cl-primary"` substring without closing paren.
    - File still contains the assertion message `"Status chip must use brand token (never hardcoded hex)"` (or whatever the exact message at line 316 is — preserve byte-for-byte from the current file).
    - File line count is unchanged vs. before (no line additions or deletions — only in-line string substitution).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>All 3 assertions in tool_execution_outcome_live_test.exs are re-pinned to `"var(--cl-primary)"` with closing paren; trailing assertion-message strings preserved byte-for-byte; file compiles warnings-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin 2 assertions in test/integration/bulk_recovery_live_test.exs (lines 99 + 267 — one primary, one danger)</name>
  <files>test/integration/bulk_recovery_live_test.exs</files>
  <read_first>
    - test/integration/bulk_recovery_live_test.exs (full file — 2 assertions to re-pin: line 99 uses `--cl-primary`, line 267 uses `--cl-danger`; each has surrounding never-color-alone context: line 99 follows `"2 selected"` + `"Send recovery follow-up to 2"`; line 267 follows `<svg`, `"Batch too large."`, `"safe send limit of 2"`)
    - .planning/phases/29-brand-token-css-extraction-d-10-closure/29-PATTERNS.md (the "test/integration/bulk_recovery_live_test.exs" section shows BOTH assertion forms — primary at line 99, danger at line 267 — and confirms surrounding never-color-alone assertions stay byte-for-byte unchanged)
    - .planning/phases/29-brand-token-css-extraction-d-10-closure/29-RESEARCH.md (Pitfall 8 — keep the closing paren for both)
  </read_first>
  <action>
    Re-pin 2 assertions in `test/integration/bulk_recovery_live_test.exs`:

    Site 1 (line 99 area — primary assertion):
    Replace the literal substring `"var(--cl-primary, #A94F30)"` with `"var(--cl-primary)"`.

    Site 2 (line 267 area — danger assertion):
    Replace the literal substring `"var(--cl-danger, #B54C36)"` with `"var(--cl-danger)"`.

    Both re-pins MUST keep the closing parenthesis (Pitfall 8): `"var(--cl-primary)"` and `"var(--cl-danger)"`. Without the paren, future hex-fallback regression would silently match.

    Surrounding context lines stay byte-for-byte unchanged at both sites:
    - Site 1 (line 99 area): the preceding `assert html =~ "2 selected"`, `assert html =~ "Send recovery follow-up to 2"`, and the `# Brand §7.5 — text label AND token (never-color-alone).` comment all stay as-is.
    - Site 2 (line 267 area): the preceding `# Refusal banner — never-color-alone: SVG icon + heading text + token.` comment plus `assert html =~ "<svg"`, `assert html =~ "Batch too large."`, `assert html =~ "safe send limit of 2"` all stay as-is.

    Do NOT touch any other line in this file.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && grep -q 'assert html =~ "var(--cl-primary)"' test/integration/bulk_recovery_live_test.exs && grep -q 'assert html =~ "var(--cl-danger)"' test/integration/bulk_recovery_live_test.exs && ! grep -q '"var(--cl-primary, #A94F30)"' test/integration/bulk_recovery_live_test.exs && ! grep -q '"var(--cl-danger, #B54C36)"' test/integration/bulk_recovery_live_test.exs && grep -q 'Batch too large.' test/integration/bulk_recovery_live_test.exs && grep -q 'Send recovery follow-up to 2' test/integration/bulk_recovery_live_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - File contains `assert html =~ "var(--cl-primary)"` (Site 1, bare-token form with closing paren).
    - File contains `assert html =~ "var(--cl-danger)"` (Site 2, bare-token form with closing paren).
    - File does NOT contain `"var(--cl-primary, #A94F30)"` (old form gone).
    - File does NOT contain `"var(--cl-danger, #B54C36)"` (old form gone).
    - File still contains `"Batch too large."` (refusal banner copy preserved — never-color-alone context).
    - File still contains `"Send recovery follow-up to 2"` (selected-cohort copy preserved).
    - File still contains `assert html =~ "<svg"` (icon assertion preserved — never-color-alone).
    - File line count is unchanged vs. before.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Both assertions (primary at line 99 area, danger at line 267 area) re-pinned to bare-token form with closing parens; surrounding never-color-alone assertions and refusal banner copy preserved byte-for-byte; file compiles warnings-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies integration test files. No new trust boundaries are introduced.
The changes are byte-replacement substitutions in test source code (assertion
literals); no user input, no runtime configuration, no network traffic, no
persisted data, no new authentication or session surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-Strict | Tampering | Loss of test strictness (admitting hex-fallback regressions silently) | mitigate | Pitfall 8 mandates the closing parenthesis in every re-pinned literal: `"var(--cl-primary)"` not `"var(--cl-primary"`. Acceptance_criteria assert the closing-paren form is present AND the prefix-only form (no paren) is absent. The combination keeps the test strict against `"var(--cl-primary, #foo)"` re-introduction. |
| T-29-03-Spoof | Spoofing | Never-color-alone contract weakening (token swap without text-label cross-check) | accept | Brand §7.5 enforcement is unchanged. Each re-pinned assertion still appears alongside the corresponding text-label or icon assertion at the same call site (`"Send recovery follow-up to 2"`, `"Batch too large."`, `<svg`, `"Status chip must use brand token"`). Acceptance_criteria explicitly assert those surrounding assertions are preserved. |
| T-29-03-SC | Tampering | Supply chain (npm/pip/cargo installs) | n/a | No new packages installed; no install surface to audit. |

ASVS L1 evaluation: V2/V3/V4/V5/V6/V7 do not apply (test-only file edits, no
production surface). V14 Configuration applies in the "headless test contract"
sense and is addressed by the strictness-mitigation above plus the BRAND-04
gate test from Plan 02 (which negatively asserts the same contract from the
opposite direction).
</threat_model>

<verification>
- `mix compile --warnings-as-errors` from project root exits 0.
- `grep -rE '"var\(--cl-(primary|danger), #' test/integration/approval_footer_live_test.exs test/integration/tool_execution_outcome_live_test.exs test/integration/bulk_recovery_live_test.exs` returns nothing.
- `grep -c 'assert html =~ "var(--cl-primary)"' test/integration/approval_footer_live_test.exs test/integration/tool_execution_outcome_live_test.exs test/integration/bulk_recovery_live_test.exs` returns >= 5 (1 + 3 + 1 sites).
- `grep -c 'assert html =~ "var(--cl-danger)"' test/integration/bulk_recovery_live_test.exs` returns >= 1 (the line 267 site).
- `mix test.integration test/integration/approval_footer_live_test.exs test/integration/tool_execution_outcome_live_test.exs test/integration/bulk_recovery_live_test.exs` exits 0 (per CLAUDE.md: integration tests need real Postgres; if `Cairnloop.Repo` is unavailable in this workspace, document the result honestly and rely on `mix compile --warnings-as-errors` + visual inspection of the re-pinned strings; CI's integration lane is the authoritative proof).
- `mix test test/cairnloop/web/brand_token_gate_test.exs` exits 0 (BRAND-04 gate from Plan 02 still passes — re-pin doesn't affect the gate).
</verification>

<success_criteria>
BRAND-03 satisfied: all 6 hex-fallback assertions across 3 integration test
files are re-pinned to the bare `var(--cl-<token>)` form, each preserving the
closing parenthesis for strictness (Pitfall 8). Surrounding never-color-alone
assertions (text labels, SVG icons, refusal banner copy) are preserved
byte-for-byte. `mix compile --warnings-as-errors` passes; the BRAND-04 gate
(Plan 02) continues to pass.

Phase 29 as a whole now satisfies all 4 ROADMAP success criteria:
1. Canonical `:root` block in `app.css` (Plan 01).
2. Zero hex fallbacks in render code (Plan 02).
3. 6 hex-fallback assertions re-pinned to bare form (this Plan).
4. Negative-grep gate enforces zero hex fallbacks going forward (Plan 02 Task 1).

D-10 deferred decision from vM013 close is now resolved via Option B.
</success_criteria>

<output>
Create `.planning/phases/29-brand-token-css-extraction-d-10-closure/29-03-SUMMARY.md` when done.
</output>
