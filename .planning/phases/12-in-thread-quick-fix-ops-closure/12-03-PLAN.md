---
phase: "12"
plan: "03"
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - lib/cairnloop/web/conversation_live.ex
  - lib/cairnloop/web/review_task_presenter.ex
  - lib/cairnloop/web/article_suggestion_presenter.ex
  - test/cairnloop/web/conversation_live_test.exs
  - test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "The primary quick-fix launch control lives in the conversation evidence rail."
    - "Operators can see review-ready, shell-created, blocked/manual-required, published, and reindexed states from the thread without a second dashboard."
    - "The thread action deep-links into the shared review lane or manual authoring path using durable state."
---

<objective>
Add the in-thread quick-fix card and the conversation-side status experience.

Purpose: satisfy the operator-facing part of `OPS-01` by making quick-fix initiation and follow-through visible inside `ConversationLive`.
Output: evidence-rail card, status rail, launch action, and thread-side links into review or manual authoring.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-in-thread-quick-fix-ops-closure/12-CONTEXT.md
@.planning/phases/12-in-thread-quick-fix-ops-closure/12-UI-SPEC.md
@.planning/phases/12-in-thread-quick-fix-ops-closure/12-RESEARCH.md
@lib/cairnloop/web/conversation_live.ex
@lib/cairnloop/web/review_task_presenter.ex
@lib/cairnloop/web/article_suggestion_presenter.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the quick-fix card in the conversation evidence rail</name>
  <files>lib/cairnloop/web/conversation_live.ex, lib/cairnloop/web/review_task_presenter.ex, lib/cairnloop/web/article_suggestion_presenter.ex, test/cairnloop/web/conversation_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex
    - .planning/phases/12-in-thread-quick-fix-ops-closure/12-UI-SPEC.md
    - lib/cairnloop/web/review_task_presenter.ex
    - lib/cairnloop/web/article_suggestion_presenter.ex
  </read_first>
  <acceptance_criteria>
    - `lib/cairnloop/web/conversation_live.ex` contains a dedicated quick-fix card in the evidence rail
    - the card renders typed layer summary and shell/blocked reason callout when applicable
    - `test/cairnloop/web/conversation_live_test.exs` exits 0 and asserts the quick-fix card, launch copy, and state copy
  </acceptance_criteria>
  <action>Add a dedicated quick-fix card to the evidence rail below conversation evidence/context and above draft audit cards, matching the approved UI contract. The card must show a KB-maintenance title, one-sentence status summary, typed layer summary, optional shell/blocked reason callout, a primary action row, and a follow-through status rail. Keep the conversation timeline dominant, use the existing warm card pattern rather than a new modal/drawer, and do not put the primary launch action in the reply composer or generic tool list.</action>
  <verify>
    <automated>mix test test/cairnloop/web/conversation_live_test.exs</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire launch and follow-through actions from the thread into the shared maintenance lane</name>
  <files>lib/cairnloop/web/conversation_live.ex, test/cairnloop/web/conversation_live_test.exs, test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex
    - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex
    - lib/cairnloop/knowledge_automation.ex
    - .planning/phases/12-in-thread-quick-fix-ops-closure/12-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - thread launch handler calls the conversation quick-fix create-or-reuse seam
    - thread launch deep-links into `/knowledge-base/suggestions` when a review task exists
    - blocked/manual-required state exposes a manual-authoring action instead of a dead end
    - `test/cairnloop/web/conversation_live_test.exs` exits 0 and covers launch, reuse, and manual fallback
  </acceptance_criteria>
  <action>Wire `ConversationLive.handle_event/3` so the quick-fix button calls the new `KnowledgeAutomation` conversation quick-fix seam and then routes by durable outcome: review-ready and shell-created quick fixes deep-link into the shared review lane; blocked/manual-required outcomes expose an explicit manual-authoring path while keeping the reason visible in the thread card. Reuse durable suggestion/task state for follow-through rendering so published and reindexed updates can appear back in the thread without introducing transient-only state.</action>
  <verify>
    <automated>mix test test/cairnloop/web/conversation_live_test.exs test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conversation evidence rail -> maintenance action | Thread-side UI must not look like a generic reply or tool action |
| thread status -> review lane truth | The thread must mirror durable maintenance truth without inventing a second workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | S | thread-side quick-fix UI | mitigate | Keep the card in the evidence rail with explicit maintenance copy and durable state links, not in the composer or generic tool registry. |
</threat_model>

<verification>
Run the thread-side quick-fix suites:

`mix test test/cairnloop/web/conversation_live_test.exs test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs`
</verification>

<success_criteria>
- Operators can launch KB maintenance from the conversation evidence rail.
- Thread-side statuses map to durable maintenance state.
- The shared review lane remains the destination for deeper review and publish work.
</success_criteria>

