---
phase: "12"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_automation/article_suggestion.ex
  - lib/cairnloop/knowledge_automation/review_task.ex
  - test/cairnloop/knowledge_automation/article_suggestion_test.exs
  - test/cairnloop/knowledge_automation/review_task_test.exs
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "Quick fix is a conversation-scoped entrypoint into the existing suggestion/review lane, not a new workflow."
    - "Thread context and assistive case context stay typed and bounded without weakening canonical citation rules."
    - "Repeated quick-fix launches for the same conversation reuse one durable maintenance lane when the evidence digest has not changed."
  artifacts:
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "Conversation quick-fix create-or-reuse command, lookup seam, and typed package builder"
    - path: "lib/cairnloop/knowledge_automation/article_suggestion.ex"
      provides: "Conversation entrypoint support without relaxing citation-only evidence rules"
    - path: "lib/cairnloop/knowledge_automation/review_task.ex"
      provides: "Existing task model remains the reusable quick-fix workflow truth"
---

<objective>
Add the durable conversation quick-fix backend contract and typed evidence package.

Purpose: establish Phase 12's conversation-scoped identity, typed quick-fix packaging, and lookup seams before fallback or UI work begins.
Output: a `KnowledgeAutomation` quick-fix create-or-reuse API, conversation-scoped lookup, and a typed package that preserves canonical-only `evidence_snapshot` rules.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-in-thread-quick-fix-ops-closure/12-CONTEXT.md
@.planning/phases/12-in-thread-quick-fix-ops-closure/12-RESEARCH.md
@.planning/phases/12-in-thread-quick-fix-ops-closure/12-PATTERNS.md
@lib/cairnloop/knowledge_automation.ex
@lib/cairnloop/knowledge_automation/article_suggestion.ex
@lib/cairnloop/knowledge_automation/article_suggestion_evidence.ex
@lib/cairnloop/knowledge_automation/review_task.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add conversation-scoped quick-fix identity and typed package preparation</name>
  <files>lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_automation/article_suggestion.ex, test/cairnloop/knowledge_automation/article_suggestion_test.exs</files>
  <read_first>
    - lib/cairnloop/knowledge_automation.ex
    - lib/cairnloop/knowledge_automation/article_suggestion.ex
    - lib/cairnloop/knowledge_automation/article_suggestion_evidence.ex
    - .planning/phases/12-in-thread-quick-fix-ops-closure/12-CONTEXT.md
    - .planning/phases/12-in-thread-quick-fix-ops-closure/12-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `lib/cairnloop/knowledge_automation/article_suggestion.ex` contains a conversation-scoped entrypoint value for quick fix
    - `lib/cairnloop/knowledge_automation.ex` contains a public create-or-reuse seam for conversation quick fix
    - `lib/cairnloop/knowledge_automation.ex` contains a typed package builder with explicit `thread_context`, `canonical_retrieval`, and `resolved_case_assists` keys
    - `test/cairnloop/knowledge_automation/article_suggestion_test.exs` exits 0 and covers conversation quick-fix identity plus typed package preservation
  </acceptance_criteria>
  <action>Extend the existing suggestion identity model so quick fix uses a conversation-scoped entrypoint instead of pretending to be a gap candidate. Add a public `create_or_reuse_conversation_quick_fix/2` seam in `KnowledgeAutomation` that accepts a `conversation_id`, `host_user_id`, bounded thread summary attrs, and optionally precomputed retrieval evidence. Build a typed quick-fix package with explicit top-level `thread_context`, `canonical_retrieval`, and `resolved_case_assists` keys, persist it under a host-owned structured metadata key, and keep `evidence_snapshot` canonical-only so `ArticleSuggestionEvidence` still enforces citation anchors. Reuse the existing stable-key and evidence-digest patterns so repeated launches with unchanged evidence reuse the same suggestion instead of creating forks.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/article_suggestion_test.exs</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add conversation quick-fix lookup and review-task reuse seams</name>
  <files>lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_automation/review_task.ex, test/cairnloop/knowledge_automation/review_task_test.exs</files>
  <read_first>
    - lib/cairnloop/knowledge_automation.ex
    - lib/cairnloop/knowledge_automation/review_task.ex
    - test/cairnloop/knowledge_automation/review_task_test.exs
    - .planning/phases/12-in-thread-quick-fix-ops-closure/12-PATTERNS.md
  </read_first>
  <acceptance_criteria>
    - `lib/cairnloop/knowledge_automation.ex` contains a conversation quick-fix lookup function
    - `lib/cairnloop/knowledge_automation.ex` contains create-or-reuse logic that returns an existing active review task when quick fix already exists
    - `test/cairnloop/knowledge_automation/review_task_test.exs` exits 0 and proves repeated conversation quick-fix launches do not create duplicate active tasks
  </acceptance_criteria>
  <action>Add a `get_conversation_quick_fix/2` or equivalently named read seam that returns the latest conversation-scoped suggestion plus any active review task so `ConversationLive` can render durable status. Reuse `ensure_review_task_for_suggestion/2` and the existing one-active-task discipline rather than adding a second workflow table. The create-or-reuse command must return the already-linked task when one exists, preserve tenant and host scoping, and expose enough structured quick-fix state for later UI work without requiring `ConversationLive` to inspect raw DB fields.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/review_task_test.exs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conversation thread -> maintenance package | Raw support context becomes AI-adjacent maintenance input and must stay typed and bounded |
| conversation quick fix -> review lane | Repeated launches must converge on one durable workflow lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T | quick-fix evidence package | mitigate | Keep canonical citations in `evidence_snapshot` and persist thread/assistive layers under explicit typed keys instead of relaxing the citation validator. |
| T-12-02 | E | repeated quick-fix launches | mitigate | Reuse stable key + evidence digest + conversation entrypoint identity so repeated launches return one active suggestion/task lane. |
</threat_model>

<verification>
Run the focused quick-fix backend suites:

`mix test test/cairnloop/knowledge_automation/article_suggestion_test.exs test/cairnloop/knowledge_automation/review_task_test.exs`
</verification>

<success_criteria>
- Conversation quick fix is a first-class suggestion entrypoint.
- Thread context stays separate from canonical citation evidence.
- One active review lane is reusable for repeated quick-fix launches.
</success_criteria>

