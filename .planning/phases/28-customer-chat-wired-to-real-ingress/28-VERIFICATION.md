---
phase: 28-customer-chat-wired-to-real-ingress
verified: 2026-05-27T22:22:01Z
status: human_needed
score: 23/23 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Two-tab round trip (operator inbox + customer /chat)"
    expected: "Open http://localhost:4000/support in Tab 1 and http://localhost:4000/chat in Tab 2 (after `cd examples/cairnloop_example && mix setup && mix phx.server`). Type 'hello from customer' in Tab 2; it appears in Tab 1's inbox without a refresh. Click into the conversation in Tab 1 and reply 'hello from operator'; the reply appears in Tab 2's thread within ~1s. The 'Message sent — waiting on operator.' row disappears when the operator reply lands."
    why_human: "The Completeness test in the phase goal explicitly requires two live browser tabs against a running Phoenix server with Postgres. Headless ExUnit cannot verify live PubSub round-trip across two LiveView processes + JS Channel transport. 28-VALIDATION.md flags this as manual-only. SUMMARY 03 records 'Manual UAT not performed in this automated execution (no live Postgres/browser available in workspace).'"
  - test: "Multi-message-stack manual UAT (UI-SPEC interaction step 11)"
    expected: "In Tab 2 send 'ping one' then 'ping two' back-to-back. The pending 'Message sent — waiting on operator.' row stays visible after both. In Tab 1, click into the new conversation and reply 'got both'. In Tab 2, pending clears and exactly ONE operator bubble appears (not duplicated)."
    why_human: "Locked by headless Test 5b in chat_live_test.exs, but the live-browser version is the contractual completeness signal — JS hook timing, DOM render order, and PubSub delivery are not fully exercised by direct ExUnit calls into handle_event/handle_info."
  - test: "No mock 'Bot reply' / 'AI is thinking' / 'Ticket submitted' / 'Agent will reply' text appears in customer thread"
    expected: "After sending a message in Tab 2, no mock copy appears at any point — only the operator's actual reply lands. The previously-mocked 1-second Process.send_after auto-reply is fully removed."
    why_human: "Negative-grep test 7 covers source-level absence, but runtime confirmation (and confirmation that the operator-reply path is the only source of operator-side messages reaching the customer) needs the live demo. Source-level absence is verified by automated grep; runtime absence needs the two-tab UAT."
  - test: "Operator inbox refreshes when a new customer joins /chat (D-09/D-10)"
    expected: "Without manually refreshing Tab 1, observe that opening Tab 2 (which calls `WidgetChannel.join('widget:lobby')` and creates a fresh Conversation row) causes a new conversation entry to appear in Tab 1's inbox within ~1s — driven by the `{:conversations_changed}` PubSub broadcast on the 'conversations' topic."
    why_human: "InboxLive subscribes to 'conversations' in mount/3 (verified by source grep) and reloads on handle_info({:conversations_changed}). Cross-LV PubSub timing under a live Phoenix server cannot be exercised headlessly."
---

# Phase 28: Customer Chat Wired to Real Ingress Verification Report

**Phase Goal:** Wire the customer-facing chat (ChatLive) through the real library ingress path so operator replies appear live without polling. Completeness test: a developer can open two browser tabs — one as customer at /chat, one as operator at /conversations — type a message on each side, and see it appear on the other side in real time.

**Verified:** 2026-05-27T22:22:01Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (merged from ROADMAP Success Criteria + PLAN must_haves)

| #   | Truth                                                                                                                                                                                                                                          | Status     | Evidence                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | ROADMAP SC1: `examples/.../endpoint.ex` mounts `Cairnloop.Channels.WidgetSocket` at its canonical socket path (CHAT-01)                                                                                                                       | ✓ VERIFIED | endpoint.ex:23 — `socket "/widget", Cairnloop.Channels.WidgetSocket, websocket: true, longpoll: false`. Grep count = 1.                                                                                                                                                          |
| 2   | ROADMAP SC2: Customer types in /chat → pushes through WidgetChannel → lands in operator inbox conversation → operator reply broadcasts back to customer ChatLive via PubSub; no `Process.send_after` / `:bot_reply` mock remains (CHAT-02) | ✓ VERIFIED | All pipeline pieces wired (see truths 4–14 below). `grep -c "Process.send_after" chat_live.ex` = 0, `grep -c ":bot_reply" chat_live.ex` = 0. **Live two-tab round trip remains a human verification** — see human_verification section.                                          |
| 3   | ROADMAP SC3: Example app README documents the two-tab demo with exact local-dev commands (CHAT-03)                                                                                                                                            | ✓ VERIFIED | README:32 `## Two-Tab Demo`; lines 37–38 list both URLs; setup commands `mix setup`/`mix phx.server` at lines 23, 29. Section order via `awk '/^## /'`: Requirements (9), Setup (14), Two-Tab Demo (32), Included Integrations (44), Routing (54) — placement correct.            |
| 4   | Plan 01: Example app supervisor starts `{Phoenix.PubSub, name: Cairnloop.PubSub}` adjacent to `CairnloopExample.PubSub` so library broadcasts have a registry (Pitfall 1)                                                                       | ✓ VERIFIED | `examples/cairnloop_example/lib/cairnloop_example/application.ex:14,22` — both PubSub registries via `Supervisor.child_spec/2` with unique `:id` atoms (Rule 1 fix recorded in SUMMARY 02).                                                                                       |
| 5   | Plan 01: `Chat.create_customer_conversation/1` inserts `%Conversation{status: :open, subject: "Customer chat"}`, broadcasts `{:conversations_changed}` on "conversations", returns `{:ok, conversation}` (D-05)                                | ✓ VERIFIED | `lib/cairnloop/chat.ex:34-50` — exact signature, broadcasts via `broadcast_safely("conversations", {:conversations_changed})`. Tests in chat_test.exs pass (22 tests, 0 failures).                                                                                                |
| 6   | Plan 01: `Chat.ingest_widget_message/2` inserts `%Message{role: :user}` via Message.changeset (NOT through `reply_to_conversation/4`); post-commit broadcasts both `{:message_created, id}` and `{:conversations_changed}` (D-06)              | ✓ VERIFIED | `lib/cairnloop/chat.ex:60-77` — `repo().insert(changeset)` + two `broadcast_safely` calls; no `reply_to_conversation` call. Negative check via `awk` confirms function body does not call `reply_to_conversation`.                                                                |
| 7   | Plan 01: `reply_to_conversation/4` `:ok` branch broadcasts `{:message_created, results.message.id}` on the conversation topic (OQ-1, additive, sealed contract preserved, `{result, meta}` lambda trailing expression preserved)                | ✓ VERIFIED | `lib/cairnloop/chat.ex:198-218` — case-statement as side-effecting statement between `result = repo().transaction(multi)` and `{result, meta}`; sealed signature unchanged at line 79. Locked by OQ-1 Test 3 (return-shape guard) in chat_test.exs.                                |
| 8   | Plan 01: ConversationLive has `handle_info({:message_created, _id}, socket)` that calls `reload_conversation_with_context/2` (D-17)                                                                                                            | ✓ VERIFIED | `lib/cairnloop/web/conversation_live.ex:47` — `def handle_info({:message_created, _message_id}, socket)` mirrors existing handle_info cluster body verbatim.                                                                                                                      |
| 9   | Plan 01: InboxLive `mount/3` subscribes to `Cairnloop.PubSub` topic `"conversations"` inside `connected?(socket)`, and `handle_info({:conversations_changed}, socket)` reloads + prunes selected_ids (D-09, D-10)                              | ✓ VERIFIED | `lib/cairnloop/web/inbox_live.ex:88` subscribe call; lines 321-325 handle_info that calls `Chat.list_conversations/0` + `prune_selected_ids/2` (pre-existing helper now load-bearing).                                                                                            |
| 10  | Plan 02: `WidgetChannel.join("widget:lobby", _, socket)` calls `Chat.create_customer_conversation/1` with `host_user_id: socket.assigns[:user_token]`, stores `conversation.id` in `socket.assigns`, replies `{:ok, %{conversation_id: id}, socket}` (D-01) | ✓ VERIFIED | `lib/cairnloop/channels/widget_channel.ex:12-23` — exact body matches plan. The `{:error, _changeset}` branch returns `{:error, %{reason: "could_not_create_conversation"}}`.                                                                                                    |
| 11  | Plan 02: Existing conversation-scoped `join("widget:" <> _private_room_id, ...)` clause preserved byte-for-byte (deferred per CONTEXT.md §122)                                                                                                  | ✓ VERIFIED | `lib/cairnloop/channels/widget_channel.ex:29-37` — clause intact; existing token-check body preserved.                                                                                                                                                                            |
| 12  | Plan 02: `WidgetChannel.handle_in("new_message", %{"content" => content}, socket)` reads `conversation_id` ONLY from `socket.assigns[:conversation_id]` (T-M001 mitigation), enqueues `ProcessMessage` with `{channel, conversation_id, content}` (D-07) | ✓ VERIFIED | `widget_channel.ex:45-53` — payload pattern is `%{"content" => content}` (no `"conversation_id"` key). Negative-grep enforced: `grep -B1 -A6 "handle_in(\"new_message\"" ... \| grep -c "\"conversation_id\""` = 0. Sealed-contract negative gate: `grep -c "Application.get_env(:cairnloop, :oban_module"` = 0. |
| 13  | Plan 02: `ProcessMessage` worker has `unique: [period: 30, fields: [:worker, :args], keys: [:conversation_id, :content]]` header + two `perform/1` clauses (widget → `Chat.ingest_widget_message/2`; email → preserved logger stub) (D-07, Pitfall 2 / OQ-2) | ✓ VERIFIED | `lib/cairnloop/workers/process_message.ex:28-56` — exact header + two-clause perform/1; widget branch calls `Cairnloop.Chat.ingest_widget_message(id, content)`; email branch uses `Logger.warning` (deviation documented in SUMMARY 02 — `:warning` chosen for test-env log filter). |
| 14  | Plan 02: `Cairnloop.Ingress.EmailWebhookPlug` continues to call `ProcessMessage.new(%{channel: "email", content: content})` and is untouched (sealed secondary caller)                                                                          | ✓ VERIFIED | `lib/cairnloop/ingress/email_webhook_plug.ex:19` — call preserved; `git log -1 -- lib/cairnloop/ingress/email_webhook_plug.ex` last touched in `5cd7946` (M005-S02), not Phase 28.                                                                                                |
| 15  | Plan 03: `ChatLive` is rewritten end-to-end: no `Process.send_after`, no `handle_info(:bot_reply, _)`, no `:bot_reply`. Replaces with mount + handle_event(conversation_id) + handle_event(channel_status) + handle_event(send_message) + handle_event(send_error) + handle_info({:message_created, _}) + colocated WidgetChat JS hook (CHAT-02, D-01..D-04, D-11..D-14) | ✓ VERIFIED | `examples/cairnloop_example/lib/cairnloop_example_web/live/chat_live.ex` — full rewrite. `grep -c "Process.send_after"` = 0, `grep -c ":bot_reply"` = 0. All 5 event handlers + handle_info + colocated hook present.                                                            |
| 16  | Plan 03: Colocated hook opens `new Socket("/widget", {params: {token}})`, joins `"widget:lobby"`, pushes `conversation_id` on join `.receive("ok", ...)`, pushes `channel_status` on `onOpen`/`onError`/`onClose`, forwards `widget:send` events to `channel.push("new_message", ...)`. Hook script starts with `import {Socket} from "phoenix"` (Pitfall 4) | ✓ VERIFIED | `chat_live.ex:137-179` — entire hook present; `import {Socket} from "phoenix"` at line 138 (Pitfall 4 mitigated); `Socket("/widget", {params: {token}})` at line 143; `channel("widget:lobby", {})` at line 154; `handleEvent("widget:send", ...)` at line 168.                  |
| 17  | Plan 03: ChatLive assigns: `@channel_status :: :connecting \| :connected \| :disconnected`, `@messages :: list`, `@pending :: boolean`, `@conversation_id :: String.t() \| nil` (D-03). LV subscribes to `Cairnloop.PubSub` topic `"conversation:#{id}"` only after the hook delivers the id (D-02)                                       | ✓ VERIFIED | `chat_live.ex:11-21` mount initializes 5 assigns (the 4 above + `:send_error`). `chat_live.ex:194-196` subscribes inside `if connected?(socket)` in `handle_event("conversation_id", ...)`.                                                                                       |
| 18  | Plan 03: Optimistic-vs-PubSub dedup-by-role (Pitfall 7): `handle_info({:message_created, msg_id}, socket)` loads via `Chat.get_message/1` and ONLY appends if `role == :agent`; `:user` role messages skipped because the customer already optimistically rendered them                                                              | ✓ VERIFIED | `chat_live.ex:259-278` — exact case-match on `%Message{role: :agent, ...}` appending operator msg; fallback `_` skips :user/nil silently.                                                                                                                                        |
| 19  | Plan 03: `Cairnloop.Chat.get_message(id)` is a new narrow read function returning `%Message{}` or `nil` (tolerant — never raises)                                                                                                              | ✓ VERIFIED | `lib/cairnloop/chat.ex:17-19` — `repo().get(Cairnloop.Message, id)` (not `get!/2`). Three `describe "get_message/1"` tests cover happy + nil paths.                                                                                                                              |
| 20  | Plan 03: Render markup follows UI-SPEC §1: `role="log" aria-live="polite"` on thread, brand tokens `var(--cl-<token>, #<hex>)`, no typing indicator, dot+label channel state, `min-h-[44px]` Send button, HEEx auto-escapes content                  | ✓ VERIFIED | `chat_live.ex:64-67` thread; `min-h-[44px]` at line 124; brand tokens `var(--cl-primary, #A94F30)` at line 125; `<%= msg.content %>` at 84 auto-escapes; no `raw(` calls.                                                                                                        |
| 21  | Plan 03: All 6 locked copy strings present per UI-SPEC §1d ("Cairnloop Support", "How can we help you today?", "Connecting…", "Connected", "Disconnected — reconnecting", "Message sent — waiting on operator.") + 2 error strings ("Your message could not be sent. ...", "Could not connect to support. ...") — none of the forbidden strings ("Bot is typing", "AI is thinking", "Ticket submitted", "Agent will reply") | ✓ VERIFIED | All 8 required strings present (grep counts each = 1 excluding comments); all 4 forbidden strings absent (grep counts = 0).                                                                                                                                                       |
| 22  | Plan 03: `examples/cairnloop_example/test/cairnloop_example_web/live/chat_live_test.exs` is a NEW file covering mount + handle_event clauses + handle_info role-dedup + multi-message-stack scenario (UI-SPEC step 11) + negative-grep for mock paths (path resolved via `Application.app_dir/2`) | ✓ VERIFIED | File present (298 LOC, 8 tests). All 8 tests pass (`mix test test/cairnloop_example_web/live/chat_live_test.exs --warnings-as-errors`: 8 tests, 0 failures). Test 5b multi-message-stack invariant present at line 197; negative-grep at line 255 resolves via both `Application.app_dir(:cairnloop_example)` (line 270) and `File.cwd!()` (line 276). |
| 23  | Plan 03: `mix compile --warnings-as-errors` AND `mix test --warnings-as-errors` are green in BOTH the root library AND `examples/cairnloop_example/` (baseline DraftTest failure tolerated per CLAUDE.md)                                       | ✓ VERIFIED | Root: `mix compile --warnings-as-errors` exits 0; full suite `697 tests, 1 failure (47 excluded)` — single failure is `Cairnloop.Automation.DraftTest.changeset/2 requires content, status, and conversation_id` (matches CLAUDE.md "Cairnloop baseline DraftTest failure" memory exactly). Example app: `mix compile --warnings-as-errors` exits 0; full suite `24 tests, 0 failures`. |

**Score:** 23/23 truths verified

### Required Artifacts (cross-cut by plan)

| Artifact                                                                            | Expected                                                          | Status     | Details                                                                                                                                                                                                          |
| ----------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `examples/cairnloop_example/lib/cairnloop_example/application.ex`                  | Cairnloop.PubSub registry in supervisor + unique :id atoms        | ✓ VERIFIED | Both PubSub children via `Supervisor.child_spec/2` (auto-fix recorded in SUMMARY 02 — duplicate :id collision was a Rule 1 issue surfaced + fixed during Plan 02 execution).                                    |
| `lib/cairnloop/chat.ex`                                                            | create_customer_conversation/1, ingest_widget_message/2, get_message/1, broadcast_safely/2, OQ-1 broadcast inside reply_to_conversation/4 | ✓ VERIFIED | All 4 new functions present + OQ-1 case-as-side-effect-statement preserves `{result, meta}` trailing expression. 22 tests pass in chat_test.exs.                                                                |
| `lib/cairnloop/web/conversation_live.ex`                                           | handle_info({:message_created, _}) clause                          | ✓ VERIFIED | Line 47 — body mirrors existing :draft_created/:tool_executed clauses.                                                                                                                                          |
| `lib/cairnloop/web/inbox_live.ex`                                                  | PubSub subscribe in mount/3 + handle_info({:conversations_changed}) | ✓ VERIFIED | Subscribe at line 88, handle_info at line 321 with reload + prune.                                                                                                                                              |
| `lib/cairnloop/channels/widget_channel.ex`                                         | Rewritten join lobby + handle_in("new_message"); CSAT + private-room join preserved | ✓ VERIFIED | All four behaviors confirmed. Sealed-contract negative gate green (no oban indirection).                                                                                                                       |
| `lib/cairnloop/workers/process_message.ex`                                         | unique: header + two-clause perform/1                              | ✓ VERIFIED | Header at line 28-30 with exact D-07 shape; widget + email clauses present.                                                                                                                                     |
| `examples/cairnloop_example/lib/cairnloop_example_web/endpoint.ex`                 | socket "/widget", Cairnloop.Channels.WidgetSocket, websocket: true, longpoll: false | ✓ VERIFIED | Lines 18-25 — placed IMMEDIATELY AFTER /live socket per D-15.                                                                                                                                                   |
| `examples/cairnloop_example/lib/cairnloop_example_web/live/chat_live.ex`           | Full rewrite + colocated WidgetChat JS hook + UI-SPEC §1 compliance | ✓ VERIFIED | ~300 LOC rewrite. All UI-SPEC contractual elements present.                                                                                                                                                       |
| `examples/cairnloop_example/README.md`                                             | ## Two-Tab Demo section verbatim from UI-SPEC §3                   | ✓ VERIFIED | Section present at line 32 with both URLs and setup commands.                                                                                                                                                    |
| `examples/cairnloop_example/mix.exs`                                               | Local path dep so example app sees Chat.get_message/1               | ✓ VERIFIED | `{:cairnloop, path: "../.."}` (Rule 3 auto-fix recorded in SUMMARY 03 — necessary because hex 0.1.0 pre-dates Plan 03 additions).                                                                                |
| `test/cairnloop/chat_test.exs`                                                     | Extended with create_customer_conversation, ingest_widget_message, OQ-1, get_message tests | ✓ VERIFIED | 22 tests, 0 failures.                                                                                                                                                                                            |
| `test/cairnloop/channels/widget_channel_test.exs`                                  | Extended with join/3 widget:lobby describe                          | ✓ VERIFIED | 3 tests, 0 failures (handle_in Oban tests relocated to example app — see SUMMARY 02 deviation 3).                                                                                                                |
| `test/cairnloop/workers/process_message_test.exs`                                  | NEW with widget + email branch tests                                | ✓ VERIFIED | 2 tests, 0 failures.                                                                                                                                                                                            |
| `examples/cairnloop_example/test/cairnloop_example/widget_channel_oban_test.exs`    | NEW Oban.Testing tests for handle_in (requires_postgres tagged)     | ✓ VERIFIED | File present; uses `Oban.Testing, repo: CairnloopExample.Repo`; 2 tests gated on `:requires_postgres` (excluded in workspace without Postgres — documented in SUMMARY 02).                                       |
| `examples/cairnloop_example/test/cairnloop_example_web/live/chat_live_test.exs`    | NEW file with 8 tests including Test 5b + negative grep            | ✓ VERIFIED | 8 tests, 0 failures.                                                                                                                                                                                            |

### Key Link Verification

| From                                                            | To                                                          | Via                                                                              | Status   | Details                                                                                                                                                                  |
| --------------------------------------------------------------- | ----------------------------------------------------------- | -------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `lib/cairnloop/chat.ex` (ingest_widget_message/2)               | `Cairnloop.PubSub`                                          | `Phoenix.PubSub.broadcast` via `broadcast_safely/2` on two topics                  | ✓ WIRED  | Lines 70-71 broadcast `{:message_created, message.id}` on `"conversation:#{conversation_id}"` AND `{:conversations_changed}` on `"conversations"`. Both call broadcast_safely. |
| `lib/cairnloop/chat.ex` (reply_to_conversation/4 :ok branch)    | ChatLive + ConversationLive subscribers                     | Additive `{:message_created, msg.id}` broadcast on conversation topic (OQ-1)      | ✓ WIRED  | Lines 210-216 case-statement; `{result, meta}` trailing expression preserved per OQ-1 Test 3.                                                                              |
| `lib/cairnloop/web/inbox_live.ex` (mount/3)                     | `Chat.ingest_widget_message/2` + `Chat.create_customer_conversation/1` broadcasts | `Phoenix.PubSub.subscribe` on "conversations" + handle_info reload                | ✓ WIRED  | mount/3:88 subscribe + 321-325 handle_info reload+prune.                                                                                                                  |
| `examples/cairnloop_example/lib/cairnloop_example/application.ex` | `Cairnloop.PubSub` registry                                  | Additive supervisor child via `Supervisor.child_spec/2` with unique :id            | ✓ WIRED  | Line 22 — sits adjacent to `CairnloopExample.PubSub` (line 14); both run side-by-side; Rule 1 :id collision auto-fixed.                                                  |
| `lib/cairnloop/channels/widget_channel.ex` (join)               | `Cairnloop.Chat.create_customer_conversation/1`             | Chat facade call with `host_user_id: socket.assigns[:user_token]`                  | ✓ WIRED  | widget_channel.ex:13-15 — exact wiring.                                                                                                                                  |
| `lib/cairnloop/channels/widget_channel.ex` (handle_in)          | `Cairnloop.Workers.ProcessMessage`                          | `Oban.insert` with conversation_id from `socket.assigns`                          | ✓ WIRED  | widget_channel.ex:45-52 — pipes through `ProcessMessage.new` to `Oban.insert`.                                                                                            |
| `lib/cairnloop/workers/process_message.ex` (widget branch)      | `Cairnloop.Chat.ingest_widget_message/2`                    | Pattern-match on `%Oban.Job{args: %{"channel" => "widget", ...}}`                  | ✓ WIRED  | process_message.ex:35-44 — exact call.                                                                                                                                   |
| `lib/cairnloop/workers/process_message.ex` (email branch)       | `Cairnloop.Ingress.EmailWebhookPlug` (silent secondary)     | Preserved arg shape `%{"channel" => "email", "content" => content}`               | ✓ WIRED  | process_message.ex:46-55 — clause preserves contract; EmailWebhookPlug.ex:19 unchanged.                                                                                  |
| `examples/cairnloop_example/lib/cairnloop_example_web/endpoint.ex` | `Cairnloop.Channels.WidgetSocket`                        | `socket "/widget"` mount                                                          | ✓ WIRED  | endpoint.ex:23-25 — exact mount with `websocket: true, longpoll: false` per D-15.                                                                                         |
| `chat_live.ex` (hook)                                           | `WidgetSocket.connect/3`                                    | `new Socket("/widget", {params: {token}})`                                        | ✓ WIRED  | chat_live.ex:143.                                                                                                                                                         |
| `chat_live.ex` (handle_event conversation_id)                   | `Cairnloop.PubSub`                                          | `Phoenix.PubSub.subscribe(Cairnloop.PubSub, "conversation:#{id}")` after hook delivers id | ✓ WIRED  | chat_live.ex:194-196 — subscribe inside `if connected?(socket)`.                                                                                                          |
| `chat_live.ex` (handle_info {:message_created, _})              | `Cairnloop.Chat.get_message/1`                              | Tolerant lookup + role-based dedup                                                | ✓ WIRED  | chat_live.ex:259-278.                                                                                                                                                     |

### Data-Flow Trace (Level 4)

| Artifact                                                                            | Data Variable                                | Source                                                          | Produces Real Data | Status      |
| ----------------------------------------------------------------------------------- | -------------------------------------------- | --------------------------------------------------------------- | ------------------ | ----------- |
| `chat_live.ex` (@messages)                                                          | Optimistic appends + PubSub-driven appends   | `handle_event("send_message")` + `handle_info({:message_created, _})` → `Chat.get_message/1` | Yes — DB-backed via `repo().get/2` | ✓ FLOWING  |
| `chat_live.ex` (@channel_status)                                                    | atom from JS hook event                       | `handle_event("channel_status")` ← hook `pushEvent("channel_status", ...)` | Yes — runtime, atom-table-safe | ✓ FLOWING  |
| `chat_live.ex` (@conversation_id)                                                   | string id from JS hook event                 | `handle_event("conversation_id")` ← hook reads join `.receive("ok")` reply ← server's `WidgetChannel.join("widget:lobby")` ← `Chat.create_customer_conversation/1` → DB insert | Yes — end-to-end from DB | ✓ FLOWING  |
| `inbox_live.ex` (@conversations)                                                    | List of Conversations                         | `Chat.list_conversations/0` triggered on `{:conversations_changed}` PubSub | Yes — DB query     | ✓ FLOWING  |
| `conversation_live.ex` (operator reply timeline)                                    | Conversation w/ messages preload              | `reload_conversation_with_context/2` triggered on `{:message_created, _}` | Yes — DB query     | ✓ FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                | Command                                                                                                                                | Result                                | Status |
| ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ------ |
| Root library compiles warnings-clean                    | `mix compile --warnings-as-errors`                                                                                                     | exit 0, no warnings                   | ✓ PASS |
| Example app compiles warnings-clean                     | `cd examples/cairnloop_example && mix compile --warnings-as-errors`                                                                     | exit 0, no warnings                   | ✓ PASS |
| Chat facade headless tests pass                         | `mix test test/cairnloop/chat_test.exs --warnings-as-errors`                                                                            | 22 tests, 0 failures                  | ✓ PASS |
| Widget channel + process message headless tests pass    | `mix test test/cairnloop/channels/widget_channel_test.exs test/cairnloop/workers/process_message_test.exs --warnings-as-errors`         | 5 tests, 0 failures                   | ✓ PASS |
| ChatLive headless tests pass                            | `cd examples/cairnloop_example && mix test test/cairnloop_example_web/live/chat_live_test.exs --warnings-as-errors`                     | 8 tests, 0 failures                   | ✓ PASS |
| Root full suite green (baseline DraftTest tolerated)    | `mix test --warnings-as-errors`                                                                                                        | 1 doctest + 697 tests, 1 failure (baseline DraftTest M005 drift per CLAUDE.md) | ✓ PASS (baseline only) |
| Example app full suite green                            | `cd examples/cairnloop_example && mix test --warnings-as-errors`                                                                        | 24 tests, 0 failures                  | ✓ PASS |

### Probe Execution

No conventional `scripts/*/tests/probe-*.sh` probes exist in this Elixir project (verified `find scripts -path '*/tests/probe-*.sh' -type f` returns empty). Phase 28 PLAN/SUMMARY documents do not declare any probe scripts. Probe execution does not apply.

### Requirements Coverage

| Requirement | Source Plan(s) | Description                                                                                                                                                                                       | Status      | Evidence                                                                                                                                                                          |
| ----------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CHAT-01     | Plan 03        | `examples/cairnloop_example/lib/cairnloop_example_web/endpoint.ex` mounts `Cairnloop.Channels.WidgetSocket` at its canonical socket path — the example endpoint is currently missing this mount.   | ✓ SATISFIED | endpoint.ex:23 — `socket "/widget", Cairnloop.Channels.WidgetSocket, websocket: true, longpoll: false`. Truth 1 verified.                                                          |
| CHAT-02     | Plans 01+02+03 | `examples/.../chat_live.ex` is rewritten from its current 51-LOC mock (`Process.send_after(self(), :bot_reply, 1000)`) to push customer messages through `WidgetChannel` and receive operator replies via PubSub — no mock bot reply path remains. | ✓ SATISFIED (source level); ? NEEDS HUMAN (live UAT) | Source-level: complete rewrite with real channel client + colocated hook + PubSub subscribe + role-dedup handle_info. Negative greps green (Process.send_after = 0, :bot_reply = 0). Live two-tab round trip awaits human verification. |
| CHAT-03     | Plan 03        | A two-tab demo doc snippet (operator inbox + customer `/chat`) is added to the example app README showing the end-to-end customer→operator→customer round trip on the local dev server.            | ✓ SATISFIED | README:32 `## Two-Tab Demo`; both URLs + setup commands present. Truth 3 verified.                                                                                                |

### Anti-Patterns Found

| File                                                                                | Line  | Pattern                                                                                                       | Severity   | Impact                                                                                                                                                              |
| ----------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `examples/cairnloop_example/README.md`                                              | 69    | "Visit /chat to interact with the **mock** customer ChatLive view" — stale mock reference outside `## Two-Tab Demo` and updated bullet at line 52 | ℹ️ Info     | Documentation lag in the `## Routing` section: line 52 was correctly updated to "Customer Chat (real ingress)" but line 69 still calls the ChatLive view "mock." Pre-existing copy, not blocking goal — but worth a follow-up nit. |
| `examples/cairnloop_example/lib/cairnloop_example_web/live/chat_live.ex`            | 213   | `String.to_existing_atom(status)` with no rescue                                                              | ⚠️ Warning | REVIEW.md CR-01: an unexpected status string crashes the LV process. JS hook only emits known atoms in practice; abnormal/tampered values produce a clean crash + restart (Phoenix re-mounts). Documented as intentional DoS mitigation in source + REVIEW.md; reviewer recommends `case` fallback. NOT a goal blocker. |
| `lib/cairnloop/channels/widget_channel.ex`                                          | 61-71 | `handle_in("submit_csat", ...)` clause crashes on `widget:lobby` topic (destructure mismatch + `repo().get!`) | ⚠️ Warning | REVIEW.md CR-02: deferred per CONTEXT.md OQ-4 and explicitly preserved byte-for-byte. Customer demo path never sends `submit_csat` on `widget:lobby`. Not exercised in Phase 28 goal — but a tampered client could crash the channel. NOT a goal blocker. |
| `lib/cairnloop/channels/widget_channel.ex`                                          | 48-52 | `Oban.insert/1` result silently discarded; customer always sees `:ok`                                          | ⚠️ Warning | REVIEW.md WR-01: on enqueue failure the message is silently dropped; the JS hook's `.receive("error", ...)` never fires. Inherits the existing channel surface; the Oban `unique:` option dedupes retry storms but does not surface enqueue errors. NOT a goal blocker for the demo path (Oban.insert succeeds in practice when Postgres is up). |
| `lib/cairnloop/workers/process_message.ex`                                          | 40-43 | `:error` return on changeset failure (retryable)                                                              | ⚠️ Warning | REVIEW.md WR-02: changeset failures are deterministic; Oban will retry up to 20 times. Should be `{:cancel, reason}`. Quality issue, not a goal blocker — changeset failures should be rare in the demo path (only `:content` and `:role` are required, and the channel always supplies both). |
| `examples/cairnloop_example/lib/cairnloop_example_web/live/chat_live.ex`            | 245-247 | `send_error` handler does not remove the optimistic ghost message or clear `:pending`                          | ⚠️ Warning | REVIEW.md WR-03: on send error, the optimistic message stays in the thread and `:pending` stays true forever. UX bug, not a goal blocker — `send_error` only fires when the channel push errors out (rare; Oban.insert succeeds normally). |
| `examples/cairnloop_example/lib/cairnloop_example_web/live/chat_live.ex`            | 284-290 | `connection_label/1` and `connection_dot_color/1` lack catch-all clauses                                       | ⚠️ Warning | REVIEW.md WR-04: render raises `FunctionClauseError` on unknown atom. Belt-and-suspenders defense missing. NOT a goal blocker because the four atom values are controlled in source. |
| `examples/cairnloop_example/test/cairnloop_example_web/live/chat_live_test.exs`     | —     | No direct test coverage for `handle_event("channel_status", ...)` or `handle_event("send_error", ...)`         | ℹ️ Info     | REVIEW.md IN-01: gaps in headless coverage. Multiple indirect paths (Test 2 sets `:connected` via handle_event(conversation_id)). NOT a goal blocker. |
| `lib/cairnloop/channels/widget_socket.ex`                                           | 11-13 | Token verification is a stub — any binary token accepted                                                       | ℹ️ Info     | REVIEW.md IN-02: explicit demo posture (host-owned library); the comment at widget_socket.ex:12 acknowledges this and CLAUDE.md "host-owned posture" carries it forward as adopter-supplied. NOT a goal blocker — out of phase scope. |

**No 🛑 BLOCKER anti-patterns found.** All warnings are documented in 28-REVIEW.md and either inherit from sealed phase decisions or are quality follow-ups that do not gate the Phase 28 success criteria.

### Human Verification Required

Goal completeness explicitly requires a live two-tab UAT (per ROADMAP Phase 28 Goal: "a developer can open two browser tabs ... and see it appear on the other side in real time"). Headless test coverage is complete and green, but live PubSub round-trip across two LiveView processes + JS Channel transport cannot be exercised in ExUnit. SUMMARY 03 documents: "Manual UAT not performed in this automated execution (no live Postgres/browser available in workspace)."

#### 1. Two-tab round trip (operator inbox + customer /chat)

**Test:** Open http://localhost:4000/support in Tab 1 and http://localhost:4000/chat in Tab 2 (after `cd examples/cairnloop_example && mix setup && mix phx.server`). Type 'hello from customer' in Tab 2; it appears in Tab 1's inbox without a refresh. Click into the conversation in Tab 1 and reply 'hello from operator'; the reply appears in Tab 2's thread within ~1s.
**Expected:** Customer message flows: WidgetChannel → ProcessMessage → Chat.ingest_widget_message → DB insert → `{:message_created, id}` broadcast → operator's ConversationLive renders it. Operator reply flows: ConversationLive → Chat.reply_to_conversation → DB insert → OQ-1 broadcast `{:message_created, id}` on `"conversation:#{id}"` → customer's ChatLive's `handle_info` calls `Chat.get_message(id)` → `:agent` role → append to `@messages` and clear `:pending`. Pending row "Message sent — waiting on operator." appears after customer send, disappears after operator reply lands.
**Why human:** Headless ExUnit cannot exercise live PubSub round-trip across two browser tabs + JS Channel transport. 28-VALIDATION.md explicitly flags this as manual-only.

#### 2. Multi-message-stack manual UAT (UI-SPEC interaction step 11)

**Test:** In Tab 2 send 'ping one' then 'ping two' back-to-back. The pending 'Message sent — waiting on operator.' row stays visible after both. In Tab 1, click into the new conversation and reply 'got both'. In Tab 2, pending clears and exactly ONE operator bubble appears (not duplicated).
**Expected:** Two customer bubbles stacked, then one operator bubble — no operator bubble duplication, no extra pending state churn.
**Why human:** Locked by headless Test 5b in chat_live_test.exs (`mix test test/.../chat_live_test.exs` exercises the exact assign transitions) — but the live-browser version is the contractual completeness signal. JS hook timing, DOM render order, and PubSub delivery are not fully exercised by direct ExUnit calls into handle_event/handle_info.

#### 3. No mock "Bot reply" / "AI is thinking" / "Ticket submitted" / "Agent will reply" text appears in customer thread at any time

**Test:** After sending a message in Tab 2, observe the thread for ≥ 5 seconds with no operator reply in Tab 1. The previously-mocked 1-second `Process.send_after` auto-reply must NOT appear.
**Expected:** Only the customer's own optimistic bubble + the pending row are visible. No mock copy under any timing condition.
**Why human:** Negative-grep Test 7 covers source-level absence, but runtime confirmation (and confirmation that the operator-reply path is the only source of operator-side messages reaching the customer) needs the live demo.

#### 4. Operator inbox refreshes when a new customer joins /chat (D-09/D-10)

**Test:** With Tab 1 (operator inbox at /support) open and no manual refresh, open a fresh Tab 2 (customer /chat). The new conversation row should appear in Tab 1's inbox within ~1s.
**Expected:** `WidgetChannel.join('widget:lobby')` → `Chat.create_customer_conversation/1` → DB insert + `{:conversations_changed}` broadcast → InboxLive's `handle_info({:conversations_changed}, ...)` calls `Chat.list_conversations/0` and re-assigns. New conversation appears at the top of the inbox.
**Why human:** Cross-LV PubSub timing under a live Phoenix server cannot be exercised headlessly. InboxLive subscribe + handle_info are source-verified (truth 9).

### Gaps Summary

**No gaps.** All 23 observable truths are source-verified in the codebase. All 15 required artifacts exist, are substantive, are wired, and (where applicable) carry real data through the pipeline. All 5 ROADMAP+PLAN key links between modules are confirmed by source inspection. All compile-clean and test-pass gates are green in both build trees (root library carries the documented baseline DraftTest M005 drift per CLAUDE.md; that single failure is not a Phase 28 regression — it is the pre-existing baseline failure recorded in user memory).

The 8 anti-pattern findings carried over from 28-REVIEW.md are all classified as ⚠️ Warning or ℹ️ Info — none are 🛑 BLOCKER for the Phase 28 goal. CR-01 (`String.to_existing_atom/1`) and CR-02 (deferred `submit_csat` crash) are abnormal/tampered-input edge cases that are out of the demo path; WR-01..WR-04 are UX-quality follow-ups that do not gate the success criteria. These are explicitly recorded for follow-up but the phase goal is achieved.

**The reason status is `human_needed`, not `passed`:** the Phase 28 ROADMAP Goal explicitly defines the completeness test as a live two-tab UAT. Every piece of the pipeline is verified in source + headless tests, but the end-to-end PubSub round trip across two browser tabs + a live Phoenix server has not been exercised in this verification pass (no Postgres + browser available in the workspace). The four `human_verification` items above each test one slice of that completeness contract.

---

_Verified: 2026-05-27T22:22:01Z_
_Verifier: Claude (gsd-verifier)_
