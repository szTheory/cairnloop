---
phase: 28-customer-chat-wired-to-real-ingress
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - lib/cairnloop/channels/widget_channel.ex
  - lib/cairnloop/workers/process_message.ex
  - test/cairnloop/channels/widget_channel_test.exs
  - test/cairnloop/workers/process_message_test.exs
autonomous: true
requirements:
  - CHAT-02
tags:
  - phoenix-channel
  - oban-worker
  - elixir
  - email-compat
  - process-message
must_haves:
  truths:
    - "`Cairnloop.Channels.WidgetChannel.join(\"widget:lobby\", _, socket)` calls `Cairnloop.Chat.create_customer_conversation/1` with `%{host_user_id: socket.assigns[:user_token]}`, stores the returned `conversation.id` in `socket.assigns.conversation_id`, and replies `{:ok, %{conversation_id: conversation.id}, socket}` so the JS hook can `pushEvent(\"conversation_id\", {id})` on the LiveView in Plan 03 (D-01)"
    - "The existing conversation-scoped `join(\"widget:\" <> _private_room_id, ...)` clause is preserved byte-for-byte (deferred per CONTEXT.md specifics §122 — Phase 28 stays on `widget:lobby`)"
    - "`Cairnloop.Channels.WidgetChannel.handle_in(\"new_message\", %{\"content\" => content}, socket)` reads `conversation_id` from `socket.assigns[:conversation_id]` (NEVER from the inbound payload — T-M001 input-trust mitigation), and enqueues a `ProcessMessage` Oban job with args `%{channel: \"widget\", conversation_id: conversation_id, content: content}` (D-07)"
    - "`Cairnloop.Workers.ProcessMessage` is rewritten with `unique: [period: 30, fields: [:worker, :args], keys: [:conversation_id, :content]]` on the worker header and TWO clauses on `perform/1` — one for the `\"widget\"` channel that calls `Cairnloop.Chat.ingest_widget_message/2`, and one for the existing `\"email\"` channel that preserves the current logger stub (Pitfall 2 / OQ-2)"
    - "`Cairnloop.Ingress.EmailWebhookPlug` continues to call `ProcessMessage.new(%{channel: \"email\", content: content})` and the email branch returns `:ok` without crashing — the silent secondary caller is preserved"
    - "`test/cairnloop/channels/widget_channel_test.exs` extends with a `describe \"join/3 widget:lobby\"` block (asserts conversation_id is created + stored on the socket) AND a `describe \"handle_in/3 new_message\"` block (asserts Oban.insert is called with conversation_id read from `socket.assigns`)"
    - "`test/cairnloop/workers/process_message_test.exs` is a NEW file with TWO tests: widget-channel branch calls `Chat.ingest_widget_message/2` and returns `:ok`; email-channel branch returns `:ok` and emits the logger line (verified via `ExUnit.CaptureLog.capture_log/1`)"
    - "`mix compile --warnings-as-errors` and `mix test` are green in the root library after this plan ships"
  artifacts:
    - path: "lib/cairnloop/channels/widget_channel.ex"
      provides: "Rewritten join lobby clause + handle_in new_message that uses conversation_id from socket.assigns"
      contains: "conversation_id"
    - path: "lib/cairnloop/workers/process_message.ex"
      provides: "Multi-clause perform/1 with unique: option and Chat.ingest_widget_message/2 call for widget branch"
      contains: "ingest_widget_message"
    - path: "test/cairnloop/channels/widget_channel_test.exs"
      provides: "Coverage for join lobby + handle_in new_message"
      contains: "widget:lobby"
    - path: "test/cairnloop/workers/process_message_test.exs"
      provides: "Headless coverage of widget + email branches of perform/1"
      contains: "ingest_widget_message"
  key_links:
    - from: "lib/cairnloop/channels/widget_channel.ex (join)"
      to: "Cairnloop.Chat.create_customer_conversation/1"
      via: "Chat facade call with host_user_id from socket.assigns.user_token"
      pattern: "Chat\\.create_customer_conversation"
    - from: "lib/cairnloop/channels/widget_channel.ex (handle_in)"
      to: "Cairnloop.Workers.ProcessMessage"
      via: "Oban.insert with conversation_id from socket.assigns"
      pattern: "ProcessMessage\\.new"
    - from: "lib/cairnloop/workers/process_message.ex (widget branch)"
      to: "Cairnloop.Chat.ingest_widget_message/2"
      via: "Chat facade call (worker → context module → post-commit broadcast pattern)"
      pattern: "Chat\\.ingest_widget_message"
    - from: "lib/cairnloop/workers/process_message.ex (email branch)"
      to: "Cairnloop.Ingress.EmailWebhookPlug (silent secondary caller)"
      via: "pattern match on %{\"channel\" => \"email\", \"content\" => content} preserves existing arg shape"
      pattern: "\"channel\" => \"email\""
---

<objective>
Rewire the channel → worker → Chat-facade pipeline so customer messages flow from `WidgetChannel` through `ProcessMessage` and into `Cairnloop.Chat.ingest_widget_message/2` (added in Plan 01). Preserve the silent secondary caller (`EmailWebhookPlug`) by giving `ProcessMessage.perform/1` a multi-clause definition that pattern-matches on `channel`. Add idempotency via the Oban `unique:` option so a channel reconnect cannot duplicate a message within 30 seconds.

Purpose: Plan 01 added the data-tier facade but nothing calls it yet. Plan 02 puts the channel and worker on top of that facade. Plan 03 then mounts the socket on the example endpoint and rewrites ChatLive to push through this pipeline.
Output: Rewritten `widget_channel.ex` (join lobby + handle_in new_message), rewritten `process_message.ex` (multi-clause perform with unique-option header), extended `widget_channel_test.exs`, new `process_message_test.exs`. Email-channel behavior preserved unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-customer-chat-wired-to-real-ingress/28-CONTEXT.md
@.planning/phases/28-customer-chat-wired-to-real-ingress/28-RESEARCH.md
@.planning/phases/28-customer-chat-wired-to-real-ingress/28-PATTERNS.md
@.planning/phases/28-customer-chat-wired-to-real-ingress/28-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key interface contracts the executor needs. Plan 01 added the Chat facade functions; this plan consumes them. -->

From lib/cairnloop/chat.ex (added in Plan 01):
- `def create_customer_conversation(%{host_user_id: token})` → `{:ok, %Cairnloop.Conversation{}}` or `{:error, %Ecto.Changeset{}}`. Broadcasts `{:conversations_changed}` on `"conversations"` post-commit.
- `def ingest_widget_message(conversation_id, content)` → `{:ok, %Cairnloop.Message{role: :user}}` or `{:error, _}`. Broadcasts `{:message_created, msg.id}` on `"conversation:#{id}"` AND `{:conversations_changed}` on `"conversations"` post-commit.

From lib/cairnloop/channels/widget_socket.ex (no change in this plan):
- `connect/3` accepts any binary `params["token"]` and assigns it to `socket.assigns[:user_token]`. Demo mode — any binary token is accepted (validated via `is_binary/1` guard at widget_socket.ex:11).
- `channel("widget:*", Cairnloop.Channels.WidgetChannel)` already routes all `widget:*` topics to this channel.

From lib/cairnloop/channels/widget_channel.ex (current state — to be modified):
- `join("widget:lobby", _payload, socket)` currently does `{:ok, socket}` with no conversation creation. REPLACE with the D-01 conversation-creation flow.
- `join("widget:" <> _private_room_id, _payload, socket)` is the second join clause; PRESERVE byte-for-byte (deferred per CONTEXT.md specifics §122).
- `handle_in("new_message", %{"content" => content}, socket)` currently enqueues `ProcessMessage.new(%{channel: "widget", content: content})`. REPLACE with the D-07 version that reads `conversation_id` from `socket.assigns[:conversation_id]` and includes it in the args.
- `handle_in("submit_csat", %{"rating" => rating}, socket)` is the third handle_in clause; PRESERVE byte-for-byte (it destructures `socket.topic` via `"widget:" <> conversation_id = socket.topic` — that works because CSAT requires conversation-scoped topics; CSAT-from-/chat is deferred per CONTEXT.md OQ-4).

From lib/cairnloop/workers/process_message.ex (current state — to be rewritten):
- Current body is a 12-LOC stub-logger with a single `perform/1` clause matching `%{"channel" => channel, "content" => content}` (ANY channel). REWRITE per D-07.
- The `use Oban.Worker, queue: :default` header gets the `unique:` option added per D-07: `unique: [period: 30, fields: [:worker, :args], keys: [:conversation_id, :content]]`.

From lib/cairnloop/ingress/email_webhook_plug.ex (silent secondary caller — DO NOT MODIFY):
- Line 18-20 calls `%{channel: "email", content: content} |> Cairnloop.Workers.ProcessMessage.new() |> Oban.insert()`. The new ProcessMessage multi-clause `perform/1` MUST preserve this arg shape; the email branch keeps the existing logger stub behavior (per Pitfall 2 / OQ-2).

From lib/cairnloop/automation/workers/draft_worker.ex (canonical Oban.Worker header — lines 1-5):
- Established pattern: `use Oban.Worker, queue: :default, unique: [period: 60, states: [:scheduled]], replace: [scheduled: [:scheduled_at]]`. Phase 28's ProcessMessage `unique:` option uses different fields (`[:conversation_id, :content]`) and period (30s) per D-07.

From test/cairnloop/channels/widget_channel_test.exs (current pattern):
- Uses `%Phoenix.Socket{topic: "widget:123"}` direct stub (NOT `use Phoenix.ChannelTest`) — call `WidgetChannel.handle_in/3` directly with the stub socket and pattern-match the return tuple.
- MockRepo + `Application.put_env(:cairnloop, :repo, MockRepo)` setup pattern is already in place.
- For the new `join/3` widget:lobby test, EXTEND MockRepo with an `insert/1` clause that returns `{:ok, %Cairnloop.Conversation{id: 42, host_user_id: ...}}` (mirrors Plan 01's chat_test.exs MockRepo extension).

From test/cairnloop/workers/sla_countdown_worker_test.exs (sibling worker test convention):
- Headless test pattern — directly call `Worker.perform/1` with a hand-rolled `%Oban.Job{args: %{...}}` struct. No need to run Oban itself. Assertions use the existing MockRepo pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite WidgetChannel join lobby + handle_in new_message (D-01, D-07) and extend widget_channel_test.exs</name>
  <files>
    lib/cairnloop/channels/widget_channel.ex,
    test/cairnloop/channels/widget_channel_test.exs
  </files>
  <read_first>
    lib/cairnloop/channels/widget_channel.ex,
    lib/cairnloop/channels/widget_socket.ex,
    test/cairnloop/channels/widget_channel_test.exs,
    test/cairnloop/chat_test.exs,
    examples/cairnloop_example/config/test.exs,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-CONTEXT.md,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-PATTERNS.md,
    CLAUDE.md
  </read_first>
  <behavior>
    - Behavior 1: `WidgetChannel.join("widget:lobby", _payload, socket)` creates a new conversation via `Cairnloop.Chat.create_customer_conversation(%{host_user_id: socket.assigns[:user_token]})`, stores `conversation.id` in `socket.assigns.conversation_id`, and replies `{:ok, %{conversation_id: conversation.id}, socket}`. On insert failure replies `{:error, %{reason: "could_not_create_conversation"}}` (D-01).
    - Behavior 2: The second `join("widget:" <> _private_room_id, _payload, socket)` clause is PRESERVED byte-for-byte (deferred per CONTEXT.md specifics §122 — Phase 28 stays on `widget:lobby`).
    - Behavior 3: `WidgetChannel.handle_in("new_message", %{"content" => content}, socket)` reads `conversation_id` from `socket.assigns[:conversation_id]` (NEVER from the inbound payload), enqueues `%{channel: "widget", conversation_id: conversation_id, content: content} |> ProcessMessage.new() |> Oban.insert()`, and replies `{:reply, :ok, socket}` (D-07).
    - Behavior 4: The `handle_in("submit_csat", ...)` clause is PRESERVED byte-for-byte.
    - Behavior 5: `test/cairnloop/channels/widget_channel_test.exs` extends with two new describe blocks covering the lobby join + handle_in new_message paths.
  </behavior>
  <action>
    In `lib/cairnloop/channels/widget_channel.ex`:

    1. REPLACE the existing `def join("widget:lobby", _payload, socket)` clause with the D-01 version. The new body:
       - Extract `token = socket.assigns[:user_token]` (Phoenix Channels guarantees this is set by `WidgetSocket.connect/3` — the channel join only fires after a successful socket connect, so the token is present).
       - Call `Cairnloop.Chat.create_customer_conversation(%{host_user_id: token})`.
       - On `{:ok, conversation}` return `{:ok, %{conversation_id: conversation.id}, assign(socket, :conversation_id, conversation.id)}`.
       - On `{:error, _changeset}` return `{:error, %{reason: "could_not_create_conversation"}}`.
       - Add a leading comment that cites D-01 + D-04 (orphan-conversation acceptance) + Pitfall 5 (rejoin race acceptance) + the carried decision that `host_user_id` is `:string` (so the binary token is fine without coercion).

    2. PRESERVE the second `def join("widget:" <> _private_room_id, _payload, socket)` clause byte-for-byte. Do NOT modify it. Add a leading comment that explicitly notes this clause is deferred per CONTEXT.md specifics §122 (Phase 28 stays on `widget:lobby`).

    3. REPLACE the existing `def handle_in("new_message", %{"content" => content}, socket)` body. The new body:
       - Extract `conversation_id = socket.assigns[:conversation_id]` (NEVER read from the payload — T-M001 input-trust mitigation, see Plan 02 threat model).
       - Enqueue `%{channel: "widget", conversation_id: conversation_id, content: content} |> Cairnloop.Workers.ProcessMessage.new() |> Oban.insert()`. **Call `Oban.insert/1` directly — do NOT introduce an `Application.get_env(:cairnloop, :oban_module, Oban)` indirection fallback.** CLAUDE.md "Seal completed phases" forbids adding new indirection contract surface to support tests; the test harness configures Oban in `:manual` mode and uses `Oban.Testing` against `Oban.insert/1` directly (see test guidance below).
       - Reply `{:reply, :ok, socket}`.
       - Add a leading comment that cites D-07 (channel args reshape) + the security note (conversation_id is server-trust state, never client input).

    4. PRESERVE the `def handle_in("submit_csat", %{"rating" => rating}, socket)` clause byte-for-byte. Add a leading comment noting that CSAT-from-/chat is deferred per CONTEXT.md OQ-4 (this clause destructures `socket.topic` which only works on conversation-scoped topics; Phase 28 stays on `widget:lobby` so CSAT submission from the customer widget is out of scope until the channel re-join is implemented in a future phase).

    In `test/cairnloop/channels/widget_channel_test.exs`:

    1. EXTEND MockRepo with an `insert/1` clause that handles a bare `%Ecto.Changeset{}` argument: return `{:ok, Ecto.Changeset.apply_changes(changeset) |> Map.put(:id, 42)}`. This unblocks the `Chat.create_customer_conversation/1` call path inside the new join test.

    2. Ensure `Cairnloop.PubSub` is started in this test file's setup (mirror Plan 01's pattern: `start_supervised!({Phoenix.PubSub, name: Cairnloop.PubSub})` tolerating already-started errors). This is needed because `Chat.create_customer_conversation/1` defensively broadcasts on `"conversations"` — without the registry, the broadcast is silently swallowed (which is fine for production but means the test cannot subscribe and verify; the new test only needs the join return shape, not the broadcast — but the broadcast must not raise).

    3. Add `describe "join/3 widget:lobby"` with tests:
       - Test 1: `"creates a conversation and replies with conversation_id"` — build `socket = %Phoenix.Socket{assigns: %{user_token: "demo_customer"}}`, call `WidgetChannel.join("widget:lobby", %{}, socket)`, pattern-match the return `{:ok, %{conversation_id: id}, ^updated_socket}` where `updated_socket.assigns.conversation_id == id`. Assert `id == 42` (MockRepo sentinel).
       - Test 2: `"replies with error when Chat returns {:error, _}"` — set up a MockRepo override (use `Process.put(:mock_create_conversation_result, {:error, ...})` and have MockRepo's `insert/1` honor it for Conversation changesets) OR document the skip if the override plumbing is heavier than the value. Happy-path-only is acceptable provided the skip is documented.

    4. Add `describe "handle_in/3 new_message"` with tests. **Oban testing harness (LOCKED — do NOT deviate):**
       - The library has its own `Cairnloop.Repo` which CLAUDE.md flags as potentially unavailable in this workspace; the example app's `CairnloopExample.Repo` is always available because Phase 28's pipeline lives in the example app. Configure `Oban.Testing` against the **example app's Oban instance**, not the library's: `use Oban.Testing, repo: CairnloopExample.Repo`. The corresponding `config :cairnloop_example, Oban, testing: :manual` line lives in `examples/cairnloop_example/config/test.exs` — NOT in the library's `config/test.exs`. Before adding it, verify it is not already present: `grep -n "Oban" examples/cairnloop_example/config/test.exs`. If absent, add the single line `config :cairnloop_example, Oban, testing: :manual` in that config file. DO NOT add a duplicate. DO NOT touch the library's `config/test.exs` for Oban — there is no library Oban instance to put into manual mode (Phase 25's sealed boundary).
       - Test 1: `"reads conversation_id from socket.assigns and enqueues ProcessMessage with channel + conversation_id + content"` — build `socket = %Phoenix.Socket{assigns: %{conversation_id: 42}}`, call `WidgetChannel.handle_in("new_message", %{"content" => "hi"}, socket)`, assert `assert_enqueued(worker: Cairnloop.Workers.ProcessMessage, args: %{channel: "widget", conversation_id: 42, content: "hi"})`. If running this test exposes that the example app's Oban configuration is not visible from the library's test lane (the test file lives at `test/cairnloop/channels/widget_channel_test.exs` — library tree), STOP and surface the path mismatch in the SUMMARY. The fix is to add an `oban_testing/3` helper or — preferred — move the test to `examples/cairnloop_example/test/` if needed. **Do NOT introduce an indirection in `widget_channel.ex`** — that would be a sealed-phase contract change. CLAUDE.md is explicit on this point: the executor must NOT add `Application.get_env(:cairnloop, :oban_module, Oban)` to the library code to make testing easier.
       - Test 2: `"never reads conversation_id from the inbound payload (T-M001 security)"` — build `socket = %Phoenix.Socket{assigns: %{conversation_id: 42}}`, call `WidgetChannel.handle_in("new_message", %{"content" => "hi", "conversation_id" => 999}, socket)`. Assert that the enqueued job's `conversation_id` arg is `42` (from socket.assigns), NOT `999` (from payload). Same `assert_enqueued` shape as Test 1.

    5. Existing `describe "handle_in/3 with submit_csat"` tests MUST continue to pass byte-for-byte. The CSAT topic destructure (`"widget:" <> conversation_id = socket.topic`) is unchanged.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/channels/widget_channel_test.exs --warnings-as-errors 2>&1 | tail -15; grep -c "Chat.create_customer_conversation" lib/cairnloop/channels/widget_channel.ex; grep -c "socket.assigns\\[:conversation_id\\]" lib/cairnloop/channels/widget_channel.ex; grep -c "submit_csat" lib/cairnloop/channels/widget_channel.ex; grep -c "Application.get_env(:cairnloop, :oban_module" lib/cairnloop/channels/widget_channel.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0.
    - `mix test test/cairnloop/channels/widget_channel_test.exs --warnings-as-errors` exits 0 with all existing AND new tests green.
    - Source: `grep -c "Chat.create_customer_conversation" lib/cairnloop/channels/widget_channel.ex` returns `1` (called inside `join("widget:lobby", ...)`).
    - Source: `grep -c "socket.assigns\\[:conversation_id\\]" lib/cairnloop/channels/widget_channel.ex` returns ≥ `1` (read inside `handle_in("new_message", ...)`).
    - Source: `grep -c "submit_csat" lib/cairnloop/channels/widget_channel.ex` returns `1` — the existing CSAT clause is preserved.
    - **Sealed-contract negative gate: `grep -c "Application.get_env(:cairnloop, :oban_module" lib/cairnloop/channels/widget_channel.ex` returns `0`** (no test-driven indirection added to the sealed channel — CLAUDE.md "Seal completed phases" invariant).
    - Sealed second join clause preserved: `grep -c "widget:\" <> _private_room_id" lib/cairnloop/channels/widget_channel.ex` returns `1` (the second join clause). `grep -B1 -A4 "widget:\" <> _private_room_id" lib/cairnloop/channels/widget_channel.ex` shows the body still does `if socket.assigns[:user_token], do: {:ok, socket}, else: {:error, ...}` (unchanged).
    - Security source assertion: the new `handle_in("new_message", %{"content" => content}, socket)` clause does NOT read `conversation_id` from the payload pattern — `grep -B1 -A6 "handle_in(\"new_message\"" lib/cairnloop/channels/widget_channel.ex | grep -c "\"conversation_id\""` returns `0` (the payload pattern is `%{"content" => content}`, with NO `"conversation_id"` key, so the channel cannot honor a client-forged id).
    - Oban-testing path correctness: `grep -c "use Oban.Testing" test/cairnloop/channels/widget_channel_test.exs` returns `1` AND that line uses `repo: CairnloopExample.Repo` (or the test file documents the path-mismatch decision in the SUMMARY). `grep -c "config :cairnloop_example, Oban, testing: :manual" examples/cairnloop_example/config/test.exs` returns ≥ `1` (idempotent — only add if missing).
    - Behavior: `mix test test/cairnloop/channels/widget_channel_test.exs` reports ≥ 2 new tests in the lobby join describe + ≥ 1 new test in the handle_in new_message describe (the T-M001 security test).
  </acceptance_criteria>
  <done>
    WidgetChannel lobby join creates a Conversation via the Chat facade and stores `conversation_id` in socket assigns; handle_in new_message uses the server-trust `conversation_id` and enqueues ProcessMessage with the D-07 args shape via the direct `Oban.insert/1` call (no indirection); existing private-room join + CSAT handlers unchanged; tests pass using the example-app `Oban.Testing` harness.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite ProcessMessage with multi-clause perform/1 + unique-option header + NEW headless tests</name>
  <files>
    lib/cairnloop/workers/process_message.ex,
    test/cairnloop/workers/process_message_test.exs
  </files>
  <read_first>
    lib/cairnloop/workers/process_message.ex,
    lib/cairnloop/ingress/email_webhook_plug.ex,
    lib/cairnloop/automation/workers/draft_worker.ex,
    test/cairnloop/workers/sla_countdown_worker_test.exs,
    test/cairnloop/chat_test.exs,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-CONTEXT.md,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-RESEARCH.md,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-PATTERNS.md
  </read_first>
  <behavior>
    - Behavior 1: `Cairnloop.Workers.ProcessMessage` uses `Oban.Worker` with `queue: :default, unique: [period: 30, fields: [:worker, :args], keys: [:conversation_id, :content]]` — a duplicate enqueue within 30 seconds for the same `(conversation_id, content)` pair is deduped by Oban (idempotency guard on channel reconnect, per D-07).
    - Behavior 2: `perform/1` has TWO clauses: one matches `%Oban.Job{args: %{"channel" => "widget", "conversation_id" => id, "content" => content}}` and calls `Cairnloop.Chat.ingest_widget_message(id, content)` (returns `:ok` on success, `:error` on failure); one matches `%Oban.Job{args: %{"channel" => "email", "content" => content}}` and preserves the existing logger stub (returns `:ok`).
    - Behavior 3: `Cairnloop.Ingress.EmailWebhookPlug` continues to call `ProcessMessage.new(%{channel: "email", content: content})` unchanged — Pitfall 2 / OQ-2 closed.
    - Behavior 4: `test/cairnloop/workers/process_message_test.exs` is a NEW headless test file with two tests covering both branches. Uses MockRepo + start_supervised PubSub setup mirror.
    - Behavior 5: `mix compile --warnings-as-errors` clean. `mix test` green.
  </behavior>
  <action>
    REWRITE `lib/cairnloop/workers/process_message.ex` per D-07 + Pitfall 2 / OQ-2:

    1. Replace the worker header `use Oban.Worker, queue: :default` with the D-07 version: `use Oban.Worker, queue: :default, unique: [period: 30, fields: [:worker, :args], keys: [:conversation_id, :content]]`. This dedupes any duplicate enqueue within 30 seconds for the same conversation+content pair — guards against channel-reconnect retry loops (Pitfall 5 acceptance acknowledged; this is the partial mitigation).

    2. Replace the single-clause `perform/1` with TWO clauses:
       - First clause matches `%Oban.Job{args: %{"channel" => "widget", "conversation_id" => id, "content" => content}}` and calls `Cairnloop.Chat.ingest_widget_message(id, content)`. Pattern-match the return: `{:ok, _message} -> :ok` and `{:error, _changeset} -> :error`. Per the worker → context-facade → broadcast pattern, the broadcast itself lives inside `Chat.ingest_widget_message/2` (already shipped in Plan 01), not here.
       - Second clause matches `%Oban.Job{args: %{"channel" => "email", "content" => content}}` and preserves the existing logger stub: `require Logger; Logger.info("Processed email message: #{content}"); :ok`. Add a leading comment explicitly citing Pitfall 2 / OQ-2 — email-channel handling stays as the existing stub until a future host-integration phase wires it into a Conversation. The `EmailWebhookPlug` at `lib/cairnloop/ingress/email_webhook_plug.ex:18-20` continues to call `ProcessMessage.new(%{channel: "email", content: content})` unchanged; the new clause exists specifically to keep that call site working under D-07's reshape.

    3. Add a module-level `@moduledoc` (one paragraph) explaining the two-channel pattern-match: widget messages flow into `Cairnloop.Chat.ingest_widget_message/2`; email messages are a sealed stub until a future phase. Cite Phase 28 D-07 + Pitfall 2 + the canonical "Worker → context facade → broadcast" pattern from `draft_worker.ex:101-114`. Do NOT add any runtime branching beyond the two `perform/1` clauses (no `case channel do` switch — the pattern-match in the function head is the dispatch).

    CREATE `test/cairnloop/workers/process_message_test.exs` (NEW file):

    1. Module shell: `defmodule Cairnloop.Workers.ProcessMessageTest do use ExUnit.Case, async: false; alias Cairnloop.Workers.ProcessMessage; ...`.

    2. MockRepo: define an inline `defmodule MockRepo` (mirror the `test/cairnloop/chat_test.exs` pattern) with an `insert/1` clause that handles a `%Ecto.Changeset{}` argument by returning `{:ok, Ecto.Changeset.apply_changes(changeset) |> Map.put(:id, 777)}`. No `transaction/1` reducer needed because `Chat.ingest_widget_message/2` does NOT use Multi.

    3. `setup` block: `Application.put_env(:cairnloop, :repo, MockRepo); start_supervised!({Phoenix.PubSub, name: Cairnloop.PubSub}); on_exit(fn -> Application.delete_env(:cairnloop, :repo) end); :ok`. Tolerate the `Phoenix.PubSub` "already started" case.

    4. Tests:
       - Test 1: `"widget channel ingests via Chat.ingest_widget_message/2"` — subscribe `Phoenix.PubSub.subscribe(Cairnloop.PubSub, "conversation:1")`, build `job = %Oban.Job{args: %{"channel" => "widget", "conversation_id" => 1, "content" => "hi"}}`, call `assert :ok = ProcessMessage.perform(job)`, then `assert_receive {:message_created, _id}, 200` (proves the worker → facade → broadcast pattern fires end-to-end).
       - Test 2: `"email channel preserves logger stub (Pitfall 2)"` — use `ExUnit.CaptureLog.capture_log/1`: assert `log = capture_log(fn -> :ok = ProcessMessage.perform(%Oban.Job{args: %{"channel" => "email", "content" => "hello"}}) end); assert log =~ "Processed email message: hello"`. This locks the secondary caller's contract.
       - Optional test 3: `"unique option dedupes duplicate enqueues within 30s"` — pure shape test of the worker module config: `assert ProcessMessage.__worker__(:queue) == :default` (Oban exposes config via `__worker__/1`). The actual dedup happens at the Oban.insert site, not in perform — this test just locks the header config from regressing. Skip if the introspection accessor is not stable in the Oban version in deps; the grep gate below covers the same shape.

    5. Existing `Cairnloop.Ingress.EmailWebhookPlug` MUST NOT be modified. Per CLAUDE.md "Seal completed phases" + the sealed-contract invariant, leave that file untouched — the new ProcessMessage email clause is the compat layer.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/workers/process_message_test.exs --warnings-as-errors 2>&1 | tail -15; grep -c "ingest_widget_message" lib/cairnloop/workers/process_message.ex; grep -c "\"channel\" => \"email\"" lib/cairnloop/workers/process_message.ex; grep -c "unique:" lib/cairnloop/workers/process_message.ex; grep -c "ProcessMessage.new" lib/cairnloop/ingress/email_webhook_plug.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 in the root library.
    - `mix test test/cairnloop/workers/process_message_test.exs --warnings-as-errors` exits 0 with both new tests green.
    - `mix test --warnings-as-errors` exits 0 in the root library (baseline DraftTest failure aside, per CLAUDE.md memory).
    - Source: `grep -c "ingest_widget_message" lib/cairnloop/workers/process_message.ex` returns `1`.
    - Source: `grep -c "\"channel\" => \"email\"" lib/cairnloop/workers/process_message.ex` returns `1` (the email branch pattern-match in `perform/1`).
    - Source: `grep -c "unique:" lib/cairnloop/workers/process_message.ex` returns `1` (in the worker header).
    - Source: `grep -c "fields: \\[:worker, :args\\]" lib/cairnloop/workers/process_message.ex` returns `1` (D-07 unique option shape).
    - Source: `grep -c "keys: \\[:conversation_id, :content\\]" lib/cairnloop/workers/process_message.ex` returns `1`.
    - Sealed secondary caller untouched: `grep -c "ProcessMessage.new" lib/cairnloop/ingress/email_webhook_plug.ex` returns `1` AND `git diff lib/cairnloop/ingress/email_webhook_plug.ex` is empty.
    - Behavior: the widget test asserts a `{:message_created, _}` broadcast on `"conversation:1"`, proving the worker → Chat → PubSub chain works end-to-end with the Plan 01 facade.
  </acceptance_criteria>
  <done>
    ProcessMessage rewritten with the D-07 unique option + two-clause perform/1; widget branch calls the Plan 01 Chat facade and the broadcast fires through; email branch preserves the existing stub; secondary caller (EmailWebhookPlug) is untouched; new headless test file covers both branches; suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer browser → WidgetSocket | Demo mode accepts any binary token (V2 ASVS deferred per WidgetSocket comment). Real authentication is a future host-integration concern. |
| WidgetChannel handle_in payload → server | Customer-supplied `content` is the only user-tainted field; `conversation_id` is read EXCLUSIVELY from `socket.assigns` (server-trust state). |
| Oban worker → Chat facade | `ProcessMessage.perform/1` calls into `Cairnloop.Chat.ingest_widget_message/2`; the facade owns the post-commit broadcast emit point. |
| ProcessMessage email branch ↔ EmailWebhookPlug | Sealed secondary caller; the new clause exists specifically to keep `EmailWebhookPlug`'s arg shape working under D-07's reshape. |
| Sealed library code ↔ test harness | Tests configure `Oban.Testing` against `CairnloopExample.Repo` (example app's Oban instance); library code does NOT add indirection to ease test discovery — CLAUDE.md sealed-phase invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Tampering / Elevation | Customer forges `conversation_id` in `handle_in("new_message", ...)` payload to inject messages into another conversation | mitigate | `WidgetChannel.handle_in("new_message", %{"content" => content}, socket)` pattern-matches ONLY `"content"` — the `conversation_id` is read from `socket.assigns[:conversation_id]` set during `join/3`. A client-side payload with `"conversation_id"` key is silently ignored. Acceptance criterion enforces a grep of `0` for `"conversation_id"` in the payload pattern. |
| T-28-02-02 | Tampering | Customer sends oversized `content` (DoS via large message) | accept | `Cairnloop.Message.changeset/2` validates `content` is present but does not currently enforce a `validate_length` cap. Phase 28 inherits the existing message-content surface; bounded length is a follow-up (RESEARCH.md "Standard Mitigations" Section). Out of phase scope. |
| T-28-02-03 | Denial of Service | Channel reconnect storm enqueues duplicate `ProcessMessage` jobs | mitigate | Oban `unique:` option on the worker header (period 30s, fields `[:worker, :args]`, keys `[:conversation_id, :content]`) dedupes identical `(conversation_id, content)` enqueues within the window. Partial mitigation — full TTL reaper of orphan conversations is deferred per D-04. |
| T-28-02-04 | Repudiation | Email branch logs to Logger only, no audit trail | accept | The email branch behavior is sealed and unchanged from before Phase 28 (Pitfall 2 / OQ-2 explicit). Future host-integration phase will wire email ingress to `Cairnloop.Auditor`. |
| T-28-02-05 | Information Disclosure | `unique:` option's `keys: [:conversation_id, :content]` exposes message content in Oban metadata | accept | Oban stores job args in the unique key by design; this is no different from the existing `DraftWorker` `unique:` config. Operator-only DB access. No external exposure. |
| T-28-02-06 | Spoofing | Channel join clause uses `socket.assigns[:user_token]` set in `WidgetSocket.connect/3` which accepts any binary token in demo mode | accept | Per `widget_socket.ex:9` "T-M001-01 Mitigation: Validate user auth tokens or signatures during socket connection" — this is acknowledged demo behavior. Production adopters bring their own token verification (host-owned library posture). Out of phase scope. |
| T-28-02-07 | Tampering | Test-driven indirection added to sealed channel code (`Application.get_env(:cairnloop, :oban_module, Oban)`) to ease test stubbing — would weaken the sealed contract | mitigate | Acceptance criterion enforces `grep -c "Application.get_env(:cairnloop, :oban_module" lib/cairnloop/channels/widget_channel.ex` returns `0`. Tests configure `Oban.Testing` against `CairnloopExample.Repo` instead. CLAUDE.md "Seal completed phases" invariant. |
| T-28-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced by this plan. RESEARCH.md Package Legitimacy Audit applies: phase 28 adds no new dependencies. Slopcheck not applicable. |
</threat_model>

<verification>
After both tasks complete:

```
cd /Users/jon/projects/cairnloop
mix compile --warnings-as-errors
mix test --warnings-as-errors
```

Both must exit 0. The root `mix test` carries a known baseline DraftTest failure per CLAUDE.md memory; record this in the SUMMARY and confirm no new failures were introduced.

Sanity greps (run from repo root):
```
grep -c "Chat.create_customer_conversation" lib/cairnloop/channels/widget_channel.ex   # → 1
grep -c "socket.assigns\\[:conversation_id\\]" lib/cairnloop/channels/widget_channel.ex # → ≥ 1
grep -c "Application.get_env(:cairnloop, :oban_module" lib/cairnloop/channels/widget_channel.ex # → 0 (sealed-contract negative gate)
grep -c "ingest_widget_message" lib/cairnloop/workers/process_message.ex                # → 1
grep -c "unique:" lib/cairnloop/workers/process_message.ex                              # → 1
grep -c "\"channel\" => \"email\"" lib/cairnloop/workers/process_message.ex             # → 1
grep -c "config :cairnloop_example, Oban, testing: :manual" examples/cairnloop_example/config/test.exs # → ≥ 1
git diff lib/cairnloop/ingress/email_webhook_plug.ex                                    # → empty
```
</verification>

<success_criteria>
- WidgetChannel lobby join creates a conversation via the Plan 01 facade and stores `conversation_id` on the socket.
- WidgetChannel handle_in new_message enqueues `ProcessMessage` with conversation_id from `socket.assigns` (NEVER from payload — T-28-02-01 mitigation) via the direct `Oban.insert/1` call (no test-driven indirection — T-28-02-07 mitigation).
- ProcessMessage worker uses the D-07 unique-option header + two-clause perform/1 (widget calls Chat facade, email preserves stub).
- EmailWebhookPlug is unchanged (sealed secondary caller).
- New + extended tests cover all behaviors above using `Oban.Testing` against `CairnloopExample.Repo` (example app's Oban instance) and `config :cairnloop_example, Oban, testing: :manual` in the example app's test config.
- `mix compile --warnings-as-errors` + `mix test --warnings-as-errors` green (baseline DraftTest aside).
</success_criteria>

<output>
Create `.planning/phases/28-customer-chat-wired-to-real-ingress/28-02-SUMMARY.md` when done. Record changes by file, test results, any deviations (e.g., if the library-tree test file cannot reach the example-app Oban config and the test had to be relocated), and the open item handed to Plan 03 (endpoint mount + ChatLive rewrite + README — the now-real pipeline is ready to receive messages from the JS hook).
</output>
