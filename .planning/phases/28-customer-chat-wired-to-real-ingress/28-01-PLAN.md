---
phase: 28-customer-chat-wired-to-real-ingress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/lib/cairnloop_example/application.ex
  - lib/cairnloop/chat.ex
  - lib/cairnloop/web/conversation_live.ex
  - lib/cairnloop/web/inbox_live.ex
  - test/cairnloop/chat_test.exs
autonomous: true
requirements:
  - CHAT-02
tags:
  - chat-facade
  - pubsub
  - elixir
  - phoenix
  - liveview
  - additive-broadcast
must_haves:
  truths:
    - "Example app supervisor (`CairnloopExample.Application`) starts `{Phoenix.PubSub, name: Cairnloop.PubSub}` immediately after `{Phoenix.PubSub, name: CairnloopExample.PubSub}` so library broadcasts no longer crash with `unknown registry: Cairnloop.PubSub` (Pitfall 1)"
    - "`Cairnloop.Chat.create_customer_conversation/1` exists with the signature `create_customer_conversation(%{host_user_id: token})`, inserts a `%Cairnloop.Conversation{status: :open, subject: \"Customer chat\"}`, broadcasts `{:conversations_changed}` on topic `\"conversations\"` via `Cairnloop.PubSub`, and returns `{:ok, conversation}` on success (D-05)"
    - "`Cairnloop.Chat.ingest_widget_message/2` exists with the signature `ingest_widget_message(conversation_id, content)`, inserts a `%Cairnloop.Message{role: :user}` via a plain `Cairnloop.Message.changeset/2` insert (NOT through `reply_to_conversation/4`, which would trigger `DraftWorker` for the `:user` branch), then post-commit broadcasts `{:message_created, message.id}` on `\"conversation:#{conversation_id}\"` AND `{:conversations_changed}` on `\"conversations\"` (D-06)"
    - "`Cairnloop.Chat.reply_to_conversation/4`'s `:ok` branch (additive only, sealed contract preserved) post-commit broadcasts `{:message_created, results.message.id}` on `\"conversation:#{conversation.id}\"` so the customer's ChatLive can render operator replies via PubSub (OQ-1 fix, defensive `try/rescue _ -> :ok` wrapper)"
    - "`Cairnloop.Web.ConversationLive` has a new `handle_info({:message_created, _message_id}, socket)` clause that calls `reload_conversation_with_context/2` — mirrors the existing `{:draft_created, _}` / `{:tool_executed, _}` clauses verbatim (D-17)"
    - "`Cairnloop.Web.InboxLive.mount/3` subscribes to `\"conversations\"` on `Cairnloop.PubSub` inside the `connected?(socket)` block, and a new `handle_info({:conversations_changed}, socket)` clause reloads via `Chat.list_conversations/0` then prunes through the already-wired `prune_selected_ids/2` helper (D-09, D-10)"
    - "`test/cairnloop/chat_test.exs` adds `describe \"create_customer_conversation/1\"`, `describe \"ingest_widget_message/2\"`, and a `describe \"reply_to_conversation/4 broadcast (OQ-1)\"` block that asserts the `{:message_created, _}` broadcast on the conversation topic — all using the existing `MockRepo` + `Application.put_env(:cairnloop, :repo, MockRepo)` setup pattern"
    - "`mix compile --warnings-as-errors` and `mix test` are green in the root library after this plan ships"
  artifacts:
    - path: "examples/cairnloop_example/lib/cairnloop_example/application.ex"
      provides: "Supervisor child list with both PubSub registries running side-by-side"
      contains: "Cairnloop.PubSub"
    - path: "lib/cairnloop/chat.ex"
      provides: "Two new facade functions (create_customer_conversation/1, ingest_widget_message/2) plus an additive post-commit broadcast inside reply_to_conversation/4"
      contains: "create_customer_conversation"
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "Additive handle_info({:message_created, _}, _) clause"
      contains: ":message_created"
    - path: "lib/cairnloop/web/inbox_live.ex"
      provides: "PubSub subscribe in mount/3 + handle_info({:conversations_changed}, _) reload + prune"
      contains: ":conversations_changed"
    - path: "test/cairnloop/chat_test.exs"
      provides: "Headless coverage for D-05, D-06, and OQ-1 broadcast"
      contains: "create_customer_conversation"
  key_links:
    - from: "lib/cairnloop/chat.ex (ingest_widget_message/2)"
      to: "Cairnloop.PubSub"
      via: "Phoenix.PubSub.broadcast on conversation topic + conversations topic"
      pattern: "Phoenix\\.PubSub\\.broadcast\\(Cairnloop\\.PubSub"
    - from: "lib/cairnloop/chat.ex (reply_to_conversation/4 :ok branch)"
      to: "ChatLive + ConversationLive subscribers"
      via: "additive {:message_created, msg.id} broadcast on conversation topic"
      pattern: ":message_created"
    - from: "lib/cairnloop/web/inbox_live.ex (mount/3)"
      to: "Chat.ingest_widget_message/2 + create_customer_conversation/1 broadcasts"
      via: "Phoenix.PubSub.subscribe on conversations topic + handle_info reload"
      pattern: "subscribe.*conversations"
    - from: "examples/cairnloop_example/lib/cairnloop_example/application.ex"
      to: "Cairnloop.PubSub registry"
      via: "additive supervisor child {Phoenix.PubSub, name: Cairnloop.PubSub}"
      pattern: "name: Cairnloop.PubSub"
---

<objective>
Land the data-tier foundation for Phase 28's customer-chat round trip: (a) close the load-bearing example-app PubSub gap so library broadcasts have a place to land, (b) add the two new narrow `Cairnloop.Chat` facade functions that the channel + worker pipeline depend on, (c) add the additive post-commit broadcast inside the sealed `reply_to_conversation/4` so operator replies surface to the customer's `/chat`, and (d) wire the operator-side LiveViews (`ConversationLive`, `InboxLive`) to react to the new PubSub messages without churning sealed reload paths.

Purpose: Plans 02 and 03 cannot ship a working round trip until this plan is green. The example app crashes today on any `Cairnloop.PubSub.subscribe` (Pitfall 1), `Chat.ingest_widget_message/2` does not exist (D-06), `reply_to_conversation/4` broadcasts nothing (OQ-1), and `InboxLive` never refreshes when new conversations appear (D-09/D-10).
Output: Two new Chat facade functions + one additive broadcast inside a sealed function + two LiveView handle_info clauses + a one-line supervisor edit + headless tests covering all of the above.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-customer-chat-wired-to-real-ingress/28-CONTEXT.md
@.planning/phases/28-customer-chat-wired-to-real-ingress/28-RESEARCH.md
@.planning/phases/28-customer-chat-wired-to-real-ingress/28-PATTERNS.md
@.planning/phases/28-customer-chat-wired-to-real-ingress/28-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key interface contracts the executor needs. Extracted from in-repo files. -->
<!-- Executor must respect these — they are sealed or load-bearing. -->

From lib/cairnloop/chat.ex (sealed; only ADDITIONS allowed):
- `defp repo, do: Application.fetch_env!(:cairnloop, :repo)` — single Repo indirection point. All new functions MUST go through `repo()`, not `Cairnloop.Repo` directly. MockRepo substitution depends on this.
- `def reply_to_conversation(conversation_id, content, role \\ :agent, opts \\ [])` — sealed signature. DO NOT change arity, default, or `:user`-vs-`:agent` branching semantics. Only ADDITIVE post-commit broadcast may be inserted inside the `Telemetry.span` callback's `result = repo().transaction(multi)` `{:ok, %{message: message}}` branch.
- `def list_conversations` — already exists; will be re-called by InboxLive's new handle_info.

From lib/cairnloop/conversation.ex:
- `Cairnloop.Conversation.changeset(conversation, attrs)` accepts `[:status, :subject, :host_user_id, :resolved_at, :csat_rating]`; requires `[:status]`.
- `status` is `Ecto.Enum, values: [:open, :resolved, :archived], default: :open`.
- `host_user_id` is `:string` (NOT integer; binary token is fine).

From lib/cairnloop/message.ex:
- `Cairnloop.Message.changeset(message, attrs)` accepts `[:content, :role, :conversation_id, :metadata, :run_key]`; requires `[:content, :role, :conversation_id]`.
- `role` is `Ecto.Enum, values: [:user, :agent, :system, :internal_note, :system_outbound], default: :user`.
- DO NOT set `metadata` or `run_key` for the new widget-ingest path — both default cleanly.

From lib/cairnloop/automation/workers/draft_worker.ex (canonical post-commit broadcast — lines 101-114):
- After a successful Chat-facade call, broadcast on `Cairnloop.PubSub` with topic `"conversation:#{id}"` and message tuple `{:event_name, primary_key_id}`. Worker boundary tolerates broadcast errors (no rescue at the call site).

From lib/cairnloop/workers/tool_execution_worker.ex (defensive variant — lines 580-588):
- Wrap `Phoenix.PubSub.broadcast/3` in `try/rescue _ -> :ok` when the calling path must NOT propagate broadcast failures back. Use this variant inside the sealed `reply_to_conversation/4` so a missing registry can never roll back a committed message.

From lib/cairnloop/web/conversation_live.ex (existing handle_info cluster lines 26-39 — mirror exactly for D-17):
- Existing clauses for `{:draft_created, _}`, `{:tool_executed, _}`, `{:tool_execution_failed, _}` all share the same one-liner body: `{:noreply, reload_conversation_with_context(socket, socket.assigns.conversation.id)}`. The new D-17 clause MUST mirror this byte-for-byte. Mount-time subscribe (lines 13-16) is already in place — no mount change needed.

From lib/cairnloop/web/inbox_live.ex:
- Lines 80-91 contain a "WR-02 forward-compat" dead `if connected?(socket) do … end` comment-only block. Phase 28 IS the future phase referenced there — replace the dead comment block with a real subscribe inside `connected?(socket)`.
- Lines 568-571 already define `prune_selected_ids/2` — call it from the new handle_info.
- `assign(socket, conversations: ..., selected_ids: ..., ...)` is the established assign shape.

From examples/cairnloop_example/lib/cairnloop_example/application.ex (children list — additive only):
- Existing entries: `CairnloopExampleWeb.Telemetry`, `CairnloopExample.Repo`, `{DNSCluster, ...}`, `{Phoenix.PubSub, name: CairnloopExample.PubSub}`, `{Oban, ...}`, `CairnloopExampleWeb.Endpoint`.
- Insert `{Phoenix.PubSub, name: Cairnloop.PubSub}` IMMEDIATELY AFTER the existing `CairnloopExample.PubSub` child so the two named registries sit adjacent (visual clarity). Both run side-by-side with zero conflict — independent named registries.

From test/cairnloop/chat_test.exs (canonical MockRepo + setup pattern lines 1-86):
- `defmodule MockRepo` provides `get!/2`, `transaction/1` reducer (handles `:insert`, `:update`, `:run`, `:merge` inside Multi), `update/1`, `one/1`, `preload/2`.
- A top-level `insert/1` for the new non-Multi facade functions MUST be added (the existing reducer only handles inserts INSIDE a Multi). Copy the inside-Multi `:insert` shape: return `{:ok, Ecto.Changeset.apply_changes(changeset) |> Map.put(:id, 999)}`.
- `setup do; Application.put_env(:cairnloop, :repo, MockRepo); on_exit(fn -> Application.delete_env(:cairnloop, :repo) end); :ok; end`
- `Cairnloop.PubSub` is started in test_helper.exs:24 ONLY when `:integration` is included. The new headless tests for D-05/D-06/OQ-1 broadcast must start a per-test `Phoenix.PubSub` via `start_supervised!({Phoenix.PubSub, name: Cairnloop.PubSub})` (tolerating the "already started" case if integration tests also run).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Cairnloop.PubSub to example app supervisor (Pitfall 1) + ConversationLive {:message_created, _} handle_info (D-17)</name>
  <files>
    examples/cairnloop_example/lib/cairnloop_example/application.ex,
    lib/cairnloop/web/conversation_live.ex
  </files>
  <read_first>
    examples/cairnloop_example/lib/cairnloop_example/application.ex,
    lib/cairnloop/web/conversation_live.ex,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-CONTEXT.md,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-RESEARCH.md,
    CLAUDE.md
  </read_first>
  <behavior>
    - Behavior 1: After this task, `Process.whereis(Cairnloop.PubSub)` returns a pid in `iex -S mix phx.server` for the example app — library broadcasts have a registry to land in (Pitfall 1 closed).
    - Behavior 2: `Cairnloop.Web.ConversationLive` reacts to a `{:message_created, _id}` PubSub message on `"conversation:#{id}"` by calling `reload_conversation_with_context/2` — the operator's open conversation refreshes the moment `Chat.ingest_widget_message/2` (next task) commits a new customer message (D-17).
    - Behavior 3: `mix compile --warnings-as-errors` is clean in both the root library and the example app.
  </behavior>
  <action>
    In `examples/cairnloop_example/lib/cairnloop_example/application.ex`, insert `{Phoenix.PubSub, name: Cairnloop.PubSub}` into the `children` list IMMEDIATELY AFTER the existing `{Phoenix.PubSub, name: CairnloopExample.PubSub}` line (per D-08 + Pitfall 1; the two registries are independent named processes and run side-by-side with zero conflict — `CairnloopExample.PubSub` stays the endpoint's `pubsub_server`, `Cairnloop.PubSub` carries cross-LV and worker broadcasts from the library).

    In `lib/cairnloop/web/conversation_live.ex`, add a new `def handle_info({:message_created, _message_id}, socket)` clause IMMEDIATELY AFTER the existing `{:tool_execution_failed, _}` clause (keep the handle_info cluster contiguous). The body is the one-liner `{:noreply, reload_conversation_with_context(socket, socket.assigns.conversation.id)}` — mirror the existing `{:draft_created, _}` and `{:tool_executed, _}` clauses exactly per D-17. Add a leading comment that cites Phase 28 D-17 and notes the topic (`conversation:#{id}`) plus the broadcasters (Chat.ingest_widget_message/2 and Chat.reply_to_conversation/4). The mount-time subscribe at lines 13-16 already exists; do NOT modify it.

    Do NOT touch `Cairnloop.Application` (the host-owned library application module). Per CLAUDE.md "Cairnloop.Application boots ZERO children by design" — the supervisor edit lives ONLY in the example app.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5; cd /Users/jon/projects/cairnloop/examples/cairnloop_example && mix compile --warnings-as-errors 2>&1 | tail -5; cd /Users/jon/projects/cairnloop && grep -c "name: Cairnloop.PubSub" examples/cairnloop_example/lib/cairnloop_example/application.ex; grep -c "{:message_created, _message_id}" lib/cairnloop/web/conversation_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 in the root library AND in `examples/cairnloop_example/` with zero warnings.
    - `grep -c "name: Cairnloop.PubSub" examples/cairnloop_example/lib/cairnloop_example/application.ex` returns exactly `1`.
    - `grep -c "name: CairnloopExample.PubSub" examples/cairnloop_example/lib/cairnloop_example/application.ex` still returns `1` — the existing `CairnloopExample.PubSub` child MUST remain.
    - `grep -c "handle_info({:message_created, _message_id}" lib/cairnloop/web/conversation_live.ex` returns `1`.
    - The new ConversationLive clause body calls `reload_conversation_with_context(socket, socket.assigns.conversation.id)` (verify: `grep -A1 "handle_info({:message_created, _message_id}" lib/cairnloop/web/conversation_live.ex | grep -c "reload_conversation_with_context(socket, socket.assigns.conversation.id)"` returns 1).
    - `Cairnloop.Application` was NOT modified — `git diff lib/cairnloop/application.ex` is empty.
  </acceptance_criteria>
  <done>
    Supervisor edit committed, ConversationLive D-17 clause committed, compile-warnings-as-errors clean in both build trees, grep gates green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chat facade — create_customer_conversation/1, ingest_widget_message/2, reply_to_conversation/4 OQ-1 broadcast + headless tests</name>
  <files>
    lib/cairnloop/chat.ex,
    test/cairnloop/chat_test.exs
  </files>
  <read_first>
    lib/cairnloop/chat.ex,
    lib/cairnloop/conversation.ex,
    lib/cairnloop/message.ex,
    lib/cairnloop/automation/workers/draft_worker.ex,
    lib/cairnloop/workers/tool_execution_worker.ex,
    test/cairnloop/chat_test.exs,
    test/test_helper.exs,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-CONTEXT.md,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-RESEARCH.md,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-PATTERNS.md
  </read_first>
  <behavior>
    - Behavior 1: `Cairnloop.Chat.create_customer_conversation(%{host_user_id: "demo_customer"})` inserts a `%Cairnloop.Conversation{status: :open, subject: "Customer chat", host_user_id: "demo_customer"}` via `repo().insert/1`, broadcasts `{:conversations_changed}` on `Cairnloop.PubSub` topic `"conversations"` (defensive try/rescue), and returns `{:ok, conversation}`. On insert failure returns `{:error, changeset}` and broadcasts nothing (D-05).
    - Behavior 2: `Cairnloop.Chat.ingest_widget_message(conversation_id, content)` inserts a `%Cairnloop.Message{role: :user, content: content, conversation_id: conversation_id}` via `repo().insert/1` (NOT through `reply_to_conversation/4` — per D-06 explicit). On success defensively broadcasts TWO messages: `{:message_created, message.id}` on `"conversation:#{conversation_id}"` and `{:conversations_changed}` on `"conversations"`. Returns `{:ok, message}` on success, `{:error, changeset}` on failure (D-06).
    - Behavior 3: `Cairnloop.Chat.reply_to_conversation/4`'s sealed signature is preserved; the only change is an additive defensive broadcast of `{:message_created, results.message.id}` on `"conversation:#{conversation.id}"` inside the `{:ok, %{message: _} = results}` branch (OQ-1). The `:user`-role DraftWorker branch, SLA Multi.merge logic, auditor wiring, and Telemetry.span semantics are UNCHANGED.
    - Behavior 4: Headless tests in `test/cairnloop/chat_test.exs` cover all three behaviors above, including PubSub message receipt assertions (subscribe in test setup, `assert_receive` with bounded timeout). All tests pass on `mix test`.
  </behavior>
  <action>
    In `lib/cairnloop/chat.ex`, add two new functions and one additive broadcast inside the sealed `reply_to_conversation/4`:

    1. `def create_customer_conversation(attrs) when is_map(attrs)` (per D-05): build a `Cairnloop.Conversation.changeset/2` with `status: :open`, `subject: Map.get(attrs, :subject, "Customer chat")`, and `host_user_id: Map.fetch!(attrs, :host_user_id)`. Call `repo().insert/1`. On `{:ok, conversation}` invoke the defensive broadcast helper for `{:conversations_changed}` on topic `"conversations"`, then return `{:ok, conversation}`. On `{:error, _}` return the error unchanged. Do NOT introduce a Telemetry.span (this is a single-row insert, not a workflow event).

    2. `def ingest_widget_message(conversation_id, content) when is_binary(content)` (per D-06): build a `Cairnloop.Message.changeset/2` with `conversation_id`, `content`, `role: :user`. Call `repo().insert/1`. On `{:ok, message}` invoke the defensive broadcast helper TWICE: first `{:message_created, message.id}` on `"conversation:#{conversation_id}"`, then `{:conversations_changed}` on `"conversations"`. Return `{:ok, message}`. On `{:error, _}` return the error unchanged. Per D-06: DO NOT call `reply_to_conversation/4` — that path triggers DraftWorker for the `:user` branch and is wrong for raw customer ingress.

    3. Add a private helper `defp broadcast_safely(topic, message)` that wraps `Phoenix.PubSub.broadcast(Cairnloop.PubSub, topic, message)` in `try/rescue _ -> :ok` (defensive variant from `tool_execution_worker.ex:580-588`). Use it for all three new emit points in this plan (and for OQ-1 below). Rationale: a missing registry in dev/test must never propagate back to the data-tier `:ok` branch since the insert has already committed.

    4. Modify `reply_to_conversation/4` (OQ-1 additive fix, sealed contract preserved): inside the `Cairnloop.Telemetry.span(..., fn -> … end)` block, after `result = repo().transaction(multi)`, BEFORE the `{result, meta}` tuple returns, pattern-match `result` and — when it matches `{:ok, %{message: %{id: msg_id}}}` — call `broadcast_safely("conversation:#{conversation.id}", {:message_created, msg_id})`. Place the new call BETWEEN `result = repo().transaction(multi)` and `{result, meta}`; the rest of the function body is byte-for-byte unchanged. Do NOT alter the function arity, the `role \\ :agent` default, the `opts \\ []` default, the auditor wiring, the SLA `Multi.merge` logic, or any branch of the `if role == :user do` structure. Add a leading comment that cites Phase 28 OQ-1 and explains the sealed-contract-preserved rationale (reference research Pitfall 3 + OQ-1).

    In `test/cairnloop/chat_test.exs`, EXTEND the existing test file (do not create a new file). Use the existing MockRepo + setup pattern. Add a top-level `setup_all` (or augment `setup`) that ensures `Cairnloop.PubSub` is running for these tests: use `start_supervised!({Phoenix.PubSub, name: Cairnloop.PubSub})` and tolerate `{:error, {:already_started, _}}`. Extend `MockRepo` with a top-level `insert/1` clause that handles a bare `%Ecto.Changeset{}` argument: return `{:ok, Ecto.Changeset.apply_changes(changeset) |> Map.put(:id, 999)}` (mirrors the inside-Multi shape).

    Add THREE new describe blocks AFTER the existing `describe "reply_to_conversation/3"`:

    - `describe "create_customer_conversation/1"` with tests:
      - Test 1: `"inserts conversation with status :open, subject default \"Customer chat\", and host_user_id from attrs"` — assert `{:ok, %Cairnloop.Conversation{status: :open, subject: "Customer chat", host_user_id: "demo_customer"}} = Chat.create_customer_conversation(%{host_user_id: "demo_customer"})`.
      - Test 2: `"broadcasts {:conversations_changed} on \"conversations\" topic"` — subscribe before the call (`Phoenix.PubSub.subscribe(Cairnloop.PubSub, "conversations")`), then call `Chat.create_customer_conversation(...)`, then `assert_receive {:conversations_changed}, 200`.
      - Test 3 (optional, document if skipped): `"returns {:error, changeset} when changeset is invalid"` — if implementing the error branch requires significant MockRepo plumbing, skip with a `# REPO-UNAVAILABLE` style comment that names the gap; happy-path coverage is sufficient because the error branch is identical in shape to existing facade functions.

    - `describe "ingest_widget_message/2"` with tests:
      - Test 1: `"inserts :user-role message via the Message changeset (NOT reply_to_conversation)"` — assert `{:ok, %Cairnloop.Message{role: :user, content: "hi", conversation_id: 1}} = Chat.ingest_widget_message(1, "hi")`. Negative assertion: the return value does NOT contain a `:draft_job` key (proves we are not on the `reply_to_conversation/4` `:user` path).
      - Test 2: `"broadcasts {:message_created, id} on conversation topic"` — subscribe to `"conversation:1"`, call once, `assert_receive {:message_created, _msg_id}, 200`.
      - Test 3: `"also broadcasts {:conversations_changed} on conversations topic"` — subscribe to `"conversations"`, call once, `assert_receive {:conversations_changed}, 200`.

    - `describe "reply_to_conversation/4 broadcast (OQ-1)"` with tests:
      - Test 1: `"post-commit broadcasts {:message_created, msg_id} on conversation topic"` — subscribe to `"conversation:1"`, call `Chat.reply_to_conversation(1, "operator reply", :agent)`, `assert_receive {:message_created, msg_id}, 200`, assert `msg_id == 999` (matches MockRepo's `Map.put(:id, 999)` sentinel).
      - Test 2: `"does not change the sealed :user role + DraftWorker insertion semantics"` — call `Chat.reply_to_conversation(1, "user message", :user)`, assert the existing `results.draft_job.worker == "Cairnloop.Automation.Workers.DraftWorker"` and `results.message.role == :user` are unchanged (mirrors existing test at chat_test.exs:88-103); ALSO assert the broadcast fires for the `:user` branch (it should — OQ-1 broadcast lives outside the `if role == :user do` branch).

    All new tests use bounded `assert_receive ..., 200`. DO NOT modify any existing test in `test/cairnloop/chat_test.exs` — only EXTEND. The existing `describe "reply_to_conversation/3"` tests must continue to pass byte-for-byte.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/chat_test.exs --warnings-as-errors 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 in the root library.
    - `mix test test/cairnloop/chat_test.exs --warnings-as-errors` exits 0 with all existing AND new tests green.
    - Source: `grep -c "def create_customer_conversation" lib/cairnloop/chat.ex` returns `1`.
    - Source: `grep -c "def ingest_widget_message" lib/cairnloop/chat.ex` returns `1`.
    - Source: `grep -c ":message_created" lib/cairnloop/chat.ex` returns ≥ `2` (one inside `ingest_widget_message/2`, one inside the `reply_to_conversation/4` `:ok` branch).
    - Source: `grep -c ":conversations_changed" lib/cairnloop/chat.ex` returns ≥ `2` (one in `create_customer_conversation/1`, one in `ingest_widget_message/2`).
    - Negative: `grep -A 25 "def ingest_widget_message" lib/cairnloop/chat.ex | grep -c "reply_to_conversation"` returns `0` (proves the new function does NOT call into the sealed reply path).
    - Source: `grep -c "defp broadcast_safely" lib/cairnloop/chat.ex` returns `1`.
    - Sealed contract preserved: `grep -n "def reply_to_conversation" lib/cairnloop/chat.ex` shows exactly ONE definition with arity 4 and unchanged defaults (`role \\ :agent, opts \\ []`).
    - Behavior: `mix test test/cairnloop/chat_test.exs` reports ≥ 7 new tests passing across the three new describe blocks.
  </acceptance_criteria>
  <done>
    Both new Chat facade functions ship with their defensive post-commit broadcasts; the additive OQ-1 broadcast lives inside `reply_to_conversation/4`'s `:ok` branch without churning the sealed function; new headless tests cover the broadcast topics and message shapes; `mix test test/cairnloop/chat_test.exs --warnings-as-errors` is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: InboxLive PubSub subscribe + handle_info({:conversations_changed}, _) reload + prune (D-09, D-10)</name>
  <files>
    lib/cairnloop/web/inbox_live.ex
  </files>
  <read_first>
    lib/cairnloop/web/inbox_live.ex,
    lib/cairnloop/web/conversation_live.ex,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-CONTEXT.md,
    .planning/phases/28-customer-chat-wired-to-real-ingress/28-PATTERNS.md
  </read_first>
  <behavior>
    - Behavior 1: When an InboxLive process is mounted on a connected socket, it subscribes to `Cairnloop.PubSub` topic `"conversations"`. The dead "WR-02 forward-compat" comment block at lines 80-91 is replaced with a real subscribe (D-09).
    - Behavior 2: When InboxLive receives `{:conversations_changed}` on the subscribed topic, it reloads via `Chat.list_conversations/0`, prunes `@selected_ids` through `prune_selected_ids/2`, and assigns the new list — the operator inbox refreshes the moment a customer joins `/chat` or a new message lands (D-10).
    - Behavior 3: `mix compile --warnings-as-errors` clean; existing InboxLive tests (if `test/cairnloop/web/inbox_live_test.exs` exists, the `mount/3` test still passes — the new subscribe is gated on `connected?(socket)` which is false in the test's `build_socket()` helper).
  </behavior>
  <action>
    In `lib/cairnloop/web/inbox_live.ex`:

    1. Replace the dead "WR-02 forward-compat" comment block at lines 80-91 (the placeholder explaining "subscribe here when pubsub becomes load-bearing"). Inside `def mount(_params, session, socket)`, insert a real `if connected?(socket) do Phoenix.PubSub.subscribe(Cairnloop.PubSub, "conversations") end` block at the very top of the function body (BEFORE the `conversations = Chat.list_conversations()` line). Keep the existing `{:ok, assign(socket, ...)}` return shape byte-for-byte (do not change any assign key). Update the leading comment to note that Phase 28 has landed the PubSub subscription (cite D-09 + D-10).

    2. Add a new `def handle_info({:conversations_changed}, socket)` clause. Place it AFTER `mount/3` and BEFORE the first `handle_event/3` clause (group by callback type — `mount`, then `handle_info`, then `handle_event` — matching the established ConversationLive layout). Body: assign `conversations = Chat.list_conversations()`, then `selected_ids = prune_selected_ids(socket.assigns.selected_ids, conversations)`, then `{:noreply, assign(socket, conversations: conversations, selected_ids: selected_ids)}`. Add a leading comment citing D-10, the topic (`"conversations"`), the broadcasters (`Chat.create_customer_conversation/1` + `Chat.ingest_widget_message/2`), and the fact that `prune_selected_ids/2` is already wired (line ~568).

    3. Do NOT modify `prune_selected_ids/2` itself — it is already correctly implemented at line 568. Do NOT change the bulk-modal assigns (`bulk_modal_open`, `bulk_preview`, `bulk_refusal`) — they remain at their existing defaults in `mount/3` and are unaffected by the new handle_info (a selected-id pruned out by `prune_selected_ids/2` is correctly removed; the bulk-modal flow already handles empty selection).

    4. Do NOT add new module imports — `Phoenix.PubSub` resolves via its module name.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5; if [ -f test/cairnloop/web/inbox_live_test.exs ]; then mix test test/cairnloop/web/inbox_live_test.exs --warnings-as-errors 2>&1 | tail -10; else echo "no inbox_live_test.exs — skipping per-file test run"; fi; grep -c ":conversations_changed" lib/cairnloop/web/inbox_live.ex; grep -c "Phoenix.PubSub.subscribe(Cairnloop.PubSub" lib/cairnloop/web/inbox_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 in the root library.
    - If `test/cairnloop/web/inbox_live_test.exs` exists, `mix test test/cairnloop/web/inbox_live_test.exs --warnings-as-errors` exits 0 (existing mount test continues to pass — the new subscribe is gated on `connected?(socket)` which is false in the test helper's `build_socket()`).
    - Source: `grep -c ":conversations_changed" lib/cairnloop/web/inbox_live.ex` returns ≥ `1` (the new handle_info clause).
    - Source: `grep -c "Phoenix.PubSub.subscribe(Cairnloop.PubSub" lib/cairnloop/web/inbox_live.ex` returns `1`.
    - Source: `grep -B1 -A4 "handle_info({:conversations_changed}" lib/cairnloop/web/inbox_live.ex | grep -c "prune_selected_ids"` returns ≥ `1` (the new handle_info calls the existing helper).
    - Source: existing `prune_selected_ids/2` function is unchanged — `git diff lib/cairnloop/web/inbox_live.ex` does not show modifications inside the `def prune_selected_ids(...)` body.
    - Whole-suite gate: `mix test --warnings-as-errors` exits 0 (records the carried baseline DraftTest failure in the SUMMARY if present per CLAUDE.md "Cairnloop baseline DraftTest failure" memory).
  </acceptance_criteria>
  <done>
    InboxLive subscribe + handle_info wired, dead WR-02 comment block replaced, `prune_selected_ids/2` is now load-bearing (called from handle_info), `mix compile --warnings-as-errors` clean, root suite green (baseline DraftTest failure aside).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supervisor → BEAM registry | Adding a new named `Phoenix.PubSub` process must not conflict with the existing `CairnloopExample.PubSub` registry — independent names, no shared state. |
| Chat facade → Repo | `repo()` indirection holds; MockRepo substitution works for headless tests. |
| Chat facade → Cairnloop.PubSub | Broadcast emissions are defensive (`try/rescue`) so a missing registry never propagates back into a committed `:ok` branch. |
| sealed `reply_to_conversation/4` → additive broadcast | Sealed-contract invariant per CLAUDE.md "Seal completed phases"; only additive behavior allowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering | `Chat.ingest_widget_message/2` content arg | mitigate | `Cairnloop.Message.changeset/2` casts only `[:content, :role, :conversation_id, :metadata, :run_key]` and requires `[:content, :role, :conversation_id]`. HEEx auto-escapes `content` on render in ConversationLive (V5 ASVS). The function arg is binary-only (guard `is_binary(content)`). |
| T-28-01-02 | Information Disclosure | PubSub topic enumeration via `conversation:#{id}` | accept | Topic namespacing is per-conversation; two customers cannot cross-subscribe to each other's topics without knowing the integer id. The id is already exposed to the operator side at mount time; the customer side only ever subscribes to the conversation it created on join (D-02). |
| T-28-01-03 | Denial of Service | Broadcast inside sealed `reply_to_conversation/4` raising and rolling back | mitigate | Defensive `try/rescue _ -> :ok` wrapper inside `broadcast_safely/2` — the additive OQ-1 broadcast cannot propagate failures back to the sealed function's `:ok` branch (insert has already committed). |
| T-28-01-04 | Repudiation | Customer-ingested message lacks audit trail | accept | Phase 28 D-04 explicitly accepts orphan conversation rows in dev mode. `Cairnloop.Auditor` integration for customer-side widget ingress is deferred to a future host-integration phase. The `host_user_id: token` annotation on the conversation row provides minimum traceability. |
| T-28-01-05 | Elevation of Privilege | `host_user_id` arg manipulation via `Map.fetch!/2` | mitigate | `host_user_id` is set server-side from `socket.assigns[:user_token]` in WidgetChannel.join/3 (Plan 02). The customer browser cannot forge a `host_user_id` through the channel payload because the channel never reads it from `handle_in` args. |
| T-28-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced by this plan. Per the RESEARCH.md Package Legitimacy Audit: phase 28 adds no new dependencies. Slopcheck not applicable. |
</threat_model>

<verification>
After all three tasks complete:

```
cd /Users/jon/projects/cairnloop
mix compile --warnings-as-errors
mix test --warnings-as-errors
cd examples/cairnloop_example && mix compile --warnings-as-errors
```

All three commands must exit 0. The root `mix test` carries a known baseline DraftTest failure per CLAUDE.md memory; record this in the SUMMARY and confirm no new failures were introduced.

Confirmation greps (run from repo root):
```
grep -c "name: Cairnloop.PubSub" examples/cairnloop_example/lib/cairnloop_example/application.ex   # → 1
grep -c "def create_customer_conversation" lib/cairnloop/chat.ex                                   # → 1
grep -c "def ingest_widget_message" lib/cairnloop/chat.ex                                          # → 1
grep -c "defp broadcast_safely" lib/cairnloop/chat.ex                                              # → 1
grep -c ":message_created" lib/cairnloop/web/conversation_live.ex                                  # → ≥ 1
grep -c ":conversations_changed" lib/cairnloop/web/inbox_live.ex                                   # → ≥ 1
grep -c "Phoenix.PubSub.subscribe(Cairnloop.PubSub" lib/cairnloop/web/inbox_live.ex                # → 1
```
</verification>

<success_criteria>
- All three tasks acceptance criteria green.
- `examples/cairnloop_example` boots `Cairnloop.PubSub` in its supervisor tree (Pitfall 1 closed — operator inbox no longer raises `unknown registry` on subscribe).
- `Cairnloop.Chat.create_customer_conversation/1` and `Cairnloop.Chat.ingest_widget_message/2` shipped with defensive post-commit broadcasts.
- `Cairnloop.Chat.reply_to_conversation/4` post-commit broadcasts `{:message_created, msg_id}` on the conversation topic — additive, sealed contract preserved.
- `Cairnloop.Web.ConversationLive` reacts to `{:message_created, _}` PubSub messages (D-17).
- `Cairnloop.Web.InboxLive` subscribes to the `"conversations"` topic and reloads + prunes on `{:conversations_changed}` (D-09, D-10).
- Headless tests cover D-05, D-06, and the OQ-1 broadcast.
- `mix compile --warnings-as-errors` clean in both build trees.
- `mix test --warnings-as-errors` green in the root library (baseline DraftTest failure aside, per CLAUDE.md memory).
</success_criteria>

<output>
Create `.planning/phases/28-customer-chat-wired-to-real-ingress/28-01-SUMMARY.md` when done. Per the templates/summary.md format: record the changes by file, the test results, any deviations from the plan, and any open items handed to Plan 02 (e.g., "ProcessMessage is still the stub-logger — Plan 02 will rewrite it to call `Chat.ingest_widget_message/2`").
</output>
