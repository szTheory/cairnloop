---
phase: 40-drift-remediation-brand-token-gate-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/web/search_modal_component.ex
autonomous: true
requirements: [DRIFT-01]
must_haves:
  truths:
    - "search_modal_component.ex contains zero off-palette hex or rgba()/hsl()"
    - "KB/case source badges render via cl_chip variant=success|info (no inline rgba/hex pill helpers)"
    - "Active/inactive result rows and panels use existing solid surface/border tokens (glass aesthetic dropped per D-01)"
    - "mix compile --warnings-as-errors passes; the existing brand-token gate stays green"
  artifacts:
    - path: "lib/cairnloop/web/search_modal_component.ex"
      provides: "On-palette search modal with chip-based source/trust badges"
      contains: "cl_chip"
  key_links:
    - from: "source/trust badge call sites"
      to: "cl_chip variant=success|info"
      via: "Phoenix.Component function-component calls"
      pattern: "<\\.cl_chip variant=\"(success|info)\""
---

<objective>
Remediate all `rgba()` and helper-returned `#hex` in `lib/cairnloop/web/search_modal_component.ex`
per the D-01 snap-to-solid-tokens strategy: snap white-alpha panels to `--cl-surface*`, the basalt
text-opacity steps to the 3-tier text scale, border hairlines to `--cl-border`, and replace the
inline-pill `source_badge_style/1` / `trust_badge_style/1` rgba helpers with `cl_chip variant`
(success/info) per DRIFT-01.

Purpose: Search is the heaviest rgba drift surface; this makes it visually consistent with the rest
of the cockpit (solid surfaces, owner-preferred per D-01) and lets the hardened gate land green.
Output: A fully on-palette search modal; the rgba chip-style helpers are deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-drift-remediation-brand-token-gate-hardening/40-CONTEXT.md
@.planning/phases/40-drift-remediation-brand-token-gate-hardening/40-RESEARCH.md

<interfaces>
<!-- Verified live this session — use directly. -->

cl_chip (lib/cairnloop/web/components.ex:69, @status_variants ~w(success info warning danger ai neutral)):
  attr :variant — values include "success" and "info"  (both exist).
  attr :label / :icon / :class / :rest / inner_block slot.
  Renders an inline pill: span class="cl-chip cl-chip--{variant}" with icon+text (never color
  alone — brand 7.5 satisfied). CSS at cairnloop.css:351-353 (.cl-chip--success/info/warning).
  RESEARCH map: KB (#4A6238 olive) -> variant="success"; resolved_case (#3F6F80 slate) -> variant="info".

Solid surface/border tokens (priv/static/cairnloop.css; respond to dark theme at :160+):
  --cl-surface-raised (#FFFFFF light), --cl-surface-sunken (#EFE9DC light), --cl-surface,
  --cl-border, --cl-text, --cl-text-muted.
  There is NO --cl-primary-surface / --cl-primary-border token (verified this session). For the
  active result row primary tint, use existing tokens: border var(--cl-primary) + background
  var(--cl-surface-sunken). Inactive row: border var(--cl-border) + background var(--cl-surface-raised).

Badge call sites (verified): result_row_style/1 used at line ~126; source_badge_style/1 at ~129
  and ~165; trust_badge_style/1 at ~132 and ~168. Helper defs at ~613-635.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snap render-body rgba (panels, text tiers, borders) to solid tokens</name>
  <files>lib/cairnloop/web/search_modal_component.ex</files>
  <read_first>
    - lib/cairnloop/web/search_modal_component.ex (read current render body ~50-214; line numbers from 2026-06-04, may have drifted)
    - .planning/phases/40-drift-remediation-brand-token-gate-hardening/40-RESEARCH.md §B (search rgba inventory) + §A (search render rows)
    - priv/static/cairnloop.css (confirm --cl-surface-raised/-sunken, --cl-text/-muted, --cl-border)
  </read_first>
  <action>
    Apply the D-01 snap map to every rgba() in the render body (NOT the helper fns — those are
    Task 2). Per RESEARCH §B: white-alpha panels rgba(255,255,255,0.72/0.76) (empty-state ~111,
    preview-pane ~162) -> var(--cl-surface-raised). Basalt text steps: rgba(47,36,29,0.62)
    (~105,145,175,198) and rgba(47,36,29,0.68/0.76) (~111,141,207) -> var(--cl-text-muted) (the
    0.62-0.76 muted band, D-01); rgba(47,36,29,0.84) (~181) -> var(--cl-text) (>=0.82 tier). Border
    hairlines rgba(64,51,43,0.08) (~56 border-bottom, ~162 preview border) -> var(--cl-border).
    Keep each as a token-valued inline style="...var(--cl-...)..." where no utility fits (gate-PASS),
    or migrate the element to class="cl-text-muted" where it is a pure color swap. Leave layout-only
    styles (flex/grid/overflow/padding at ~73,74,97,100,101,128,136) UNCHANGED — they carry no color
    literal and are gate-PASS. Line ~149 color:var(--cl-primary) and ~193 color:white (CSS keyword,
    not hex/rgba) are already gate-PASS — leave them. Do NOT add tokens or CSS (D-01).
  </action>
  <verify>
    <automated>grep -nE 'rgba\(|hsl\(' lib/cairnloop/web/search_modal_component.ex | grep -vE 'defp (result_row_style|source_badge_style|trust_badge_style)\(|rgba\(169|rgba\(64, 51|rgba\(255, 255|rgba\(74, 98|rgba\(63, 111|rgba\(47, 36, 29, 0\.82\)' ; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - After Task 1, the only remaining rgba() matches in the file are inside the three helper fns (result_row_style/source_badge_style/trust_badge_style) — the render body is clean
    - `mix compile --warnings-as-errors` exits 0
    - No `class="cl-text"` anywhere (grep -c 'class="cl-text"' returns 0)
    - git diff of priv/static/cairnloop.css is empty (no token/class added — D-01)
  </acceptance_criteria>
  <done>All render-body rgba in search_modal_component.ex snapped to existing solid tokens; layout-only styles untouched; build warnings-clean; cairnloop.css untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace rgba badge/row helpers with cl_chip variants + token-valued row styles</name>
  <files>lib/cairnloop/web/search_modal_component.ex</files>
  <read_first>
    - lib/cairnloop/web/search_modal_component.ex (helper fns ~613-635 + their call sites ~126,129,132,165,168)
    - lib/cairnloop/web/components.ex (cl_chip attrs ~line 64-130 — confirm variant values + label/inner_block)
    - .planning/phases/40-drift-remediation-brand-token-gate-hardening/40-RESEARCH.md §A (search chip-helper rows) + §F gaps #4 and #5
    - priv/static/cairnloop.css:351-353 (.cl-chip--success/info), :38-40 (surface tokens)
  </read_first>
  <action>
    Resolve the two structural gaps with these recorded calls (planner's decision):
    (a) BADGE PRIMITIVE (gap #4) — use `cl_chip variant="success|info"`, NOT cl_source_card. The
    search badges are inline pills; cl_chip is the exact silhouette and exists. At the source-badge
    call sites (~129, ~165) replace `<span style={source_badge_style(...)}>` with
    `<.cl_chip variant="success">` for :knowledge_base and `<.cl_chip variant="info">` for
    :resolved_case (map the source_type -> variant inline or via a small pure presenter clause).
    At the trust-badge call sites (~132, ~168) collapse trust pills onto the SAME chip variant as
    the source badge, differentiated by label only (RESEARCH §B note: the faint 0.08 trust tier is
    part of the dropped glass aesthetic, D-01); use variant="success" for :canonical-equivalent and
    variant="info" otherwise per the existing source-type mapping, label carrying the trust word.
    Then DELETE the now-unused `source_badge_style/1` and `trust_badge_style/1` helper defs entirely
    (~621-635) so no helper-returned hex/rgba remains.
    (b) ACTIVE-ROW TINT (gap #5) — no `--cl-primary-surface`/`--cl-primary-border` token exists
    (verified). Rewrite `result_row_style/1` to return token-valued strings using EXISTING tokens:
    active(true) -> "...border: 1px solid var(--cl-primary); background: var(--cl-surface-sunken); ..."
    (keep the non-color layout props: width/text-align/padding/border-radius/cursor); inactive(false)
    -> "...border: 1px solid var(--cl-border); background: var(--cl-surface-raised); ...". This is a
    token-valued return string -> gate-PASS, zero CSS churn, no new token (D-01). Keep the helper as
    a returned-string (do NOT move to a CSS class — that would touch cairnloop.css). Survive
    --warnings-as-errors (cl_chip with a removed unused helper must not leave a compile warning;
    delete the dead clauses, don't comment them out).
  </action>
  <verify>
    <automated>grep -nE '#[0-9a-fA-F]{3,6}|rgba\(|hsl\(' lib/cairnloop/web/search_modal_component.ex; echo "exit:$?"; grep -c '<\.cl_chip variant="\(success\|info\)"' lib/cairnloop/web/search_modal_component.ex; grep -c 'source_badge_style\|trust_badge_style' lib/cairnloop/web/search_modal_component.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,6}|rgba\(|hsl\(' lib/cairnloop/web/search_modal_component.ex` returns ZERO matches (entire file clean)
    - `grep -c '<\.cl_chip variant="\(success\|info\)"' lib/cairnloop/web/search_modal_component.ex` returns at least 2
    - `grep -c 'source_badge_style\|trust_badge_style' lib/cairnloop/web/search_modal_component.ex` returns 0 (helpers deleted, no call sites remain)
    - `result_row_style/1` returns strings containing only var(--cl-...) for colors (grep its body: no rgba/hex)
    - `mix compile --warnings-as-errors` exits 0 (no unused-function warning from deleted helpers)
    - git diff of priv/static/cairnloop.css is empty
  </acceptance_criteria>
  <done>Source/trust badges render via cl_chip success|info; the rgba badge helpers are deleted; result_row_style returns token-valued strings; whole file is hex/rgba-free; build warnings-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> search modal | Existing client-side search/keyboard-nav UI — unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02 | Information disclosure | search result badges | accept | Refactor preserves what is rendered (source type + trust level) — only the styling primitive changes from inline rgba pill to cl_chip. No new data exposed; cl_chip carries icon+text (brand 7.5 never-color-alone preserved). |

No new threat surface: CSS string snapping + swapping inline-pill helpers for the tested cl_chip
primitive. No new dependency, DB, network, auth, or input handling.
</threat_model>

<verification>
- `grep -nE '#[0-9a-fA-F]{3,6}|rgba\(|hsl\(' lib/cairnloop/web/search_modal_component.ex` → empty
- `mix compile --warnings-as-errors` → exit 0
- `mix test test/cairnloop/web/brand_token_gate_test.exs` → still green
- Manual dark-mode spot check (RESEARCH §E): toggle `<html data-theme="dark">`; empty-state panel, preview pane, KB/case badges flip to dark palette.
</verification>

<success_criteria>
search_modal_component.ex carries zero off-palette hex/rgba; source/trust badges use cl_chip
success|info; result-row styles are token-valued; the rgba helpers are deleted; cairnloop.css is
untouched (D-01); `mix compile --warnings-as-errors` and the existing gate both pass.
</success_criteria>

<output>
Create `.planning/phases/40-drift-remediation-brand-token-gate-hardening/40-02-SUMMARY.md` when done
</output>
