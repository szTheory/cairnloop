---
phase: 13-governed-tool-contract-proposal-records
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - lib/cairnloop/tool_registry.ex
  - lib/cairnloop/application.ex
  - lib/cairnloop/web/conversation_live.ex
  - lib/cairnloop/governance.ex
  - test/cairnloop/tool_registry_test.exs
  - test/cairnloop/web/conversation_live_test.exs
autonomous: true
requirements: [TOOL-02, TOOL-03]
must_haves:
  truths:
    - "The synchronous inline `execute_tool` LiveView path is replaced entirely by `Governance.propose/3` — no try/rescue, no run/3, no execute/3, no optimistic UI (D-27, TOOL-02)"
    - "`{:ok, proposal}` surfaces a calm 'Proposed — pending review' flash carrying the proposal id (Phase 14 seam); `{:blocked, outcome, reason}` surfaces an explicit operator-visible reason (D-27)"
    - "Tools are resolved from the registry by module-list match, never via String.to_existing_atom/1 (D-19)"
    - "Each declared tool is validated at boot (implements the behaviour + valid __tool_spec__/0) and the app fails fast on a misconfigured tool (D-07)"
    - "`get_available_tools/2` filters visible tools on scope/0 + authorize/2 as ADVISORY UX only — `Governance.validate/3` is the authoritative gate (D-28)"
  artifacts:
    - path: "lib/cairnloop/tool_registry.ex"
      provides: "Evolved registry: validate_configured_tools!/0 (boot), get_available_tools/2 on scope+authorize, find_tool_module/1 (no String.to_existing_atom)"
      contains: "find_tool_module"
    - path: "lib/cairnloop/application.ex"
      provides: "Boot-time call to ToolRegistry.validate_configured_tools!/0 so misconfigured tools fail fast"
      contains: "validate_configured_tools"
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "execute_tool handler swapped to Governance.propose/3 + failure_reason_message/2; no inline execution"
      contains: "Cairnloop.Governance.propose"
    - path: "test/cairnloop/web/conversation_live_test.exs"
      provides: "TOOL-02 test: handler no longer calls execute/3 or run/3; proposal-first flash"
      contains: "execute_tool"
    - path: "test/cairnloop/tool_registry_test.exs"
      provides: "Registry tests: find_tool_module module-match (no String.to_existing_atom), boot validation rejects misconfigured tool, advisory scope+authorize filter"
      contains: "find_tool_module"
  key_links:
    - from: "lib/cairnloop/web/conversation_live.ex (handle_event execute_tool)"
      to: "lib/cairnloop/governance.ex (propose/3)"
      via: "proposal-first call; flash carries proposal id (D-27)"
      pattern: "Cairnloop.Governance.propose"
    - from: "lib/cairnloop/application.ex (start/2)"
      to: "lib/cairnloop/tool_registry.ex (validate_configured_tools!/0)"
      via: "boot-time fail-fast validation (D-07)"
      pattern: "validate_configured_tools"
    - from: "lib/cairnloop/governance.ex (validate/3 gate 0)"
      to: "lib/cairnloop/tool_registry.ex (find_tool_module/1)"
      via: "centralized registry resolution, no String.to_existing_atom (D-19)"
      pattern: "find_tool_module"
---

<objective>
Close the entrypoint cutover. Replace the synchronous inline `execute_tool` handler in `ConversationLive` entirely with `Cairnloop.Governance.propose/3` (D-27): no `try/rescue`, no `run/3`/`execute/3`, no `String.to_existing_atom/1`, no optimistic UI. Evolve `Cairnloop.ToolRegistry` to (a) validate each declared tool at boot and fail fast (D-07), (b) resolve a string `tool_ref` to a module by registry module-list match — never `String.to_existing_atom/1` (D-19), and (c) filter visible tools on `scope/0` + `authorize/2` as advisory UX only (D-28). Wire `validate_configured_tools!/0` into the application boot. Centralize gate-0 resolution: `Governance.validate/3` delegates to `ToolRegistry.find_tool_module/1`.

Purpose: This is the production cutover that supersedes the "replace synchronous execute_tool" pending todo in STATE.md and realizes TOOL-02 + the entrypoint half of TOOL-03. The handler returns the proposal id so Phase 14 can swap the flash for a real timeline card without touching `Governance`. The visibility filter is advisory; the durable `validate/3` gate is authoritative.
Output: evolved `tool_registry.ex`, boot wiring in `application.ex`, swapped handler in `conversation_live.ex`, a small delegation in `governance.ex`, and 2 new test files (Wave 0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-governed-tool-contract-proposal-records/13-CONTEXT.md
@.planning/phases/13-governed-tool-contract-proposal-records/13-RESEARCH.md
@.planning/phases/13-governed-tool-contract-proposal-records/13-PATTERNS.md
@.planning/phases/13-governed-tool-contract-proposal-records/13-VALIDATION.md
@.planning/phases/13-governed-tool-contract-proposal-records/13-01-SUMMARY.md
@.planning/phases/13-governed-tool-contract-proposal-records/13-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
A governed tool exposes: __tool_spec__/0 (returns %Cairnloop.Tool.Spec{}), changeset/2, scope/0 :: [atom()], authorize/2 :: :ok | {:error, atom()}.

<!-- From Plan 02 -->
Cairnloop.Governance.propose(tool_ref :: String.t(), actor_id :: String.t(), context :: map())
  :: {:ok, proposal} | {:blocked, :unsupported, reason} | {:blocked, :needs_input, cs}
   | {:blocked, :scope_invalid, reason} | {:blocked, :policy_denied, reason}
Cairnloop.Governance.validate/3 — pure pipeline; gate 0 resolves the tool (currently a private resolve_tool/1).
  THIS PLAN promotes that resolution into ToolRegistry.find_tool_module/1 and has validate/3 delegate to it.

<!-- Current ConversationLive handler (lib/cairnloop/web/conversation_live.ex line 173 — to replace ENTIRELY) -->
def handle_event("execute_tool", %{"tool" => tool_name} = params, socket) do
  tool_module = String.to_existing_atom(tool_name)            # REMOVE (D-19)
  actor_id = socket.assigns.conversation.host_user_id
  context  = socket.assigns.host_context
  if tool_module.can_execute?(actor_id, context) do ...       # REMOVE (D-06)
    ... changeset.valid? -> try tool_module.execute(...) ...   # REMOVE try/rescue + execute (D-27/D-05)
end
# Template (lib/cairnloop/web/conversation_live.ex):
#   line 427: Cairnloop.ToolRegistry.get_available_tools(assigns.actor_id, assigns.context)   (keep call; registry impl changes)
#   line 528: phx-click="execute_tool" phx-value-tool={inspect(@tool)}   ("Elixir.MyTool" string)
#   line 539: phx-submit="execute_tool"   (form path passes "tool_params")
# socket.assigns.conversation.host_user_id and socket.assigns.host_context already exist.

<!-- Module-string identity invariant (Pitfall 8): -->
inspect(MyTool) == Atom.to_string(MyTool) == "Elixir.MyTool"  → find_tool_module/1 matches on Atom.to_string/1.

<!-- Application boot (lib/cairnloop/application.ex) -->
def start(_type, _args) do
  children = [ ... ]
  Supervisor.start_link(children, opts)
end
# Add ToolRegistry.validate_configured_tools!/0 before/at start (fail fast on misconfigured tool, D-07).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evolve ToolRegistry (boot validation + find_tool_module + advisory scope/authorize filter) and wire boot validation</name>
  <files>lib/cairnloop/tool_registry.ex, lib/cairnloop/application.ex, lib/cairnloop/governance.ex, test/cairnloop/tool_registry_test.exs</files>
  <read_first>
    - lib/cairnloop/tool_registry.ex (the current 18-line registry to evolve in place)
    - lib/cairnloop/application.ex (the boot module — add the fail-fast validation call)
    - lib/cairnloop/governance.ex (Plan 02 — its private resolve_tool/1 gate; redirect it to delegate to ToolRegistry.find_tool_module/1)
    - 13-PATTERNS.md § tool_registry.ex (Phase 13 cutover shape — validate_configured_tools!/0, get_available_tools/2 on scope+authorize, find_tool_module/1)
    - 13-RESEARCH.md § Existing Code Seams → tool_registry.ex cutover (D-07, D-19, D-28) + Pitfalls 1 and 8
  </read_first>
  <behavior>
    - find_tool_module/1 returns {:ok, module} for a tool_ref equal to Atom.to_string(module) of a configured tool.
    - find_tool_module/1 returns {:error, :unknown_tool} for any string not matching a configured module, and never raises ArgumentError (proving no String.to_existing_atom/1).
    - find_tool_module("Elixir.SomeUnregisteredMod") returns {:error, :unknown_tool} even if that module exists but is not configured.
    - validate_configured_tools!/0 raises (ArgumentError) when a configured entry does not export __tool_spec__/0 or its __tool_spec__/0 is not a %Cairnloop.Tool.Spec{}; returns :ok when all configured tools are valid.
    - get_available_tools/2 includes a tool only when all of tool.scope/0 are in context[:scopes] AND tool.authorize(actor_id, context) == :ok; otherwise excludes it (advisory only).
  </behavior>
  <action>
    Evolve `Cairnloop.ToolRegistry` in place (D-07/D-19/D-28). Add `validate_configured_tools!/0`: iterate `Application.get_env(:cairnloop, :tools, []) || []`; for each module, raise `ArgumentError` unless `function_exported?(module, :__tool_spec__, 0)` and `is_struct(module.__tool_spec__(), Cairnloop.Tool.Spec)`. Add `find_tool_module/1` (D-19, Pitfall 8): `Enum.find(configured, fn mod -> Atom.to_string(mod) == tool_ref end)` → `{:ok, mod}` or `{:error, :unknown_tool}`; NEVER `String.to_existing_atom/1`. Rewrite `get_available_tools/2` (D-28) to filter on `Enum.all?(mod.scope(), &(&1 in Map.get(context, :scopes, []))) and mod.authorize(actor_id, context) == :ok` — document it is advisory UX only; `Governance.validate/3` is the gate.

    Wire boot validation (D-07): in `lib/cairnloop/application.ex` `start/2`, call `Cairnloop.ToolRegistry.validate_configured_tools!()` before `Supervisor.start_link/2` so a misconfigured tool fails fast at boot, not at first user interaction.

    Centralize resolution: update `Cairnloop.Governance.validate/3` gate 0 to delegate to `Cairnloop.ToolRegistry.find_tool_module/1` (replace the Plan-02 private resolve_tool/1 body with a delegation, keeping the same `{:ok, module} | {:error, :unknown_tool}` contract so the pipeline and Plan-02 tests are unchanged). This is the single source of truth for tool resolution and keeps the String.to_existing_atom/1 ban in one place.

    Create `test/cairnloop/tool_registry_test.exs` (`async: false`) covering the behaviors above: define in-test governed tool modules (a valid one; one whose scope is unmet; one whose authorize denies; one configured-but-not-a-tool fake to drive validate_configured_tools! to raise); set `Application.put_env(:cairnloop, :tools, [...])` per test and clean up in on_exit.
  </action>
  <verify>
    <automated>mix test test/cairnloop/tool_registry_test.exs test/cairnloop/governance_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "String.to_existing_atom\|String.to_atom" lib/cairnloop/tool_registry.ex` == 0 (D-19); `grep -c "can_execute" lib/cairnloop/tool_registry.ex` == 0 (D-06 removed).
    - `grep -c "def find_tool_module" lib/cairnloop/tool_registry.ex` >= 1 and `grep -c "def validate_configured_tools" lib/cairnloop/tool_registry.ex` >= 1.
    - `grep -c "validate_configured_tools" lib/cairnloop/application.ex` >= 1 (boot wiring, D-07).
    - `grep -c "ToolRegistry.find_tool_module" lib/cairnloop/governance.ex` >= 1 (gate-0 delegation, D-19 single source of truth).
    - Behavior: find_tool_module/1 returns {:error, :unknown_tool} for an unconfigured string and never raises; returns {:ok, mod} for a configured module's Atom.to_string.
    - Behavior: validate_configured_tools!/0 raises ArgumentError for a configured non-tool entry; returns :ok for valid config.
    - Behavior: get_available_tools/2 excludes a scope-unmet tool and a policy-denying tool, includes a fully-permitted tool.
    - `mix test test/cairnloop/governance_test.exs` still passes (gate-0 delegation did not regress the Plan-02 pipeline tests).
  </acceptance_criteria>
  <done>Registry validates declared tools at boot and fails fast; resolves tool_refs by module-list match (never String.to_existing_atom/1); filters visible tools advisory-only on scope+authorize; Governance.validate/3 gate 0 delegates to find_tool_module/1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the ConversationLive execute_tool handler with Governance.propose/3 + failure_reason_message/2</name>
  <files>lib/cairnloop/web/conversation_live.ex, test/cairnloop/web/conversation_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex (the handler at line 173 to replace entirely; the context_pane get_available_tools call ~line 427; the tool_renderer template ~lines 528/539; host_user_id/host_context assigns)
    - lib/cairnloop/knowledge_automation.ex (analog: fail-closed flash / bounded-reason message style to mirror for failure_reason_message/2)
    - 13-PATTERNS.md § conversation_live.ex (Phase 13 replacement handler + failure_reason_message/2 clauses; note: do NOT change the template event name or get_available_tools call)
    - 13-RESEARCH.md § Existing Code Seams → conversation_live.ex (D-27 replacement; pass tool_params via context so validate/3 can call changeset/2) + Pitfalls 1 and 8
    - 13-VALIDATION.md (TOOL-02 row: handler no longer calls execute/3 or run/3; emits "Proposed — pending review")
  </read_first>
  <behavior>
    - On a successful propose, the handler puts an :info flash containing "Proposed" and the proposal id, and returns {:noreply, socket}.
    - On {:blocked, outcome, reason}, the handler puts an :error flash with an explicit operator-visible message per outcome (unsupported/needs_input/scope_invalid/policy_denied).
    - The handler passes the raw tool_ref string to Governance.propose/3 (no atom conversion) and forwards params["tool_params"] into the context map so validate/3 can run changeset/2.
    - The handler contains no try/rescue, no call to .execute( or .run(, no can_execute?, no String.to_existing_atom.
    - The context_pane still calls ToolRegistry.get_available_tools/2 (now scope+authorize filtered) and the template still fires the "execute_tool" event (no template event rename required).
  </behavior>
  <action>
    Replace the body of `handle_event("execute_tool", %{"tool" => tool_ref} = params, socket)` entirely (D-27): read `actor_id = socket.assigns.conversation.host_user_id` and `context = socket.assigns.host_context`; merge the form params into context (e.g. `context = Map.put(context, :tool_params, params["tool_params"] || %{})`) so `Governance.validate/3` can call `changeset/2`; call `Cairnloop.Governance.propose(tool_ref, actor_id, context)`; on `{:ok, proposal}` → `put_flash(socket, :info, "Proposed — pending review. (##{proposal.id})")` and return `{:noreply, ...}` (the id is the Phase 14 seam); on `{:blocked, outcome, reason}` → `put_flash(socket, :error, failure_reason_message(outcome, reason))`. Add `failure_reason_message/2` private clauses for `:unsupported`, `:needs_input`, `:scope_invalid`, `:policy_denied`, and a catch-all, mirroring the existing fail-closed flash style (operator-legible, distinct per outcome). REMOVE the `String.to_existing_atom/1` call (D-19), the `can_execute?/2` branch (D-06), and the `try/rescue`-around-`execute` block (D-27/D-05). Do NOT call `run/3` or `execute/3`. Leave the `get_available_tools/2` call and the template event name `"execute_tool"` unchanged (the registry impl already changed in Task 1; module-string identity holds per Pitfall 8).

    Create `test/cairnloop/web/conversation_live_test.exs` (TOOL-02). If a full LiveView mount is heavy without a live Repo, prefer a focused unit test that calls the `handle_event/3` clause (or asserts via grep + a `Governance` stub) proving: (a) source no longer references `execute(`/`run(`/`String.to_existing_atom`/`can_execute?` in the handler region; (b) a successful propose yields an :info flash containing "Proposed"; (c) a blocked propose yields an :error flash with the outcome-specific message. Use the Plan-02 MockRepo / in-test tool registration pattern if exercising `handle_event` end-to-end; otherwise assert the handler delegates to `Cairnloop.Governance.propose/3` and renders the documented flashes.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/conversation_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Cairnloop.Governance.propose" lib/cairnloop/web/conversation_live.ex` >= 1.
    - In the execute_tool handler region: `grep -c "String.to_existing_atom" lib/cairnloop/web/conversation_live.ex` for the handler — the handler no longer uses it (note: line 754 has an unrelated map-key atomization; do NOT remove that one — only the handler's tool resolution at the old line 174 is removed). Confirm by reading the handler body that `String.to_existing_atom` and `can_execute?` and `try`/`rescue` and `.execute(`/`.run(` are absent from the execute_tool handler.
    - `grep -c "def failure_reason_message\|defp failure_reason_message" lib/cairnloop/web/conversation_live.ex` >= 1 with clauses for unsupported/needs_input/scope_invalid/policy_denied.
    - The success flash text contains "Proposed" and the proposal id — `grep -c "Proposed" lib/cairnloop/web/conversation_live.ex` >= 1.
    - The template still fires "execute_tool" (unchanged) and still calls `get_available_tools/2` — `grep -c "get_available_tools" lib/cairnloop/web/conversation_live.ex` >= 1.
    - `mix test test/cairnloop/web/conversation_live_test.exs` is green (TOOL-02: handler proposal-first, no inline execution).
  </acceptance_criteria>
  <done>The execute_tool handler is fully proposal-first via Governance.propose/3 with a calm "Proposed — pending review" flash carrying the proposal id and explicit per-outcome blocked reasons; no try/rescue, no run/3/execute/3, no String.to_existing_atom, no can_execute?; advisory tool list and template event unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green gate — confirm cutover compiles and no stale execute/can_execute references remain</name>
  <files>test/cairnloop/web/conversation_live_test.exs, test/cairnloop/tool_registry_test.exs</files>
  <read_first>
    - 13-VALIDATION.md § Sampling Rate (per-wave merge runs `mix test`) and § Validation Sign-Off
    - lib/cairnloop/web/conversation_live.ex (confirm no stale tool callbacks remain in any code path, not only the handler)
    - any existing tool implementations under lib/ (grep for modules that `use Cairnloop.Tool` and still define `can_execute?`/`execute` — they must be updated to the new contract or the compiler will flag the removed/renamed callbacks)
  </read_first>
  <action>
    This is the phase merge gate (no new production code beyond cleanup). Run `mix compile --warnings-as-errors` to confirm the contract cutover is complete — the compiler enforces that no caller still expects `can_execute?/2` or `execute/3` (D-05/D-06 compiler-enforced cutover). Grep the whole `lib/` tree for stale references to the removed/renamed callbacks: any module that `use`s `Cairnloop.Tool` and still defines `can_execute?/2` or `execute/3` (rather than `run/3` + `scope/0` + `authorize/2`) must be updated to the new contract; any non-tool caller of `tool.can_execute?`/`tool.execute` must be removed. Then run the full suite. If a demo/example tool exists that implements the old contract, migrate it to the new callbacks (add `scope/0`, rename `execute/3`→`run/3`, drop `can_execute?/2`) — keep it minimal and governed.

    Do NOT add new behavior; this task only ensures the cutover is total and the suite is green. Update the two test files from Tasks 1-2 if compile-time discoveries require it.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` succeeds (no undefined `can_execute?/2`/`execute/3` callers; no unused-function warnings from the cutover).
    - Repo-wide: `grep -rn "\.can_execute?\|\.execute(" lib/ | grep -v "Cairnloop.Telemetry.execute\|:telemetry.execute\|Cairnloop.Tool.execute_callback" ` returns no governed-tool execution callsite (the only `.execute(` allowed are telemetry/`Cairnloop.Telemetry.execute/3`).
    - `grep -rn "def can_execute?" lib/` returns 0 (no tool still defines the removed callback).
    - `mix test` is green (full suite, all TOOL-01..04 rows from 13-VALIDATION.md).
  </acceptance_criteria>
  <done>The contract cutover is total: the project compiles with warnings-as-errors, no stale can_execute?/execute references remain anywhere in lib/, and the full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView client (phx-value-tool / form params) → handler | Operator-supplied tool_ref string and tool params cross from the browser into the governance entrypoint. |
| host tool config → application boot | Misconfigured/non-conforming tool modules must fail fast at boot, not silently at first use. |
| visibility filter (UX) vs authoritative gate | The advisory tool list must never be mistaken for the authorization gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-11 | Tampering / DoS | execute_tool handler tool_ref | high | mitigate | Handler passes the raw string to Governance.propose/3 which resolves via ToolRegistry.find_tool_module/1 (module-list match) — the handler performs NO `String.to_existing_atom/1` (D-19, Pitfall 1). Task 2 acceptance confirms its absence in the handler. |
| T-13-12 | Elevation of Privilege | visibility filter mistaken for gate | high | mitigate | `get_available_tools/2` is advisory UX only; `Governance.validate/3` re-checks scope + authorize authoritatively at propose time (D-28). A tool hidden from the list cannot be proposed past the gate, and a tool shown in the list still must pass validate/3. Task 1 proves the filter; Plan 02 proves the authoritative gate. |
| T-13-13 | Tampering / DoS | misconfigured tool at boot | medium | mitigate | `validate_configured_tools!/0` runs at application boot and raises on any non-conforming tool (D-07), so a broken/ungoverned tool cannot reach a live request path. Task 1 acceptance proves the raise + boot wiring. |
| T-13-14 | Repudiation / Tampering | inline execution removal | high | mitigate | The handler has no try/rescue, no run/3/execute/3, no optimistic UI — every action becomes a durable proposal via Governance.propose/3 (D-27). `mix compile --warnings-as-errors` (Task 3) enforces no caller still invokes the removed/renamed execution callbacks. |
| T-13-15 | Information Disclosure | blocked-reason flash copy | low | accept | failure_reason_message/2 surfaces a bounded, operator-legible reason per outcome; reasons are governance outcome atoms, not raw internal payloads. No sensitive data in flash. |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs (13-RESEARCH.md Package Legitimacy Audit: not applicable). No checkpoint required. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` succeeds (compiler-enforced cutover of removed/renamed callbacks).
- `mix test test/cairnloop/tool_registry_test.exs test/cairnloop/web/conversation_live_test.exs` is green (TOOL-02 + registry/D-07/D-19/D-28).
- `mix test` full suite is green (all TOOL-01..04 from 13-VALIDATION.md).
- `grep -rn "def can_execute?" lib/` returns 0; handler region free of String.to_existing_atom / try-rescue / .execute( / .run(.
- LiveView mount tests that need a live Repo are deferred per the STATE.md/13-VALIDATION.md no-DB caveat — focused handler-level proof is sufficient for the merge gate.
</verification>

<success_criteria>
- The synchronous inline `execute_tool` path is replaced entirely by `Governance.propose/3`; no try/rescue, no run/3/execute/3, no optimistic UI; flash carries the proposal id (D-27, TOOL-02).
- `ToolRegistry` validates declared tools at boot (fail fast, D-07), resolves tool_refs by module match (never String.to_existing_atom/1, D-19), and filters visible tools advisory-only on scope+authorize (D-28).
- Boot wiring calls `validate_configured_tools!/0`; `Governance.validate/3` gate 0 delegates to `find_tool_module/1` (single source of truth for resolution).
- `mix compile --warnings-as-errors` and the full `mix test` suite are green; no stale can_execute?/execute references remain in lib/.
</success_criteria>

<output>
Create `.planning/phases/13-governed-tool-contract-proposal-records/13-03-SUMMARY.md` when done.
</output>
