---
phase: 26-observability-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - lib/cairnloop/governance.ex
  - test/cairnloop/governance_test.exs
  - test/cairnloop/outbound_test.exs
autonomous: true
requirements:
  - OBS-02
must_haves:
  truths:
    - "Hosts call `Cairnloop.Governance.list_recent_bulk_outbound_envelopes/1` and receive `BulkEnvelope` rows ordered `requested_at desc`, default limit 50."
    - "Calling `list_recent_bulk_outbound_envelopes/1` with `limit: 501` raises `ArgumentError` (defense-in-depth against unbounded reads — D-06 hard cap 500)."
    - "`list_recent_bulk_outbound_envelopes/1` accepts `:status` filter `:submitted | :refused_cap_exceeded | :all`, default `:all`, and returns the filtered subset."
    - "`Cairnloop.Governance.get_bulk_outbound_envelope/1` returns `nil` on miss (does NOT raise) — callers branch on the result."
    - "Both facade reads go through `repo().all/1` / `repo().get/2` — never `Cairnloop.Repo` directly (D-14 narrow facade enforced)."
    - "`Outbound.trigger/2` calls `auditor.audit(:outbound_trigger, actor, %{conversation_id: ..., template_id: ...})` with EXACTLY that metadata key set — no extras, no drift (D-05 regression)."
    - "`Outbound.bulk_trigger/2` submit path calls `auditor.audit(:bulk_outbound_trigger, actor, %{bulk_envelope_id: ..., count: ..., template_id: ...})` with EXACTLY that metadata key set (D-05 regression)."
    - "`mix compile --warnings-as-errors` exits 0 and `mix test` (headless) is green."
  artifacts:
    - path: "lib/cairnloop/governance.ex"
      provides: "list_recent_bulk_outbound_envelopes/1 + get_bulk_outbound_envelope/1 appended after preview_bulk_recovery_cohort/1 (~line 1081)"
      contains: "def list_recent_bulk_outbound_envelopes"
    - path: "test/cairnloop/governance_test.exs"
      provides: "MockRepo dispatch arm for `cairnloop_outbound_bulk_envelopes` table + describe block covering D-06 contract (default limit, hard cap, status filter, ordering, get returns nil on miss)"
      contains: "describe \"list_recent_bulk_outbound_envelopes/1 (Phase 26 OBS-02 D-06)\""
    - path: "test/cairnloop/outbound_test.exs"
      provides: "Auditor metadata shape regression tests pinning the EXACT key set for :outbound_trigger and :bulk_outbound_trigger"
      contains: "describe \"auditor metadata shape regression (Phase 26 OBS-02 D-05)\""
  key_links:
    - from: "lib/cairnloop/governance.ex"
      to: "Cairnloop.Outbound.BulkEnvelope"
      via: "alias + Ecto query builder targeting the schema (where/order_by/limit) + repo().all/1 + repo().get/2"
      pattern: "alias Cairnloop.Outbound.BulkEnvelope"
    - from: "test/cairnloop/governance_test.exs"
      to: "Cairnloop.Outbound.BulkEnvelope"
      via: "MockRepo `all/1` clause matching `%Ecto.Query{from: %{source: {\"cairnloop_outbound_bulk_envelopes\", _}}}` + `get/2` clause matching `Cairnloop.Outbound.BulkEnvelope`"
      pattern: "cairnloop_outbound_bulk_envelopes"
---

<objective>
Land OBS-02's narrow audit READ facade in `Cairnloop.Governance` — two functions (`list_recent_bulk_outbound_envelopes/1` and `get_bulk_outbound_envelope/1`) that let hosts consume `BulkEnvelope` audit history without breaching D-14 (no direct Ecto queries from the web layer) — plus a D-05 regression test pinning the auditor metadata shape on both `Outbound.trigger/2` and `Outbound.bulk_trigger/2` so future refactors can't drift it.

Purpose: Close OBS-02. The audit-WRITE substrate already exists (Phase 25 inserts `BulkEnvelope` rows on submit + refused lanes; `Outbound.trigger/2` line 95 + `bulk_trigger_submit/6` line 339 already call `auditor.audit/4`). Phase 26 adds the consumer-side READ facade so host apps (or a future example-app admin panel) can render bulk history through the narrow Governance boundary, and pins the auditor metadata contract via regression test.

Output: Two new public functions on `Cairnloop.Governance` reading `BulkEnvelope` rows through `repo().all/1` and `repo().get/2`; module-attribute constants for the limit (`50` default, `500` hard cap); new test coverage in `governance_test.exs` (MockRepo dispatch + filter + cap-guard) and `outbound_test.exs` (auditor metadata shape regression for both trigger lanes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-observability-polish/26-CONTEXT.md
@.planning/phases/26-observability-polish/26-RESEARCH.md
@.planning/phases/26-observability-polish/26-PATTERNS.md
@.planning/phases/26-observability-polish/26-VALIDATION.md
@lib/cairnloop/governance.ex
@lib/cairnloop/outbound/bulk_envelope.ex
@lib/cairnloop/outbound.ex
@test/cairnloop/governance_test.exs
@test/cairnloop/outbound_test.exs
@.planning/phases/26-observability-polish/26-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From `lib/cairnloop/governance.ex` lines 59-83 (alias block + repo() indirection):
```elixir
require Logger
import Ecto.Query

alias Cairnloop.Governance.{Policy, Preview, Telemetry, ToolActionEvent, ToolApproval, ToolProposal}
alias Cairnloop.Governance.Telemetry.Traces
alias Cairnloop.Workers.{ApprovalExpiryWorker, ApprovalResumeWorker, ToolExecutionWorker}

# Phase 25 plan 01 (D-14): cohort-eligibility reads target the Conversation schema.
alias Cairnloop.Conversation

defp repo do
  Application.fetch_env!(:cairnloop, :repo)
end
```

From `lib/cairnloop/governance.ex` lines 1021-1082 (THE narrow-facade pattern — append after line 1081):
```elixir
def list_eligible_conversation_ids_for_bulk_recovery(candidate_ids)
    when is_list(candidate_ids) do
  Conversation
  |> where([c], c.id in ^candidate_ids and c.status == :resolved)
  |> select([c], c.id)
  |> repo().all()
end

def preview_bulk_recovery_cohort(candidate_ids) when is_list(candidate_ids) do
  rows =
    Conversation
    |> where([c], c.id in ^candidate_ids and c.status == :resolved)
    |> order_by([c], desc: c.updated_at)
    |> select([c], %{id: c.id, subject: c.subject, host_user_id: c.host_user_id, updated_at: c.updated_at})
    |> repo().all()

  total = length(rows)
  sample = rows |> Enum.take(5) |> Enum.map(&bulk_recovery_label_for/1)
  more = max(total - 5, 0)

  %{eligible_ids: Enum.map(rows, & &1.id), sample: sample, more: more, total: total}
end
```

From `lib/cairnloop/outbound/bulk_envelope.ex` (schema columns — append-after target for the alias block):
- PK is `:binary_id` (UUID string), NOT integer — `get_bulk_outbound_envelope/1` callers pass a UUID string (per RESEARCH Pitfall 4).
- Columns: `:effective_cap`, `:requested_by`, `:requested_at`, `:status` (`:submitted | :refused_cap_exceeded`), `:refused_reason`, `:rendered_body`, `:recipient_conversation_ids`, `:count`, `:template_id`.
- Source: `cairnloop_outbound_bulk_envelopes` table (used in MockRepo `from.source` dispatch).

From `lib/cairnloop/outbound.ex` lines 95-98 (existing `Outbound.trigger/2` auditor.audit call — Phase 22 substrate):
```elixir
|> auditor.audit(:outbound_trigger, actor, %{
  conversation_id: conversation_id,
  template_id: template_id
})
```
Exact metadata key set: `[:conversation_id, :template_id]` — sorted. NO extras allowed (D-05 regression).

From `lib/cairnloop/outbound.ex` lines 339-343 (existing `bulk_trigger_submit/6` auditor.audit call — Phase 25 substrate):
```elixir
|> auditor.audit(:bulk_outbound_trigger, actor, %{
  bulk_envelope_id: envelope_id,
  count: count,
  template_id: template_id
})
```
Exact metadata key set: `[:bulk_envelope_id, :count, :template_id]` — sorted. NO extras allowed (D-05 regression).

From `test/cairnloop/governance_test.exs` lines 64-96 (MockRepo `all/1` dispatch — extend with a new `from.source` arm):
```elixir
def all(%Ecto.Query{from: %{source: {"cairnloop_conversations", _}}} = query) do
  candidate_ids = extract_candidate_ids(query)
  rows = Process.get(:conversations, []) |> Enum.filter(fn c -> c.id in candidate_ids and c.status == :resolved end) |> Enum.sort_by(& &1.updated_at, {:desc, DateTime})
  # ... existing logic
end
```

From `test/cairnloop/outbound_test.exs` lines 177-191 (existing `TestAuditor` precedent for the D-05 regression block):
```elixir
defmodule TestAuditor do
  @behaviour Cairnloop.Auditor
  def audit(multi, action, actor, metadata) do
    Ecto.Multi.run(multi, :audit, fn _repo, _changes ->
      {:ok, %{action: action, actor: actor, metadata: metadata}}
    end)
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append list_recent_bulk_outbound_envelopes/1 + get_bulk_outbound_envelope/1 to Cairnloop.Governance</name>
  <files>lib/cairnloop/governance.ex, test/cairnloop/governance_test.exs</files>
  <read_first>
    - lib/cairnloop/governance.ex lines 55-95 (alias block + `repo()` indirection) and lines 1000-1082 (the existing narrow-facade pair — THE analog)
    - lib/cairnloop/outbound/bulk_envelope.ex (full file — `:binary_id` PK, column inventory, `cairnloop_outbound_bulk_envelopes` table source name)
    - test/cairnloop/governance_test.exs lines 1-200 (existing MockRepo with `all/1` dispatch on `query.from.source`; existing `Process.put`/`get` pattern for seeding canned rows)
    - .planning/phases/26-observability-polish/26-CONTEXT.md `<decisions>` block — D-06 (facade read shape, default 50, hard cap 500, `:status` filter), D-14 (narrow facade — no direct `Cairnloop.Repo` access)
    - .planning/phases/26-observability-polish/26-RESEARCH.md Pattern 3 (lines 363-431) — full function body example
    - .planning/phases/26-observability-polish/26-RESEARCH.md Pitfall 4 (lines 522-530) — `:binary_id` PK; `get_bulk_outbound_envelope/1` callers pass a UUID string
    - .planning/phases/26-observability-polish/26-RESEARCH.md Pitfall 8 (lines 576-584) — REPO-UNAVAILABLE; every facade test uses MockRepo
    - .planning/phases/26-observability-polish/26-PATTERNS.md "Wave 2, Plan 01 — `lib/cairnloop/governance.ex` (ADD 2 read funcs)" and "Wave 2, Plan 01 — `test/cairnloop/governance_test.exs` (ADD facade tests)" sections
    - CLAUDE.md "Architecture posture" (narrow `Cairnloop.Governance` facade; `repo()` indirection)
  </read_first>
  <behavior>
    - Test 1 (default limit 50): seed 75 `BulkEnvelope` rows via `Process.put(:bulk_envelopes, ...)`; call `Governance.list_recent_bulk_outbound_envelopes()`; assert `length(result) == 50`.
    - Test 2 (custom limit): seed 10 rows; call with `limit: 5`; assert `length(result) == 5`.
    - Test 3 (hard cap raises): call with `limit: 501`; `assert_raise ArgumentError, fn -> Governance.list_recent_bulk_outbound_envelopes(limit: 501) end`. The message should include the requested limit and the cap (e.g. `"limit 501 exceeds bulk envelope hard cap 500"`).
    - Test 4 (status `:submitted` filter): seed mix of submit + refused rows; call with `status: :submitted`; assert all results have `.status == :submitted`.
    - Test 5 (status `:refused_cap_exceeded` filter): seed mix; call with `status: :refused_cap_exceeded`; assert all results have `.status == :refused_cap_exceeded` (which includes both `:refused_cap_exceeded` and `:refused_cap_exceeded_audit_failed` ONLY IF the schema enum stores `:refused_cap_exceeded` for both — per Phase 25 plan 02, the SCHEMA status enum is `[:submitted, :refused_cap_exceeded]` (the audit-failed lane still writes `status: :refused_cap_exceeded` and reflects the failure in telemetry only — the row IS persisted on the changeset-ok arm). Confirm by re-reading `lib/cairnloop/outbound/bulk_envelope.ex` schema. The filter applies to the schema enum, not telemetry outcomes.).
    - Test 6 (status `:all`): default — returns rows of both lanes.
    - Test 7 (ordering): seed rows with `requested_at` `~U[2026-05-25 ...]`, `~U[2026-05-27 ...]`, `~U[2026-05-26 ...]`; call with default opts; assert ordering descends (`2026-05-27` first, `2026-05-25` last).
    - Test 8 (`get_bulk_outbound_envelope/1` happy path): seed one row with `id: "uuid-test-1"`; call `Governance.get_bulk_outbound_envelope("uuid-test-1")`; assert returns the row struct.
    - Test 9 (`get_bulk_outbound_envelope/1` miss returns nil): seed empty list; call with any UUID; assert `nil` returned, NOT raised.
    - Test 10 (no direct Cairnloop.Repo dependency in facade body — D-14): source assertion grep — see `<source_assertions>`.
  </behavior>
  <action>
    Patch `lib/cairnloop/governance.ex`:

    1. Extend the alias block (after line 75 `alias Cairnloop.Conversation`):
       `alias Cairnloop.Outbound.BulkEnvelope`
       with a single-line comment `# Phase 26 plan 02 (D-06): bulk-envelope audit READ facade targets the BulkEnvelope schema.`
    2. Add two module-attribute constants near the existing `approval_ttl_seconds` block (around line 86-91):
       ```
       # Phase 26 D-06: hard cap on bulk-envelope audit reads (defense-in-depth — RESEARCH "Specific Ideas").
       @bulk_envelope_default_limit 50
       @bulk_envelope_hard_cap 500
       ```
    3. Append two public functions IMMEDIATELY AFTER `preview_bulk_recovery_cohort/1` (after line 1072) and BEFORE the existing `defp bulk_recovery_label_for/1` private helpers (line 1077). Both go through `repo().all/1` / `repo().get/2` — never `Cairnloop.Repo` directly.

       Function 1 — `list_recent_bulk_outbound_envelopes(opts \\ [])`:
       - `@doc` block describing return shape (`BulkEnvelope` rows ordered `requested_at desc`, bounded by `:limit`), `:limit` option (default 50, hard cap 500, raises `ArgumentError` on overflow per RESEARCH "Specific Ideas"), `:status` option (`:submitted | :refused_cap_exceeded | :all`, default `:all`), cross-reference to D-14 (narrow facade; web layer forbidden from `Cairnloop.Repo` directly), cross-reference to D-06.
       - Body: extract `limit = Keyword.get(opts, :limit, @bulk_envelope_default_limit)` and `status = Keyword.get(opts, :status, :all)`; if `limit > @bulk_envelope_hard_cap`, `raise ArgumentError, "limit #{limit} exceeds bulk envelope hard cap #{@bulk_envelope_hard_cap}"`; build the query: `BulkEnvelope |> filter_envelope_status(status) |> order_by([e], desc: e.requested_at) |> limit(^limit) |> repo().all()`.

       Function 2 — `get_bulk_outbound_envelope(id)`:
       - `@doc` block describing: returns a single `BulkEnvelope` by `id` (a binary UUID string per Pitfall 4) or `nil` on miss. NEVER raises. Goes through `repo().get/2`.
       - Body: one line — `repo().get(BulkEnvelope, id)`.

       Private helpers (place BELOW the public functions, with the existing `defp bulk_recovery_label_for/1` helpers):
       - `defp filter_envelope_status(query, :all), do: query`
       - `defp filter_envelope_status(query, status) when status in [:submitted, :refused_cap_exceeded], do: where(query, [e], e.status == ^status)`

    4. DO NOT touch the existing `list_eligible_conversation_ids_for_bulk_recovery/1` or `preview_bulk_recovery_cohort/1` (Phase 25 plan 01 — sealed). The new functions go AFTER them, not interleaved.

    Patch `test/cairnloop/governance_test.exs`:

    1. Extend the in-file MockRepo (find the existing `defmodule MockRepo do ... end` block, around lines 13-96):
       - Add a new `all/1` clause that matches `%Ecto.Query{from: %{source: {"cairnloop_outbound_bulk_envelopes", _}}} = query`. Read seeded rows from `Process.get(:bulk_envelopes, [])`. Apply the `where status == ^X` filter if present in `query.wheres` (a simple pattern-match on the query's `where` against `:status` is acceptable — or simpler: store the status filter via a separate process key `Process.put(:bulk_envelopes_filter, :submitted | :refused_cap_exceeded | :all)` set by the test, and have the MockRepo apply it; mirror whichever approach the existing `cairnloop_conversations` arm uses for filtering). Apply `order_by desc: :requested_at` via `Enum.sort_by`. Apply `limit` from `query.limit.expr`. Return the resulting list.
       - Add a new `get/2` clause matching `Cairnloop.Outbound.BulkEnvelope`: `def get(Cairnloop.Outbound.BulkEnvelope, id), do: Process.get(:bulk_envelopes, []) |> Enum.find(fn e -> e.id == id end)`. Returns `nil` on miss (Enum.find default).
    2. Add a new `describe "list_recent_bulk_outbound_envelopes/1 (Phase 26 OBS-02 D-06)"` block covering Tests 1-7.
    3. Add a new `describe "get_bulk_outbound_envelope/1 (Phase 26 OBS-02 D-06)"` block covering Tests 8-9.
    4. Seed test rows via `Process.put(:bulk_envelopes, [%Cairnloop.Outbound.BulkEnvelope{id: "uuid-1", status: :submitted, requested_at: ~U[2026-05-27 10:00:00Z], ...minimal-required-fields}, ...])`. Use `setup do ... on_exit(fn -> Process.delete(:bulk_envelopes) end) :ok end` for cleanup.
    5. ALSO add a `@tag :integration` placeholder test (commented with `# REPO-UNAVAILABLE`) that calls `flunk("integration-only: requires Cairnloop.Repo + cairnloop_outbound_bulk_envelopes table")` — mirrors the Phase 25 BLOCKING-handoff pattern from `test/cairnloop/workers/outbound_worker_test.exs:133-154`. Excluded by default; operator runs on a Postgres host.

    Run `mix compile --warnings-as-errors` and `mix test test/cairnloop/governance_test.exs`. Headless arms must pass.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | tee /tmp/26-02-task1-compile.log; grep -c -E "^(warning|error):" /tmp/26-02-task1-compile.log; mix test test/cairnloop/governance_test.exs</automated>
    <source_assertions>
      - `grep -c "alias Cairnloop.Outbound.BulkEnvelope" lib/cairnloop/governance.ex` returns `1`.
      - `grep -c "def list_recent_bulk_outbound_envelopes" lib/cairnloop/governance.ex` returns `1`.
      - `grep -c "def get_bulk_outbound_envelope" lib/cairnloop/governance.ex` returns `1`.
      - `grep -c "@bulk_envelope_default_limit 50" lib/cairnloop/governance.ex` returns `1`.
      - `grep -c "@bulk_envelope_hard_cap 500" lib/cairnloop/governance.ex` returns `1`.
      - `grep -c "raise ArgumentError" lib/cairnloop/governance.ex | tr -d '[:space:]' | grep -q '[1-9]'` succeeds (at least one ArgumentError raise — the new cap guard).
      - `grep -c "Cairnloop.Repo\\." lib/cairnloop/governance.ex` returns `0` (D-14 — facade NEVER references `Cairnloop.Repo` directly; only `repo()` indirection).
      - `grep -c "repo().all" lib/cairnloop/governance.ex | tr -d '[:space:]'` is at least `3` (existing 2 + new 1).
      - `grep -c "repo().get(BulkEnvelope" lib/cairnloop/governance.ex` returns `1`.
      - `grep -c "describe \"list_recent_bulk_outbound_envelopes/1 (Phase 26 OBS-02 D-06)\"" test/cairnloop/governance_test.exs` returns `1`.
      - `grep -c "describe \"get_bulk_outbound_envelope/1 (Phase 26 OBS-02 D-06)\"" test/cairnloop/governance_test.exs` returns `1`.
      - `grep -c "cairnloop_outbound_bulk_envelopes" test/cairnloop/governance_test.exs | tr -d '[:space:]' | grep -q '[1-9]'` succeeds (MockRepo dispatch arm references the table source).
      - `grep -c "# REPO-UNAVAILABLE" test/cairnloop/governance_test.exs | tr -d '[:space:]' | grep -q '[1-9]'` succeeds (operator-handoff integration block present).
    </source_assertions>
  </verify>
  <done>
    - `Cairnloop.Governance.list_recent_bulk_outbound_envelopes/1` returns ordered `BulkEnvelope` rows with default limit 50, custom limit honored, hard cap 500 enforced via `ArgumentError`, and `:status` filter applied via `filter_envelope_status/2`.
    - `Cairnloop.Governance.get_bulk_outbound_envelope/1` returns the row by binary UUID id or `nil` on miss (never raises).
    - MockRepo extended with `from.source: "cairnloop_outbound_bulk_envelopes"` arm and `BulkEnvelope` get clause; seeded via `Process.put(:bulk_envelopes, ...)`; tests pass headless.
    - Integration `@tag :integration # REPO-UNAVAILABLE` placeholder test present for the operator's Postgres-host run.
    - `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/governance_test.exs` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-05 auditor metadata shape regression tests in outbound_test.exs</name>
  <files>test/cairnloop/outbound_test.exs</files>
  <read_first>
    - test/cairnloop/outbound_test.exs lines 1-200 (existing `TestAuditor` defmodule + `assert results.audit.metadata == ...` pattern at lines 177-191 — THE analog)
    - lib/cairnloop/outbound.ex lines 95-98 (existing `Outbound.trigger/2` `auditor.audit(:outbound_trigger, actor, %{conversation_id, template_id})` call)
    - lib/cairnloop/outbound.ex lines 339-343 (existing `bulk_trigger_submit/6` `auditor.audit(:bulk_outbound_trigger, actor, %{bulk_envelope_id, count, template_id})` call)
    - lib/cairnloop/auditor.ex (the `@behaviour Cairnloop.Auditor` contract — `audit(multi, action, actor, metadata)` callback shape)
    - .planning/phases/26-observability-polish/26-CONTEXT.md `<decisions>` block — D-05 (audit-write substrate exists; pin metadata shape via regression test)
    - .planning/phases/26-observability-polish/26-RESEARCH.md "Phase Requirements → Test Map" lines 891-892 — the D-05 acceptance criteria
    - .planning/phases/26-observability-polish/26-PATTERNS.md "Wave 2, Plan 01 — `test/cairnloop/outbound_test.exs` (ADD auditor-shape regression)" section
  </read_first>
  <behavior>
    - Test 1 (`:outbound_trigger` exact metadata key set): instantiate a `MapShapeAuditor` (similar to the existing `TestAuditor` at lines 177-191, but captures the metadata into a `send/2` to test process); call `Outbound.trigger(1, template_id: "test", actor: "system", auditor: MapShapeAuditor)`; `assert_receive {:audited, %{action: :outbound_trigger, actor: "system", metadata: metadata}}, 500`; `assert Map.keys(metadata) |> Enum.sort() == [:conversation_id, :template_id]`; AND `assert metadata == %{conversation_id: 1, template_id: "test"}` (exact equality — no extras).
    - Test 2 (`:bulk_outbound_trigger` exact metadata key set): call `Outbound.bulk_trigger([1, 2], template_id: "test", rendered_body: "Body", actor: "system", auditor: MapShapeAuditor)` against a MockRepo configured to succeed; `assert_receive {:audited, %{action: :bulk_outbound_trigger, actor: "system", metadata: metadata}}, 500`; `assert Map.keys(metadata) |> Enum.sort() == [:bulk_envelope_id, :count, :template_id]`; AND `assert metadata.count == 2 and metadata.template_id == "test" and is_binary(metadata.bulk_envelope_id)`. The `:bulk_envelope_id` value is a UUID generated at runtime so check `is_binary/1` rather than exact equality.
    - Test 3 (`:outbound_trigger` rejects extra keys — NEGATIVE assertion): same as Test 1; assert `refute Map.has_key?(metadata, :actor)`; `refute Map.has_key?(metadata, :rendered_body)`; `refute Map.has_key?(metadata, :bulk_envelope_id)`. Pins that the lane stays narrow.
    - Test 4 (`:bulk_outbound_trigger` rejects extra keys — NEGATIVE assertion): same as Test 2; assert `refute Map.has_key?(metadata, :rendered_body)`; `refute Map.has_key?(metadata, :recipient_conversation_ids)`; `refute Map.has_key?(metadata, :actor)`; `refute Map.has_key?(metadata, :effective_cap)`. Pins that the lane stays narrow.
  </behavior>
  <action>
    Patch `test/cairnloop/outbound_test.exs`:

    1. Add a new `describe "auditor metadata shape regression (Phase 26 OBS-02 D-05)"` block at the bottom of the file (after any existing describe blocks, before any top-level `@tag :integration` block).

    2. Define a `MapShapeAuditor` defmodule INSIDE the describe block (or at the top of the file alongside the existing `TestAuditor` at lines 177-191) implementing `@behaviour Cairnloop.Auditor`:
       ```
       defmodule MapShapeAuditor do
         @behaviour Cairnloop.Auditor
         def audit(multi, action, actor, metadata) do
           send(self(), {:audited, %{action: action, actor: actor, metadata: metadata}})
           Ecto.Multi.run(multi, :audit, fn _repo, _changes -> {:ok, :captured} end)
         end
       end
       ```
       Note: ExUnit tests run inside processes spawned per-test; `send(self(), ...)` from inside `auditor.audit/4` lands in the test's mailbox because the auditor callback fires inside the same `Ecto.Multi` execution that runs in the test process. This is the same pattern `MockNotifier.on_outbound_triggered/2` uses in `test/cairnloop/workers/outbound_worker_test.exs` lines 30-39.

    3. Write Tests 1-4 enumerated in `<behavior>`. Use `setup` to ensure `Application.put_env(:cairnloop, :repo, MockRepo)` (mirror existing setup in the same file) and `on_exit` cleanup.

    4. DO NOT modify the existing `TestAuditor` block at lines 177-191 — that one stays as the positive integration probe for the auditor lane (Phase 22 substrate). The new `MapShapeAuditor` is a separate, narrower regression that pins the metadata key SET.

    Run `mix compile --warnings-as-errors` and `mix test test/cairnloop/outbound_test.exs`. Headless arms must pass.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | tee /tmp/26-02-task2-compile.log; grep -c -E "^(warning|error):" /tmp/26-02-task2-compile.log; mix test test/cairnloop/outbound_test.exs</automated>
    <source_assertions>
      - `grep -c "describe \"auditor metadata shape regression (Phase 26 OBS-02 D-05)\"" test/cairnloop/outbound_test.exs` returns `1`.
      - `grep -c "defmodule MapShapeAuditor" test/cairnloop/outbound_test.exs` returns `1`.
      - `grep -c "Map.keys(metadata) |> Enum.sort() == \\[:conversation_id, :template_id\\]" test/cairnloop/outbound_test.exs` returns `1`.
      - `grep -c "Map.keys(metadata) |> Enum.sort() == \\[:bulk_envelope_id, :count, :template_id\\]" test/cairnloop/outbound_test.exs` returns `1`.
      - `grep -c "refute Map.has_key?(metadata, :rendered_body)" test/cairnloop/outbound_test.exs | tr -d '[:space:]'` is at least `2` (Tests 3 + 4 both refute).
    </source_assertions>
  </verify>
  <done>
    - `test/cairnloop/outbound_test.exs` contains a new `describe "auditor metadata shape regression (Phase 26 OBS-02 D-05)"` block with 4 tests pinning the EXACT auditor metadata key sets for both `:outbound_trigger` (`[:conversation_id, :template_id]`) and `:bulk_outbound_trigger` (`[:bulk_envelope_id, :count, :template_id]`).
    - Negative assertions explicitly `refute` extra keys (`:rendered_body`, `:recipient_conversation_ids`, `:actor`, `:effective_cap`, `:bulk_envelope_id` on the single-trigger lane) so future refactors that drift the shape fail loudly.
    - The existing Phase 22 `TestAuditor` block at lines 177-191 is untouched (positive integration probe still passes).
    - `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/outbound_test.exs` exits 0.
    - Full headless suite `mix test` exits 0 (modulo documented baseline).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host app / future admin LiveView → `Cairnloop.Governance.list_recent_bulk_outbound_envelopes/1` | Untrusted callers can request bulk envelope history; unbounded reads (`limit: 100_000`) could exhaust memory or DB connection pool. |
| Library → `Cairnloop.Repo` (Postgres) | Audit reads materialize rows from the durable substrate. Phase 26 only ADDS reads; no new write paths. |
| `Cairnloop.Auditor` callback → host implementation | The metadata map crosses the library boundary. Drift in the key set (e.g., a future PR accidentally adding `:rendered_body`) would leak free-text into a host's audit log. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06 | Denial of service (unbounded read) | `Cairnloop.Governance.list_recent_bulk_outbound_envelopes/1` | mitigate | Module-attribute `@bulk_envelope_hard_cap 500`; `raise ArgumentError` BEFORE the query if `limit > 500`. Pinned by Test 3 (`assert_raise ArgumentError`). Defense-in-depth: host apps cannot bypass via `Cairnloop.Repo.all/1` direct (D-14 — Threat T-25-04 grep enforces no direct queries from web layer). |
| T-26-07 | Information disclosure (audit metadata drift) | `auditor.audit/4` calls in `Outbound.trigger/2` + `bulk_trigger_submit/6` | mitigate | Tests 3 + 4 in Task 2 `refute Map.has_key?` for `:rendered_body`, `:recipient_conversation_ids`, `:actor` and other PII-rich keys. The narrow metadata key SET is pinned by `Enum.sort()` equality assertion. Future refactors adding keys fail the regression. |
| T-26-08 | Tampering (D-14 facade bypass) | `lib/cairnloop/governance.ex` | mitigate | Source assertion `grep -c "Cairnloop.Repo\\." lib/cairnloop/governance.ex == 0` proves the facade never references the concrete repo module — only `repo()` indirection. New functions follow the same pattern. |
| T-26-09 | Tampering | `Cairnloop.Outbound.BulkEnvelope` schema (`status` enum) | accept | Phase 25 schema is sealed; this plan only READS. No new `Ecto.Changeset` or `update/2` path is added on the schema. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed — RESEARCH "Package Legitimacy Audit" not applicable. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 after each task.
- `mix test test/cairnloop/governance_test.exs` exits 0 (headless) — all Task 1 behaviors pass against MockRepo.
- `mix test test/cairnloop/outbound_test.exs` exits 0 — all Task 2 behaviors pass; existing Phase 22 `TestAuditor` test still green.
- Full headless suite `mix test` exits 0 (modulo documented Phase 25 `@tag :integration` BLOCKING gates and the baseline `Automation.DraftTest` pre-existing failure).
- D-14 enforcement: source grep proves `lib/cairnloop/governance.ex` contains zero `Cairnloop.Repo.` references (all access through `repo()` indirection).
- D-05 enforcement: the auditor metadata regression block exists with negative `refute Map.has_key?` for extra keys on both lanes.
- Integration handoff: the `@tag :integration # REPO-UNAVAILABLE` placeholder in `governance_test.exs` documents the operator-host check for Postgres-backed envelope round-trip.
</verification>

<success_criteria>
1. **OBS-02 facade complete:** `Cairnloop.Governance.list_recent_bulk_outbound_envelopes/1` + `get_bulk_outbound_envelope/1` exist on the narrow facade, go through `repo().all/1` / `repo().get/2`, enforce the 500-row hard cap, and accept the `:status` filter.
2. **D-14 narrow-facade contract preserved:** zero `Cairnloop.Repo` direct references in `governance.ex`.
3. **D-05 regression complete:** the auditor metadata shape for `:outbound_trigger` (`[:conversation_id, :template_id]`) and `:bulk_outbound_trigger` (`[:bulk_envelope_id, :count, :template_id]`) is pinned via `Enum.sort()` exact equality + negative `refute Map.has_key?` on PII-rich keys.
4. **REPO-UNAVAILABLE handoff captured:** integration `@tag :integration # REPO-UNAVAILABLE` placeholder test in `governance_test.exs` documents the operator's Postgres-host check.
5. **Build/test gates (D-12):** `mix compile --warnings-as-errors` clean; `mix test` (headless) green modulo documented baseline.
6. **No sealed surface mutated:** existing `list_eligible_conversation_ids_for_bulk_recovery/1`, `preview_bulk_recovery_cohort/1`, `Outbound.trigger/2`, and `Outbound.bulk_trigger/2` are byte-for-byte unchanged.
</success_criteria>

<output>
Create `.planning/phases/26-observability-polish/26-02-SUMMARY.md` when done.
</output>
