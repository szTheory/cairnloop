---
phase: 26-observability-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/outbound/telemetry/traces.ex
  - test/cairnloop/outbound/telemetry/traces_test.exs
  - lib/cairnloop/workers/outbound_worker.ex
  - test/cairnloop/workers/outbound_worker_test.exs
  - lib/cairnloop/outbound.ex
  - test/cairnloop/outbound_test.exs
  - lib/cairnloop/telemetry.ex
autonomous: true
requirements:
  - OBS-01
must_haves:
  truths:
    - "Hosts attaching to `[:cairnloop, :outbound, :delivery, :sent]` receive a point-in-time event each time `OutboundWorker.perform/1` runs a successful arm, with `metadata.reason` in {`:notifier_ok`, `:no_notifier_configured`} and `measurements.count == 1`."
    - "Hosts attaching to `[:cairnloop, :outbound, :delivery, :failed]` receive a point-in-time event when the configured notifier returns an error, with `metadata.reason == :notifier_returned_error` and `measurements.count == 1`."
    - "Hosts attaching to `[:cairnloop, :outbound, :trace, :delivery_sent]` / `:delivery_failed` receive an OI trace with `metadata[\"openinference.span.kind\"] == \"TOOL\"` carrying attribution refs only (no `:content`, no `:rendered_body`)."
    - "Hosts attaching to `[:cairnloop, :outbound, :trace, :trigger_started | :trigger_completed | :trigger_failed]` receive OI traces with `\"openinference.span.kind\" == \"GUARDRAIL\"` around the existing sealed `[:cairnloop, :outbound, :triggered, :start|:stop|:exception]` span (the bounded-metrics span is untouched)."
    - "Hosts attaching to `[:cairnloop, :outbound, :trace, :bulk_submitted]` receive a GUARDRAIL trace from the bulk submit lane; hosts attaching to `[:cairnloop, :outbound, :trace, :bulk_refused]` receive a GUARDRAIL trace on ALL THREE arms of `bulk_trigger_refused/6` (`{:ok, _}`, `{:error, %Ecto.Changeset{}}`, and `other`)."
    - "`Cairnloop.Outbound.Telemetry.Traces.emit/2` called with an unknown atom returns `:ok` and emits nothing (fail-closed guard clause)."
    - "Telemetry metadata for all bounded-metrics events stays enum-only (D-01): `outcome :: atom`, `count :: integer`, optional `reason :: atom`; never carries `:conversation_id`, `:template_id`, `:actor`, recipient ids, `:bulk_envelope_id`, or free-text payload."
    - "`mix compile --warnings-as-errors` exits 0 and `mix test` (headless) is green."
  artifacts:
    - path: "lib/cairnloop/outbound/telemetry/traces.ex"
      provides: "Cairnloop.Outbound.Telemetry.Traces module — emit/2 + build_metadata/2 + span_kind_for/1 + @events whitelist + guard-clause no-op"
      contains: "defmodule Cairnloop.Outbound.Telemetry.Traces"
    - path: "test/cairnloop/outbound/telemetry/traces_test.exs"
      provides: "Headless unit proof of OI span-kind mapping, payload exclusion, guard-clause no-op, and namespace isolation"
      contains: "defmodule Cairnloop.Outbound.Telemetry.TracesTest"
    - path: "lib/cairnloop/workers/outbound_worker.ex"
      provides: "perform/1 emits `[:cairnloop, :outbound, :delivery, :sent | :failed]` and OI `:delivery_sent | :delivery_failed` on each of FOUR terminal arms"
      contains: "Cairnloop.Outbound.Telemetry.Traces"
    - path: "lib/cairnloop/outbound.ex"
      provides: "trigger/2 emits OI :trigger_started/:trigger_completed/:trigger_failed around the sealed bounded-metrics span; bulk_trigger_submit/6 emits OI :bulk_submitted inside the span; bulk_trigger_refused/6 emits OI :bulk_refused on all 3 arms"
      contains: "alias Cairnloop.Outbound.Telemetry.Traces"
    - path: "lib/cairnloop/telemetry.ex"
      provides: "@moduledoc gains an `## Outbound Events` block documenting both bounded-metrics + OI trace vocabularies"
      contains: "## Outbound Events"
  key_links:
    - from: "lib/cairnloop/workers/outbound_worker.ex"
      to: "Cairnloop.Outbound.Telemetry.Traces"
      via: "alias + Traces.emit(:delivery_sent | :delivery_failed, ...) inside emit_delivery/4 helper"
      pattern: "Cairnloop\\.Outbound\\.Telemetry\\.Traces\\.emit"
    - from: "lib/cairnloop/outbound.ex"
      to: "Cairnloop.Outbound.Telemetry.Traces"
      via: "alias + Traces.emit/2 calls around trigger/2 + inside bulk_trigger_submit/6 + on each bulk_trigger_refused/6 arm"
      pattern: "Traces\\.emit\\(:(trigger_started|trigger_completed|trigger_failed|bulk_submitted|bulk_refused)"
    - from: "lib/cairnloop/telemetry.ex"
      to: "Cairnloop.Outbound.Telemetry.Traces"
      via: "moduledoc cross-reference in the `## Outbound Events` block"
      pattern: "Cairnloop\\.Outbound\\.Telemetry\\.Traces"
---

<objective>
Land the OBS-01 observability substrate: an OpenInference-conformant trace lane for the outbound domain (mirroring Phase 17 verbatim), delivery-side bounded-metrics events on `OutboundWorker.perform/1`, OI trace emissions wired alongside (never replacing) the existing sealed bounded-metrics spans in `Outbound.trigger/2` and `Outbound.bulk_trigger/2`, and a documentation block in `Cairnloop.Telemetry`'s moduledoc.

Purpose: Close the OBS-01 gap so hosts can observe outbound triggers AND delivery outcomes on both lanes (low-cardinality bounded metrics for Prometheus/Datadog and attribution-ref OI traces for span-tree consumers like Scoria), without touching any sealed Phase 22/25 public signature.

Output: New `Cairnloop.Outbound.Telemetry.Traces` module + new traces test file; additive emit calls in `OutboundWorker.perform/1` and `Cairnloop.Outbound` at the four delivery arms + the trigger lifecycle + both bulk lanes; new headless test coverage in `outbound_worker_test.exs` and `outbound_test.exs`; an `## Outbound Events` block in `lib/cairnloop/telemetry.ex`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-observability-polish/26-CONTEXT.md
@.planning/phases/26-observability-polish/26-RESEARCH.md
@.planning/phases/26-observability-polish/26-PATTERNS.md
@.planning/phases/26-observability-polish/26-VALIDATION.md
@prompts/cairnloop_brand_book.md
@lib/cairnloop/governance/telemetry/traces.ex
@test/cairnloop/governance/telemetry/traces_test.exs
@lib/cairnloop/telemetry.ex
@lib/cairnloop/outbound.ex
@lib/cairnloop/workers/outbound_worker.ex
@test/cairnloop/workers/outbound_worker_test.exs
@test/cairnloop/outbound_test.exs

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From `lib/cairnloop/governance/telemetry/traces.ex` (THE canonical pattern — copy-modify verbatim, swap `:governance` → `:outbound`, swap event list, swap attribution refs):
```elixir
defmodule Cairnloop.Governance.Telemetry.Traces do
  @events [:proposal_created, :proposal_blocked, :approval_requested, :approved, :rejected, :deferred, :expired, :revalidation_passed, :revalidation_failed, :execution_started, :execution_succeeded, :execution_failed]
  @span_kind_tool "TOOL"
  @span_kind_guardrail "GUARDRAIL"

  def emit(event, attrs) when event in @events do
    :telemetry.execute([:cairnloop, :governance, :trace, event], %{count: 1}, build_metadata(event, attrs))
  end
  def emit(_event, _attrs), do: :ok

  defp build_metadata(event, attrs) do
    %{"openinference.span.kind" => span_kind_for(event), tool_proposal_id: attrs[:tool_proposal_id], actor_id: attrs[:actor_id], policy_snapshot_ref: attrs[:tool_proposal_id], decided_by: attrs[:decided_by], attempt: attrs[:attempt]}
  end
  defp span_kind_for(event) when event in [:execution_started, :execution_succeeded, :execution_failed], do: @span_kind_tool
  defp span_kind_for(_event), do: @span_kind_guardrail
end
```

From `lib/cairnloop/telemetry.ex`:
```elixir
def span(event_suffix, metadata, fun) when is_list(event_suffix), do: :telemetry.span([:cairnloop | event_suffix], metadata, fun)
def execute(event_suffix, measurements, metadata) when is_list(event_suffix), do: :telemetry.execute([:cairnloop | event_suffix], measurements, metadata)
```
(Use `Cairnloop.Telemetry.execute/3` for the new bounded-metrics delivery events; use `:telemetry.execute/3` DIRECTLY inside the new `Traces` module — disjoint namespace, mirror Phase 17.)

From `lib/cairnloop/outbound.ex` (existing sealed call sites — Phase 26 ADDS emissions ALONGSIDE, never replaces):
- `trigger/2` body lines 71-103 — `Cairnloop.Telemetry.span([:outbound, :triggered], %{outcome: :triggered}, fn ... end)`.
- `bulk_trigger_submit/6` body lines 298-349 — `Cairnloop.Telemetry.span([:outbound, :bulk, :triggered], %{outcome: :submitted, count: count}, fn ... end)`.
- `bulk_trigger_refused/6` body lines 220-293 — 3-arm `case repo().insert(BulkEnvelope.changeset(...))` with `Cairnloop.Telemetry.execute([:outbound, :bulk, :triggered], %{count: count}, %{outcome: :refused_cap_exceeded | :refused_cap_exceeded_audit_failed, count: count})` on every arm; `envelope_id`, `cap`, `count`, `template_id`, `actor` are all already in lexical scope.

From `lib/cairnloop/workers/outbound_worker.ex` lines 70-93 (THE 4-arm case — see RESEARCH Pitfall 1):
- Arm A `:ok` → `update_message_status(message, "sent")` → emit `:sent`, `reason: :notifier_ok`.
- Arm B `{:ok, _}` → `update_message_status(message, "sent")` → emit `:sent`, `reason: :notifier_ok`.
- Arm C `error` → `update_message_status(message, "failed")` → emit `:failed`, `reason: :notifier_returned_error`, then `{:error, error}`.
- Arm D outer `_ ->` (no notifier) → `update_message_status(message, "sent")` → emit `:sent`, `reason: :no_notifier_configured`, then `:ok`.
- The current `perform/1` head is `def perform(%Oban.Job{args: %{"message_id" => message_id}})` — to make `bulk_envelope_id` available inside the new `emit_delivery/4` helper, change to `def perform(%Oban.Job{args: %{"message_id" => message_id} = args})` (purely additive — `args` was already the whole map).

From `test/cairnloop/governance/telemetry/traces_test.exs` lines 23-37 (test-attach helper — copy-modify verbatim):
```elixir
defp attach_trace_handler(test_id, event_atom) do
  handler_id = "test-trace-handler-#{test_id}-#{event_atom}"
  :telemetry.attach(handler_id, [:cairnloop, :governance, :trace, event_atom], fn _event, _measurements, metadata, _config -> send(self(), {:trace_metadata, metadata}) end, nil)
  on_exit(fn -> :telemetry.detach(handler_id) end)
  handler_id
end
```

From `test/cairnloop/workers/outbound_worker_test.exs` lines 7-58 (in-file `MockRepo` + `MockNotifier` + `ErrorNotifier` + `setup` already exists — extend it).

From `test/cairnloop/outbound_test.exs` lines 149-175 (existing in-file telemetry-attach pattern — extend with new `describe "OI trace lane (Phase 26 D-03)"`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Cairnloop.Outbound.Telemetry.Traces module + headless test</name>
  <files>lib/cairnloop/outbound/telemetry/traces.ex, test/cairnloop/outbound/telemetry/traces_test.exs</files>
  <read_first>
    - lib/cairnloop/governance/telemetry/traces.ex (THE pattern — copy-modify verbatim end-to-end; 130 lines)
    - test/cairnloop/governance/telemetry/traces_test.exs (THE test pattern — copy-modify verbatim, ~190 lines)
    - .planning/phases/26-observability-polish/26-CONTEXT.md `<decisions>` block — D-01 (enum-only labels), D-03 (OI trace lane, 7-atom @events whitelist, span-kind taxonomy, attribution-ref-only metadata, fail-closed guard clause)
    - .planning/phases/26-observability-polish/26-RESEARCH.md Pattern 1 (lines 248-329) and Code Example 1 (lines 588-698) — full module skeleton + test pattern
    - .planning/phases/26-observability-polish/26-RESEARCH.md Pitfall 5 (lines 532-540) — `:telemetry_test` is NOT in deps; use plain `:telemetry.attach/4`
    - .planning/phases/26-observability-polish/26-RESEARCH.md Open Question 3 (lines 969-972) — include `:effective_cap` in `:bulk_refused` metadata only
    - .planning/phases/26-observability-polish/26-PATTERNS.md "Wave 1, Plan 01 — `lib/cairnloop/outbound/telemetry/traces.ex` (NEW)" and "Wave 1, Plan 01 — `test/cairnloop/outbound/telemetry/traces_test.exs` (NEW)" sections
    - CLAUDE.md "Build / test conventions" (warnings-as-errors mandatory)
  </read_first>
  <behavior>
    - Test 1 (span-kind TOOL): attach handler to `[:cairnloop, :outbound, :trace, :delivery_sent]` → `Traces.emit(:delivery_sent, @attrs)` → `assert_receive {:trace_metadata, meta}` with `meta["openinference.span.kind"] == "TOOL"`.
    - Test 2 (span-kind TOOL for failed): same with `:delivery_failed` → `"TOOL"`.
    - Test 3 (span-kind GUARDRAIL for trigger_started): `:trigger_started` → `"GUARDRAIL"`.
    - Test 4 (span-kind GUARDRAIL for trigger_completed): `:trigger_completed` → `"GUARDRAIL"`.
    - Test 5 (span-kind GUARDRAIL for trigger_failed): `:trigger_failed` → `"GUARDRAIL"`.
    - Test 6 (span-kind GUARDRAIL for bulk_submitted): `:bulk_submitted` → `"GUARDRAIL"`.
    - Test 7 (span-kind GUARDRAIL for bulk_refused): `:bulk_refused` → `"GUARDRAIL"` AND `meta[:effective_cap]` is present (per RESEARCH OQ3 — cap on `:bulk_refused` only).
    - Test 8 (attribution refs present): `meta` carries `:bulk_envelope_id`, `:conversation_id`, `:template_id`, `:actor_id`, `:outcome` from `@attrs`.
    - Test 9 (payload exclusion — `:content`): emit with extra `:content` key → assert `refute Map.has_key?(meta, :content)`.
    - Test 10 (payload exclusion — `:rendered_body`): same with `:rendered_body` → refute.
    - Test 11 (payload exclusion — `:refused_reason`): same with `:refused_reason` → refute.
    - Test 12 (guard-clause no-op): attach handler to `[:cairnloop, :outbound, :trace, :not_a_real_event]` → `Traces.emit(:not_a_real_event, @attrs)` → `refute_receive :should_not_fire, 100`.
    - Test 13 (namespace isolation): attach handler to bounded-metrics path `[:cairnloop, :outbound, :delivery, :sent]` → `Traces.emit(:delivery_sent, @attrs)` → `refute_receive :leaked, 100`. Proves the disjoint 4-segment trace namespace per D-03.
    - Test 14 (`:effective_cap` only on `:bulk_refused`): for `:bulk_submitted` and `:delivery_sent` events, assert `refute Map.has_key?(meta, :effective_cap)` even when attrs include it. For `:bulk_refused`, assert it IS present.
  </behavior>
  <action>
    Create `lib/cairnloop/outbound/telemetry/traces.ex` defining `Cairnloop.Outbound.Telemetry.Traces` by verbatim copy-modify of `lib/cairnloop/governance/telemetry/traces.ex`, swapping:

    1. Module name: `Cairnloop.Governance.Telemetry.Traces` → `Cairnloop.Outbound.Telemetry.Traces`.
    2. `@moduledoc`: rewrite Phase 17 references to Phase 26 D-03 references; explain disjoint 4-segment namespace `[:cairnloop, :outbound, :trace, <event_atom>]` vs bounded-metrics 3-/4-segment `[:cairnloop, :outbound, ...]`; explain OI span kinds (`"TOOL"` for delivery, `"GUARDRAIL"` for lifecycle); explain payload exclusion (no `:content`, `:rendered_body`, `:refused_reason`); explain fail-closed guard clause; document that lifecycle events carry attribution refs `:bulk_envelope_id`, `:conversation_id`, `:template_id`, `:actor_id`, `:outcome`, and `:bulk_refused` ALSO carries `:effective_cap` (per RESEARCH OQ3).
    3. `@events` list: replace with the 7 atoms from D-03 — `[:trigger_started, :trigger_completed, :trigger_failed, :bulk_submitted, :bulk_refused, :delivery_sent, :delivery_failed]`.
    4. `@span_kind_tool` / `@span_kind_guardrail` constants: keep as-is (`"TOOL"`, `"GUARDRAIL"`).
    5. `emit/2` head: keep the `:telemetry.execute/3` direct call (NOT through `Cairnloop.Telemetry.execute/3` — bypasses centralizer per D-03 + RESEARCH Anti-Patterns); swap the event path to `[:cairnloop, :outbound, :trace, event]`. Keep the guard-clause no-op `def emit(_event, _attrs), do: :ok`.
    6. `build_metadata/2`: outbound attribution-ref shape — return a map with `"openinference.span.kind" => span_kind_for(event)`, `bulk_envelope_id: attrs[:bulk_envelope_id]`, `conversation_id: attrs[:conversation_id]`, `template_id: attrs[:template_id]`, `actor_id: attrs[:actor_id]`, `outcome: attrs[:outcome]`. Add a SECOND clause guarded by `when event == :bulk_refused` that additionally includes `effective_cap: attrs[:effective_cap]` (or use a single body with conditional `Map.put(base, :effective_cap, attrs[:effective_cap])` when event is `:bulk_refused`).
    7. `span_kind_for/1`: TOOL only for `[:delivery_sent, :delivery_failed]`; GUARDRAIL for everything else.
    8. Do NOT alias `Cairnloop.Telemetry` — the centralizer is the bounded-metrics surface; OI lane is disjoint by D-03.

    Create `test/cairnloop/outbound/telemetry/traces_test.exs` defining `Cairnloop.Outbound.Telemetry.TracesTest` by verbatim copy-modify of `test/cairnloop/governance/telemetry/traces_test.exs`, swapping:

    - `use ExUnit.Case, async: false` (process-global `:telemetry.attach/4` — per RESEARCH Pitfall 2).
    - `alias Cairnloop.Outbound.Telemetry.Traces`.
    - `attach_trace_handler/2` helper: identical shape, swap namespace to `[:cairnloop, :outbound, :trace, event_atom]`, handler id prefix `"outbound-trace-#{test_id}-#{event_atom}"`.
    - `@attrs` map: `%{bulk_envelope_id: "env-1", conversation_id: 42, template_id: "recovery_v1", actor_id: "actor-1", outcome: :triggered, effective_cap: 25}`.
    - Add all 14 tests enumerated in `<behavior>` above, using `%{test: test_id} = context` for per-test unique handler ids (per RESEARCH Pitfall 2).
    - Use `assert_receive {:trace_metadata, meta}, 500` for positive assertions; `refute_receive ..., 100` for negative assertions.
    - The namespace-isolation test (Test 13) attaches to `[:cairnloop, :outbound, :delivery, :sent]` and asserts the OI emit does NOT fire it (proving trace-path is disjoint, per D-03 + RESEARCH lines 666-680).

    Run `mix compile --warnings-as-errors` to confirm no warnings. Then run `mix test test/cairnloop/outbound/telemetry/traces_test.exs` — all 14 tests must pass.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | tee /tmp/26-01-task1-compile.log; grep -c -E "^(warning|error):" /tmp/26-01-task1-compile.log; mix test test/cairnloop/outbound/telemetry/traces_test.exs</automated>
    <source_assertions>
      - `grep -c "defmodule Cairnloop.Outbound.Telemetry.Traces" lib/cairnloop/outbound/telemetry/traces.ex` returns `1`.
      - `grep -c ":telemetry.execute" lib/cairnloop/outbound/telemetry/traces.ex` returns `1` (direct call, bypassing centralizer).
      - `grep -c "Cairnloop.Telemetry" lib/cairnloop/outbound/telemetry/traces.ex` returns `0` (the OI lane never imports/aliases the centralizer — RESEARCH Anti-Patterns).
      - `grep -cE "@events \\[" lib/cairnloop/outbound/telemetry/traces.ex` returns `1`; module file contains all 7 atoms: `:trigger_started`, `:trigger_completed`, `:trigger_failed`, `:bulk_submitted`, `:bulk_refused`, `:delivery_sent`, `:delivery_failed`.
      - `grep -c "openinference.span.kind" lib/cairnloop/outbound/telemetry/traces.ex` returns `1`.
      - `grep -c "def emit(_event, _attrs)" lib/cairnloop/outbound/telemetry/traces.ex` returns `1` (the fail-closed guard).
      - `grep -c "defmodule Cairnloop.Outbound.Telemetry.TracesTest" test/cairnloop/outbound/telemetry/traces_test.exs` returns `1`.
      - `grep -c "refute Map.has_key?(meta, :rendered_body)" test/cairnloop/outbound/telemetry/traces_test.exs` returns `1` (D-03 payload exclusion).
    </source_assertions>
  </verify>
  <done>
    - `lib/cairnloop/outbound/telemetry/traces.ex` exists with `defmodule Cairnloop.Outbound.Telemetry.Traces`, the 7-atom `@events` whitelist, direct `:telemetry.execute/3` calls, `build_metadata/2` returning the OI span-kind string key + attribution refs only, `span_kind_for/1` mapping delivery atoms to `"TOOL"` and others to `"GUARDRAIL"`, and the fail-closed guard clause for unknown atoms.
    - `test/cairnloop/outbound/telemetry/traces_test.exs` exists with 14 tests passing on the 4-segment trace namespace and proving disjoint isolation from the bounded-metrics path.
    - `mix compile --warnings-as-errors` exits 0 with no new warnings.
    - `mix test test/cairnloop/outbound/telemetry/traces_test.exs` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delivery-side telemetry + OI traces in OutboundWorker.perform/1 (FOUR arms)</name>
  <files>lib/cairnloop/workers/outbound_worker.ex, test/cairnloop/workers/outbound_worker_test.exs</files>
  <read_first>
    - lib/cairnloop/workers/outbound_worker.ex (lines 1-103 in full — current 4-arm `case` block at lines 70-93)
    - lib/cairnloop/outbound/telemetry/traces.ex (NEW — Task 1 output; the `emit/2` contract this task calls)
    - lib/cairnloop/telemetry.ex (the `execute/3` centralizer used for bounded-metrics)
    - test/cairnloop/workers/outbound_worker_test.exs (lines 1-160 — existing `MockRepo`, `MockNotifier`, `ErrorNotifier`, `setup` block, and `# REPO-UNAVAILABLE` `@tag :integration` precedent)
    - .planning/phases/26-observability-polish/26-CONTEXT.md `<decisions>` block — D-01 (enum-only labels), D-02 (delivery telemetry call sites — point-in-time, NOT spans), D-03 (OI trace side-by-side)
    - .planning/phases/26-observability-polish/26-RESEARCH.md Pitfall 1 (lines 458-490) — FOUR arms, not two; arm-by-arm `reason` enum mapping
    - .planning/phases/26-observability-polish/26-RESEARCH.md Open Question 2 (lines 964-967) — delivery `actor_id: nil` recommendation
    - .planning/phases/26-observability-polish/26-RESEARCH.md Code Example 2 (lines 700-756) — full patched perform/1 + `emit_delivery/4` helper
    - .planning/phases/26-observability-polish/26-PATTERNS.md "Wave 1, Plan 01 — `lib/cairnloop/workers/outbound_worker.ex` (ADD)" section (lines 213-294)
    - CLAUDE.md "Architecture posture" (seal completed phases — D-11 unique-clause stays untouched; only ADD telemetry)
  </read_first>
  <behavior>
    - Test 1 (arm A — MockNotifier `:ok`): attach handler to `[:cairnloop, :outbound, :delivery, :sent]`; run `OutboundWorker.perform/1` with default `MockNotifier` (already returns `:ok`); `assert_receive {:delivery_event, %{count: 1}, %{outcome: :sent, reason: :notifier_ok}}, 500`.
    - Test 2 (arm B — `{:ok, _}` tuple): introduce a local `OkTupleNotifier` returning `{:ok, :delivered}`; attach + perform; same `:sent`/`:notifier_ok` assertion as Test 1.
    - Test 3 (arm C — `ErrorNotifier`): attach to `[:cairnloop, :outbound, :delivery, :failed]`; `Application.put_env(:cairnloop, :notifier, ErrorNotifier)` (already defined in the file); run perform; `assert_receive {:delivery_event, %{count: 1}, %{outcome: :failed, reason: :notifier_returned_error}}, 500`. The function still returns `{:error, _}` as before — Phase 22/23 regression-safe.
    - Test 4 (arm D — no notifier): `Application.delete_env(:cairnloop, :notifier)`; attach to `[:cairnloop, :outbound, :delivery, :sent]`; run perform; `assert_receive ... %{outcome: :sent, reason: :no_notifier_configured}`.
    - Test 5 (OI trace lane parity — sent): attach to `[:cairnloop, :outbound, :trace, :delivery_sent]`; run perform with `MockNotifier`; `assert_receive {:trace_metadata, meta}` where `meta["openinference.span.kind"] == "TOOL"`, `meta[:conversation_id] == 10` (the MockRepo conversation id), `meta[:template_id] == "test"` (from MockRepo metadata), `meta[:outcome] == :sent`.
    - Test 6 (OI trace lane parity — failed): attach to `[:cairnloop, :outbound, :trace, :delivery_failed]`; run perform with `ErrorNotifier`; `assert_receive {:trace_metadata, meta}` with `"TOOL"` and `outcome: :failed`.
    - Test 7 (`bulk_envelope_id` threaded): construct an Oban job with `args: %{"message_id" => 1, "bulk_envelope_id" => "env-uuid-77"}`; attach to OI trace lane; perform; `assert meta[:bulk_envelope_id] == "env-uuid-77"`. Confirms the `args` shape extension is wired into `emit_delivery/4`.
    - Test 8 (existing unique-clause regression PRESERVED): the existing unique-clause test in `outbound_worker_test.exs` (Phase 25 D-11 gate) must still pass without modification — this is a NEGATIVE regression assertion (run the existing test, do NOT touch it).
  </behavior>
  <action>
    Patch `lib/cairnloop/workers/outbound_worker.ex`:

    1. Add alias near the existing `alias Cairnloop.{Chat, Message}` (line 68):
       `alias Cairnloop.Outbound.Telemetry.Traces`
    2. Change `perform/1` head signature from `def perform(%Oban.Job{args: %{"message_id" => message_id}})` to `def perform(%Oban.Job{args: %{"message_id" => message_id} = args})` so `args` flows into the new helper. This is purely additive — Phase 25 already documents the full args shape in the moduledoc at lines 7-21.
    3. At each of the FOUR terminal arms (per RESEARCH Pitfall 1 — arms A, B, C, D), immediately AFTER the existing `update_message_status/2` call, call the new private helper `emit_delivery/4`:
       - Arm A (line 79 `:ok ->` branch) → `emit_delivery(:sent, :notifier_ok, message, args)`
       - Arm B (line 82 `{:ok, _} ->` branch) → `emit_delivery(:sent, :notifier_ok, message, args)`
       - Arm C (line 85 `error ->` branch, BEFORE the `{:error, error}` return) → `emit_delivery(:failed, :notifier_returned_error, message, args)`
       - Arm D (line 90 outer `_ ->` branch, BEFORE the `:ok` return) → `emit_delivery(:sent, :no_notifier_configured, message, args)`
    4. Add the private helper `emit_delivery/4` at the bottom of the module (after `update_message_status/2`):
       - Guard: `when outcome in [:sent, :failed]`.
       - First call: `Cairnloop.Telemetry.execute([:outbound, :delivery, outcome], %{count: 1}, %{outcome: outcome, reason: reason})` — enum-only labels per D-01, point-in-time per D-02.
       - Second call: `Traces.emit(if(outcome == :sent, do: :delivery_sent, else: :delivery_failed), %{conversation_id: message.conversation_id, template_id: Map.get(message.metadata || %{}, "template_id"), bulk_envelope_id: Map.get(args, "bulk_envelope_id"), actor_id: nil, outcome: outcome})` — OI trace lane per D-03 and RESEARCH OQ2 (`actor_id: nil` at delivery time — trigger-time actor is captured at the trigger event, delivery is system-initiated).
       - Add a single-line comment above the helper: `# Phase 26 OBS-01 D-02 + D-03: bounded-metrics + OI trace, side-by-side, enum-only labels.`
    5. Do NOT change `update_message_status/2` (line 95-102). Do NOT change the `use Oban.Worker` unique-clause (line 64-66) — Phase 25 D-11 is sealed.

    Patch `test/cairnloop/workers/outbound_worker_test.exs`:

    1. Add a new `defmodule OkTupleNotifier do ... end` next to the existing `MockNotifier` / `ErrorNotifier` (after line 41), `@behaviour Cairnloop.Notifier`, with `on_outbound_triggered(_, _), do: {:ok, :delivered}`.
    2. Add a new `describe "delivery telemetry (Phase 26 OBS-01 D-02)"` block at the bottom of the file (before any existing `@tag :integration` block).
    3. Define an `attach_delivery_handler/2` helper inside the describe block (mirroring the traces-test pattern from Task 1): `:telemetry.attach(handler_id, [:cairnloop, :outbound, :delivery, outcome_atom], fn _ev, m, md, _c -> send(self(), {:delivery_event, m, md}) end, nil)` paired with `on_exit(fn -> :telemetry.detach(handler_id) end)`. Use `%{test: test_id} = context` for unique handler ids.
    4. Define an `attach_trace_handler/2` helper for the OI lane: `:telemetry.attach(handler_id, [:cairnloop, :outbound, :trace, event_atom], fn _ev, _m, md, _c -> send(self(), {:trace_metadata, md}) end, nil)` + `on_exit` detach.
    5. Write Tests 1-8 enumerated in `<behavior>`. For Test 7, build the Oban job explicitly: `%Oban.Job{args: %{"message_id" => 1, "bulk_envelope_id" => "env-uuid-77"}}` and call `Cairnloop.Workers.OutboundWorker.perform/1` directly.
    6. Do NOT modify the existing unique-clause regression block (Phase 25 D-11 gate).

    Run `mix compile --warnings-as-errors` and `mix test test/cairnloop/workers/outbound_worker_test.exs`. The headless subset (everything not `@tag :integration`) must pass.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | tee /tmp/26-01-task2-compile.log; grep -c -E "^(warning|error):" /tmp/26-01-task2-compile.log; mix test test/cairnloop/workers/outbound_worker_test.exs</automated>
    <source_assertions>
      - `grep -c "alias Cairnloop.Outbound.Telemetry.Traces" lib/cairnloop/workers/outbound_worker.ex` returns `1`.
      - `grep -c "defp emit_delivery(" lib/cairnloop/workers/outbound_worker.ex` returns `1`.
      - `grep -c "emit_delivery(:sent, :notifier_ok," lib/cairnloop/workers/outbound_worker.ex` returns `2` (arms A + B both use this).
      - `grep -c "emit_delivery(:failed, :notifier_returned_error," lib/cairnloop/workers/outbound_worker.ex` returns `1` (arm C).
      - `grep -c "emit_delivery(:sent, :no_notifier_configured," lib/cairnloop/workers/outbound_worker.ex` returns `1` (arm D).
      - `grep -c "Traces.emit" lib/cairnloop/workers/outbound_worker.ex` returns at least `1` (inside `emit_delivery/4`).
      - `grep -cE "use Oban.Worker," lib/cairnloop/workers/outbound_worker.ex` returns `1` AND the existing unique-clause is untouched: `grep -c "unique: \\[period: :infinity, fields: \\[:worker, :args\\], keys: \\[:conversation_id, :template_id, :bulk_envelope_id\\]\\]" lib/cairnloop/workers/outbound_worker.ex` returns `1`.
      - `grep -c "defmodule OkTupleNotifier" test/cairnloop/workers/outbound_worker_test.exs` returns `1`.
      - `grep -c "describe \"delivery telemetry (Phase 26 OBS-01 D-02)\"" test/cairnloop/workers/outbound_worker_test.exs` returns `1`.
    </source_assertions>
  </verify>
  <done>
    - `OutboundWorker.perform/1` emits both bounded-metrics `[:cairnloop, :outbound, :delivery, :sent | :failed]` AND OI `[:cairnloop, :outbound, :trace, :delivery_sent | :delivery_failed]` on all four terminal arms.
    - All 8 new behaviors pass against `MockRepo`/`MockNotifier`/`OkTupleNotifier`/`ErrorNotifier`/no-notifier.
    - The Phase 25 D-11 unique-clause regression test still passes (not touched).
    - `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/workers/outbound_worker_test.exs` exits 0 (headless arms).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OI trace emissions in Outbound.trigger/2 + bulk_trigger_submit/6 + bulk_trigger_refused/6, plus Cairnloop.Telemetry moduledoc</name>
  <files>lib/cairnloop/outbound.ex, test/cairnloop/outbound_test.exs, lib/cairnloop/telemetry.ex</files>
  <read_first>
    - lib/cairnloop/outbound.ex (lines 1-351 in full — sealed call sites at 71-103 trigger/2, 220-293 bulk_trigger_refused/6, 298-349 bulk_trigger_submit/6)
    - lib/cairnloop/outbound/telemetry/traces.ex (NEW — Task 1 output; the `emit/2` contract)
    - lib/cairnloop/telemetry.ex (lines 1-62 — moduledoc gets new `## Outbound Events` block per D-04)
    - lib/cairnloop/governance.ex lines 380-392 + 946-952 (THE canonical "Telemetry then Traces side-by-side" comment + call shape — RESEARCH lines 339-353)
    - test/cairnloop/outbound_test.exs lines 1-200 (existing in-file telemetry-attach pattern + `TestAuditor` + MockRepo setup)
    - .planning/phases/26-observability-polish/26-CONTEXT.md `<decisions>` block — D-03 (OI trace lane wired around trigger + bulk lanes), D-04 (moduledoc "Outbound Events" block)
    - .planning/phases/26-observability-polish/26-RESEARCH.md Pattern 2 (lines 331-361), Pitfall 3 (lines 512-520) — bulk_refused has THREE arms; Pitfall 7 (additive, not replacement)
    - .planning/phases/26-observability-polish/26-RESEARCH.md Open Question 3 (lines 969-972) — `:effective_cap` on `:bulk_refused` only
    - .planning/phases/26-observability-polish/26-PATTERNS.md "Wave 1, Plan 01 — `lib/cairnloop/outbound.ex` `trigger/2`", "...`bulk_trigger_submit/6`", "...`bulk_trigger_refused/6`", and "Wave 1, Plan 01 — `lib/cairnloop/telemetry.ex` `@moduledoc`" sections
    - CLAUDE.md "Architecture posture" (seal completed phases — DO NOT restructure the sealed `:telemetry.span/3` blocks; emit AROUND/AFTER)
  </read_first>
  <behavior>
    - Test 1 (trigger/2 OI happy path — `:trigger_started`): attach to `[:cairnloop, :outbound, :trace, :trigger_started]`; call `Outbound.trigger(1, template_id: "test", actor: "operator_42")`; `assert_receive {:trace_metadata, meta}, 500` with `meta["openinference.span.kind"] == "GUARDRAIL"`, `meta[:conversation_id] == 1`, `meta[:template_id] == "test"`, `meta[:actor_id] == "operator_42"`, `meta[:outcome] == :triggered`.
    - Test 2 (trigger/2 OI completion): attach to `[:cairnloop, :outbound, :trace, :trigger_completed]`; same call; assert receive with GUARDRAIL.
    - Test 3 (trigger/2 OI failure): force the test MockRepo to return `{:error, ...}` from `transaction/1` (mirror existing failure-path test patterns in the file); attach to `[:cairnloop, :outbound, :trace, :trigger_failed]`; call `Outbound.trigger/2`; assert receive with GUARDRAIL. (If the existing MockRepo doesn't have a failure switch, add one via `Process.put(:mock_repo_force_transaction_failure, true)` and read it in the MockRepo — mirror existing failure-arm patterns.)
    - Test 4 (bulk_trigger submit OI): attach to `[:cairnloop, :outbound, :trace, :bulk_submitted]`; call `Outbound.bulk_trigger([1, 2], template_id: "test", rendered_body: "Body", actor: "op_99")` against a configured MockRepo + cap; assert GUARDRAIL with `meta[:bulk_envelope_id]` non-nil, `meta[:count]` not present (count is enum-only on bounded-metrics; OI carries attribution refs only — `:outcome` is the lane signal).
    - Test 5 (bulk_trigger refused — arm A `{:ok, _envelope}`): trigger cap exceedance against a MockRepo whose `insert/1` returns `{:ok, _}`; attach to `[:cairnloop, :outbound, :trace, :bulk_refused]`; assert GUARDRAIL with `meta[:outcome] == :refused_cap_exceeded`, `meta[:effective_cap]` present, `meta[:bulk_envelope_id]` non-nil.
    - Test 6 (bulk_trigger refused — arm B `{:error, %Ecto.Changeset{}}`): force MockRepo `insert/1` to return a changeset error; attach + assert receive `:bulk_refused` with `meta[:outcome] == :refused_cap_exceeded_audit_failed`.
    - Test 7 (bulk_trigger refused — arm C `other`): force MockRepo `insert/1` to return `{:foo, :bar}` or similar; assert receive `:bulk_refused` with `meta[:outcome] == :refused_cap_exceeded_audit_failed`.
    - Test 8 (moduledoc invariant — `Cairnloop.Telemetry` `@moduledoc` contains `## Outbound Events`): no test file change needed; verified via source assertion grep (per RESEARCH OQ1 — no pinning test, follow existing precedent).
  </behavior>
  <action>
    Patch `lib/cairnloop/outbound.ex`:

    1. Add alias at the top of the module near the existing `alias Cairnloop.Message` + `alias Cairnloop.Outbound.BulkEnvelope` (line 28-29):
       `alias Cairnloop.Outbound.Telemetry.Traces`
    2. `trigger/2` (current body lines 71-103) — DO NOT restructure the `Cairnloop.Telemetry.span([:outbound, :triggered], telemetry_meta, fn ... end)` block (sealed per CLAUDE.md). Add OI emissions AROUND it:
       - BEFORE the `Cairnloop.Telemetry.span(...)` line, emit `Traces.emit(:trigger_started, %{conversation_id: conversation_id, template_id: template_id, actor_id: actor, outcome: :triggered, bulk_envelope_id: Keyword.get(opts, :bulk_envelope_id)})`.
       - Capture the span return value: `span_result = Cairnloop.Telemetry.span(...)`. The span returns the `fn`'s first tuple element (`result`).
       - AFTER the span call, branch on `span_result` to emit `Traces.emit(:trigger_completed, ...)` on `{:ok, _}` (with `outcome: :triggered`) or `Traces.emit(:trigger_failed, ...)` on `{:error, _}` (with `outcome: :failed`). Then `span_result` is the function's return.
       - Add comment `# Phase 26 D-03: OI trace lane — additive, fire-and-forget, around the sealed bounded-metrics span.`
    3. `bulk_trigger_submit/6` (current body lines 298-349) — INSIDE the `Cairnloop.Telemetry.span(...)` block's `fn`, AFTER `result = repo().transaction(multi)` (line 345) and BEFORE the return tuple `{result, %{outcome: :submitted, count: count}}` (line 347), emit `Traces.emit(:bulk_submitted, %{bulk_envelope_id: envelope_id, template_id: template_id, actor_id: actor, outcome: :submitted, conversation_id: nil})`. Add comment `# Phase 26 D-03: OI trace lane — bulk envelope is the unit of work; per-recipient traces fire from OutboundWorker.perform/1.`
    4. `bulk_trigger_refused/6` (current body lines 220-293) — on EACH of the three `case repo().insert(...)` arms (lines 253-274 `{:ok, _envelope}`, 260-274 `{:error, %Ecto.Changeset{} = changeset}`, 276-289 `other`), immediately AFTER the existing `Cairnloop.Telemetry.execute(...)` call within that arm, emit `Traces.emit(:bulk_refused, %{bulk_envelope_id: envelope_id, template_id: template_id, actor_id: actor, outcome: <arm-specific outcome atom>, conversation_id: nil, effective_cap: cap})`. The per-arm outcome atoms (matching the existing bounded-metrics outcome):
       - Arm A `{:ok, _envelope}` → `outcome: :refused_cap_exceeded`
       - Arm B `{:error, %Ecto.Changeset{}}` → `outcome: :refused_cap_exceeded_audit_failed`
       - Arm C `other` → `outcome: :refused_cap_exceeded_audit_failed`
    5. Do NOT touch `build_trigger_multi/2` (line 115-160), `bulk_trigger/2` public signature (line ~196), or any other sealed surface.

    Patch `lib/cairnloop/telemetry.ex` `@moduledoc` (D-04):

    - Insert a new section `## Outbound Events` AFTER the `## Knowledge-Maintenance Events` block (after line 45) and BEFORE the closing `"""` (line 46). Document BOTH vocabularies:
      - Bounded-metrics: `[:cairnloop, :outbound, :triggered, :start | :stop | :exception]`, `[:cairnloop, :outbound, :bulk, :triggered, :start | :stop | :exception]`, `[:cairnloop, :outbound, :bulk, :triggered]` (point-in-time), `[:cairnloop, :outbound, :delivery, :sent | :failed]`.
      - OI trace lane: `[:cairnloop, :outbound, :trace, :trigger_started | :trigger_completed | :trigger_failed | :bulk_submitted | :bulk_refused | :delivery_sent | :delivery_failed]`. Cross-reference `Cairnloop.Outbound.Telemetry.Traces`.
      - Cardinality note: outbound bounded-metrics metadata is enum-only (`:outcome`, `:count`, optional `:reason`) per Phase 25 D-B / Phase 26 D-01; attribution refs (`:bulk_envelope_id`, `:conversation_id`, `:template_id`, `:actor_id`) live in the OI trace lane and in the durable rows + auditor metadata.
    - Style: match the existing block format exactly (`## Section`, `The following events are emitted using ...`, bullet list of paths with brief metadata notes). No code blocks — pure prose with backtick-quoted event paths.

    Patch `test/cairnloop/outbound_test.exs`:

    - Add a new `describe "OI trace lane (Phase 26 D-03)"` block at the bottom of the file (before any existing `@tag :integration` block, and before the existing `TestAuditor` block).
    - Define an `attach_trace_handler/2` helper (mirror the Task 1 / Task 2 pattern): `:telemetry.attach(handler_id, [:cairnloop, :outbound, :trace, event_atom], fn _ev, _m, md, _c -> send(self(), {:trace_metadata, md}) end, nil)` + `on_exit/1` detach. Use `%{test: test_id} = context` for unique handler ids.
    - Write Tests 1-7 enumerated in `<behavior>`. For Tests 5-7, force MockRepo failure modes via `Process.put(:mock_repo_force_insert_result, ...)` (or follow whatever the existing test file uses for failure-arm simulation — extend the MockRepo's `insert/1` clause to read the flag). Headless-only — REPO-UNAVAILABLE-safe.

    Run `mix compile --warnings-as-errors` and `mix test test/cairnloop/outbound_test.exs`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | tee /tmp/26-01-task3-compile.log; grep -c -E "^(warning|error):" /tmp/26-01-task3-compile.log; mix test test/cairnloop/outbound_test.exs</automated>
    <source_assertions>
      - `grep -c "alias Cairnloop.Outbound.Telemetry.Traces" lib/cairnloop/outbound.ex` returns `1`.
      - `grep -c "Traces.emit(:trigger_started" lib/cairnloop/outbound.ex` returns `1`.
      - `grep -c "Traces.emit(:trigger_completed" lib/cairnloop/outbound.ex` returns `1`.
      - `grep -c "Traces.emit(:trigger_failed" lib/cairnloop/outbound.ex` returns `1`.
      - `grep -c "Traces.emit(:bulk_submitted" lib/cairnloop/outbound.ex` returns `1`.
      - `grep -c "Traces.emit(:bulk_refused" lib/cairnloop/outbound.ex` returns `3` (one per refused arm — RESEARCH Pitfall 3 enforcement).
      - `grep -c "Cairnloop.Telemetry.span(\\[:outbound, :triggered\\]," lib/cairnloop/outbound.ex` returns `1` (sealed bounded-metrics span untouched).
      - `grep -c "Cairnloop.Telemetry.span(\\[:outbound, :bulk, :triggered\\]," lib/cairnloop/outbound.ex` returns `1`.
      - `grep -E "outcome: :refused_cap_exceeded_audit_failed" lib/cairnloop/outbound.ex | grep -v '^#' | wc -l` returns at least `4` (2 existing bounded-metrics + 2 new Traces.emit on arms B+C).
      - `grep -c "## Outbound Events" lib/cairnloop/telemetry.ex` returns `1`.
      - `grep -c "Cairnloop.Outbound.Telemetry.Traces" lib/cairnloop/telemetry.ex` returns at least `1` (moduledoc cross-reference).
      - `grep -c "describe \"OI trace lane (Phase 26 D-03)\"" test/cairnloop/outbound_test.exs` returns `1`.
    </source_assertions>
  </verify>
  <done>
    - `Outbound.trigger/2` emits `Traces.emit(:trigger_started, ...)` BEFORE the sealed bounded-metrics span and `Traces.emit(:trigger_completed | :trigger_failed, ...)` AFTER, branching on the span return.
    - `Outbound.bulk_trigger_submit/6` emits `Traces.emit(:bulk_submitted, ...)` after `repo().transaction(multi)` inside the sealed span's `fn`.
    - `Outbound.bulk_trigger_refused/6` emits `Traces.emit(:bulk_refused, ..., effective_cap: cap, outcome: <arm-specific>)` on ALL THREE arms after the existing `Cairnloop.Telemetry.execute/3` call.
    - `Cairnloop.Telemetry` `@moduledoc` contains an `## Outbound Events` block documenting both bounded-metrics + OI trace vocabularies and cross-referencing `Cairnloop.Outbound.Telemetry.Traces`.
    - The Phase 22/25 sealed bounded-metrics span semantics are untouched (the two `Cairnloop.Telemetry.span` blocks are not restructured).
    - New `describe "OI trace lane (Phase 26 D-03)"` block in `outbound_test.exs` covers all 7 OI emit points (trigger happy/completed/failed + bulk submit + 3 refused arms).
    - `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/outbound_test.exs` exits 0 (headless arms).
    - Full headless suite: `mix test` exits 0 except for the documented Phase 25 `@tag :integration` BLOCKING gates and the documented baseline `Automation.DraftTest` pre-existing failure (per CLAUDE.md MEMORY).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cairnloop library → host telemetry handler (Prometheus / StatsD / Datadog) | Bounded-metrics labels cross here. PII or unbounded-cardinality leakage would explode the host's metrics aggregator. |
| Cairnloop library → host OI trace consumer (Scoria / Phoenix.Tracer / OpenTelemetry exporter) | OI trace metadata crosses here. Free-text payload (rendered body, refused reason, customer content) would leak via sampled span trees. |
| `OutboundWorker.perform/1` notifier branch → external delivery channel | Already trust-bounded in Phase 23/24 — this phase adds observability ONLY; no new code path crosses the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information disclosure | `Cairnloop.Outbound.Telemetry.Traces.build_metadata/2` | mitigate | Whitelist-shape `build_metadata/2` returns ONLY attribution refs (`:bulk_envelope_id`, `:conversation_id`, `:template_id`, `:actor_id`, `:outcome`, and `:effective_cap` on `:bulk_refused` only). Tests 9-11 (`refute Map.has_key?` for `:content`, `:rendered_body`, `:refused_reason`) pin the exclusion contract. Even if a caller passes free-text in `attrs`, the helper drops it on the floor. |
| T-26-02 | Denial of service (cardinality explosion) | `OutboundWorker.perform/1` delivery telemetry + `Outbound.trigger/2` bounded-metrics | mitigate | Enum-only labels per D-01 — `outcome :: atom`, `count :: integer`, `reason :: atom`. Source assertions grep for the exact metadata key set; tests assert `refute Map.has_key?(metadata, :conversation_id)` etc. on bounded-metrics paths. |
| T-26-03 | Tampering / silent regression | Phase 25 D-11 unique-clause on `OutboundWorker` | mitigate | Task 2 source assertion pins the exact `unique:` clause string verbatim. Existing Phase 25 unique-clause regression test is not modified — its continued green is the gate. |
| T-26-04 | Information disclosure | OI trace lane on `:bulk_refused` | accept | `effective_cap` IS included on `:bulk_refused` only (per RESEARCH OQ3) — it's a low-cardinality integer (one of `25` or whatever the host configured), useful for OI consumers correlating refusals against policy changes. Not free-text PII. |
| T-26-05 | Tampering | Sealed `:telemetry.span/3` semantics in `Outbound.trigger/2` + `bulk_trigger_submit/6` | mitigate | Task 3 source assertions grep for the exact span signature strings to confirm the sealed blocks are untouched (only emissions are added AROUND/AFTER). |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed this plan — all libraries already in `mix.exs` from prior phases (RESEARCH "Package Legitimacy Audit" — not applicable). |
</threat_model>

<verification>
- Run `mix compile --warnings-as-errors` after each task — MUST exit 0 with no new warnings.
- Run the touched test file after each task — MUST exit 0.
- After Task 3, run full headless suite `mix test` — MUST exit 0 except for the documented Phase 25 `@tag :integration` BLOCKING gates and the baseline `Automation.DraftTest` pre-existing failure (NOT a Phase 26 regression).
- Confirm no Phase 22/23/24/25 regression: existing tests in `outbound_worker_test.exs` (unique-clause), `outbound_test.exs` (sealed trigger/bulk signatures), `inbox_live_test.exs`, `conversation_live_test.exs` all stay green without modification.
- Confirm OI trace lane is observable end-to-end: a host attaching to `[:cairnloop, :outbound, :trace, :delivery_sent]` after a real trigger+delivery cycle would receive a `"TOOL"` span with the attribution refs (Tests 5-6 in Task 2 prove this in the headless harness).
</verification>

<success_criteria>
1. **OBS-01 (Wave 1 substrate complete):** `Cairnloop.Outbound.Telemetry.Traces` module exists, follows the Phase 17 pattern verbatim (4-segment disjoint namespace, `@events` whitelist, OI span-kind mapping, attribution-ref-only metadata, fail-closed guard clause), and has end-to-end headless test coverage including namespace-isolation proof.
2. **Delivery telemetry on all FOUR arms (D-02):** `OutboundWorker.perform/1` emits the correct bounded-metrics event + OI trace event on each of arms A, B, C, D with the right `reason` enum.
3. **OI traces wired alongside (never replacing) bounded-metrics (D-03):** `Outbound.trigger/2` (GUARDRAIL around the span), `bulk_trigger_submit/6` (GUARDRAIL inside the span after `repo().transaction`), `bulk_trigger_refused/6` (GUARDRAIL on all 3 arms with `:effective_cap` per OQ3). All sealed `:telemetry.span/3` block semantics unchanged.
4. **`Cairnloop.Telemetry` moduledoc (D-04):** `## Outbound Events` block documents BOTH vocabularies and cross-references `Cairnloop.Outbound.Telemetry.Traces`.
5. **Build/test gates (D-12):** `mix compile --warnings-as-errors` clean; `mix test` (headless) green modulo the documented baseline.
6. **No sealed surface mutated:** the Phase 22/25 sealed public signatures (`Outbound.trigger/2`, `Outbound.bulk_trigger/2`, `OutboundWorker` `use Oban.Worker` unique-clause) are byte-for-byte unchanged.
</success_criteria>

<output>
Create `.planning/phases/26-observability-polish/26-01-SUMMARY.md` when done.
</output>
