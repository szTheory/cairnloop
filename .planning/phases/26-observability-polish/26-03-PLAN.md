---
phase: 26-observability-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 26-01
  - 26-02
files_modified:
  - lib/cairnloop/web/inbox_live.ex
  - test/cairnloop/web/inbox_live_test.exs
  - lib/cairnloop/web/conversation_live.ex
  - test/cairnloop/web/conversation_live_test.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "When `@conversations == []` in `InboxLive`, the rendered HTML contains the calm sentence 'No conversations yet.' and does NOT render the bulk-selection header."
    - "When `@conversations != []` but `visible_eligible_ids/1` is empty (e.g., all rows are non-resolved), the bulk-selection header still does NOT render — the existing `has_visible_eligible?/1` gate (Phase 25 plan 03) is preserved as a regression."
    - "The bulk-confirm modal renders a top-right `×` close button inside the dialog with `aria-label=\"Close\"`, a 44px minimum tap target, and `phx-click=\"cancel_bulk_confirm\"`."
    - "The dialog div's inline `style` attribute includes `position: relative` (so the absolute-positioned close button anchors correctly per RESEARCH Pitfall 6)."
    - "When a `:system_outbound` message has `metadata[\"status\"] == \"failed\"`, ConversationLive renders the existing `message-status-chip status-failed` chip with the text 'Failed' AND a calm subhead 'Delivery did not complete. Try again from the Outbound recovery card.' immediately below the message content."
    - "When the same message has `metadata[\"status\"] == \"sent\"` or `\"pending\"`, the failed-bubble subhead does NOT render."
    - "The `outbound_recovery_card/1` component renders a `<section ... aria-label=\"Outbound recovery\">` (a11y verification gate — already passing; pinned by test)."
    - "`mix compile --warnings-as-errors` exits 0 and `mix test` (headless) is green."
  artifacts:
    - path: "lib/cairnloop/web/inbox_live.ex"
      provides: "Empty-state paragraph under `<h1>Inbox</h1>` + top-right `×` close button inside `<div class=\"bulk-confirm-dialog\">` (D-08)"
      contains: "inbox-empty-state"
    - path: "test/cairnloop/web/inbox_live_test.exs"
      provides: "Three new tests covering empty-state render + modal close button + has_visible_eligible regression (D-08)"
      contains: "describe \"Phase 26 D-08 polish\""
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "Failed-bubble calm subhead below message-content for :system_outbound messages with status == \"failed\" (D-09); outbound_recovery_card a11y unchanged (verification gate)"
      contains: "outbound-failed-subhead"
    - path: "test/cairnloop/web/conversation_live_test.exs"
      provides: "Failed-bubble subhead render tests (failed renders, sent/pending do not) + outbound_recovery_card a11y verification (D-09)"
      contains: "describe \"Phase 26 D-09 failed-bubble subhead\""
  key_links:
    - from: "lib/cairnloop/web/inbox_live.ex (close button)"
      to: "existing `cancel_bulk_confirm` event handler"
      via: "phx-click=\"cancel_bulk_confirm\" — no new event handler is added"
      pattern: "phx-click=\"cancel_bulk_confirm\""
    - from: "lib/cairnloop/web/conversation_live.ex (failed subhead)"
      to: "existing `outbound_status_label/1` defp at lines 993-1003"
      via: "conditional render keyed off `outbound_status_label(msg) == \"Failed\"`"
      pattern: "outbound_status_label\\(msg\\) == \"Failed\""
---

<objective>
Land the Phase 26 polish punch list on `InboxLive` and `ConversationLive`: empty-inbox calm sentence, modal close-button affordance, failed-bubble reason-forward subhead, and a11y verification on `outbound_recovery_card`. All edits are pure template patches on the existing `render/1` trees — no new state, no new event handlers, no new LiveViews, and no scope-reduction (D-10 brand-token CSS extraction is explicitly deferred).

Purpose: Close the Phase 26 roadmap success criterion 3 ("tightens empty/error states and outbound affordance polish"). The Phase 22-25 substrate is sealed; this plan delivers the final operator-visible polish pass on what already exists.

Output: Additive HTML/heex template fragments in two LiveView files (`inbox_live.ex` empty-state + modal `×`; `conversation_live.ex` failed-bubble subhead). New `describe` blocks in `inbox_live_test.exs` (D-08 polish + regression) and `conversation_live_test.exs` (D-09 subhead + a11y verification). Zero behavior change; all signals are non-color (text + class), brand-token-vocabulary inline styles, no emoji, no exclamation marks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-observability-polish/26-CONTEXT.md
@.planning/phases/26-observability-polish/26-RESEARCH.md
@.planning/phases/26-observability-polish/26-PATTERNS.md
@.planning/phases/26-observability-polish/26-VALIDATION.md
@prompts/cairnloop_brand_book.md
@lib/cairnloop/web/inbox_live.ex
@test/cairnloop/web/inbox_live_test.exs
@lib/cairnloop/web/conversation_live.ex
@test/cairnloop/web/conversation_live_test.exs

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From `lib/cairnloop/web/inbox_live.ex` lines 113-128 (existing render tree — insertion target 1 for empty state):
```elixir
def render(assigns) do
  ~H"""
  <div class="cairnloop-inbox">
    <h1>Inbox</h1>

    <%= if has_visible_eligible?(@conversations) do %>
      <div class="cairnloop-inbox-bulk-header" style="display: flex; align-items: center; gap: 8px; padding: 8px 0;">
        ...
      </div>
    <% end %>

    <ul>
      ...
```
Empty-state insertion: AFTER `<h1>Inbox</h1>` (line 116) and BEFORE the existing `<%= if has_visible_eligible?(@conversations) do %>` block (line 118). DO NOT modify `has_visible_eligible?/1` — verification-only.

From `lib/cairnloop/web/inbox_live.ex` lines 187-191 (existing dialog div — insertion target 2 for modal close button):
```elixir
<.focus_wrap id="bulk-confirm-wrap">
  <div
    class="bulk-confirm-dialog"
    style="background: var(--cl-surface, #FBF7EE); color: var(--cl-text, #2f241d); border-radius: 18px; width: min(640px, 92vw); max-height: 78vh; box-shadow: 0 24px 60px var(--cl-shadow, rgba(47, 36, 29, 0.18)); overflow: hidden; display: flex; flex-direction: column; padding: 24px;"
  >
    <%= if @bulk_refusal do %>
      ...
```
Modal close-button insertion: mutate the dialog `style` to PREPEND `position: relative; ` to the existing inline style string; insert the `<button>` as the FIRST child of the dialog div (so focus lands there first on modal-open per `<.focus_wrap>` semantics — RESEARCH Pitfall 6).

From `lib/cairnloop/web/conversation_live.ex` lines 767-778 (existing message-card render — failed-bubble subhead target):
```elixir
<%= for msg <- @conversation.messages do %>
  <div class={["message-card", "role-#{msg.role}"]}>
    <div class="message-card-header">
      <span class="message-role-label"><%= message_role_label(msg.role) %></span>
      <%= if outbound_status = outbound_status_label(msg) do %>
        <span class={["message-status-chip", outbound_status_class(msg)]}>
          <%= outbound_status %>
        </span>
      <% end %>
    </div>
    <p class="message-content"><%= msg.content %></p>
  </div>
<% end %>
```
Subhead insertion: AFTER `<p class="message-content"><%= msg.content %></p>` (line 777) and BEFORE the closing `</div>` (line 778). Wrapped in `<%= if outbound_status_label(msg) == "Failed" do %> ... <% end %>`. The existing chip stays UNTOUCHED — Pitfall 7.

From `lib/cairnloop/web/conversation_live.ex` lines 823-847 (outbound_recovery_card — a11y verification target):
```elixir
def outbound_recovery_card(assigns) do
  ~H"""
  <%= if @conversation.status == :resolved do %>
    <section class="rail-card outbound-action-card" aria-label="Outbound recovery">
      ...
    </section>
  <% end %>
  """
end
```
The `<section aria-label="Outbound recovery">` ALREADY exists — D-09 sub-bullet 1 is verification-only; no behavior change.

From `lib/cairnloop/web/conversation_live.ex` lines 993-1003 (`outbound_status_label/1` returns string atoms — the gate for the new subhead's conditional):
```elixir
defp outbound_status_label(%{role: :system_outbound, metadata: metadata}) when is_map(metadata) do
  case Map.get(metadata, "status") do
    "pending" -> "Pending"
    "sent" -> "Sent"
    "failed" -> "Failed"
    _ -> nil
  end
end
defp outbound_status_label(_message), do: nil
```

From `test/cairnloop/web/inbox_live_test.exs` lines 614-642 (existing `render_html/1` + `build_assigns/1` helpers — extend with new polish tests):
```elixir
defp render_html(assigns) do
  assigns =
    assigns
    |> Map.put_new(:selected_ids, MapSet.new())
    |> Map.put_new(:bulk_modal_open, false)
    |> Map.put_new(:bulk_preview, nil)
    |> Map.put_new(:bulk_refusal, nil)
    |> Map.put_new(:host_user_id, nil)
    |> Map.put_new(:conversations, [])
    |> Map.put_new(:flash, %{})
  render_component(&InboxLive.render/1, assigns)
end
```

From `test/cairnloop/web/conversation_live_test.exs` lines 629-659 (existing system_outbound chip render test pattern) and lines 1461-1463 (`render_html/1` helper):
```elixir
defp render_html(assigns) do
  render_component(&ConversationLive.render/1, assigns)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InboxLive empty-state + modal `×` close button (D-08) + has_visible_eligible regression</name>
  <files>lib/cairnloop/web/inbox_live.ex, test/cairnloop/web/inbox_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/inbox_live.ex lines 100-300 (full render tree — insertion targets at lines 116-118 for empty state and 188-191 for modal dialog)
    - test/cairnloop/web/inbox_live_test.exs lines 1-80 (`use ExUnit.Case, async: true`, `EmptyRepo` mount-test pattern, `render_component(&InboxLive.render/1, assigns)` direct-render approach) and lines 614-642 (`render_html/1` + `build_assigns/1` helpers — THE in-file analogs)
    - .planning/phases/26-observability-polish/26-CONTEXT.md `<decisions>` block — D-08 (empty inbox state, no-eligible-resolved verification, modal close-button, refusal banner copy review), D-10 (NOT extracting brand-token CSS — defer)
    - .planning/phases/26-observability-polish/26-RESEARCH.md Pitfall 6 (lines 542-555) — modal close-button must be inside `<.focus_wrap>`, must NOT have `tabindex="-1"`, must have `aria-label="Close"`, dialog needs `position: relative;`
    - .planning/phases/26-observability-polish/26-RESEARCH.md "Specific Ideas" (lines 405-409) — modal close-button placement + 44px tap target + brand-token muted color
    - .planning/phases/26-observability-polish/26-RESEARCH.md Code Example 4 (lines 786-826) — full template patches
    - .planning/phases/26-observability-polish/26-PATTERNS.md "Wave 3, Plan 01 — `lib/cairnloop/web/inbox_live.ex`" and "Wave 3, Plan 01 — `test/cairnloop/web/inbox_live_test.exs`" sections
    - prompts/cairnloop_brand_book.md §7.5 (never state-by-color-alone; calm operator copy register; no emoji, no exclamation marks)
    - CLAUDE.md "Architecture posture" (brand tokens over hardcoded hex with `var(--cl-<token>, <hex>)` fallback so headless tests assert on the brand vocabulary)
  </read_first>
  <behavior>
    - Test 1 (empty inbox state renders): `assigns = build_assigns(conversations: [])`; `html = render_html(assigns)`; `assert html =~ "No conversations yet."`; `assert html =~ ~s(class="inbox-empty-state")`; `assert html =~ "var(--cl-text-muted"`; `refute html =~ "cairnloop-inbox-bulk-header"`.
    - Test 2 (modal close button renders inside dialog): `assigns = build_assigns(bulk_modal_open: true, bulk_preview: %{count: 1, sample: ["Test (#1)"], more: 0, rendered_body: "Body"})`; `html = render_html(assigns)`; `assert html =~ ~s(aria-label="Close")`; `assert html =~ "min-width: 44px"` AND `assert html =~ "min-height: 44px"`; `assert html =~ "var(--cl-text-muted"`. The close-button-text is the multiplication sign character: `assert html =~ "×"` (U+00D7).
    - Test 3 (modal close button calls cancel_bulk_confirm): same render as Test 2; `assert html =~ ~s(phx-click="cancel_bulk_confirm")` AND that this string appears at least TWICE in the rendered HTML (the new `×` button + the existing "Cancel" button — both call the same handler).
    - Test 4 (dialog has position: relative for absolute-positioned close button): same render as Test 2; `assert html =~ "position: relative"` inside the rendered dialog div. Use a targeted regex/substring check to confirm `position: relative` appears alongside `bulk-confirm-dialog` (e.g., `assert html =~ ~r{class="bulk-confirm-dialog"[^>]*position: relative}`).
    - Test 5 (close button is FIRST child of dialog — RESEARCH Pitfall 6 ordering): assert the offset of `aria-label="Close"` in the rendered HTML is LESS than the offset of `id="bulk-confirm-title"` (which is in the existing modal content). `close_offset = :binary.match(html, "aria-label=\"Close\"") |> elem(0); title_offset = :binary.match(html, "bulk-confirm-title") |> elem(0); assert close_offset < title_offset`.
    - Test 6 (refusal banner copy review — verification gate): `assigns = build_assigns(bulk_modal_open: true, bulk_refusal: %{max: 25})`; `html = render_html(assigns)`; `assert html =~ "Batch too large."`; `assert html =~ "Narrow your selection and try again."`; `assert html =~ "var(--cl-danger"` (the danger token is paired with text + SVG — never color-alone, brand §7.5).
    - Test 7 (has_visible_eligible regression — non-resolved cohort): `assigns = build_assigns(conversations: [%Cairnloop.Conversation{id: 1, status: :open, subject: "Test", inserted_at: ~U[2026-05-27 10:00:00Z], updated_at: ~U[2026-05-27 10:00:00Z], host_user_id: "user_1"}])`; `html = render_html(assigns)`; `refute html =~ "cairnloop-inbox-bulk-header"`; `refute html =~ "Select all visible"`. Confirms the existing Phase 25 `has_visible_eligible?/1` gate still works after the new empty-state branch is added.
    - Test 8 (non-empty resolved cohort — empty state NOT rendered): `assigns = build_assigns(conversations: [%Cairnloop.Conversation{id: 1, status: :resolved, subject: "Test", ...}])`; `refute html =~ "No conversations yet."`; `assert html =~ "cairnloop-inbox-bulk-header"`. Confirms the empty-state branch is mutually exclusive with the resolved-cohort branch. **Dependency:** this assertion relies on the existing Phase 25 plan 03 D-14 `has_visible_eligible?/1` gate in `inbox_live.ex` returning `true` when the cohort contains at least one `:resolved` conversation — that gate is the sealed predicate controlling whether `cairnloop-inbox-bulk-header` renders. The new empty-state branch in Task 1 lives as a sibling conditional above the existing `<%= if has_visible_eligible?(@conversations) do %>` block, so this test pins both the empty branch (does NOT render on non-empty) AND the Phase 25 gate (DOES render on a resolved cohort).
  </behavior>
  <action>
    Patch `lib/cairnloop/web/inbox_live.ex`:

    1. Empty-state insertion (between line 116 `<h1>Inbox</h1>` and line 118 `<%= if has_visible_eligible?(@conversations) do %>`):
       Add this exact heex block immediately AFTER the closing `</h1>` and BEFORE the existing `<%= if has_visible_eligible?...` line:
       ```
       <%= if @conversations == [] do %>
         <%!-- Phase 26 D-08: empty inbox state. Calm, reason-forward, brand-aligned (brand book §7.5). --%>
         <p
           class="inbox-empty-state"
           style="margin-top: 12px; font-size: 14px; color: var(--cl-text-muted, rgba(47, 36, 29, 0.62));"
         >
           No conversations yet.
         </p>
       <% end %>
       ```
       Do NOT wrap the existing `has_visible_eligible?` branch in an `else` — they are mutually exclusive (empty list cannot have visible-eligible rows; non-empty list might or might not). Leaving them as sibling conditionals keeps the diff minimal.

    2. Modal close-button insertion at the dialog div (line 188-191):
       - Mutate the dialog `style` attribute string: change `style="background: var(--cl-surface, #FBF7EE); color: var(--cl-text, #2f241d); border-radius: 18px; width: min(640px, 92vw); max-height: 78vh; box-shadow: 0 24px 60px var(--cl-shadow, rgba(47, 36, 29, 0.18)); overflow: hidden; display: flex; flex-direction: column; padding: 24px;"` to PREPEND `position: relative; ` at the start of the value: `style="position: relative; background: var(--cl-surface, #FBF7EE); ...rest..."`.
       - Insert this exact `<button>` element as the FIRST child of `<div class="bulk-confirm-dialog">` (immediately AFTER the opening `<div ...>` tag and BEFORE the existing `<%= if @bulk_refusal do %>`):
       ```
       <%!-- Phase 26 D-08: visible close affordance. Escape already works via phx-window-keydown. Anchored by position:relative on the dialog div. --%>
       <button
         type="button"
         phx-click="cancel_bulk_confirm"
         aria-label="Close"
         style="position: absolute; top: 12px; right: 12px; min-width: 44px; min-height: 44px; border: none; background: transparent; color: var(--cl-text-muted, rgba(47, 36, 29, 0.62)); font-size: 24px; line-height: 1; cursor: pointer; padding: 0;"
       >
         ×
       </button>
       ```
       The `×` is the U+00D7 MULTIPLICATION SIGN, NOT the letter x — per RESEARCH "Don't Hand-Roll" (plain glyph, no custom SVG, no icon library).
    3. Do NOT add a new event handler (`cancel_bulk_confirm` already exists in `handle_event/3` from Phase 25 plan 03).
    4. Do NOT touch `has_visible_eligible?/1`, `visible_eligible_ids/1`, `all_visible_selected?/2`, or any existing event handler. The empty-state branch is additive; the modal close button reuses the existing handler.
    5. Do NOT extract any brand-token inline style into a CSS class (D-10 — explicitly deferred this phase).

    Patch `test/cairnloop/web/inbox_live_test.exs`:

    1. Add a new `describe "Phase 26 D-08 polish"` block at the bottom of the file (before any existing `@tag :integration` block).
    2. Add Tests 1-8 enumerated in `<behavior>`, using the existing `render_html/1` + `build_assigns/1` helpers at lines 614-642. For tests that need specific `Conversation` struct shape, copy the existing struct construction pattern from elsewhere in the test file (look for existing `%Cairnloop.Conversation{...}` instantiations).
    3. For Test 5 (close-button-first-child ordering), use `:binary.match/2` to compare positions of `"aria-label=\"Close\""` and `"bulk-confirm-title"` in the rendered HTML string. If `:binary.match` returns `:nomatch`, the test should fail with a descriptive message.
    4. DO NOT modify the existing tests — only add the new `describe` block.

    Run `mix compile --warnings-as-errors` and `mix test test/cairnloop/web/inbox_live_test.exs`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | tee /tmp/26-03-task1-compile.log; grep -c -E "^(warning|error):" /tmp/26-03-task1-compile.log; mix test test/cairnloop/web/inbox_live_test.exs</automated>
    <source_assertions>
      - `grep -c "inbox-empty-state" lib/cairnloop/web/inbox_live.ex` returns `1`.
      - `grep -c "No conversations yet." lib/cairnloop/web/inbox_live.ex` returns `1`.
      - `grep -c "Phase 26 D-08: empty inbox state" lib/cairnloop/web/inbox_live.ex` returns `1`.
      - `grep -c 'aria-label="Close"' lib/cairnloop/web/inbox_live.ex` returns `1`.
      - `grep -c "position: relative;" lib/cairnloop/web/inbox_live.ex` returns at least `1` (on the dialog div style).
      - `grep -c "min-width: 44px; min-height: 44px;" lib/cairnloop/web/inbox_live.ex` returns at least `1` (the close button).
      - `grep -c "phx-click=\"cancel_bulk_confirm\"" lib/cairnloop/web/inbox_live.ex | tr -d '[:space:]'` is at least `2` (new × button + existing Cancel button + existing Cancel-on-refusal button — the actual count is 3 with the new addition; assert >= 2 to be safe).
      - `grep -c "describe \"Phase 26 D-08 polish\"" test/cairnloop/web/inbox_live_test.exs` returns `1`.
      - `grep -c "No conversations yet." test/cairnloop/web/inbox_live_test.exs` returns at least `1` (Test 1 positive assertion).
      - `grep -c 'aria-label="Close"' test/cairnloop/web/inbox_live_test.exs` returns at least `1` (Test 2 positive assertion).
    </source_assertions>
  </verify>
  <done>
    - InboxLive renders the calm "No conversations yet." paragraph under `<h1>Inbox</h1>` when `@conversations == []`, with brand-token muted color and no toolbar/bulk header.
    - InboxLive's bulk-confirm modal dialog has `position: relative` in its inline style, and a top-right `×` button (`aria-label="Close"`, 44px tap target, `phx-click="cancel_bulk_confirm"`) as its FIRST child.
    - The `has_visible_eligible?/1` regression gate still works on non-resolved cohorts (no bulk header rendered).
    - The refusal banner copy ("Batch too large.", "Narrow your selection and try again.") + danger-token + SVG icon combination passes brand book §7.5 (text + icon + color, never color-alone) — verified by Test 6.
    - `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/web/inbox_live_test.exs` exits 0; no new warnings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConversationLive failed-bubble subhead (D-09) + outbound_recovery_card a11y verification</name>
  <files>lib/cairnloop/web/conversation_live.ex, test/cairnloop/web/conversation_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex lines 760-850 (render tree + outbound_recovery_card) and lines 985-1015 (outbound_status_label/1 + outbound_status_class/1 — the conditional gate for the new subhead)
    - test/cairnloop/web/conversation_live_test.exs lines 1-80 (test file structure, imports, helpers) and lines 629-659 (existing system_outbound chip render test — THE analog) and lines 1461-1463 (`render_html/1` helper)
    - .planning/phases/26-observability-polish/26-CONTEXT.md `<decisions>` block — D-09 (outbound_recovery_card a11y verification + failed-bubble subhead sentence "Delivery did not complete. Try again from the Outbound recovery card.")
    - .planning/phases/26-observability-polish/26-RESEARCH.md Pitfall 7 (lines 558-574) — additive subhead, DO NOT remove or modify existing chip
    - .planning/phases/26-observability-polish/26-RESEARCH.md "Specific Ideas" lines 410-412 — exact copy string
    - .planning/phases/26-observability-polish/26-RESEARCH.md Code Example 3 (lines 758-784) — full template patch
    - .planning/phases/26-observability-polish/26-PATTERNS.md "Wave 3, Plan 01 — `lib/cairnloop/web/conversation_live.ex`" and "Wave 3, Plan 01 — `test/cairnloop/web/conversation_live_test.exs`" sections
    - prompts/cairnloop_brand_book.md §7.5 (failed state now has THREE non-color signals: `.status-failed` class + "Failed" chip text + new reason-forward subhead)
  </read_first>
  <behavior>
    - Test 1 (failed status renders subhead + EXISTING chip): build assigns with one `%Cairnloop.Message{role: :system_outbound, content: "Hey, checking in.", metadata: %{"template_id" => "recovery_v1", "status" => "failed"}}` and a `:resolved` Conversation; `html = render_html(assigns)`; `assert html =~ "message-status-chip status-failed"` (CHIP STILL RENDERS — Pitfall 7 regression gate); `assert html =~ "Failed"`; `assert html =~ "Delivery did not complete. Try again from the Outbound recovery card."`; `assert html =~ "outbound-failed-subhead"`; `assert html =~ "var(--cl-text-muted"`.
    - Test 2 (sent status does NOT render subhead): same assigns but `"status" => "sent"`; `assert html =~ "message-status-chip status-sent"`; `assert html =~ "Sent"`; `refute html =~ "Delivery did not complete"`; `refute html =~ "outbound-failed-subhead"`.
    - Test 3 (pending status does NOT render subhead): same assigns but `"status" => "pending"`; `assert html =~ "message-status-chip status-pending"`; `refute html =~ "Delivery did not complete"`.
    - Test 4 (non-system_outbound role does NOT render subhead): same with `role: :user, content: "Customer message"`; `refute html =~ "Delivery did not complete"`; `refute html =~ "message-status-chip"`.
    - Test 5 (outbound_recovery_card a11y verification — `aria-label="Outbound recovery"` IS present on `:resolved` conversation): build assigns with a `:resolved` Conversation (no special messages required, just status); `assert html =~ ~s(aria-label="Outbound recovery")`; `assert html =~ "outbound-action-card"`. Verification-only — no behavior change.
    - Test 6 (outbound_recovery_card hidden on non-resolved): build assigns with status `:open`; `refute html =~ "outbound-action-card"`. Regression — Phase 24 sealed behavior.
    - Test 7 (subhead position — AFTER message-content, BEFORE closing message-card div): assert the offset of `"outbound-failed-subhead"` in the rendered HTML is GREATER than the offset of `class="message-content"` (the `<p>` tag with the message body). Confirms the subhead is BELOW the message body, not above (per RESEARCH Code Example 3).
  </behavior>
  <action>
    Patch `lib/cairnloop/web/conversation_live.ex`:

    1. Failed-bubble subhead insertion in the message-card render at lines 767-778. Insert this exact heex block AFTER `<p class="message-content"><%= msg.content %></p>` (line 777) and BEFORE the closing `</div>` of the `message-card` div (line 778):
       ```
       <%!-- Phase 26 D-09: calm reason-forward subhead on :failed delivery only. Additive — chip stays untouched (Pitfall 7). --%>
       <%= if outbound_status_label(msg) == "Failed" do %>
         <p
           class="outbound-failed-subhead"
           style="margin: 6px 0 0; font-size: 14px; line-height: 1.4; color: var(--cl-text-muted, rgba(47, 36, 29, 0.62));"
         >
           Delivery did not complete. Try again from the Outbound recovery card.
         </p>
       <% end %>
       ```
       Use `outbound_status_label(msg) == "Failed"` (the string returned by the existing private function at lines 993-1003) as the conditional. DO NOT pattern-match `msg.metadata["status"] == "failed"` directly — going through `outbound_status_label/1` keeps the conditional consistent with the chip's gate (`outbound_status = outbound_status_label(msg)` at line 771).
    2. DO NOT touch the existing chip render (lines 771-775), `outbound_status_label/1` (lines 993-1003), or `outbound_status_class/1` (lines 1005-1014). Pitfall 7 — Phase 22/23/24 tests assert on the chip class strings; any modification breaks them.
    3. DO NOT touch `outbound_recovery_card/1` (lines 825-847) — D-09 sub-bullet 1 is verification-only; the existing `<section ... aria-label="Outbound recovery">` already satisfies the requirement.

    Patch `test/cairnloop/web/conversation_live_test.exs`:

    1. Add a new `describe "Phase 26 D-09 failed-bubble subhead"` block at the bottom of the file (before any existing `@tag :integration` block).
    2. Add Tests 1-7 enumerated in `<behavior>`. Reuse the existing test pattern at lines 629-659 (assigns struct construction with `%Cairnloop.Conversation{...}` and `%Cairnloop.Message{...}`) and the `render_html/1` helper at lines 1461-1463.
    3. For Test 7 (ordering check), use `:binary.match/2` on the rendered HTML to compare the byte-offset of `"class=\"message-content\""` and `"class=\"outbound-failed-subhead\""`. The subhead offset MUST be greater (it comes BELOW the message content).
    4. DO NOT modify any existing tests — Phase 22/23/24 chip tests must stay byte-for-byte unchanged.

    Run `mix compile --warnings-as-errors` and `mix test test/cairnloop/web/conversation_live_test.exs`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | tee /tmp/26-03-task2-compile.log; grep -c -E "^(warning|error):" /tmp/26-03-task2-compile.log; mix test test/cairnloop/web/conversation_live_test.exs</automated>
    <source_assertions>
      - `grep -c "outbound-failed-subhead" lib/cairnloop/web/conversation_live.ex` returns `1`.
      - `grep -c "Delivery did not complete. Try again from the Outbound recovery card." lib/cairnloop/web/conversation_live.ex` returns `1`.
      - `grep -c "outbound_status_label(msg) == \"Failed\"" lib/cairnloop/web/conversation_live.ex` returns `1`.
      - `grep -c "Phase 26 D-09" lib/cairnloop/web/conversation_live.ex` returns at least `1`.
      - Sealed-surface check — chip render untouched: `grep -c "message-status-chip" lib/cairnloop/web/conversation_live.ex` returns at least `1` (the existing chip render remains).
      - Sealed-surface check — `outbound_recovery_card/1` aria-label preserved: `grep -c 'aria-label="Outbound recovery"' lib/cairnloop/web/conversation_live.ex` returns `1`.
      - Sealed-surface check — `outbound_status_label/1` body bytes unchanged: `grep -c "\"failed\" -> \"Failed\"" lib/cairnloop/web/conversation_live.ex` returns `1`.
      - `grep -c "describe \"Phase 26 D-09 failed-bubble subhead\"" test/cairnloop/web/conversation_live_test.exs` returns `1`.
      - `grep -c "Delivery did not complete" test/cairnloop/web/conversation_live_test.exs | tr -d '[:space:]'` is at least `1` (Test 1 positive assertion).
      - `grep -c 'aria-label="Outbound recovery"' test/cairnloop/web/conversation_live_test.exs` returns at least `1` (Test 5 verification assertion).
    </source_assertions>
  </verify>
  <done>
    - ConversationLive renders the calm "Delivery did not complete. Try again from the Outbound recovery card." subhead ONLY when `outbound_status_label(msg) == "Failed"` — i.e., when `metadata["status"] == "failed"` on a `:system_outbound` message.
    - The existing `message-status-chip status-failed` chip is still rendered alongside the subhead (three non-color signals total: class, chip text, subhead).
    - The `outbound_recovery_card/1` component still renders `<section ... aria-label="Outbound recovery">` on `:resolved` conversations (a11y verification gate passes).
    - Phase 22/23/24 chip render tests still pass without modification.
    - `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/web/conversation_live_test.exs` exits 0; no new warnings.
    - Full headless suite `mix test` exits 0 (modulo documented Phase 25 BLOCKING `@tag :integration` gates and the baseline `Automation.DraftTest` pre-existing failure).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-rendered HTML → operator's browser | Plain HTML is the trust boundary. Untrusted strings (e.g., `inspect/1` output, `Map.get(...)` raw atoms, error tuples) escaping to operator copy would breach the calm-copy register and possibly leak internal state. |
| LiveView event handlers → application state | The new `×` close button reuses the existing `cancel_bulk_confirm` handler — no new write path or state transition is introduced. |
| Brand-token vocabulary → headless test assertions | The inline `var(--cl-<token>, <hex>)` fallback is the assertion target for headless tests. Hardcoded hex without the var() prefix would silently drift the brand vocabulary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-10 | Information disclosure (operator copy) | InboxLive empty-state + modal close button + ConversationLive failed subhead | mitigate | All copy strings are pre-authored calm prose ("No conversations yet.", "Delivery did not complete. Try again from the Outbound recovery card.", `×`); zero `inspect/1` calls in rendered HTML (T-25-06 — already enforced by the inbox test suite per Phase 25 D-14 grep). No new dynamic interpolation introduced. |
| T-26-11 | Spoofing / accessibility regression | Modal close button | mitigate | `aria-label="Close"` is mandatory; placement is inside `<.focus_wrap id="bulk-confirm-wrap">` so the focus trap traps tab order correctly; `phx-click="cancel_bulk_confirm"` reuses the existing handler (no new attack surface). |
| T-26-12 | Tampering | `outbound_recovery_card/1` (Phase 24 sealed) | mitigate | D-09 sub-bullet 1 is verification-only — Test 5 in Task 2 pins the existing `aria-label="Outbound recovery"` so future refactors cannot silently drift the a11y label. Source assertion grep confirms zero modification to lines 825-847. |
| T-26-13 | Tampering | `outbound_status_label/1` / `outbound_status_class/1` (Phase 24 sealed) | mitigate | Source assertion grep confirms the `"failed" -> "Failed"` body is byte-for-byte unchanged. The new subhead conditional reads through `outbound_status_label/1` but does not modify it. |
| T-26-14 | Tampering | Phase 25 `has_visible_eligible?/1` regression gate | mitigate | Test 7 in Task 1 explicitly pins the gate's behavior on non-resolved cohorts (asserts `cairnloop-inbox-bulk-header` does NOT render). The new empty-state branch is mutually-exclusive sibling logic. |
| T-26-15 | Information disclosure | Brand-token CSS extraction (D-10) | accept | Deferred this phase per D-10 + RESEARCH "Deferred Ideas". The inline `var(--cl-<token>, <hex>)` pattern is intentional — headless tests assert against the brand vocabulary in rendered HTML; extracting to a class breaks the test contract. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed — RESEARCH "Package Legitimacy Audit" not applicable. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 after each task with no new warnings.
- `mix test test/cairnloop/web/inbox_live_test.exs` exits 0 — all 8 new D-08 tests pass; existing Phase 25 plan 03 tests continue to pass.
- `mix test test/cairnloop/web/conversation_live_test.exs` exits 0 — all 7 new D-09 tests pass; existing Phase 22/23/24 chip tests (lines 629-659) continue to pass byte-for-byte unchanged.
- Full headless suite `mix test` exits 0 (modulo documented Phase 25 BLOCKING `@tag :integration` gates and the baseline `Automation.DraftTest` pre-existing failure).
- Brand book §7.5 compliance verified: failed-bubble has three non-color signals (`.status-failed` class + "Failed" chip text + reason-forward subhead); modal close button is a plain `×` glyph with calm muted-text color (NOT red); empty-state uses muted-text (no shouting); all copy strings have no emoji and no exclamation marks.
- Sealed-surface verification: `outbound_status_label/1`, `outbound_status_class/1`, `outbound_recovery_card/1`, `has_visible_eligible?/1`, `cancel_bulk_confirm` handler — all byte-for-byte untouched.
- Manual verification handoff: VALIDATION.md "Manual-Only Verifications" table lists 4 operator-side visual checks (empty-state appearance in dev server, modal close button tap target, failed-bubble subhead visual placement, refusal banner color/icon/text combination) — captured for the operator to run on a Postgres-available host once Plan 25-01 Task 4 + Plan 25-03 Task 3 BLOCKING gates clear.
</verification>

<success_criteria>
1. **InboxLive empty-state (D-08):** "No conversations yet." renders under `<h1>Inbox</h1>` when `@conversations == []`, with brand-token muted color and no bulk header.
2. **InboxLive modal `×` close button (D-08):** Top-right `×` button inside the dialog with `aria-label="Close"`, 44px tap target, `phx-click="cancel_bulk_confirm"`, anchored by `position: relative` on the dialog div, placed FIRST in the dialog children so `<.focus_wrap>` focus lands there on open.
3. **has_visible_eligible regression (D-08 sub-bullet 2):** non-resolved cohort does NOT render the bulk header (Phase 25 plan 03 gate preserved).
4. **Refusal banner copy review (D-08 sub-bullet 4):** existing copy passes brand book §7.5 (text + SVG + danger token — three non-color signals); verified by Test 6 in Task 1.
5. **ConversationLive failed-bubble subhead (D-09):** "Delivery did not complete. Try again from the Outbound recovery card." renders below `message-content` ONLY when `outbound_status_label(msg) == "Failed"`. Existing `.status-failed` chip stays untouched.
6. **outbound_recovery_card a11y (D-09 sub-bullet 1):** `<section ... aria-label="Outbound recovery">` verified — already passing, pinned by Test 5 in Task 2.
7. **Build/test gates (D-12):** `mix compile --warnings-as-errors` clean; `mix test` (headless) green modulo documented baseline.
8. **No sealed surface mutated:** `outbound_status_label/1`, `outbound_status_class/1`, `outbound_recovery_card/1`, `has_visible_eligible?/1`, `cancel_bulk_confirm` handler all byte-for-byte unchanged; D-10 (brand-token CSS extraction) explicitly deferred and NOT addressed in this plan.
</success_criteria>

<output>
Create `.planning/phases/26-observability-polish/26-03-SUMMARY.md` when done.
</output>
