---
phase: "18"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - CHANGELOG.md
  - mix.exs
autonomous: true
requirements:
  - REL-02
  - REL-04
  - REL-06
must_haves:
  truths:
    - Cairnloop is licensed under MIT
    - CHANGELOG.md exists in keep-a-changelog format covering all capabilities
    - mix.exs contains all required metadata for publishing to Hex
    - ExDoc is configured with semantic module groups and generates API docs
  artifacts:
    - path: LICENSE
      provides: MIT license text
      contains: MIT
    - path: CHANGELOG.md
      provides: Release history
      contains: "[0.1.0]"
    - path: mix.exs
      provides: Project metadata and dependencies
      contains: groups_for_modules
  key_links:
    - from: mix.exs
      to: CHANGELOG.md
      via: docs extras
      pattern: CHANGELOG.md
---

<objective>
Configure package metadata, license, and changelog for the initial v0.1.0 Hex.pm publish.

Purpose: To make Cairnloop publicly discoverable and document its capabilities.
Output: `LICENSE`, `CHANGELOG.md`, and updated `mix.exs` ready for publishing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-release-gate-hex-pm-publish/18-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LICENSE and CHANGELOG.md</name>
  <files>LICENSE, CHANGELOG.md</files>
  <action>
    Create a standard MIT `LICENSE` file at the repo root. Copyright holder is `szTheory` (per D-01).
    
    Create `CHANGELOG.md` at the repo root using the keep-a-changelog format (D-06). Include an `[Unreleased]` section at the top, and a `## [0.1.0] - YYYY-MM-DD` section (use today's date).
    
    Under the `[0.1.0]` "Added" section, include the following bullet set (D-07):
    - Host-owned hybrid retrieval corpus (pgvector + PG full-text) via `Cairnloop.Retrieval`
    - Operator search with trust, recency, and citation cues
    - Citation-backed grounded drafting with clarification and escalation states
    - Durable gap-event storage and ranked KB gaps dashboard
    - AI-prepared KB draft/revision suggestions with stale-revision gating and citation validation
    - Review-gated KB update workflow: approve, reject, defer, publish — with append-only task event history
    - In-thread quick-fix KB maintenance launched from live support conversations
    - Host-owned governed-action contract: compile-time `use Cairnloop.Tool` with risk tiers and deny-by-default `authorize/2`
    - Durable `ToolProposal` + `ToolActionEvent` records with Stripe-style idempotency
    - Approval state machine with Oban-backed resume, expiry, and deferral paths
    - Three-layer at-most-once execution: Oban unique + terminal guard + SHA-256 per-attempt run key
    - Bounded `[:cairnloop, :retrieval, …]` and `Cairnloop.Governance.Telemetry` event namespaces
    - Read-only MCP seam (`tools/list`, `initialize`) via optional `Cairnloop.Web.MCP.Router` Plug

    Do not include milestone internal names (like vM009) as sub-headings (D-05).
  </action>
  <verify>
    <automated>ls LICENSE CHANGELOG.md</automated>
  </verify>
  <done>LICENSE and CHANGELOG.md are committed with the exact requested content.</done>
</task>

<task type="auto">
  <name>Task 2: Update mix.exs Metadata and ExDoc Configuration</name>
  <files>mix.exs</files>
  <action>
    Update `mix.exs` `project/0` to add the missing publishing fields (D-08):
    - `:description` — e.g. "Host-owned customer support automation for Phoenix apps — governed drafting, retrieval-backed answers, and durable workflow tools." (keep under 300 chars).
    - `:source_url` — "https://github.com/szTheory/cairnloop"
    - `:homepage_url` — "https://github.com/szTheory/cairnloop"
    - `:package` — `[name: "cairnloop", licenses: ["MIT"], links: %{"GitHub" => "https://github.com/szTheory/cairnloop", "Changelog" => "https://hexdocs.pm/cairnloop/changelog.html"}, maintainers: ["szTheory"]]`
    
    Add `:docs` block (D-08, D-10, D-11):
    ```elixir
    docs: [
      main: "readme",
      extras: ["README.md", "CHANGELOG.md"],
      groups_for_modules: [
        "Governance": [~r/^Cairnloop\.Governance/, ~r/^Cairnloop\.Tool/],
        "Knowledge Base": [~r/^Cairnloop\.KnowledgeBase/, ~r/^Cairnloop\.KnowledgeAutomation/],
        "Retrieval": [~r/^Cairnloop\.Retrieval/],
        "MCP": [~r/^Cairnloop\.Web\.MCP/],
        "Web": [~r/^Cairnloop\.Web/],
        "Core": [~r/^Cairnloop/] # Fallback
      ]
    ]
    ```

    In the `deps/0` list, add `{:ex_doc, "~> 0.34", only: :dev, runtime: false}` (D-09).
  </action>
  <verify>
    <automated>mix deps.get && mix docs</automated>
  </verify>
  <done>mix.exs contains all Hex package metadata and ExDoc configuration, and `mix docs` generates successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer -> Hex.pm | Package metadata and source published externally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | mix.exs | accept | Metadata only exposes public repository URLs and descriptions; no sensitive logic |
</threat_model>

<verification>
Check that `mix deps.get` and `mix docs` run successfully without errors.
</verification>

<success_criteria>
`mix.exs` is ready for publishing, and `CHANGELOG.md` + `LICENSE` accurately document the v0.1.0 release.
</success_criteria>

<output>
After completion, create `.planning/phases/18-release-gate-hex-pm-publish/18-01-SUMMARY.md`
</output>
