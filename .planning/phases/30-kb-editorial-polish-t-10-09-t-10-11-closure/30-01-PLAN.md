---
phase: 30-kb-editorial-polish-t-10-09-t-10-11-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/knowledge_automation/editor_handoff.ex
  - lib/cairnloop/web/knowledge_base_live/editor_handoff.ex
  - lib/cairnloop/knowledge_automation/article_suggestion.ex
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_base.ex
  - test/cairnloop/web/knowledge_base_live/editor_handoff_test.exs
autonomous: true
requirements: [SEC-01, SEC-02, KB-02, KB-03]
must_haves:
  truths:
    - "A handoff token signed without a manual_edit_opened_at marker fails verify!/2 (raises Ecto.NoResultsError)"
    - "A handoff token signed WITH a non-empty manual_edit_opened_at ISO8601 string and matching attrs passes verify!/2"
    - "record_editor_handoff/2 writes manual_edit_opened_at to the suggestion and returns {:ok, suggestion}"
    - "KnowledgeBase.list_articles/1 returns articles ordered newest-first and accepts an opts keyword list"
    - "KnowledgeAutomation.get_gap_candidate/2 returns nil instead of raising when the gap candidate is missing"
  artifacts:
    - path: "lib/cairnloop/knowledge_automation/editor_handoff.ex"
      provides: "Domain decode/1 + marker-carrying normalize/1"
      contains: "def decode"
    - path: "lib/cairnloop/web/knowledge_base_live/editor_handoff.ex"
      provides: "sign/5 keyword opts + three-step verify!/2 pipeline + assert_handoff_marker/1"
      contains: "assert_handoff_marker"
    - path: "lib/cairnloop/knowledge_automation/article_suggestion.ex"
      provides: "manual_edit_changeset/2 narrow wrapper"
      contains: "def manual_edit_changeset"
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "record_editor_handoff/2 + get_gap_candidate/2"
      contains: "def record_editor_handoff"
    - path: "lib/cairnloop/knowledge_base.ex"
      provides: "list_articles/1 + maybe_filter_article_status/2"
      contains: "def list_articles"
    - path: "test/cairnloop/web/knowledge_base_live/editor_handoff_test.exs"
      provides: "Pure decode/1 + verify!/2 marker-gate tests"
      contains: "manual_edit_opened_at"
  key_links:
    - from: "lib/cairnloop/web/knowledge_base_live/editor_handoff.ex"
      to: "lib/cairnloop/knowledge_automation/editor_handoff.ex"
      via: "Token.decode/1 single Plug.Crypto.verify call"
      pattern: "Token\\.decode"
    - from: "lib/cairnloop/knowledge_automation.ex"
      to: "ArticleSuggestion.manual_edit_changeset"
      via: "record_editor_handoff write path"
      pattern: "manual_edit_changeset"
---

<objective>
Build the security-gate primitives and facade reads that every other Phase 30 plan consumes:
the double-layer editor-handoff gate (SEC-01 + SEC-02 token/DB halves) plus the two new facade
reads `KnowledgeBase.list_articles/1` (KB-02) and `KnowledgeAutomation.get_gap_candidate/2` (KB-03).

This plan touches NO LiveView render files — it is pure domain/web-wrapper/facade plumbing so the
Wave 2 LiveView plans (03, 04) can call finished, tested functions.

Purpose: Close T-10-09 (auditable DB handoff marker) and T-10-11 (no bare-URL preload of
proposed_markdown) via an additive token-shape change + DB write, and restore arch-invariant-#5
facade reads before the LiveView wiring lands.
Output: Extended `EditorHandoff` (domain + web), `record_editor_handoff/2` + `get_gap_candidate/2`
on `KnowledgeAutomation`, `list_articles/1` on `KnowledgeBase`, `manual_edit_changeset/2` on
`ArticleSuggestion`, and a new pure test file `editor_handoff_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-PATTERNS.md
@CLAUDE.md
@SECURITY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from in-tree source this session. Use directly. -->

Domain token module — lib/cairnloop/knowledge_automation/editor_handoff.ex (current):
  @salt "knowledge-base-editor-handoff"
  @max_age 1800
  def sign(attrs) when is_map(attrs)  # Plug.Crypto.sign(secret_key_base(), @salt, normalize(attrs), max_age: @max_age)
  def verify(token, attrs) when is_map(attrs)  # SEALED — leave untouched
  defp normalize(attrs)  # returns string-keyed map: "article_id","review_task_id","return_to","suggestion_id"
  defp normalize_integer/1, defp secret_key_base/0  # reuse as-is

Web wrapper — lib/cairnloop/web/knowledge_base_live/editor_handoff.ex (current):
  alias Cairnloop.KnowledgeAutomation.EditorHandoff, as: Token
  alias Cairnloop.KnowledgeBase.Article            # already imported
  def sign(suggestion_id, article_id, review_task_id, return_to)  # builds map, calls Token.sign/1
  def verify!(params, article_id)  # currently: Token.verify(...) -> :ok | raise Ecto.NoResultsError, queryable: Article
  def ensure_review_task_match!(task, suggestion)  # SEALED — leave untouched

ArticleSuggestion schema — lib/cairnloop/knowledge_automation/article_suggestion.ex:
  field(:manual_edit_opened_at, :utc_datetime_usec)   # line 41 — ALREADY EXISTS, no migration
  def changeset(article_suggestion, attrs)  # casts :manual_edit_opened_at (line 68) but heavy validate_required
  def dismiss_changeset(article_suggestion, dismissed_at)  # WRAPPER PRECEDENT to mirror (lines 89-94)
  def regenerate_changeset(article_suggestion)            # second wrapper precedent (96-102)
  @entrypoint_type_values [:gap_candidate, :article_revision, :conversation_quick_fix]

KnowledgeAutomation facade — lib/cairnloop/knowledge_automation.ex:
  def list_gap_candidates(opts \\ [])  # MODEL for opts-list reads (40-50): apply_scope |> maybe_filter_status |> order_by |> repo().all()
  def get_gap_candidate!(id, opts \\ [])  # BANG sibling to wrap (52-62): apply_scope |> where id |> preload(:memberships) |> repo().one!() |> enforce_scope! |> hydrate_memberships
  def get_article_suggestion!(id, opts \\ [])  # bang load (72-78): apply_scope |> where id |> repo().one!() |> enforce_scope!
  defp now_fn(opts)  # line 1663: Keyword.get(opts, :now_fn, &DateTime.utc_now/0) — reuse for test-pinnable timestamps
  defp apply_scope(query, opts)  # 1940
  defp enforce_scope!(record, opts, queryable)  # 2113

KnowledgeBase facade — lib/cairnloop/knowledge_base.ex:
  import Ecto.Query        # already at line 2
  alias Cairnloop.KnowledgeBase.{Article, Revision}   # already at line 3
  defp repo/0              # already at lines 5-7
  def create_article(attrs)  # already exists (65-69)
  Article schema: field(:status, Ecto.Enum, values: [:draft, :published, :archived], default: :draft)

Existing test fixture token-shape impact (lib/cairnloop/web/knowledge_base_live_test.exs):
  ~8 call sites use EditorHandoff.sign(id, article_id, review_task_id, return_to) WITHOUT a marker.
  Those tests are owned and updated by Plan 03 (do NOT edit that file in this plan).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the domain + web EditorHandoff to carry and assert the manual_edit_opened_at marker</name>
  <files>lib/cairnloop/knowledge_automation/editor_handoff.ex, lib/cairnloop/web/knowledge_base_live/editor_handoff.ex</files>
  <read_first>
    - lib/cairnloop/knowledge_automation/editor_handoff.ex (domain token module — current sign/verify/normalize)
    - lib/cairnloop/web/knowledge_base_live/editor_handoff.ex (web wrapper — current sign/4, verify!/2)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-PATTERNS.md (Pattern 2 + "decode/1" + verify!/2 rewrite excerpts)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md (D-02, D-03, D-04)
  </read_first>
  <behavior>
    - decode/1 of a token signed by the domain sign/1 returns {:ok, payload_map} with string keys.
    - decode/1 of a tampered/garbage token returns {:error, _}.
    - normalize/1 output includes the key "manual_edit_opened_at" (nil when the input map omits it).
    - web verify!/2 returns :ok when the handoff token carries a non-empty binary "manual_edit_opened_at" AND all other attrs match the params/article_id.
    - web verify!/2 raises Ecto.NoResultsError (queryable: Article) when the token lacks the marker (nil or "").
    - web verify!/2 raises Ecto.NoResultsError when attrs (suggestion_id/article_id/review_task_id/return_to) do not match the decoded payload.
    - verify!/2 calls Plug.Crypto.verify exactly once (via Token.decode/1) — no second decode.
  </behavior>
  <action>
    Domain `editor_handoff.ex` (D-02, D-04): add a public `decode/1` that calls
    `Plug.Crypto.verify(secret_key_base(), @salt, token, max_age: @max_age)` and returns its
    `{:ok, payload} | {:error, reason}` result directly. Extend `normalize/1` by adding ONE key
    `"manual_edit_opened_at" => Map.get(attrs, :manual_edit_opened_at) || Map.get(attrs, "manual_edit_opened_at")`
    using the same dual atom/string access idiom as the other keys (nil-default keeps the value
    serializable and backward-compatible). Do NOT modify `sign/1`, `verify/2`, `normalize_integer/*`,
    or `secret_key_base/0`.

    Web `editor_handoff.ex` (D-03, D-04): change `sign/4` to
    `sign(suggestion_id, article_id, review_task_id, return_to, opts \\ [])` and add
    `manual_edit_opened_at: Keyword.get(opts, :manual_edit_opened_at)` to the map passed to `Token.sign/1`.
    Rewrite `verify!/2` to a three-step `with` pipeline: (1) `{:ok, payload} <- Token.decode(Map.get(params, "handoff"))`,
    (2) `:ok <- assert_handoff_marker(payload)`, (3) `true <- payload == expected` where `expected` is
    built by a private `normalized_attrs(params, article_id)` that produces the SAME string-keyed shape
    the domain `normalize/1` produces (article_id, review_task_id, return_to, suggestion_id, AND
    manual_edit_opened_at). On any `else` branch, `raise Ecto.NoResultsError, queryable: Article`
    (preserve the existing fail-closed contract byte-for-byte). Add private
    `assert_handoff_marker(%{"manual_edit_opened_at" => v}) when is_binary(v) and v != "", do: :ok`
    and a catch-all `assert_handoff_marker(_), do: {:error, :missing_handoff_marker}`.
    Do NOT call `Token.verify/2` (forbidden double-decode per D-03). Leave `ensure_review_task_match!/2`
    untouched. Build `normalized_attrs/2` to reuse the integer-normalization behavior of the domain
    (parse suggestion_id/review_task_id from string params); article_id arrives as an integer already.
    Note the marker key in `expected` must be `Map.get(params, "manual_edit_opened_at")` (nil for the
    bare-URL case, which the mismatch step would also reject — but the marker assert fails first).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - lib/cairnloop/knowledge_automation/editor_handoff.ex contains `def decode(token)`
    - lib/cairnloop/knowledge_automation/editor_handoff.ex normalize/1 source contains `manual_edit_opened_at`
    - lib/cairnloop/web/knowledge_base_live/editor_handoff.ex contains `def sign(suggestion_id, article_id, review_task_id, return_to, opts \\ [])`
    - lib/cairnloop/web/knowledge_base_live/editor_handoff.ex contains `defp assert_handoff_marker`
    - `grep -c 'Token.verify' lib/cairnloop/web/knowledge_base_live/editor_handoff.ex` returns 0 (no double-decode)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>decode/1 exists on the domain module; web sign/5 + verify!/2 three-step pipeline + assert_handoff_marker/1 exist; no Token.verify call remains in the web wrapper; warnings-clean compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the pure editor_handoff_test.exs covering decode/1 and the verify!/2 marker gate</name>
  <files>test/cairnloop/web/knowledge_base_live/editor_handoff_test.exs</files>
  <read_first>
    - lib/cairnloop/knowledge_automation/editor_handoff.ex (post-Task-1 state)
    - lib/cairnloop/web/knowledge_base_live/editor_handoff.ex (post-Task-1 state)
    - test/cairnloop/web/knowledge_base_live_test.exs (top: how EditorHandoff.sign is invoked in tests, secret-key env pattern)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-RESEARCH.md (Pitfall 5 Mock-injection; Validation Architecture → Wave 0 Gaps)
  </read_first>
  <behavior>
    - Test: decode/1 round-trips a domain-signed token → {:ok, payload} with string keys including "manual_edit_opened_at".
    - Test: decode/1 of "not-a-real-token" returns {:error, _}.
    - Test: web verify!/2 returns :ok for a token signed with manual_edit_opened_at opt + matching params/article_id.
    - Test: web verify!/2 raises Ecto.NoResultsError for a token signed WITHOUT the marker opt (bare suggestion_id handoff).
    - Test: web verify!/2 raises Ecto.NoResultsError when suggestion_id in params does not match the signed token.
  </behavior>
  <action>
    Create `test/cairnloop/web/knowledge_base_live/editor_handoff_test.exs` as a pure ExUnit module
    (`use ExUnit.Case, async: false` — it mutates `Application.put_env`). In `setup`, pin a deterministic
    signing key via `Application.put_env(:cairnloop, Cairnloop.KnowledgeAutomation.EditorHandoff,
    secret_key_base: "phase-30-test-secret-key-base-deterministic")` with an `on_exit` that deletes it,
    so signing/verifying is reproducible without a live Repo. Alias the domain module as `Token` and the
    web module as `Web`. Cover the five behaviors above. For the matching-success case, sign with
    `Web.sign(15, 42, nil, nil, manual_edit_opened_at: DateTime.utc_now() |> DateTime.to_iso8601())`
    then call `Web.verify!(%{"suggestion_id" => "15", "handoff" => token}, 42)` and assert it returns
    `:ok`. For the no-marker case, sign with `Web.sign(15, 42, nil, nil)` and assert
    `assert_raise Ecto.NoResultsError, fn -> Web.verify!(%{"suggestion_id" => "15", "handoff" => token}, 42) end`.
    For the mismatch case, sign suggestion_id 15 with a marker but call verify! with
    `"suggestion_id" => "16"`. No Repo, no Mock module needed — these are pure transform tests.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live/editor_handoff_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - test/cairnloop/web/knowledge_base_live/editor_handoff_test.exs exists and contains `manual_edit_opened_at`
    - `mix test test/cairnloop/web/knowledge_base_live/editor_handoff_test.exs` exits 0
    - The file asserts both an `:ok` success path and at least two `assert_raise Ecto.NoResultsError` failure paths
  </acceptance_criteria>
  <done>The pure test file exists, pins a deterministic secret key, and all assertions pass green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add record_editor_handoff/2, get_gap_candidate/2, manual_edit_changeset/2, and list_articles/1 facade reads/writes</name>
  <files>lib/cairnloop/knowledge_automation/article_suggestion.ex, lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_base.ex</files>
  <read_first>
    - lib/cairnloop/knowledge_automation.ex (lines 40-78 list_gap_candidates/get_gap_candidate!/get_article_suggestion!; now_fn at 1663; apply_scope at 1940; enforce_scope! at 2113)
    - lib/cairnloop/knowledge_automation/article_suggestion.ex (dismiss_changeset 89-94 wrapper precedent; manual_edit_opened_at field at 41; changeset 48-87)
    - lib/cairnloop/knowledge_base.ex (full — import Ecto.Query, repo/0, create_article, Article alias)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md (D-05, D-07, D-09)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-PATTERNS.md (Pattern 3 + Pattern 4; "AVOID mark_review_task_material_edit as analog")
  </read_first>
  <behavior>
    - ArticleSuggestion.manual_edit_changeset/2 casts only :manual_edit_opened_at (no re-validation of unrelated required fields).
    - KnowledgeAutomation.record_editor_handoff(suggestion_id, opts) loads the suggestion (bang), updates manual_edit_opened_at via the narrow changeset, returns {:ok, suggestion}; the written timestamp uses now_fn(opts) so tests can pin it.
    - record_editor_handoff overwrites an existing manual_edit_opened_at (refresh-to-latest idempotency).
    - KnowledgeAutomation.get_gap_candidate(id, opts) returns the candidate when present and nil when get_gap_candidate!/2 raises Ecto.NoResultsError.
    - KnowledgeBase.list_articles(opts) returns Article rows ordered desc by inserted_at then id; a :status opt filters to that status; absent/:all/nil status returns all.
  </behavior>
  <action>
    `ArticleSuggestion` (D-05 changeset): add a public `manual_edit_changeset(article_suggestion, opened_at)`
    that builds an Ecto changeset casting ONLY `:manual_edit_opened_at` to `opened_at` — mirror the
    `dismiss_changeset/2` wrapper shape but cast the single field directly via
    `cast(article_suggestion, %{manual_edit_opened_at: opened_at}, [:manual_edit_opened_at])`
    (do NOT route through the heavy `changeset/2`, which re-runs validate_required over unrelated fields
    per RESEARCH Pitfall 2).

    `KnowledgeAutomation` (D-05 write): add `record_editor_handoff(suggestion_id, opts \\ [])` that
    computes `now = now_fn(opts).()`, loads `suggestion = get_article_suggestion!(suggestion_id, opts)`,
    then `suggestion |> ArticleSuggestion.manual_edit_changeset(now) |> repo().update()`. Returns the
    `{:ok, suggestion} | {:error, changeset}` tuple from `repo().update/1`. Overwrite-on-each-open is
    intentional (RESEARCH Open Q2 resolution). Ensure `ArticleSuggestion` is already aliased in the
    module (it is — used by get_article_suggestion!).

    `KnowledgeAutomation` (D-07 read): add `get_gap_candidate(id, opts \\ [])` that calls
    `get_gap_candidate!(id, opts)` and rescues `Ecto.NoResultsError -> nil`. This is the documented
    non-bang sibling.

    `KnowledgeBase` (D-09 read): add `list_articles(opts \\ [])` that pipes
    `Article |> maybe_filter_article_status(opts) |> order_by([a], desc: a.inserted_at, desc: a.id) |> repo().all()`.
    Add private `maybe_filter_article_status(query, opts)` that `case Keyword.get(opts, :status, :all)`
    returns the query unchanged for `:all` and `nil`, and `where(query, [a], a.status == ^status)`
    otherwise. Article has no tenant fields yet, so non-status opts are accepted and ignored (reserved
    for future tenant scope per D-09). `import Ecto.Query` is already present.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - lib/cairnloop/knowledge_automation/article_suggestion.ex contains `def manual_edit_changeset`
    - lib/cairnloop/knowledge_automation.ex contains `def record_editor_handoff` and `def get_gap_candidate(id`
    - lib/cairnloop/knowledge_base.ex contains `def list_articles` and `defp maybe_filter_article_status`
    - `grep -c 'mark_review_task_material_edit' lib/cairnloop/knowledge_automation.ex` shows record_editor_handoff did NOT reuse it as a write path (record_editor_handoff body references manual_edit_changeset, not mark_review_task_material_edit)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>All four facade additions compile warnings-clean; record_editor_handoff uses the narrow manual_edit_changeset; get_gap_candidate is the non-bang rescue sibling; list_articles mirrors list_gap_candidates with status-only filtering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser URL → Editor.mount/3 | Untrusted URL params (`suggestion_id`, `handoff`) cross into the LiveView; the editor must not preload reviewed `proposed_markdown` from a bare/forged URL. |
| SuggestionReview/ConversationLive → EditorHandoff.sign | The only legitimate token minters; they attest a deliberate "open for manual edit" by including the `manual_edit_opened_at` marker. |
| EditorHandoff token → Plug.Crypto.verify | Signature integrity boundary; HMAC + max-age enforced by the framework, never hand-rolled. |
| web layer → KnowledgeAutomation/KnowledgeBase facades | Arch invariant #5 — all reads/writes cross the narrow facade, not direct Repo queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Repudiation | `record_editor_handoff/2` (KnowledgeAutomation) + `manual_edit_changeset/2` (ArticleSuggestion) | mitigate | Durable DB write of `manual_edit_opened_at = now_fn(opts).()` on every editor open creates an auditable handoff record. Verified by record_editor_handoff returning {:ok, suggestion} with the timestamp set. |
| T-10-11 | Spoofing / Information disclosure | `EditorHandoff.verify!/2` (web wrapper) + `assert_handoff_marker/1` | mitigate | `verify!/2` requires a non-empty binary `manual_edit_opened_at` in the signed token before returning :ok; bare-URL `suggestion_id` (no marker token) raises `Ecto.NoResultsError`, so `proposed_markdown` is never preloaded. Verified by editor_handoff_test.exs no-marker assert_raise case. |
| T-30-01 | Tampering | `EditorHandoff` token | mitigate | `Plug.Crypto.verify/4` constant-time HMAC check; any tampered token returns `{:error, _}` from `Token.decode/1`, routed to the fail-closed `else` raise. |
| T-30-02 | Elevation / replay | `EditorHandoff` token | mitigate | `@max_age 1800` (30 min) enforced by `Plug.Crypto.verify`; expired tokens decode to `{:error, :expired}` and fail closed. Token-shape change invalidates pre-deploy tokens (acceptable — minted at click time, short-lived). |
| T-30-03 | Information disclosure | cross-tenant read via new facades | mitigate | `record_editor_handoff/2` and `get_gap_candidate/2` thread `opts` through the existing `apply_scope/2` + `enforce_scope!/3` helpers (via get_article_suggestion!/get_gap_candidate!); `list_articles/1` accepts scope opts (Article has no tenant fields yet — reserved). |
| T-10-10 | Tampering | authoring-target seam (`knowledge_automation.ex` domain) | accept (deferred) | Out of scope — domain-layer threat deferred to vM015 per STATE.md vM014 SECURITY split. |
| T-10-12 | Tampering | `suggest_article/2` gap-candidate prep (domain) | accept (deferred) | Out of scope — deferred to vM015 (different file: knowledge_automation.ex domain path). |
| T-10-13 | Spoofing | `suggest_revision/2` stale gate inputs (domain) | accept (deferred) | Out of scope — deferred to vM015. |
| T-30-SC | Tampering | npm/pip/cargo installs | n/a | No package installs this phase (RESEARCH Package Legitimacy Audit: zero new deps). No legitimacy checkpoint required. |

**High-severity threats in this plan:** T-10-09, T-10-11 — both mitigated and proven by `editor_handoff_test.exs` (Task 2) plus the consumer-side proofs in Plans 03/04. No high-severity threat is left undispositioned.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` is clean (CLAUDE.md mandatory gate).
- `mix test test/cairnloop/web/knowledge_base_live/editor_handoff_test.exs` is green (pure, no Repo).
- `grep -c 'Token.verify' lib/cairnloop/web/knowledge_base_live/editor_handoff.ex` returns 0 (no double-decode — D-03).
- Existing `knowledge_base_live_test.exs` is NOT run-green by THIS plan; it depends on the Plan 03 mount-rescue + token-fixture updates. Note this in the SUMMARY so the verifier expects Plan 03 to close those.
</verification>

<success_criteria>
- Domain `decode/1` + marker-carrying `normalize/1` exist; domain `sign/1` and `verify/2` unchanged.
- Web `sign/5` keyword opts + three-step `verify!/2` pipeline + `assert_handoff_marker/1` exist; no `Token.verify` call remains.
- `record_editor_handoff/2`, `get_gap_candidate/2`, `manual_edit_changeset/2`, `list_articles/1` exist and compile warnings-clean.
- `editor_handoff_test.exs` proves the marker gate (success + ≥2 fail-closed assert_raise cases).
</success_criteria>

<output>
Create `.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-01-SUMMARY.md` when done.
Record: token-shape change invalidates pre-deploy tokens (expected, not a regression); the
~8 existing `knowledge_base_live_test.exs` sign-without-marker fixtures + the 3 `assert_raise`
mount tests will fail until Plan 03 updates them (this is the documented hand-off).
</output>
