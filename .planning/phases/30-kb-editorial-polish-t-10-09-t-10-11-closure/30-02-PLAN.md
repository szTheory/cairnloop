---
phase: 30-kb-editorial-polish-t-10-09-t-10-11-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/web/knowledge_base_live/nav_component.ex
  - lib/cairnloop/web/review_task_presenter.ex
  - test/cairnloop/web/knowledge_base_live/nav_component_test.exs
  - test/cairnloop/web/review_task_presenter_test.exs
autonomous: true
requirements: [KB-01, KB-04]
must_haves:
  truths:
    - "kb_nav/1 renders three links — Knowledge base, Suggestions, Gaps — to their exact routes"
    - "kb_nav/1 marks the current route with aria-current=\"page\" AND a 2px primary border (never color alone)"
    - "action_label(:open_for_edit, task) returns 'Open for manual edit' by default, 'Create manual draft' for new-article/blocked-manual suggestions, and 'Review and draft manually' for failed suggestions"
    - "No action_label variant renders a raw Elixir atom or struct repr"
  artifacts:
    - path: "lib/cairnloop/web/knowledge_base_live/nav_component.ex"
      provides: "kb_nav/1 + kb_nav_link/1 Phoenix function component"
      contains: "def kb_nav"
      min_lines: 25
    - path: "lib/cairnloop/web/review_task_presenter.ex"
      provides: "3-variant action_label/2 calm copy"
      contains: "Open for manual edit"
    - path: "test/cairnloop/web/knowledge_base_live/nav_component_test.exs"
      provides: "Pure render assertions for links + active marker + aria-current"
      contains: "aria-current"
    - path: "test/cairnloop/web/review_task_presenter_test.exs"
      provides: "Pure 3-variant copy assertions"
      contains: "Review and draft manually"
  key_links:
    - from: "lib/cairnloop/web/review_task_presenter.ex"
      to: "ArticleSuggestionPresenter.quick_fix_outcome_label/1"
      via: "blocked-manual branch predicate"
      pattern: "quick_fix_outcome_label"
---

<objective>
Build the two presentation primitives the Wave 2 LiveView plans wire in: the shared editorial
nav function component `NavComponent.kb_nav/1` (KB-01) and the calm 3-variant
`ReviewTaskPresenter.action_label/2` copy (KB-04). This plan creates a NEW module and edits only
`review_task_presenter.ex` — it does NOT touch any KB LiveView render file, so it runs parallel
to Plan 01 in Wave 1 with zero file conflict. Plans 03 and 04 import and call these.

Purpose: One coherent editorial nav across all 4 KB routes and reason-forward handoff copy that
never leaks raw Elixir terms — both per the APPROVED 30-UI-SPEC.
Output: `nav_component.ex` (new), extended `review_task_presenter.ex`, and two new pure test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-UI-SPEC.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-PATTERNS.md
@CLAUDE.md
@prompts/cairnloop_brand_book.md

<interfaces>
<!-- Contracts extracted from in-tree source this session. -->

ReviewTaskPresenter — lib/cairnloop/web/review_task_presenter.ex (current, lines 194-208):
  def action_label(:approve), do: "Approve"
  def action_label(:reject), do: "Reject"
  def action_label(:defer), do: "Defer"
  def action_label(:publish), do: "Publish"
  def action_label(:open_for_edit), do: "Open for edit"   # <-- change default to "Open for manual edit"
  def action_label(action, %ReviewTask{article_suggestion: suggestion}) when not is_nil(suggestion) do
    case {action, ArticleSuggestionPresenter.quick_fix_outcome_label(suggestion)} do
      {:open_for_edit, "Manual draft required"} -> "Open manual draft"   # <-- change to "Create manual draft"
      _ -> action_label(action)
    end
  end
  def action_label(action, _task), do: action_label(action)   # <-- KEEP this fallback

ArticleSuggestion enums — lib/cairnloop/knowledge_automation/article_suggestion.ex:
  @status_values [:pending_generation, :ready, :failed, :dismissed]   # :failed is the failed variant
  @suggestion_type_values [:article, :revision]   # NO :new_article — UI-SPEC :new_article maps to :article

ArticleSuggestionPresenter — lib/cairnloop/web/article_suggestion_presenter.ex:
  def quick_fix_outcome_label(%ArticleSuggestion{} = suggestion)  # returns "Manual draft required" for blocked_manual_required (line 82,85), else nil/other labels

UI-SPEC §KB-04 copy contract:
  | Default :open_for_edit                                   -> "Open for manual edit"
  | suggestion_type == :new_article OR quick_fix_outcome == :blocked_manual_required -> "Create manual draft"
  | suggestion is :failed                                   -> "Review and draft manually"

Phoenix function component pattern (RESEARCH Pattern 1):
  use Phoenix.Component
  attr :current, :atom, required: true   # :index | :editor | :suggestions | :gaps
  def kb_nav(assigns)  # ~H nav with 3 links; :editor renders no active marker (no top-level entry)

UI-SPEC §1 nav visual contract (bare brand tokens — NO hex fallback, Phase 29 BRAND-04 gate):
  container: background var(--cl-surface); border-bottom 1px solid var(--cl-border); padding 0 24px; height 48px; flex; align-items center; gap 8px
  link: padding 12px 16px; font-size 13px; font-weight 600; color var(--cl-text-muted); text-decoration none; border-bottom 2px solid transparent
  active link: color var(--cl-text); border-bottom 2px solid var(--cl-primary); aria-current="page"
  wrapper: <nav aria-label="Knowledge base">
  labels/routes: "Knowledge base" -> /knowledge-base ; "Suggestions" -> /knowledge-base/suggestions ; "Gaps" -> /knowledge-base/gaps

Test idiom precedent: test/cairnloop/web/tool_proposal_presenter_test.exs (pure presenter test).
Render-to-string for the component test: Phoenix.LiveViewTest.render_component(&NavComponent.kb_nav/1, current: :index) returns the HTML string.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create NavComponent.kb_nav/1 shared editorial nav function component</name>
  <files>lib/cairnloop/web/knowledge_base_live/nav_component.ex</files>
  <read_first>
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-UI-SPEC.md (§1 Shared Editorial Nav Shell — exact spacing/typography/color/labels/routes/aria)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-PATTERNS.md (Pattern 1 + "No Analog Found" nav row)
    - lib/cairnloop/web/knowledge_base_live/gaps.ex (a KB LiveView for the surrounding module/import conventions)
    - test/cairnloop/web/brand_token_gate_test.exs (the negative-grep gate that forbids inline hex fallbacks)
  </read_first>
  <behavior>
    - kb_nav/1 renders exactly three <.link navigate=...> entries to /knowledge-base, /knowledge-base/suggestions, /knowledge-base/gaps with labels "Knowledge base", "Suggestions", "Gaps".
    - When current={:index} the "Knowledge base" link has aria-current="page" and the others do not.
    - When current={:suggestions} the "Suggestions" link is the active one.
    - When current={:editor} NO link is marked active (Editor has no top-level nav entry).
    - The wrapper element is <nav aria-label="Knowledge base">.
    - Active styling pairs the primary border-bottom with aria-current (never color alone).
    - No style attribute contains an inline hex fallback (e.g. var(--cl-primary, #...)).
  </behavior>
  <action>
    Create `lib/cairnloop/web/knowledge_base_live/nav_component.ex` as
    `defmodule Cairnloop.Web.KnowledgeBaseLive.NavComponent do use Phoenix.Component`. Declare
    `attr :current, :atom, required: true` and `def kb_nav(assigns)` rendering a `~H` `<nav
    aria-label="Knowledge base">` styled per UI-SPEC §1 (container: background var(--cl-surface);
    border-bottom 1px solid var(--cl-border); padding 0 24px; height 48px; display flex; align-items
    center; gap 8px). Render three `kb_nav_link` calls with `active={@current == :index}` /
    `:suggestions` / `:gaps`. Add a private `kb_nav_link/1` with
    `attr :to, :string, required: true`, `attr :label, :string, required: true`,
    `attr :active, :boolean, default: false`, rendering `<.link navigate={@to}
    aria-current={if @active, do: "page"}>` with the inactive/active styles from the UI-SPEC
    (inactive: color var(--cl-text-muted), border-bottom 2px solid transparent; active: color
    var(--cl-text), border-bottom 2px solid var(--cl-primary)); font-size 13px; font-weight 600;
    padding 12px 16px; text-decoration none. Wrap the border-color transition in
    `@media (prefers-reduced-motion: no-preference)` only if you add a transition — keep it minimal.
    ALL CSS custom property references are BARE `var(--cl-<token>)` with NO comma-hex fallback
    (Phase 29 BRAND-04 gate). `:editor` is not a link target — passing `current={:editor}` simply
    yields no active marker.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live/nav_component_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - lib/cairnloop/web/knowledge_base_live/nav_component.ex contains `def kb_nav(assigns)`
    - lib/cairnloop/web/knowledge_base_live/nav_component.ex contains `aria-label="Knowledge base"`
    - `grep -E 'var\(--cl-[a-z-]+, #' lib/cairnloop/web/knowledge_base_live/nav_component.ex` returns no matches (BRAND-04 gate)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>NavComponent module compiles warnings-clean; renders 3 routed links with aria-current + primary-border active marker; uses only bare brand tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the pure nav_component_test.exs covering links, active marker, and aria-current</name>
  <files>test/cairnloop/web/knowledge_base_live/nav_component_test.exs</files>
  <read_first>
    - lib/cairnloop/web/knowledge_base_live/nav_component.ex (post-Task-1 state)
    - test/cairnloop/web/tool_proposal_presenter_test.exs (pure-test idiom in this repo)
  </read_first>
  <behavior>
    - Test: render with current: :index contains all three labels and all three routes.
    - Test: render with current: :index includes aria-current="page" exactly once, on the Knowledge base link.
    - Test: render with current: :suggestions marks the Suggestions link active.
    - Test: render with current: :editor contains no aria-current="page".
    - Test: the active link's style includes var(--cl-primary) (border) — proving not-color-alone is paired with aria-current.
  </behavior>
  <action>
    Create `test/cairnloop/web/knowledge_base_live/nav_component_test.exs`
    (`use ExUnit.Case, async: true`, `import Phoenix.LiveViewTest`,
    `alias Cairnloop.Web.KnowledgeBaseLive.NavComponent`). Render via
    `render_component(&NavComponent.kb_nav/1, current: :index)` and assert the HTML string contains
    `/knowledge-base`, `/knowledge-base/suggestions`, `/knowledge-base/gaps`, the three labels, and
    that `aria-current="page"` appears (count it via a helper or assert it appears with the Knowledge
    base link nearby). Repeat for `:suggestions` (active = Suggestions) and `:editor` (no
    `aria-current="page"`). Assert the active variant's markup includes `var(--cl-primary)`. Pure —
    no Repo, no Mock.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live/nav_component_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - test/cairnloop/web/knowledge_base_live/nav_component_test.exs exists and contains `aria-current`
    - `mix test test/cairnloop/web/knowledge_base_live/nav_component_test.exs` exits 0
    - The test asserts the :editor case has no aria-current="page"
  </acceptance_criteria>
  <done>Pure nav render test passes green covering all four current values and the not-color-alone pairing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend action_label/2 to the 3 calm KB-04 copy variants + pure presenter test</name>
  <files>lib/cairnloop/web/review_task_presenter.ex, test/cairnloop/web/review_task_presenter_test.exs</files>
  <read_first>
    - lib/cairnloop/web/review_task_presenter.ex (lines 194-216 — current action_label/1 + action_label/2 + fallback)
    - lib/cairnloop/web/article_suggestion_presenter.ex (quick_fix_outcome_label/1 around line 78-88)
    - lib/cairnloop/knowledge_automation/article_suggestion.ex (lines 7-9 — @status_values, @suggestion_type_values)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-UI-SPEC.md (§4 button copy contract table)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md (D-13)
    - test/cairnloop/web/tool_proposal_presenter_test.exs (pure presenter test idiom)
  </read_first>
  <behavior>
    - action_label(:open_for_edit) (1-arity default) returns "Open for manual edit".
    - action_label(:open_for_edit, task) with a :failed suggestion returns "Review and draft manually".
    - action_label(:open_for_edit, task) with suggestion_type :article OR quick_fix_outcome_label == "Manual draft required" returns "Create manual draft".
    - action_label(:open_for_edit, task) for an ordinary :revision/:ready suggestion returns "Open for manual edit".
    - action_label(action, _task) fallback for other actions (e.g. :approve) still returns the 1-arity label.
    - No variant returns a raw atom or struct.
  </behavior>
  <action>
    In `review_task_presenter.ex` (D-13): change the 1-arity default `action_label(:open_for_edit)`
    from `"Open for edit"` to `"Open for manual edit"`. Rewrite the `action_label(action,
    %ReviewTask{article_suggestion: suggestion})` clause so that for `action == :open_for_edit` it
    evaluates a `cond`: (1) `suggestion.status == :failed -> "Review and draft manually"`;
    (2) `suggestion.suggestion_type == :article or
    ArticleSuggestionPresenter.quick_fix_outcome_label(suggestion) == "Manual draft required" ->
    "Create manual draft"` (UI-SPEC's `:new_article` maps to the in-tree `:article` type — there is
    no `:new_article` enum value); (3) `true -> "Open for manual edit"`. For any non-`:open_for_edit`
    action, delegate to `action_label(action)`. Keep all other `action_label/1` clauses and the final
    `action_label(action, _task), do: action_label(action)` fallback intact. Do NOT introduce
    `String.to_atom` on any value (RESEARCH Pitfall 6) — match the existing atoms only.

    Create `test/cairnloop/web/review_task_presenter_test.exs` (`use ExUnit.Case, async: true`,
    alias `Cairnloop.Web.ReviewTaskPresenter`, `Cairnloop.KnowledgeAutomation.{ReviewTask,
    ArticleSuggestion}`). Build `%ReviewTask{article_suggestion: %ArticleSuggestion{...}}` fixtures
    for each variant and assert the exact returned strings: default revision/ready -> "Open for
    manual edit"; status :failed -> "Review and draft manually"; suggestion_type :article ->
    "Create manual draft"; and assert no returned value matches `~r/^:/` (no raw atom leakage). Pure
    — no Repo.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/review_task_presenter_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - lib/cairnloop/web/review_task_presenter.ex contains the strings "Open for manual edit", "Create manual draft", and "Review and draft manually"
    - `grep -c 'Open for edit"' lib/cairnloop/web/review_task_presenter.ex` returns 0 (the old default copy is gone)
    - test/cairnloop/web/review_task_presenter_test.exs exists and contains "Review and draft manually"
    - `mix test test/cairnloop/web/review_task_presenter_test.exs` exits 0
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>action_label/2 returns the three UI-SPEC variants; the old "Open for edit" default is replaced; pure presenter test passes; no raw atoms leak.</done>
</task>

</tasks>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/cairnloop/web/knowledge_base_live/nav_component_test.exs test/cairnloop/web/review_task_presenter_test.exs` green.
- `grep -E 'var\(--cl-[a-z-]+, #' lib/cairnloop/web/knowledge_base_live/nav_component.ex` returns no matches (BRAND-04).
</verification>

<success_criteria>
- `NavComponent.kb_nav/1` exists, renders 3 routed links, marks the active route with aria-current + primary border, uses only bare brand tokens.
- `action_label(:open_for_edit, task)` returns the three calm KB-04 variants and never a raw atom.
- Both new pure test files pass green.
</success_criteria>

<output>
Create `.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-02-SUMMARY.md` when done.
Record: the nav component module name + function arity so Plans 03/04 import it correctly
(`import Cairnloop.Web.KnowledgeBaseLive.NavComponent`, call `<.kb_nav current={...} />`).
</output>
