---
phase: 30-kb-editorial-polish-t-10-09-t-10-11-closure
plan: 03
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - lib/cairnloop/web/knowledge_base_live/index.ex
  - lib/cairnloop/web/knowledge_base_live/editor.ex
  - test/cairnloop/web/knowledge_base_live_test.exs
autonomous: true
requirements: [KB-01, KB-02, KB-03, SEC-02]
must_haves:
  truths:
    - "KnowledgeBase.Index mounts via KnowledgeBase.list_articles/1 (no direct repo().all(Article))"
    - "Index renders the shared nav shell and a 'New article' button that creates a draft article and navigates to its editor"
    - "Editor opened from a gap-candidate suggestion renders a read-only 'Source gap' sidebar; non-gap suggestions render no sidebar"
    - "Editor.mount/3 rescues a gate failure (missing/invalid handoff marker) into a calm flash + redirect to /knowledge-base/suggestions — never a raw exception"
    - "Editor renders the shared nav shell"
  artifacts:
    - path: "lib/cairnloop/web/knowledge_base_live/index.ex"
      provides: "list_articles mount, new_article event, kb_nav, New article button"
      contains: "new_article"
    - path: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      provides: "gap_candidate assign, load_gap_candidate_from_suggestion/2, mount rescue, kb_nav, sidebar"
      contains: "load_gap_candidate_from_suggestion"
    - path: "test/cairnloop/web/knowledge_base_live_test.exs"
      provides: "Updated handoff fixtures + gate-rescue + new_article + gap-sidebar tests"
      contains: "manual_edit_opened_at"
  key_links:
    - from: "lib/cairnloop/web/knowledge_base_live/index.ex"
      to: "Cairnloop.KnowledgeBase.list_articles/1"
      via: "mount read through facade"
      pattern: "list_articles"
    - from: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      to: "knowledge_automation().get_gap_candidate/2"
      via: "load_gap_candidate_from_suggestion derivation"
      pattern: "get_gap_candidate"
    - from: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      to: "EditorHandoff.verify!/2"
      via: "load_suggestion gate (rescued in mount)"
      pattern: "verify!"
---

<objective>
Wire the Index and Editor LiveviewS to the Plan 01 facades/gate and the Plan 02 nav component:
Index switches to the `list_articles/1` facade read (arch invariant #5), gains the shared nav shell
and a "New article" button (KB-02); Editor gains the shared nav shell, the read-only "Source gap"
sidebar derived from the suggestion's gap-candidate relationship (KB-03), and a `mount/3` rescue
that turns the strengthened SEC-02 gate failure into a calm flash + redirect (D-06) instead of a
raw exception. This plan also UPDATES the existing `knowledge_base_live_test.exs` whose handoff
fixtures and `assert_raise` expectations are invalidated by the Plan 01 token-shape change and the
new mount rescue.

Purpose: Make the consumer side of SEC-02 fail-closed-and-calm, restore facade-only reads on Index,
and land KB-01/KB-02/KB-03 on the two highest-traffic KB surfaces.
Output: Rewired `index.ex` + `editor.ex` (additive, sealed render structure preserved) and a
green `knowledge_base_live_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-UI-SPEC.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-PATTERNS.md
@CLAUDE.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-01-SUMMARY.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-02-SUMMARY.md

<interfaces>
<!-- From Plan 01/02 outputs + in-tree source read this session. -->

Plan 01 facades (now available):
  Cairnloop.KnowledgeBase.list_articles(opts \\ [])         # ordered desc inserted_at,id; :status filter
  Cairnloop.KnowledgeBase.create_article(attrs)            # already existed; {:ok, article}|{:error, cs}
  Cairnloop.KnowledgeAutomation.get_gap_candidate(id, opts) # non-bang; nil on not-found
  Cairnloop.Web.KnowledgeBaseLive.EditorHandoff.verify!/2  # now requires manual_edit_opened_at marker, raises Ecto.NoResultsError otherwise

Plan 02 component:
  import Cairnloop.Web.KnowledgeBaseLive.NavComponent       # then <.kb_nav current={:index} /> or :editor

Index — lib/cairnloop/web/knowledge_base_live/index.ex (current):
  alias Cairnloop.KnowledgeAutomation
  alias Cairnloop.KnowledgeBase.Article
  defp repo/0
  def mount(_params, session, socket)  # currently: articles = repo().all(Article)
  def handle_event("suggest_revision", ...)  # EXISTING event-call-flash pattern to mirror for new_article
  defp knowledge_automation/0, defp scope_filters/1
  render/1: <h1>Knowledge Base</h1>, article <ul>, "Suggest revision" button per row

Editor — lib/cairnloop/web/knowledge_base_live/editor.ex (current):
  use Phoenix.LiveView
  alias Cairnloop.KnowledgeBase / .Article / Cairnloop.KnowledgeAutomation / EditorHandoff
  defp repo/0
  def mount(params, session, socket)  # repo().get!(Article,id); get_latest_revision; load_suggestion; ensure_editor_target_matches!; load_review_context; preload_content; assigns
  defp load_suggestion(%{"suggestion_id"=>...}=params, scope_filters, article_id)  # calls EditorHandoff.verify!(params, article_id) BEFORE get_article_suggestion!
  defp load_suggestion(_,_,_), do: nil
  defp knowledge_automation/0, defp scope_filters/1, defp normalize_id/1
  render/1: editor-layout flex; review-context aside when @review_origin?; textarea; preview-pane

ArticleSuggestion gap relationship (the KB-03 source of truth — D-07):
  field(:entrypoint_type, Ecto.Enum, values: [:gap_candidate, :article_revision, :conversation_quick_fix])
  field(:entrypoint_id, :integer)   # when entrypoint_type == :gap_candidate, entrypoint_id IS the gap_candidate_id

GapCandidate fields available to the sidebar — lib/cairnloop/knowledge_automation/gap_candidate.ex:
  :title, :seed_excerpt, :candidate_type, :evidence_count, :manual_case_count,
  :weak_grounding_count, :no_hit_count, has_many :memberships
GapCandidatePresenter — lib/cairnloop/web/gap_candidate_presenter.ex:
  def reason_label(%{candidate_type: ...}), def freshness_label/1, def why_raised/1  # reuse for calm display copy

UI-SPEC §3 sidebar contract:
  Condition: render only when @gap_candidate != nil ; <%= if @gap_candidate do %>
  aria-label="Source gap evidence" ; header "Source gap" (13px/600) ; gap title (15px/600)
  width 280px; background var(--cl-surface); border 1px solid var(--cl-border); border-radius var(--cl-radius-md); padding 16px
  evidence section heading "Retrieval evidence"; empty: "No retrieval evidence linked to this gap."
  read-only; no events/forms. Bare brand tokens only (BRAND-04).

UI-SPEC §2 New article button:
  phx-click="new_article"; label "New article"; background var(--cl-primary); color var(--cl-primary-text);
  border-radius var(--cl-radius-sm); padding 8px 16px; min-height 44px; phx-click-loading disabled state;
  error flash: "Unable to create the article right now. Try again."

UI-SPEC §5 / D-06 gate-failure flash (EXACT copy):
  "This editor can only be opened from the review queue. Return to Suggestions and use 'Open for manual edit' to begin editing."
  redirect to /knowledge-base/suggestions

EXISTING test impact — test/cairnloop/web/knowledge_base_live_test.exs (THIS plan owns these edits):
  Tokens signed WITHOUT a marker now FAIL the gate. Affected sign calls (need marker opt added or
  rewritten to assert the rescue):
   - line 174  EditorHandoff.sign(15, 42, nil, nil)                                 -> expects {:ok, socket} preloading content -> ADD marker opt
   - line 209  EditorHandoff.sign(15, 42, 27, "/knowledge-base/suggestions?task=27") -> expects {:ok, socket} -> ADD marker opt
   - line 259  EditorHandoff.sign(16, 42, nil, nil)  (wrong-article test)            -> currently assert_raise; now mount RESCUES -> rewrite to assert flash+redirect
   - line 286  EditorHandoff.sign(15, 42, 28, nil)   (wrong review_task test)        -> currently assert_raise; now mount RESCUES -> rewrite to assert flash+redirect
   - line 338  EditorHandoff.sign(15, 42, 27, nil)   (scope-forwarding test)         -> expects {:ok, socket} -> ADD marker opt
   - line 400  EditorHandoff.sign(17, 42, nil, nil)  (authoring_article_id test)     -> expects {:ok, socket} -> ADD marker opt
   - line 455  EditorHandoff.sign(15, 42, 27, nil)   (material-edit save test)       -> expects {:ok, socket} -> ADD marker opt
  line 232 "editor rejects bare suggestion ids without a signed handoff": currently assert_raise from mount;
   now mount RESCUES -> rewrite to assert returned socket has the gate flash + redirect.
  line 297 "editor rejects bare review_task ids that target a different article": currently assert_raise from mount;
   now mount RESCUES -> rewrite to assert flash+redirect.
  MockKnowledgeAutomation (lines 59-102) lacks get_gap_candidate/2 — add it for the gap-sidebar test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Index to list_articles/1, the shared nav shell, and the New article button</name>
  <files>lib/cairnloop/web/knowledge_base_live/index.ex</files>
  <read_first>
    - lib/cairnloop/web/knowledge_base_live/index.ex (full — mount, suggest_revision pattern, render, scope_filters)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-UI-SPEC.md (§2 New article button + Copywriting Contract index empty-state)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md (D-10, D-11, D-12)
    - lib/cairnloop/web/knowledge_base_live/nav_component.ex (Plan 02 output — import + call signature)
    - test/cairnloop/web/brand_token_gate_test.exs (BRAND-04 gate)
  </read_first>
  <behavior>
    - mount loads articles via Cairnloop.KnowledgeBase.list_articles(scope_filters(session)) — not repo().all(Article).
    - render includes the shared nav with current :index.
    - render includes a button with phx-click="new_article" labeled "New article".
    - handle_event("new_article", ...) on success creates a %{title: "Untitled article", status: :draft} article and push_navigates to /knowledge-base/<id>/edit.
    - handle_event("new_article", ...) on {:error, _} puts flash :error "Unable to create the article right now. Try again."
    - When the article list is empty, render shows the empty-state copy "No articles yet."
  </behavior>
  <action>
    `index.ex` (D-10/D-11/D-12): add `alias Cairnloop.KnowledgeBase` and
    `import Cairnloop.Web.KnowledgeBaseLive.NavComponent`. Change `mount/3` to set
    `articles: Cairnloop.KnowledgeBase.list_articles(scope_filters(session))` (remove the
    `repo().all(Article)` call — the local `repo/0` may become unused; delete it if so to stay
    warnings-clean, OR keep it only if still referenced). Add
    `handle_event("new_article", _params, socket)` mirroring the existing `suggest_revision`
    case-shape: `case KnowledgeBase.create_article(%{title: "Untitled article", status: :draft}) do
    {:ok, article} -> push_navigate(socket, to: "/knowledge-base/#{article.id}/edit");
    {:error, _changeset} -> put_flash(socket, :error, "Unable to create the article right now. Try again.")`.
    In `render/1`: add `<.kb_nav current={:index} />` at the top of the `~H` block; add the "New
    article" button (UI-SPEC §2: `phx-click="new_article"`, label `"New article"`, bare brand tokens
    `var(--cl-primary)` / `var(--cl-primary-text)`, border-radius `var(--cl-radius-sm)`, padding
    `8px 16px`, `min-height: 44px`, a `phx-click-loading` disabled treatment) in the page header
    area aligned with the title. Add the index empty-state copy "No articles yet." +
    "Create the first article to start building your knowledge base." when `@articles == []`. All
    new style attrs use BARE `var(--cl-<token>)` — no hex fallback.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - lib/cairnloop/web/knowledge_base_live/index.ex contains `list_articles` and `handle_event("new_article"`
    - `grep -c 'repo().all(Article)' lib/cairnloop/web/knowledge_base_live/index.ex` returns 0
    - lib/cairnloop/web/knowledge_base_live/index.ex contains `<.kb_nav current={:index}` and `New article`
    - `grep -E 'var\(--cl-[a-z-]+, #' lib/cairnloop/web/knowledge_base_live/index.ex` returns no matches
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>Index reads through list_articles/1, renders the shared nav + New article button, handles the new_article event with calm error copy, and is warnings-clean with no inline hex.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the Editor gap sidebar, mount rescue, and shared nav shell</name>
  <files>lib/cairnloop/web/knowledge_base_live/editor.ex</files>
  <read_first>
    - lib/cairnloop/web/knowledge_base_live/editor.ex (full — mount, load_suggestion, render, helpers)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-UI-SPEC.md (§3 sidebar, §5 gate failure, Copywriting Contract sidebar rows)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md (D-06, D-07, D-08, D-11)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-PATTERNS.md (Editor section — mount rescue + derivation excerpts + caution)
    - lib/cairnloop/knowledge_automation/gap_candidate.ex (sidebar fields)
    - lib/cairnloop/web/gap_candidate_presenter.ex (calm display helpers to reuse)
    - lib/cairnloop/web/knowledge_base_live/nav_component.ex (Plan 02 output)
  </read_first>
  <behavior>
    - mount derives gap_candidate from the loaded suggestion: when suggestion.entrypoint_type == :gap_candidate and entrypoint_id is an integer, gap_candidate = knowledge_automation().get_gap_candidate(id, scope_filters); otherwise nil.
    - mount assigns :gap_candidate.
    - render shows the "Source gap" sidebar (aria-label="Source gap evidence") only when @gap_candidate != nil; non-gap suggestions render no sidebar.
    - the sidebar renders at least one evidence detail beyond the heading: an evidence-count chip derived from @gap_candidate.evidence_count and a freshness chip from GapCandidatePresenter.freshness_label/1 (never a raw atom/struct).
    - mount rescues Ecto.NoResultsError (gate failure / missing article) and returns {:ok, socket} with put_flash(:error, <exact D-06 copy>) and push_navigate to /knowledge-base/suggestions — never re-raises.
    - render includes the shared nav with current :editor.
  </behavior>
  <action>
    `editor.ex` (D-06/D-07/D-08/D-11): add `import Cairnloop.Web.KnowledgeBaseLive.NavComponent`.
    Wrap the existing `mount/3` body in `try/rescue` (PATTERNS Editor caution: rescue the whole body
    — fail-closed, redirect target always valid). In the `try`, after
    `suggestion = load_suggestion(...)`, compute
    `gap_candidate = load_gap_candidate_from_suggestion(suggestion, scope_filters)` and add
    `|> assign(gap_candidate: gap_candidate)` to the existing assign chain (preserve every existing
    assign). In `rescue Ecto.NoResultsError ->` return
    `{:ok, socket |> put_flash(:error, "This editor can only be opened from the review queue. Return to Suggestions and use 'Open for manual edit' to begin editing.") |> push_navigate(to: "/knowledge-base/suggestions")}`.
    Add private `load_gap_candidate_from_suggestion(nil, _), do: nil` and
    `load_gap_candidate_from_suggestion(suggestion, scope_filters)` that `case
    {suggestion.entrypoint_type, suggestion.entrypoint_id} do {:gap_candidate, gid} when
    is_integer(gid) -> knowledge_automation().get_gap_candidate(gid, scope_filters); _ -> nil end`.
    UI-SPEC SUPERSESSION (read before implementing the sidebar): UI-SPEC §3's
    `@review_context.gap_candidate != nil` / "`gap_candidate_id` param present alongside the handoff
    token" / "`EditorHandoff.sign/4` gains a `gap_candidate_id` optional field" wording (UI-SPEC §3
    lines 184-185, 339-340) is SUPERSEDED by CONTEXT.md D-07: there is NO token/URL change for
    `gap_candidate_id`. Use the `@gap_candidate` assign derived in `mount/3` from
    `suggestion.entrypoint_id` (via `load_gap_candidate_from_suggestion/2`), and gate the sidebar on
    `@gap_candidate` (a top-level assign), NOT `@review_context.gap_candidate`.

    In `render/1`: add `<.kb_nav current={:editor} />` near the top; add the read-only sidebar as a
    third column inside the editor-layout, wrapped in `<%= if @gap_candidate do %>` with
    `aria-label="Source gap evidence"`, header "Source gap", the gap title
    (`@gap_candidate.title`), and the evidence content (UI-SPEC §3 evidence rows). Render an
    evidence-count chip from `@gap_candidate.evidence_count` — emit a humanized string such as
    `"#{@gap_candidate.evidence_count} evidence"` (count chip per UI-SPEC §3; never the raw integer
    alone without a noun, never a raw atom/struct — brand §5.5) — and a freshness chip from
    `GapCandidatePresenter.freshness_label(@gap_candidate)` (returns "Seen today" / "Seen N days ago"
    off the `:last_seen_at` field, which exists on GapCandidate). Use `GapCandidatePresenter`
    (`reason_label/1`, `why_raised/1`, `dominant_source_label/1`, `freshness_label/1`) for any other
    humanized labels — never raw atoms/struct repr. Include the evidence section headed "Retrieval
    evidence" and the empty fallback "No retrieval evidence linked to this gap." when
    `@gap_candidate.evidence_count == 0`. Styles per UI-SPEC §3, BARE brand tokens only. Do not alter
    the existing review-context aside, textarea, preview, save/publish behavior.
    NOTE: the existing `load_suggestion/3` already calls `EditorHandoff.verify!/2` — it now enforces
    the marker (Plan 01); no change to `load_suggestion/3` itself is needed for the gate.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - lib/cairnloop/web/knowledge_base_live/editor.ex contains `def load_gap_candidate_from_suggestion` or `defp load_gap_candidate_from_suggestion`
    - lib/cairnloop/web/knowledge_base_live/editor.ex contains the exact flash string "This editor can only be opened from the review queue. Return to Suggestions and use 'Open for manual edit' to begin editing."
    - lib/cairnloop/web/knowledge_base_live/editor.ex contains `rescue` and `push_navigate(to: "/knowledge-base/suggestions"`
    - lib/cairnloop/web/knowledge_base_live/editor.ex contains `aria-label="Source gap evidence"` and `<.kb_nav current={:editor}`
    - lib/cairnloop/web/knowledge_base_live/editor.ex sidebar markup references `@gap_candidate.evidence_count` (evidence chip) and `GapCandidatePresenter.freshness_label` (freshness chip) — the sidebar is NOT heading-only
    - lib/cairnloop/web/knowledge_base_live/editor.ex sidebar gates on `@gap_candidate` (NOT `@review_context.gap_candidate` — D-07 supersedes UI-SPEC §3)
    - `grep -E 'var\(--cl-[a-z-]+, #' lib/cairnloop/web/knowledge_base_live/editor.ex` returns no matches in the NEW sidebar/nav markup (note: pre-existing render code may use literal hex like #e5e7eb — do NOT introduce new var(--cl-*, #) fallbacks; leave sealed pre-existing literals alone)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>Editor derives + assigns + renders the read-only gap sidebar, rescues gate failures into the exact calm flash + redirect, and renders the shared nav; warnings-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update knowledge_base_live_test.exs for the marker gate, mount rescue, new_article, and gap sidebar</name>
  <files>test/cairnloop/web/knowledge_base_live_test.exs</files>
  <read_first>
    - test/cairnloop/web/knowledge_base_live_test.exs (full — MockRepo, MockKnowledgeAutomation, all 7 affected sign call sites + 2 assert_raise mount tests listed in interfaces)
    - lib/cairnloop/web/knowledge_base_live/editor.ex (post-Task-2 state — the rescue + sidebar)
    - lib/cairnloop/web/knowledge_base_live/index.ex (post-Task-1 state — new_article)
    - lib/cairnloop/web/knowledge_base_live/editor_handoff.ex (Plan 01 — sign/5 marker opt signature)
    - lib/cairnloop/knowledge_automation/article_suggestion.ex (entrypoint_type/entrypoint_id for the gap fixture)
  </read_first>
  <behavior>
    - All previously-passing editor mount tests that expected successful preload now sign with a manual_edit_opened_at marker and still pass.
    - The two tests that previously asserted assert_raise Ecto.NoResultsError from mount now assert mount returns {:ok, socket} whose flash :error equals the D-06 copy and whose redirect target is /knowledge-base/suggestions.
    - A new test: a suggestion with entrypoint_type :gap_candidate causes the Editor to render the "Source gap" sidebar showing the gap title AND at least one evidence detail (the "2 evidence" count chip and the freshness label string); a non-gap suggestion renders no sidebar (no "Source gap").
    - A new test: Index "new_article" event creates an article and push_navigates to its editor; the error branch flashes the calm copy.
    - The full file passes mix test.
  </behavior>
  <action>
    Update `test/cairnloop/web/knowledge_base_live_test.exs`. (1) For each successful-preload mount
    test (lines ~174, 209, 338, 400, 455 per interfaces), change the `EditorHandoff.sign(...)` call
    to pass `manual_edit_opened_at: DateTime.utc_now() |> DateTime.to_iso8601()` as the 5th opt so
    the strengthened gate passes; add the same marker to the corresponding mount `params` ONLY if
    the assertion compares decoded payload to params — it does not (verify! reads the marker from the
    token, not the URL), so adding the opt to `sign` is sufficient. (2) Rewrite the two
    previously-`assert_raise` mount tests (line ~232 bare-suggestion, line ~297 bare-review_task that
    targets a different article, and line ~259/286 wrong-article/wrong-task — confirm which now
    rescue vs. which still raise: ensure_editor_target_matches! and ensure_review_task_match! raise
    INSIDE mount, so under the whole-body rescue they ALSO become flash+redirect) to call
    `{:ok, socket} = Editor.mount(...)` and assert
    `socket.assigns.flash["error"] == "This editor can only be opened from the review queue. Return to Suggestions and use 'Open for manual edit' to begin editing."`
    (read flash via `Phoenix.LiveView.Utils` or the socket's `:flash` assign as the existing tests
    do) and that the redirect target is `/knowledge-base/suggestions` (assert on
    `socket.redirected` / the push_navigate tuple per the LiveView test idiom already used in this
    repo — match the existing redirect-assertion style if present, else assert the flash alone and
    note the redirect is covered by the exact-copy match). (3) Add `get_gap_candidate/2` to
    `MockKnowledgeAutomation` returning a fixture `%GapCandidate{id: 7, title: "Billing export gap",
    seed_excerpt: "...", candidate_type: :manual_handling, evidence_count: 2}` for a known id and
    `nil` otherwise; add a fixture suggestion (e.g. id 18) with
    `entrypoint_type: :gap_candidate, entrypoint_id: 7` and a non-gap suggestion. The
    `get_gap_candidate/2` Mock fixture for id 7 MUST also set `last_seen_at: DateTime.utc_now()` (so
    `GapCandidatePresenter.freshness_label/1` resolves) in addition to `evidence_count: 2`. Then add a
    test asserting the rendered Editor HTML for the gap case includes "Source gap", the gap title
    ("Billing export gap"), AND at least one evidence-item detail — assert BOTH `html =~ "2 evidence"`
    (the evidence-count chip from `evidence_count: 2`) and `html =~ "Seen today"` (the
    `freshness_label/1` string for a now `last_seen_at`); for the non-gap case assert the HTML
    EXCLUDES "Source gap". (4) Add an Index `new_article` test using `MockRepo`
    (its `insert/2` returns `{:ok, apply_changes}`) and/or a Mock KnowledgeBase: call
    `Index.handle_event("new_article", %{}, socket)` and assert a `push_navigate` to
    `/knowledge-base/<id>/edit`; for the error branch, drive `create_article` to `{:error, _}` and
    assert the flash copy "Unable to create the article right now. Try again." Keep the existing
    `setup`/`on_exit` env-injection structure; restore any new Process keys in `on_exit`.
    IMPORTANT: pin the EditorHandoff secret key in this file's setup the same way Plan 01's
    editor_handoff_test does (Application.put_env for the domain module) so signed test tokens verify
    deterministically — if the existing file already relies on the per-node random key, signing and
    verifying happen in the same process so it works; only add the env pin if a test flakes.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - test/cairnloop/web/knowledge_base_live_test.exs contains `manual_edit_opened_at`
    - test/cairnloop/web/knowledge_base_live_test.exs contains the exact gate flash copy "This editor can only be opened from the review queue."
    - test/cairnloop/web/knowledge_base_live_test.exs contains a `get_gap_candidate` clause in MockKnowledgeAutomation (fixture sets `evidence_count: 2` and `last_seen_at`) and a test asserting "Source gap", the gap title, AND an evidence detail (`html =~ "2 evidence"` and the freshness label) — not the heading alone
    - test/cairnloop/web/knowledge_base_live_test.exs contains a test invoking `handle_event("new_article"`
    - `mix test test/cairnloop/web/knowledge_base_live_test.exs` exits 0
  </acceptance_criteria>
  <done>The full knowledge_base_live_test.exs passes green: marker-bearing tokens preload, gate failures assert the calm flash+redirect (not assert_raise), the gap sidebar renders conditionally, and new_article is covered.</done>
</task>

</tasks>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/cairnloop/web/knowledge_base_live_test.exs` green (including the rewritten rescue tests + new sidebar/new_article tests).
- `mix test test/cairnloop/web/knowledge_base_live/editor_handoff_test.exs` still green (no regression to Plan 01's gate).
- `grep -c 'repo().all(Article)' lib/cairnloop/web/knowledge_base_live/index.ex` returns 0 (arch invariant #5 restored).
- No NEW `var(--cl-*, #hex)` in the added nav/sidebar/button markup (BRAND-04).
</verification>

<success_criteria>
- Index reads via `list_articles/1`, renders the shared nav + "New article" button, handles `new_article` with calm error copy.
- Editor renders the conditional read-only "Source gap" sidebar, rescues gate failures into the exact calm flash + redirect, renders the shared nav.
- `knowledge_base_live_test.exs` is fully green with updated marker fixtures and rescue assertions.
</success_criteria>

<output>
Create `.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-03-SUMMARY.md` when done.
Record which existing tests were converted from assert_raise to flash+redirect assertions, and the
gap-sidebar Mock fixture shape (for the Phase 31 golden-path test to reuse).
</output>
