---
phase: 30-kb-editorial-polish-t-10-09-t-10-11-closure
plan: 04
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex
  - lib/cairnloop/web/knowledge_base_live/gaps.ex
  - lib/cairnloop/web/conversation_live.ex
  - test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs
autonomous: true
requirements: [KB-01, SEC-01]
must_haves:
  truths:
    - "open_for_manual_edit calls record_editor_handoff/2 (DB write) before minting the handoff token"
    - "The handoff token minted by SuggestionReview carries a non-empty manual_edit_opened_at ISO8601 marker"
    - "The handoff token minted by ConversationLive open_manual_draft also carries the marker and records the DB handoff (so the strengthened gate does not regress the quick-fix editor entry)"
    - "SuggestionReview and Gaps render the shared editorial nav shell"
  artifacts:
    - path: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      provides: "record_editor_handoff call + marker-bearing sign/5 + kb_nav"
      contains: "record_editor_handoff"
    - path: "lib/cairnloop/web/knowledge_base_live/gaps.ex"
      provides: "kb_nav shell"
      contains: "kb_nav current={:gaps}"
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "marker-bearing sign + record_editor_handoff on open_manual_draft (gate regression fix)"
      contains: "manual_edit_opened_at"
    - path: "test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs"
      provides: "record_editor_handoff-called + marker-in-token assertions"
      contains: "record_editor_handoff"
  key_links:
    - from: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      to: "knowledge_automation().record_editor_handoff/2"
      via: "open_for_manual_edit DB write before sign"
      pattern: "record_editor_handoff"
    - from: "lib/cairnloop/web/conversation_live.ex"
      to: "EditorHandoff.sign marker opt"
      via: "open_manual_draft handoff mint"
      pattern: "manual_edit_opened_at"
---

<objective>
Wire the token-MINTING side of the SEC-01 double-layer gate and finish the nav shell on the two
remaining KB surfaces. `SuggestionReview.open_for_manual_edit` records the auditable DB handoff and
signs a marker-bearing token (D-01/D-04); `Gaps` gains the shared nav (D-11). Critically, this plan
also fixes a cross-file regression the strengthened gate introduces: `ConversationLive`'s
`open_manual_draft` is a SECOND legitimate editor entry point that signs a handoff token WITHOUT a
marker — after Plan 01 it would hit the gate-failure flash. This plan brings it into the gate by
recording the handoff and signing with the marker.

Purpose: Complete SEC-01 (auditable DB handoff at the minting sites) and KB-01 (nav on all 4 KB
routes) without regressing the conversation-quick-fix path into the editor.
Output: Rewired `suggestion_review.ex`, `gaps.ex`, `conversation_live.ex` (all additive, sealed
render structure preserved) and an updated `suggestion_review_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-UI-SPEC.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-PATTERNS.md
@CLAUDE.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-01-SUMMARY.md
@.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-02-SUMMARY.md

<interfaces>
<!-- From Plan 01/02 outputs + in-tree source read this session. -->

Plan 01 (now available):
  Cairnloop.KnowledgeAutomation.record_editor_handoff(suggestion_id, opts \\ [])  # {:ok, suggestion}|{:error, cs}; writes manual_edit_opened_at
  Cairnloop.Web.KnowledgeBaseLive.EditorHandoff.sign(suggestion_id, article_id, review_task_id, return_to, opts \\ [])
    # pass opts [manual_edit_opened_at: DateTime.utc_now() |> DateTime.to_iso8601()] to satisfy the gate

Plan 02:
  import Cairnloop.Web.KnowledgeBaseLive.NavComponent   # <.kb_nav current={:suggestions} /> | :gaps

SuggestionReview — lib/cairnloop/web/knowledge_base_live/suggestion_review.ex (current):
  def handle_event("open_for_manual_edit", %{"id" => task_id}, socket)  # lines 138-174
    {:ok, task, suggestion} = load_task_selection(task_id, socket)
    target_article_id = (revision -> suggestion.article_id) | (else -> create_or_reuse_authoring_article_for_suggestion(...))
    return_to = task.id |> task_patch(queue_filter) |> URI.encode_www_form()
    handoff_token = EditorHandoff.sign(suggestion.id, target_article_id, task.id, URI.decode_www_form(return_to))   # NO marker — fix
    push_navigate ... &handoff=...
  defp knowledge_automation/0 (410-412), defp scope_filters/1 (418-426), socket.assigns.scope_filters available
  Existing DB-write call shape used elsewhere in this file (mirror it):
    knowledge_automation().<fn>(suggestion.id, socket.assigns.scope_filters)   # e.g. regenerate (62-65), dismiss (76-79)
  alias ... EditorHandoff already present; render/1 starts ~line 176 (<div class="suggestion-review"> ... <header>)

Gaps — lib/cairnloop/web/knowledge_base_live/gaps.ex (current):
  render/1 ~H block opens at line 60 (<div class="knowledge-base-gaps"> ... <header><h1>KB gap candidates</h1>)
  defp knowledge_automation/0, defp scope_filters/1 present; read-only surface — nav only, no other changes

ConversationLive — lib/cairnloop/web/conversation_live.ex (current):
  def handle_event("open_manual_draft", _params, socket)  # lines 162-201
    suggestion_id = socket.assigns.quick_fix_card[:suggestion_id]   # may be nil -> early flash, leave as-is
    case knowledge_automation().create_or_reuse_authoring_article_for_suggestion(suggestion_id, quick_fix_scope_opts(conversation)) do
      {:ok, article_id} -> ... handoff_token = EditorHandoff.sign(suggestion_id, article_id, review_task_id, return_path)   # NO marker — fix
    end
  defp quick_fix_scope_opts(conversation)  # line 1743 — scope opts for facade calls
  alias ... EditorHandoff already present (it calls EditorHandoff.sign at line 181)

EditorHandoff.sign marker opt (Plan 01 contract):
  EditorHandoff.sign(sid, aid, rtid, return_to, manual_edit_opened_at: DateTime.utc_now() |> DateTime.to_iso8601())
  # marker MUST be an ISO8601 STRING (RESEARCH Pitfall 3 — :utc_datetime_usec is not term-safe for signing)

UI-SPEC §1 nav (same as Plan 02): <.kb_nav current={:suggestions} /> on SuggestionReview, current={:gaps} on Gaps.

Test — test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs (existing; extend):
  Uses the repo Mock + knowledge_automation Mock injection idiom. Add a MockKnowledgeAutomation
  clause for record_editor_handoff/2 that records the call (send self() a message) and returns
  {:ok, suggestion}; assert open_for_manual_edit triggers it and that the navigated URL's handoff
  token decodes to a payload containing a non-empty manual_edit_opened_at.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SuggestionReview — record DB handoff + sign marker-bearing token + shared nav</name>
  <files>lib/cairnloop/web/knowledge_base_live/suggestion_review.ex</files>
  <read_first>
    - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex (full — open_for_manual_edit 138-174, existing facade-call shapes, scope_filters, render header)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md (D-01, D-04, D-11)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-PATTERNS.md (SuggestionReview section — DB write before sign + marker opt excerpts)
    - lib/cairnloop/web/knowledge_base_live/nav_component.ex (Plan 02 output)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-01-SUMMARY.md (record_editor_handoff/2 signature + return shape)
  </read_first>
  <behavior>
    - open_for_manual_edit calls knowledge_automation().record_editor_handoff(suggestion.id, socket.assigns.scope_filters) BEFORE signing the token, and matches on {:ok, _suggestion}.
    - The minted handoff token carries manual_edit_opened_at: DateTime.utc_now() |> DateTime.to_iso8601() so it passes the strengthened Editor gate.
    - The push_navigate URL still includes suggestion_id, review_task_id, return_to, and handoff params exactly as before.
    - render shows the shared nav with current :suggestions.
  </behavior>
  <action>
    `suggestion_review.ex` (D-01/D-04/D-11): add
    `import Cairnloop.Web.KnowledgeBaseLive.NavComponent`. In
    `handle_event("open_for_manual_edit", ...)`, after `target_article_id` is resolved and before the
    `EditorHandoff.sign` call, insert the auditable DB write
    `{:ok, _suggestion} = knowledge_automation().record_editor_handoff(suggestion.id, socket.assigns.scope_filters)`
    (mirror the existing `knowledge_automation().<fn>(suggestion.id, socket.assigns.scope_filters)`
    call shape used by regenerate/dismiss in this file). Change the `EditorHandoff.sign(...)` call to
    pass the 5th opt `manual_edit_opened_at: DateTime.utc_now() |> DateTime.to_iso8601()`. Leave the
    `push_navigate` URL construction unchanged. In `render/1`, add `<.kb_nav current={:suggestions} />`
    at the top of the `~H` block (above the existing `<header>`). Do not change the existing filter
    list, selected-task rendering, or other event handlers. Any new markup uses bare brand tokens.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex contains `record_editor_handoff(suggestion.id, socket.assigns.scope_filters)`
    - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex contains `manual_edit_opened_at: DateTime.utc_now() |> DateTime.to_iso8601()`
    - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex contains `<.kb_nav current={:suggestions}`
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>open_for_manual_edit records the DB handoff then mints a marker-bearing token; SuggestionReview renders the shared nav; warnings-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bring ConversationLive open_manual_draft into the gate + add nav to Gaps</name>
  <files>lib/cairnloop/web/conversation_live.ex, lib/cairnloop/web/knowledge_base_live/gaps.ex</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex (lines 162-201 open_manual_draft; quick_fix_scope_opts at 1743; EditorHandoff alias)
    - lib/cairnloop/web/knowledge_base_live/gaps.ex (full — mount, render header at line 60, helpers)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-CONTEXT.md (D-01 double-layer intent, D-11 nav)
    - .planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-01-SUMMARY.md (record_editor_handoff/2 signature)
    - lib/cairnloop/web/knowledge_base_live/nav_component.ex (Plan 02 output)
  </read_first>
  <behavior>
    - ConversationLive open_manual_draft, on the {:ok, article_id} branch, records the DB handoff for suggestion_id (via record_editor_handoff with the conversation's scope opts) and signs the token with a manual_edit_opened_at marker — so opening the editor from a conversation quick fix does NOT hit the strengthened gate.
    - The existing nil-suggestion early flash and the {:error, _} branch copy are unchanged.
    - Gaps renders the shared nav with current :gaps; no other Gaps behavior changes.
  </behavior>
  <action>
    `conversation_live.ex` (gate regression fix, D-01): in `handle_event("open_manual_draft", ...)`
    on the `{:ok, article_id} ->` branch, before building `handoff_token`, add
    `{:ok, _suggestion} = knowledge_automation().record_editor_handoff(suggestion_id, quick_fix_scope_opts(socket.assigns.conversation))`
    (reuse the existing `quick_fix_scope_opts/1` already used for the create-or-reuse call). Change
    the `EditorHandoff.sign(suggestion_id, article_id, review_task_id, return_path)` call to pass the
    5th opt `manual_edit_opened_at: DateTime.utc_now() |> DateTime.to_iso8601()`. Do not change the
    `nil ->` early-return flash or the `{:error, _reason} ->` "Manual draft could not be opened right
    now." branch. (If `record_editor_handoff` could return `{:error, _}` here, wrap it so a write
    failure surfaces the existing "Manual draft could not be opened right now." flash rather than
    crashing — prefer a `case` that falls through to the error flash, matching the calm fail-closed
    posture.)

    `gaps.ex` (D-11): add `import Cairnloop.Web.KnowledgeBaseLive.NavComponent` and put
    `<.kb_nav current={:gaps} />` at the very top of the `render/1` `~H` block (above the existing
    `<header>`). No event/state/mount changes — Gaps is a read-only surface for Phase 30.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - lib/cairnloop/web/conversation_live.ex contains `manual_edit_opened_at: DateTime.utc_now() |> DateTime.to_iso8601()` inside the open_manual_draft path
    - lib/cairnloop/web/conversation_live.ex contains `record_editor_handoff` in the open_manual_draft path
    - lib/cairnloop/web/knowledge_base_live/gaps.ex contains `<.kb_nav current={:gaps}`
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>ConversationLive open_manual_draft records the handoff + signs a marker-bearing token (no gate regression); Gaps renders the shared nav; warnings-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend suggestion_review_test.exs to prove the DB handoff write + marker-bearing token</name>
  <files>test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</files>
  <read_first>
    - test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs (full — existing Mock injection, open_for_manual_edit coverage, navigated-URL assertions)
    - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex (post-Task-1 state)
    - lib/cairnloop/web/knowledge_base_live/editor_handoff.ex (Plan 01 — Token.decode to inspect the minted token)
    - lib/cairnloop/knowledge_automation/editor_handoff.ex (Plan 01 — decode/1 to read the payload in the test)
  </read_first>
  <behavior>
    - Test: triggering open_for_manual_edit calls record_editor_handoff/2 with the suggestion id (assert via a message the Mock sends).
    - Test: the handoff token in the navigated URL decodes to a payload whose "manual_edit_opened_at" is a non-empty binary.
    - The full file passes mix test.
  </behavior>
  <action>
    Update `test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs`. Add a
    `record_editor_handoff(suggestion_id, opts \\ [])` clause to the file's MockKnowledgeAutomation
    (or the relevant mock module) that does `send(self(), {:record_editor_handoff, suggestion_id,
    opts})` and returns `{:ok, %ArticleSuggestion{id: suggestion_id, manual_edit_opened_at:
    DateTime.utc_now()}}` (or the existing suggestion fixture with the field set). In the
    open_for_manual_edit test, after invoking the event, `assert_received {:record_editor_handoff,
    sid, _opts}` for the expected suggestion id. Then extract the `handoff=` param from the
    push_navigate target URL (URI.decode_www_form the value), call
    `Cairnloop.KnowledgeAutomation.EditorHandoff.decode(token)` and assert
    `{:ok, payload}` with `is_binary(payload["manual_edit_opened_at"]) and payload["manual_edit_opened_at"] != ""`.
    If the existing test already asserts the navigated URL, extend that assertion block rather than
    duplicating setup. Pin the EditorHandoff secret key in setup (Application.put_env for the domain
    module) if needed for deterministic decode within the test process. Keep existing setup/on_exit
    structure and restore any new env/Process keys.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs contains `record_editor_handoff` and `manual_edit_opened_at`
    - the file asserts the minted handoff token decodes to a payload with a non-empty manual_edit_opened_at
    - `mix test test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs` exits 0
  </acceptance_criteria>
  <done>suggestion_review_test proves the DB handoff is recorded and the minted token carries a non-empty marker; file is green.</done>
</task>

</tasks>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs` green.
- `mix test test/cairnloop/web/conversation_live_test.exs` still green (no regression to the quick-fix open_manual_draft path — the marker fix keeps the editor reachable).
- Phase gate (run by the merge/verify step): full `mix test` green modulo the one known pre-existing `Automation.DraftTest` baseline failure (M005 drift — not a regression).
</verification>

<success_criteria>
- SuggestionReview records the DB handoff then mints a marker-bearing token; renders the shared nav.
- ConversationLive open_manual_draft no longer regresses into the gate (records handoff + marker sign).
- Gaps renders the shared nav.
- suggestion_review_test proves the write + marker; file green.
</success_criteria>

<output>
Create `.planning/phases/30-kb-editorial-polish-t-10-09-t-10-11-closure/30-04-SUMMARY.md` when done.
Record the cross-file fix: ConversationLive open_manual_draft was brought into the SEC-01 gate
(record + marker) — note this so Phase 31's golden path and any future quick-fix work knows the
conversation→editor path now requires the handoff marker too.
</output>
