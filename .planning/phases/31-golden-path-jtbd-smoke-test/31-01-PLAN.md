---
phase: 31-golden-path-jtbd-smoke-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/golden_path_test.exs
autonomous: true
requirements: [E2E-01, E2E-03]
must_haves:
  truths:
    - "Running `mix test.integration test/integration/golden_path_test.exs` exits 0 (the single sequential JTBD round-trip test passes against real Postgres + pgvector)"
    - "The test drives all 9 stages in one accumulating sequential test: seed → inbox sees → ConversationLive cmd+k search + citation chip → approve AI draft → tool proposal approve → ToolExecutionWorker :success → resolve → outbound trigger → bulk recovery"
    - "The test uses no new dependency and adds no file under test/support/"
    - "`mix compile --warnings-as-errors` succeeds with the new test file present (no new compiler warnings)"
  artifacts:
    - path: "test/integration/golden_path_test.exs"
      provides: "E2E-01 full JTBD round-trip integration test"
      contains: "test \"full JTBD round trip\""
      min_lines: 120
  key_links:
    - from: "test/integration/golden_path_test.exs"
      to: "Cairnloop.Web.ConversationLive"
      via: "live(conn, \"/governance/#{conversation.id}\")"
      pattern: "/governance/"
    - from: "test/integration/golden_path_test.exs"
      to: "Cairnloop.Web.SearchModalComponent"
      via: "send_update(... retrieval_module: StubRetrieval)"
      pattern: "send_update\\(Cairnloop.Web.SearchModalComponent"
    - from: "test/integration/golden_path_test.exs"
      to: "Cairnloop.Outbound.BulkEnvelope"
      via: "Repo.aggregate(BulkEnvelope, :count, :id)"
      pattern: "BulkEnvelope"
---

<objective>
Create `test/integration/golden_path_test.exs` — a single sequential integration test that drives the full Jobs-To-Be-Done round trip against real Postgres + pgvector, closing E2E-01 (and contributing to E2E-03 by living in the `mix test.integration` lane).

Purpose: Lock the substrate of Phases 27-30 into CI so adopters who run the suite get a green light on the exact path the two-tab demo walks. No browser driver, no new test dependency.

Output: One new test file with one `test "full JTBD round trip"` block structured as 9 inline `# Stage N: ...` stages that accumulate DB state, plus inline `defmodule` stubs (`StubRetrieval`, `StubContextProvider`, `InlineTestTool`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-golden-path-jtbd-smoke-test/31-CONTEXT.md
@.planning/phases/31-golden-path-jtbd-smoke-test/31-RESEARCH.md
@CLAUDE.md

# Canonical structural templates — read these before writing; mirror their patterns exactly.
@test/integration/tool_execution_outcome_live_test.exs
@test/integration/bulk_recovery_live_test.exs
@test/integration/approval_flow_test.exs

<interfaces>
<!-- Source-verified contracts the executor needs. No codebase exploration required. -->

Test infrastructure (from test/support/conn_case.ex):
- `use Cairnloop.ConnCase, async: false` — sets `@moduletag :integration`, `@endpoint Cairnloop.Web.Endpoint`, imports `Phoenix.LiveViewTest`, `Phoenix.ConnTest`, `Plug.Conn`, and `errors_on/1` from DataCase.
- ConnCase setup returns `{:ok, conn: Phoenix.ConnTest.build_conn()}` — a BARE conn. Auth via `Plug.Test.init_test_session(conn, %{"host_user_id" => "..."})`.
- ConnCase does NOT import `Ecto.Query` — add `import Ecto.Query` in the test module (needed for `where`/`order_by`/`limit` on BulkEnvelope/Message).

Test router (from test/support/router.ex):
- ConversationLive is mounted at `live("/governance/:id", ...)` — test URL is `/governance/#{conversation.id}`, NOT `/#{conversation.id}`.
- InboxLive is mounted at `live("/inbox", ...)`.

Fixtures (from test/support/fixtures.ex — import Cairnloop.Fixtures):
- `conversation_fixture(%{status: :open, subject: "...", host_user_id: "..."})` → `%Cairnloop.Conversation{}` (defaults: status: :open, subject: "Integration conversation", host_user_id: "test_operator").
- `message_fixture(%{conversation_id: id, content: "...", role: :user})` → `%Cairnloop.Message{}`.
- `proposal_fixture(%{tool_ref: "...", approval_mode: :requires_approval, scope_snapshot: %{scopes: []}, input_snapshot: %{...}, policy_snapshot: %{outcome: :proposed}})` → `%Cairnloop.Governance.ToolProposal{}`.
- `approval_fixture(proposal, %{status: :pending})` → `%Cairnloop.Governance.ToolApproval{}`.
- There is NO draft_fixture, NO user_fixture, NO operator_fixture.

Chat facade (lib/cairnloop/chat.ex):
- `Chat.get_conversation!(id)` → `%Conversation{}`.
- `Chat.resolve_conversation(conversation_id, resolved_by: "golden_operator")` → `{:ok, _}`. THIS is how stage 7 resolves — there is NO `"resolve"` LiveView event.

Automation facade (lib/cairnloop/automation.ex) — stage 4 draft seed path:
- `Cairnloop.Automation.create_draft(conversation_id, %{customer_reply: "...", proposal_type: :reply, status: :pending})` → `{:ok, %Draft{}}`. Draft.changeset validates_required `[:status, :conversation_id, :proposal_type]` and requires a non-blank `content` OR `customer_reply`. `proposal_type` enum: `:reply | :clarification | :escalation`; `status` enum: `:pending | :approved | :edited | :discarded`.
- `Cairnloop.Automation.approve_draft(draft_id)` → `{:ok, _}` (inserts an `:agent` Message, flips draft to `:approved`).

Governance facade (lib/cairnloop/governance.ex) — stage 5 approval path (mirror approval_flow_test.exs lines 50-77):
- `Governance.request_approval(proposal, enqueue_fn: capture)` → `{:ok, approval}`; sends `{:enqueued, expiry_job}` to test_pid via capture.
- `Governance.approve(approval.id, "golden_operator", enqueue_fn: capture)` → `{:ok, approved}`; sends `{:enqueued, resume_job}`. `approve/3` enqueues via injectable `enqueue_fn` — REQUIRED in tests (Pitfall 8).
- `enqueue_fn` capture pattern: `capture = fn job -> send(test_pid, {:enqueued, job}); {:ok, job} end`.

Workers (direct perform/1 — D-09, never Oban.drain_queue):
- `ApprovalResumeWorker.perform(%Oban.Job{args: %{"approval_id" => approval.id}})` → `:ok` (transitions :approved → :execution_pending).
- `ToolExecutionWorker.perform(%Oban.Job{attempt: 1, max_attempts: 3, args: %{"approval_id" => approval.id}})` → `:ok` (transitions to :executed). BOTH `attempt` AND `max_attempts` fields REQUIRED (Pitfall 5).

SearchModalComponent (lib/cairnloop/web/search_modal_component.ex):
- Component id in ConversationLive is `"search-modal"`.
- `:retrieval_module` is an overridable assign (default `Cairnloop.Retrieval`). Inject via `send_update(Cairnloop.Web.SearchModalComponent, id: "search-modal", retrieval_module: StubRetrieval)`.
- Events: `"toggle_search"` (opens palette, params `%{"key" => key}`), `"search"` (params `%{"query" => query}`, requires `byte_size(query) >= 2`), `"activate_result"` (params `%{"dom_id" => dom_id}`), `"open_active_result"`, `"open_result"` (params `%{"dom_id" => dom_id}`). No `"cite"`/`"open_result"` event is pushed to the PARENT — the modal navigates internally via push_navigate.
- `run_search/3` non-default clause expects the stub's `search/2` to return a PLAIN LIST of `%Cairnloop.Retrieval.Result{}` structs (no `{:ok, ...}` wrapper).

Cairnloop.Retrieval.Result struct fields (lib/cairnloop/retrieval/result.ex) — set at minimum: `id`, `title`, `content`, `source_type` (`:knowledge_base | :resolved_case`), `trust_level` (`:canonical | :assistive`), `article_id`, `score`. Returning plain maps crashes `build_sections/1` (Pitfall 4).

ConversationLive events (lib/cairnloop/web/conversation_live.ex):
- `"approve_draft"` params `%{"draft-id" => id}`; draft chips render from `@conversation.drafts` assoc when loaded + non-empty (render line ~824).
- `"trigger_recovery_follow_up"` (no params) — fires `Outbound.trigger/2`. Button only renders when `@conversation.status == :resolved` (render guard line 856) AND `Application.get_env(:cairnloop, :outbound_recovery_template_id)` is set (line 1773).
- `"approve_action"` params `%{"approval-id" => id}` (the LiveView approval-footer event; the test uses the Governance facade path directly per the approval_flow_test template).

InboxLive (lib/cairnloop/web/inbox_live.ex):
- Bulk events: `toggle_select` (input, `phx-value-id`), `open_bulk_confirm` (button), `confirm_bulk_send` (button). NOTE: the real event is `confirm_bulk_send`, NOT `confirm_bulk_recovery` (CONTEXT.md D-02 wording is stale).
- Selector pattern from bulk_recovery_live_test.exs: `element(~s(input[phx-click="toggle_select"][phx-value-id="#{id}"]))`, `element(~s(button[phx-click="open_bulk_confirm"]))`, `element(~s(button[phx-click="confirm_bulk_send"]))`.

Outbound (lib/cairnloop/outbound.ex):
- `Outbound.trigger(conversation.id, template_id: "recovery_v1", actor: conversation.host_user_id)` → inserts a `:system_outbound` Message row. Stage 8 prefers the LiveView event `trigger_recovery_follow_up`.
- Bulk submit (`confirm_bulk_send`) creates a `%BulkEnvelope{status: :submitted}` row + per-recipient `:system_outbound` Message rows. There are NO Oban `OutboundWorker` jobs enqueued by the bulk path — assert the BulkEnvelope row + the `:system_outbound` Message rows (Pitfall in RESEARCH §Outbound.bulk_trigger/2). The E2E-01 acceptance phrasing "OutboundWorker jobs enqueued" is satisfied by the per-recipient system_outbound Message rows that the bulk fan-out creates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module skeleton, inline stubs, and setup block</name>
  <files>test/integration/golden_path_test.exs</files>
  <read_first>
    - test/integration/tool_execution_outcome_live_test.exs (canonical: ConnCase usage, inline `NoteWriteTool` + `StubContextProvider` defmodule shape, `Application.put_env`/`on_exit` setup, Oban.Job struct calls)
    - test/integration/bulk_recovery_live_test.exs (canonical: `import Ecto.Query`, `Plug.Test.init_test_session`, alias `Cairnloop.Outbound.BulkEnvelope`)
    - lib/cairnloop/retrieval/result.ex (exact `%Cairnloop.Retrieval.Result{}` field list for StubRetrieval)
    - .planning/phases/31-golden-path-jtbd-smoke-test/31-CONTEXT.md (D-10 inline stubs; D-12 no SeedRun.run/0)
    - CLAUDE.md (`# REPO-UNAVAILABLE` convention; warnings-clean build)
  </read_first>
  <action>
    Create `test/integration/golden_path_test.exs` with module `Cairnloop.Integration.GoldenPathTest` and `use Cairnloop.ConnCase, async: false` (D-07). Add a `@moduledoc` that names the requirement IDs it closes (E2E-01, contributes to E2E-03) and carries the `# REPO-UNAVAILABLE — only runs under mix test.integration` note per CLAUDE.md.

    Add imports/aliases: `import Cairnloop.Fixtures`, `import Ecto.Query`, `alias Cairnloop.{Chat, Governance, Outbound}`, `alias Cairnloop.Outbound.BulkEnvelope`, `alias Cairnloop.Message`, `alias Cairnloop.Workers.{ApprovalResumeWorker, ToolExecutionWorker}`.

    Add three inline `defmodule` stubs (D-10), inside the test module:
    - `StubContextProvider` with `def get_context(_host_user_id, _opts), do: {:ok, %{}}`.
    - `InlineTestTool` using `Cairnloop.Tool, risk_tier: :low_write, title: "Golden Path Tool", description: "No scope required."` with an `embedded_schema` of `field(:conversation_id, :string)` + `field(:note, :string)`, and the four `@impl Cairnloop.Tool` callbacks: `changeset/2` (cast `[:conversation_id, :note]`), `scope/0` → `[]`, `authorize/2` → `:ok`, `run/3` → `{:ok, %{done: true}}`. Mirror the `NoteWriteTool` shape in tool_execution_outcome_live_test.exs.
    - `StubRetrieval` with `def search(_query, _opts)` returning a PLAIN LIST containing at least one `%Cairnloop.Retrieval.Result{}` struct with `id: 1, article_id: 1, title: "Test Article", content: "Stub content for golden path", source_type: :knowledge_base, trust_level: :canonical, score: 0.9` (D-04, Pitfall 4 — must be a Result struct, not a plain map, with a valid `article_id` + `content`).

    Add a `setup` block that: `Application.put_env(:cairnloop, :tools, [InlineTestTool])`, `Application.put_env(:cairnloop, :context_provider, StubContextProvider)`, `Application.put_env(:cairnloop, :outbound_recovery_template_id, "recovery_v1")` (required so the stage-8 `trigger_recovery_follow_up` button renders, per line 1773); register `on_exit` that deletes all three env keys; build the authed conn via `conn = Plug.Test.init_test_session(conn, %{"host_user_id" => "golden_operator"})` (take `conn` from the ConnCase-provided `%{conn: conn}`) and return `%{conn: conn}`.

    Do NOT call `SeedRun.run/0` (D-12). Do NOT use `Oban.drain_queue` anywhere (D-09).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; grep -c "defmodule StubRetrieval\|defmodule InlineTestTool\|defmodule StubContextProvider" test/integration/golden_path_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File `test/integration/golden_path_test.exs` exists and begins with `defmodule Cairnloop.Integration.GoldenPathTest do`.
    - File contains `use Cairnloop.ConnCase, async: false`.
    - `grep -c "defmodule StubRetrieval"` returns 1, `grep -c "defmodule InlineTestTool"` returns 1, `grep -c "defmodule StubContextProvider"` returns 1.
    - `StubRetrieval.search/2` body contains `%Cairnloop.Retrieval.Result{` (struct, not a bare map literal `%{`).
    - `grep -c "REPO-UNAVAILABLE" test/integration/golden_path_test.exs` returns at least 1.
    - `grep -c "Oban.drain_queue\|SeedRun.run" test/integration/golden_path_test.exs` returns 0.
    - `mix compile --warnings-as-errors` exits 0 with no new warnings.
  </acceptance_criteria>
  <done>The test module compiles warnings-clean with all three inline stubs and a setup block wiring the tools/context_provider/outbound_recovery_template_id env keys and an authed conn.</done>
</task>

<task type="auto">
  <name>Task 2: Stages 1-5 — seed through tool-proposal approve</name>
  <files>test/integration/golden_path_test.exs</files>
  <read_first>
    - test/integration/approval_flow_test.exs (lines 49-82: enqueue_fn capture pattern, `request_approval`/`approve` with `enqueue_fn:`, `assert_received {:enqueued, _}`, `ApprovalResumeWorker.perform/1`)
    - test/integration/tool_execution_outcome_live_test.exs (proposal_fixture with input_snapshot/policy_snapshot, the `/governance/#{id}` mount + render assertions)
    - lib/cairnloop/web/search_modal_component.ex (event names `toggle_search`/`search`/`activate_result`/`open_active_result`; `:retrieval_module` injection point; component id `search-modal`)
    - lib/cairnloop/automation.ex (lines 10-35: `create_draft/2` shape and return)
    - lib/cairnloop/automation/draft.ex (changeset required fields + enums)
    - .planning/phases/31-golden-path-jtbd-smoke-test/31-CONTEXT.md (D-02 stages, D-03 send_update inject, D-04 stub result)
  </read_first>
  <action>
    Open the single `test "full JTBD round trip", %{conn: conn} do` block. Set `test_pid = self()` and `capture = fn job -> send(test_pid, {:enqueued, job}); {:ok, job} end` near the top.

    Stage 1 (Seed): create `conversation = conversation_fixture(%{status: :open, host_user_id: "golden_operator", subject: "Golden path conversation"})`; seed a customer message via `message_fixture(%{conversation_id: conversation.id, content: "I need a refund", role: :user})`. Add a `# Stage 1: Seed` comment.

    Stage 2 (Inbox sees): `{:ok, inbox_view, html} = live(conn, "/inbox")`; `assert html =~ conversation.subject`. Comment `# Stage 2: Inbox sees`.

    Stage 3 (ConversationLive + cmd+k search + citation chip): `{:ok, view, _html} = live(conn, "/governance/#{conversation.id}")` (use `/governance/` — Pitfall 1). Inject the stub: `send_update(Cairnloop.Web.SearchModalComponent, id: "search-modal", retrieval_module: StubRetrieval)` (D-03). Open the palette by firing the `toggle_search` event at the component root, then fire `search` with a query of length >= 2 (e.g. `"refund"`), then `assert render(view) =~ "Test Article"` (the stub result title). Fire `activate_result` then `open_active_result` to simulate the citation-chip click. Per RESEARCH Open Question 1, prefer targeting the component root via `element("[phx-window-keydown='toggle_search']") |> render_keydown(%{"key" => "k"})`; if the view-level form/element targeting is needed for `search`, target the component's search input. Comment `# Stage 3: ConversationLive + cmd+k search + citation chip`. The citation-chip navigation is internal (push_navigate) — do not assert a parent event.

    Stage 4 (Approve AI draft): seed a draft with `{:ok, draft} = Cairnloop.Automation.create_draft(conversation.id, %{customer_reply: "Here is your refund confirmation.", proposal_type: :reply, status: :pending})`. Remount `{:ok, view, _html} = live(conn, "/governance/#{conversation.id}")` so the `@conversation.drafts` assoc loads the new draft; assert the rendered HTML shows the draft approval affordance (e.g. `assert render(view) =~ "approve_draft"`). Fire the `approve_draft` event with `%{"draft-id" => to_string(draft.id)}` via the rendered button element. Assert `Cairnloop.Repo.get!(Cairnloop.Automation.Draft, draft.id).status == :approved`. Comment `# Stage 4: Approve AI draft`.

    Stage 5 (Tool proposal approve): create `proposal = proposal_fixture(%{tool_ref: Atom.to_string(InlineTestTool), approval_mode: :requires_approval, scope_snapshot: %{scopes: []}, input_snapshot: %{conversation_id: to_string(conversation.id), note: "golden path note"}, policy_snapshot: %{outcome: :proposed}})`. Then `assert {:ok, approval} = Governance.request_approval(proposal, enqueue_fn: capture)`; `assert_received {:enqueued, _expiry_job}`. Then `assert {:ok, _approved} = Governance.approve(approval.id, "golden_operator", enqueue_fn: capture)`; `assert_received {:enqueued, _resume_job}` (Pitfall 8 — enqueue_fn required). Bind `approval` and `proposal` for use in Task 3 (they accumulate as test-local variables). Comment `# Stage 5: Tool proposal approve`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; grep -c "Stage 1:\|Stage 2:\|Stage 3:\|Stage 4:\|Stage 5:" test/integration/golden_path_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The test body contains comments `# Stage 1:`, `# Stage 2:`, `# Stage 3:`, `# Stage 4:`, `# Stage 5:` (grep count >= 5).
    - Stage 3 contains `live(conn, "/governance/#{conversation.id}")` (NOT `live(conn, "/#{conversation.id}")`).
    - Stage 3 contains `send_update(Cairnloop.Web.SearchModalComponent` with `retrieval_module: StubRetrieval` and `id: "search-modal"`.
    - Stage 4 contains `Cairnloop.Automation.create_draft(` and an assertion that the draft status equals `:approved`.
    - Stage 5 contains `Governance.request_approval(proposal, enqueue_fn: capture)` AND `Governance.approve(approval.id, "golden_operator", enqueue_fn: capture)` AND two `assert_received {:enqueued, ...}` assertions.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Stages 1-5 are present in the single test block, accumulate `conversation`/`draft`/`approval`/`proposal` state, use the verified routes/events/facades, and the file compiles warnings-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Stages 6-9 — ToolExecutionWorker through bulk recovery + run the test</name>
  <files>test/integration/golden_path_test.exs</files>
  <read_first>
    - test/integration/tool_execution_outcome_live_test.exs (lines 152-160: ApprovalResumeWorker then ToolExecutionWorker with full Oban.Job struct; lines 305-317: render `/governance/#{id}` and assert "Action completed")
    - test/integration/bulk_recovery_live_test.exs (lines 164-231: toggle_select → open_bulk_confirm → confirm_bulk_send event chain; BulkEnvelope count + system_outbound Message assertions)
    - lib/cairnloop/chat.ex (line 222: `resolve_conversation/2`)
    - lib/cairnloop/web/conversation_live.ex (line 856 `:resolved` render guard; line 214 `trigger_recovery_follow_up`; line 1773 template_id env read)
    - .planning/phases/31-golden-path-jtbd-smoke-test/31-RESEARCH.md (Pitfall 2/3/5; §Outbound.bulk_trigger/2 — no OutboundWorker jobs, assert system_outbound Message rows)
  </read_first>
  <action>
    Continue the same test block after Stage 5.

    Stage 6 (ToolExecutionWorker :success): `assert :ok = ApprovalResumeWorker.perform(%Oban.Job{args: %{"approval_id" => approval.id}})`; then `assert :ok = ToolExecutionWorker.perform(%Oban.Job{attempt: 1, max_attempts: 3, args: %{"approval_id" => approval.id}})` (BOTH attempt + max_attempts — Pitfall 5). Assert `Cairnloop.Repo.get!(Cairnloop.Governance.ToolApproval, approval.id).status == :executed`. Remount `{:ok, _view, html} = live(conn, "/governance/#{conversation.id}")` and `assert html =~ "Action completed"` (the done-group chip text). Comment `# Stage 6: ToolExecutionWorker :success`.

    Stage 7 (Resolve): `assert {:ok, _} = Chat.resolve_conversation(conversation.id, resolved_by: "golden_operator")` — there is NO `"resolve"` LiveView event (Pitfall 2). Reload `conversation = Chat.get_conversation!(conversation.id)`; `assert conversation.status == :resolved`. Comment `# Stage 7: Resolve`.

    Stage 8 (Outbound.trigger/2 from sidebar): remount `{:ok, view, html} = live(conn, "/governance/#{conversation.id}")` (the `trigger_recovery_follow_up` button only renders for `:resolved` conversations — Pitfall 3; remount because `resolve_conversation/2` does not broadcast a topic ConversationLive handles). `assert html =~ "trigger_recovery_follow_up"`. Record `outbound_before = Cairnloop.Repo.aggregate(from(m in Message, where: m.conversation_id == ^conversation.id and m.role == :system_outbound), :count, :id)`. Fire the button via `element(~s(button[phx-click="trigger_recovery_follow_up"])) |> render_click()`. Assert a new `:system_outbound` Message row landed for this conversation (count increased by 1). Comment `# Stage 8: Outbound.trigger/2`.

    Stage 9 (Bulk recovery): record `envelope_before = Cairnloop.Repo.aggregate(BulkEnvelope, :count, :id)`. `{:ok, inbox_view, _html} = live(conn, "/inbox")`. Fire the bulk chain mirroring bulk_recovery_live_test.exs: `inbox_view |> element(~s(input[phx-click="toggle_select"][phx-value-id="#{conversation.id}"])) |> render_click()`; then `element(~s(button[phx-click="open_bulk_confirm"])) |> render_click()`; then `element(~s(button[phx-click="confirm_bulk_send"])) |> render_click()` (the real event is `confirm_bulk_send`, NOT `confirm_bulk_recovery`). Assert `Cairnloop.Repo.aggregate(BulkEnvelope, :count, :id) == envelope_before + 1`; fetch the latest envelope via `BulkEnvelope |> order_by([e], desc: e.inserted_at) |> limit(1) |> Cairnloop.Repo.one!()` and assert `envelope.status == :submitted`; assert at least one `:system_outbound` Message row exists for the conversation produced by the bulk fan-out. Comment `# Stage 9: Bulk recovery`.

    Run the full test and confirm green.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3 && mix test.integration test/integration/golden_path_test.exs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Stage 6 contains `ToolExecutionWorker.perform(%Oban.Job{attempt: 1, max_attempts: 3, args: %{"approval_id" => approval.id}})` and an assertion on `:executed` status.
    - Stage 7 contains `Chat.resolve_conversation(conversation.id, resolved_by: "golden_operator")` and asserts `conversation.status == :resolved`. There is NO `render_click(view, "resolve")` or `render_keydown(view, "resolve", ...)` anywhere in the file.
    - Stage 8 contains a remount of `/governance/#{conversation.id}` after resolve and fires `phx-click="trigger_recovery_follow_up"`.
    - Stage 9 contains `confirm_bulk_send` (and does NOT contain `confirm_bulk_recovery`), asserts `BulkEnvelope` count increased by 1, and asserts `envelope.status == :submitted`.
    - `grep -c "Stage [1-9]:" test/integration/golden_path_test.exs` returns 9.
    - `mix test.integration test/integration/golden_path_test.exs` exits 0 — the test passes against real Postgres + pgvector.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>All 9 stages are present and the single `test "full JTBD round trip"` passes under `mix test.integration` against real Postgres + pgvector with no new compiler warnings.</done>
</task>

</tasks>

<verification>
- `mix test.integration test/integration/golden_path_test.exs` exits 0 (E2E-01).
- `mix compile --warnings-as-errors` exits 0 (CLAUDE.md mandatory gate; E2E-03 no new deps/warnings).
- `grep -rn "Wallaby\|PhoenixTest" test/integration/golden_path_test.exs` returns nothing (E2E-03 no browser driver, no PhoenixTest dep).
- `git diff --stat mix.exs` shows no change (E2E-03 no new dependency).
- `grep -c "Stage [1-9]:" test/integration/golden_path_test.exs` returns 9 (all D-02 stages present).
</verification>

<success_criteria>
- A single sequential `test "full JTBD round trip"` covers all 9 JTBD stages and passes against real Postgres + pgvector via `mix test.integration`.
- No new file under `test/support/`; no new dependency in `mix.exs`.
- The 8 RESEARCH.md pitfalls relevant to the golden path are honored: `/governance/:id` route (P1), no `"resolve"` event (P2), `trigger_recovery_follow_up` after resolve + remount (P3), `Retrieval.Result` structs from StubRetrieval (P4), full Oban.Job struct for ToolExecutionWorker (P5), enqueue_fn capture for Governance.approve (P8).
- Build is warnings-clean.
</success_criteria>

<output>
Create `.planning/phases/31-golden-path-jtbd-smoke-test/31-01-SUMMARY.md` when done.
</output>
