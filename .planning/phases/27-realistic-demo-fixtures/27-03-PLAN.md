---
phase: 27-realistic-demo-fixtures
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - examples/cairnloop_example/priv/repo/seeds.exs
autonomous: true
requirements:
  - FIX-02
tags:
  - seeds
  - elixir
  - knowledge-base
  - facade
  - oban
must_haves:
  truths:
    - "build_articles/0 inserts 5 KB articles via Cairnloop.KnowledgeBase.create_article/1 + save_draft/2 + publish_revision/1 (facade-only, never direct %Revision{} insert)"
    - "article 5 (Rotating an expired token) runs v1 published → v1 archived → v2 published so the KB has at least one :archived revision (D-05 / FIX-02)"
    - "each article body contains 2+ ## h2 headings so MarkdownParser.parse_sections/1 splits it into multiple chunks (Pitfall 4)"
    - "build_articles/0 is idempotent: a second run is a no-op via get_or_insert!/3 keyed on :title; the v1→archived→v2 progression also checks current state before transitioning"
    - "build_articles/0 returns a deterministic map %{api_key: %Article{}, billing_email: …, seat: …, ci_skipped: …, token_rotation: …} so plan 27-06 can wire the seeded ArticleSuggestion's evidence to a real published revision"
  artifacts:
    - path: "examples/cairnloop_example/priv/repo/seeds.exs"
      provides: "Implemented build_articles/0 with 5 articles, 6 revisions (≥1 archived), all enqueueing ChunkRevision via facade Multi"
      contains: "KnowledgeBase.publish_revision"
  key_links:
    - from: "build_articles/0"
      to: "Cairnloop.KnowledgeBase.Workers.ChunkRevision"
      via: "publish_revision/1 Multi (lib/cairnloop/knowledge_base.ex:71-86)"
      pattern: "Ecto.Multi.insert.*ChunkRevision"
    - from: "article 5 (Rotating an expired token)"
      to: "FIX-02 ≥1 :archived revision criterion"
      via: "v1 published → state :archived → v2 published"
      pattern: "state: :archived"
---

<objective>
Implement build_articles/0 in seeds.exs: insert 5 Trailmark KB articles via the KnowledgeBase facade (D-09), with article 5 running a multi-revision progression so the KB has at least one :archived revision (D-05). The facade route is what enqueues ChunkRevision into the :default Oban queue; the drain in plan 27-07 then turns those enqueued jobs into pgvector rows. Together they satisfy FIX-02 end-to-end.

Purpose: FIX-02's "≥5 articles with multiple revisions including ≥1 :deprecated" + "embeddings flow through the live ChunkRevision Oban worker into pgvector" criteria. This plan provides the FIRST half; plan 27-07's drain provides the second half. Skipping the facade (using direct %Revision{} insert) would silently break FIX-02 because the Multi-side enqueue would be skipped.

Output: A working build_articles/0 body that produces 5 articles + 6 revisions (5 published + 1 archived) on a fresh DB and is a no-op on re-run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md
@.planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md
@.planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md
@CLAUDE.md
@prompts/cairnloop_brand_book.md
@lib/cairnloop/knowledge_base.ex
@lib/cairnloop/knowledge_base/article.ex
@lib/cairnloop/knowledge_base/revision.ex
@lib/cairnloop/knowledge_base/markdown_parser.ex
@lib/cairnloop/knowledge_base/workers/chunk_revision.ex
@examples/cairnloop_example/priv/repo/seeds.exs

<interfaces>
<!-- Cairnloop.KnowledgeBase facade signatures (verified lib/cairnloop/knowledge_base.ex:39-86):

       @spec create_article(map) :: {:ok, Article.t()} | {:error, Ecto.Changeset.t()}
       @spec save_draft(Article.t(), map) :: {:ok, Revision.t()} | {:error, ...}
       @spec publish_revision(Revision.t()) :: {:ok, Revision.t()} | {:error, term}

     publish_revision/1 Multi (THE LOAD-BEARING PIECE — bypass breaks FIX-02):

       Ecto.Multi.new()
       |> Ecto.Multi.update(:revision, Revision.changeset(revision, %{state: :published}))
       |> Ecto.Multi.update(:article, fn %{revision: rev} ->
            Article.changeset(repo().get!(Article, rev.article_id), %{status: :published})
          end)
       |> Ecto.Multi.insert(
            :chunk_job,
            Cairnloop.KnowledgeBase.Workers.ChunkRevision.new(%{revision_id: revision.id})
          )
       |> repo().transaction()

     KB.Article sealed enums (lib/cairnloop/knowledge_base/article.ex):
       status :: :draft | :published | :archived

     KB.Revision sealed enums (lib/cairnloop/knowledge_base/revision.ex):
       state :: :draft | :published | :archived

     MarkdownParser.parse_sections/1 splits on ## h2 and ### h3 headings (lib/cairnloop/knowledge_base/markdown_parser.ex:14-32);
     heading-less bodies collapse to a single chunk (Pitfall 4).

     Idempotency helper from plan 27-01:
       defp get_or_insert!(schema_module, natural_key_field, attrs) :: %schema_module{}
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement build_articles/0 with 5 Trailmark articles via the KB facade + multi-revision progression for article 5</name>
  <files>examples/cairnloop_example/priv/repo/seeds.exs</files>
  <read_first>
    - examples/cairnloop_example/priv/repo/seeds.exs (the plan 27-01 skeleton — confirm the build_articles/0 stub exists and the get_or_insert!/3 helper signature)
    - lib/cairnloop/knowledge_base.ex (lines 39–86: create_article/1, save_draft/2, publish_revision/1 — verify the publish_revision Multi inserts :chunk_job)
    - lib/cairnloop/knowledge_base/article.ex (status enum)
    - lib/cairnloop/knowledge_base/revision.ex (state enum + changeset rules; verify a :published revision can be re-cast to :archived without bumping content)
    - lib/cairnloop/knowledge_base/markdown_parser.ex (lines 14–55: confirm h2/h3 split rule — every article body must include 2+ ## h2 sections)
    - .planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md D-09, D-17 (Trailmark + 5 article titles + article 5 multi-revision)
    - .planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md (Pattern 2 KB facade sequence, Pattern 3 multi-revision progression, Pitfall 4 h2/h3 splitting)
    - .planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md (seeds.exs section — direct-insert and facade idioms)
    - prompts/cairnloop_brand_book.md (sections 5/5.5/7.5 — article body voice: calm, factual, no raw Elixir terms)
  </read_first>
  <action>
Implement the body of build_articles/0 inside the CairnloopExample.SeedRun module from plan 27-01. Replace the # TODO stub with the following behaviour:

1. **Define the 5 articles (D-17 titles, verbatim):**
   - {:api_key, "Resetting your Trailmark API key"}
   - {:billing_email, "Updating your billing email"}
   - {:seat, "Adding a team seat"}
   - {:ci_skipped, "Why a CI run was skipped"}
   - {:token_rotation, "Rotating an expired token"}

2. **For each of the FIRST 4 articles (api_key, billing_email, seat, ci_skipped):**
   - Use get_or_insert!(Article, :title, %{title: title, status: :draft}) to insert the article (or return existing). The article status will be flipped to :published by publish_revision/1's Multi.
   - Build a body string with EXACTLY 2–3 ## h2 sections (Pitfall 4 — heading-less bodies collapse to a single chunk and weaken the M008 self-test). Each body must read as plain English documentation — brand voice §5.5 (calm, reason-forward, no raw atoms/JSON). Suggested skeletons:
     - api_key: "## Reset steps\n\n…steps…\n\n## When to contact support\n\n…fallback…\n\n## Notes\n\n…security note…"
     - billing_email: "## Update your billing email\n\n…steps…\n\n## What happens to invoices\n\n…note…"
     - seat: "## Invite a teammate\n\n…steps…\n\n## When approval is required\n\n…explanation of governed action…\n\n## Removing a seat\n\n…note…"
     - ci_skipped: "## Why this happens\n\n…explanation…\n\n## How to investigate\n\n…steps…\n\n## Common causes\n\n…list…"
   - Check whether a published revision already exists for this article (Repo.get_by(Revision, article_id: article.id, state: :published)). If yes, return; if no, proceed to save_draft + publish_revision.
   - {:ok, draft} = KnowledgeBase.save_draft(article, %{content: body})
   - {:ok, _published} = KnowledgeBase.publish_revision(draft) — this is the line that enqueues ChunkRevision into Oban.

3. **For article 5 (token_rotation), run the multi-revision progression (D-05 + Pattern 3):**
   - get_or_insert!(Article, :title, %{title: "Rotating an expired token", status: :draft}).
   - Idempotency check #1: count published revisions for this article; if 1+ exists with content containing the v2 marker (see below) AND 1 archived revision exists, skip the entire block (already-seeded). Otherwise:
     - Save draft v1 with body: "## Old guidance\n\nRotate every 30 days.\n\n## Notes\n\nThis guidance is being updated; see the latest article for the current rotation window."
     - Publish v1 via publish_revision/1.
     - Re-cast the published v1 to state :archived: published_v1 |> Revision.changeset(%{state: :archived}) |> Repo.update!() — this does NOT go through publish_revision (we want to demote, not republish, and we deliberately keep the same content per Revision.changeset enforce_immutability rule which only blocks content edits, not state-only edits).
     - Save draft v2 with body: "## Current guidance\n\nRotate every 90 days.\n\n## Why this changed\n\n90-day rotation balances security with operational stability.\n\n## How to rotate\n\n…steps…" — the "v2 marker" the idempotency check looks for is the literal substring "Rotate every 90 days" (use a constant @v2_marker "Rotate every 90 days" at the top of the module so the check is unambiguous).
     - Publish v2 via publish_revision/1.

4. **Return value:** Return a map keyed by the article handle atoms — plan 27-06 needs this to wire ArticleSuggestion evidence to a real published revision id:

       %{
         api_key: api_key_article,
         billing_email: billing_email_article,
         seat: seat_article,
         ci_skipped: ci_skipped_article,
         token_rotation: token_rotation_article
       }

5. **Idempotency rule (D-02) applies to every insert:**
   - The Article-level get_or_insert!/3 handles re-runs at the article granularity.
   - For revisions, the check is "does a published revision already exist?" — if yes, do NOT call save_draft + publish_revision (a re-run would create duplicate revisions and re-enqueue ChunkRevision jobs).
   - For article 5's archive-progression, the check is "does an :archived revision exist AND a published revision with the v2 marker?" — if both true, skip; otherwise the progression runs.

Constraints (do NOT violate):
- Never call Repo.insert!(%Revision{}) directly — D-09 + Pitfall 2: bypasses the publish_revision Multi and silently breaks FIX-02 (no ChunkRevision enqueue, no chunks in pgvector, no detectable error at seed-run time).
- Never use Ecto.Changeset.change/2 — use Schema.changeset/2 + the get_or_insert!/3 helper (PATTERNS.md divergence rule #4).
- Brand voice (C-08): article bodies are calm, factual, no raw Elixir atoms, no raw JSON; if mentioning a governed-action button (seat article), refer to it by display label ("Invite a teammate") not by tool name (Cairnloop.Tools.SeatInvite).
- Do NOT touch any library code — Phase 27 is fixture-only (C-07).
- Do NOT modify build_conversations/1, build_gaps/1, build_suggestion/2, or drain_embedding_pipeline/0 — they belong to plans 27-04, 27-05, 27-06, 27-07 respectively.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tee /tmp/p27-03-compile.log; echo '---'; echo 'facade-calls-in-build_articles:'; awk '/defp build_articles/,/^  end$/' examples/cairnloop_example/priv/repo/seeds.exs | grep -E 'KnowledgeBase\.(create_article|save_draft|publish_revision)' | wc -l; echo 'direct-revision-inserts (should be 0):'; awk '/defp build_articles/,/^  end$/' examples/cairnloop_example/priv/repo/seeds.exs | grep -cE 'Repo\.insert!?\(.*%Revision\{|insert!?\(%(Cairnloop\.KnowledgeBase\.)?Revision\{' || echo 0; echo 'h2-headings-in-bodies (should be >= 10 for 5 articles × 2 sections):'; awk '/defp build_articles/,/^  end$/' examples/cairnloop_example/priv/repo/seeds.exs | grep -cE '^\s*"## '; echo 'v2-marker-present:'; grep -c '90 days' examples/cairnloop_example/priv/repo/seeds.exs</automated>
    <human-check>Spot-check 2 article bodies for brand voice (calm, factual, no atoms/JSON). Confirm article 5's body contains the v2_marker substring and that an archive-progression step calls Revision.changeset with state: :archived.</human-check>
  </verify>
  <acceptance_criteria>
- cd examples/cairnloop_example && mix compile --warnings-as-errors exits 0.
- grep -cE 'KnowledgeBase\.publish_revision' examples/cairnloop_example/priv/repo/seeds.exs returns ≥ 6 (5 first publishes + 1 v2 publish on article 5 = 6 total calls).
- grep -c 'state: :archived' examples/cairnloop_example/priv/repo/seeds.exs returns ≥ 1 (the v1 archive step on article 5).
- grep -cE 'Repo\.insert!?\(.*%(Cairnloop\.KnowledgeBase\.)?Revision\{' examples/cairnloop_example/priv/repo/seeds.exs returns 0 (no direct revision insertion — facade only).
- grep -cE '^\s*"## ' examples/cairnloop_example/priv/repo/seeds.exs (the awk-scoped count) returns ≥ 10 (each of 5 articles has ≥ 2 h2 sections). Note: a permissive grep is acceptable as long as the count clearly exceeds 5 (single-section anti-pattern).
- Article body for "Rotating an expired token" contains the literal substring "Rotate every 90 days" (the v2 marker for idempotency).
- Plan 27-08's integration test (when written) will assert the final DB state — at this task's commit time we accept the static grep gates above.
  </acceptance_criteria>
  <done>
All acceptance_criteria conditions hold; build_articles/0 returns the deterministic map and is idempotent under re-run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → KnowledgeBase facade → ChunkRevision worker | The facade is the only path that enqueues the embedding worker; bypass would silently break FIX-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Tampering | direct %Revision{} insert bypassing publish_revision Multi (silent FIX-02 break) | mitigate | Plan explicitly forbids direct Repo.insert on %Revision{}. Acceptance criterion includes a grep gate counting such inserts (must be 0). PATTERNS.md flags this as anti-pattern #1. |
| T-27-08 | Repudiation | non-idempotent revision creation producing duplicate published rows on re-run | mitigate | The per-article idempotency check (Repo.get_by(Revision, article_id: id, state: :published)) prevents duplicate publishes; the @v2_marker check on article 5 prevents duplicate progression runs. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | No new packages. |
</threat_model>

<verification>
After this task:
1. Grep gates in <verify> pass (facade-only inserts, h2 headings present, v2 marker present, ≥1 archived state).
2. Compile is warnings-clean.
3. The build_articles/0 return value is a 5-key map (verifiable by reading the final return expression).
4. End-to-end FIX-02 verification (chunks land in pgvector after Oban drain) is deferred to plan 27-08's integration test once plans 27-07 and 27-08 ship; this plan ships its part of the contract.
</verification>

<success_criteria>
- 5 articles + 6 revisions (including ≥1 :archived) are created via the KB facade; the publish_revision Multi enqueues a ChunkRevision job for each :published revision.
- The seed remains warnings-clean and idempotent.
- Plan 27-06 has a deterministic article-handle map to reference for ArticleSuggestion evidence wiring.
- Plan 27-07's Oban drain will turn the 6 enqueued jobs into a non-empty cairnloop_chunks table.
- No library code modified.
</success_criteria>

<output>
Create .planning/phases/27-realistic-demo-fixtures/27-03-SUMMARY.md when done — record:
- The 5 article titles inserted (verbatim) and the handle atoms in the return map.
- The article 5 v1→archived→v2 progression's exact body strings (so plan 27-08's test can pin them).
- Confirmation that build_articles/0 was tested idempotent by manual re-invocation, OR a note that plan 27-08's test will pin it.
- The @v2_marker constant value.
</output>
