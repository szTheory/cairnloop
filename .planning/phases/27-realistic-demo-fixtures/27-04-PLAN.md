---
phase: 27-realistic-demo-fixtures
plan: 04
type: execute
wave: 3
depends_on:
  - 27-03
files_modified:
  - examples/cairnloop_example/priv/repo/seeds.exs
autonomous: true
requirements:
  - FIX-01
tags:
  - seeds
  - elixir
  - conversations
  - messages
  - jtbd
must_haves:
  truths:
    - "build_conversations/1 inserts exactly 16 conversations across 4 JTBD-derived cohorts (4 :new + 4 :open + 4 :awaiting_customer + 4 :resolved) per D-03"
    - "each conversation has 2–6 Cairnloop.Message rows with valid role enum values from [:user, :agent, :internal_note, :system_outbound]; per-cohort role ordering matches the D-03 derivation rules (the :new cohort uses 2 :user-only messages — opening + a follow-up — which still satisfies the JTBD-derived contract 'status :open + 0 :agent messages')"
    - "at least one conversation per article (5 articles → 5+ matching conversations) so cmd+k lex-match yields a sensible hit (D-19)"
    - "each conversation's Conversation.host_user_id matches one of the 5 demo customer ids the DemoContextProvider knows (plan 27-02), so the operator inbox renders ContextProvider snippets"
    - "all message inserts use Schema.changeset/2 (NOT Ecto.Changeset.change/2) so validate_template_id_for_outbound/1 runs; :system_outbound messages always carry metadata.template_id (Pitfall 6)"
    - "build_conversations/1 is idempotent: re-run is a no-op via get_or_insert!/3 keyed on Conversation.subject (subjects prefixed [demo-NN])"
  artifacts:
    - path: "examples/cairnloop_example/priv/repo/seeds.exs"
      provides: "Implemented build_conversations/1 producing 16 conversations × 2–6 messages spanning 4 JTBD cohorts (total ~60 messages: 4*2 + 4*4 + 4*3 + 4*5 = 8+16+12+20 = 56, plus 2 :internal_note additions = 58)"
      contains: "build_conversations"
  key_links:
    - from: "Conversation.host_user_id (customer identity)"
      to: "CairnloopExample.DemoContextProvider clauses"
      via: "lib/cairnloop/web/conversation_live.ex:361 — provider.get_context(conversation.host_user_id, [])"
      pattern: "demo_user_"
    - from: ":resolved cohort"
      to: "Cairnloop.Outbound eligibility (sidebar Send Recovery Follow-up)"
      via: "Conversation.status :resolved + resolved_at set"
      pattern: "status: :resolved"
---

<objective>
Implement build_conversations/1 in seeds.exs: insert 16 conversations × 2–6 messages each across the 4 JTBD-derived cohorts (D-03), with at least one conversation per article (D-19) and all conversations keyed to the 5 demo customer ids the DemoContextProvider knows (plan 27-02). The Conversation.status enum is sealed at [:open, :resolved, :archived]; the four spec-language JTBD states are DERIVED from status + message ordering, not stored.

Purpose: FIX-01's "12–16 conversations spanning :new, :open, :awaiting_customer, :resolved with realistic operator + customer messages and ContextProvider snippets" criterion. The ContextProvider snippets half of FIX-01 ships in plan 27-02; the conversation-rendering half ships here.

Output: A working build_conversations/1 body producing exactly 16 conversations + ~56–58 messages on fresh DB, idempotent on re-run, returning a list of inserted %Conversation{} rows so plan 27-05 (gaps) and plan 27-06 (suggestion) can reference them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md
@.planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md
@.planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md
@CLAUDE.md
@prompts/cairnloop_brand_book.md
@lib/cairnloop/conversation.ex
@lib/cairnloop/message.ex
@test/support/fixtures.ex
@examples/cairnloop_example/priv/repo/seeds.exs

<interfaces>
<!-- Cairnloop.Conversation sealed schema (verified lib/cairnloop/conversation.ex):
       status   :: :open | :resolved | :archived       (default :open)
       subject  :: String.t
       host_user_id :: String.t  -- customer identity (drives ContextProvider lookup)
       resolved_at  :: DateTime.t | nil
       csat_rating  :: :positive | :negative | nil

     Cairnloop.Message sealed schema (verified lib/cairnloop/message.ex):
       content  :: String.t
       role     :: :user | :agent | :system | :internal_note | :system_outbound  (default :user)
       metadata :: map  (default %{})
       conversation_id :: belongs_to Cairnloop.Conversation
       run_key  :: String.t | nil    -- DO NOT SET: Pitfall 5; example app's migration lacks the column

     Message.changeset/2 validates:
       - validate_required([:content, :role, :conversation_id])
       - validate_template_id_for_outbound/1 — :system_outbound role REQUIRES metadata["template_id"]

     JTBD cohort derivation rules (D-03; the planner's verbatim contract):
       "new"               -> status: :open  + 0 :agent messages                                 (4 conversations)
       "open"              -> status: :open  + has :agent reply + last msg role :user            (4 conversations)
       "awaiting_customer" -> status: :open  + has :agent reply + last msg role :agent           (4 conversations)
       "resolved"          -> status: :resolved + resolved_at set + last msg :agent | :system_outbound  (4 conversations)
       Total: 16.

     Per-cohort message-count guidance (Pitfall 6 + D-03 + planner shift-left):
       - :new cohort uses 2 :user-only messages (opening + 1 follow-up) — zero :agent messages still
         satisfies the JTBD derivation contract; multiple :user messages model real "customer left
         a clarifying note before anyone replied" inbox state.
       - For the 4 :resolved conversations: 3 close with role :agent (simpler — no template_id required),
         1 closes with role :system_outbound carrying metadata: %{"template_id" => "demo_resolve_confirm"}
         so the "outbound bubble" visual appears in the demo.
       - Internal-note bodies (role :internal_note) may reference IDs/typed terms (D-18 carve-out); 1–2 of
         the 16 conversations include a single :internal_note message demonstrating this surface.

     Idempotency: each conversation's subject is prefixed [demo-NN] where NN is 01..16; the helper
       get_or_insert!(Conversation, :subject, attrs) provides idempotency at the conversation level.
       If the conversation already exists (Repo.get_by hit), the messages are NOT re-inserted
       (the helper returns early; messages are conditionally inserted only on the nil branch).
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement build_conversations/1 with 16 conversations × 4 JTBD-derived cohorts and topic-aligned message bodies</name>
  <files>examples/cairnloop_example/priv/repo/seeds.exs</files>
  <read_first>
    - examples/cairnloop_example/priv/repo/seeds.exs (skeleton from plan 27-01; build_articles/0 implementation from plan 27-03; the get_or_insert!/3 helper)
    - lib/cairnloop/conversation.ex (sealed status enum + schema fields)
    - lib/cairnloop/message.ex (sealed role enum + validate_template_id_for_outbound/1)
    - test/support/fixtures.ex (lines 12–88: the direct-insert idiom; mimic this shape but NOT the test-helper module style — the seed uses get_or_insert! for idempotency)
    - .planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md D-03 (JTBD cohort mapping), D-18 (brand voice), D-19 (topic alignment to articles)
    - .planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md (Pitfall 3 host_user_id customer vs operator; Pitfall 5 no run_key column; Pitfall 6 :system_outbound template_id; FIX-01 row of phase_requirements)
    - .planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md (seeds.exs section — direct-insert pattern, brand voice rules)
    - prompts/cairnloop_brand_book.md (sections 5/5.5/7.5 — customer + agent message bodies)
    - Plan 27-02's summary OR plan 27-02 file itself for the verbatim 5 demo customer ids (cross-reference: "demo_user_acme_billing", "demo_user_globex_seats", "demo_user_initech_billing", "demo_user_umbrella_ci", "demo_user_hooli_tokens")
    - Plan 27-03's article handle map keys (api_key, billing_email, seat, ci_skipped, token_rotation) so each cohort cycles through the 5 article topics
  </read_first>
  <action>
Replace the # TODO stub body of build_conversations/1 inside CairnloopExample.SeedRun with:

1. **Build the 16-row plan as a module-attribute list** at the top of the module (above the function defs) for grep-ability and idempotency-test clarity. Use this exact structure:

       @demo_conversations [
         # cohort: :new  (status :open, no :agent reply; 4 rows)
         %{n: 1,  cohort: :new,  topic: :api_key,        host_user_id: "demo_user_acme_billing"},
         %{n: 2,  cohort: :new,  topic: :billing_email,  host_user_id: "demo_user_initech_billing"},
         %{n: 3,  cohort: :new,  topic: :seat,           host_user_id: "demo_user_globex_seats"},
         %{n: 4,  cohort: :new,  topic: :ci_skipped,     host_user_id: "demo_user_umbrella_ci"},
         # cohort: :open  (status :open, has :agent reply, last msg :user; 4 rows)
         %{n: 5,  cohort: :open, topic: :api_key,        host_user_id: "demo_user_hooli_tokens"},
         %{n: 6,  cohort: :open, topic: :billing_email,  host_user_id: "demo_user_acme_billing"},
         %{n: 7,  cohort: :open, topic: :token_rotation, host_user_id: "demo_user_hooli_tokens"},
         %{n: 8,  cohort: :open, topic: :ci_skipped,     host_user_id: "demo_user_umbrella_ci"},
         # cohort: :awaiting_customer  (status :open, has :agent reply, last msg :agent; 4 rows)
         %{n: 9,  cohort: :awaiting_customer, topic: :seat,           host_user_id: "demo_user_globex_seats"},
         %{n: 10, cohort: :awaiting_customer, topic: :billing_email,  host_user_id: "demo_user_initech_billing"},
         %{n: 11, cohort: :awaiting_customer, topic: :api_key,        host_user_id: "demo_user_acme_billing"},
         %{n: 12, cohort: :awaiting_customer, topic: :ci_skipped,     host_user_id: "demo_user_umbrella_ci"},
         # cohort: :resolved  (status :resolved, resolved_at set, last msg :agent or :system_outbound; 4 rows)
         %{n: 13, cohort: :resolved, topic: :api_key,        host_user_id: "demo_user_hooli_tokens", csat_rating: :positive, closer: :agent},
         %{n: 14, cohort: :resolved, topic: :token_rotation, host_user_id: "demo_user_hooli_tokens", csat_rating: :positive, closer: :agent},
         %{n: 15, cohort: :resolved, topic: :seat,           host_user_id: "demo_user_globex_seats", csat_rating: nil,       closer: :agent},
         %{n: 16, cohort: :resolved, topic: :billing_email,  host_user_id: "demo_user_initech_billing", csat_rating: :positive, closer: :system_outbound}
       ]

   Note: row 16 is the single :system_outbound closer; rows 13/14/15 close with :agent (Pitfall 6 + planner shift-left — avoids the template_id requirement on most resolved rows).

   Cross-check that every host_user_id value appears in plan 27-02's 5 known clauses — if any drift, the operator inbox will render `{:ok, %{}}` (DemoContextProvider's fail-open) for that conversation.

   Topic coverage (D-19): each of the 5 article handles (api_key, billing_email, seat, ci_skipped, token_rotation) appears ≥ 1 time across the 16 conversations. The mapping above gives api_key=3, billing_email=3, seat=3, ci_skipped=3, token_rotation=2 = 14; the 2 remaining slots are duplicates of high-volume topics. Acceptable for D-19.

2. **Iterate over @demo_conversations and seed each row idempotently:**

       Enum.map(@demo_conversations, fn row -> seed_conversation_row(row, articles) end)

   Define a private helper:

       defp seed_conversation_row(row, articles) do
         subject = "[demo-#{String.pad_leading(Integer.to_string(row.n), 2, "0")}] " <> topic_subject(row.topic)
         attrs = conversation_attrs(row, subject)

         case CairnloopExample.Repo.get_by(Cairnloop.Conversation, subject: subject) do
           nil ->
             conv = %Cairnloop.Conversation{} |> Cairnloop.Conversation.changeset(attrs) |> CairnloopExample.Repo.insert!()
             insert_messages_for_cohort(conv, row, articles)
             conv

           existing ->
             existing
         end
       end

3. **Define topic_subject/1** — returns a brand-voice-compliant subject string for each topic atom:
   - :api_key -> "Resetting my Trailmark API key"
   - :billing_email -> "Updating billing email after switching CFO"
   - :seat -> "Adding a teammate to our Trailmark account"
   - :ci_skipped -> "CI run skipped without explanation"
   - :token_rotation -> "Rotation reminder for expiring token"

4. **Define conversation_attrs/2** — returns the per-row Conversation attrs map. For `cohort: :resolved`, set `status: :resolved` AND `resolved_at: DateTime.add(DateTime.utc_now(), -row.n, :day)` (spaced over the past N days for visual distinction) AND `csat_rating: row.csat_rating`. For all other cohorts, set `status: :open` and leave `resolved_at`/`csat_rating` unset.

5. **Define insert_messages_for_cohort/3** — inserts 2–5 messages per conversation following the cohort's derivation rule. Apply this exact rule table:

       :new                 -> 2 messages: [{:user, opening}, {:user, followup_clarification}]
                               (D-03: status :open + 0 :agent messages — multiple :user messages still
                                satisfy the contract; the second message models a real "customer added
                                more detail while waiting for an agent" inbox state.)
       :open                -> 4 messages: [{:user, opening}, {:agent, first_reply}, {:user, followup}, {:user, second_followup}] (last must be :user)
       :awaiting_customer   -> 3 messages: [{:user, opening}, {:user, additional_detail}, {:agent, response}] (last must be :agent)
       :resolved            -> 5 messages: [{:user, opening}, {:agent, ack}, {:user, more_info}, {:agent, solution}, {row.closer, closing}]
                               If row.closer == :system_outbound, the 5th message has metadata: %{"template_id" => "demo_resolve_confirm"}; otherwise no metadata.

   Add 1 :internal_note message AFTER the first :agent reply in 2 of the 16 conversations (e.g. n=5 and n=13) — D-18 carve-out demonstrating the operator-only timeline surface. Internal-note bodies MAY reference typed terms (e.g. "Flagged billing_email change — Stripe metadata reviewed; no fraud signal.").

   For every Message insert, use the canonical idiom (PATTERNS.md divergence rule #4):

       %Cairnloop.Message{}
       |> Cairnloop.Message.changeset(Map.merge(%{role: :user, metadata: %{}}, attrs))
       |> CairnloopExample.Repo.insert!()

   Do NOT use Ecto.Changeset.change/2. Do NOT set :run_key (Pitfall 5: the example app's migration lacks the column).

   Brand voice (C-08, D-18): all customer + agent message bodies are calm, plain-English, no raw Elixir atoms, no raw JSON. Customer messages may be slightly informal (matching real support tickets). Agent messages strictly follow brand book §5.5 register. Each cohort's messages must logically reference the conversation's `topic` (D-19: e.g. an :api_key conversation's opening user message mentions "API key" or "reset"; the agent reply references the canonical KB article title — this is what makes cmd+k against the article title yield matching results even with zero-vector embeddings).

6. **Return value:** Return the full list of inserted/found %Conversation{} structs (Enum.map result above). Plan 27-05 (gaps) takes this list and links GapCandidateMembership rows to specific conversations; plan 27-06 (suggestion) does the same for ArticleSuggestionEvidence.

Constraints:
- 16 conversations exactly (top of the D-03 12–16 band; matches FIX-01 success criterion #1).
- All 5 demo customer ids from plan 27-02 used at least once (cross-verify against the list above).
- No `:run_key` set on any Message (Pitfall 5).
- :system_outbound message MUST carry metadata["template_id"] (Pitfall 6).
- Messages use Schema.changeset/2; no Ecto.Changeset.change/2 anywhere.
- No library code touched.
- Brand voice on every visible string.
- Total message count on fresh DB: 4*2 + 4*4 + 4*3 + 4*5 + 2 internal_notes = 8+16+12+20+2 = **58**. Plan 27-08's integration test pins this with a ≥ 48 lower bound and a ≤ 80 upper bound (loose enough to absorb future copy edits that add a clarifying line; tight enough to catch a runaway-loop regression).
  </action>
  <verify>
    <automated>cd examples/cairnloop_example &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tee /tmp/p27-04-compile.log; echo '---'; echo 'cohort-tagged-rows:'; grep -E 'cohort: :(new|open|awaiting_customer|resolved)' examples/cairnloop_example/priv/repo/seeds.exs | wc -l; echo 'demo-NN-prefix-references:'; grep -cE '\[demo-' examples/cairnloop_example/priv/repo/seeds.exs; echo 'ecto-changeset-change-anywhere (should be 0):'; grep -c 'Ecto.Changeset.change' examples/cairnloop_example/priv/repo/seeds.exs; echo 'run_key-sets-on-message (should be 0):'; grep -cE 'run_key:\s*"' examples/cairnloop_example/priv/repo/seeds.exs; echo 'system_outbound-template_id-pairing:'; awk '/role: :system_outbound/,/}/' examples/cairnloop_example/priv/repo/seeds.exs | grep -c 'template_id'; echo 'demo-customer-ids-referenced:'; grep -oE 'demo_user_[a-z_]+' examples/cairnloop_example/priv/repo/seeds.exs | sort -u</automated>
    <human-check>Verify the @demo_conversations list contains exactly 16 entries (count rows in the list); confirm each cohort has 4 entries; confirm the closer: :system_outbound row also passes a template_id in its metadata when the message is inserted; spot-check 4 message bodies (one per cohort) for brand voice.</human-check>
  </verify>
  <acceptance_criteria>
- cd examples/cairnloop_example && mix compile --warnings-as-errors exits 0.
- grep -cE 'cohort: :(new|open|awaiting_customer|resolved)' on the file returns exactly 16 (the @demo_conversations list).
- grep -cE 'n: ([0-9]+)' inside the @demo_conversations list returns exactly 16 distinct entries n=1..16 (use `grep -oE 'n: [0-9]+' | sort -u | wc -l == 16`).
- grep -c 'Ecto.Changeset.change' on the file returns 0 (no anti-pattern survived).
- grep -cE 'run_key:\s*"' returns 0 (Pitfall 5).
- Every :system_outbound role insert is paired (within the same map literal) with a metadata key containing "template_id" (Pitfall 6) — verify by reading the seed_resolved branch.
- grep -oE 'demo_user_[a-z_]+' | sort -u returns exactly the 5 ids from plan 27-02: demo_user_acme_billing, demo_user_globex_seats, demo_user_hooli_tokens, demo_user_initech_billing, demo_user_umbrella_ci.
- Each of the 5 topic atoms (api_key, billing_email, seat, ci_skipped, token_rotation) appears ≥ 1 time in the @demo_conversations list (verify via grep on topic: :api_key etc.).
- End-to-end row-count assertions (16 conversations, 48 ≤ messages ≤ 80, 4 per cohort) ship in plan 27-08's integration test.
  </acceptance_criteria>
  <done>
All acceptance_criteria conditions hold; build_conversations/1 produces the 16 conversations + messages on fresh DB and is idempotent on re-run; returns a list of %Conversation{} structs for downstream plans.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Conversation.host_user_id (customer identity) → ContextProvider | The demo provider's pattern-matched clauses must match these ids verbatim or the operator inbox renders {:ok, %{}} for that conversation |
| Message.role :system_outbound → outbound delivery contract | Pitfall 6 — without metadata.template_id the changeset rejects insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-09 | Tampering | host_user_id overload (customer vs operator id confusion — Pitfall 3) | mitigate | This plan exclusively sets Conversation.host_user_id to CUSTOMER identities (the 5 "demo_user_*" strings). Operator-scope identities ("demo_operator") belong on GapCandidate/ArticleSuggestion/ReviewTask in plans 27-05 and 27-06 only. Acceptance criterion pins the grep-derived set to exactly 5 demo_user_* values. |
| T-27-10 | Repudiation | non-idempotent message creation producing duplicate messages on re-run | mitigate | The get_or_insert! short-circuit at the Conversation level (matching on subject) skips the entire message-insert block when the conversation already exists. Messages have no clean natural key; idempotency rides on the conversation's [demo-NN] subject prefix being unique. |
| T-27-11 | Information Disclosure | demo customer PII leaking into production | accept | All names are pseudonymous, all emails use .example TLD (RFC 2606), no real payment data or addresses. Plan 27-01 documents this in the seeds.exs header. |
| T-27-12 | Tampering | :system_outbound message without template_id (Pitfall 6) | mitigate | The single :system_outbound message (row 16) carries metadata: %{"template_id" => "demo_resolve_confirm"}. Schema-level validation (validate_template_id_for_outbound/1) would catch a regression; the changeset path runs it. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | No new packages. |
</threat_model>

<verification>
After this task:
1. Grep gates in <verify> all pass.
2. Compile is warnings-clean.
3. End-to-end row counts (Repo.aggregate(Conversation, :count) >= 16, Repo.aggregate(Message, :count) >= 48 and <= 80) are pinned by plan 27-08's integration test once seed-test infra ships.
</verification>

<success_criteria>
- 16 conversations × 4 JTBD cohorts, with realistic 2–5 message threads per conversation, all keyed to the 5 demo customer ids the DemoContextProvider knows.
- D-19 article topic coverage: every article (api_key, billing_email, seat, ci_skipped, token_rotation) is the topic of ≥ 1 conversation.
- Idempotent on re-run (subject-prefix natural key).
- :system_outbound usage demonstrates the outbound-bubble visual in 1 of 4 resolved conversations.
- 2 conversations include an :internal_note message (D-18 carve-out demo).
- No library code modified.
</success_criteria>

<output>
Create .planning/phases/27-realistic-demo-fixtures/27-04-SUMMARY.md when done — record:
- Confirmed 16 entries in @demo_conversations (with the verbatim n + cohort + topic + host_user_id grid).
- Confirmation that the 5 demo customer ids match plan 27-02 exactly.
- The single :system_outbound row index (n=16) and the template_id it carries.
- The 2 conversation indices that include an :internal_note message.
- The total message count produced on a fresh DB (calculate from cohort message-count rules: 4*2 + 4*4 + 4*3 + 4*5 = 8+16+12+20 = 56, plus 2 :internal_note additions = 58).
</output>
</content>
</invoke>