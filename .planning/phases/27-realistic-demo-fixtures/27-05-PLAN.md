---
phase: 27-realistic-demo-fixtures
plan: 05
type: execute
wave: 4
depends_on:
  - 27-04
files_modified:
  - examples/cairnloop_example/priv/repo/seeds.exs
autonomous: true
requirements:
  - FIX-03
tags:
  - seeds
  - elixir
  - knowledge-automation
  - gap-candidates
  - retrieval
must_haves:
  truths:
    - "build_gaps/1 inserts ≥ 3 Cairnloop.KnowledgeAutomation.GapCandidate rows (D-13/D-14) via direct Schema.changeset/2 + Repo.insert! (CandidateBuilder is NOT invoked)"
    - "each GapCandidate has status: :open, candidate_type: :mixed, score in 0.4..0.8, evidence_count 2..4, manual_case_count and weak_grounding_count non-zero, first_seen_at and last_seen_at distributed across the past 14 days (D-14)"
    - "every GapCandidate.host_user_id is 'demo_operator' (operator-scope per Pitfall 3) — NOT the customer-identifying demo_user_* values"
    - "each GapCandidate has ≥ 1 GapCandidateMembership row with source_type: :retrieval_gap_event and source_id pointing at a seeded Cairnloop.Retrieval.GapEvent row (RESEARCH Open Question 1 recommendation accepted: seed 1 real GapEvent per GapCandidate so the gap-queue detail view renders evidence rather than the empty-state copy)"
    - "build_gaps/1 is idempotent via get_or_insert!/3 keyed on :stable_key for GapCandidate; GapCandidateMembership has a unique constraint on (gap_candidate_id, source_type, source_id) so re-inserts are no-ops"
    - "build_gaps/1 returns a list of inserted %GapCandidate{} rows so plan 27-06 can reference at least one as the ArticleSuggestion's entrypoint_id"
  artifacts:
    - path: "examples/cairnloop_example/priv/repo/seeds.exs"
      provides: "Implemented build_gaps/1 producing ≥ 3 GapCandidate rows + membership rows + RetrievalGapEvent rows"
      contains: "build_gaps"
  key_links:
    - from: "GapCandidate"
      to: "KnowledgeBaseLive.Gaps (LiveView at /support/knowledge-base/gaps)"
      via: "KnowledgeAutomation.list_gap_candidates/1 (filter status: :open, scope host_user_id: 'demo_operator', order by score DESC)"
      pattern: "host_user_id: \"demo_operator\""
    - from: "GapCandidateMembership.source_id"
      to: "Cairnloop.Retrieval.GapEvent.id"
      via: "hydrate_memberships/1 in KnowledgeAutomation (lib/cairnloop/knowledge_automation.ex:2169)"
      pattern: "source_type: :retrieval_gap_event"
---

<objective>
Implement build_gaps/1 in seeds.exs: insert ≥ 3 GapCandidate rows + companion GapCandidateMembership rows + 1 RetrievalGapEvent per gap, via direct Schema.changeset + Repo.insert! (D-13: CandidateBuilder is M010 substrate — Phase 31's smoke exercises it; Phase 27 stays out of M010's path). The gap queue at /support/knowledge-base/gaps then renders ranked inspectable maintenance work on first boot.

Purpose: FIX-03's "≥ 3 GapCandidate rows with evidence linked to seeded conversations; gap queue shows ranked inspectable maintenance work" criterion.

Output: A working build_gaps/1 body producing 3 GapCandidates + 3 RetrievalGapEvents + 3+ memberships on fresh DB, idempotent on re-run, returning the list of %GapCandidate{} structs for plan 27-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md
@.planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md
@.planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md
@CLAUDE.md
@lib/cairnloop/knowledge_automation/gap_candidate.ex
@lib/cairnloop/knowledge_automation/gap_candidate_membership.ex
@lib/cairnloop/retrieval/gap_event.ex
@examples/cairnloop_example/priv/repo/seeds.exs

<interfaces>
<!-- Cairnloop.KnowledgeAutomation.GapCandidate (verified lib/cairnloop/knowledge_automation/gap_candidate.ex):
       status enum         :: :open | :accepted | :dismissed                (default :open)
       candidate_type enum :: :no_hit | :weak_grounding | :manual_handling | :mixed   (default :mixed)
       tenant_scope enum   :: :host_user_scoped | :public_only | :system_unscoped
       ui_surface enum     :: :conversation | :inbox | :settings | :unspecified
       required fields     :: stable_key, status, candidate_type, title, seed_excerpt,
                              tenant_scope, ui_surface, first_seen_at, last_seen_at
       validation          :: stable_key length 8..128; counts >= 0; host_user_scoped requires host_user_id

     Cairnloop.KnowledgeAutomation.GapCandidateMembership (verified):
       source_type enum :: :retrieval_gap_event | :manual_handling_case
       required         :: source_type, source_id
       validation       :: source_id > 0; unique on (gap_candidate_id, source_type, source_id)

     Cairnloop.Retrieval.GapEvent (verified lib/cairnloop/retrieval/gap_event.ex):
       surface enum       :: :draft_generation | :search_modal | :api | :unspecified
       outcome_class enum :: :empty_recall | :retrieval_error | :weak_grounding | :policy_limit
       reason enum        :: :canonical_results | :mixed_results | :assistive_only_results |
                              :no_canonical_results | :canonical_insufficient_detail |
                              :clarification_limit_reached | :provider_timeout |
                              :index_unavailable | :unexpected_error
       required fields    :: occurred_at, surface, outcome_class, reason, tenant_scope,
                              ui_surface, query_fingerprint, sanitized_query_excerpt
       validation         :: query_fingerprint length == 64 (sha256 hex), excerpt <= 160 chars,
                              counts >= 0, snapshots <= 5

     Scope rule (Pitfall 3 — DO NOT mix up):
       - GapCandidate.host_user_id   :: "demo_operator"           (operator-scope; matches router.ex:20 session)
       - RetrievalGapEvent.host_user_id :: "demo_operator"        (same scope; this is the operator's view)
       - GapCandidateMembership has no host_user_id field         (scope comes via gap_candidate_id FK)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement build_gaps/1 with 3 GapCandidates + RetrievalGapEvents + memberships, all operator-scoped</name>
  <files>examples/cairnloop_example/priv/repo/seeds.exs</files>
  <read_first>
    - examples/cairnloop_example/priv/repo/seeds.exs (current state after plans 27-01, 27-03, 27-04)
    - lib/cairnloop/knowledge_automation/gap_candidate.ex (sealed enum lists; validate_host_scope rule)
    - lib/cairnloop/knowledge_automation/gap_candidate_membership.ex (sealed source_type enum; unique constraint)
    - lib/cairnloop/retrieval/gap_event.ex (sealed enums; required fields; 64-char query_fingerprint requirement)
    - .planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md D-13 (no CandidateBuilder), D-14 (per-row attrs)
    - .planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md (Pitfall 3 host_user_id scoping; Open Question 1 — seed real GapEvent rows; Code Examples for GapCandidate insert)
    - .planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md (seeds.exs section — direct-insert idiom)
  </read_first>
  <action>
Replace the # TODO stub body of build_gaps/1 with:

1. **Define the 3 demo gaps as a module attribute** (parallel to plan 27-04's @demo_conversations):

       @demo_gaps [
         %{
           stable_key:          "demo_gap_billing_export",
           title:                "Exporting Trailmark billing receipts",
           seed_excerpt:         "Adopters repeatedly ask how to download multi-month billing receipts; no canonical KB article exists yet.",
           sanitized_query:      "export billing receipts past months",
           ui_surface:           :conversation,
           first_seen_offset_d:  -14,
           last_seen_offset_d:   -2,
           evidence_count:       3,
           manual_case_count:    2,
           weak_grounding_count: 1,
           no_hit_count:         0,
           score:                0.65,
           score_components:     %{"manual_handling" => 0.4, "weak_grounding" => 0.15, "freshness_boost" => 0.10}
         },
         %{
           stable_key:          "demo_gap_ci_skip_diagnostics",
           title:                "Diagnosing why a CI run was skipped",
           seed_excerpt:         "CI-skip diagnostics surface intermittently; current article gives causes but not a step-by-step debug flow.",
           sanitized_query:      "ci run skipped not triggered",
           ui_surface:           :conversation,
           first_seen_offset_d:  -10,
           last_seen_offset_d:   -1,
           evidence_count:       4,
           manual_case_count:    1,
           weak_grounding_count: 2,
           no_hit_count:         1,
           score:                0.55,
           score_components:     %{"manual_handling" => 0.2, "weak_grounding" => 0.3, "no_hit" => 0.05}
         },
         %{
           stable_key:          "demo_gap_team_seat_governance",
           title:                "Clarifying the seat-invite governed-action flow",
           seed_excerpt:         "Operators need clearer guidance on when seat-invite proposals require approval vs auto-apply.",
           sanitized_query:      "add team seat governed approval",
           ui_surface:           :inbox,
           first_seen_offset_d:  -7,
           last_seen_offset_d:   -3,
           evidence_count:       2,
           manual_case_count:    2,
           weak_grounding_count: 1,
           no_hit_count:         0,
           score:                0.45,
           score_components:     %{"manual_handling" => 0.4, "weak_grounding" => 0.05}
         }
       ]

   Constraints these specs satisfy (D-14):
   - score in 0.4..0.8 ✓ (0.65, 0.55, 0.45)
   - evidence_count in 2..4 ✓
   - manual_case_count and weak_grounding_count both non-zero across all 3 ✓
   - first_seen_at and last_seen_at offsets distributed across the past 14 days ✓

2. **Iterate and seed each gap idempotently:**

       Enum.map(@demo_gaps, fn spec -> seed_gap_with_evidence(spec) end)

   Define a private helper `seed_gap_with_evidence/1`:

       defp seed_gap_with_evidence(spec) do
         now = DateTime.utc_now()

         gap_attrs = %{
           stable_key:    spec.stable_key,
           status:        :open,
           candidate_type: :mixed,
           title:         spec.title,
           seed_excerpt:  spec.seed_excerpt,
           tenant_scope:  :host_user_scoped,
           host_user_id:  "demo_operator",           # operator-scope (Pitfall 3)
           ui_surface:    spec.ui_surface,
           first_seen_at: DateTime.add(now, spec.first_seen_offset_d, :day),
           last_seen_at:  DateTime.add(now, spec.last_seen_offset_d, :day),
           evidence_count: spec.evidence_count,
           manual_case_count: spec.manual_case_count,
           weak_grounding_count: spec.weak_grounding_count,
           no_hit_count:  spec.no_hit_count,
           score:         spec.score,
           score_components: spec.score_components
         }

         gap = get_or_insert!(Cairnloop.KnowledgeAutomation.GapCandidate, :stable_key, gap_attrs)

         # Seed exactly 1 RetrievalGapEvent per gap (RESEARCH Open Question 1 recommendation),
         # then 1 membership linking the gap to that event.
         event = get_or_insert_gap_event!(spec, now)
         _membership = upsert_membership!(gap, event)

         gap
       end

   Define `get_or_insert_gap_event!/2` — there is no schema-level natural key on GapEvent, so use query_fingerprint (a sha256 hex of the spec.stable_key is deterministic and 64 chars):

       defp get_or_insert_gap_event!(spec, now) do
         fingerprint = :crypto.hash(:sha256, "demo:gap_event:" <> spec.stable_key) |> Base.encode16(case: :lower)
         attrs = %{
           occurred_at:             DateTime.add(now, spec.last_seen_offset_d, :day),
           surface:                 :search_modal,
           outcome_class:           :empty_recall,
           reason:                  :no_canonical_results,
           host_user_id:            "demo_operator",
           tenant_scope:            :host_user_scoped,
           ui_surface:              spec.ui_surface,
           query_fingerprint:       fingerprint,
           sanitized_query_excerpt: spec.sanitized_query,
           canonical_hit_count:     0,
           assistive_hit_count:     0,
           clarification_attempts:  0
         }

         case CairnloopExample.Repo.get_by(Cairnloop.Retrieval.GapEvent, query_fingerprint: fingerprint) do
           nil ->
             %Cairnloop.Retrieval.GapEvent{}
             |> Cairnloop.Retrieval.GapEvent.changeset(attrs)
             |> CairnloopExample.Repo.insert!()

           existing ->
             existing
         end
       end

   Define `upsert_membership!/2` — the schema has a unique constraint on (gap_candidate_id, source_type, source_id), so a plain insert is safe-or-conflict; we use `Repo.get_by` first for explicit no-op behavior:

       defp upsert_membership!(gap, event) do
         attrs = %{gap_candidate_id: gap.id, source_type: :retrieval_gap_event, source_id: event.id}

         case CairnloopExample.Repo.get_by(Cairnloop.KnowledgeAutomation.GapCandidateMembership,
                gap_candidate_id: gap.id, source_type: :retrieval_gap_event, source_id: event.id) do
           nil ->
             %Cairnloop.KnowledgeAutomation.GapCandidateMembership{}
             |> Cairnloop.KnowledgeAutomation.GapCandidateMembership.changeset(attrs)
             |> CairnloopExample.Repo.insert!()

           existing ->
             existing
         end
       end

3. **Return value:** Enum.map result — a list of 3 %GapCandidate{} structs. Plan 27-06's build_suggestion/2 wires the seeded ArticleSuggestion's entrypoint_type: :gap_candidate + entrypoint_id to the first gap in this list (typically `demo_gap_billing_export` — the title aligns with the FIX-04 demo suggestion content).

Constraints:
- All 3 GapCandidate.host_user_id values are "demo_operator" (Pitfall 3 — operator-scope; otherwise the gap queue scope filter hides them).
- All 3 RetrievalGapEvent.host_user_id values are "demo_operator" (same scope).
- candidate_type is :mixed on all 3 (D-14).
- score is in 0.4..0.8 (D-14).
- query_fingerprint is exactly 64 hex chars (sha256 hex; the helper above guarantees this).
- sanitized_query_excerpt is ≤ 160 chars (the demo strings above are well under).
- No CandidateBuilder invocation, no Workers.* invocation (D-13).
- Conversation cross-reference: this plan does NOT need to map gaps to specific conversations from plan 27-04's return value — the connection is rendered via the LiveView's hydrate_memberships path, which fetches the GapEvent's sanitized_query_excerpt for display. The `conversations` argument to build_gaps/1 is currently UNUSED at this granularity; underscore-prefix it (`_conversations`) to silence the unused-variable warning. (A future plan can deepen the linkage via GapEventSnapshot rows referencing conversation excerpts; that is OUT OF SCOPE for FIX-03.)
- No library code touched.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tee /tmp/p27-05-compile.log; echo '---'; echo 'gap-stable-keys-defined:'; grep -cE 'stable_key:\s*"demo_gap_' examples/cairnloop_example/priv/repo/seeds.exs; echo 'host_user_id-demo_operator-in-gaps-context (should be >= 6: 3 gaps + 3 events):'; awk '/defp seed_gap_with_evidence|defp get_or_insert_gap_event/,/^  end$/' examples/cairnloop_example/priv/repo/seeds.exs | grep -c 'host_user_id:\s*"demo_operator"'; echo 'CandidateBuilder-invocations (should be 0):'; grep -c 'CandidateBuilder' examples/cairnloop_example/priv/repo/seeds.exs; echo 'source_type-retrieval_gap_event-references:'; grep -c ':retrieval_gap_event' examples/cairnloop_example/priv/repo/seeds.exs; echo 'unused-arg-underscored:'; grep -cE 'defp build_gaps\(_conversations\)' examples/cairnloop_example/priv/repo/seeds.exs</automated>
    <human-check>Confirm: each of the 3 gaps has a unique stable_key starting with demo_gap_; each gap's first_seen_at offset is older (more negative) than its last_seen_at offset; the build_gaps function signature still matches plan 27-01's skeleton (defp build_gaps/1).</human-check>
  </verify>
  <acceptance_criteria>
- cd examples/cairnloop_example && mix compile --warnings-as-errors exits 0.
- grep -cE 'stable_key:\s*"demo_gap_' returns exactly 3 (the 3 gap rows in @demo_gaps).
- grep -c 'CandidateBuilder' returns 0 (D-13 — no M010 invocation from seeds).
- grep -c 'Workers\.GenerateArticleSuggestion' returns 0 (kept clean here; plan 27-06's gate).
- The 3 unique stable_keys are exactly: demo_gap_billing_export, demo_gap_ci_skip_diagnostics, demo_gap_team_seat_governance.
- The build_gaps/1 signature uses an underscore-prefixed _conversations arg (no unused-variable warning).
- End-to-end FIX-03 assertions (Repo.aggregate(GapCandidate, :count) >= 3 with status: :open; each gap has ≥ 1 membership row) ship in plan 27-08's integration test.
  </acceptance_criteria>
  <done>
All acceptance_criteria conditions hold; build_gaps/1 produces 3 GapCandidate + 3 RetrievalGapEvent + 3 membership rows on fresh DB, is idempotent on re-run, and returns the gap list for plan 27-06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → KnowledgeAutomation gap-queue read path | Operator-scope filtering in apply_scope/2 means seeded host_user_id must be "demo_operator" or rows are invisible (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-13 | Information Disclosure | gap data scoped to the wrong tenant (operator vs customer identity confusion) | mitigate | All GapCandidate + RetrievalGapEvent host_user_id values are hard-coded to "demo_operator". Acceptance criterion gate counts these explicitly. |
| T-27-14 | Tampering | invalid query_fingerprint length (must be 64 hex per GapEvent.changeset) | mitigate | The helper computes :crypto.hash(:sha256, …) |> Base.encode16(case: :lower) which is guaranteed 64 hex chars. |
| T-27-15 | Tampering | duplicate membership rows on re-run (would violate unique constraint and raise) | mitigate | upsert_membership!/2 calls Repo.get_by first and only inserts when nil; the unique constraint would catch a regression. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | No new packages. |
</threat_model>

<verification>
After this task:
1. Grep gates pass; compile is warnings-clean.
2. The 3 gap stable_keys are demo-namespaced (demo_gap_*) so they cannot collide with any future production rows from M010's CandidateBuilder.
3. End-to-end Repo aggregates (>= 3 :open GapCandidates with >= 1 membership each) ship in plan 27-08's integration test.
</verification>

<success_criteria>
- 3 GapCandidate + 3 RetrievalGapEvent + 3 GapCandidateMembership rows seeded with operator-scope host_user_id values.
- The gap queue LiveView at /support/knowledge-base/gaps renders 3 inspectable rows ordered by score DESC; clicking into a detail view shows the seeded RetrievalGapEvent's sanitized_query_excerpt.
- No CandidateBuilder, no Workers.GenerateArticleSuggestion invocation (D-13 respected).
- No library code modified.
- Idempotent on re-run.
</success_criteria>

<output>
Create .planning/phases/27-realistic-demo-fixtures/27-05-SUMMARY.md when done — record:
- The 3 confirmed stable_keys and per-row score/counts.
- Confirmation that all host_user_id values in this builder are "demo_operator".
- Which gap is intended as plan 27-06's entrypoint_id target (recommendation: demo_gap_billing_export — its title aligns with the FIX-04 demo suggestion "Exporting Trailmark billing receipts").
- Any deviations from this plan.
</output>
