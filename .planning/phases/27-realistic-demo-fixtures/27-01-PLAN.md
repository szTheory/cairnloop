---
phase: 27-realistic-demo-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/priv/repo/seeds.exs
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
  - FIX-04
tags:
  - seeds
  - elixir
  - phoenix
  - fixtures
  - idempotency
must_haves:
  truths:
    - "seeds.exs body is replaced with a skeleton organized into D-01 builder functions and a one-shot Oban drain at the end"
    - "the header comment in seeds.exs documents the full spec-language → schema-enum reconciliation table and the idempotency / OPENAI_API_KEY / Oban-drain contracts"
    - "an idempotency helper `get_or_insert!/3` exists in the script and uses `Repo.get_by` natural-key lookups (no on_conflict magic, no Ecto.Changeset.change/2)"
    - "running `cd examples/cairnloop_example && mix compile --warnings-as-errors` exits 0 with no warnings (the body of each builder is permitted to be a stub at this stage)"
  artifacts:
    - path: "examples/cairnloop_example/priv/repo/seeds.exs"
      provides: "Skeleton seed script with builder shells, header comments, idempotency helper, and Oban drain wiring (no real data yet)"
      contains: "build_articles"
    - path: "examples/cairnloop_example/priv/repo/seeds.exs"
      provides: "Sealed-enum reconciliation table in header comment"
      contains: "ready_for_review"
  key_links:
    - from: "examples/cairnloop_example/priv/repo/seeds.exs"
      to: "Cairnloop.KnowledgeBase facade"
      via: "comment header documents D-09 facade rule"
      pattern: "KnowledgeBase.publish_revision"
    - from: "examples/cairnloop_example/priv/repo/seeds.exs"
      to: "Oban"
      via: "drain_embedding_pipeline/0 calls Oban.drain_queue(queue: :default, with_recursion: true)"
      pattern: "Oban.drain_queue"
---

<objective>
Rewrite `examples/cairnloop_example/priv/repo/seeds.exs` from the current 49-LOC `Ecto.Changeset.change/2` script into the D-01 builder-function skeleton with documented header comments, an idempotency helper, and the Oban drain wiring — but with empty / stub builder bodies. This plan establishes the structural shell so plans 03–07 can each fill in one builder without re-litigating the script's overall shape.

Purpose: Lock in the load-bearing structural decisions (D-01 builder names, D-02 idempotency idiom via `Repo.get_by`, D-05/A1 sealed-enum mapping table, D-08 Oban drain, D-21 warnings-clean) in one focused diff so the remaining builder plans can be authored as additions, not arguments.

Output: A skeleton `seeds.exs` that compiles warnings-clean, with every architecturally load-bearing decision visible in the file header. Builder function bodies are stubs (`# TODO: filled by plan 03` etc.) but the function signatures, the `get_or_insert!/3` helper, and the drain call are real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md
@.planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md
@.planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md
@.planning/phases/27-realistic-demo-fixtures/27-VALIDATION.md
@CLAUDE.md
@examples/cairnloop_example/priv/repo/seeds.exs

<interfaces>
<!-- Sealed-enum reconciliation table — copy verbatim into the seeds.exs header comment.
     Spec language used in roadmap / CONTEXT.md / REQUIREMENTS.md (left column) maps to the
     actual schema enum values (right column). The planner MUST surface this table to the
     executor because failing to translate produces silent Ecto.Changeset cast errors. -->

| Spec language (roadmap / CONTEXT) | Actual schema enum / derived state | Schema location |
|-----------------------------------|------------------------------------|-----------------|
| `:new` (Conversation JTBD)        | derived: `status: :open` + 0 `:agent` messages | `lib/cairnloop/conversation.ex:6` |
| `:awaiting_customer` (Conversation JTBD) | derived: `status: :open` + last msg `role: :agent` | `lib/cairnloop/conversation.ex:6` |
| `:deprecated` (Revision)          | `state: :archived` | `lib/cairnloop/knowledge_base/revision.ex` |
| `:ready_for_review` (ArticleSuggestion) | `status: :ready` | `lib/cairnloop/knowledge_automation/article_suggestion.ex:7` |
| `:new_article` (ArticleSuggestion suggestion_type) | `suggestion_type: :article` | `lib/cairnloop/knowledge_automation/article_suggestion.ex:8` |

<!-- Cairnloop.Conversation sealed status enum:
       status :: :open | :resolved | :archived
     Cairnloop.Message sealed role enum:
       role :: :user | :agent | :system | :internal_note | :system_outbound
     KB.Article sealed status enum:
       status :: :draft | :published | :archived
     KB.Revision sealed state enum:
       state :: :draft | :published | :archived
     KnowledgeAutomation.ArticleSuggestion sealed enums:
       status :: :pending_generation | :ready | :failed | :dismissed
       suggestion_type :: :article | :revision

     Idempotency helper signature (this plan creates it):
       defp get_or_insert!(schema_module, natural_key_field, attrs) :: %{__struct__: schema_module}

     Oban drain call shape (verified deps/oban/lib/oban.ex:920-928):
       %{success: success, failure: failure, snoozed: _, cancelled: _, discard: _} =
         Oban.drain_queue(queue: :default, with_recursion: true)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace seeds.exs body with builder-skeleton + idempotency helper + Oban drain wiring</name>
  <files>examples/cairnloop_example/priv/repo/seeds.exs</files>
  <read_first>
    - examples/cairnloop_example/priv/repo/seeds.exs (the 49-LOC body being replaced wholesale)
    - .planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md (D-01 builder names, D-02 idempotency rule, D-05 deprecated→archived mapping, D-08 drain, D-21 warnings-clean)
    - .planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md §`seeds.exs` (composite analog: idempotency, facade, Oban drain) + §Sealed-enum reconciliation table
    - .planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md §Pitfall 5 (no `run_key` column in example app migrations), §Pitfall 6 (`:system_outbound` requires `metadata.template_id`), §Pitfall 7 (Oban testing: :manual), §Pitfall 8 (mix run starts app — keep default, do not use --no-start)
    - lib/cairnloop/conversation.ex (sealed status enum)
    - lib/cairnloop/message.ex (sealed role enum + validate_template_id_for_outbound)
    - lib/cairnloop/knowledge_base/article.ex (sealed status enum)
    - lib/cairnloop/knowledge_base/revision.ex (sealed state enum)
    - lib/cairnloop/knowledge_automation/article_suggestion.ex (sealed status + suggestion_type enums — verify lines 7–9)
  </read_first>
  <action>
Replace the entire body of `examples/cairnloop_example/priv/repo/seeds.exs` with a skeleton that contains, in order:

1. A top-of-file `@moduledoc`-style block comment (using `#`-prefixed lines since `.exs` scripts have no module) documenting:
   - **Idempotency contract (D-02):** "Re-running this script against an already-seeded DB is a no-op. Every insert is guarded by `Repo.get_by` on a natural key. No `on_conflict` magic."
   - **OPENAI_API_KEY note (D-06):** "Set `OPENAI_API_KEY` before `mix setup` for semantically ranked search; otherwise zero-vector embeddings are written via the existing dev-safety fallback in `Cairnloop.Embedder.ExternalApi`."
   - **End-of-script Oban drain (D-08):** "After all builders run, `Oban.drain_queue(queue: :default, with_recursion: true)` synchronously executes every enqueued `ChunkRevision` job so the M008 substrate self-test completes before this script exits."
   - **Facade rule (D-09):** "Articles + revisions go ONLY through `Cairnloop.KnowledgeBase.create_article/1`, `save_draft/2`, `publish_revision/1`. Bypassing the facade with a direct `%Revision{}` insert skips the Multi that enqueues `ChunkRevision` and silently breaks FIX-02."
   - **Sealed-enum reconciliation table (D-04, D-05, A1):** include the full 5-row table verbatim from the `<interfaces>` block in this plan's `<context>` section. Header text: "Spec language in CONTEXT.md / roadmap maps to actual schema enum values as follows — do NOT propose schema migrations to add the spec-language values; they are sealed-contract forbidden in vM014."
   - **Concurrency caveat (Pitfall 8):** "`mix run` starts the Application by default (Oban supervisor up — required by the drain). Do not run this script while `mix phx.server` holds port 4000."
   - **Schema migration caveat (Pitfall 5):** "Never set `Cairnloop.Message.run_key` to a non-nil value — the example app's migration does not add the column."

2. An `alias` block aliasing `CairnloopExample.Repo` and the library modules the builders will need: `Cairnloop.Conversation`, `Cairnloop.Message`, `Cairnloop.KnowledgeBase`, `Cairnloop.KnowledgeBase.Article`, `Cairnloop.KnowledgeBase.Revision`, `Cairnloop.KnowledgeAutomation`, `Cairnloop.KnowledgeAutomation.ArticleSuggestion`, `Cairnloop.KnowledgeAutomation.ArticleSuggestionEvidence`, `Cairnloop.KnowledgeAutomation.GapCandidate`, `Cairnloop.KnowledgeAutomation.GapCandidateMembership`, `Cairnloop.Retrieval.GapEvent`.

3. A private idempotency helper `get_or_insert!/3` with signature `defp get_or_insert!(schema_module, natural_key_field, attrs)`. It does `Repo.get_by(schema_module, [{natural_key_field, Map.fetch!(attrs, natural_key_field)}])`; on `nil` it builds `struct(schema_module) |> schema_module.changeset(attrs) |> Repo.insert!()`; on a row it returns the existing row. Use `Map.fetch!/2` (not `Map.get/2`) so a missing natural-key field raises clearly.

4. The five builder function shells matching D-01 exactly (use `defp` since they're private to the script):
   - `defp build_articles() do … end` — body `# TODO: filled by 27-03-PLAN.md (FIX-02 articles + revisions). Returns %{api_key: %Article{}, billing_email: …, seat: …, ci_skipped: …, token_rotation: …} for downstream builders.`. Stub return value: `%{}`.
   - `defp build_conversations(articles) do … end` — body `# TODO: filled by 27-04-PLAN.md (FIX-01 16 conversations × 4 JTBD cohorts). Returns a list of inserted %Conversation{} rows so downstream builders can reference them.`. Stub return value: `[]`.
   - `defp build_gaps(conversations) do … end` — body `# TODO: filled by 27-05-PLAN.md (FIX-03 ≥3 GapCandidates + memberships + RetrievalGapEvent). Returns list of inserted %GapCandidate{} rows.`. Stub return value: `[]`.
   - `defp build_suggestion(articles, conversations) do … end` — body `# TODO: filled by 27-06-PLAN.md (FIX-04 1 ArticleSuggestion :ready + evidence + ReviewTask). Returns {%ArticleSuggestion{}, %ReviewTask{}}.`. Stub return value: `{nil, nil}` (the script tolerates nil at this stage; the real plan returns real structs).
   - `defp drain_embedding_pipeline() do … end` — REAL body even at skeleton stage: pattern-match the result of `Oban.drain_queue(queue: :default, with_recursion: true)` into `%{success: success, failure: failure, snoozed: _, cancelled: _, discard: _}`; if `failure > 0` call `IO.warn("Seed embedding pipeline drained with #{failure} failures. Inspect oban_jobs.errors for details.")`; emit `IO.puts("Drained #{success} embedding jobs.")` on the happy path.

5. The script's main flow at the bottom of the file (after all `defp` definitions — but note `.exs` allows top-level expressions interleaved with `defp`-via-anonymous-module-only; use the pattern where the entire script body is wrapped in `defmodule CairnloopExample.SeedRun do … end` followed by `CairnloopExample.SeedRun.run()` at the very bottom — D-01 says "anonymous module-style flow"). The `run/0` public function calls in order: `articles = build_articles()`, `conversations = build_conversations(articles)`, `_gaps = build_gaps(conversations)`, `{_sugg, _task} = build_suggestion(articles, conversations)`, `drain_embedding_pipeline()`. Wrap the module definition in a one-line comment: `# Wrapped in a module so private helpers can be defp'd; not added to lib/ — this remains a script.`

Naming requirements:
- The module name MUST be `CairnloopExample.SeedRun` (not `CairnloopExample.Seeds` — avoid collision with future Seeds.* modules in vM015+).
- Builder names MUST match D-01 verbatim: `build_articles/0`, `build_conversations/1`, `build_gaps/1`, `build_suggestion/2`, `drain_embedding_pipeline/0`. Do NOT rename `build_suggestion` to `build_suggestion_with_review_task` (RESEARCH.md surfaced this as planner discretion; we keep D-01's name to preserve traceability with CONTEXT.md).

Anti-patterns to delete from the current 49-LOC body:
- Every `Ecto.Changeset.change(%Schema{}, attrs)` call — replaced by `Schema.changeset(struct(Schema), attrs)` via the new helper (Pitfall 6 / PATTERNS.md §3.226 — `change/2` skips schema validations like `validate_template_id_for_outbound/1`).
- The direct `%Cairnloop.KnowledgeBase.Revision{} |> Ecto.Changeset.change(…) |> Repo.insert()` pattern at the current lines 41–49 — this is exactly the FIX-02-breaking anti-pattern. The skeleton leaves the `build_articles` body empty; plan 27-03 fills it using the facade.

Warnings hygiene (D-21, C-02):
- Use `_` prefix on all unused variables in stub returns (e.g., `_articles = build_articles()` — wait, `articles` IS used in `build_conversations(articles)`. Use `_gaps = build_gaps(conversations)` because nothing further consumes gaps at the skeleton stage. The `_sugg, _task` pair in the suggestion line is similarly underscored.).
- No `IO.inspect/1` calls in the committed code.
- No `Logger.*` calls (the script uses `IO.puts/IO.warn` for operator-visible feedback per the prevailing script convention).

Do NOT touch:
- `examples/cairnloop_example/config/config.exs` (Plan 02 owns the ContextProvider wire).
- `examples/cairnloop_example/mix.exs` (sealed — the `ecto.setup` alias target is `priv/repo/seeds.exs`; this plan keeps the file path stable per D-01).
- Any file under `lib/cairnloop/` (the library is sealed — Phase 27 is fixture/config only per C-07).
  </action>
  <verify>
    <automated>cd examples/cairnloop_example &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tee /tmp/p27-01-compile.log &amp;&amp; grep -E '(warning|undefined function|UndefinedFunctionError)' /tmp/p27-01-compile.log | grep -v '^#' | grep -v 'no warnings' &amp;&amp; exit 1 || echo 'CLEAN'</automated>
    <human-check>Open seeds.exs and confirm: (a) the 5-row sealed-enum reconciliation table is present verbatim in the header comment, (b) all D-01 builder names match exactly, (c) the `drain_embedding_pipeline/0` body uses `Oban.drain_queue(queue: :default, with_recursion: true)`, (d) no `Ecto.Changeset.change/2` calls remain in the file.</human-check>
  </verify>
  <done>
- `examples/cairnloop_example/priv/repo/seeds.exs` exists with a `CairnloopExample.SeedRun` module containing the 5 D-01 builder functions (4 stubs + the real `drain_embedding_pipeline/0`), the `get_or_insert!/3` helper, and the header comment block including the sealed-enum reconciliation table.
- `cd examples/cairnloop_example && mix compile --warnings-as-errors` exits 0 (the sole gate; we do NOT attempt `mix run --no-start` here because the Oban drain in `run/0` requires the supervisor and would fail-fast — the runtime gate ships in plan 27-08's DB-backed integration test).
- `grep -c 'Ecto.Changeset.change' examples/cairnloop_example/priv/repo/seeds.exs` returns `0`.
- `grep -c 'ready_for_review' examples/cairnloop_example/priv/repo/seeds.exs` returns `>= 1` (the reconciliation table mentions the spec name).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-machine → CairnloopExample.Repo (Postgres) | The seed script is the only adopter-run write surface at `mix setup` time; seeded data must be safe to ship as a public demo |
| seed script → Oban supervisor | The drain call assumes the Oban supervisor is up (D-08 + Pitfall 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | demo data leaking into production deploys | accept | The seed lives at `examples/cairnloop_example/priv/repo/seeds.exs`. The published Hex package's `package.files` in `mix.exs:17-25` excludes `examples/`. Verified at research-time. No additional mitigation needed in this plan. |
| T-27-02 | Tampering | accidental `Repo.delete_all` against seeded or prod data | mitigate | D-02 mandates `Repo.get_by` guards exclusively — no `delete_all` calls anywhere in the new seed script. The skeleton this plan ships does not include any deletion operations. |
| T-27-03 | Denial of Service | seed run racing with `mix phx.server` on port 4000 (Pitfall 8) | mitigate | Header comment in seeds.exs documents the constraint: "do not run while `mix phx.server` holds port 4000". Adopter-facing warning; the script itself will fail-fast with `:already_started`. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | Phase 27 introduces NO new package dependencies. RESEARCH.md §Package Legitimacy Audit confirms zero new installs. No checkpoint required. |
</threat_model>

<verification>
After this task:
1. `cd examples/cairnloop_example && mix compile --warnings-as-errors` returns 0.
2. `cat examples/cairnloop_example/priv/repo/seeds.exs | grep -E "build_articles|build_conversations|build_gaps|build_suggestion|drain_embedding_pipeline"` returns all 5 D-01 builder names.
3. `grep -c "Ecto.Changeset.change" examples/cairnloop_example/priv/repo/seeds.exs` returns `0` (the old anti-pattern is fully removed).
4. The file header contains the 5-row sealed-enum reconciliation table (grep for `:ready_for_review` + `:ready` on the same comment line returns ≥1 hit; grep for `:new_article` returns ≥1 hit; grep for `:deprecated` returns ≥1 hit).
</verification>

<success_criteria>
- `seeds.exs` skeleton compiles warnings-clean and is structurally ready to receive plans 03–07's builder bodies.
- All architecturally load-bearing decisions (D-01, D-02, D-04, D-05, D-08, D-09, D-21, A1, Pitfalls 5/6/8) are visible in the file header so subsequent plan executors do not need to re-derive them from CONTEXT.md.
- No library code touched (C-07 sealed-phase invariant respected).
- Zero new packages installed (Package Legitimacy gate trivially satisfied).
</success_criteria>

<output>
Create `.planning/phases/27-realistic-demo-fixtures/27-01-SUMMARY.md` when done — record:
- Confirmed builder function names and signatures
- The verbatim sealed-enum reconciliation table that was committed to the header
- Confirmation that no library code was modified
- Any deviations from this plan (should be none; flag if there were any)
</output>
</content>
</invoke>