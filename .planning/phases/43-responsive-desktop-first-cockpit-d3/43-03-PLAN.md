---
phase: 43-responsive-desktop-first-cockpit-d3
plan: 03
type: execute
wave: 3
depends_on: ["43-01", "43-02"]
files_modified:
  - priv/static/cairnloop.css
  - lib/cairnloop/web/inbox_live.ex
  - test/cairnloop/web/responsive_markup_test.exs
  - examples/cairnloop_example/test/e2e/inbox_geometry_test.exs
  - examples/cairnloop_example/test/support/rail_fixtures.ex
autonomous: true
requirements: [RESP-02]
must_haves:
  truths:
    - "Both raw inbox checkboxes (select-all + per-row) carry a class giving a >=44x44px tap target"
    - "The bulk-bar action buttons render at >=44px height (size lg)"
    - "The sticky bulk-bar reserves bottom clearance so it never occludes the last inbox row"
    - "The inbox bulk-bar's var(--cl-primary) literal token remains present in rendered HTML (integration tests unbroken)"
    - "A gated Playwright E2E test measures the three rendered-geometry facts (tap targets >=44px, no bulk-bar occlusion, no 768px regression) at a 768px viewport, replacing the former human-verify checkpoint"
  artifacts:
    - path: "priv/static/cairnloop.css"
      provides: ".cl-checkbox 44px tap-target utility + bulk-bar last-row clearance"
      contains: ".cl-checkbox"
    - path: "lib/cairnloop/web/inbox_live.ex"
      provides: "44px checkboxes + size=lg bulk-bar buttons + bottom clearance on the list"
      contains: "cl-checkbox"
    - path: "examples/cairnloop_example/test/e2e/inbox_geometry_test.exs"
      provides: "Gated browser E2E measuring rendered tap-target sizes, bulk-bar non-occlusion, and 768px non-regression via evaluate/3"
      contains: "getBoundingClientRect"
  key_links:
    - from: "lib/cairnloop/web/inbox_live.ex"
      to: "priv/static/cairnloop.css"
      via: "class=\"cl-checkbox\" + size=\"lg\" referencing .cl-button--lg"
      pattern: "cl-checkbox"
---

<objective>
Close the only confirmed sub-44px tap-target gap and the sticky-bar occlusion gap in the inbox (D3-06, D3-07): give both raw `<input type="checkbox">` controls a `.cl-checkbox` ≥44×44px hit area, bump the bulk-bar action buttons to `size="lg"` (44px), and add bottom clearance so the sticky bulk-bar never occludes the last selectable row. Extend the source-scan test to pin the tap-target sizing. Preserve the `var(--cl-primary)` literal that four integration tests assert.

Purpose: RESP-02 (tap targets ≥44px + sticky bulk-bar clears last row half) — the inbox raw checkboxes (~13-16px) and 36px bulk-bar buttons are the real remaining tap-target gaps; the sticky bar has no bottom spacer today.
Output: New `.cl-checkbox` utility + bulk-bar clearance in `cairnloop.css`; `cl-checkbox` + `size="lg"` + list bottom clearance in `inbox_live.ex`; extended `responsive_markup_test.exs`; one human-verify checkpoint for last-row clearance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-responsive-desktop-first-cockpit-d3/43-CONTEXT.md
@.planning/phases/43-responsive-desktop-first-cockpit-d3/43-RESEARCH.md
@.planning/phases/43-responsive-desktop-first-cockpit-d3/43-VALIDATION.md

<interfaces>
<!-- Verified current state (line-cited reads). -->

Two RAW sub-44px checkboxes in lib/cairnloop/web/inbox_live.ex (the ONLY confirmed tap-target gap):
  - L193-198 select-all: <input type="checkbox" phx-click="toggle_select_all_visible" checked={...} aria-label="Select all visible resolved conversations" />
  - L207-213 per-row:    <input type="checkbox" phx-click="toggle_select" phx-value-id={conv.id} checked={...} aria-label={"Select conversation: ..."} />
  Native checkboxes render ~13-16px — well below 44px. NO .cl-checkbox utility exists in cairnloop.css yet.

Bulk-bar (lib/cairnloop/web/inbox_live.ex L223-239) renders AFTER the <ul class="cl-stack"> (L203):
  - L228: <div role="region" aria-label="Bulk actions" class="bulk-action-bar cl-inbox-bulk-bar cl-row cl-row--wrap">
  - L231: <.cl_button variant="ghost" phx-click="clear_selection">Clear selection</.cl_button>      (default size = md/36px)
  - L236: <.cl_button variant="primary" phx-click="open_bulk_confirm" style="background: var(--cl-primary);">  (default md/36px)
  DO NOT remove the style="background: var(--cl-primary);" — four integration tests assert the literal
  string var(--cl-primary) in rendered HTML (bulk_recovery_live_test.exs:99, approval_footer_live_test.exs:52,
  tool_execution_outcome_live_test.exs:319/398/448). It is a color-token affordance, NOT responsive-relevant,
  and is OUT of D3-08 scope. Leave it inline (Pitfall 3 + Assumption A3).

CSS sizing primitives (priv/static/cairnloop.css):
  - L133: --cl-control-h-lg: 44px   (the 44px token)
  - L324: .cl-button--lg { height: var(--cl-control-h-lg, 44px); padding: 0 var(--cl-control-px-lg, 20px); }
  - .cl_button component (lib/cairnloop/web/components.ex ~L30-43) supports size="lg" -> .cl-button--lg.
  - L525: .cl-inbox-bulk-bar { position: sticky; bottom: 0; ... padding: var(--cl-space-4, 12px); z-index: 1100; }
  - L203 inbox <ul class="cl-stack"> has NO padding-bottom / bottom spacer today.

The .css file is structurally excluded from the brand-token gate; CSS edits cannot trip it. The
inbox_live.ex edits add only class/size/utility attributes (no hex, no rgba) — gate-safe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .cl-checkbox 44px tap-target utility + bulk-bar last-row clearance to cairnloop.css</name>
  <files>priv/static/cairnloop.css</files>
  <read_first>
    - priv/static/cairnloop.css (read L130-156 control tokens incl. --cl-control-h-lg:44px, L297-324 .cl-button/.cl-button--lg, L521-525 modal/bulk-bar, L799-817 .cl-switch 44px pattern as the sizing precedent)
    - .planning/phases/43-responsive-desktop-first-cockpit-d3/43-RESEARCH.md (Pitfall 4 the two raw checkboxes; Pitfall 5 sticky-bar clearance + the ~60-68px bar height -> reserve space-10/space-12; Open Question 2 recommends a shared .cl-checkbox utility; Don't Hand-Roll table)
    - .planning/phases/43-responsive-desktop-first-cockpit-d3/43-CONTEXT.md (D3-06, D3-07)
  </read_first>
  <action>
    Per D3-07 + Open Question 2: add a reusable `.cl-checkbox` utility near the other control rules in `cairnloop.css`. It must give the native checkbox a ≥44×44px hit area without altering checkbox semantics — set `min-width: var(--cl-control-h-lg, 44px); min-height: var(--cl-control-h-lg, 44px);` plus enough padding/margin to make the clickable area 44px, mirroring the `.cl-modal-close`/`.cl-switch` 44px precedent. Keep cursor: pointer and a visible `:focus-visible` ring consistent with the existing focus-ring idiom so keyboard a11y is preserved. Do NOT change the checkbox `appearance` in a way that hides the check state (never state-by-affordance-alone).

    Per D3-06 + Pitfall 5: give the sticky bulk-bar last-row clearance. Because the bulk-bar renders as a sibling AFTER the `<ul class="cl-stack">` and the page scrolls at document level, add a bottom-clearance rule so the last `<li>` is reachable above the sticky bar. Add a scoped class (e.g. `.cl-inbox-list--bulk-clearance` or reuse a clear utility) with `padding-bottom` ≥ the bar's rendered height — reserve `var(--cl-space-10, 48px)` (the bar is ~60-68px; 48px token is the closest existing step — if a larger step exists like space-12, prefer the one that fully clears a 44px button + 12px×2 padding). Document in a brief comment that this reserves space for the sticky bulk-bar. Do NOT use a `max-width` media query and do NOT add a `var()` inside any `@media`.

    Keep all edits token-valued (no hex, no rgba). The `.css` file is gate-excluded; these edits cannot trip the brand-token gate.
  </action>
  <verify>
    <automated>grep -q '\.cl-checkbox' priv/static/cairnloop.css && grep -q 'min-height: var(--cl-control-h-lg' priv/static/cairnloop.css && grep -q '\.cl-inbox-list--bulk-clearance' priv/static/cairnloop.css && grep -q 'padding-bottom: var(--cl-space-10' priv/static/cairnloop.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `priv/static/cairnloop.css` defines a `.cl-checkbox` rule with `min-width` and `min-height` resolving to the 44px control token (`--cl-control-h-lg`)
    - `priv/static/cairnloop.css` defines a bottom-clearance rule reserving `padding-bottom` of at least `var(--cl-space-10, 48px)` for the inbox list under the sticky bulk-bar
    - No new `@media (max-width` and no `var(` inside any `@media` condition introduced (Plan 01 mobile-first contract preserved)
    - `mix test test/cairnloop/web/cairnloop_css_test.exs` exits 0 (RESP-01 contract unbroken)
  </acceptance_criteria>
  <done>cairnloop.css has a `.cl-checkbox` 44px tap-target utility and an inbox-list bottom-clearance rule for the sticky bulk-bar, both token-valued, with the mobile-first contract intact.</done>
</task>

<task type="auto">
  <name>Task 2: Apply cl-checkbox + size=lg bulk-bar buttons + list bottom clearance in inbox_live.ex; extend the source-scan test</name>
  <files>lib/cairnloop/web/inbox_live.ex, test/cairnloop/web/responsive_markup_test.exs</files>
  <read_first>
    - lib/cairnloop/web/inbox_live.ex (read L185-245 — both raw checkboxes L193/L207, the <ul class="cl-stack"> L203, the bulk-bar L223-239 incl. the var(--cl-primary) inline style at L236)
    - test/cairnloop/web/responsive_markup_test.exs (the source-scan test created in Plan 02 — extend it, do not create a duplicate; mirror its File.read! style)
    - .planning/phases/43-responsive-desktop-first-cockpit-d3/43-RESEARCH.md (Pitfall 3 — do NOT migrate var(--cl-primary); Pitfall 4 — size="lg" for bulk-bar buttons; D3-08 scope: only responsive/layout styles, color-token inline styles are out of scope)
    - lib/cairnloop/web/components.ex (~L30-43 — confirm cl_button accepts size="lg")
  </read_first>
  <action>
    Per D3-07: add `class="cl-checkbox"` to BOTH raw `<input type="checkbox">` controls (L193-198 select-all and L207-213 per-row). Preserve every existing attribute (`type`, `phx-click`, `phx-value-id`, `checked`, `aria-label`) — additive class only; do NOT change the sealed bulk-select event wiring.

    Per D3-07 + Pitfall 4: add `size="lg"` to BOTH bulk-bar `.cl_button` calls (L231 ghost "Clear selection" and L236 primary "Send recovery follow-up"), giving them a 44px height via `.cl-button--lg`. On the L236 primary button, KEEP the existing `style="background: var(--cl-primary);"` unchanged — it is a color-token affordance four integration tests assert in rendered HTML; it is NOT responsive-relevant and is explicitly out of D3-08 scope (Pitfall 3). Do not migrate it to a utility class (a class would not put the literal token string in rendered HTML).

    Per D3-06: apply the bottom-clearance class from Task 1 to the inbox `<ul class="cl-stack">` (L203) so the sticky bulk-bar clears the last row — add the clearance class alongside `cl-stack` (e.g. `class="cl-stack cl-inbox-list--bulk-clearance"`). Do not alter the list's children or the bulk-bar markup beyond the size attributes above.

    Per D3-08 (conservative scope): do NOT migrate the `var(--cl-primary)` (L236) or `var(--cl-danger)` (L267) color-token inline styles — they are not responsive-relevant and are gate-safe as-is. No other inbox inline styles are in scope this plan.

    Then extend `test/cairnloop/web/responsive_markup_test.exs` (created in Plan 02) with a `describe "tap targets >= 44px (D3-07 / RESP-02)"` block, source-scanning `inbox_live.ex`: assert both checkbox occurrences carry `class="cl-checkbox"` (e.g. assert the source contains `cl-checkbox` at least twice, or assert `class="cl-checkbox"` present and that the two `type="checkbox"` inputs are sized), assert `size="lg"` appears on the bulk-bar buttons, and assert the bottom-clearance class (`cl-inbox-list--bulk-clearance`) is applied to the inbox list. Add a CSS-scan assertion (reading `priv/static/cairnloop.css`) that pins BOTH the bulk-bar clearance rule by class (`.cl-inbox-list--bulk-clearance`) AND its `padding-bottom: var(--cl-space-10` declaration — so a regression that drops the sticky-bar last-row clearance rule fails the gate (warning #2). Also assert the `var(--cl-primary)` literal is STILL present in `inbox_live.ex` (a guard that the integration-test contract was not broken). Keep the test `async: true` and Repo-free.
  </action>
  <verify>
    <automated>grep -c 'cl-checkbox' lib/cairnloop/web/inbox_live.ex; grep -c 'size="lg"' lib/cairnloop/web/inbox_live.ex; grep -q 'var(--cl-primary)' lib/cairnloop/web/inbox_live.ex && mix test test/cairnloop/web/responsive_markup_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cl-checkbox' lib/cairnloop/web/inbox_live.ex` returns at least 2 (both checkboxes classed)
    - `grep -c 'size="lg"' lib/cairnloop/web/inbox_live.ex` returns at least 2 (both bulk-bar buttons)
    - `grep -q 'var(--cl-primary)' lib/cairnloop/web/inbox_live.ex` succeeds (the asserted token literal is preserved)
    - The inbox `<ul>` carries both `cl-stack` and the bulk-bar bottom-clearance class
    - The extended test contains a CSS-scan assertion that `cairnloop.css` defines `.cl-inbox-list--bulk-clearance` with a `padding-bottom: var(--cl-space-10` declaration (the sticky-bar last-row clearance rule is pinned)
    - `mix test test/cairnloop/web/responsive_markup_test.exs` exits 0 (new tap-target describe block green)
    - `mix compile --warnings-as-errors` passes
  </acceptance_criteria>
  <done>Both inbox checkboxes are `.cl-checkbox` (44px), both bulk-bar buttons are `size="lg"`, the list reserves bottom clearance, the `var(--cl-primary)` literal is preserved, and the extended source-scan test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Automate the rendered-geometry verification as a gated Playwright E2E (replaces the former human-verify checkpoint)</name>
  <files>examples/cairnloop_example/test/e2e/inbox_geometry_test.exs, examples/cairnloop_example/test/support/rail_fixtures.ex</files>
  <what-built>
    A `.cl-checkbox` 44×44px tap target applied to both inbox checkboxes, `size="lg"` (44px) bulk-bar buttons, and bottom clearance on the inbox list so the sticky bulk-bar (`position: sticky; bottom: 0`) no longer occludes the last selectable row. Plus the Plan 01 mobile-first min-width conversions (`.cl-main` padding, `.cl-nav` layout). This task converts the verification of those three rendered-geometry facts from a human eyeball into an automated, CI-gated browser assertion — the same pattern Phases 41/42 used (rail_disclosure_test.exs, thread_navigation_test.exs). Owner directive: zero human verification/UAT; shift left onto the gated CI e2e lane where it has recurring drift-proofing value.
  </what-built>
  <action>
    Author a Playwright E2E that MEASURES (not source-scans) the three facts the human was being asked to confirm, at a 768px viewport via the `PhoenixTest.Playwright` `evaluate/3` JS bridge:

    1. Add a `resolved_inbox_rows/1` fixture to `examples/cairnloop_example/test/support/rail_fixtures.ex` (mirror the existing `resolved_conversation_with_next_open/0` insert idiom) that seeds ~25 `status: :resolved` conversations so the inbox list overflows a 720px-tall viewport and the sticky-bar clearance is exercised under a real scroll.

    2. Create `examples/cairnloop_example/test/e2e/inbox_geometry_test.exs` (`use PhoenixTest.Playwright.Case, async: false, browser_context_opts: [viewport: %{width: 768, height: 720}]`, `@moduletag :e2e`). Path is `/support/inbox` (the `cairnloop_dashboard` macro scopes the lib's `live("/inbox")` under `/support`; the sandbox `LiveAcceptance` on_mount already covers the whole dashboard live_session). Three tests:
       - Tap targets: `getBoundingClientRect()` on both `.cl-checkbox` inputs and both `.cl-button--lg` bulk-bar buttons → assert width/height ≥ 44.
       - No occlusion: select all, `window.scrollTo(0, document.body.scrollHeight)`, then assert the last `ul.cl-inbox-list--bulk-clearance li` bottom ≤ `.cl-inbox-bulk-bar` top (the genuinely browser-only fact; also guards a future `position: fixed` regression).
       - No 768px regression: assert `.cl-main` computed `padding-left` ≥ 24px (the min-width conversion fired) and `.cl-nav__link` renders.

    Do NOT alter inbox markup or the `var(--cl-primary)` inline style (the four integration contracts and Task 2's source-scan guard stay intact). No CI-config change is needed: `mix test.e2e` auto-discovers `test/e2e/*_test.exs` and the gated `e2e` release-gate job always runs it.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example && MIX_ENV=test mix compile --warnings-as-errors && grep -q 'getBoundingClientRect' test/e2e/inbox_geometry_test.exs && grep -q 'resolved_inbox_rows' test/support/rail_fixtures.ex && echo PASS</automated>
    <ci-gated>mix test.e2e (gated `e2e` release-gate job, pgvector/pgvector:pg16 + Chromium) executes inbox_geometry_test.exs on every push — this is the empirical execution gate. Local execution is blocked only because this workspace's Postgres lacks the pgvector extension; the module compiles and the harness/selectors are verified.</ci-gated>
  </verify>
  <acceptance_criteria>
    - `examples/cairnloop_example/test/e2e/inbox_geometry_test.exs` exists, is tagged `@moduletag :e2e`, sets a 768px viewport, and uses `evaluate/3` + `getBoundingClientRect()`/`getComputedStyle()` to measure the three facts
    - `examples/cairnloop_example/test/support/rail_fixtures.ex` defines `resolved_inbox_rows/1`
    - The example app compiles warnings-clean (`MIX_ENV=test mix compile --warnings-as-errors`) and the E2E module loads without compile error
    - No inbox markup / `var(--cl-primary)` change (integration + source-scan contracts preserved)
  </acceptance_criteria>
  <done>The three rendered-geometry facts are asserted by a gated Playwright E2E (`inbox_geometry_test.exs`) at 768px; the former human-verify checkpoint is removed and the phase requires zero human UAT — verification runs in the gated CI e2e lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds CSS utilities and markup class/size attributes. No untrusted input crosses any boundary; the sealed inbox bulk-select event wiring is untouched; no data, auth, or network surface is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05 | (a11y/governance-adjacent, not STRIDE) | inbox checkboxes + bulk-bar buttons | mitigate | `.cl-checkbox` and `size="lg"` give ≥44px tap targets; source-scan test pins both; `:focus-visible` ring preserved for keyboard a11y. |
| T-43-06 | Tampering | inbox bulk-bar primary button (var(--cl-primary)) | accept | The color-token affordance is left inline and a test guard asserts its literal presence, preventing accidental removal that would break four integration tests (never-color-alone, brand §7.5). No user-controlled style string is introduced. |
| T-43-07 | (rendered-geometry, not STRIDE) | sticky bulk-bar occlusion | mitigate | Bottom-clearance padding reserves ≥48px so the last row is reachable; confirmed by a gated Playwright E2E (`inbox_geometry_test.exs`) that scrolls to the bottom and asserts last-row bottom ≤ bulk-bar top at 768px (replaces the former human-verify; also catches a future `position: fixed` regression). |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs this plan (zero external dependencies). |

**Honest assessment:** No new input/output/auth/data-access surface; the sealed bulk-select logic is untouched (additive class/size attributes only). Risks are accessibility (tap-target/keyboard) and rendered-geometry occlusion — mitigated by the tap-target utility, the source-scan test, and the gated Playwright E2E that measures last-row clearance (no human in the loop). ASVS V2–V6 do not apply.
</threat_model>

<verification>
- `mix test test/cairnloop/web/responsive_markup_test.exs` is green (tap-target + token-preserved assertions).
- `mix test` (full headless suite incl. brand-token gate + the four integration-asserted `var(--cl-primary)` tests in their suites) remains green — no regression.
- `mix compile --warnings-as-errors` passes (lib + example app).
- Automated geometry E2E (Task 3): `examples/cairnloop_example/test/e2e/inbox_geometry_test.exs` measures last-row clearance, ≥44px tap targets, and 768px non-regression — executed by the gated CI `e2e` lane (no human verification).
</verification>

<success_criteria>
- Both inbox checkboxes have a ≥44×44px tap target via `.cl-checkbox`.
- Both bulk-bar buttons render at 44px (`size="lg"`).
- The sticky bulk-bar reserves bottom clearance so the last row is never occluded (gated E2E-verified, no human in the loop).
- The `var(--cl-primary)` literal remains in rendered inbox HTML (integration tests unbroken).
- The source-scan test pins the tap-target sizing and the token preservation; the gated Playwright E2E pins the rendered geometry.
</success_criteria>

<output>
Create `.planning/phases/43-responsive-desktop-first-cockpit-d3/43-03-SUMMARY.md` when done.
</output>
