---
phase: 32.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/config/dev.exs
  - examples/cairnloop_example/config/runtime.exs
  - examples/cairnloop_example/mix.lock
  - test/cairnloop/web/brand_token_gate_test.exs
  - guides/04-troubleshooting.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [INT-BLOCKER-01, DOC-04, BRAND-04]
must_haves:
  truths:
    - "EditorHandoff token key configuration is present in development environment."
    - "Application fail-fast configuration for EditorHandoff is active in production environment."
    - "Troubleshooting guide has a dedicated section for EditorHandoff token key."
    - "DOC-04 requirement is marked as complete in REQUIREMENTS.md."
    - "BRAND-04 gate test recursively checks all Elixir files in the configured directories."
  artifacts:
    - path: "examples/cairnloop_example/config/dev.exs"
      provides: "Development configuration for EditorHandoff"
      contains: "secret_key_base"
    - path: "examples/cairnloop_example/config/runtime.exs"
      provides: "Production configuration for EditorHandoff"
      contains: "System.fetch_env!"
    - path: "guides/04-troubleshooting.md"
      provides: "Documentation for token key configuration"
      contains: "EditorHandoff Token Key"
    - path: "test/cairnloop/web/brand_token_gate_test.exs"
      provides: "Recursive file checking for brand token tests"
      contains: "**/*.ex"
  key_links:
    - from: "examples/cairnloop_example/config/runtime.exs"
      to: "System Environment"
      via: "System.fetch_env!"
      pattern: "CAIRNLOOP_HANDOFF_SECRET_KEY_BASE"
---

<objective>
Close the specific gaps identified in the vM014 milestone audit.

Purpose: Fix a runtime crash in dev due to missing config, update documentation for fail-closed secrets in prod, fix a wildcard path in testing, and resolve pending documentation requirements.
Output: Configured EditorHandoff secrets, an updated troubleshooting guide, a fixed test wildcard, and resolved requirements.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-close-gap-editorhandoff-dev-config-requirements-md-checkbox-/32.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EditorHandoff Configuration and Lock Dependencies</name>
  <files>examples/cairnloop_example/config/dev.exs, examples/cairnloop_example/config/runtime.exs, examples/cairnloop_example/mix.lock</files>
  <action>
    Implement D-01: Add the EditorHandoff `secret_key_base` to `examples/cairnloop_example/config/dev.exs` with the static value `"dev_only_64_byte_minimum_secret_for_editor_handoff_tokens_cairnloop"`. Place this after the `CairnloopExampleWeb.Endpoint` config block and before the `import_config` line at the bottom.
    
    Implement D-02: Add the EditorHandoff `secret_key_base` to `examples/cairnloop_example/config/runtime.exs` using `System.fetch_env!("CAIRNLOOP_HANDOFF_SECRET_KEY_BASE")`. Place this inside the `if config_env() == :prod do` block, mirroring existing prod-only secret patterns.
    
    Implement D-06: Ensure that `examples/cairnloop_example/mix.lock` is tracked and staged in the plan's commit (this captures pre-existing dependency bumps like decimal, oban, and swoosh). Do not attempt to run `mix deps.update` — just stage the existing modifications in the file.
  </action>
  <verify>
    <automated>grep -q "dev_only_64_byte_minimum_secret" examples/cairnloop_example/config/dev.exs &amp;&amp; grep -q "CAIRNLOOP_HANDOFF_SECRET_KEY_BASE" examples/cairnloop_example/config/runtime.exs</automated>
  </verify>
  <done>Configuration keys are set and `mix.lock` is included for the changes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Test Wildcards and Resolve Documentation Gaps</name>
  <files>test/cairnloop/web/brand_token_gate_test.exs, guides/04-troubleshooting.md, .planning/REQUIREMENTS.md</files>
  <action>
    Implement D-05: In `test/cairnloop/web/brand_token_gate_test.exs`, update both `Path.wildcard` calls, replacing `*.ex` with `**/*.ex` for consistent recursive scanning across `@web_dir` and `@example_live_dir`.
    
    Implement D-03: Add a new "EditorHandoff Token Key" section to `guides/04-troubleshooting.md` at the end of the guide. Follow the standard "Symptom / Cause / Fix" pattern documenting the `CAIRNLOOP_HANDOFF_SECRET_KEY_BASE` requirement in production vs. dev.
    
    Implement D-04: In `.planning/REQUIREMENTS.md`, update the line for `DOC-04` to tick the checkbox (change `[ ]` to `[x]`). Also, update the "Last updated" footer comment to mention this cleanup pass.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/brand_token_gate_test.exs</automated>
  </verify>
  <done>Tests pass with the recursive wildcard, docs have the new section, and the DOC-04 requirement is marked complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config Injection | Application secrets must be securely provided via environment variables in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1-01 | Information Disclosure | `runtime.exs` | mitigate | Use `System.fetch_env!` (fail-closed) to ensure the application will not start without the EditorHandoff token key |
| T-32.1-02 | Information Disclosure | `dev.exs` | accept | Use a documented, static, clearly labeled "dev_only" minimum 64-byte key for local development |
</threat_model>

<verification>
1. Run `grep -v '^#' examples/cairnloop_example/config/dev.exs | grep -c dev_only_64_byte_minimum_secret` to ensure > 0 matches
2. Run `grep -v '^#' examples/cairnloop_example/config/runtime.exs | grep -c CAIRNLOOP_HANDOFF_SECRET_KEY_BASE` to ensure > 0 matches
3. Run `mix test test/cairnloop/web/brand_token_gate_test.exs` and ensure it passes
</verification>

<success_criteria>
All audited gaps are closed: the EditorHandoff runs without runtime crashes in dev, the token key is documented for production use in troubleshooting, the tests verify brand tokens recursively, and the requirement documentation accurately reflects task completion.
</success_criteria>

<output>
Create `.planning/phases/32.1-close-gap-editorhandoff-dev-config-requirements-md-checkbox-/32.1-01-SUMMARY.md` when done
</output>