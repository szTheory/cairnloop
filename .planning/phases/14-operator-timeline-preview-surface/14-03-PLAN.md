---
phase: 14-operator-timeline-preview-surface
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - lib/cairnloop/web/conversation_live.ex
autonomous: true
requirements: [FLOW-01, FLOW-02]
user_setup: []

must_haves:
  truths:
    - "`handle_event(\"execute_tool\")` threads `conversation_id` (from `socket.assigns.conversation.id`) into the propose context, so proposals are conversation-scoped (D-07)."
    - "`reload_conversation_with_context/2` loads `Governance.list_proposals_for_conversation/1` and assigns `governed_actions` alongside the existing assigns (D-09)."
    - "A new 'Governed actions' rail section renders the `governed_actions` list via a plain-assign list-comprehension of `governed_action_card/1` — NO `Phoenix.LiveView.stream/3` (D-02)."
    - "Blocked proposals (`:needs_input`, `:scope_invalid`, `:policy_denied`) appear durably in the rail (Support-Truth Gate)."
    - "The launcher button reads 'Propose' (not 'Execute') and uses the brand token `var(--cl-primary, #A94F30)` instead of the hardcoded `#2563eb` (D-04)."
    - "All three `inspect(reason)` calls in `failure_reason_message/1` are replaced with `ToolProposalPresenter.reason_label/1` — no raw Elixir terms reach the operator (D-14)."
  artifacts:
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "conversation_id threading + governed_actions assign + rail section + Propose rename + reason_label wiring"
      contains: "governed_actions"
  key_links:
    - from: "lib/cairnloop/web/conversation_live.ex (reload_conversation_with_context/2)"
      to: "Cairnloop.Governance.list_proposals_for_conversation/1"
      via: "load + assign governed_actions"
      pattern: "list_proposals_for_conversation"
    - from: "lib/cairnloop/web/conversation_live.ex (rail section)"
      to: "governed_action_card/1"
      via: "plain-assign list-comprehension over @governed_actions"
      pattern: "governed_action_card"
    - from: "lib/cairnloop/web/conversation_live.ex (handle_event execute_tool)"
      to: "Governance.propose/3 context"
      via: "Map.put(context, :conversation_id, socket.assigns.conversation.id)"
      pattern: "conversation_id"
---

<objective>
Wire the governed-action timeline into `ConversationLive`: thread `conversation_id` into the propose
context, load + assign `governed_actions`, render the new "Governed actions" rail section using the
Wave-2 `governed_action_card/1` via a plain-assign list-comprehension (no streams), reframe the
launcher ("Execute" → "Propose" + brand-token color), and replace the operator-facing
`inspect(reason)` calls with `ToolProposalPresenter.reason_label/1`.

This is the final wave: it makes FLOW-01 observable end to end (durable per-conversation timeline,
blocked states visible — the Support-Truth Gate) and completes the FLOW-02 surface.

Purpose: Connect the Wave-1 facade/presenter and the Wave-2 card into the live operator workflow,
keeping the established thin-notification → full-reload PubSub pattern (D-02).
Output: `conversation_live.ex` fully wired; the Wave-0 LiveView + source-assertion tests green.

NOTE: This plan modifies the same file as Wave 2 (`conversation_live.ex`). It runs AFTER Wave 2
(separate wave) — the two must never run in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-operator-timeline-preview-surface/14-CONTEXT.md
@.planning/phases/14-operator-timeline-preview-surface/14-RESEARCH.md
@CLAUDE.md
@prompts/cairnloop_brand_book.md

<interfaces>
<!-- Confirmed touch points (line numbers from RESEARCH, verified against source). -->

`lib/cairnloop/web/conversation_live.ex` (the four touch points):
- `handle_event("execute_tool", %{"tool" => tool_ref} = params, socket)` (L173-185) — currently:
  `context = Map.put(context, :tool_params, ...)` then `Cairnloop.Governance.propose(tool_ref, actor_id, context)`.
  Add `Map.put(context, :conversation_id, socket.assigns.conversation.id)` before the propose call (D-07).
- `failure_reason_message/1` (L188-198) — three clauses use `inspect(reason)`:
  `:scope_invalid` (L191-192), `:policy_denied` (L194-195), catch-all (L197-198). Replace each
  `inspect(reason)` with `Cairnloop.Web.ToolProposalPresenter.reason_label(reason)` (D-14).
- `reload_conversation_with_context/2` (L200) — currently assigns `conversation, host_context,
  context_error, quick_fix_card`. Add `governed_actions: Governance.list_proposals_for_conversation(conversation_id)`.
- `tool_renderer/1` (L503-548) — button at L540 has `background: #2563eb` and text `Execute`.
  Change to brand token `var(--cl-primary, #A94F30)` and text `Propose` (D-04). The zero-field button
  at L521 needs no text change (its label is the humanized tool name).

Consume:
- `Cairnloop.Governance.list_proposals_for_conversation/1` (Wave 1).
- `governed_action_card/1` (Wave 2 — the rail section renders this).
- `Cairnloop.Web.ToolProposalPresenter.reason_label/1` (Wave 1).

Rail render pattern (existing precedent in this file):
- Drafts list-comprehension (L401-408): `if Ecto.assoc_loaded?(...) and length(...) > 0 do` →
  `for draft <- @conversation.drafts do <.draft_audit_card ... /> end`. The governed-actions rail
  section mirrors this with `@governed_actions` (a plain assign list, already loaded — no
  `assoc_loaded?` needed on the list itself, but guard an empty list with calm empty-state copy).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread conversation_id, replace inspect with reason_label, assign governed_actions, rename Execute→Propose + brand token</name>
  <files>lib/cairnloop/web/conversation_live.ex</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex L173-185 (handle_event execute_tool — conversation_id thread), L188-198 (failure_reason_message — three inspect calls), L200-... (reload_conversation_with_context — governed_actions assign), L503-548 (tool_renderer — button at L540)
    - lib/cairnloop/governance.ex (list_proposals_for_conversation/1 from Wave 1; propose/3 reads :conversation_id from context)
    - lib/cairnloop/web/tool_proposal_presenter.ex (reason_label/1 from Wave 1)
    - test/cairnloop/web/conversation_live_test.exs (Wave-0 tests: governed_actions assign, blocked-in-rail, inspect-removal source assertion, Propose rename)
  </read_first>
  <action>
    In `handle_event("execute_tool", ...)` (L173): after the existing
    `context = Map.put(context, :tool_params, ...)`, add
    `context = Map.put(context, :conversation_id, socket.assigns.conversation.id)` before
    `Cairnloop.Governance.propose/3` (symmetric to the `:tool_params` merge — D-07). This makes both
    valid and blocked proposals conversation-scoped (the Wave-1 facade persists it on both paths).

    In `failure_reason_message/1` (L188-198): replace all three `inspect(reason)` usages —
    `:scope_invalid` (L192), `:policy_denied` (L195), catch-all (L198) — with
    `Cairnloop.Web.ToolProposalPresenter.reason_label(reason)` (alias as appropriate). The
    `:unsupported` and `:needs_input` clauses already use static copy — leave them. After this change
    NO `inspect(` remains inside `failure_reason_message/1` (D-14).

    In `reload_conversation_with_context/2` (L200): add
    `governed_actions = Cairnloop.Governance.list_proposals_for_conversation(conversation_id)` and add
    `governed_actions: governed_actions` to the `assign/2` call alongside `quick_fix_card` (D-09).
    Ensure every render path that reads `@governed_actions` is covered (this function is the single
    reload seam used by mount + all handle_info/handle_event reload calls).

    In `tool_renderer/1` (L540): change the submit button `style` `background: #2563eb` →
    `background: var(--cl-primary, #A94F30)` and the button text `Execute` → `Propose` (D-04). Do NOT
    touch the zero-field button at L521 (its label is the humanized tool name; no "Execute" text).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/web/conversation_live_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `handle_event("execute_tool")` threads conversation_id: `grep -c "conversation_id" lib/cairnloop/web/conversation_live.ex` ≥ 1 in the execute_tool path; the Wave-0 test asserting proposals are conversation-scoped passes.
    - No `inspect(` remains inside `failure_reason_message/1`: extract the function body and assert zero `inspect(` — `awk '/defp failure_reason_message/,/^  defp [a-z]/' lib/cairnloop/web/conversation_live.ex | grep -c "inspect("` returns `0` (the source-assertion Wave-0 test is the authoritative gate).
    - `reload_conversation_with_context/2` assigns `governed_actions`: `grep -c "governed_actions" lib/cairnloop/web/conversation_live.ex` ≥ 1 and `grep -c "list_proposals_for_conversation" lib/cairnloop/web/conversation_live.ex` ≥ 1.
    - Launcher renamed + brand token: `grep -c "Propose" lib/cairnloop/web/conversation_live.ex` ≥ 1; `grep -c "#2563eb" lib/cairnloop/web/conversation_live.ex` returns `0`; `grep -c "var(--cl-primary" lib/cairnloop/web/conversation_live.ex` ≥ 1.
    - `mix compile --warnings-as-errors` clean; existing tests still pass.
  </acceptance_criteria>
  <done>conversation_id is threaded into propose, governed_actions is loaded + assigned via the facade, operator-facing reasons are humanized (no inspect), and the launcher is reframed to "Propose" with the brand token — Wave-0 wiring + source-assertion tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Render the "Governed actions" rail section (plain-assign list-comprehension, no streams)</name>
  <files>lib/cairnloop/web/conversation_live.ex</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex L381 (message-timeline list-comprehension), L401-408 (drafts list-comprehension + draft_audit_card render — the exact rail-section pattern to mirror), the rail/right-panel render region around quick_fix_card placement
    - lib/cairnloop/web/conversation_live.ex governed_action_card/1 (Wave 2 — the component this section renders)
    - prompts/cairnloop_brand_book.md §10.2 (rail layout — right panel = context/draft/sources/policy/actions)
    - test/cairnloop/web/conversation_live_test.exs (Wave-0 tests: blocked proposals visible in the rail; rail section renders the timeline)
  </read_first>
  <action>
    Add a new "Governed actions" section to the right evidence rail in the LiveView render, as a
    SIBLING to the existing `quick_fix_card`/drafts sections (NOT inline in the center
    `.message-timeline` — D-01; brand §10.2). Render it with a plain-assign list-comprehension over
    `@governed_actions` (the assign loaded in Task 1) — mirror the drafts pattern at L401-408:
    iterate `for proposal <- @governed_actions do <.governed_action_card proposal={proposal} /> end`.
    Do NOT use `Phoenix.LiveView.stream/3` (D-02) — this is a bounded, per-conversation, rarely-updated
    list fed by the established full-reload path.

    Empty state: when `@governed_actions` is `[]`, render a calm, honest empty-state line (e.g. a
    short "No governed actions yet" copy in the brand voice) rather than an empty section or a crash.
    Newest-first ordering is already guaranteed by the facade (`desc: inserted_at`); do not re-sort in
    the template. Add a section header consistent with the other rail section headers, and minimal
    scoped CSS following the existing rail-section idiom (brand tokens only).

    This section is OBSERVE-only (read-only timeline). The launcher (INITIATE surface) stays in the
    `context_pane` "Actions" section reframed in Task 1 — keep initiate-vs-observe distinct (D-04).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/web/conversation_live_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The rail section renders the card: `grep -c "governed_action_card" lib/cairnloop/web/conversation_live.ex` ≥ 2 (definition from Wave 2 + the new render call site).
    - No streams introduced: `grep -c "stream(" lib/cairnloop/web/conversation_live.ex` returns `0` and `grep -c "phx-update=\"stream\"" lib/cairnloop/web/conversation_live.ex` returns `0` (D-02).
    - The section iterates `@governed_actions` via a list-comprehension (plain assign), with a calm empty-state branch for `[]`.
    - The Wave-0 test asserting blocked proposals (`:needs_input`, `:scope_invalid`, `:policy_denied`) appear in the rail passes (Support-Truth Gate).
    - The Wave-0 test asserting the rail section renders the timeline (the card appears for a seeded `governed_actions` list) passes.
    - `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>The "Governed actions" rail section renders the per-conversation timeline via a plain-assign list-comprehension of `governed_action_card/1` (no streams), with a calm empty state and durable visibility of blocked proposals — FLOW-01 observable end to end; Wave-0 rail tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → `handle_event("execute_tool")` | untrusted form params cross into the propose context. |
| blocked-proposal reason → operator UI | `failure_reason_message/1` currently emits raw `inspect(reason)`. |
| per-conversation proposal list → rail render | `governed_actions` assign rendered into the DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 (T-pii) | Information Disclosure / brand | `inspect(reason)` in `failure_reason_message/1` reaching the operator | mitigate | Replace all three `inspect(reason)` with `ToolProposalPresenter.reason_label/1`; source-assertion test gates that no `inspect(` remains in the function (D-14). Prevents raw Elixir term / PII-in-reason leakage to the operator. |
| T-14-03-02 | Tampering | conversation_id sourced from client params | mitigate | `conversation_id` is taken from `socket.assigns.conversation.id` (server-trusted), NOT from request params; it is routing metadata excluded from the idempotency key (D-08, enforced in Wave 1). |
| T-14-03-03 (T-pii) | Information Disclosure | rail rendering of proposals incl. blocked ones | mitigate | Cards render exclusively through the Wave-2 component (which uses the Wave-1 masking choke point); the rail section never renders raw snapshot fields directly. Blocked proposals are shown (Support-Truth Gate) but still through the masking path. |
| T-14-03-04 | Denial of Service | unbounded rail list | accept | List is bounded per-conversation and loaded via the indexed `[conversation_id, inserted_at]` query (Wave 1); Phase 14 has no status churn (no streams needed, D-02). Re-evaluate at Phase 16 when execution events flow. |
| T-14-SC | Tampering | mix/external installs | accept | No package installs in Phase 14 (RESEARCH Package Legitimacy Audit: Not applicable). |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/cairnloop/web/conversation_live_test.exs` green for wiring + rail + source-assertion describe blocks.
- `mix test` (full suite) green at the end of this wave (phase gate) — report any pre-existing baseline failures / `*.Repo` boot noise honestly per CLAUDE.md / STATE.md.
- Visual brand compliance of the rail section is a manual-only check per 14-VALIDATION.md.
</verification>

<success_criteria>
- conversation_id threaded; governed_actions assigned via the facade; "Governed actions" rail section renders the timeline via plain-assign list-comprehension (no streams).
- Blocked proposals durably visible (Support-Truth Gate); launcher reframed to "Propose" with the brand token; no `inspect` to the operator.
- FLOW-01 observable end to end; Wave-0 wiring + rail + source-assertion tests green; full suite green at the phase gate.
</success_criteria>

<output>
Create `.planning/phases/14-operator-timeline-preview-surface/14-03-SUMMARY.md` when done.
</output>
