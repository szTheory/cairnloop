---
phase: "35"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/cairnloop/web/health_plug.ex
  - lib/cairnloop/web/metrics_plug.ex
  - lib/cairnloop/auditor.ex
  - test/cairnloop/web/health_plug_test.exs
  - test/cairnloop/web/metrics_plug_test.exs
  - test/cairnloop/auditor_test.exs
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - AUDIT-01
must_haves:
  truths:
    - Adopter infrastructure can poll `/health` to verify application liveness.
    - Adopter infrastructure can scrape `/metrics` to gather telemetry data.
    - `Cairnloop.Auditor` behavior supports reading audit events.
  artifacts:
    - path: lib/cairnloop/web/health_plug.ex
      provides: "200 OK liveness checks"
    - path: lib/cairnloop/web/metrics_plug.ex
      provides: "Prometheus formatted metrics exporter"
  key_links:
    - from: lib/cairnloop/web/metrics_plug.ex
      to: telemetry_metrics_prometheus_core
      via: "Optional compilation check"
---

<objective>
Provide health/metrics plugs for adopters and extend the Auditor behaviour for read support.

Purpose: Implement operational endpoints and prepare the backend for the new Audit Log UI.
Output: HealthPlug, MetricsPlug, and expanded Auditor behaviour with tests.
</objective>

<context>
@.planning/phases/35-audit-operations-support/35-RESEARCH.md
@lib/cairnloop/auditor.ex
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Metrics | Metrics endpoint exposes operational data but no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Information Disclosure | `MetricsPlug` | accept | Metrics payload is designed to be public/internal; adopters handle network exposure. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Health and Metrics Plugs (OPS-01, OPS-02)</name>
  <files>mix.exs, lib/cairnloop/web/health_plug.ex, lib/cairnloop/web/metrics_plug.ex, test/cairnloop/web/health_plug_test.exs, test/cairnloop/web/metrics_plug_test.exs</files>
  <action>
    - In `mix.exs`, add `{:telemetry_metrics_prometheus_core, "~> 1.2", optional: true}` to `deps()`.
    - Create `Cairnloop.Web.HealthPlug` implementing the `Plug` behaviour. It should respond to `call/2` with a 200 status and `{"status": "ok"}` JSON.
    - Create `Cairnloop.Web.MetricsPlug` implementing the `Plug` behaviour. Check `Code.ensure_loaded?(TelemetryMetricsPrometheus.Core)`. If loaded, respond with 200 and the Prometheus text payload (via `scrape/1`). If not loaded, respond with 501 and a message indicating the missing dependency.
    - Create test/cairnloop/web/health_plug_test.exs asserting HealthPlug returns 200 JSON.
    - Create test/cairnloop/web/metrics_plug_test.exs verifying MetricsPlug handles the 200 and 501 cases.
  </action>
  <verify>
    <automated>mix deps.get && mix compile && mix test test/cairnloop/web/health_plug_test.exs test/cairnloop/web/metrics_plug_test.exs</automated>
  </verify>
  <done>Health and Metrics plugs are defined and cleanly handle the optional dependency, and are tested.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Auditor Behaviour (AUDIT-01 backend)</name>
  <files>lib/cairnloop/auditor.ex, test/cairnloop/auditor_test.exs</files>
  <action>
    - Add `@callback list_events(opts :: keyword()) :: [map()]` to the `Cairnloop.Auditor` module.
    - In the `Cairnloop.Auditor.NoOp` module (inside the same file), implement `@impl true def list_events(_opts), do: []`.
    - Create or update test/cairnloop/auditor_test.exs to verify Cairnloop.Auditor.NoOp.list_events/1 returns an empty list.
  </action>
  <verify>
    <automated>mix test test/cairnloop/auditor_test.exs</automated>
  </verify>
  <done>The `Cairnloop.Auditor` behaviour has a `list_events/1` callback and `NoOp` implements it, with passing tests.</done>
</task>

</tasks>

<verification>
mix compile --warnings-as-errors
mix test
</verification>

<success_criteria>
Plugs compile cleanly, tests pass, and Auditor behaviour includes read support.
</success_criteria>

<output>
Create `.planning/phases/35-audit-operations-support/35-01-SUMMARY.md` when done
</output>
