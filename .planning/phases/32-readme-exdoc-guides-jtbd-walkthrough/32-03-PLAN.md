---
phase: 32-readme-exdoc-guides-jtbd-walkthrough
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [DOC-01, DOC-04]
must_haves:
  truths:
    - "An adopter reading README.md sees `mix cairnloop.install` in the Installation section before any bare deps block."
    - "The README no longer contains the 'If [available in Hex]' hedge or the Mermaid diagram, and the inline telemetry/Notifier code is gone (moved to the host-integration guide)."
    - "CHANGELOG.md's existing `## [Unreleased]` section is populated with the vM014 entry (Phases 27-32)."
  artifacts:
    - path: "README.md"
      provides: "Igniter-first front door: badges → tagline → Installation (installer first) → Why → What → Explore the guides → contribute + license"
      contains: "mix cairnloop.install"
    - path: "CHANGELOG.md"
      provides: "vM014 entry under the existing [Unreleased] section, Keep-a-Changelog format"
      contains: "README rewritten"
  key_links:
    - from: "README.md Explore the guides section"
      to: "the four HexDocs guide pages"
      via: "hexdocs.pm/cairnloop/0N-*.html links"
      pattern: "hexdocs.pm/cairnloop/01-quickstart"
    - from: "CHANGELOG.md [Unreleased]"
      to: "vM014 Phases 27-32 summary"
      via: "### Added bullets"
      pattern: "Phase 32"
---

<objective>
Restructure `README.md` into an Igniter-first front door (D-03) and populate the existing
`## [Unreleased]` CHANGELOG section with the vM014 entry (D-06). Both are independent text files
that can be edited in parallel with the guide plans.

Purpose: The front door matches the shipped install path and the release narrative records the
adopter-surface improvements. (DOC-01, DOC-04)
Output: Restructured `README.md`; populated `## [Unreleased]` block in `CHANGELOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-readme-exdoc-guides-jtbd-walkthrough/32-CONTEXT.md
@.planning/phases/32-readme-exdoc-guides-jtbd-walkthrough/32-PATTERNS.md

<interfaces>
<!-- Authoritative facts — extracted from source. Do NOT paraphrase from memory. -->

README full restructure (D-03), section order:
  1. Title + badges — KEEP verbatim (current README lines 1-5: title "# Cairnloop 🏔️" + the 3 badges).
  2. One-line tagline — tighten the existing tagline prose (lines 7-9).
  3. Installation — LEAD with the installer. Two-step form (VERIFIED): `mix deps.get` then
     `mix cairnloop.install`. State the installer adds `{:cairnloop, "~> 0.1.0"}` and generates a
     `create_cairnloop_tables` migration against the detected Ecto repo. Below it, a secondary
     "### Manual install (without Igniter)" subsection with the bare deps block
     `{:cairnloop, "~> 0.1.0"}`. Version pin is load-bearing: `~> 0.1.0` (matches install.ex + mix.exs).
  4. "Why Cairnloop?" — trim to 4-5 crisp bullets (reuse/condense the existing bullets lines 13-18).
  5. "What it does" — 3-4 sentence prose. REMOVE the Mermaid diagram entirely (lines 22-34).
  6. "Explore the guides" — link the four HexDocs guide pages:
       https://hexdocs.pm/cairnloop/01-quickstart.html
       https://hexdocs.pm/cairnloop/02-jtbd-walkthrough.html
       https://hexdocs.pm/cairnloop/03-host-integration.html
       https://hexdocs.pm/cairnloop/04-troubleshooting.html
  7. Contribute + license — at the bottom.

REMOVALS (D-03):
  - The "If [available in Hex](https://hex.pm/docs/publish)..." hedge (current README lines 36-48) —
    the package IS live on Hex; remove the hedge.
  - The Mermaid ```mermaid graph TD ...``` diagram (lines 22-34) — stale; remove, do not update.
  - The inline telemetry + Notifier sections (lines 50-137) — these MOVED to
    guides/03-host-integration.md in plan 32-02; DELETE them from README (do not duplicate).
  - The "Documentation can be generated with ExDoc..." boilerplate paragraph (line 48) — drop it.

KEEP: the existing link to docs/cairnloop-jtbd-and-user-flows.md may be kept or dropped at
discretion (D-04 keeps that file as-is; it is an internal memo, not an ExDoc extra). Prefer
dropping the inline README pointer in favor of the "Explore the guides" section, but do not delete
the docs/ file.

CHANGELOG (D-06): the file ALREADY has an empty `## [Unreleased]` at line 8 (above `## [0.1.0]`).
Populate THAT section in place — do NOT add a second `## [Unreleased]` header. Use a `### Added`
subsection with these 7 verbatim bullets (Keep-a-Changelog format, backtick-wrapped identifiers):
  - Realistic demo fixtures: 12–16 seeded conversations spanning all JTBD states, 5 KB articles with revisions, 3 GapCandidates, 1 ArticleSuggestion ready for review (Phase 27)
  - Customer `/chat` widget wired to real ingress via `Cairnloop.Channels.WidgetSocket` + `WidgetChannel`; two-tab demo (Phase 28)
  - Brand-token CSS extraction: `prompts/cairnloop.css` `:root` block in example app; `var(--cl-token)` without hex fallback; negative-grep gate (Phase 29, D-10 closure)
  - KB editorial polish: shared nav shell across 4 KB routes, "Create new article" affordance, gap-evidence sidebar in Editor, calm copy on SuggestionReview handoff (Phase 30)
  - T-10-09 and T-10-11 closure: `EditorHandoff` double-layer gate (DB `manual_edit_opened_at` timestamp + signed token assertion) prevents preloading `proposed_markdown` without deliberate handoff (Phase 30)
  - Golden-path JTBD smoke test in CI: `golden_path_test.exs` (E2E-01) + `widget_channel_test.exs` (E2E-02) under `mix test.integration` (Phase 31)
  - README rewritten as an Igniter-first front door; four ExDoc guides (quickstart, JTBD walkthrough, host integration, troubleshooting) published to HexDocs (Phase 32)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure README.md (Igniter-first front door)</name>
  <files>README.md</files>
  <read_first>
    - README.md (CURRENT full content — badges to keep, hedge/diagram/telemetry to remove)
    - lib/mix/tasks/cairnloop/install.ex (install one-liner + version pin authority)
    - examples/cairnloop_example/README.md (cross-reference for install/mount phrasing)
    - prompts/cairnloop_brand_book.md (calm, reason-forward tagline + "Why" bullets)
  </read_first>
  <action>
    Rewrite README.md following the D-03 section order in interfaces: (1) keep the title
    `# Cairnloop 🏔️` and the three badges verbatim; (2) tighten the one-line tagline; (3) an
    `## Installation` section that LEADS with the two-step installer (`mix deps.get` then
    `mix cairnloop.install`), states the installer adds `{:cairnloop, "~> 0.1.0"}` and generates a
    `create_cairnloop_tables` migration, then a `### Manual install (without Igniter)` subsection
    with the bare `{:cairnloop, "~> 0.1.0"}` deps block; (4) a trimmed `## Why Cairnloop?` with 4-5
    bullets; (5) a 3-4 sentence `## What it does` prose paragraph; (6) an `## Explore the guides`
    section linking the four `https://hexdocs.pm/cairnloop/0N-*.html` pages (quickstart, jtbd-walkthrough,
    host-integration, troubleshooting); (7) contribute + license at the bottom. REMOVE the Mermaid
    diagram, the "If [available in Hex]" hedge, the "Documentation can be generated with ExDoc"
    paragraph, and the entire inline telemetry + Notifier code (that content now lives in
    guides/03-host-integration.md — do not duplicate it). Use the exact version pin `~> 0.1.0`. Do
    NOT delete docs/cairnloop-jtbd-and-user-flows.md (D-04). Keep prose calm and reason-forward per
    the brand book.
  </action>
  <verify>
    <automated>grep -q "mix cairnloop.install" README.md && grep -q "~> 0.1.0" README.md && grep -q "hexdocs.pm/cairnloop/01-quickstart" README.md && grep -q "hexdocs.pm/cairnloop/02-jtbd-walkthrough" README.md && grep -q "hexdocs.pm/cairnloop/03-host-integration" README.md && grep -q "hexdocs.pm/cairnloop/04-troubleshooting" README.md && ! grep -q "available in Hex" README.md && ! grep -q '```mermaid' README.md && ! grep -q "cairnloop-apm-tracker" README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `mix cairnloop.install` and the version pin `~> 0.1.0`.
    - The Installation section leads with the installer: the line containing `mix cairnloop.install` appears BEFORE any line containing `def deps do` (verifiable: `grep -n "mix cairnloop.install\|def deps do" README.md` shows the install command first).
    - README.md contains all four `hexdocs.pm/cairnloop/0N-*.html` guide links.
    - `grep "available in Hex" README.md` returns nothing (hedge removed).
    - `grep '```mermaid' README.md` returns nothing (diagram removed).
    - `grep "cairnloop-apm-tracker" README.md` returns nothing (inline telemetry block removed/relocated).
    - README.md still contains the three existing badge lines (`img.shields.io/hexpm`, `hexdocs`, `actions/workflows/ci.yml`).
  </acceptance_criteria>
  <done>README is an Igniter-first front door: installer leads Installation, guides linked, Mermaid/hedge/inline-telemetry removed, badges + version pin intact.</done>
</task>

<task type="auto">
  <name>Task 2: Populate the [Unreleased] CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (CURRENT content — the empty `## [Unreleased]` at line 8 and the `## [0.1.0]` format exemplar)
  </read_first>
  <action>
    Populate the EXISTING empty `## [Unreleased]` section (it already sits above `## [0.1.0]` — do
    NOT insert a second `## [Unreleased]` header). Add a `### Added` subsection containing the 7
    verbatim vM014 bullets from D-06 (see interfaces), in Phase 27→32 order, matching the
    Keep-a-Changelog `### Added` / `- ` / backtick-wrapped-identifier style of the existing
    `## [0.1.0]` block. Do not modify the `## [0.1.0]` block or the file header.
  </action>
  <verify>
    <automated>grep -q "## \[Unreleased\]" CHANGELOG.md && grep -q "README rewritten" CHANGELOG.md && grep -q "Phase 32" CHANGELOG.md && grep -q "EditorHandoff" CHANGELOG.md && grep -q "golden_path_test.exs" CHANGELOG.md && [ "$(grep -c '## \[Unreleased\]' CHANGELOG.md)" -eq 1 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md has exactly ONE `## [Unreleased]` header (`grep -c '## \[Unreleased\]' CHANGELOG.md` == 1).
    - The `## [Unreleased]` section now contains a `### Added` subsection with all 7 vM014 bullets (verifiable: contains `Phase 27`, `Phase 28`, `Phase 29`, `Phase 30`, `Phase 31`, `Phase 32`, `EditorHandoff`, `golden_path_test.exs`, and `README rewritten`).
    - The `## [0.1.0] - 2026-05-25` block is unchanged.
  </acceptance_criteria>
  <done>The existing [Unreleased] section carries the 7-bullet vM014 entry in Keep-a-Changelog format; no duplicate header introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan. No executable surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Information Disclosure | README code examples | mitigate | Placeholders only — no real secrets/tokens in the deps/install snippets. |

No blocking threats — documentation phase with nil attack surface.
</threat_model>

<verification>
- README leads Installation with `mix cairnloop.install`; Mermaid diagram, "available in Hex" hedge, and inline telemetry block all removed; four guide links present; badges + `~> 0.1.0` intact.
- CHANGELOG `## [Unreleased]` populated with the 7-bullet vM014 entry; exactly one `[Unreleased]` header; `[0.1.0]` block untouched.
</verification>

<success_criteria>
README.md is an Igniter-first front door (DOC-01) and CHANGELOG.md carries the vM014 entry (DOC-04). The guide links resolve once the guides ship (plans 32-01/32-02) and the `mix.exs` wiring lands (plan 32-04).
</success_criteria>

<output>
Create `.planning/phases/32-readme-exdoc-guides-jtbd-walkthrough/32-03-SUMMARY.md` when done.
</output>
