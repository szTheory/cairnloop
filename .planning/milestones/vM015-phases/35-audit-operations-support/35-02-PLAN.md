---
phase: "35"
plan: "02"
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - lib/cairnloop/web/audit_log_live.ex
  - lib/cairnloop/router.ex
  - lib/cairnloop/governance.ex
  - lib/cairnloop/web/conversation_live.ex
  - test/cairnloop/web/audit_log_live_test.exs
  - test/cairnloop/governance_test.exs
  - test/cairnloop/web/conversation_live_test.exs
autonomous: true
requirements:
  - AUDIT-01
  - TECH-01
must_haves:
  truths:
    - Operator can view and filter a timeline of system actions in the new Audit Log UI.
    - Operator can page through long lists of governed actions in the conversation sidebar.
  artifacts:
    - path: lib/cairnloop/web/audit_log_live.ex
      provides: "Searchable audit timeline"
    - path: lib/cairnloop/web/conversation_live.ex
      provides: "Governed actions rail plain-assign pagination"
  key_links:
    - from: lib/cairnloop/web/conversation_live.ex
      to: lib/cairnloop/governance.ex
      via: "limit-based Ecto queries"
---

<objective>
Build the `AuditLogLive` UI and implement plain-assign pagination for the governed actions rail.

Purpose: Provide visibility into system actions and prevent unbounded DOM growth.
Output: AuditLogLive view and pagination logic for ConversationLive with tests.
</objective>

<context>
@.planning/phases/35-audit-operations-support/35-RESEARCH.md
@.planning/phases/35-audit-operations-support/35-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator→AuditLog | Operator accesses sensitive logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02 | Information Disclosure | `AuditLogLive` | accept | View is protected by `cairnloop_dashboard` router auth in host. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Audit Log UI (AUDIT-01 UI)</name>
  <files>lib/cairnloop/web/audit_log_live.ex, lib/cairnloop/router.ex, test/cairnloop/web/audit_log_live_test.exs</files>
  <action>
    - Create `Cairnloop.Web.AuditLogLive`. In `mount/3`, load events via `Application.get_env(:cairnloop, :auditor, Cairnloop.Auditor.NoOp).list_events([])`.
    - Implement a `render/1` function to display the timeline of events.
    - Update `lib/cairnloop/router.ex` to mount `AuditLogLive` at `/audit-log` within the `cairnloop_dashboard/2` macro, like `live("/audit-log", Cairnloop.Web.AuditLogLive, :index, as: :cairnloop_audit_log)`.
    - Create test/cairnloop/web/audit_log_live_test.exs to verify that AuditLogLive mounts successfully and renders audit events.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/audit_log_live_test.exs</automated>
  </verify>
  <done>AuditLogLive view exists and is routable, and is tested.</done>
</task>

<task type="auto">
  <name>Task 2: Governed Actions Rail Pagination (TECH-01)</name>
  <files>lib/cairnloop/governance.ex, lib/cairnloop/web/conversation_live.ex, test/cairnloop/governance_test.exs, test/cairnloop/web/conversation_live_test.exs</files>
  <action>
    - In `lib/cairnloop/governance.ex`, update `list_proposals_for_conversation/2` to accept `opts \\ []`. Extract `:limit` using `Keyword.get(opts, :limit)`. If present, append an Ecto `limit(^limit)` clause to the query.
    - In test/cairnloop/governance_test.exs, add a test verifying list_proposals_for_conversation/2 respects the :limit option.
    - In `lib/cairnloop/web/conversation_live.ex`, add `:governed_actions_limit` to the socket assigns (e.g., start at 10) in `mount/3` or where proposals are initialized. Use this limit when fetching proposals.
    - Add a `handle_event("load_more_actions", _, socket)` that increases the limit state by 10 and reloads the proposals using plain assigns (no `Phoenix.LiveView.stream`).
    - Update the governed-actions rail template to show a "Load more" button if the count of actions equals the limit.
    - In test/cairnloop/web/conversation_live_test.exs, add a test simulating the "load_more_actions" event and verifying the governed actions list expands up to the new limit.
  </action>
  <verify>
    <automated>mix test test/cairnloop/governance_test.exs test/cairnloop/web/conversation_live_test.exs</automated>
  </verify>
  <done>Governed actions list is paginated via plain assigns and the behavior is fully tested.</done>
</task>

</tasks>

<verification>
mix test
mix compile --warnings-as-errors
</verification>

<success_criteria>
AuditLogLive is reachable, governed actions rail implements limit-based pagination, and tests pass for both features.
</success_criteria>

<output>
Create `.planning/phases/35-audit-operations-support/35-02-SUMMARY.md` when done
</output>
