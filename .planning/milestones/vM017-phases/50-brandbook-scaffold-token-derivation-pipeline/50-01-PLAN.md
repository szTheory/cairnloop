---
phase: 50-brandbook-scaffold-token-derivation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/derive_brandbook_tokens.exs
  - scripts/verify_brandbook_file_load.mjs
  - test/cairnloop/web/brandbook_scaffold_test.exs
  - brandbook/index.html
  - brandbook/TOKENS.md
  - brandbook/assets/css/tokens.css
  - brandbook/assets/css/brandbook.css
  - brandbook/color/swatches.json
  - brandbook/logo/.gitkeep
  - brandbook/raster/.gitkeep
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
user_setup: []
must_haves:
  truths:
    - "BOOK-01: brandbook/index.html opens directly from file:// with no console errors, page errors, failed requests, or remote network requests."
    - "BOOK-01: brandbook uses only relative/local paths and keeps fonts on graceful local fallback stacks."
    - "BOOK-02: brandbook/assets/css/tokens.css and brandbook/color/swatches.json are generated from priv/static/cairnloop.css :root and [data-theme=\"dark\"], not manually forked."
    - "BOOK-02: brandbook/TOKENS.md names the canonical source, regeneration command, check command, and drift-prevention contract."
    - "BOOK-01/BOOK-02: brandbook/ is git-tracked collateral and remains absent from mix.exs package files."
  artifacts:
    - path: "scripts/derive_brandbook_tokens.exs"
      provides: "Repo-local Elixir generator/checker for derived brandbook token mirrors"
      commands: ["mix run scripts/derive_brandbook_tokens.exs", "mix run scripts/derive_brandbook_tokens.exs --check"]
    - path: "scripts/verify_brandbook_file_load.mjs"
      provides: "Standalone Playwright file:// browser proof for brandbook/index.html"
      commands: ["node scripts/verify_brandbook_file_load.mjs"]
    - path: "test/cairnloop/web/brandbook_scaffold_test.exs"
      provides: "DB-free ExUnit source/layout/package guard for the brandbook scaffold"
      module: "Cairnloop.Web.BrandbookScaffoldTest"
    - path: "brandbook/index.html"
      provides: "Compact static brandbook scaffold shell"
    - path: "brandbook/TOKENS.md"
      provides: "Maintainer token provenance and regeneration documentation"
    - path: "brandbook/assets/css/tokens.css"
      provides: "Generated --cl-* token mirror from canonical CSS"
    - path: "brandbook/assets/css/brandbook.css"
      provides: "Scaffold-only layout CSS consuming generated --cl-* tokens"
    - path: "brandbook/color/swatches.json"
      provides: "Generated grouped swatch data with provenance and display values"
    - path: "brandbook/logo/.gitkeep"
      provides: "Tracked relative destination for Phase 51 logo brandbook inputs"
    - path: "brandbook/raster/.gitkeep"
      provides: "Tracked relative destination for Phase 51/52 allowed raster inputs"
  key_links:
    - from: "scripts/derive_brandbook_tokens.exs"
      to: "priv/static/cairnloop.css"
      via: "File.read! parses :root and [data-theme=\"dark\"] blocks"
      pattern: "priv/static/cairnloop\\.css"
    - from: "scripts/derive_brandbook_tokens.exs"
      to: "brandbook/assets/css/tokens.css"
      via: "Generated bytes written and compared in --check mode"
      pattern: "brandbook/assets/css/tokens\\.css"
    - from: "scripts/derive_brandbook_tokens.exs"
      to: "brandbook/color/swatches.json"
      via: "Jason.encode! emits deterministic grouped swatch JSON"
      pattern: "brandbook/color/swatches\\.json"
    - from: "brandbook/index.html"
      to: "brandbook/assets/css/tokens.css"
      via: "Relative stylesheet link"
      pattern: "assets/css/tokens\\.css"
    - from: "brandbook/index.html"
      to: "brandbook/assets/css/brandbook.css"
      via: "Relative stylesheet link"
      pattern: "assets/css/brandbook\\.css"
    - from: "scripts/verify_brandbook_file_load.mjs"
      to: "brandbook/index.html"
      via: "pathToFileURL navigates Chromium to absolute file:// URL"
      pattern: "pathToFileURL"
---

<objective>
Create the Phase 50 brandbook scaffold and token-derivation substrate for BOOK-01 and BOOK-02.

Purpose: Phase 51 needs a self-contained, offline-safe brandbook folder and deterministic token mirrors before it can assemble the full HTML brand book.
Output: A tracked `brandbook/` folder with generated token artifacts, a compact static `index.html`, maintainer provenance docs, and automated source/browser/package verification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-CONTEXT.md
@.planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-RESEARCH.md
@.planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-PATTERNS.md
@.planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-VALIDATION.md
@.planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-UI-SPEC.md
@.planning/phases/48-token-evolution-lock-propagate/48-01-SUMMARY.md
@.planning/phases/48-token-evolution-lock-propagate/48-02-SUMMARY.md
@.planning/phases/49-chosen-logo-finalization-asset-family/49-03-SUMMARY.md
@priv/static/cairnloop.css
@prompts/cairnloop.tokens.json
@test/cairnloop/web/token_drift_test.exs
@test/cairnloop/web/brand_token_gate_test.exs
@examples/cairnloop_example/screenshots/capture.mjs
@mix.exs
</context>

## Artifacts This Phase Produces

| Artifact | Type | Created symbol / field / command | Purpose |
| --- | --- | --- | --- |
| `scripts/derive_brandbook_tokens.exs` | Elixir script | `mix run scripts/derive_brandbook_tokens.exs` | Generate `tokens.css` and `swatches.json` from canonical CSS per D-50-01 and BOOK-02. |
| `scripts/derive_brandbook_tokens.exs` | CLI flag | `--check` | Fail loudly on drift, missing source blocks, malformed output, unresolved required token groups, or stale committed bytes per D-50-02. |
| `scripts/derive_brandbook_tokens.exs` | Parser helpers | `css_block/2`, `declarations/1`, alias resolution helper | Extract `:root` and `[data-theme="dark"]` `--cl-*` declarations from `priv/static/cairnloop.css`. |
| `brandbook/assets/css/tokens.css` | Generated CSS | `:root` and `[data-theme="dark"]` `--cl-*` declarations | Derived token mirror consumed by the static brandbook. |
| `brandbook/color/swatches.json` | Generated JSON | `schema_version`, `source_file`, `generated_by`, `check_command`, `groups.primitive`, `groups.semantic_light`, `groups.semantic_dark` | Reviewable swatch data with provenance and grouped rows per D-50-05 through D-50-08. |
| `brandbook/index.html` | Static HTML | relative stylesheet links to `assets/css/tokens.css` and `assets/css/brandbook.css` | Offline scaffold shell proving BOOK-01 local load behavior. |
| `brandbook/assets/css/brandbook.css` | Static CSS | scaffold layout classes consuming `--cl-*` variables | Compact UI shell styling per the Phase 50 UI contract. |
| `brandbook/TOKENS.md` | Documentation | regeneration command, check command, canonical source note, drift contract | Maintainer handoff proving derived-token discipline for BOOK-02. |
| `brandbook/logo/.gitkeep` | Directory marker | tracked `brandbook/logo/` path | Phase 51 relative destination for logo gallery inputs. |
| `brandbook/raster/.gitkeep` | Directory marker | tracked `brandbook/raster/` path | Phase 51/52 relative destination for allowed favicon/OG raster inputs. |
| `scripts/verify_brandbook_file_load.mjs` | Node script | `node scripts/verify_brandbook_file_load.mjs` | Playwright `file://` proof with console, page error, request, and requestfailed collection per D-50-14 through D-50-16. |
| `test/cairnloop/web/brandbook_scaffold_test.exs` | ExUnit test module | `Cairnloop.Web.BrandbookScaffoldTest` | DB-free source/layout/package guard for required files, forbidden dependencies, provenance, and package exclusion. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the canonical token derivation script and generated mirrors</name>
  <files>scripts/derive_brandbook_tokens.exs, brandbook/assets/css/tokens.css, brandbook/color/swatches.json</files>
  <read_first>
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-CONTEXT.md
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-RESEARCH.md
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-PATTERNS.md
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-UI-SPEC.md
    priv/static/cairnloop.css
    prompts/cairnloop.tokens.json
    test/cairnloop/web/token_drift_test.exs
    mix.exs
    scripts/derive_brandbook_tokens.exs
    brandbook/assets/css/tokens.css
    brandbook/color/swatches.json
  </read_first>
  <behavior>
    - RED: Before generated files exist, `mix run scripts/derive_brandbook_tokens.exs --check` fails with a clear stale-or-missing output message.
    - GREEN: Running `mix run scripts/derive_brandbook_tokens.exs` creates `brandbook/assets/css/tokens.css` and `brandbook/color/swatches.json` from `priv/static/cairnloop.css`.
    - GREEN: Running `mix run scripts/derive_brandbook_tokens.exs --check` exits 0 when generated bytes match committed files, and exits nonzero after any committed generated byte is changed.
    - GREEN: Missing `:root`, missing `[data-theme="dark"]`, absent required color groups, unresolved alias loops, or non-`--cl-*` token extraction fail closed with actionable messages.
  </behavior>
  <action>Create `scripts/derive_brandbook_tokens.exs` per D-50-01 through D-50-08 and BOOK-02. Use repo-relative paths, `File.read!/1`, `File.write!/2`, `File.mkdir_p!/1`, and existing `Jason.encode!/2`; do not add dependencies, do not create `lib/mix/tasks`, and do not introduce Style Dictionary, Node token tooling, POSIX parsing, or a second source of truth. Parse only the canonical `:root` and `[data-theme="dark"]` blocks in `priv/static/cairnloop.css`, restrict extracted declarations to `--cl-*`, preserve raw token declarations in generated CSS, resolve simple `var(--cl-*)` aliases only for JSON display values, and make `--check` compare expected generated bytes against committed `tokens.css` and `swatches.json`. Emit `swatches.json` with stable ordering and fields `schema_version`, `source_file`, `generated_by`, `check_command`, and `groups` containing `primitive`, `semantic_light`, and `semantic_dark`; rows preserve `token`, raw `value`, `group`, role/description where available, theme where relevant, and resolved display hex when possible. Do not compute or store pairwise contrast badges per D-50-08.</action>
  <acceptance_criteria>
    - `scripts/derive_brandbook_tokens.exs` exists outside `lib/` and supports both default generation and `--check`.
    - `brandbook/assets/css/tokens.css` starts with a provenance comment naming `priv/static/cairnloop.css`, the generation command, and the check command, then contains only generated `:root` and `[data-theme="dark"]` token blocks.
    - `brandbook/assets/css/tokens.css` preserves canonical `--cl-*` token names and values from `priv/static/cairnloop.css`; it does not include `.cl-` component selectors or `@media` blocks.
    - `brandbook/color/swatches.json` includes `schema_version`, `source_file`, `generated_by`, `check_command`, and `groups.primitive`, `groups.semantic_light`, `groups.semantic_dark`.
    - `brandbook/color/swatches.json` does not include contrast badge fields or pairwise contrast matrices.
    - `mix run scripts/derive_brandbook_tokens.exs --check` passes after generation and fails if either generated output is edited by hand.
  </acceptance_criteria>
  <verify>
    <automated>mix run scripts/derive_brandbook_tokens.exs && mix run scripts/derive_brandbook_tokens.exs --check</automated>
    <automated>test -f brandbook/assets/css/tokens.css && test -f brandbook/color/swatches.json && rg -n 'Generated from priv/static/cairnloop.css|Regenerate with: mix run scripts/derive_brandbook_tokens.exs|Check drift with: mix run scripts/derive_brandbook_tokens.exs --check' brandbook/assets/css/tokens.css brandbook/color/swatches.json</automated>
    <automated>test "$(rg -n '^\\s*\\.(cl|brandbook)-|^\\s*@media' brandbook/assets/css/tokens.css | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>BOOK-02 token derivation exists as a repeatable repo-local Elixir script, generated token CSS and swatch JSON are committed, and `--check` proves they are not hand-maintained forks.</done>
</task>

<task type="auto">
  <name>Task 2: Create the offline static brandbook scaffold and token provenance documentation</name>
  <files>brandbook/index.html, brandbook/assets/css/brandbook.css, brandbook/TOKENS.md, brandbook/logo/.gitkeep, brandbook/raster/.gitkeep</files>
  <read_first>
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-CONTEXT.md
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-UI-SPEC.md
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-PATTERNS.md
    .planning/phases/48-token-evolution-lock-propagate/48-02-SUMMARY.md
    .planning/phases/49-chosen-logo-finalization-asset-family/49-03-SUMMARY.md
    priv/static/cairnloop.css
    logo/USAGE.md
    brandbook/index.html
    brandbook/assets/css/brandbook.css
    brandbook/TOKENS.md
    brandbook/logo/.gitkeep
    brandbook/raster/.gitkeep
  </read_first>
  <action>Create the approved `brandbook/` layout for BOOK-01 and BOOK-02: `index.html`, `assets/css/tokens.css`, `assets/css/brandbook.css`, `logo/`, `raster/`, and `color/swatches.json`. In `index.html`, link only `./assets/css/tokens.css` and `./assets/css/brandbook.css`; include the exact required copy from D-50-11; render only the compact title/provenance header, three status cells, small live token preview, restrained type specimens, minimal light/dark token proof block, folder/file readiness list, and footer provenance note per D-50-09 through D-50-13. In `brandbook.css`, use brandbook-specific classes that consume `--cl-*` tokens, the locked fallback type stacks, 1120px max width, 16px mobile and 32px desktop gutters, approved type sizes, visible token-driven focus rings for any focusable links, and reduced-motion handling if transitions exist. In `TOKENS.md`, document the canonical source, generated outputs, regeneration command, check command, drift-prevention rule, package boundary, and Phase 51/52 handoff. Keep page copy operational; do not render the full logo gallery, contrast badge tables, downloads, light/dark JS toggle, README/example-app wiring, remote assets, analytics, registries, or live-text recreation of the wordmark.</action>
  <acceptance_criteria>
    - `brandbook/index.html` contains exact labels `Cairnloop brand book`, `Canonical source: priv/static/cairnloop.css :root`, `Token status: derived from canonical CSS`, `Network dependency: none`, `Full brand book assembly is Phase 51`, and `Brandbook is git-tracked and unshipped`.
    - `brandbook/index.html` links `./assets/css/tokens.css` and `./assets/css/brandbook.css` or equivalent relative paths that work from `file://`.
    - `brandbook/assets/css/brandbook.css` uses `var(--cl-...)` tokens for color, typography, spacing, focus, and surface styling; any palette literals are limited to token fallback values only where needed for resilience.
    - `brandbook/TOKENS.md` names `priv/static/cairnloop.css`, `mix run scripts/derive_brandbook_tokens.exs`, `mix run scripts/derive_brandbook_tokens.exs --check`, `brandbook/assets/css/tokens.css`, and `brandbook/color/swatches.json`.
    - `brandbook/logo/.gitkeep` and `brandbook/raster/.gitkeep` exist so the approved folder layout is git-trackable.
    - Phase 51/52 content remains out of scope: no complete logo gallery, do/don't panels, light/dark toggle, README wiring, example-app wiring, favicon metadata, or OG metadata is added.
  </acceptance_criteria>
  <verify>
    <automated>test -f brandbook/index.html && test -f brandbook/assets/css/brandbook.css && test -f brandbook/TOKENS.md && test -f brandbook/logo/.gitkeep && test -f brandbook/raster/.gitkeep</automated>
    <automated>rg -n 'Cairnloop brand book|Canonical source: priv/static/cairnloop.css :root|Token status: derived from canonical CSS|Network dependency: none|Full brand book assembly is Phase 51|Brandbook is git-tracked and unshipped' brandbook/index.html</automated>
    <automated>rg -n 'priv/static/cairnloop.css|mix run scripts/derive_brandbook_tokens.exs|mix run scripts/derive_brandbook_tokens.exs --check|brandbook/assets/css/tokens.css|brandbook/color/swatches.json' brandbook/TOKENS.md</automated>
    <automated>test "$(rg -n 'README.md|root.html.heex|og:image|favicon metadata|light/dark toggle|download=' brandbook/index.html brandbook/assets/css/brandbook.css brandbook/TOKENS.md | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>BOOK-01 static scaffold opens from local files by construction, consumes derived tokens, presents only Phase 50 proof content, and gives Phase 51 a tracked folder/data substrate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add source, package, and file-url browser verification gates</name>
  <files>scripts/verify_brandbook_file_load.mjs, test/cairnloop/web/brandbook_scaffold_test.exs</files>
  <read_first>
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-CONTEXT.md
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-RESEARCH.md
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-VALIDATION.md
    .planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-PATTERNS.md
    examples/cairnloop_example/screenshots/capture.mjs
    test/cairnloop/web/brand_token_gate_test.exs
    test/cairnloop/web/token_drift_test.exs
    mix.exs
    brandbook/index.html
    brandbook/assets/css/tokens.css
    brandbook/assets/css/brandbook.css
    brandbook/TOKENS.md
    scripts/verify_brandbook_file_load.mjs
    test/cairnloop/web/brandbook_scaffold_test.exs
  </read_first>
  <behavior>
    - RED: Before the brandbook scaffold exists, `mix test test/cairnloop/web/brandbook_scaffold_test.exs` fails because required files/directories are absent.
    - GREEN: The ExUnit test passes when all required files exist, forbidden remote/root dependencies are absent, provenance labels are present, derivation check passes, and `mix.exs` excludes `brandbook/`.
    - GREEN: `node scripts/verify_brandbook_file_load.mjs` opens `brandbook/index.html` through an absolute file URL and fails on console errors, page errors, failed requests, or any non-local network request.
  </behavior>
  <action>Create `scripts/verify_brandbook_file_load.mjs` per D-50-14 through D-50-17 and BOOK-01, using the existing Playwright dependency from `examples/cairnloop_example/assets` without adding or upgrading packages. The script must build an absolute file URL for `brandbook/index.html`, launch Chromium with deterministic viewport and reduced motion, register `console`, `pageerror`, `request`, and `requestfailed` listeners before navigation, assert required page text and stylesheet links exist after navigation, and exit nonzero with aggregated diagnostics on any failure. Create `test/cairnloop/web/brandbook_scaffold_test.exs` as a pure async DB-free ExUnit module that checks required layout files/directories, the forbidden dependency/path patterns from D-50-16, required copy/provenance labels, `TOKENS.md` regeneration/check notes, `mix.exs` package exclusion, and `mix run scripts/derive_brandbook_tokens.exs --check`. Do not route this through Phoenix, do not add a human verification checkpoint, and do not expand CI or release gates unless all checks stay local and uncoupled from the shipped package.</action>
  <acceptance_criteria>
    - `scripts/verify_brandbook_file_load.mjs` imports Playwright from the existing dependency path or otherwise resolves the existing locked install without a new package install.
    - Browser verification captures console errors, page errors, all requests, and failed requests before navigating.
    - Browser verification fails if the page issues a remote request, fails to load a local dependency, or misses the required Phase 50 labels.
    - `test/cairnloop/web/brandbook_scaffold_test.exs` is async, DB-free, and does not start Repo, Endpoint, Phoenix server, or the example app.
    - The ExUnit test proves required brandbook files/directories exist, `TOKENS.md` documents derivation, generated outputs pass `--check`, forbidden remote/import/root dependencies are absent from `brandbook/`, and `brandbook/` is absent from `mix.exs` package files.
    - No package install, npm lockfile change, CI expansion, Phoenix route, README change, example-app change, favicon wiring, or OG wiring is introduced.
  </acceptance_criteria>
  <verify>
    <automated>mix run scripts/derive_brandbook_tokens.exs --check</automated>
    <automated>mix test test/cairnloop/web/brandbook_scaffold_test.exs</automated>
    <automated>node scripts/verify_brandbook_file_load.mjs</automated>
    <automated>mix compile --warnings-as-errors && mix test</automated>
    <automated>rg -n 'files: ~w\\(lib priv guides mix\\.exs README\\.md LICENSE CHANGELOG\\.md\\)' mix.exs && test "$(rg -n 'files: .*brandbook' mix.exs | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(rg -n 'https?://|(^|[^:])//|@import|<iframe|sendBeacon|url\\((['\\''\"]?)https?:|url\\((['\\''\"]?)/' brandbook | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test -z "$(git diff --name-only -- README.md examples/cairnloop_example/priv/static/images/logo.svg examples/cairnloop_example/lib/cairnloop_example_web/components/layouts/root.html.heex mix.exs .github/workflows/ci.yml examples/cairnloop_example/assets/package-lock.json examples/cairnloop_example/assets/package.json)"</automated>
  </verify>
  <done>BOOK-01 and BOOK-02 have automated source, derivation, browser, and package-boundary gates; no manual UAT or shipped package coupling remains.</done>
</task>

</tasks>

## Source Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | Phase 50 | Self-contained `brandbook/` folder opens from `file://`; tokens derived from canonical `cairnloop.css` `:root` | 50-01 | COVERED | Tasks 1-3 cover derivation, scaffold, and automated proof. |
| REQ | BOOK-01 | Self-contained `brandbook/` opens from `file://` with no network dependency and no console or failed-network errors | 50-01 | COVERED | Tasks 2-3 create the static scaffold and Playwright proof. |
| REQ | BOOK-02 | `tokens.css` derived from canonical CSS and documented with regeneration note | 50-01 | COVERED | Tasks 1-3 create generator, outputs, docs, and drift checks. |
| RESEARCH | Standard stack | Use Elixir/Mix + existing Jason; no package installs | 50-01 | COVERED | Task 1 uses existing Elixir/Jason and forbids new token tooling. |
| RESEARCH | Browser proof | Use Playwright file-url harness with console/page/network listeners | 50-01 | COVERED | Task 3 creates `scripts/verify_brandbook_file_load.mjs`. |
| RESEARCH | Package exclusion | Keep `brandbook/` out of Hex package files | 50-01 | COVERED | Task 3 tests package allowlist; no `mix.exs` modification planned. |
| RESEARCH | No schema push | No ORM/schema files detected in ROADMAP, CONTEXT, or RESEARCH for this phase | 50-01 | COVERED | Schema push task intentionally absent because no schema-relevant files are in scope. |
| CONTEXT | D-50-01 | Repo-local Elixir derivation script invoked with `mix run` | 50-01 | COVERED | Task 1 action cites D-50-01. |
| CONTEXT | D-50-02 | Generate/check `tokens.css` and `swatches.json` from canonical `:root` and dark block with `--check` | 50-01 | COVERED | Task 1 action cites D-50-02. |
| CONTEXT | D-50-03 | Do not place helper under `lib/mix/tasks` | 50-01 | COVERED | Task 1 forbids `lib/mix/tasks`. |
| CONTEXT | D-50-04 | Do not introduce Style Dictionary, Node tooling, or new token source | 50-01 | COVERED | Task 1 forbids those choices. |
| CONTEXT | D-50-05 | Use lean grouped swatch schema | 50-01 | COVERED | Task 1 defines grouped schema. |
| CONTEXT | D-50-06 | Include file-level provenance and grouped primitive/light/dark rows | 50-01 | COVERED | Task 1 acceptance criteria covers JSON fields. |
| CONTEXT | D-50-07 | Preserve token name, value, group, role/description, theme, optional resolved hex | 50-01 | COVERED | Task 1 action specifies row fields. |
| CONTEXT | D-50-08 | Do not compute/store pairwise contrast badges | 50-01 | COVERED | Task 1 explicitly excludes contrast badge data. |
| CONTEXT | D-50-09 | Compact professional scaffold shell | 50-01 | COVERED | Task 2 implements scaffold shell. |
| CONTEXT | D-50-10 | Include only the listed proof sections | 50-01 | COVERED | Task 2 action lists allowed sections. |
| CONTEXT | D-50-11 | Required exact copy labels | 50-01 | COVERED | Task 2 acceptance criteria greps exact labels. |
| CONTEXT | D-50-12 | Avoid marketing hero, remote imagery, gradients/orbs, nested cards, parser guts | 50-01 | COVERED | Task 2 action scopes visual/copy boundaries. |
| CONTEXT | D-50-13 | Pair visual proof cues with text and use locked type stacks/focus | 50-01 | COVERED | Task 2 action and criteria require text and token focus. |
| CONTEXT | D-50-14 | Automated `file://` browser proof plus source checks | 50-01 | COVERED | Task 3 creates Playwright and ExUnit gates. |
| CONTEXT | D-50-15 | Do not route brandbook through Phoenix/example app | 50-01 | COVERED | Task 3 forbids Phoenix/example-app routing. |
| CONTEXT | D-50-16 | Verification includes layout, package exclusion, source grep, derivation check, TOKENS doc, browser proof | 50-01 | COVERED | Task 3 verification commands cover all listed evidence. |
| CONTEXT | D-50-17 | Keep verification focused; no casual CI/release expansion | 50-01 | COVERED | Task 3 forbids CI expansion unless uncoupled and local. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| canonical CSS -> derivation script | Repo-local parser consumes `priv/static/cairnloop.css` and transforms it into generated CSS/JSON. |
| brandbook files -> local browser | Static HTML/CSS/JSON are opened directly from `file://` and may request local or remote resources. |
| repo collateral -> Hex package | `brandbook/` is tracked in git but must not cross into `mix.exs` package files. |
| verification script -> browser result | Automated Playwright proof must not falsely pass when console/network failures occur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-50-01 | Tampering | `scripts/derive_brandbook_tokens.exs`, `tokens.css`, `swatches.json` | mitigate | Generate both outputs from `priv/static/cairnloop.css` and make `--check` compare committed bytes so token mirror drift fails BOOK-02. |
| T-50-02 | Information Disclosure | `brandbook/index.html`, `brandbook/assets/css/brandbook.css` | mitigate | ExUnit/source guards and Playwright request capture forbid accidental remote assets, analytics, iframes, imports, remote CSS URLs, and root-relative production dependencies. |
| T-50-03 | Repudiation | `scripts/verify_brandbook_file_load.mjs` | mitigate | Browser proof aggregates console errors, page errors, all requests, and request failures, and asserts required page labels so a blank or partially loaded page cannot pass BOOK-01. |
| T-50-04 | Information Disclosure | `mix.exs` package files | mitigate | Static test asserts the package allowlist remains `lib priv guides mix.exs README.md LICENSE CHANGELOG.md` and does not include `brandbook/`. |
| T-50-05 | Denial of Service | `scripts/derive_brandbook_tokens.exs` alias resolution | mitigate | Alias resolution detects missing or circular aliases and fails closed instead of looping or emitting malformed JSON. |
| T-50-06 | Spoofing | `brandbook/color/swatches.json` | mitigate | JSON includes provenance (`source_file`, generation command, check command) and `TOKENS.md` states generated mirrors are not design authority. |
| T-50-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are planned; existing Playwright is reused from the example app lockfile and no npm lockfile changes are allowed. |
</threat_model>

<verification>
Run the phase gate after all tasks complete:

1. `mix run scripts/derive_brandbook_tokens.exs --check`
2. `mix test test/cairnloop/web/brandbook_scaffold_test.exs`
3. `node scripts/verify_brandbook_file_load.mjs`
4. `mix compile --warnings-as-errors`
5. `mix test`
6. `rg -n 'files: ~w\(lib priv guides mix\.exs README\.md LICENSE CHANGELOG\.md\)' mix.exs && test "$(rg -n 'files: .*brandbook' mix.exs | wc -l | tr -d ' ')" = "0"`
7. `test "$(rg -n 'https?://|(^|[^:])//|@import|<iframe|sendBeacon|url\((['\''"]?)https?:|url\((['\''"]?)/' brandbook | wc -l | tr -d ' ')" = "0"`
8. `test -z "$(git diff --name-only -- README.md examples/cairnloop_example/priv/static/images/logo.svg examples/cairnloop_example/lib/cairnloop_example_web/components/layouts/root.html.heex mix.exs .github/workflows/ci.yml examples/cairnloop_example/assets/package-lock.json examples/cairnloop_example/assets/package.json)"`
</verification>

<success_criteria>
BOOK-01 is complete when `brandbook/index.html` opens from `file://` under Playwright with required labels present, no console/page errors, no failed requests, and no remote network requests; source guards also prove no remote/import/root dependency patterns under `brandbook/`.

BOOK-02 is complete when `brandbook/assets/css/tokens.css` and `brandbook/color/swatches.json` are generated from canonical `priv/static/cairnloop.css`, `mix run scripts/derive_brandbook_tokens.exs --check` passes, and `brandbook/TOKENS.md` documents source, regeneration, check, drift prevention, and handoff boundaries.

Repo hygiene is complete when the approved `brandbook/` structure exists, is git-trackable, `mix.exs` package files exclude `brandbook/`, and no README/example-app/favicon/OG/CI/package-lock surfaces are changed.
</success_criteria>

<output>
Create `.planning/phases/50-brandbook-scaffold-token-derivation-pipeline/50-01-SUMMARY.md` when done.
</output>
