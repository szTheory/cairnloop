---
phase: 48-token-evolution-lock-propagate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/cairnloop/web/token_drift_test.exs
  - priv/static/cairnloop.css
  - examples/cairnloop_example/assets/css/app.css
  - prompts/cairnloop.tokens.json
autonomous: true
requirements:
  - TOKEN-02
  - TOKEN-03
  - TOKEN-04
must_haves:
  truths:
    - "Selected palette Refined and selected type current stack are applied to canonical :root per D-47-PALETTE and D-47-TYPE."
    - "No existing --cl-* token name is removed or renamed per D-A and TOKEN-02."
    - "Derivative expressed values in app.css and prompts/cairnloop.tokens.json match the canonical source exactly per TOKEN-03."
    - "Danger button text contrast is verifiable after adding a semantic text token rather than renaming existing tokens."
  artifacts:
    - path: "test/cairnloop/web/token_drift_test.exs"
      provides: "Pure ExUnit drift, no-rename, and contrast verifier"
      exports: []
    - path: "priv/static/cairnloop.css"
      provides: "Canonical evolved Cairnloop tokens"
      contains: "--cl-color-basalt:        #141B19;"
    - path: "examples/cairnloop_example/assets/css/app.css"
      provides: "Example app Tailwind @theme and @layer base derivative mirror"
      contains: "--color-cl-basalt: #141B19;"
    - path: "prompts/cairnloop.tokens.json"
      provides: "Machine-readable color/type derivative mirror"
      contains: "\"basalt\""
  key_links:
    - from: "test/cairnloop/web/token_drift_test.exs"
      to: "priv/static/cairnloop.css"
      via: "File.read! canonical token parser"
      pattern: "priv/static/cairnloop\\.css"
    - from: "test/cairnloop/web/token_drift_test.exs"
      to: "examples/cairnloop_example/assets/css/app.css"
      via: "Derivative expressed-value comparison"
      pattern: "examples/cairnloop_example/assets/css/app\\.css"
    - from: "test/cairnloop/web/token_drift_test.exs"
      to: "prompts/cairnloop.tokens.json"
      via: "Jason.decode! and semantic key comparison"
      pattern: "cairnloop\\.tokens\\.json"
---

<objective>
Apply the selected Phase 47 Refined palette and current type direction to the canonical token source, then propagate the expressed values to the example app and prompt-token derivatives with a repeatable verifier in place first.

Purpose: Phase 48 must lock the brand evolution without creating a second source of truth or breaking the sealed `--cl-*` token contract.
Output: `token_drift_test.exs`, evolved canonical CSS tokens, synchronized `app.css`, and synchronized `cairnloop.tokens.json`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-token-evolution-lock-propagate/48-RESEARCH.md
@.planning/phases/48-token-evolution-lock-propagate/48-PATTERNS.md
@.planning/phases/48-token-evolution-lock-propagate/48-VALIDATION.md
@.planning/phases/48-token-evolution-lock-propagate/48-UI-SPEC.md
@.planning/phases/47-brand-direction-exploration-selection-gate/47-SELECTION-GATE.md
@.planning/phases/47-brand-direction-exploration-selection-gate/47-CONTEXT.md
@.planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md
@.planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md
@test/cairnloop/web/brand_token_gate_test.exs
@test/cairnloop/web/cairnloop_css_test.exs
@priv/static/cairnloop.css
@examples/cairnloop_example/assets/css/app.css
@prompts/cairnloop.tokens.json
</context>

## Artifacts This Phase Produces

- `test/cairnloop/web/token_drift_test.exs` — new DB-free ExUnit verifier module `Cairnloop.Web.TokenDriftTest`.
- `priv/static/cairnloop.css` — evolved canonical `:root`, `[data-theme="dark"]`, and `.cl-button--danger` token usage.
- `examples/cairnloop_example/assets/css/app.css` — evolved `@theme`, `@layer base :root`, and dark derivative values.
- `prompts/cairnloop.tokens.json` — evolved `color.primitive`, `semantic_light`, `semantic_dark`, and unchanged current type stack strings.
- Additive CSS token: `--cl-danger-button-text`.
- Test helper concepts inside `Cairnloop.Web.TokenDriftTest`: canonical CSS token extraction, derivative mirror comparison, WCAG contrast calculation, and Phase 46 contrast row assertions.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 verifier for no-renames, derivative drift, and contrast rows</name>
  <files>test/cairnloop/web/token_drift_test.exs</files>
  <read_first>
    @test/cairnloop/web/brand_token_gate_test.exs
    @test/cairnloop/web/cairnloop_css_test.exs
    @.planning/phases/48-token-evolution-lock-propagate/48-PATTERNS.md
    @.planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md
    @.planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md
  </read_first>
  <behavior>
    - Test no-removal: the current expected `--cl-*` token set from `priv/static/cairnloop.css` remains present after evolution; include all existing names and the additive `--cl-danger-button-text`.
    - Test selected values: canonical CSS contains Refined values `#141B19`, `#F4EEE2`, `#FAF5EB`, `#A8492A`, `#D98A4A`, `#5E665D`, and dark `--cl-danger: #C96A55`.
    - Test derivative parity: expressed `@theme --color-cl-*`, `@layer base --cl-color-*`, core light/dark semantics, and `prompts/cairnloop.tokens.json` primitive/semantic values match canonical resolved values exactly.
    - Test drift closure: `app.css` expresses `--cl-shadow-raised: var(--cl-shadow-1)` and no longer carries the divergent literal shadow from the Phase 46 ledger.
    - Test contrast: recompute Phase 46 row IDs for text, large/UI, copper route-marker, and meaningful UI boundaries; assert all meaningful text pairings meet 4.5 and focus/route indicators meet 3.0.
  </behavior>
  <action>Create `Cairnloop.Web.TokenDriftTest` as an async, DB-free ExUnit test. Use stdlib string parsing plus existing `Jason.decode!`; do not add packages. Keep CSS parsing scoped to token declarations in `:root` and `[data-theme="dark"]`, resolving direct `var(--cl-*)` aliases needed by the derivative comparisons. Encode Phase 46 row IDs explicitly so failures report the row number, token names, resolved values, ratio, threshold, file, and next action. Include the dark danger button assertion for `--cl-danger-button-text` on `--cl-danger` so `#C96A55` is not paired with white text. This implements TOKEN-02/TOKEN-03/TOKEN-04 and D-47-PALETTE/D-47-TYPE.</action>
  <verify>
    <automated>test -f test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n 'Cairnloop\\.Web\\.TokenDriftTest' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n -- '--cl-danger-button-text' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n '#141B19' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n '#F4EEE2' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n '#FAF5EB' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n '#A8492A' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n '#D98A4A' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n '#5E665D' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n '#C96A55' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n 'Row 13' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n 'Row 14' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n 'Row 22' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n 'CU-L' test/cairnloop/web/token_drift_test.exs &amp;&amp; rg -n 'CU-D' test/cairnloop/web/token_drift_test.exs</automated>
    <automated>mix test test/cairnloop/web/token_drift_test.exs; status=$?; test "$status" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Static structure checks pass and confirm the verifier contains selected Refined values, additive danger text token coverage, Phase 46 row IDs, and route-marker rows.
    - The intentional RED diagnostic `mix test test/cairnloop/web/token_drift_test.exs; status=$?; test "$status" -ne 0` passes before token edits because the new assertions fail against the old token values and/or derivative drift.
    - The failure output names at least one concrete token/file mismatch, not only a generic assertion failure.
    - No package manager files change.
  </acceptance_criteria>
  <done>`test/cairnloop/web/token_drift_test.exs` exists, is async and DB-free, and provides red coverage for TOKEN-02, TOKEN-03, and TOKEN-04 before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply selected Refined tokens to canonical CSS</name>
  <files>priv/static/cairnloop.css</files>
  <read_first>
    @priv/static/cairnloop.css
    @.planning/phases/48-token-evolution-lock-propagate/48-UI-SPEC.md
    @.planning/phases/47-brand-direction-exploration-selection-gate/47-SELECTION-GATE.md
    @.planning/phases/46-brand-fidelity-audit-token-consolidation/46-CONTRAST-BASELINE.md
  </read_first>
  <behavior>
    - Existing `--cl-*` token names remain present.
    - Light canonical primitives use basalt `#141B19`, trailpaper `#F4EEE2`, warm stone `#FAF5EB`, path copper `#A8492A`, and slate lichen `#5E665D`.
    - Dark canonical semantics use `--cl-primary: #D98A4A`, `--cl-focus: #D98A4A`, and `--cl-danger: #C96A55`.
    - Danger buttons use `color: var(--cl-danger-button-text, #FFFFFF)` and dark mode sets `--cl-danger-button-text: #141B19`.
    - Light muted-on-bg, muted-on-sunken, and neutral chip text pairings meet AA after the token changes.
  </behavior>
  <action>Update only values and additive tokens in `priv/static/cairnloop.css` per D-A, D-47-PALETTE, and D-47-TYPE. Preserve every existing `--cl-*` name. Add `--cl-danger-button-text` in `:root` as `#FFFFFF` and in `[data-theme="dark"]` as `#141B19`, then update `.cl-button--danger` to read that token. Set `--cl-color-basalt`, `--cl-color-trailpaper`, `--cl-color-warm-stone`, `--cl-color-slate-lichen`, and `--cl-color-path-copper` to the locked Refined values. Keep the current Atkinson/Fraunces/Martian stacks unchanged. Tune only adjacent semantic values required by 48-UI-SPEC: `--cl-surface-sunken`, `--cl-neutral-surface`, `--cl-neutral-text`, `--cl-primary-hover`, dark `--cl-danger`, and any meaningful input/button boundary token needed to satisfy contrast. Add or adjust a concise CSS comment classifying status chip borders as decorative because chip text/icon carries the status meaning; do not classify input/button boundaries as decorative.</action>
  <verify>
    <automated>rg -n -- '--cl-danger-button-text' priv/static/cairnloop.css &amp;&amp; rg -n -- '--cl-color-basalt:\\s*#141B19' priv/static/cairnloop.css &amp;&amp; rg -n -- '--cl-color-trailpaper:\\s*#F4EEE2' priv/static/cairnloop.css &amp;&amp; rg -n -- '--cl-color-warm-stone:\\s*#FAF5EB' priv/static/cairnloop.css &amp;&amp; rg -n -- '--cl-color-path-copper:\\s*#A8492A' priv/static/cairnloop.css &amp;&amp; rg -n -- '--cl-color-slate-lichen:\\s*#5E665D' priv/static/cairnloop.css &amp;&amp; rg -n -- '--cl-primary:\\s*#D98A4A' priv/static/cairnloop.css &amp;&amp; rg -n -- '--cl-focus:\\s*#D98A4A' priv/static/cairnloop.css &amp;&amp; rg -n -- '--cl-danger:\\s*#C96A55' priv/static/cairnloop.css &amp;&amp; rg -n -- 'color:\\s*var\\(--cl-danger-button-text' priv/static/cairnloop.css</automated>
    <automated>mix test test/cairnloop/web/brand_token_gate_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n -- '--cl-danger-button-text|#141B19|#F4EEE2|#FAF5EB|#A8492A|#D98A4A|#5E665D|#C96A55' priv/static/cairnloop.css` shows the selected values and additive danger button text token.
    - `git diff -- priv/static/cairnloop.css | rg -n '^[-+]\\s*--cl-[a-z0-9-]+:'` shows value/additive changes only; no existing token deletion is left unresolved.
    - `mix test test/cairnloop/web/brand_token_gate_test.exs` passes at this task boundary; full derivative parity is deferred to Task 3 because derivative files are not edited in Task 2.
  </acceptance_criteria>
  <done>Canonical CSS carries the selected Refined palette/current type direction, fixes dark danger button contrast with an additive semantic token, and preserves the sealed token-name contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Propagate canonical values to app.css and tokens.json</name>
  <files>examples/cairnloop_example/assets/css/app.css, prompts/cairnloop.tokens.json</files>
  <read_first>
    @examples/cairnloop_example/assets/css/app.css
    @prompts/cairnloop.tokens.json
    @priv/static/cairnloop.css
    @.planning/phases/46-brand-fidelity-audit-token-consolidation/46-DISCREPANCY-LEDGER.md
    @.planning/phases/48-token-evolution-lock-propagate/48-UI-SPEC.md
  </read_first>
  <behavior>
    - `app.css @theme --color-cl-*` primitive values match canonical primitive hex values exactly.
    - `app.css @layer base :root` expressed `--cl-color-*` and core semantic values match canonical values or canonical aliases exactly.
    - `app.css [data-theme="dark"]` expressed dark semantic values match canonical dark values exactly.
    - `prompts/cairnloop.tokens.json` expressed primitive and light/dark semantic values match canonical resolved values exactly.
    - `prompts/cairnloop.tokens.json` typography remains the current Atkinson Hyperlegible, Fraunces, and Martian Mono stack per D-47-TYPE.
  </behavior>
  <action>Update `examples/cairnloop_example/assets/css/app.css` after the canonical CSS edit. Mirror the selected primitive values in both `@theme` and `@layer base :root`; mirror the expressed light/dark semantic values and add `--cl-danger-button-text` only if the verifier compares it as an expressed derivative semantic. Replace `--cl-shadow-raised` with `var(--cl-shadow-1)` per the Phase 46 drift ledger. Update `prompts/cairnloop.tokens.json` primitive and semantic values to canonical resolved values; keep it a color/type/voice derivative and do not turn it into a full token dump. Do not edit daisyUI plugin theme blocks because they are excluded scaffolding noise in the Phase 46 ledger.</action>
  <verify>
    <automated>mix test test/cairnloop/web/token_drift_test.exs test/cairnloop/web/brand_token_gate_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/web/token_drift_test.exs test/cairnloop/web/brand_token_gate_test.exs` passes.
    - `rg -n '#141B19|#F4EEE2|#FAF5EB|#A8492A|#D98A4A|#5E665D|#C96A55|--cl-shadow-raised: var\\(--cl-shadow-1\\)' examples/cairnloop_example/assets/css/app.css prompts/cairnloop.tokens.json` shows the propagated values and corrected shadow alias.
    - `git diff --name-only` includes only this plan's files during Plan 01 execution.
  </acceptance_criteria>
  <done>`app.css` and `cairnloop.tokens.json` express the same canonical values and the new drift test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token source -> browser runtime | Static CSS values affect rendered operator UI contrast and state perception. |
| Canonical CSS -> derivative mirrors | Manual propagation can introduce drift that misleads downstream consumers. |
| Planning artifacts -> executor behavior | Phase handoff files are trusted project guidance but must not expand scope beyond locked decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | `priv/static/cairnloop.css` token names | mitigate | `token_drift_test.exs` asserts existing `--cl-*` names remain present; actions permit value changes and additive tokens only. |
| T-48-02 | Tampering | `app.css` and `cairnloop.tokens.json` derivatives | mitigate | Pure ExUnit derivative parity check compares expressed values against canonical CSS. |
| T-48-03 | Information Disclosure | Visual status/error meaning through color alone | mitigate | Preserve text/icon status pairings and contrast assertions; do not add color-only UI states. |
| T-48-04 | Denial of Service | Test suite requires unavailable DB | mitigate | New verifier is DB-free and async; final gate failures must distinguish environment caveats from token regressions. |
| T-48-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are planned; if an executor attempts one, stop and require package legitimacy review. |
</threat_model>

<verification>
Run after this plan:

<automated>mix test test/cairnloop/web/token_drift_test.exs test/cairnloop/web/brand_token_gate_test.exs</automated>

Also run:

<automated>git diff --name-only</automated>

Expected changed files are `test/cairnloop/web/token_drift_test.exs`, `priv/static/cairnloop.css`, `examples/cairnloop_example/assets/css/app.css`, and `prompts/cairnloop.tokens.json`.
</verification>

<success_criteria>
TOKEN-02 is satisfied for canonical CSS value/additive-token evolution. TOKEN-03 is satisfied for expressed derivative parity. TOKEN-04 has repeatable pure-test coverage ready for the final full gate plan.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | phase-48 | Chosen palette/type applied once to canonical source and propagated with zero drift/full gates | 48-01, 48-02 | COVERED | Canonical and derivative edits here; final gates in Plan 02.
REQ | TOKEN-02 | Canonical `:root` value/additive evolution with no token renames | 48-01 | COVERED | Tasks 1-2 cite TOKEN-02 and D-A.
REQ | TOKEN-03 | Propagate to `app.css` and `cairnloop.tokens.json` with zero drift | 48-01 | COVERED | Tasks 1 and 3.
REQ | TOKEN-04 | Brand-token gate, integration smoke, E2E, and contrast baseline re-verification | 48-01, 48-02 | COVERED | Pure verifier starts here; full gates and evidence in Plan 02.
RESEARCH | selected-refined-values | Apply basalt/trailpaper/warm-stone/copper/muted/dark-danger values | 48-01 | COVERED | Task 2 and Task 3.
RESEARCH | danger-button-contrast | `#C96A55` with white text fails; use additive danger button text token | 48-01 | COVERED | Task 2.
RESEARCH | no-new-packages | Use stdlib/Jason/ExUnit, no package install | 48-01 | COVERED | Task 1 and threat model.
RESEARCH | shadow-drift | Replace app.css `--cl-shadow-raised` literal with canonical alias | 48-01 | COVERED | Task 3.
CONTEXT | D-A | Core palette/type may evolve by value/additive changes only | 48-01 | COVERED | Task 2.
CONTEXT | D-47-PALETTE | Refined palette selected | 48-01 | COVERED | Tasks 1-3.
CONTEXT | D-47-TYPE | Current Atkinson/Fraunces/Martian stack selected | 48-01 | COVERED | Tasks 2-3.
CONTEXT | D-47-LOGO | C3.6 logo selected but logo assets are not Phase 48 | 48-02 | COVERED | Scope guard in Plan 02.
</source_audit>

<output>
Create `.planning/phases/48-token-evolution-lock-propagate/48-01-SUMMARY.md` when done.
</output>
