---
phase: 52-collateral-wiring-qa-validation-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/52-collateral-wiring-qa-validation-sweep/52-LOGO-FAMILY-SIGNOFF.md
  - README.md
  - test/cairnloop/web/collateral_wiring_test.exs
autonomous: false
requirements:
  - WIRE-02
  - HYGIENE-01
  - HYGIENE-02
  - HYGIENE-03
must_haves:
  truths:
    - "D-52-01: logo-family sign-off is recorded before README or example-app collateral edits."
    - "D-52-02/D-52-03/D-52-04/D-52-05: README starts with the approved repo-relative horizontal SVG logo, alt text Cairnloop, badges immediately below, and no packaged HexDocs rendering claim."
    - "D-52-10/D-52-11/D-52-13: DB-free static tests guard README, approved logo inventory, SVG safe subset, raster budget policy, and package allowlist."
  artifacts:
    - path: ".planning/phases/52-collateral-wiring-qa-validation-sweep/52-LOGO-FAMILY-SIGNOFF.md"
      provides: "Recorded subjective logo-family sign-off gate for D-52-01."
    - path: "README.md"
      provides: "GitHub-renderable README header using logo/cairnloop-lockup-horizontal.svg."
    - path: "test/cairnloop/web/collateral_wiring_test.exs"
      provides: "Cairnloop.Web.CollateralWiringTest DB-free collateral/source guard."
  key_links:
    - from: "README.md"
      to: "logo/cairnloop-lockup-horizontal.svg"
      via: "repo-relative image src"
      pattern: "logo/cairnloop-lockup-horizontal\\.svg"
    - from: "test/cairnloop/web/collateral_wiring_test.exs"
      to: "mix.exs"
      via: "package files allowlist assertion"
      pattern: "files:\\s*~w\\(lib priv guides mix\\.exs README\\.md LICENSE CHANGELOG\\.md\\)"
---

<objective>
Create the Phase 52 subjective sign-off record, wire the GitHub README header to the approved horizontal logo, and add the first DB-free source/package/SVG/raster guard for WIRE-02, HYGIENE-01, HYGIENE-02, and HYGIENE-03.

Purpose: D-52-01 makes logo-family sign-off the only subjective precondition, while D-52-02 requires all rendered implementation checks to stay automated. This plan records that gate, then turns the README and baseline hygiene contract into executable checks.

Output: `52-LOGO-FAMILY-SIGNOFF.md`, updated `README.md`, and `test/cairnloop/web/collateral_wiring_test.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-collateral-wiring-qa-validation-sweep/52-CONTEXT.md
@.planning/phases/52-collateral-wiring-qa-validation-sweep/52-RESEARCH.md
@.planning/phases/52-collateral-wiring-qa-validation-sweep/52-PATTERNS.md
@.planning/phases/52-collateral-wiring-qa-validation-sweep/52-UI-SPEC.md
@.planning/phases/52-collateral-wiring-qa-validation-sweep/52-VALIDATION.md
@.planning/phases/49-chosen-logo-finalization-asset-family/49-01-SUMMARY.md
@.planning/phases/49-chosen-logo-finalization-asset-family/49-02-SUMMARY.md
@.planning/phases/49-chosen-logo-finalization-asset-family/49-03-SUMMARY.md
@README.md
@logo/USAGE.md
@test/cairnloop/web/brandbook_scaffold_test.exs
@mix.exs
</context>

## Artifacts This Phase Produces

- `.planning/phases/52-collateral-wiring-qa-validation-sweep/52-LOGO-FAMILY-SIGNOFF.md` — sign-off record for the approved Phase 49 logo family before wiring.
- `README.md` — first visible line becomes `<img src="logo/cairnloop-lockup-horizontal.svg" alt="Cairnloop" width="260">`, with existing badges directly below.
- `test/cairnloop/web/collateral_wiring_test.exs` — `Cairnloop.Web.CollateralWiringTest` static/source guard.
- `examples/cairnloop_example/priv/static/images/logo.svg` — copied from `logo/cairnloop-lockup-horizontal.svg` by Plan 02.
- `examples/cairnloop_example/priv/static/favicon.ico` — copied from `logo/favicon.ico` by Plan 02.
- `examples/cairnloop_example/priv/static/images/favicon.svg` — copied from `logo/favicon.svg` by Plan 02.
- `examples/cairnloop_example/priv/static/images/cairnloop-og.png` — copied from `logo/cairnloop-og.png` by Plan 02.
- `examples/cairnloop_example/lib/cairnloop_example_web/components/layouts/root.html.heex` — title, favicon, and OG metadata wiring by Plan 02.
- `examples/cairnloop_example/lib/cairnloop_example_web/components/layouts.ex` — accessible example logo markup by Plan 02.
- `examples/cairnloop_example/test/e2e/collateral_wiring_test.exs` — `CairnloopExampleWeb.CollateralWiringE2ETest` browser proof by Plan 02.
- `.planning/phases/52-collateral-wiring-qa-validation-sweep/52-QA-REPORT.md` — final automated QA evidence by Plan 03.
- CLI-test artifacts: `mix test test/cairnloop/web/collateral_wiring_test.exs`, `mix test`, `cd examples/cairnloop_example && PW_TRACE=true mix test.e2e`, `xmllint --noout $(git ls-files '*.svg')`, `magick identify ...`, `du -ck ...`, `mix hex.build --unpack`, `git diff --stat`, and `git diff --name-only`.

<must_haves>
- D-52-01 is satisfied before collateral edits: the sign-off record exists and names `cairnloop-lockup-horizontal.svg`, `favicon.ico`, and `cairnloop-og.png`.
- WIRE-02 is satisfied: README begins with the approved horizontal SVG logo from `logo/`, uses `alt="Cairnloop"`, preserves badges immediately below, and does not present the old text/emoji heading as the page brand.
- HYGIENE-01 starts here: the static guard scans every tracked SVG, not only `logo/`.
- HYGIENE-02 starts here: the guard asserts approved raster policy and the 150KB maximum using the Phase 49 source assets, with Plan 02 extending the same file for copied runtime rasters.
- HYGIENE-03 starts here: the guard locks the package files allowlist and asserts `brandbook/`, `logo/`, and `scripts/` remain unshipped.
</must_haves>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Record logo-family sign-off before collateral edits</name>
  <files>.planning/phases/52-collateral-wiring-qa-validation-sweep/52-LOGO-FAMILY-SIGNOFF.md</files>
  <read_first>
    .planning/phases/52-collateral-wiring-qa-validation-sweep/52-CONTEXT.md
    .planning/phases/49-chosen-logo-finalization-asset-family/49-03-SUMMARY.md
    logo/USAGE.md
    .planning/STATE.md
  </read_first>
  <action>Implement D-52-01 and D-52-02 before any README or example-app collateral edit. Inspect the phase context, state, and Phase 49 summary for an explicit owner approval of the Phase 49 logo family. If approval is not already recorded, pause for the owner to approve the committed asset family, then create `52-LOGO-FAMILY-SIGNOFF.md` with: `logo-family sign-off: approved`, approval date, source asset list (`logo/cairnloop-lockup-horizontal.svg`, `logo/favicon.svg`, `logo/favicon.ico`, `logo/cairnloop-og.png`), and the statement that all later logo/favicon/metadata behavior checks are automated. Do not ask for rendered implementation UAT; the only human input is subjective asset approval.</action>
  <verify>
    <automated>test -f .planning/phases/52-collateral-wiring-qa-validation-sweep/52-LOGO-FAMILY-SIGNOFF.md &amp;&amp; rg -n 'logo-family sign-off: approved|logo/cairnloop-lockup-horizontal\.svg|logo/favicon\.ico|logo/cairnloop-og\.png|automated' .planning/phases/52-collateral-wiring-qa-validation-sweep/52-LOGO-FAMILY-SIGNOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - `52-LOGO-FAMILY-SIGNOFF.md` exists before `README.md` or example-app collateral files are edited.
    - The record names the approved horizontal lockup, favicon ICO, and OG PNG assets.
    - The record explicitly says implementation verification after sign-off is automated, satisfying D-52-02.
  </acceptance_criteria>
  <done>Logo-family sign-off is durably recorded, or execution stops before collateral edits with a blocking decision checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the restrained README logo header</name>
  <files>README.md</files>
  <read_first>
    README.md
    logo/USAGE.md
    .planning/phases/52-collateral-wiring-qa-validation-sweep/52-UI-SPEC.md
    .planning/phases/52-collateral-wiring-qa-validation-sweep/52-CONTEXT.md
    .planning/phases/52-collateral-wiring-qa-validation-sweep/52-LOGO-FAMILY-SIGNOFF.md
  </read_first>
  <action>Implement D-52-03, D-52-04, D-52-05, D-52-15, and D-52-16 in `README.md`. Make the first visible line an HTML image using repo-relative `logo/cairnloop-lockup-horizontal.svg`, exact alt text `Cairnloop`, and a restrained width of `260`. Keep the existing Hex, HexDocs, and CI badges immediately below the image. Remove the old text/emoji brand heading from the top presentation. Keep installation and guide content close to the top; do not add launch-page copy, remote brand imagery, screenshots, or claims that the `logo/` path is packaged for HexDocs.</action>
  <verify>
    <automated>ruby -e 's=File.read("README.md"); lines=s.lines.reject{|l| l.strip.empty?}; abort("first visible line must be approved logo") unless lines.first.include?(%q(src="logo/cairnloop-lockup-horizontal.svg")) &amp;&amp; lines.first.include?(%q(alt="Cairnloop")); abort("badges must remain immediately below logo") unless lines[1..3].join.include?("img.shields.io/hexpm/v/cairnloop.svg") &amp;&amp; lines[1..4].join.include?("GitHub Actions CI"); abort("old emoji identity still present") if s.include?("🏔️")'</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` first visible line contains `src="logo/cairnloop-lockup-horizontal.svg"` and `alt="Cairnloop"`.
    - The existing badge links remain directly below the logo.
    - The old emoji identity is absent from the README.
    - README copy stays OSS-library concise and does not introduce a marketing hero.
  </acceptance_criteria>
  <done>WIRE-02 is implemented for README GitHub rendering without changing package policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add DB-free collateral source and hygiene guard</name>
  <files>test/cairnloop/web/collateral_wiring_test.exs</files>
  <read_first>
    test/cairnloop/web/brandbook_scaffold_test.exs
    README.md
    logo/USAGE.md
    mix.exs
    .planning/phases/52-collateral-wiring-qa-validation-sweep/52-VALIDATION.md
    .planning/phases/52-collateral-wiring-qa-validation-sweep/52-PATTERNS.md
  </read_first>
  <behavior>
    - Test README header: first visible nonblank line uses `logo/cairnloop-lockup-horizontal.svg` and exact alt `Cairnloop`; badges remain below; no root-relative, raw GitHub, `file://`, or data URL logo source is accepted.
    - Test approved inventory: every asset named in `logo/USAGE.md` exists under `logo/`; rejected contest files remain absent.
    - Test SVG safe subset across `git ls-files '*.svg'`: XML well-formed through `xmllint`, root has `xmlns`, `viewBox` parses as four positive numbers, and unsafe constructs are rejected with failure messages naming the file.
    - Test raster/package policy: source favicon/OG rasters stay under the 150KB maximum, no non-favicon/non-OG logo PNG fallback exists, and `mix.exs` package files remain `lib priv guides mix.exs README.md LICENSE CHANGELOG.md` with `brandbook/`, `logo/`, and `scripts/` unshipped.
  </behavior>
  <action>Create `Cairnloop.Web.CollateralWiringTest` using `ExUnit.Case, async: true`, modeled on `Cairnloop.Web.BrandbookScaffoldTest`. Keep it DB-free: use `File`, `Path.wildcard`, `System.cmd`, `Regex`, `Jason` only if already available, and no Repo/Endpoint/Phoenix server boot. Implement D-52-10, D-52-11, D-52-13, and D-52-17 for README, approved asset inventory, all tracked SVGs, source raster budget, and package allowlist. Use actionable assertion messages naming the failing path and the expected next action. Leave Plan 02 to extend this same guard for copied example-app runtime assets and root metadata.</action>
  <verify>
    <automated>mix test test/cairnloop/web/collateral_wiring_test.exs</automated>
    <automated>xmllint --noout $(git ls-files '*.svg')</automated>
    <automated>du -ck logo/*.png logo/*.ico</automated>
  </verify>
  <acceptance_criteria>
    - `test/cairnloop/web/collateral_wiring_test.exs` defines `Cairnloop.Web.CollateralWiringTest`.
    - Focused `mix test test/cairnloop/web/collateral_wiring_test.exs` passes after README wiring.
    - The test scans `git ls-files '*.svg'` rather than only `logo/*.svg`.
    - The test includes explicit package allowlist coverage for `brandbook/`, `logo/`, and `scripts/`.
  </acceptance_criteria>
  <done>The first automated source/package/SVG/raster guard is green and ready for Plan 02 example-app extensions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo collateral→GitHub README renderer | Committed SVG and Markdown are interpreted by GitHub and developer browsers. |
| repo collateral→Hex package build | `mix.exs` package files decide which collateral becomes shipped package payload. |
| test guard→phase closeout claims | Source tests must fail before summaries can claim QA pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | tracked SVG files | mitigate | `Cairnloop.Web.CollateralWiringTest` runs XML and safe-subset checks across `git ls-files '*.svg'`, blocking script, foreignObject, raster embeds, external/data hrefs, and editor metadata per D-52-11. |
| T-52-02 | Information Disclosure | README logo source | mitigate | README uses repo-relative `logo/cairnloop-lockup-horizontal.svg`; tests reject remote, root-relative, `file://`, and data URL logo sources per D-52-03/D-52-05. |
| T-52-03 | Spoofing | README brand identity | mitigate | README test asserts approved asset path and exact accessible name `Cairnloop`, and Task 2 removes the old emoji identity per D-52-04/D-52-16. |
| T-52-04 | Repudiation | sign-off gate | mitigate | `52-LOGO-FAMILY-SIGNOFF.md` records the subjective approval before edits, so implementation work does not imply unrecorded owner approval per D-52-01. |
| T-52-05 | Information Disclosure | Hex package allowlist | mitigate | Static guard asserts `files: ~w(lib priv guides mix.exs README.md LICENSE CHANGELOG.md)` and absence of `brandbook/`, `logo/`, and `scripts/` from package files per HYGIENE-03. |
| T-52-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install task exists in this plan; if execution introduces one, stop and add a package legitimacy checkpoint before install. |
</threat_model>

<verification>
- Focused plan gate: `mix test test/cairnloop/web/collateral_wiring_test.exs`.
- SVG XML gate: `xmllint --noout $(git ls-files '*.svg')`.
- Source raster budget sample: `du -ck logo/*.png logo/*.ico`.
- Package source policy is asserted in `Cairnloop.Web.CollateralWiringTest`; unpacked package proof is Plan 03.
</verification>

<success_criteria>
- D-52-01 and D-52-02 are represented by a sign-off record and no rendered human-verification task.
- README satisfies WIRE-02 exactly: approved repo-relative SVG logo, alt `Cairnloop`, badges retained, no old emoji heading.
- HYGIENE-01/HYGIENE-02/HYGIENE-03 have executable DB-free guard coverage that Plan 02 and Plan 03 build on.
</success_criteria>

## Source Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | phase goal | README collateral plus static QA gate | 52-01 | COVERED | This plan covers README and baseline hygiene; Plans 02-03 complete example/E2E/final sweep. |
| REQ | WIRE-02 | README leads with chosen SVG logo header | 52-01 | COVERED | Task 2 and Task 3. |
| REQ | HYGIENE-01 | committed SVG validity and safe subset | 52-01, 52-03 | COVERED | Task 3 starts guard; Plan 03 records full evidence. |
| REQ | HYGIENE-02 | raster footprint and no logo PNG fallbacks | 52-01, 52-02, 52-03 | COVERED | Task 3 starts source budget; Plan 02 adds runtime copies; Plan 03 records final budget. |
| REQ | HYGIENE-03 | package exclusion and diff evidence | 52-01, 52-03 | COVERED | Task 3 locks source policy; Plan 03 unpacks package and records diff. |
| RESEARCH | hybrid gate with DB-free ExUnit and E2E | 52-01, 52-02 | COVERED | Static guard starts here; browser E2E is Plan 02. |
| RESEARCH | no package installs | 52-01, 52-02, 52-03 | COVERED | Threat model and tasks forbid new dependencies. |
| CONTEXT | D-52-01 | sign-off before wiring | 52-01 | COVERED | Task 1. |
| CONTEXT | D-52-02 | no rendered human UAT | 52-01, 52-02, 52-03 | COVERED | Task 1 and automated test strategy. |
| CONTEXT | D-52-03 | approved README logo path and alt | 52-01 | COVERED | Task 2. |
| CONTEXT | D-52-04 | demote/remove old H1 identity | 52-01 | COVERED | Task 2. |
| CONTEXT | D-52-05 | GitHub README path, no HexDocs package claim | 52-01 | COVERED | Task 2 and Task 3. |
| CONTEXT | D-52-10 | hybrid fast source tests plus E2E | 52-01, 52-02 | COVERED | Task 3 starts source lane. |
| CONTEXT | D-52-11 | static source coverage list | 52-01, 52-02 | COVERED | Task 3 and Plan 02 static extensions. |
| CONTEXT | D-52-13 | false-pass prevention in checks | 52-01, 52-02 | COVERED | Task 3 failure-message and structured-check instructions. |
| CONTEXT | D-52-15 | restrained operator-grade posture | 52-01 | COVERED | Task 2. |
| CONTEXT | D-52-16 | logo alt semantics | 52-01, 52-02 | COVERED | README alt here; example app alt in Plan 02. |
| CONTEXT | D-52-17 | no dependencies or custom asset API | 52-01, 52-02 | COVERED | Threat model and actions. |
</verification>

<output>
Create `.planning/phases/52-collateral-wiring-qa-validation-sweep/52-01-SUMMARY.md` when done.
</output>
