---
phase: "12"
plan: "04"
type: execute
wave: 4
depends_on:
  - 12-02
  - 12-03
files_modified:
  - lib/cairnloop/knowledge_automation/telemetry.ex
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_automation/candidate_builder.ex
  - lib/cairnloop/knowledge_base/workers/chunk_revision.ex
  - lib/cairnloop/telemetry.ex
  - test/cairnloop/retrieval/telemetry_test.exs
  - test/cairnloop/knowledge_automation/review_task_test.exs
  - test/cairnloop/web/conversation_live_test.exs
autonomous: true
requirements:
  - OPS-03
must_haves:
  truths:
    - "Gap creation, suggestion outcomes, review decisions, publish, and reindex all emit bounded maintenance telemetry."
    - "Telemetry metadata stays low-cardinality and never becomes workflow truth."
    - "Thread and review-lane copy preserve the distinction between approved, published, and reindexed."
---

<objective>
Add bounded maintenance telemetry and finish operator-visible closure.

Purpose: satisfy `OPS-03` by normalizing maintenance telemetry across quick fix, gap creation, review decisions, and publish/reindex follow-through while keeping thread/review-lane state legible.
Output: telemetry helper, event emission at key workflow seams, and final status-copy alignment for thread and review surfaces.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-in-thread-quick-fix-ops-closure/12-CONTEXT.md
@.planning/phases/12-in-thread-quick-fix-ops-closure/12-RESEARCH.md
@lib/cairnloop/retrieval/telemetry.ex
@lib/cairnloop/telemetry.ex
@lib/cairnloop/knowledge_automation.ex
@lib/cairnloop/knowledge_automation/candidate_builder.ex
@lib/cairnloop/knowledge_base/workers/chunk_revision.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded knowledge-maintenance telemetry helper and emit coarse workflow events</name>
  <files>lib/cairnloop/knowledge_automation/telemetry.ex, lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_automation/candidate_builder.ex, lib/cairnloop/telemetry.ex, test/cairnloop/retrieval/telemetry_test.exs</files>
  <read_first>
    - lib/cairnloop/retrieval/telemetry.ex
    - lib/cairnloop/telemetry.ex
    - lib/cairnloop/knowledge_automation.ex
    - lib/cairnloop/knowledge_automation/candidate_builder.ex
    - .planning/phases/12-in-thread-quick-fix-ops-closure/12-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `lib/cairnloop/knowledge_automation/telemetry.ex` exists and normalizes bounded maintenance telemetry metadata
    - `lib/cairnloop/knowledge_automation.ex` emits maintenance events for quick-fix or suggestion outcomes plus review decisions
    - `lib/cairnloop/knowledge_automation/candidate_builder.ex` or equivalent emits a bounded gap-creation event
    - `test/cairnloop/retrieval/telemetry_test.exs` exits 0 and asserts maintenance metadata omits raw query text, raw thread text, and citation payloads
  </acceptance_criteria>
  <action>Create a narrow knowledge-maintenance telemetry helper that mirrors the retrieval telemetry normalization style. Emit bounded events for gap-candidate creation, quick-fix or suggestion outcome, review decision, publish initiation/result, and reindex completion/failure. Normalize metadata to coarse enums and small counts such as `surface`, `entrypoint_type`, `outcome`, `reason`, `publish_status`, `reindex_status`, `canonical_evidence_count`, and `assistive_evidence_count`. Do not emit raw thread text, query text, evidence excerpts, citation targets, or task notes. Update `Cairnloop.Telemetry` docs so the new event families are discoverable.</action>
  <verify>
    <automated>mix test test/cairnloop/retrieval/telemetry_test.exs</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align publish/reindex follow-through visibility with bounded telemetry and thread status copy</name>
  <files>lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_base/workers/chunk_revision.ex, test/cairnloop/knowledge_automation/review_task_test.exs, test/cairnloop/web/conversation_live_test.exs</files>
  <read_first>
    - lib/cairnloop/knowledge_automation.ex
    - lib/cairnloop/knowledge_base/workers/chunk_revision.ex
    - lib/cairnloop/web/review_task_presenter.ex
    - lib/cairnloop/web/conversation_live.ex
  </read_first>
  <acceptance_criteria>
    - publish/reindex follow-through emits bounded telemetry when status changes
    - thread-side copy keeps `approved`, `published`, `reindexing`, and `reindexed` distinct
    - `test/cairnloop/knowledge_automation/review_task_test.exs` exits 0 and covers publish/reindex event emission or durable status updates
    - `test/cairnloop/web/conversation_live_test.exs` exits 0 and asserts thread-visible follow-through vocabulary
  </acceptance_criteria>
  <action>Extend the existing publish/reindex follow-through seams so status changes also emit bounded maintenance telemetry and the thread card stays aligned with the review lane. Use the durable `record_review_task_reindex_outcome/3` path from `ChunkRevision` rather than creating a second completion signal. Keep operator-visible copy distinct for review-ready, published, reindexing, reindexed, and retry-needed states so Phase 12 does not collapse the review lane into one generic done status.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/review_task_test.exs test/cairnloop/web/conversation_live_test.exs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow truth -> telemetry | Observability must not leak sensitive payloads or become the audit record |
| publish/reindex follow-through -> thread UI | Thread copy must mirror durable truth without collapsing milestone states |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | I | maintenance telemetry | mitigate | Normalize to low-cardinality metadata and exclude raw query, thread, evidence, and citation payloads. |
| T-12-07 | T | thread/review follow-through copy | mitigate | Keep approved, published, reindexing, reindexed, and retry-needed distinct in presenter and thread copy. |
</threat_model>

<verification>
Run the telemetry and follow-through suites:

`mix test test/cairnloop/retrieval/telemetry_test.exs test/cairnloop/knowledge_automation/review_task_test.exs test/cairnloop/web/conversation_live_test.exs`
</verification>

<success_criteria>
- Maintenance telemetry is bounded and consistent across the workflow.
- Publish/reindex follow-through remains durable and visible.
- Thread and review surfaces use the same distinct state vocabulary.
</success_criteria>

