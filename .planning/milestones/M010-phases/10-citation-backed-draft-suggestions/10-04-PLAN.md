---
phase: 10
plan: "04"
type: execute
wave: 4
depends_on:
  - 10-03
files_modified:
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_base.ex
  - lib/cairnloop/web/knowledge_base_live/editor.ex
  - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex
  - test/cairnloop/knowledge_automation/article_suggestion_test.exs
  - test/cairnloop/web/knowledge_base_live_test.exs
autonomous: true
requirements:
  - DRAFT-01
  - DRAFT-02
must_haves:
  truths:
    - "Manual editing stays a deliberate post-review action and never becomes the default destination after suggestion generation."
    - "Revision suggestions reuse an existing article id for editor handoff, while new-article suggestions first acquire a host-owned non-published authoring target."
    - "Navigating into the editor preloads reviewed suggestion content but does not save a draft or publish as a side effect."
  artifacts:
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "Explicit authoring-target seam that creates or reuses a non-published article id for new-article manual editing"
    - path: "lib/cairnloop/knowledge_base.ex"
      provides: "Host-owned article creation or reuse support for suggestion-backed manual authoring targets"
    - path: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      provides: "Explicit `open_for_manual_edit` action that branches correctly for revision vs new-article suggestions"
    - path: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      provides: "Suggestion-aware editor mount that preloads reviewed markdown from `suggestion_id`"
  key_links:
    - from: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      to: "lib/cairnloop/knowledge_automation.ex"
      via: "explicit `open_for_manual_edit` seam that creates or reuses an authoring article target for `:article` suggestions before navigation"
      pattern: "create_or_reuse_authoring_article_for_suggestion"
    - from: "lib/cairnloop/knowledge_automation.ex"
      to: "lib/cairnloop/knowledge_base.ex"
      via: "host-owned article target creation or reuse before `/knowledge-base/:id/edit?suggestion_id=...` navigation"
      pattern: "create_or_reuse_authoring_article_for_suggestion|create_article"
    - from: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      to: "lib/cairnloop/knowledge_automation.ex"
      via: "load reviewed suggestion content from `suggestion_id` before any explicit save/publish action"
      pattern: "suggestion_id|get_article_suggestion!"
---

<objective>
Finish the manual-edit handoff behind the dedicated Phase 10 review surface without collapsing the review boundary.

Purpose: satisfy the deliberate editor-handoff portion of `DRAFT-01` and `DRAFT-02` after the review UI exists, while keeping draft save and publish semantics explicit and unchanged.
Output: authoring-target creation or reuse seam, suggestion-aware editor preload, and focused handoff tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-citation-backed-draft-suggestions/10-CONTEXT.md
@.planning/phases/10-citation-backed-draft-suggestions/10-RESEARCH.md
@.planning/phases/10-citation-backed-draft-suggestions/10-PATTERNS.md
@lib/cairnloop/knowledge_automation.ex
@lib/cairnloop/knowledge_base.ex
@lib/cairnloop/web/knowledge_base_live/suggestion_review.ex
@lib/cairnloop/web/knowledge_base_live/editor.ex

<interfaces>
From `lib/cairnloop/web/knowledge_base_live/editor.ex`:

```elixir
def mount(params, session, socket) do
  id = (is_map(params) && params["id"]) || session["id"]
  article = repo().get!(Article, id)
  latest_revision = KnowledgeBase.get_latest_revision(id)
  content = if latest_revision, do: latest_revision.content, else: ""
end
```

Plan 04 must preserve this existing editor route while extending it to preload suggestion content safely through `suggestion_id` after an explicit review action.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the host-owned authoring-target seam for new-article suggestions</name>
  <files>lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_base.ex, test/cairnloop/knowledge_automation/article_suggestion_test.exs</files>
  <read_first>
    - lib/cairnloop/knowledge_automation.ex
    - lib/cairnloop/knowledge_base.ex
    - lib/cairnloop/knowledge_automation/article_suggestion.ex
    - .planning/phases/10-citation-backed-draft-suggestions/10-CONTEXT.md
    - .planning/phases/10-citation-backed-draft-suggestions/10-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `lib/cairnloop/knowledge_automation.ex` contains `create_or_reuse_authoring_article_for_suggestion`
    - `lib/cairnloop/knowledge_base.ex` contains article-target creation or reuse support consumed by the suggestion handoff
    - `test/cairnloop/knowledge_automation/article_suggestion_test.exs` exits 0
  </acceptance_criteria>
  <behavior>
    - Test 1: revision suggestions reuse the existing `article_id` for manual-edit handoff and do not create a new article target.
    - Test 2: new-article suggestions call `create_or_reuse_authoring_article_for_suggestion/1`, which creates or reuses a non-published article target and returns a stable article id for navigation.
    - Test 3: creating or reusing the authoring article target does not save a draft revision or publish content as a side effect.
  </behavior>
  <action>Add an explicit host-owned manual-edit seam for article suggestions. Extend `KnowledgeAutomation` with `create_or_reuse_authoring_article_for_suggestion/1` over `KnowledgeBase` so gap-driven `:article` suggestions can materialize or reuse a non-published article record before editor navigation, while revision suggestions continue using their existing `article_id`. Keep this seam suggestion-safe: it may create or reuse an article target, but it must not write a draft revision, publish content, create review tasks, or trigger reindex work. Use this seam only from the explicit `open_for_manual_edit` action after review.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/article_suggestion_test.exs</automated>
  </verify>
  <done>New-article suggestions have a concrete host-owned article target before editor navigation, revision suggestions reuse their existing article id, and the handoff seam remains side-effect free for draft save or publish.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preload reviewed suggestion markdown into the editor on explicit handoff</name>
  <files>lib/cairnloop/web/knowledge_base_live/suggestion_review.ex, lib/cairnloop/web/knowledge_base_live/editor.ex, test/cairnloop/web/knowledge_base_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex
    - lib/cairnloop/web/knowledge_base_live/editor.ex
    - lib/cairnloop/knowledge_automation.ex
    - test/cairnloop/web/knowledge_base_live_test.exs
    - .planning/phases/10-citation-backed-draft-suggestions/10-PATTERNS.md
  </read_first>
  <acceptance_criteria>
    - `lib/cairnloop/web/knowledge_base_live/suggestion_review.ex` contains `open_for_manual_edit`
    - `lib/cairnloop/web/knowledge_base_live/editor.ex` contains `suggestion_id`
    - `lib/cairnloop/web/knowledge_base_live/editor.ex` contains `get_article_suggestion!`
    - `test/cairnloop/web/knowledge_base_live_test.exs` exits 0
  </acceptance_criteria>
  <behavior>
    - Test 1: `open_for_manual_edit` for a revision suggestion navigates to `/knowledge-base/:id/edit?suggestion_id=...` using the existing article id.
    - Test 2: `open_for_manual_edit` for a new-article suggestion first obtains the authoring article id, then navigates to `/knowledge-base/:id/edit?suggestion_id=...`.
    - Test 3: `KnowledgeBaseLive.Editor` reads `suggestion_id`, loads the reviewed suggestion, and preloads `proposed_markdown` into `content` before any save or publish action.
  </behavior>
  <action>Implement the concrete editor handoff contract. In `SuggestionReview`, branch `open_for_manual_edit` by suggestion type: revision suggestions navigate directly with the existing `article_id`, while new-article suggestions first call `create_or_reuse_authoring_article_for_suggestion/1` and then navigate with the returned id. Update `KnowledgeBaseLive.Editor` only enough to read `suggestion_id`, load the reviewed suggestion via `KnowledgeAutomation`, and preload `proposed_markdown` into `content` before any explicit save or publish action. Navigation must remain side-effect free for revision persistence and publish state.</action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live_test.exs</automated>
  </verify>
  <done>The editor handoff is fully planned end to end for both suggestion types, and reviewed suggestion content is preloaded only after an explicit post-review action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review surface -> authoring target creation | Explicit review actions may create a non-published article target for new-article suggestions |
| review surface -> KB editor | Suggested content enters authoring only after deliberate operator navigation |
| editor preload -> draft/publish flows | Loading suggestion content must not implicitly save or publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | E | editor handoff | mitigate | Require an explicit `open_for_manual_edit` action from the review surface before the existing editor loads suggestion content, preserving the review boundary and preventing generation from acting like direct authoring. |
| T-10-10 | T | authoring target seam | mitigate | Create or reuse only non-published authoring article targets for new-article suggestions and keep draft save/publish outside the handoff seam. |
| T-10-11 | S | editor preload | mitigate | Load reviewed suggestion markdown from `suggestion_id` into editor state only after a deliberate handoff and without treating preload as persistence. |
</threat_model>

<verification>
Run the focused manual-edit handoff suite:

`mix test test/cairnloop/knowledge_automation/article_suggestion_test.exs test/cairnloop/web/knowledge_base_live_test.exs`
</verification>

<success_criteria>
- Revision and new-article suggestions both have a concrete, host-owned path into manual editing.
- New-article suggestions acquire a non-published authoring target before navigation.
- Editor preload stays explicit and side-effect free for save and publish semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/10-citation-backed-draft-suggestions/10-04-SUMMARY.md`
</output>
