---
phase: M010-S01
plan: "02"
type: execute
wave: 2
depends_on:
  - M010-S01-01
files_modified:
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_automation/candidate_builder.ex
  - lib/cairnloop/knowledge_automation/manual_handling_signal.ex
  - lib/cairnloop/knowledge_automation/workers/refresh_gap_candidates.ex
  - lib/cairnloop/knowledge_automation/workers/backfill_gap_candidates.ex
  - lib/cairnloop/retrieval/gap_recorder.ex
  - lib/cairnloop/retrieval/workers/index_resolved_conversation.ex
  - test/cairnloop/knowledge_automation/candidate_builder_test.exs
  - test/cairnloop/knowledge_automation/workers/refresh_gap_candidates_test.exs
autonomous: true
requirements:
  - GAP-01
  - GAP-02
must_haves:
  truths:
    - "Related no-hit, weak-grounding, and repeated manual-handling evidence collapses into one stable candidate identity."
    - "Candidate ranking is deterministic and explainable from persisted score components."
    - "New retrieval gap events and resolved manual-handling cases can refresh the candidate queue in the background without mutating the evidence ledger."
    - "Phase 9 uses lexical-first clustering only, requires at least two durable manual-handling cases to raise a standalone manual-handling candidate, and recomputes candidate counts from retained evidence on every refresh."
  artifacts:
    - path: "lib/cairnloop/knowledge_automation/candidate_builder.ex"
      provides: "Deterministic bucketing, clustering, and score-component calculation"
    - path: "lib/cairnloop/knowledge_automation/manual_handling_signal.ex"
      provides: "Projection of repeated manual handling from durable draft and resolved-case state"
    - path: "lib/cairnloop/knowledge_automation/workers/refresh_gap_candidates.ex"
      provides: "Background refresh worker for open candidates"
    - path: "lib/cairnloop/knowledge_automation/workers/backfill_gap_candidates.ex"
      provides: "Rebuild worker for historical replay"
  key_links:
    - from: "lib/cairnloop/retrieval/gap_recorder.ex"
      to: "lib/cairnloop/knowledge_automation/workers/refresh_gap_candidates.ex"
      via: "post-insert refresh scheduling"
      pattern: "schedule_gap_candidate_refresh"
    - from: "lib/cairnloop/retrieval/workers/index_resolved_conversation.ex"
      to: "lib/cairnloop/knowledge_automation/manual_handling_signal.ex"
      via: "resolved-case evidence refresh"
      pattern: "schedule_gap_candidate_refresh"
    - from: "lib/cairnloop/knowledge_automation/candidate_builder.ex"
      to: "lib/cairnloop/knowledge_automation/gap_candidate_membership.ex"
      via: "stable_key and membership generation"
      pattern: "stable_key"
---

<objective>
Turn the new read model into a working discovery pipeline that clusters evidence deterministically and refreshes the ranked queue via Oban.

Purpose: satisfy the durable clustering and explainable ranking parts of `GAP-01` and `GAP-02` without pulling in Phase 10 suggestion work.
Output: candidate builder, manual-handling projection, refresh/rebuild workers, and the input-seam scheduling hooks needed to keep candidates current.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/M010-KB-AI-MAINTENANCE-SPEC.md
@.planning/milestones/M010-phases/M010-S01/M010-S01-RESEARCH.md
@.planning/milestones/M010-phases/M010-S01/M010-S01-PATTERNS.md
@lib/cairnloop/retrieval/gap_recorder.ex
@lib/cairnloop/retrieval/workers/index_resolved_conversation.ex
@lib/cairnloop/retrieval/resolved_case_evidence.ex
@lib/cairnloop/automation/draft.ex
@lib/cairnloop/retrieval/ranker.ex

<interfaces>
From `lib/cairnloop/automation/draft.ex`:

```elixir
field(:proposal_type, Ecto.Enum, values: [:reply, :clarification, :escalation], default: :reply)
field(:status, Ecto.Enum, values: [:pending, :approved, :edited, :discarded], default: :pending)
field(:clarification_attempts, :integer, default: 0)
belongs_to(:conversation, Cairnloop.Conversation)
```

From `lib/cairnloop/retrieval/resolved_case_evidence.ex`:

```elixir
schema "cairnloop_resolved_case_evidences" do
  field :subject, :string
  field :issue_summary, :string
  field :resolution_note, :string
  field :actions_taken, {:array, :string}, default: []
  field :outcome, :string
  field :resolved_at, :utc_datetime_usec
  field :host_user_id, :string
  field :citation_backreferences, {:array, :map}, default: []
  belongs_to :conversation, Cairnloop.Conversation
end
```

Repeated manual handling must be derived from these durable workflow artifacts, not from ad hoc transcript mining.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic clustering, manual-handling projection, and score calculation</name>
  <files>lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_automation/candidate_builder.ex, lib/cairnloop/knowledge_automation/manual_handling_signal.ex, test/cairnloop/knowledge_automation/candidate_builder_test.exs</files>
  <behavior>
    - Test 1: compatible retrieval gap events with matching scope and normalized topic seed collapse into one `stable_key`.
    - Test 2: repeated manual handling is projected only from durable draft/resolved-case state such as edited drafts or clarification/escalation drafts on later-resolved conversations.
    - Test 3: persisted `score_components` and final `score` stay deterministic for the same evidence set and rank manual handling above equivalent pure no-hit volume.
  </behavior>
  <action>Implement the Phase 9 builder pipeline per the research contract: lexical-first bucketing by `tenant_scope`, `host_user_id`, failure family, and normalized topic seed; explicit membership generation back to either `Retrieval.GapEvent` or manual-handling source records; and transparent scoring with separate components for evidence volume, recency, weak-grounding weight, and manual-handling weight. Lock these defaults in code and tests: no semantic merge or embedding-based clustering in Phase 9, standalone manual-handling candidates require at least two durable manual-handling cases in the same bucket within the active 90-day evidence window, and candidate refresh always recomputes counts from retained evidence rather than preserving stale totals. Keep ranking explainable and deterministic, and keep repeated manual handling derived from `Automation.Draft` + `ResolvedCaseEvidence` state rather than transcript scraping. Do not add any LLM-based topic naming, semantic global clustering, or draft-suggestion behavior in this plan.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/candidate_builder_test.exs</automated>
  </verify>
  <done>The builder produces stable candidate identities, explicit memberships, and deterministic score components from retrieval gaps plus durable manual-handling evidence, with focused tests proving the lexical-only clustering rule, the explicit manual-handling threshold, and retention-aware recomputation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire refresh and rebuild workers to the existing durable evidence seams</name>
  <files>lib/cairnloop/knowledge_automation/workers/refresh_gap_candidates.ex, lib/cairnloop/knowledge_automation/workers/backfill_gap_candidates.ex, lib/cairnloop/retrieval/gap_recorder.ex, lib/cairnloop/retrieval/workers/index_resolved_conversation.ex, test/cairnloop/knowledge_automation/workers/refresh_gap_candidates_test.exs</files>
  <behavior>
    - Test 1: `RefreshGapCandidates` rebuilds candidate rows idempotently from recent gap events plus manual-handling projections.
    - Test 2: `BackfillGapCandidates` replays historical evidence through the same builder path instead of a second clustering implementation.
    - Test 3: successful retrieval gap writes and successful resolved-case evidence indexing schedule candidate refresh work without changing the outcome of those existing flows if refresh enqueue fails.
  </behavior>
  <action>Create dedicated Oban workers for refresh and backfill, using the same builder path for both incremental refresh and historical rebuild. Update `Cairnloop.KnowledgeAutomation` so `refresh_gap_candidates/1`, `schedule_gap_candidate_refresh/1`, and `rebuild_gap_candidates/1` call those workers or their synchronous equivalents. Hook scheduling into `GapRecorder.record/2` after successful event persistence and into `IndexResolvedConversation.perform/1` after resolved-case evidence is stored, because those are the existing durable Phase 9 input seams. Refresh logic must remove open candidates that lose all memberships after evidence pruning and must recompute counts only from currently retained evidence. Keep refresh best-effort and secondary to the primary write path, mirroring the repo’s current prune scheduling posture. Do not hook from telemetry handlers, do not mine conversations directly, and do not enqueue suggestion/review jobs.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/candidate_builder_test.exs test/cairnloop/knowledge_automation/workers/refresh_gap_candidates_test.exs</automated>
  </verify>
  <done>Background refresh and rebuild workers exist, both reuse the same deterministic builder, and Phase 9 candidate refresh is triggered from existing durable evidence boundaries without changing retrieval-gap or resolved-case persistence semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable workflow state -> manual-handling projection | Draft/resolution state is promoted into ranked maintenance candidates |
| background workers -> candidate tables | Async refresh can overwrite read-model state if bucketing is unstable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M010-S01-04 | T | `candidate_builder.ex` | mitigate | Use deterministic stable-key construction and explicit membership replacement inside one transaction so refreshes cannot drift candidate identity across runs. |
| T-M010-S01-05 | R | `manual_handling_signal.ex` | mitigate | Derive repeated manual handling only from durable `Draft` state and `ResolvedCaseEvidence`, preserving inspectable source records instead of unverifiable transcript heuristics. |
| T-M010-S01-06 | D | refresh worker scheduling | mitigate | Keep refresh enqueue best-effort and idempotent, and make worker execution rebuild from source evidence so duplicate jobs do not corrupt the queue. |
</threat_model>

<verification>
Run the deterministic clustering and worker suite:

`mix test test/cairnloop/knowledge_automation/candidate_builder_test.exs test/cairnloop/knowledge_automation/workers/refresh_gap_candidates_test.exs`
</verification>

<success_criteria>
- Phase 9 has a deterministic builder that can merge related retrieval and manual-handling evidence under one stable candidate identity.
- Ranking inputs are persisted and explainable.
- Refresh and rebuild work run through Oban-backed seams and are triggered only from existing durable evidence boundaries.
</success_criteria>

<output>
After completion, create `.planning/milestones/M010-phases/M010-S01/M010-S01-02-SUMMARY.md`
</output>
