---
phase: M010-S01
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/*_add_gap_candidates_and_memberships.exs
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_automation/gap_candidate.ex
  - lib/cairnloop/knowledge_automation/gap_candidate_membership.ex
  - test/cairnloop/knowledge_automation/gap_candidate_test.exs
autonomous: true
requirements:
  - GAP-02
must_haves:
  truths:
    - "Gap candidates have a durable identity separate from raw retrieval gap events."
    - "Each candidate stores freshness, counts, and score inputs that later ranking and UI work can query without reclustering in LiveView."
    - "Every candidate can point back to explicit source memberships instead of flattening evidence into opaque JSON."
  artifacts:
    - path: "lib/cairnloop/knowledge_automation/gap_candidate.ex"
      provides: "Durable candidate schema with stable key, status, counts, score metadata, and freshness fields"
    - path: "lib/cairnloop/knowledge_automation/gap_candidate_membership.ex"
      provides: "Explicit links from a candidate to retrieval gap events or manual-handling source records"
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "Public read-model API for listing candidates and loading one candidate with memberships"
    - path: "priv/repo/migrations/*_add_gap_candidates_and_memberships.exs"
      provides: "Tables and indexes for the durable candidate read model"
  key_links:
    - from: "lib/cairnloop/knowledge_automation.ex"
      to: "lib/cairnloop/knowledge_automation/gap_candidate.ex"
      via: "Ecto-backed list/get functions"
      pattern: "from\\(candidate in GapCandidate"
    - from: "lib/cairnloop/knowledge_automation/gap_candidate_membership.ex"
      to: "lib/cairnloop/retrieval/gap_event.ex"
      via: "source_type + source_id references"
      pattern: "source_type"
---

<objective>
Establish the durable Phase 9 read model so gap discovery no longer depends on querying raw `cairnloop_retrieval_gap_events` directly.

Purpose: satisfy the stable identity and explicit membership requirements behind `GAP-02` before any clustering job or UI surface is built.
Output: migration, schemas, and a narrow `Cairnloop.KnowledgeAutomation` public API for read-model queries.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/M010-KB-AI-MAINTENANCE-SPEC.md
@.planning/milestones/M010-phases/M010-S01/M010-S01-RESEARCH.md
@.planning/milestones/M010-phases/M010-S01/M010-S01-PATTERNS.md
@.planning/milestones/M009-phases/M009-S08/M009-S08-01-SUMMARY.md
@lib/cairnloop/retrieval/gap_event.ex
@lib/cairnloop/retrieval/gap_recorder.ex

<interfaces>
From `lib/cairnloop/retrieval/gap_event.ex`:

```elixir
schema "cairnloop_retrieval_gap_events" do
  field(:surface, Ecto.Enum, values: [:draft_generation, :search_modal, :api, :unspecified])
  field(:outcome_class, Ecto.Enum, values: [:empty_recall, :retrieval_error, :weak_grounding, :policy_limit])
  field(:reason, Ecto.Enum, values: [...])
  field(:host_user_id, :string)
  field(:tenant_scope, Ecto.Enum, values: [:host_user_scoped, :public_only, :system_unscoped])
  field(:ui_surface, Ecto.Enum, values: [:conversation, :inbox, :settings, :unspecified], default: :unspecified)
  field(:query_fingerprint, :string)
  field(:sanitized_query_excerpt, :string)
  field(:canonical_hit_count, :integer, default: 0)
  field(:assistive_hit_count, :integer, default: 0)
  field(:clarification_attempts, :integer, default: 0)
  embeds_many(:attempted_evidence_snapshots, GapEventSnapshot, on_replace: :delete)
end
```

Plan 01 must keep `cairnloop_retrieval_gap_events` immutable and build Phase 9 state beside it, not inside it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the durable gap-candidate storage contract</name>
  <files>priv/repo/migrations/*_add_gap_candidates_and_memberships.exs, lib/cairnloop/knowledge_automation/gap_candidate.ex, lib/cairnloop/knowledge_automation/gap_candidate_membership.ex, test/cairnloop/knowledge_automation/gap_candidate_test.exs</files>
  <behavior>
    - Test 1: a `GapCandidate` changeset accepts deterministic identity fields, freshness timestamps, count fields, and score-components metadata while rejecting missing stable identity fields.
    - Test 2: a `GapCandidateMembership` changeset preserves explicit `source_type` and `source_id` links without requiring mutation of `Retrieval.GapEvent`.
    - Test 3: the migration shape creates separate candidate and membership tables plus indexes for `stable_key`, `status`, `last_seen_at`, and `(gap_candidate_id, source_type, source_id)`.
  </behavior>
  <action>Create the new read-model tables and schemas per `GAP-02`, keeping `cairnloop_retrieval_gap_events` as the immutable evidence ledger. Use `Ecto.Enum` for bounded candidate state and membership source types, store explicit `stable_key`, `title`, `seed_excerpt`, `tenant_scope`, `host_user_id`, `ui_surface`, `first_seen_at`, `last_seen_at`, `evidence_count`, `manual_case_count`, `weak_grounding_count`, `no_hit_count`, `score`, and a bounded `score_components` map, and model memberships as their own table rather than embedded JSON so later detail views can join back to source evidence directly. Do not introduce any draft-suggestion, review-task, or publish-state fields in this phase.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/gap_candidate_test.exs</automated>
  </verify>
  <done>The repo has a migration and schemas for durable gap candidates plus explicit memberships, and the tests prove the storage contract matches the Phase 9 identity/freshness/count requirements without mutating retrieval gap events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the narrow public context API for candidate queries</name>
  <files>lib/cairnloop/knowledge_automation.ex, test/cairnloop/knowledge_automation/gap_candidate_test.exs</files>
  <behavior>
    - Test 1: `list_gap_candidates/1` returns candidates ordered by persisted `score DESC, last_seen_at DESC, id DESC`.
    - Test 2: `get_gap_candidate!/2` loads one candidate with memberships and refuses to cross scope boundaries when `tenant_scope` / `host_user_id` filters are supplied.
    - Test 3: `refresh_gap_candidates/1` and `schedule_gap_candidate_refresh/1` exist as public seams even if worker implementation arrives in the next plan.
  </behavior>
  <action>Create `Cairnloop.KnowledgeAutomation` as the Cairnloop-owned Phase 9 facade. Expose `list_gap_candidates/1`, `get_gap_candidate!/2`, `refresh_gap_candidates/1`, `schedule_gap_candidate_refresh/1`, and `rebuild_gap_candidates/1` signatures now so downstream plans have stable contracts. Implement only the read/query seams in this plan, preloading memberships through explicit associations and preserving deterministic ordering. Note in the code and tests that refresh and rebuild are Phase 9 read-model maintenance seams, not draft-generation entrypoints.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/gap_candidate_test.exs</automated>
  </verify>
  <done>`Cairnloop.KnowledgeAutomation` exists with stable list/get/refresh API contracts, candidate queries preload memberships, and ordering/scope behavior is locked by focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retrieval evidence ledger -> gap candidate read model | Untrusted or noisy evidence is projected into operator-facing state |
| operator query filters -> context API | Dashboard callers may request data outside their scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M010-S01-01 | T | `GapCandidate` / `GapCandidateMembership` persistence | mitigate | Keep source evidence immutable per Phase 8 and persist candidate state in separate tables with explicit membership rows instead of updating `Retrieval.GapEvent` records. |
| T-M010-S01-02 | I | `Cairnloop.KnowledgeAutomation.get_gap_candidate!/2` | mitigate | Require scoped query filters (`tenant_scope`, `host_user_id`) in the context API and lock them with focused tests so host-user-scoped candidates are not exposed across operators. |
| T-M010-S01-03 | D | candidate list queries | accept | Read-only ranking queries are low-value targets in this phase; mitigate enough with indexed `stable_key`, `status`, and `last_seen_at` columns, and defer broader ops tuning to later verification if query load proves material. |
</threat_model>

<verification>
Run the focused candidate storage suite:

`mix test test/cairnloop/knowledge_automation/gap_candidate_test.exs`
</verification>

<success_criteria>
- Gap candidates and memberships exist as first-class tables beside `cairnloop_retrieval_gap_events`.
- The public `Cairnloop.KnowledgeAutomation` facade exposes deterministic query contracts for later worker and UI plans.
- No Phase 10 or Phase 11 concepts leak into the Phase 9 storage layer.
</success_criteria>

<output>
After completion, create `.planning/milestones/M010-phases/M010-S01/M010-S01-01-SUMMARY.md`
</output>
