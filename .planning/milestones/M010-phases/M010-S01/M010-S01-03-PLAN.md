---
phase: M010-S01
plan: "03"
type: execute
wave: 3
depends_on:
  - M010-S01-02
files_modified:
  - lib/cairnloop/router.ex
  - lib/cairnloop/web/gap_candidate_presenter.ex
  - lib/cairnloop/web/knowledge_base_live/index.ex
  - lib/cairnloop/web/knowledge_base_live/gaps.ex
  - test/cairnloop/web/knowledge_base_live/gaps_test.exs
autonomous: true
requirements:
  - GAP-01
  - GAP-03
must_haves:
  truths:
    - "Operators can open a KB gaps dashboard and see a ranked queue instead of raw retrieval telemetry."
    - "Each candidate detail shows why it was raised, including grouped retrieval gap events and similar-case/manual-handling evidence."
    - "The dashboard uses the durable Phase 9 read model rather than reclustering raw gap events in LiveView."
  artifacts:
    - path: "lib/cairnloop/web/knowledge_base_live/gaps.ex"
      provides: "LiveView list/detail dashboard for ranked gap candidates"
    - path: "lib/cairnloop/router.ex"
      provides: "Dashboard route for `/knowledge-base/gaps`"
    - path: "test/cairnloop/web/knowledge_base_live/gaps_test.exs"
      provides: "Focused UI and interaction proof for list/detail rendering"
  key_links:
    - from: "lib/cairnloop/router.ex"
      to: "lib/cairnloop/web/knowledge_base_live/gaps.ex"
      via: "dashboard live route"
      pattern: "live\\(\"/knowledge-base/gaps\""
    - from: "lib/cairnloop/web/knowledge_base_live/gaps.ex"
      to: "lib/cairnloop/knowledge_automation.ex"
      via: "list_gap_candidates/get_gap_candidate!"
      pattern: "KnowledgeAutomation"
---

<objective>
Deliver the operator-facing Phase 9 surface: a ranked KB gaps dashboard with inspectable evidence detail.

Purpose: satisfy `GAP-01` and `GAP-03` by turning the durable candidate queue into a usable LiveView workflow while keeping discovery separate from draft generation or review.
Output: `/knowledge-base/gaps` route, LiveView list/detail surface, and focused UI tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/M010-KB-AI-MAINTENANCE-SPEC.md
@.planning/milestones/M010-phases/M010-S01/M010-S01-RESEARCH.md
@lib/cairnloop/router.ex
@lib/cairnloop/web/knowledge_base_live/index.ex
@lib/cairnloop/web/conversation_live.ex
@lib/cairnloop/web/search_result_presenter.ex

<interfaces>
From `lib/cairnloop/router.ex`:

```elixir
live("/", Cairnloop.Web.InboxLive, :index, as: :cairnloop_inbox)
live("/knowledge-base", Cairnloop.Web.KnowledgeBaseLive.Index, :index)
live("/knowledge-base/:id/edit", Cairnloop.Web.KnowledgeBaseLive.Editor, :edit)
live("/settings", Cairnloop.Web.SettingsLive, :index, as: :cairnloop_settings)
live("/:id", Cairnloop.Web.ConversationLive, :show, as: :cairnloop_conversation)
```

From `lib/cairnloop/web/search_result_presenter.ex`:

```elixir
def section_name(%Result{source_type: :knowledge_base}), do: "Knowledge Base"
def section_name(%Result{source_type: :resolved_case}), do: "Similar resolved cases"
def source_label(%Result{source_type: :resolved_case}), do: "Resolved case"
def open_action_label(%Result{source_type: :resolved_case}), do: "Open resolved case"
```

Carry the same calm trust language into the new dashboard instead of exposing raw telemetry/debug copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the KB gaps route and ranked queue surface</name>
  <files>lib/cairnloop/router.ex, lib/cairnloop/web/gap_candidate_presenter.ex, lib/cairnloop/web/knowledge_base_live/index.ex, lib/cairnloop/web/knowledge_base_live/gaps.ex, test/cairnloop/web/knowledge_base_live/gaps_test.exs</files>
  <behavior>
    - Test 1: operators can navigate to `/knowledge-base/gaps` from the existing dashboard shell.
    - Test 2: the page renders ranked candidates from `KnowledgeAutomation.list_gap_candidates/1` with title, raised-by reason, evidence counts, manual-case counts, and freshness labels.
    - Test 3: the empty state is explicit and calm when no candidates exist, rather than showing synthetic or placeholder AI output.
  </behavior>
  <action>Create a dedicated `KnowledgeBaseLive.Gaps` LiveView and route it under the existing dashboard session at `/knowledge-base/gaps`. Add `Cairnloop.Web.GapCandidatePresenter` as the presenter seam for trust labels, reason copy, freshness labels, and dominant-source wording so that formatting does not sprawl across the LiveView. Update the current KB index only enough to link operators into the new surface; keep published-article management and gap discovery visually adjacent but logically separate. Render the queue from the durable read model, not from direct `GapEvent` clustering in the LiveView, and preserve the repo’s evidence-first language by showing explicit reason labels (`no hit`, `weak grounding`, `manual handling`, or mixed), counts, and freshness instead of raw telemetry fields or fake precision.</action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live/gaps_test.exs</automated>
  </verify>
  <done>Operators can reach `/knowledge-base/gaps`, the list is backed by `KnowledgeAutomation.list_gap_candidates/1`, and the UI exposes a ranked queue with honest empty-state behavior through a dedicated presenter seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render inspectable evidence detail for each gap candidate</name>
  <files>lib/cairnloop/web/gap_candidate_presenter.ex, lib/cairnloop/web/knowledge_base_live/gaps.ex, test/cairnloop/web/knowledge_base_live/gaps_test.exs</files>
  <behavior>
    - Test 1: selecting a candidate shows grouped retrieval gap events with surface, reason, hit counts, and timestamps.
    - Test 2: detail panes show similar resolved cases / manual-handling evidence with issue summary, resolution note, actions taken, and conversation navigation targets when available.
    - Test 3: the detail view states why the candidate was raised using persisted score components or source-mix rollups, not opaque model-generated prose.
  </behavior>
  <action>Extend the same LiveView with a detail pane that loads `KnowledgeAutomation.get_gap_candidate!/2` and renders underlying retrieval memberships alongside manual-handling evidence. Route reason labels, freshness labels, and dominant-source explanations through `GapCandidatePresenter`, reusing the trust posture from `ConversationLive` and `SearchResultPresenter`: separate canonical KB vs similar resolved-case cues, clear timestamps, and direct navigation targets when source records expose them. Keep the explanation deterministic by deriving “why raised” from membership mix and persisted `score_components`; do not add suggestion-generation buttons, review workflow actions, or publish controls in this phase.</action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live/gaps_test.exs</automated>
  </verify>
  <done>The dashboard detail view lets an operator inspect the exact retrieval and manual-handling evidence behind a candidate, with deterministic “why raised” explanations and no Phase 10/11 controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gap candidate read model -> operator dashboard | Ranked maintenance state becomes actionable UI |
| candidate evidence detail -> navigation targets | Source links may expose unrelated records if scoping is not preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M010-S01-07 | I | `KnowledgeBaseLive.Gaps` detail view | mitigate | Load candidates through scoped `KnowledgeAutomation.get_gap_candidate!/2` queries and only expose navigation targets already present on authorized source records. |
| T-M010-S01-08 | S | operator-facing ranking explanation | mitigate | Use persisted `score_components` and membership rollups for “why raised” copy so the UI cannot spoof precision with ad hoc or model-generated explanations. |
| T-M010-S01-09 | D | LiveView dashboard | accept | Candidate browsing is an internal operator surface; keep the page read-only in Phase 9 and rely on the background read model for expensive work instead of on-demand clustering in the request path. |
</threat_model>

<verification>
Run the focused dashboard suite:

`mix test test/cairnloop/web/knowledge_base_live/gaps_test.exs`
</verification>

<success_criteria>
- Operators can open a dedicated KB gaps dashboard from the existing dashboard session.
- The list is ranked from durable candidate state, not from raw telemetry or LiveView-side clustering.
- Evidence detail makes each candidate inspectable without leaking into suggestion, review, or publish workflows.
</success_criteria>

<output>
After completion, create `.planning/milestones/M010-phases/M010-S01/M010-S01-03-SUMMARY.md`
</output>
