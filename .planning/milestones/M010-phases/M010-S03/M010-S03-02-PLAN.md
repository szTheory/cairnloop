---
phase: M010-S03
plan: "02"
type: execute
wave: 2
depends_on:
  - M010-S03-01
files_modified:
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_automation/review_task.ex
  - lib/cairnloop/knowledge_base.ex
  - test/cairnloop/knowledge_automation/review_task_test.exs
  - test/cairnloop/knowledge_base_test.exs
autonomous: true
requirements:
  - REVIEW-02
  - REVIEW-03
  - OPS-02
must_haves:
  truths:
    - "Approve stages a normal KB draft and moves the task to `approved_ready_to_publish`; it does not publish."
    - "Reject and defer record structured decisions with bounded reasons and append-only history."
    - "Approve fails closed when `KnowledgeBase.save_draft/2` would overwrite an unrelated active draft."
    - "Publish is a separate task action that reuses `KnowledgeBase.publish_revision/1` and blocks stale revision-task publishes before canonical state changes."
  artifacts:
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "Approve, reject, defer, and publish review-task command seams with freshness and collision guardrails"
    - path: "lib/cairnloop/knowledge_automation/review_task.ex"
      provides: "State-transition helpers and bounded decision validation for review workflow semantics"
    - path: "lib/cairnloop/knowledge_base.ex"
      provides: "Read helpers needed to compare latest draft/published revision state while keeping `publish_revision/1` as the only canonical publish path"
  key_links:
    - from: "lib/cairnloop/knowledge_automation.ex"
      to: "lib/cairnloop/knowledge_base.ex"
      via: "approval stages draft through guarded `save_draft/2`; publish reuses `publish_revision/1`"
      pattern: "save_draft|publish_revision"
    - from: "lib/cairnloop/knowledge_automation.ex"
      to: "lib/cairnloop/knowledge_automation/review_task.ex"
      via: "transition validation and reason taxonomy"
      pattern: "approved_ready_to_publish|rejected|deferred|review_needed"
    - from: "lib/cairnloop/knowledge_automation.ex"
      to: "lib/cairnloop/knowledge_automation/review_task_event.ex"
      via: "append-only command history"
      pattern: "event_type"
---

<objective>
Implement review-task command semantics: structured decisions, guarded draft staging, and separate publish.

Purpose: satisfy `REVIEW-02`, `REVIEW-03`, and the canonical publish-path part of `OPS-02` per D-09 through D-13 and D-24 through D-26.
Output: approve/reject/defer/publish commands, bounded reason taxonomy, draft-collision guardrails, and freshness checks that preserve `KnowledgeBase.publish_revision/1` as the only canonical publish path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/M010-KB-AI-MAINTENANCE-SPEC.md
@.planning/milestones/M010-phases/M010-S03/M010-S03-CONTEXT.md
@.planning/milestones/M010-phases/M010-S03/M010-S03-RESEARCH.md
@lib/cairnloop/knowledge_automation.ex
@lib/cairnloop/knowledge_base.ex
@test/cairnloop/knowledge_base_test.exs

<interfaces>
From `lib/cairnloop/knowledge_base.ex`:

```elixir
def save_draft(article, content_attrs) do
  latest = get_latest_revision(article.id)

  if latest && latest.state == :draft do
    Ecto.Multi.new()
    |> Ecto.Multi.update(:revision, Revision.changeset(latest, attrs))
  else
    ...
  end
end

def publish_revision(revision) do
  Ecto.Multi.new()
  |> Ecto.Multi.update(:revision, Revision.changeset(revision, %{state: :published}))
  |> Ecto.Multi.insert(:chunk_job, Cairnloop.KnowledgeBase.Workers.ChunkRevision.new(%{revision_id: revision.id}))
  |> repo().transaction()
end
```

Plan 02 must explicitly guard the `save_draft/2` collision risk from research, and it must call `publish_revision/1` unchanged as the only canonical publish path per D-11.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement approve, reject, and defer semantics with structured audit history</name>
  <files>lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_automation/review_task.ex, test/cairnloop/knowledge_automation/review_task_test.exs, test/cairnloop/knowledge_base_test.exs</files>
  <behavior>
    - Test 1: `approve_review_task/2` stages or updates a normal KB draft revision, records decision metadata, and moves the task to `approved_ready_to_publish` without publishing.
    - Test 2: if the latest draft for the article exists but is not the task's own `staged_revision_id`, approval fails closed with a bounded `draft_conflict`-style reason instead of overwriting unrelated draft content.
    - Test 3: `reject_review_task/2` and `defer_review_task/2` require bounded reasons per D-21, append a task event, and keep freeform notes optional.
    - Test 4: repeated approve calls update only the task-owned staged draft or create one when none exists; they do not create multiple competing drafts for the same task.
  </behavior>
  <action>Implement `approve_review_task/2`, `reject_review_task/2`, and `defer_review_task/2` in `KnowledgeAutomation` per D-09, D-10, D-19, D-20, D-21, D-22, and D-23. For approval, load the linked suggestion, determine the target article, and stage suggestion-backed markdown through `KnowledgeBase.save_draft/2` only after checking the current latest revision. If the latest revision is a draft and its id differs from the task's stored `staged_revision_id`, fail closed and record a structured event describing the collision risk from research item 1 rather than mutating that unrelated draft. When approval succeeds, store `staged_revision_id`, `staged_article_id`, `last_decision`, `last_reason`, `last_actor_id`, `last_decided_at`, move status to `approved_ready_to_publish`, and append a task event. Reject and defer should never touch KB draft state, but must persist structured reason, actor, timestamp, optional note, and an append-only event.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/review_task_test.exs test/cairnloop/knowledge_base_test.exs</automated>
  </verify>
  <done>Approval stages a guarded draft without publishing, reject/defer create structured history, and unrelated draft collisions are blocked instead of silently overwritten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement separate publish with base-revision freshness guards</name>
  <files>lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_base.ex, test/cairnloop/knowledge_automation/review_task_test.exs, test/cairnloop/knowledge_base_test.exs</files>
  <behavior>
    - Test 1: `publish_review_task/2` only accepts tasks in `approved_ready_to_publish` and calls `KnowledgeBase.publish_revision/1` with the staged draft revision.
    - Test 2: revision-based tasks compare the task's reviewed `base_revision_id_snapshot` or equivalent stored suggestion anchor against `KnowledgeBase.get_latest_active_revision/1`; if the base has changed, publish is blocked and the task returns to `review_needed`.
    - Test 3: article-creation tasks publish their staged draft through the same `KnowledgeBase.publish_revision/1` path and record canonical revision linkage on the task.
    - Test 4: publish command appends durable history and records `publish_status` as started or succeeded, without inventing a second publish worker or direct repo mutation path.
  </behavior>
  <action>Implement `publish_review_task/2` per D-11, D-12, D-24, D-25, and D-26. Load the staged revision referenced by the task, verify the task is in `approved_ready_to_publish`, and, for revision suggestions, re-check freshness against `KnowledgeBase.get_latest_active_revision/1` before any publish attempt. On freshness mismatch, move the task to `review_needed`, set a bounded stale-base reason, append a task event, and do not call `publish_revision/1`. On valid publish, call `KnowledgeBase.publish_revision/1` unchanged, store `published_revision_id`, `published_at`, and a transitional `publish_status` that later follow-through work can refine, then append a `publish_started` or `publish_succeeded` event depending on the implementation shape. Do not add convenience `approve and publish now`, do not bypass the task command from the editor, and do not add Phase 12 telemetry or governance work here.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/review_task_test.exs test/cairnloop/knowledge_base_test.exs</automated>
  </verify>
  <done>Publish is an explicit review-task action, revision freshness is re-checked before canonical state changes, and `KnowledgeBase.publish_revision/1` remains the only publish boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review decision -> KB draft staging | Operator approval can mutate draft state and must not overwrite unrelated work |
| approved task -> canonical publish | Publish must remain an explicit, freshness-checked boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M010-S03-04 | T | approval via `save_draft/2` | mitigate | Compare the latest draft revision against the task-owned `staged_revision_id` and fail closed on unrelated draft collisions before calling `save_draft/2`. |
| T-M010-S03-05 | E | approve semantics | mitigate | Keep `approve_review_task/2` draft-only and require `publish_review_task/2` as a separate action per D-09 through D-11. |
| T-M010-S03-06 | T | revision publish freshness | mitigate | Re-check `base_revision_id` against `KnowledgeBase.get_latest_active_revision/1` before publish and push stale tasks back to `review_needed` per D-12. |
</threat_model>

<verification>
Run the focused review-command suite:

`mix test test/cairnloop/knowledge_automation/review_task_test.exs test/cairnloop/knowledge_base_test.exs`
</verification>

<success_criteria>
- Approval stages drafts without publishing and without overwriting unrelated in-progress drafts.
- Reject and defer produce structured, queryable review history.
- Publish remains explicit and canonical through `KnowledgeBase.publish_revision/1`.
</success_criteria>

<output>
After completion, create `.planning/milestones/M010-phases/M010-S03/M010-S03-02-SUMMARY.md`
</output>
