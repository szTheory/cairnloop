---
phase: M010-S03
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/*_add_review_tasks_and_events.exs
  - lib/cairnloop/knowledge_automation/review_task.ex
  - lib/cairnloop/knowledge_automation/review_task_event.ex
  - lib/cairnloop/knowledge_automation.ex
  - test/cairnloop/knowledge_automation/review_task_test.exs
autonomous: true
requirements:
  - REVIEW-01
  - REVIEW-03
must_haves:
  truths:
    - "Each AI proposal gains one host-owned review task without turning `ArticleSuggestion` into the workflow record."
    - "Review state, structured decisions, and canonical publish outcome references are stored separately from the proposal markdown and evidence snapshot."
    - "The shared review inbox can query task rows by queue state while still showing the underlying suggestion evidence and proposal body."
    - "Every workflow transition leaves append-only, queryable history instead of relying on freeform comments."
  artifacts:
    - path: "lib/cairnloop/knowledge_automation/review_task.ex"
      provides: "Durable review-task schema with queue state, decision metadata, staged draft linkage, publish linkage, and re-review markers"
    - path: "lib/cairnloop/knowledge_automation/review_task_event.ex"
      provides: "Append-only relational audit history for review-task transitions and operator decisions"
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "Public task query and creation seams that wrap `ArticleSuggestion` without mutating proposal truth"
    - path: "priv/repo/migrations/*_add_review_tasks_and_events.exs"
      provides: "Tables and indexes enforcing one active review task per suggestion plus task-history lookup"
  key_links:
    - from: "lib/cairnloop/knowledge_automation.ex"
      to: "lib/cairnloop/knowledge_automation/review_task.ex"
      via: "task list/detail/create-or-get seams"
      pattern: "ReviewTask"
    - from: "lib/cairnloop/knowledge_automation/review_task.ex"
      to: "lib/cairnloop/knowledge_automation/article_suggestion.ex"
      via: "proposal link stays one-way through `article_suggestion_id`"
      pattern: "article_suggestion_id"
    - from: "lib/cairnloop/knowledge_automation/review_task_event.ex"
      to: "lib/cairnloop/knowledge_automation/review_task.ex"
      via: "append-only task history"
      pattern: "review_task_id"
---

<objective>
Establish the durable review-task storage contract and public query APIs before approval or UI work begins.

Purpose: satisfy the review-state separation and audit-history prerequisites behind `REVIEW-01` and `REVIEW-03`, per D-01 through D-08 and D-19 through D-23.
Output: migration, `ReviewTask` and `ReviewTaskEvent` schemas, and scoped `Cairnloop.KnowledgeAutomation` task query/create seams.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/M010-KB-AI-MAINTENANCE-SPEC.md
@.planning/milestones/M010-phases/M010-S03/M010-S03-CONTEXT.md
@.planning/milestones/M010-phases/M010-S03/M010-S03-RESEARCH.md
@.planning/milestones/M010-phases/M010-S03/M010-S03-PATTERNS.md
@lib/cairnloop/knowledge_automation.ex
@lib/cairnloop/knowledge_automation/article_suggestion.ex

<interfaces>
From `lib/cairnloop/knowledge_automation.ex`:

```elixir
def list_article_suggestions(opts \\ []) do
  ArticleSuggestion
  |> apply_scope(opts)
  |> maybe_filter_article_suggestion_status(opts)
  |> order_by([suggestion], desc: suggestion.inserted_at, desc: suggestion.id)
  |> repo().all()
end

def get_article_suggestion!(id, opts \\ []) do
  ArticleSuggestion
  |> apply_scope(opts)
  |> where([suggestion], suggestion.id == ^id)
  |> repo().one!()
  |> enforce_scope!(opts, ArticleSuggestion)
end
```

From `lib/cairnloop/knowledge_automation/article_suggestion.ex`:

```elixir
schema "cairnloop_article_suggestions" do
  field(:suggestion_type, Ecto.Enum, values: [:article, :revision])
  field(:status, Ecto.Enum, values: [:pending_generation, :ready, :failed, :dismissed])
  field(:article_id, :integer)
  field(:base_revision_id, :integer)
  field(:proposed_markdown, :string)
  field(:grounding_metadata, :map, default: %{})
  embeds_many(:evidence_snapshot, ArticleSuggestionEvidence, on_replace: :delete)
end
```

Plan 01 must keep `ArticleSuggestion` proposal-only per D-01, create a separate workflow record per D-02, and leave proposal body/evidence storage untouched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create durable `ReviewTask` and `ReviewTaskEvent` storage with active-task enforcement</name>
  <files>priv/repo/migrations/*_add_review_tasks_and_events.exs, lib/cairnloop/knowledge_automation/review_task.ex, lib/cairnloop/knowledge_automation/review_task_event.ex, test/cairnloop/knowledge_automation/review_task_test.exs</files>
  <behavior>
    - Test 1: `ReviewTask` accepts a linked `article_suggestion_id` plus workflow-safe state fields while rejecting missing task state, suggestion reference, actor metadata, or decision timestamps when those fields are required by the chosen transition.
    - Test 2: the schema preserves three separate facts per D-04: AI proposal reference, operator decision fields, and canonical published revision linkage.
    - Test 3: reject, defer, and review-needed states require bounded enum reasons per D-20 and D-21, while freeform notes remain optional.
    - Test 4: the migration creates a unique index that enforces one active task per suggestion per D-03 and a separate append-only events table keyed by `review_task_id`.
  </behavior>
  <action>Create `ReviewTask` and `ReviewTaskEvent` as new relational records per D-01, D-02, D-03, D-04, D-19, D-20, D-21, D-22, and D-23. `ReviewTask` should carry task `status` values covering the queue states in D-08 plus `review_needed` for post-edit or freshness-invalidated work, structured decision fields such as `last_decision`, `last_reason`, `last_actor_id`, `last_decided_at`, `notes`, staged draft references (`staged_article_id`, `staged_revision_id`), canonical publish references (`published_revision_id`, `published_at`), and follow-through markers (`publish_status`, `reindex_status`, `needs_re_review`). `ReviewTaskEvent` should be append-only with first-class columns for `event_type`, `from_status`, `to_status`, `decision`, `reason`, `actor_id`, `notes`, and small `metadata`, using `updated_at: false`. Add indexes for queue listing, suggestion linkage, staged revision lookup, published revision lookup, and event chronology. Do not add Phase 12 quick-fix fields, assignment workflows, or governance expansion tables.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/review_task_test.exs</automated>
  </verify>
  <done>`ReviewTask` and `ReviewTaskEvent` exist as separate durable workflow records, one active task per suggestion is enforceable, and structured review history is queryable without polluting `ArticleSuggestion`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scoped review-task query and creation seams in `KnowledgeAutomation`</name>
  <files>lib/cairnloop/knowledge_automation.ex, test/cairnloop/knowledge_automation/review_task_test.exs</files>
  <behavior>
    - Test 1: `list_review_tasks/1` orders tasks by queue urgency and filters by status while preserving scope rules already used for suggestions.
    - Test 2: `get_review_task!/2` preloads the linked `ArticleSuggestion` and task history so the review UI can render evidence, proposed markdown, and audit trail from one read model.
    - Test 3: `ensure_review_task_for_suggestion/2` creates one active task for a ready or failed suggestion and returns the existing task on repeat calls instead of duplicating workflow rows.
    - Test 4: the API refuses to write review-state fields back onto `ArticleSuggestion`.
  </behavior>
  <action>Extend `Cairnloop.KnowledgeAutomation` with the minimum public task facade per D-05, D-06, D-07, D-08, and D-22. Add `list_review_tasks/1`, `get_review_task!/2`, and `ensure_review_task_for_suggestion/2`, reusing `apply_scope/2` and `enforce_scope!/2` so the review lane matches existing tenant posture. `ensure_review_task_for_suggestion/2` should validate that the linked suggestion is reviewable (`:ready` or durable `:failed`), persist the proposal-to-task link without altering the suggestion row, and append a `task_created` history event when the row is first inserted. Keep queue filters calm and task-centric, but leave approval, rejection, deferral, publish, and editor-return semantics for later plans.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/review_task_test.exs</automated>
  </verify>
  <done>`KnowledgeAutomation` exposes one scoped review-task read model and create-or-get seam, giving later plans a stable task API while keeping proposal state and workflow state separate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suggestion artifact -> review-task storage | AI proposal data crosses into operator workflow state and must not become the workflow truth |
| operator action metadata -> durable audit history | Review decisions and reasons become durable compliance-grade records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M010-S03-01 | T | `ArticleSuggestion` / `ReviewTask` boundary | mitigate | Keep `ReviewTask` and `ReviewTaskEvent` as separate tables per D-01 and D-02, with no review-state columns added to `ArticleSuggestion`. |
| T-M010-S03-02 | R | review decision history | mitigate | Store event type, actor, reason, and timestamps in append-only `ReviewTaskEvent` rows so operator actions remain queryable per D-20 through D-23. |
| T-M010-S03-03 | E | duplicate workflow creation | mitigate | Enforce one active task per suggestion through a unique active-task index and `ensure_review_task_for_suggestion/2` idempotency per D-03. |
</threat_model>

<verification>
Run the focused review-task storage suite:

`mix test test/cairnloop/knowledge_automation/review_task_test.exs`
</verification>

<success_criteria>
- Review workflow state exists as a durable host-owned layer distinct from AI proposal state.
- The review inbox can query task rows and their linked suggestions without inventing a second proposal artifact.
- Append-only history exists before approval and publish commands are introduced.
</success_criteria>

<output>
After completion, create `.planning/milestones/M010-phases/M010-S03/M010-S03-01-SUMMARY.md`
</output>
