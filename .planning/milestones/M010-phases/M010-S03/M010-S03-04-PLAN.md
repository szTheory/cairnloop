---
phase: M010-S03
plan: "04"
type: execute
wave: 4
depends_on:
  - M010-S03-02
  - M010-S03-03
files_modified:
  - lib/cairnloop/web/knowledge_base_live/editor.ex
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_base/workers/chunk_revision.ex
  - test/cairnloop/web/knowledge_base_live_test.exs
  - test/cairnloop/knowledge_automation/review_task_test.exs
  - test/cairnloop/knowledge_base/workers/chunk_revision_test.exs
autonomous: true
requirements:
  - REVIEW-02
  - REVIEW-03
  - OPS-02
must_haves:
  truths:
    - "Operators can edit suggestion-backed drafts in the existing editor while keeping lightweight review context and a clear path back to the task."
    - "When the editor is opened from review context, the existing publish button cannot bypass review-task audit."
    - "Material edits after approval send the task back to `review_needed` and append history rather than preserving stale approval."
    - "Publish and reindex completion or failure are reflected back onto the review task from the normal `ChunkRevision` follow-through path."
  artifacts:
    - path: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      provides: "Review-aware editor mount, evidence sidebar or return affordance, and publish-bypass suppression for review-origin sessions"
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "Re-review and follow-through update seams for review-origin edits and chunk outcome reflection"
    - path: "lib/cairnloop/knowledge_base/workers/chunk_revision.ex"
      provides: "Reindex outcome hook that updates review-task publish or reindex status after canonical publish"
  key_links:
    - from: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      to: "lib/cairnloop/knowledge_automation.ex"
      via: "review-origin save marks `review_needed` when approved drafts are materially edited"
      pattern: "review_task_id|review_needed"
    - from: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      to: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      via: "return link back to task detail"
      pattern: "return_to|review_task_id"
    - from: "lib/cairnloop/knowledge_base/workers/chunk_revision.ex"
      to: "lib/cairnloop/knowledge_automation.ex"
      via: "post-publish chunk outcome reflection by `published_revision_id`"
      pattern: "published_revision_id|reindex_status"
---

<objective>
Finish the editor handoff and publish follow-through without weakening audit or canonical publish boundaries.

Purpose: close the remaining `REVIEW-02`, `REVIEW-03`, and `OPS-02` gaps per D-14 through D-18 and D-24 through D-26, including the two explicit research risks.
Output: review-aware editor behavior, re-review-after-edit semantics, and follow-through reflection from canonical publish into the inbox.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/M010-phases/M010-S03/M010-S03-CONTEXT.md
@.planning/milestones/M010-phases/M010-S03/M010-S03-RESEARCH.md
@lib/cairnloop/web/knowledge_base_live/editor.ex
@lib/cairnloop/knowledge_base/workers/chunk_revision.ex
@lib/cairnloop/knowledge_base.ex

<interfaces>
From `lib/cairnloop/web/knowledge_base_live/editor.ex`:

```elixir
def handle_event("publish", _, socket) do
  case KnowledgeBase.save_draft(socket.assigns.article, %{content: socket.assigns.content}) do
    {:ok, revision} ->
      case KnowledgeBase.publish_revision(revision) do
        {:ok, published_rev} -> ...
      end
  end
end
```

From `lib/cairnloop/knowledge_base/workers/chunk_revision.ex`:

```elixir
def perform(%Oban.Job{args: %{"revision_id" => revision_id}}) do
  ...
  result
end
```

Plan 04 must close research risk 2 by preventing review-origin editor publish bypass, and it must reflect chunk or reindex outcomes back onto the review task without replacing `KnowledgeBase.publish_revision/1`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the editor review-aware and force re-review after material edits</name>
  <files>lib/cairnloop/web/knowledge_base_live/editor.ex, lib/cairnloop/knowledge_automation.ex, test/cairnloop/web/knowledge_base_live_test.exs, test/cairnloop/knowledge_automation/review_task_test.exs</files>
  <behavior>
    - Test 1: when `review_task_id` is present, the editor preloads suggestion-backed markdown, keeps lightweight task context visible, and exposes a return path back to the task.
    - Test 2: in review-origin sessions, the editor does not expose or execute the existing direct `publish` button; it preserves review-task audit by routing operators back through the review lane.
    - Test 3: saving draft after an approved task has been materially edited marks the task `review_needed`, clears ready-to-publish posture, and appends a history event per D-18.
    - Test 4: non-review editor sessions keep existing save or publish behavior unchanged.
  </behavior>
  <action>Update `KnowledgeBaseLive.Editor` and `KnowledgeAutomation` per D-14, D-15, D-16, D-17, and D-18. When the editor is opened from review context, read `review_task_id` and return-path params, preload the linked suggestion markdown as today, and surface lightweight evidence context such as summary, source count, or a return link without turning the screen into a second review surface. Explicitly address research risk 2 by suppressing the current direct `publish` action for review-origin sessions or rerouting it back through `publish_review_task/2`; the simpler default is to hide or disable publish and send operators back to the review task after draft save. On review-origin draft save, detect material content changes compared with the approved staged revision, mark the task `review_needed`, clear `needs_re_review` only after later approval, and append a `material_edit_after_approval` event. Do not alter non-review editor flows.</action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live_test.exs test/cairnloop/knowledge_automation/review_task_test.exs</automated>
  </verify>
  <done>The editor preserves review context, review-origin publish bypass is closed, and materially edited approved drafts re-enter review instead of retaining stale approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reflect publish and reindex follow-through back onto review tasks</name>
  <files>lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_base/workers/chunk_revision.ex, test/cairnloop/knowledge_automation/review_task_test.exs, test/cairnloop/knowledge_base/workers/chunk_revision_test.exs</files>
  <behavior>
    - Test 1: when `ChunkRevision` succeeds for a published revision linked to a review task, the task shows published or reindex-complete state and appends follow-through history.
    - Test 2: when chunking fails, the task records failure or retry-needed status visible in the inbox without changing canonical published content back to draft.
    - Test 3: review tasks unrelated to the revision remain untouched when chunk jobs run.
    - Test 4: publish-follow-through updates are driven from durable revision or task references, not ad hoc process messages.
  </behavior>
  <action>Close `REVIEW-03` and `OPS-02` follow-through per D-24, D-25, and D-26. Extend `ChunkRevision.perform/1` so after its existing canonical work completes or fails it calls a `KnowledgeAutomation` seam that finds any review task linked by `published_revision_id` and updates `publish_status`, `reindex_status`, `status`, and task history accordingly. Success should make the task visibly complete from the inbox; failure should produce a durable retry-needed or failed outcome that operators can see without losing the canonical publish record. Keep this reflection host-owned and durable, but do not add broader telemetry or governance workflows from Phase 12 or later phases.</action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_automation/review_task_test.exs test/cairnloop/knowledge_base/workers/chunk_revision_test.exs</automated>
  </verify>
  <done>The inbox reflects publish and reindex completion or failure from the normal chunking path, and operators can see follow-through state without any second publish pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review lane -> editor | Review-origin editing must not create an audit bypass |
| canonical publish -> reindex follow-through | Post-publish background work must update inbox truth without becoming a second publish path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M010-S03-10 | E | review-origin editor publish | mitigate | Close research risk 2 by suppressing or rerouting editor `publish` when `review_task_id` is present, forcing review-task publish semantics to remain authoritative. |
| T-M010-S03-11 | T | approved draft after material edits | mitigate | Mark tasks `review_needed` and append event history when approved drafts are materially changed in review-origin sessions per D-18. |
| T-M010-S03-12 | R | publish/reindex follow-through visibility | mitigate | Update review tasks from `ChunkRevision` outcome through durable revision/task references so operators can audit publish completion, failure, or retry-needed state from one lane per D-25 and D-26. |
</threat_model>

<verification>
Run the focused editor and follow-through suite:

`mix test test/cairnloop/web/knowledge_base_live_test.exs test/cairnloop/knowledge_automation/review_task_test.exs test/cairnloop/knowledge_base/workers/chunk_revision_test.exs`
</verification>

<success_criteria>
- Review-origin editor flows cannot bypass review-task audit or publish semantics.
- Material edits after approval send tasks back through review.
- Publish and reindex outcomes are reflected back onto the shared inbox from the normal chunking path.
</success_criteria>

<output>
After completion, create `.planning/milestones/M010-phases/M010-S03/M010-S03-04-SUMMARY.md`
</output>
