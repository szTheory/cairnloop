---
phase: M010-S03
plan: "03"
type: execute
wave: 3
depends_on:
  - M010-S03-01
  - M010-S03-02
files_modified:
  - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex
  - lib/cairnloop/web/review_task_presenter.ex
  - lib/cairnloop/web/article_suggestion_presenter.ex
  - lib/cairnloop/web/knowledge_base_live/gaps.ex
  - lib/cairnloop/web/knowledge_base_live/index.ex
  - test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs
autonomous: true
requirements:
  - REVIEW-01
  - REVIEW-02
  - REVIEW-03
must_haves:
  truths:
    - "Operators work from one dedicated KB maintenance inbox that lists pending, approved-ready-to-publish, rejected, deferred, review-needed, and published work."
    - "Task detail keeps visible evidence, citation anchors, proposal body or diff, and structured decision history in the same lane."
    - "Approve, reject, defer, publish, and open-for-edit are task actions, not suggestion-only or editor-only shortcuts."
    - "Gap and article entrypoints deep-link back into the shared review inbox instead of growing fragmented review controls."
  artifacts:
    - path: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      provides: "Task-centric inbox/detail LiveView with queue filters, action wiring, and return-path preservation"
    - path: "lib/cairnloop/web/review_task_presenter.ex"
      provides: "Task-state labels, action availability, reason copy, and publish-follow-through wording"
    - path: "lib/cairnloop/web/article_suggestion_presenter.ex"
      provides: "Evidence and proposal formatting reused inside task detail"
    - path: "lib/cairnloop/web/knowledge_base_live/gaps.ex"
      provides: "Gap launch-point links into shared review task lane"
    - path: "lib/cairnloop/web/knowledge_base_live/index.ex"
      provides: "Article-driven revision links into shared review task lane"
  key_links:
    - from: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      to: "lib/cairnloop/knowledge_automation.ex"
      via: "list/get/approve/reject/defer/publish/open-for-edit task commands"
      pattern: "review_task"
    - from: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      to: "lib/cairnloop/web/review_task_presenter.ex"
      via: "queue state, action, and audit copy"
      pattern: "ReviewTaskPresenter"
    - from: "lib/cairnloop/web/knowledge_base_live/gaps.ex"
      to: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      via: "launch or reopen task from gap suggestion entrypoint"
      pattern: "review_task_id|suggestion"
---

<objective>
Convert the existing suggestion review lane into the dedicated review-task inbox and detail surface.

Purpose: satisfy the operator-facing parts of `REVIEW-01`, `REVIEW-02`, and `REVIEW-03` per D-05 through D-08 and D-14 through D-17.
Output: task-centric LiveView, presenter layer, queue filters, action buttons, and deep-link return paths from gap and article launch points.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/M010-phases/M010-S03/M010-S03-CONTEXT.md
@.planning/milestones/M010-phases/M010-S03/M010-S03-RESEARCH.md
@lib/cairnloop/web/knowledge_base_live/suggestion_review.ex
@lib/cairnloop/web/article_suggestion_presenter.ex
@lib/cairnloop/web/knowledge_base_live/gaps.ex
@lib/cairnloop/web/knowledge_base_live/index.ex

<interfaces>
From `lib/cairnloop/web/knowledge_base_live/suggestion_review.ex`:

```elixir
def mount(_params, session, socket) do
  scope_filters = scope_filters(session)
  suggestions = knowledge_automation().list_article_suggestions(scope_filters)
  ...
end
```

From `lib/cairnloop/web/article_suggestion_presenter.ex`:

```elixir
def status_label(%ArticleSuggestion{status: :ready}), do: "Ready for review"
def source_label(evidence), do: evidence |> to_result() |> SearchResultPresenter.source_label()
def trust_label(evidence), do: evidence |> to_result() |> SearchResultPresenter.trust_label()
```

Plan 03 should evolve this surface into a task inbox per D-07, keeping evidence rendering intact while moving action ownership to `ReviewTask`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the task-centric inbox, detail panel, and queue-state presenters</name>
  <files>lib/cairnloop/web/knowledge_base_live/suggestion_review.ex, lib/cairnloop/web/review_task_presenter.ex, lib/cairnloop/web/article_suggestion_presenter.ex, test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</files>
  <behavior>
    - Test 1: the inbox lists review tasks rather than raw suggestions and exposes queue filters for pending review, approved-ready-to-publish, rejected, deferred, review-needed, and published work per D-08.
    - Test 2: task detail renders linked suggestion evidence, citation anchors, proposal body or derived diff, and structured history in the same view.
    - Test 3: the UI distinguishes AI proposal state from task state and canonical publish outcome per D-04.
    - Test 4: approved or published tasks remain visible in the queue with calm next-step copy instead of disappearing after the first decision.
  </behavior>
  <action>Evolve `KnowledgeBaseLive.SuggestionReview` into the dedicated review inbox per D-05, D-06, D-07, D-08, D-15, and D-29. Replace list/detail loading over `ArticleSuggestion` with `ReviewTask` rows preloaded with their linked suggestion and event history. Add `ReviewTaskPresenter` for queue-state labels, action enablement, decision history copy, and publish/reindex outcome badges, while keeping proposal evidence and body formatting in `ArticleSuggestionPresenter`. Render bounded decision metadata and event history explicitly, and keep evidence sources adjacent to the proposal content. Do not add inline markdown editing or Phase 12 quick-fix launch controls.</action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</automated>
  </verify>
  <done>The review lane is task-centric, queue states are visible and filterable, and detail view keeps proposal evidence, decision history, and publish outcome references in one surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire task actions and shared-lane deep links from gap and article entrypoints</name>
  <files>lib/cairnloop/web/knowledge_base_live/suggestion_review.ex, lib/cairnloop/web/knowledge_base_live/gaps.ex, lib/cairnloop/web/knowledge_base_live/index.ex, lib/cairnloop/web/review_task_presenter.ex, test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</files>
  <behavior>
    - Test 1: approve, reject, defer, publish, and open-for-edit buttons call review-task commands and reload task detail with updated status/history.
    - Test 2: gap and article entrypoints open or resume the shared task lane instead of introducing separate review screens.
    - Test 3: open-for-edit carries `review_task_id` and a return-path parameter into the editor so review context can be recovered later.
    - Test 4: the publish action is only shown for `approved_ready_to_publish` tasks, not pending or editor-origin states.
  </behavior>
  <action>Wire the task-centric action surface per D-10, D-11, D-14, D-16, and D-29. In `SuggestionReview`, call the new `KnowledgeAutomation` task commands for approve, reject, defer, publish, and open-for-edit, then reload the selected task and preserve filter/query state. Update `Gaps` and `Index` launch points so once a suggestion exists they deep-link into the shared review lane by task id or suggestion-to-task resolution rather than opening fragmented local review controls. Ensure open-for-edit navigation passes `review_task_id` and an explicit return target for Plan 04, but leave editor-specific publish suppression and re-review-after-edit semantics to the next plan.</action>
  <verify>
    <automated>mix test test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</automated>
  </verify>
  <done>Operators can act on review tasks from one inbox, and gap or article launch points cleanly resume the shared review lane instead of branching into duplicate workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review-task domain -> operator LiveView | Durable workflow state becomes operator-visible queue and action controls |
| launch points -> shared review lane | Multiple entrypoints must converge on one authoritative review surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M010-S03-07 | S | review UI action ownership | mitigate | Render approve, reject, defer, publish, and open-for-edit from `ReviewTask` status rules, not from raw suggestion status or editor shortcuts. |
| T-M010-S03-08 | I | evidence and decision presentation | mitigate | Keep proposal evidence via `ArticleSuggestionPresenter` and workflow/audit copy via `ReviewTaskPresenter`, so AI proposal truth and operator workflow truth remain visibly distinct per D-04. |
| T-M010-S03-09 | T | fragmented review surfaces | mitigate | Route gap and article launch points back into `/knowledge-base/suggestions` task context per D-05 through D-07 instead of duplicating controls. |
</threat_model>

<verification>
Run the focused review-inbox suite:

`mix test test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs`
</verification>

<success_criteria>
- The dedicated inbox shows every relevant queue state without losing follow-through work.
- Review actions are task-based and auditable from the shared lane.
- Gap and article launch points converge on the same review workflow.
</success_criteria>

<output>
After completion, create `.planning/milestones/M010-phases/M010-S03/M010-S03-03-SUMMARY.md`
</output>
