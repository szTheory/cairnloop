---
phase: 53-demo-runtime-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/mix.exs
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
  - RUNT-05
user_setup: []
must_haves:
  truths:
    - "RUNT-01: Docker and manual local setup run host migrations before Cairnloop library migrations."
    - "RUNT-02: The example app dogfoods the local path dependency while adopter docs continue to show Hex dependency usage."
    - "RUNT-03: /health is routable without browser/session requirements and suitable for Compose readiness."
    - "RUNT-04: Development, test, and Docker runtime config boot without noisy missing Chimeway, Cairnloop behaviour, endpoint binding, or database env failures."
    - "RUNT-05: Trailmark seed data is loaded by setup and available immediately without any separate fixture command."
  prohibitions:
    - "Do not change sealed Cairnloop public contracts for demo setup."
    - "Do not merge host and library migration paths into one globally sorted migrate call."
    - "Do not add a separate fixture command for Trailmark data."
    - "Do not configure Chimeway-backed delivery unless Chimeway migrations are explicitly included in the same setup path."
    - "Do not destructively reset or revert the dirty worktree; preserve existing user edits."
  artifacts:
    - path: "examples/cairnloop_example/mix.exs"
      provides: "Example setup aliases and local path dependency contract"
      contains: "Mix.Task.reenable(\"ecto.migrate\")"
  key_links:
    - from: "examples/cairnloop_example/mix.exs"
      to: "priv/repo/migrations"
      via: "cairnloop_migrations fallback resolves library migrations for path dependency dogfooding"
      pattern: "deps/cairnloop/priv/repo/migrations|../../priv/repo/migrations"
    - from: "examples/cairnloop_example/Dockerfile.demo"
      to: "examples/cairnloop_example/mix.exs"
      via: "Docker CMD runs mix setup before Phoenix serves requests"
      pattern: "mix setup && exec mix phx.server"
---

<objective>
Lock the example app setup contract for RUNT-01, RUNT-02, and RUNT-05.

Purpose: Both Docker and manual setup must use the same ordered Mix aliases, and the example app must keep dogfooding the local path dependency without changing adopter-facing Hex guidance.
Output: `examples/cairnloop_example/mix.exs` preserves host-before-library migrations, `Mix.Task.reenable/1`, local path dependency dogfooding, and seed execution inside `mix setup`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-demo-runtime-contract/53-RESEARCH.md
@.planning/phases/53-demo-runtime-contract/53-VALIDATION.md
@.planning/phases/53-demo-runtime-contract/53-PATTERNS.md
@CLAUDE.md
@AGENTS.md
@examples/cairnloop_example/AGENTS.md
@examples/cairnloop_example/mix.exs
</context>

## Artifacts this phase produces

- `examples/cairnloop_example/mix.exs`: `deps/0` keeps `{:cairnloop, path: "../.."}`; `aliases/0` keeps `cairnloop_migrations`, `reenable_migrate`, `setup`, `ecto.setup`, `ecto.reset`, `test`, and `test.e2e`.
- `examples/cairnloop_example/lib/cairnloop_example/demo_notifier.ex`: new `CairnloopExample.DemoNotifier` module implementing `Cairnloop.Notifier`.
- `examples/cairnloop_example/config/config.exs`: explicit `config :cairnloop, :notifier, CairnloopExample.DemoNotifier`.
- `examples/cairnloop_example/config/dev.exs`, `examples/cairnloop_example/config/test.exs`, `examples/cairnloop_example/config/runtime.exs`: PG env, bounded `PHX_BIND`, Chimeway Repo quieting, and endpoint binding contract.
- `examples/cairnloop_example/lib/cairnloop_example_web/router.ex`: `/health` and `/metrics` mounted through `Cairnloop.Router.cairnloop_operations()` outside `:browser`.
- `examples/cairnloop_example/compose.demo.yml`: private pgvector DB, dynamic localhost web port, `service_healthy` dependency, and `/health` web healthcheck.
- `examples/cairnloop_example/Dockerfile.demo`: runtime command `mix setup && exec mix phx.server`.
- `examples/cairnloop_example/priv/repo/seeds.exs`: deterministic Trailmark seed data with facade calls and Oban drain.
- `examples/cairnloop_example/test/cairnloop_example/seeds_test.exs`: DB-backed `:requires_postgres` seed contract and idempotency tests.
- `README.md`, `examples/cairnloop_example/README.md`, `guides/01-quickstart.md`, `guides/04-troubleshooting.md`: narrow runtime-contract documentation corrections.

<tasks>

<task type="auto">
  <name>Task 1: Preserve path dependency dogfooding and ordered setup aliases</name>
  <files>examples/cairnloop_example/mix.exs</files>
  <read_first>
    @examples/cairnloop_example/mix.exs
    @.planning/phases/53-demo-runtime-contract/53-RESEARCH.md
    @.planning/phases/53-demo-runtime-contract/53-PATTERNS.md
    @.planning/REQUIREMENTS.md
    Also run `git diff -- examples/cairnloop_example/mix.exs` before editing and preserve existing user edits unless they conflict with RUNT-01, RUNT-02, or RUNT-05.
  </read_first>
  <action>
    Ensure `deps/0` keeps `{:cairnloop, path: "../.."}` for local dogfooding and does not replace it with the Hex dependency form. Ensure `aliases/0` defines `cairnloop_migrations` as `deps/cairnloop/priv/repo/migrations` when present and `../../priv/repo/migrations` otherwise. Ensure `reenable_migrate = fn _ -> Mix.Task.reenable("ecto.migrate") end` sits between the host migration task and the library migration task in `ecto.setup`, `test`, and `test.e2e`. Ensure `setup` still runs `deps.get`, `ecto.setup`, `assets.setup`, and `assets.build`, and ensure `ecto.setup` still ends by running `priv/repo/seeds.exs` so RUNT-05 seed loading is part of setup.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example && mix compile --warnings-as-errors</automated>
    <automated>rg -n -F 'Mix.Task.reenable("ecto.migrate")' examples/cairnloop_example/mix.exs</automated>
    <automated>rg -n -F 'run priv/repo/seeds.exs' examples/cairnloop_example/mix.exs</automated>
    <automated>rg -n -F '{:cairnloop, path: "../.."}' examples/cairnloop_example/mix.exs</automated>
  </verify>
  <acceptance_criteria>
    `examples/cairnloop_example/mix.exs` contains the local path dependency, two ordered migration phases for setup/test/e2e, an explicit `Mix.Task.reenable("ecto.migrate")` between phases, and `run priv/repo/seeds.exs` in `ecto.setup`.
  </acceptance_criteria>
  <done>RUNT-01 and RUNT-02 are source-locked in the example setup alias without weakening RUNT-05 seed setup.</done>
</task>

<task type="auto">
  <name>Task 2: Prove setup ordering with DB-backed commands</name>
  <files>examples/cairnloop_example/mix.exs</files>
  <read_first>
    @examples/cairnloop_example/mix.exs
    @examples/cairnloop_example/Dockerfile.demo
    @examples/cairnloop_example/compose.demo.yml
    @.planning/phases/53-demo-runtime-contract/53-VALIDATION.md
    Also run `git diff -- examples/cairnloop_example/mix.exs examples/cairnloop_example/Dockerfile.demo examples/cairnloop_example/compose.demo.yml` and preserve user edits.
  </read_first>
  <action>
    Run the fastest DB-backed proof available for the setup alias. Start the root Postgres service with `PGPORT=5433 docker compose up -d db`, then run the example setup or reset path from `examples/cairnloop_example`. If Docker or Postgres is unavailable in the executor workspace, do not weaken the plan; record the exact blocker and command output in `53-01-SUMMARY.md` and keep the DB-backed command as the required verification.
  </action>
  <verify>
    <automated>PGPORT=5433 docker compose up -d db && cd examples/cairnloop_example && mix ecto.reset</automated>
    <automated>cd examples/cairnloop_example && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    `mix ecto.reset` succeeds with Postgres available, proving host migrations run before library migrations and `priv/repo/seeds.exs` is part of setup. If the command cannot run because Docker/Postgres is unavailable, `53-01-SUMMARY.md` records the blocker and leaves this command as the unresolved verification item.
  </acceptance_criteria>
  <done>Manual setup ordering has DB-backed evidence or a recorded environment blocker with the correct command preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell to Mix aliases | Developer or container command runs setup tasks that create, migrate, and seed the example database. |
| Example app to Postgres | Ecto migrations and seeds mutate local demo data. |
| Docker host to Compose network | Docker publishes only intended services from the demo stack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-DB | Information Disclosure / Tampering | `examples/cairnloop_example/compose.demo.yml` | mitigate | Keep demo Postgres private to Compose in Plan 03; Plan 01 must not add any DB port publishing to setup docs or aliases. |
| T-53-BIND | Tampering | `PHX_BIND` parsing in dev/runtime config | mitigate | Plan 02 keeps an allowlist of `127.0.0.1` and `0.0.0.0`; setup aliases must not bypass endpoint config. |
| T-53-HEALTH | Information Disclosure | `/health` readiness probe | mitigate | Plan 03 keeps the existing library health plug response limited to `{"status":"ok"}` and avoids session data. |
| T-53-SEED | Tampering / Repudiation | `priv/repo/seeds.exs` | mitigate | Plan 04 keeps natural-key idempotency tests and facade-based seed creation. |
| T-53-CHIMEWAY | Information Disclosure / Tampering | Cairnloop notifier config | mitigate | Plan 02 uses an example-only no-op notifier and does not activate external delivery. |
</threat_model>

<verification>
Plan-level verification:
- `cd examples/cairnloop_example && mix compile --warnings-as-errors`
- `PGPORT=5433 docker compose up -d db && cd examples/cairnloop_example && mix ecto.reset`
</verification>

<success_criteria>
- RUNT-01 is covered by ordered `ecto.setup`, `test`, and `test.e2e` aliases.
- RUNT-02 is covered by the example path dependency in `mix.exs`.
- RUNT-05 is not separated from setup; `priv/repo/seeds.exs` remains part of `ecto.setup`.
</success_criteria>

## Source Audit

| Source | ID | Requirement or constraint | Plan | Status |
|--------|----|---------------------------|------|--------|
| GOAL | Phase 53 | Runtime contract across Docker/manual setup, health, config, and seeds | 53-01..53-05 | COVERED |
| REQ | RUNT-01 | Host migrations before library migrations | 53-01, 53-05 | COVERED |
| REQ | RUNT-02 | Local path dependency dogfooding with Hex docs | 53-01, 53-05 | COVERED |
| REQ | RUNT-03 | Routable `/health` readiness | 53-03, 53-05 | COVERED |
| REQ | RUNT-04 | Quiet Chimeway/Cairnloop/env boot | 53-02, 53-03, 53-05 | COVERED |
| REQ | RUNT-05 | Trailmark data immediately after setup | 53-01, 53-04, 53-05 | COVERED |
| RESEARCH | Migration ordering | Separate host/library migrate calls with `Mix.Task.reenable/1` | 53-01 | COVERED |
| RESEARCH | Chimeway side effects | Example-only no-op notifier | 53-02 | COVERED |
| RESEARCH | Compose readiness | DB health before web, web `/health` check | 53-03 | COVERED |
| RESEARCH | Seed idempotency | Natural keys, facades, Oban drain, DB tests | 53-04 | COVERED |
| CONTEXT | None | No Phase 53 CONTEXT.md exists | N/A | COVERED |

<output>
Create `.planning/phases/53-demo-runtime-contract/53-01-SUMMARY.md` when done.
</output>
