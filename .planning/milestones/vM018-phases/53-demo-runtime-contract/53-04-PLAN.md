---
phase: 53-demo-runtime-contract
plan: "04"
type: execute
wave: 2
depends_on:
  - "53-01"
  - "53-02"
files_modified:
  - examples/cairnloop_example/priv/repo/seeds.exs
  - examples/cairnloop_example/test/cairnloop_example/seeds_test.exs
autonomous: true
requirements:
  - RUNT-01
  - RUNT-05
user_setup: []
must_haves:
  truths:
    - "RUNT-01: Docker and manual local setup run host migrations before Cairnloop library migrations."
    - "RUNT-02: The example app dogfoods the local path dependency while adopter docs continue to show Hex dependency usage."
    - "RUNT-03: /health is routable without browser/session requirements and suitable for Compose readiness."
    - "RUNT-04: Development, test, and Docker runtime config boot without noisy missing Chimeway, Cairnloop behaviour, endpoint binding, or database env failures."
    - "RUNT-05: Trailmark seed data is loaded by setup and available immediately without any separate fixture command."
  prohibitions:
    - "Do not change sealed Cairnloop public contracts for demo setup."
    - "Do not merge host and library migration paths into one globally sorted migrate call."
    - "Do not add a separate fixture command for Trailmark data."
    - "Do not bypass KnowledgeBase, KnowledgeAutomation, Governance, or MCP facades in seeds."
    - "Do not destructively reset or revert the dirty worktree; preserve existing user edits."
  artifacts:
    - path: "examples/cairnloop_example/priv/repo/seeds.exs"
      provides: "Deterministic Trailmark seed run"
      exports: ["CairnloopExample.SeedRun.run/0"]
    - path: "examples/cairnloop_example/test/cairnloop_example/seeds_test.exs"
      provides: "DB-backed seed readiness and idempotency tests"
      exports: ["CairnloopExample.SeedsTest"]
  key_links:
    - from: "examples/cairnloop_example/mix.exs"
      to: "examples/cairnloop_example/priv/repo/seeds.exs"
      via: "`ecto.setup` runs seed script"
      pattern: "run priv/repo/seeds.exs"
    - from: "examples/cairnloop_example/priv/repo/seeds.exs"
      to: "examples/cairnloop_example/test/cairnloop_example/seeds_test.exs"
      via: "tests evaluate the seed script and assert row counts/idempotency"
      pattern: "Code.eval_file"
---

<objective>
Lock seeded Trailmark data availability for RUNT-05 with DB-backed validation.

Purpose: A fresh setup should leave every demo surface clickable immediately, with seed reruns stable and no extra fixture command.
Output: `priv/repo/seeds.exs` and `seeds_test.exs` prove deterministic Trailmark lifecycle data, facade-backed creation, Oban drain readiness, and idempotency.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-demo-runtime-contract/53-RESEARCH.md
@.planning/phases/53-demo-runtime-contract/53-VALIDATION.md
@.planning/phases/53-demo-runtime-contract/53-PATTERNS.md
@CLAUDE.md
@AGENTS.md
@examples/cairnloop_example/AGENTS.md
@examples/cairnloop_example/mix.exs
@examples/cairnloop_example/priv/repo/seeds.exs
@examples/cairnloop_example/test/cairnloop_example/seeds_test.exs
</context>

## Artifacts this phase produces

- `examples/cairnloop_example/mix.exs`: `deps/0` keeps `{:cairnloop, path: "../.."}`; `aliases/0` keeps `cairnloop_migrations`, `reenable_migrate`, `setup`, `ecto.setup`, `ecto.reset`, `test`, and `test.e2e`.
- `examples/cairnloop_example/lib/cairnloop_example/demo_notifier.ex`: new `CairnloopExample.DemoNotifier` module implementing `Cairnloop.Notifier`.
- `examples/cairnloop_example/config/config.exs`: explicit `config :cairnloop, :notifier, CairnloopExample.DemoNotifier`.
- `examples/cairnloop_example/config/dev.exs`, `examples/cairnloop_example/config/test.exs`, `examples/cairnloop_example/config/runtime.exs`: PG env, bounded `PHX_BIND`, Chimeway Repo quieting, and endpoint binding contract.
- `examples/cairnloop_example/lib/cairnloop_example_web/router.ex`: `/health` and `/metrics` mounted through `Cairnloop.Router.cairnloop_operations()` outside `:browser`.
- `examples/cairnloop_example/compose.demo.yml`: private pgvector DB, dynamic localhost web port, `service_healthy` dependency, and `/health` web healthcheck.
- `examples/cairnloop_example/Dockerfile.demo`: runtime command `mix setup && exec mix phx.server`.
- `examples/cairnloop_example/priv/repo/seeds.exs`: deterministic Trailmark seed data with facade calls and Oban drain.
- `examples/cairnloop_example/test/cairnloop_example/seeds_test.exs`: DB-backed `:requires_postgres` seed contract and idempotency tests.
- `README.md`, `examples/cairnloop_example/README.md`, `guides/01-quickstart.md`, `guides/04-troubleshooting.md`: narrow runtime-contract documentation corrections.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve deterministic seed builders and Oban drain</name>
  <files>examples/cairnloop_example/priv/repo/seeds.exs</files>
  <read_first>
    @examples/cairnloop_example/priv/repo/seeds.exs
    @examples/cairnloop_example/test/cairnloop_example/seeds_test.exs
    @.planning/phases/53-demo-runtime-contract/53-PATTERNS.md
    @.planning/phases/53-demo-runtime-contract/53-VALIDATION.md
    Also run `git diff -- examples/cairnloop_example/priv/repo/seeds.exs` before editing and preserve existing user edits.
  </read_first>
  <behavior>
    - Running the seed once creates conversations, messages, KB articles/revisions/chunks, gaps, suggestions, review tasks, governed-action audit rows, drafts, approvals, outbound recovery data, and MCP demo tokens.
    - Running the seed twice leaves stable row counts for the asserted natural-key-backed tables.
    - Chunks exist immediately after the seed because the embedding queue drains before script exit.
  </behavior>
  <action>
    Ensure `CairnloopExample.SeedRun.run/0` builds articles, conversations, showcase states, gaps, suggestions, Phase 45 evidence states, drains the embedding pipeline, emits a summary, and returns `:ok`. Preserve facade calls through `Cairnloop.KnowledgeBase`, `Cairnloop.KnowledgeAutomation`, `Cairnloop.Governance`, and MCP token helpers. Preserve natural-key guards such as `get_or_insert!/3`. Preserve `Oban.drain_queue(queue: :default, with_recursion: true)` after article/revision publishing so chunks are ready immediately after setup.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example && mix compile --warnings-as-errors</automated>
    <automated>rg -n -F 'Oban.drain_queue(queue: :default, with_recursion: true)' examples/cairnloop_example/priv/repo/seeds.exs</automated>
    <automated>rg -n -F 'KnowledgeAutomation.ensure_review_task_for_suggestion' examples/cairnloop_example/priv/repo/seeds.exs</automated>
  </verify>
  <acceptance_criteria>
    Seed script retains facade calls, natural-key guards, and synchronous Oban drain; no separate command is introduced for Trailmark fixture data.
  </acceptance_criteria>
  <done>RUNT-05 seed production path is source-locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock DB-backed seed contract tests</name>
  <files>examples/cairnloop_example/test/cairnloop_example/seeds_test.exs</files>
  <read_first>
    @examples/cairnloop_example/test/cairnloop_example/seeds_test.exs
    @examples/cairnloop_example/test/support/data_case.ex
    @examples/cairnloop_example/config/test.exs
    @.planning/phases/53-demo-runtime-contract/53-VALIDATION.md
    Also run `git diff -- examples/cairnloop_example/test/cairnloop_example/seeds_test.exs examples/cairnloop_example/test/support/data_case.ex examples/cairnloop_example/config/test.exs` before editing and preserve existing user edits.
  </read_first>
  <behavior>
    - `CairnloopExample.SeedsTest` is tagged `:requires_postgres` and is excluded from DB-free headless lanes unless explicitly selected.
    - The tests evaluate `priv/repo/seeds.exs` inside the sandboxed process.
    - Assertions cover row counts, chunks, review task companion rows, showcase states, Phase 45 evidence states, MCP token masking source data, high-risk demo proposal state, and idempotent reruns.
  </behavior>
  <action>
    Ensure `CairnloopExample.SeedsTest` uses `CairnloopExample.DataCase, async: false`, has `@moduletag :requires_postgres`, resolves the seed path with `Path.expand("../../priv/repo/seeds.exs", __DIR__)`, and calls `Code.eval_file/1` from the test process. Ensure the test file asserts the Trailmark lifecycle data described in the behavior block, including idempotency by comparing row count maps after two seed runs.
  </action>
  <verify>
    <automated>PGPORT=5433 docker compose up -d db && cd examples/cairnloop_example && mix test --only requires_postgres test/cairnloop_example/seeds_test.exs</automated>
    <automated>cd examples/cairnloop_example && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    The focused seed test passes with Postgres available. If Docker/Postgres cannot run in the executor workspace, `53-04-SUMMARY.md` records the exact blocker and leaves the focused command as the required verification.
  </acceptance_criteria>
  <done>RUNT-05 has DB-backed verification and preserves the DB-free default posture by using `:requires_postgres`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script to database | Seed code writes deterministic demo data into local Postgres. |
| Seed script to Oban | Seed code drains queued jobs synchronously for readiness. |
| Test process to sandbox | DB-backed tests evaluate seeds inside a controlled sandbox owner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-DB | Information Disclosure / Tampering | Seeded demo data in local DB | mitigate | Keep data deterministic and local; Plan 03 keeps demo DB private in Docker. |
| T-53-BIND | Tampering | Test DB env `PGPORT` | mitigate | Test command explicitly scopes DB connection to `PGPORT=5433`; Plan 02 bounds config parsing. |
| T-53-HEALTH | Information Disclosure | Docker readiness after seed | mitigate | Seed summaries may go to logs, but health output remains non-sensitive in Plan 03. |
| T-53-SEED | Tampering / Repudiation | `priv/repo/seeds.exs` and `seeds_test.exs` | mitigate | Natural keys, facade calls, Oban drain, and idempotency tests prevent duplicate/integrity drift. |
| T-53-CHIMEWAY | Information Disclosure / Tampering | Seeded outbound data and notifier callbacks | mitigate | Seeds may create outbound recovery records but Plan 02's no-op notifier prevents external delivery. |
</threat_model>

<verification>
Plan-level verification:
- `PGPORT=5433 docker compose up -d db && cd examples/cairnloop_example && mix test --only requires_postgres test/cairnloop_example/seeds_test.exs`
- `cd examples/cairnloop_example && mix compile --warnings-as-errors`
</verification>

<success_criteria>
- Trailmark data is available immediately after `mix setup`.
- Seed reruns are idempotent under DB-backed tests.
- Seed validation remains isolated behind `:requires_postgres`, preserving the DB-free headless posture.
</success_criteria>

## Source Audit

| Source | ID | Requirement or constraint | Plan | Status |
|--------|----|---------------------------|------|--------|
| GOAL | Phase 53 | Runtime contract across Docker/manual setup, health, config, and seeds | 53-01..53-05 | COVERED |
| REQ | RUNT-01 | Host migrations before library migrations | 53-01, 53-05 | COVERED |
| REQ | RUNT-02 | Local path dependency dogfooding with Hex docs | 53-01, 53-05 | COVERED |
| REQ | RUNT-03 | Routable `/health` readiness | 53-03, 53-05 | COVERED |
| REQ | RUNT-04 | Quiet Chimeway/Cairnloop/env boot | 53-02, 53-03, 53-05 | COVERED |
| REQ | RUNT-05 | Trailmark data immediately after setup | 53-01, 53-04, 53-05 | COVERED |
| RESEARCH | Seed duplicate/integrity drift | Natural keys and idempotency assertions | 53-04 | COVERED |
| RESEARCH | Oban drain readiness | Synchronous queue drain before script exit | 53-04 | COVERED |
| RESEARCH | DB-free posture | `:requires_postgres` marker on focused DB test | 53-04 | COVERED |
| CONTEXT | None | No Phase 53 CONTEXT.md exists | N/A | COVERED |

<output>
Create `.planning/phases/53-demo-runtime-contract/53-04-SUMMARY.md` when done.
</output>
