---
phase: 53-demo-runtime-contract
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/lib/cairnloop_example_web/router.ex
  - examples/cairnloop_example/compose.demo.yml
  - examples/cairnloop_example/Dockerfile.demo
autonomous: true
requirements:
  - RUNT-03
  - RUNT-04
user_setup: []
must_haves:
  truths:
    - "RUNT-01: Docker and manual local setup run host migrations before Cairnloop library migrations."
    - "RUNT-02: The example app dogfoods the local path dependency while adopter docs continue to show Hex dependency usage."
    - "RUNT-03: /health is routable without browser/session requirements and suitable for Compose readiness."
    - "RUNT-04: Development, test, and Docker runtime config boot without noisy missing Chimeway, Cairnloop behaviour, endpoint binding, or database env failures."
    - "RUNT-05: Trailmark seed data is loaded by setup and available immediately without any separate fixture command."
  prohibitions:
    - "Do not change sealed Cairnloop public contracts for demo setup."
    - "Do not merge host and library migration paths into one globally sorted migrate call."
    - "Do not add a separate fixture command for Trailmark data."
    - "Do not publish the demo Postgres service to the host in `compose.demo.yml`."
    - "Do not destructively reset or revert the dirty worktree; preserve existing user edits."
  artifacts:
    - path: "examples/cairnloop_example/lib/cairnloop_example_web/router.ex"
      provides: "Operations route mount"
      contains: "Cairnloop.Router.cairnloop_operations()"
    - path: "examples/cairnloop_example/compose.demo.yml"
      provides: "Docker demo service readiness contract"
      contains: "curl -fsS http://127.0.0.1:4000/health"
    - path: "examples/cairnloop_example/Dockerfile.demo"
      provides: "Container setup-before-server command"
      contains: "mix setup && exec mix phx.server"
  key_links:
    - from: "examples/cairnloop_example/compose.demo.yml"
      to: "examples/cairnloop_example/lib/cairnloop_example_web/router.ex"
      via: "web healthcheck curls `/health` mounted by operations macro"
      pattern: "/health"
    - from: "examples/cairnloop_example/Dockerfile.demo"
      to: "examples/cairnloop_example/mix.exs"
      via: "container command runs `mix setup` before `mix phx.server`"
      pattern: "mix setup && exec mix phx.server"
---

<objective>
Lock the `/health` route and Docker readiness contract for RUNT-03 and RUNT-04.

Purpose: Compose readiness should prove the app is serving after setup, without exposing Postgres or requiring browser/session state for infrastructure probes.
Output: Router, Compose, and Dockerfile contract verified around `/health`, private DB networking, bounded web publication, and setup-before-server order.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-demo-runtime-contract/53-RESEARCH.md
@.planning/phases/53-demo-runtime-contract/53-VALIDATION.md
@.planning/phases/53-demo-runtime-contract/53-PATTERNS.md
@CLAUDE.md
@AGENTS.md
@examples/cairnloop_example/AGENTS.md
@examples/cairnloop_example/lib/cairnloop_example_web/router.ex
@examples/cairnloop_example/compose.demo.yml
@examples/cairnloop_example/Dockerfile.demo
@lib/cairnloop/router.ex
@lib/cairnloop/web/health_plug.ex
</context>

## Artifacts this phase produces

- `examples/cairnloop_example/mix.exs`: `deps/0` keeps `{:cairnloop, path: "../.."}`; `aliases/0` keeps `cairnloop_migrations`, `reenable_migrate`, `setup`, `ecto.setup`, `ecto.reset`, `test`, and `test.e2e`.
- `examples/cairnloop_example/lib/cairnloop_example/demo_notifier.ex`: new `CairnloopExample.DemoNotifier` module implementing `Cairnloop.Notifier`.
- `examples/cairnloop_example/config/config.exs`: explicit `config :cairnloop, :notifier, CairnloopExample.DemoNotifier`.
- `examples/cairnloop_example/config/dev.exs`, `examples/cairnloop_example/config/test.exs`, `examples/cairnloop_example/config/runtime.exs`: PG env, bounded `PHX_BIND`, Chimeway Repo quieting, and endpoint binding contract.
- `examples/cairnloop_example/lib/cairnloop_example_web/router.ex`: `/health` and `/metrics` mounted through `Cairnloop.Router.cairnloop_operations()` outside `:browser`.
- `examples/cairnloop_example/compose.demo.yml`: private pgvector DB, dynamic localhost web port, `service_healthy` dependency, and `/health` web healthcheck.
- `examples/cairnloop_example/Dockerfile.demo`: runtime command `mix setup && exec mix phx.server`.
- `examples/cairnloop_example/priv/repo/seeds.exs`: deterministic Trailmark seed data with facade calls and Oban drain.
- `examples/cairnloop_example/test/cairnloop_example/seeds_test.exs`: DB-backed `:requires_postgres` seed contract and idempotency tests.
- `README.md`, `examples/cairnloop_example/README.md`, `guides/01-quickstart.md`, `guides/04-troubleshooting.md`: narrow runtime-contract documentation corrections.

<tasks>

<task type="auto">
  <name>Task 1: Keep operations health route outside browser pipeline</name>
  <files>examples/cairnloop_example/lib/cairnloop_example_web/router.ex</files>
  <read_first>
    @examples/cairnloop_example/lib/cairnloop_example_web/router.ex
    @lib/cairnloop/router.ex
    @lib/cairnloop/web/health_plug.ex
    @.planning/phases/53-demo-runtime-contract/53-PATTERNS.md
    Also run `git diff -- examples/cairnloop_example/lib/cairnloop_example_web/router.ex` before editing and preserve existing user edits.
  </read_first>
  <action>
    Ensure the dashboard remains under a `pipe_through :browser` scope and operations endpoints remain in a separate root scope with `Cairnloop.Router.cairnloop_operations()`. Keep `/health` outside session, CSRF, layout, and operator auth plugs. Do not create an example-specific health controller or alter `Cairnloop.Web.HealthPlug`.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example && mix compile --warnings-as-errors</automated>
    <automated>cd examples/cairnloop_example && mix phx.routes | rg '/health'</automated>
    <automated>cd examples/cairnloop_example && mix phx.routes | rg '/metrics'</automated>
  </verify>
  <acceptance_criteria>
    `mix phx.routes` shows `/health`; the operations route is mounted through `Cairnloop.Router.cairnloop_operations()` in a scope without `pipe_through :browser`; no sealed library health plug contract is changed.
  </acceptance_criteria>
  <done>RUNT-03 health routing is routable and infrastructure-friendly.</done>
</task>

<task type="auto">
  <name>Task 2: Lock Compose readiness and setup-before-server Docker command</name>
  <files>examples/cairnloop_example/compose.demo.yml, examples/cairnloop_example/Dockerfile.demo</files>
  <read_first>
    @examples/cairnloop_example/compose.demo.yml
    @examples/cairnloop_example/Dockerfile.demo
    @examples/cairnloop_example/config/dev.exs
    @.planning/phases/53-demo-runtime-contract/53-RESEARCH.md
    Also run `git diff -- examples/cairnloop_example/compose.demo.yml examples/cairnloop_example/Dockerfile.demo` before editing and preserve existing user edits.
  </read_first>
  <action>
    Ensure `compose.demo.yml` keeps a `db` service using `pgvector/pgvector:pg16`, a `pg_isready` healthcheck, no `ports` block on the DB service, and a `web` service that depends on `db` with `condition: service_healthy`. Ensure `web.environment` includes `MIX_ENV=dev`, `PGHOST=db`, `PGPORT=5432`, `PORT=4000`, `PHX_BIND=0.0.0.0`, and optional `OPENAI_API_KEY`. Ensure the web port publishes only the Phoenix target on `host_ip` default `127.0.0.1` with `CAIRNLOOP_WEB_PORT` default range `4100-4199`. Ensure the web healthcheck curls `http://127.0.0.1:4000/health`. Ensure `Dockerfile.demo` installs `curl` and `postgresql-client`, exposes `4000`, and keeps `CMD ["sh", "-lc", "mix setup && exec mix phx.server"]`.
  </action>
  <verify>
    <automated>docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet</automated>
    <automated>rg -n -F 'curl -fsS http://127.0.0.1:4000/health' examples/cairnloop_example/compose.demo.yml</automated>
    <automated>rg -n -F 'mix setup && exec mix phx.server' examples/cairnloop_example/Dockerfile.demo</automated>
  </verify>
  <acceptance_criteria>
    Compose config validates; Postgres is not published to the host; web starts only after DB health; web readiness checks `/health`; Docker command runs setup before server boot.
  </acceptance_criteria>
  <done>RUNT-03 and RUNT-04 Docker readiness are source-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker host to web container | Browser traffic reaches only the published Phoenix endpoint. |
| Docker web to db service | Web connects to private Compose DB by service name. |
| Infrastructure probe to Phoenix router | Health checks cross into the app without browser/session state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-DB | Information Disclosure / Tampering | `compose.demo.yml` DB service | mitigate | Keep no DB `ports` block and only expose DB on the Compose network. |
| T-53-BIND | Tampering | Web `PHX_BIND` env | mitigate | Compose may set only `0.0.0.0`; Plan 02 validates config rejects unsupported bind values. |
| T-53-HEALTH | Information Disclosure | `/health` route and healthcheck | mitigate | Keep health outside browser auth but response limited to status JSON from existing plug. |
| T-53-SEED | Tampering / Repudiation | Docker `mix setup` seed run | mitigate | Dockerfile runs `mix setup` before server; Plan 04 verifies seed idempotency. |
| T-53-CHIMEWAY | Information Disclosure / Tampering | Docker boot with notifier config | mitigate | Plan 02 wires no-op notifier; this plan must not add Chimeway delivery env or migrations. |
</threat_model>

<verification>
Plan-level verification:
- `cd examples/cairnloop_example && mix phx.routes | rg '/health'`
- `docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet`
- `cd examples/cairnloop_example && mix compile --warnings-as-errors`
</verification>

<success_criteria>
- `/health` is routable and does not require session/browser plugs.
- Compose readiness is tied to DB health and app `/health`.
- Docker starts with setup before serving Phoenix and does not expose Postgres to the host.
</success_criteria>

## Source Audit

| Source | ID | Requirement or constraint | Plan | Status |
|--------|----|---------------------------|------|--------|
| GOAL | Phase 53 | Runtime contract across Docker/manual setup, health, config, and seeds | 53-01..53-05 | COVERED |
| REQ | RUNT-01 | Host migrations before library migrations | 53-01, 53-05 | COVERED |
| REQ | RUNT-02 | Local path dependency dogfooding with Hex docs | 53-01, 53-05 | COVERED |
| REQ | RUNT-03 | Routable `/health` readiness | 53-03, 53-05 | COVERED |
| REQ | RUNT-04 | Quiet Chimeway/Cairnloop/env boot | 53-02, 53-03, 53-05 | COVERED |
| REQ | RUNT-05 | Trailmark data immediately after setup | 53-01, 53-04, 53-05 | COVERED |
| RESEARCH | Demo DB exposure | Private DB service in Compose | 53-03 | COVERED |
| RESEARCH | Health endpoint leakage | Existing minimal HealthPlug response | 53-03 | COVERED |
| RESEARCH | Docker setup order | Dockerfile `mix setup` before server | 53-03 | COVERED |
| CONTEXT | None | No Phase 53 CONTEXT.md exists | N/A | COVERED |

<output>
Create `.planning/phases/53-demo-runtime-contract/53-03-SUMMARY.md` when done.
</output>
