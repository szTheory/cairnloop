---
phase: 55-docker-first-adopter-docs
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/README.md
autonomous: true
requirements:
  - DOC-02
  - DOC-04
user_setup: []
must_haves:
  truths:
    - "DOC-02 / D-05: Example README sends Docker users to the printed base URL, not fixed localhost:4000 links."
    - "D-06: localhost:4000 remains only in manual local Phoenix context."
    - "D-07: Example README documents wrapper command vocabulary and stop/down/reset volume semantics."
    - "D-15: Route mentions align with Phase 54 locked routes."
  artifacts:
    - path: examples/cairnloop_example/README.md
      provides: "Example-app adopter entry point with Docker-aware demo and route flow."
      contains: "printed"
  key_links:
    - from: examples/cairnloop_example/README.md
      to: bin/demo
      via: "Example README mirrors printed URL and command vocabulary from ./bin/demo."
      pattern: "\\./bin/demo"
    - from: examples/cairnloop_example/README.md
      to: examples/cairnloop_example/lib/cairnloop_example_web/router.ex
      via: "Route names match mounted demo index, chat, dashboard, and operations routes."
      pattern: "/support"
---

<objective>
Make the example-app README usable for adopters who enter through `examples/cairnloop_example/`.

Purpose: DOC-02 requires the example README to stop relying on hard-coded Docker ports or stale route names.
Output: Updated `examples/cairnloop_example/README.md` with Docker-aware Demo Index, Two-Tab Demo, routing, command vocabulary, and smoke semantics.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-docker-first-adopter-docs/55-CONTEXT.md
@.planning/phases/55-docker-first-adopter-docs/55-RESEARCH.md
@.planning/phases/55-docker-first-adopter-docs/55-VALIDATION.md
@.planning/phases/55-docker-first-adopter-docs/55-PATTERNS.md
@.planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
@.planning/phases/54-demo-wrapper-experience/54-VERIFICATION.md
@.planning/phases/53-demo-runtime-contract/53-PATTERNS.md
@.planning/phases/53-demo-runtime-contract/53-VERIFICATION.md
@CLAUDE.md
@AGENTS.md
@examples/cairnloop_example/AGENTS.md
@examples/cairnloop_example/README.md
@bin/demo
@examples/cairnloop_example/lib/cairnloop_example_web/router.ex
</context>

## Artifacts This Phase Produces

- Modified by this plan: `examples/cairnloop_example/README.md`.
- New files created by this plan: none.
- Phase-level new file created by Plan 55-03: `test/cairnloop/docs/docker_first_docs_test.exs`.
- Phase-level new test module created by Plan 55-03: `Cairnloop.Docs.DockerFirstDocsTest`.
- No schema push is required: this plan modifies one Markdown file and does not touch schema, migration, or database code.

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Make first-run story consistent anywhere an adopter enters repo or HexDocs. | 55-01, 55-02, 55-03 | COVERED | This plan covers the nested example-app entry point. |
| REQ | DOC-02 | Example README avoids hard-coded Docker ports and stale route names. | 55-02 | COVERED | Both tasks target example README drift. |
| REQ | DOC-04 | Explain smoke and route coverage without requiring external credentials. | 55-02, 55-03 | COVERED | This plan aligns route and smoke references; Plan 55-03 adds OpenAI scope. |
| RESEARCH | - | Example README hard-codes localhost:4000 in Two-Tab Demo and Routing. | 55-02 | COVERED | Task 1 replaces Docker-facing hard-coded URLs with printed-base-URL guidance. |
| CONTEXT | D-03 | Calm adopter-facing copy. | 55-02 | COVERED | Tasks use outcome language: open printed URLs, reply in chat, inspect logs. |
| CONTEXT | D-04 | `./bin/demo` output/help are source of truth. | 55-02 | COVERED | Verified with `./bin/demo help`. |
| CONTEXT | D-05 | Docker docs use printed URL, not hard-coded localhost:4000. | 55-02 | COVERED | Implemented in Task 1. |
| CONTEXT | D-06 | localhost:4000 only for manual local Phoenix path. | 55-02 | COVERED | Implemented in Task 1. |
| CONTEXT | D-07 | Wrapper command vocabulary and volume semantics. | 55-02 | COVERED | Implemented in Task 2. |
| CONTEXT | D-13 | Document local isolated `./bin/demo smoke`. | 55-02 | COVERED | Implemented in Task 2 at example README level. |
| CONTEXT | D-14 | Smoke is HTTP route smoke, not full browser E2E or Phase 56 CI. | 55-02 | COVERED | Implemented in Task 2. |
| CONTEXT | D-15 | Route coverage aligns to locked route list. | 55-02 | COVERED | Implemented in Task 2. |
| CONTEXT | D-16 | Only DOC-01 through DOC-04 docs plus adjacent contradiction cleanup. | 55-02 | COVERED | Only example README is modified. |

<tasks>

<task type="auto">
  <name>Task 1: Rewrite example Demo Index and Two-Tab flow around printed URLs</name>
  <files>examples/cairnloop_example/README.md</files>
  <read_first>
    examples/cairnloop_example/AGENTS.md
    examples/cairnloop_example/README.md
    bin/demo
    examples/cairnloop_example/lib/cairnloop_example_web/router.ex
    .planning/phases/55-docker-first-adopter-docs/55-CONTEXT.md
    .planning/phases/55-docker-first-adopter-docs/55-RESEARCH.md
    .planning/phases/54-demo-wrapper-experience/54-VERIFICATION.md
  </read_first>
  <action>
    Update `examples/cairnloop_example/README.md` so the Demo Index and Two-Tab Demo lead with the Docker path and the URL printed by `./bin/demo` per D-04 and D-05. Tell Docker users to open the printed demo index URL, then use the printed base URL plus `/support` for the operator inbox and `/chat` for the customer chat per D-06. Keep `http://localhost:4000`, `http://localhost:4000/support`, and `http://localhost:4000/chat` only in sentences explicitly scoped to manual local Phoenix boot after `mix setup` and `mix phx.server`. Preserve the Trailmark seeded-flow explanation and use calm adopter-facing copy per D-03. Do not add runtime, wrapper, route, screenshot, or product UI changes.
  </action>
  <acceptance_criteria>
    - Demo Index tells Docker users to start from the demo index URL printed by `./bin/demo`.
    - Two-Tab Demo gives Docker-first instructions using the printed base URL plus `/support` and `/chat`.
    - Manual local `localhost:4000` links remain only under manual local Phoenix context.
    - The customer chat wording describes the real customer chat ingress, not a mock-only view.
  </acceptance_criteria>
  <verify>
    <automated>elixir -e 's=File.read!("examples/cairnloop_example/README.md"); Enum.each(["Operator inbox:** http://localhost:4000/support","Customer chat:** http://localhost:4000/chat","Visit [`localhost:4000/support`","Visit [`localhost:4000/chat`"], fn bad -> if String.contains?(s,bad), do: raise("stale primary hard-coded URL: #{bad}") end); unless String.contains?(s,"printed") and String.contains?(s,"/support") and String.contains?(s,"/chat"), do: raise("missing printed URL route guidance")'</automated>
    <automated>rg -n 'printed|/support|/chat|manual local|localhost:4000' examples/cairnloop_example/README.md</automated>
  </verify>
  <done>Example README Demo Index and Two-Tab Demo are Docker-aware while retaining manual-local localhost guidance only as secondary manual context.</done>
</task>

<task type="auto">
  <name>Task 2: Align example command semantics and route inventory</name>
  <files>examples/cairnloop_example/README.md</files>
  <read_first>
    examples/cairnloop_example/README.md
    bin/demo
    examples/cairnloop_example/lib/cairnloop_example_web/router.ex
    .planning/phases/55-docker-first-adopter-docs/55-PATTERNS.md
    .planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
    .planning/phases/54-demo-wrapper-experience/54-VERIFICATION.md
  </read_first>
  <action>
    Update the example README command and routing sections to mirror the Phase 54 wrapper vocabulary per D-07: default `start`/`up`, `urls`, `logs`, `status`/`ps`, `stop`, `down`, `reset`, `smoke`, and `help`. Explain that `stop` and `down` preserve named volumes and `reset` removes volumes and reseeds. Describe `./bin/demo smoke` per D-13 and D-14 as a local isolated HTTP route smoke that checks the main routes, reports the failing route plus recent web logs on errors, and cleans up containers and volumes; do not present it as a browser E2E suite or CI workflow. Where route coverage is listed, align to D-15: `/`, `/support`, `/support/inbox`, `/chat`, `/support/knowledge-base`, `/support/knowledge-base/gaps`, `/support/knowledge-base/suggestions`, `/support/audit-log`, and `/support/settings`. Keep the README scoped to DOC-02 and DOC-04 per D-16.
  </action>
  <acceptance_criteria>
    - Example README command list includes all wrapper commands and aliases from `./bin/demo help`.
    - Example README distinguishes `stop`, `down`, and `reset` volume behavior.
    - Example README route names match the locked Phase 54 route list and do not invent route names.
    - Example README smoke wording is local, isolated, HTTP route smoke language and does not describe CI workflow wiring.
  </acceptance_criteria>
  <verify>
    <automated>./bin/demo help</automated>
    <automated>elixir -e 's=File.read!("examples/cairnloop_example/README.md"); Enum.each(["./bin/demo","urls","logs","status","ps","stop","down","reset","smoke","help"], fn token -> unless String.contains?(s,token), do: raise("missing command token #{token}") end)'</automated>
    <automated>elixir -e 's=File.read!("examples/cairnloop_example/README.md"); Enum.each(["/support","/support/inbox","/chat","/support/knowledge-base","/support/knowledge-base/gaps","/support/knowledge-base/suggestions","/support/audit-log","/support/settings"], fn route -> unless String.contains?(s,route), do: raise("missing route #{route}") end)'</automated>
    <automated>mix ci.fast</automated>
  </verify>
  <done>Example README command and route sections match the wrapper/router truth and avoid stale Docker URL assumptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| adopter to nested docs | Users may enter through the example app directory and follow only its README. |
| nested docs to local shell | Example README commands launch Docker, Mix, or wrapper operations. |
| docs to demo routes | Route names in docs direct users to operator and customer surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-55-04 | Spoofing | Example README URLs | mitigate | Per D-05 and D-06, Docker route instructions use the printed base URL and reserve localhost:4000 for manual Phoenix. |
| T-55-05 | Tampering | Example README command list | mitigate | Per D-04 and D-07, mirror `./bin/demo help` and do not introduce raw Compose as the primary path. |
| T-55-06 | Denial of Service | Reset/stop/down guidance | mitigate | Per D-07, document volume semantics so adopters do not accidentally remove seeded state when they intend to stop the stack. |
| T-55-07 | Information Disclosure | Smoke/log diagnostics | mitigate | Per D-13 and D-14, describe bounded recent web logs and failing route diagnostics; do not request credential dumps or long Compose output. |
| T-55-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan; package legitimacy checkpoint is not required. |
</threat_model>

<verification>
Run these after completing both tasks:
- `./bin/demo help`
- `rg -n 'localhost:4000' examples/cairnloop_example/README.md` and confirm every hit is manual-local Phoenix context.
- `mix ci.fast`
</verification>

<success_criteria>
DOC-02 is satisfied for the example-app entry point. Example README Docker users follow printed URLs and locked wrapper commands, route names match Phase 54, and no schema push, package install, CI workflow, or runtime behavior is introduced.
</success_criteria>

<output>
Create `.planning/phases/55-docker-first-adopter-docs/55-02-SUMMARY.md` when done.
</output>
