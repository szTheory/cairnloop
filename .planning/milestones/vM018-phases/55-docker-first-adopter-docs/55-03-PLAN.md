---
phase: 55-docker-first-adopter-docs
plan: "03"
type: execute
wave: 2
depends_on:
  - "55-01"
  - "55-02"
files_modified:
  - guides/04-troubleshooting.md
  - test/cairnloop/docs/docker_first_docs_test.exs
autonomous: true
requirements:
  - DOC-03
  - DOC-04
user_setup: []
must_haves:
  truths:
    - "DOC-03 / D-08: Troubleshooting starts with Docker demo failure modes before legacy installer issues."
    - "D-09: Troubleshooting points first to bounded wrapper diagnostics: logs, status, reset, failing route, and health URL."
    - "D-10: Docs distinguish private Docker pgvector Postgres from manual local Postgres 16 plus pgvector."
    - "D-11 / D-12: OPENAI_API_KEY is optional for first-run boot, route smoke, and seeded click-through, without claiming production AI needs no provider configuration."
    - "D-13 / D-14 / D-15: Smoke docs describe the local isolated HTTP route smoke and locked route list at a high level."
  artifacts:
    - path: guides/04-troubleshooting.md
      provides: "HexDocs troubleshooting taxonomy for Docker demo failures."
      contains: "Docker demo"
    - path: test/cairnloop/docs/docker_first_docs_test.exs
      provides: "DB-free source-scan regression test for Docker-first docs consistency."
      exports:
        - "Cairnloop.Docs.DockerFirstDocsTest"
  key_links:
    - from: guides/04-troubleshooting.md
      to: bin/demo
      via: "Troubleshooting commands and diagnostics mirror wrapper behavior."
      pattern: "\\./bin/demo (logs|status|reset|smoke)"
    - from: test/cairnloop/docs/docker_first_docs_test.exs
      to: README.md
      via: "Source scan asserts README Docker-first ordering and current command vocabulary."
      pattern: "File.read!"
    - from: test/cairnloop/docs/docker_first_docs_test.exs
      to: guides/04-troubleshooting.md
      via: "Source scan asserts Docker demo troubleshooting taxonomy."
      pattern: "Docker"
---

<objective>
Add the Docker demo troubleshooting taxonomy and guard the whole first-run story with a DB-free source-scan test.

Purpose: DOC-03 and DOC-04 require adopters to resolve common demo failures and understand smoke/OpenAI behavior without external-service prerequisites.
Output: Updated `guides/04-troubleshooting.md`, new `test/cairnloop/docs/docker_first_docs_test.exs`, and final validation over all Phase 55 docs.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-docker-first-adopter-docs/55-CONTEXT.md
@.planning/phases/55-docker-first-adopter-docs/55-RESEARCH.md
@.planning/phases/55-docker-first-adopter-docs/55-VALIDATION.md
@.planning/phases/55-docker-first-adopter-docs/55-PATTERNS.md
@.planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
@.planning/phases/54-demo-wrapper-experience/54-VERIFICATION.md
@.planning/phases/53-demo-runtime-contract/53-PATTERNS.md
@.planning/phases/53-demo-runtime-contract/53-VERIFICATION.md
@CLAUDE.md
@AGENTS.md
@README.md
@guides/01-quickstart.md
@guides/04-troubleshooting.md
@examples/cairnloop_example/README.md
@mix.exs
@bin/demo
@examples/cairnloop_example/compose.demo.yml
@examples/cairnloop_example/Dockerfile.demo
@examples/cairnloop_example/lib/cairnloop_example_web/router.ex
@test/cairnloop/demo_wrapper_contract_test.exs
@test/cairnloop/web/collateral_wiring_test.exs
@test/cairnloop/web/brand_token_gate_test.exs
</context>

## Artifacts This Phase Produces

- Modified by this plan: `guides/04-troubleshooting.md`.
- New file created by this plan: `test/cairnloop/docs/docker_first_docs_test.exs`.
- New test module created by this plan: `Cairnloop.Docs.DockerFirstDocsTest`.
- New test functions to create in `Cairnloop.Docs.DockerFirstDocsTest`:
  - `README and Quickstart lead with Docker demo`
  - `example README keeps Docker URLs dynamic`
  - `troubleshooting covers Docker demo failure taxonomy`
  - `OpenAI and smoke docs stay credential-free and scoped`
- No schema push is required: this plan creates a DB-free ExUnit source-scan test and updates Markdown only; no ORM schema, Ecto schema module, or migration file is modified.

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Make first-run story consistent anywhere an adopter enters repo or HexDocs. | 55-01, 55-02, 55-03 | COVERED | This plan closes troubleshooting and phase-wide regression coverage. |
| REQ | DOC-03 | Troubleshooting covers Docker unavailable, Compose v2 missing, port conflict, unhealthy stack, reset/reseed, and pgvector/manual Postgres confusion. | 55-03 | COVERED | Task 1 implements the taxonomy. |
| REQ | DOC-04 | Maintainer can explain smoke workflow and route coverage without OpenAI or external services. | 55-03 | COVERED | Task 1 documents it; Task 2 tests it. |
| RESEARCH | - | Add Docker demo troubleshooting before legacy installer issues and point to wrapper diagnostics. | 55-03 | COVERED | Task 1. |
| RESEARCH | - | Prefer a DB-free source-scan docs consistency test when multiple docs can drift. | 55-03 | COVERED | Task 2. |
| CONTEXT | D-08 | Docker demo troubleshooting section before legacy installation issues. | 55-03 | COVERED | Task 1. |
| CONTEXT | D-09 | Troubleshooting uses bounded wrapper diagnostics. | 55-03 | COVERED | Task 1. |
| CONTEXT | D-10 | Docker private pgvector versus manual Postgres split. | 55-03 | COVERED | Task 1. |
| CONTEXT | D-11 | OPENAI_API_KEY optional for first-run, route smoke, seeded click-through. | 55-03 | COVERED | Task 1 and Task 2. |
| CONTEXT | D-12 | Credential-free claim scoped; production AI may need provider config. | 55-03 | COVERED | Task 1 and Task 2. |
| CONTEXT | D-13 | `./bin/demo smoke` local isolated smoke lane. | 55-03 | COVERED | Task 1 and Task 3. |
| CONTEXT | D-14 | Smoke docs high-level, HTTP route smoke, not browser E2E or CI workflow. | 55-03 | COVERED | Task 1 and Task 2. |
| CONTEXT | D-15 | Route coverage aligns with Phase 54 locked route list. | 55-03 | COVERED | Task 1 and Task 2. |
| CONTEXT | D-16 | Only DOC-01 through DOC-04 docs plus adjacent contradiction cleanup. | 55-03 | COVERED | Scope limited to troubleshooting and source-scan test. |

<tasks>

<task type="auto">
  <name>Task 1: Add Docker demo troubleshooting before installer issues</name>
  <files>guides/04-troubleshooting.md</files>
  <read_first>
    guides/04-troubleshooting.md
    guides/01-quickstart.md
    bin/demo
    examples/cairnloop_example/compose.demo.yml
    examples/cairnloop_example/Dockerfile.demo
    examples/cairnloop_example/lib/cairnloop_example_web/router.ex
    .planning/phases/55-docker-first-adopter-docs/55-CONTEXT.md
    .planning/phases/55-docker-first-adopter-docs/55-RESEARCH.md
    .planning/phases/55-docker-first-adopter-docs/55-PATTERNS.md
    .planning/phases/54-demo-wrapper-experience/54-VERIFICATION.md
  </read_first>
  <action>
    Update `guides/04-troubleshooting.md` with a Docker demo section before the existing `mix cairnloop.install` troubleshooting per D-08. Use symptom-first headings that cover Docker not installed or not on PATH, Docker Compose v2 missing, no available web port or fixed-port conflict, unhealthy stack, reset/reseed need, private Docker pgvector Postgres versus manual local Postgres confusion, optional `OPENAI_API_KEY`, and smoke route failures. Per D-09, point first to `./bin/demo logs`, `./bin/demo status`, `./bin/demo reset`, the failing route URL, and `/health`; keep diagnostics bounded and do not encourage long Compose output or credential dumps. Per D-10, state that Docker keeps pgvector Postgres private in Compose and needs no host Postgres, while manual local setup needs Postgres 16 plus pgvector and may use the root database helper. Per D-11 and D-12, state `OPENAI_API_KEY` is optional for first-run boot, route smoke, and seeded click-through, while host-app production AI behavior may need provider configuration. Per D-13, D-14, and D-15, describe `./bin/demo smoke` as local isolated HTTP route smoke with the Phase 54 route list and bounded failing-route plus recent-web-log diagnostics. Keep scope to docs and source-scan coverage per D-16.
  </action>
  <acceptance_criteria>
    - `guides/04-troubleshooting.md` has a Docker demo section before `mix cairnloop.install` prerequisites.
    - The section covers Docker missing, Compose v2 missing, port conflict, unhealthy stack, reset/reseed, pgvector/manual Postgres split, optional OpenAI credentials, and smoke route failures.
    - Troubleshooting points to `./bin/demo logs`, `./bin/demo status`, `./bin/demo reset`, failing route URL, and `/health` before raw Compose internals.
    - OpenAI credential-free wording is scoped to first-run boot, route smoke, and seeded click-through.
    - Smoke route coverage matches the locked Phase 54 route list and does not describe browser E2E or CI workflow behavior.
  </acceptance_criteria>
  <verify>
    <automated>elixir -e 's=File.read!("guides/04-troubleshooting.md"); {d,_}=:binary.match(s,"Docker demo"); {i,_}=:binary.match(s,"mix cairnloop.install"); if d >= i, do: raise("Docker demo troubleshooting must appear before installer troubleshooting")'</automated>
    <automated>rg -n 'Docker|Compose v2|port|healthy|reset|reseed|pgvector|OPENAI_API_KEY|./bin/demo logs|./bin/demo status|./bin/demo reset|./bin/demo smoke|/health' guides/04-troubleshooting.md</automated>
    <automated>./bin/demo help</automated>
  </verify>
  <done>Troubleshooting gives adopters a Docker-first failure taxonomy and bounded remediation path.</done>
</task>

<task type="auto">
  <name>Task 2: Add DB-free Docker-first docs source-scan test</name>
  <files>test/cairnloop/docs/docker_first_docs_test.exs</files>
  <read_first>
    test/cairnloop/demo_wrapper_contract_test.exs
    test/cairnloop/web/collateral_wiring_test.exs
    test/cairnloop/web/brand_token_gate_test.exs
    README.md
    guides/01-quickstart.md
    guides/04-troubleshooting.md
    examples/cairnloop_example/README.md
    bin/demo
    .planning/phases/55-docker-first-adopter-docs/55-VALIDATION.md
    .planning/phases/55-docker-first-adopter-docs/55-PATTERNS.md
  </read_first>
  <action>
    Create `test/cairnloop/docs/docker_first_docs_test.exs` with module `Cairnloop.Docs.DockerFirstDocsTest`. Follow the established DB-free source-scan patterns: `use ExUnit.Case, async: true`, `File.read!/1` for docs, concise helper assertions, and clear failure messages. Do not start Docker, Phoenix, Repo, browser tooling, or `./bin/demo smoke`; it is acceptable to run `bash bin/demo help` as a fast command-surface assertion like `Cairnloop.DemoWrapperContractTest`. Add tests named exactly `README and Quickstart lead with Docker demo`, `example README keeps Docker URLs dynamic`, `troubleshooting covers Docker demo failure taxonomy`, and `OpenAI and smoke docs stay credential-free and scoped`. Assert D-01 through D-16 coverage in source terms: README/Quickstart Docker-first ordering, command vocabulary from `bin/demo help`, manual-local-only `localhost:4000` context in Quickstart and example README, troubleshooting taxonomy, optional `OPENAI_API_KEY` scope, and Phase 54 smoke route list. Keep the test independent of database availability.
  </action>
  <acceptance_criteria>
    - `test/cairnloop/docs/docker_first_docs_test.exs` exists and defines `Cairnloop.Docs.DockerFirstDocsTest`.
    - The module uses `ExUnit.Case, async: true` and only reads source files plus `./bin/demo help`.
    - The four required test names exist.
    - The test fails on Docker-facing hard-coded `localhost:4000`, missing wrapper commands, missing troubleshooting taxonomy, missing optional OpenAI scope, or stale route coverage.
    - The test does not start Docker, Phoenix, Repo, Playwright, or `./bin/demo smoke`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/docs/docker_first_docs_test.exs --warnings-as-errors</automated>
    <automated>mix ci.fast</automated>
  </verify>
  <done>Docs drift is guarded by a fast DB-free source-scan test that covers the Docker-first first-run story across all Phase 55 docs.</done>
</task>

<task type="auto">
  <name>Task 3: Run final docs and smoke validation</name>
  <files>README.md, guides/01-quickstart.md, guides/04-troubleshooting.md, examples/cairnloop_example/README.md, test/cairnloop/docs/docker_first_docs_test.exs</files>
  <read_first>
    README.md
    guides/01-quickstart.md
    guides/04-troubleshooting.md
    examples/cairnloop_example/README.md
    test/cairnloop/docs/docker_first_docs_test.exs
    bin/demo
    examples/cairnloop_example/compose.demo.yml
    .planning/phases/55-docker-first-adopter-docs/55-VALIDATION.md
    .planning/phases/54-demo-wrapper-experience/54-VERIFICATION.md
  </read_first>
  <action>
    Run the phase validation after Wave 1 plans and Tasks 1-2 are complete. Confirm the docs source-scan test passes, wrapper help still exposes the command vocabulary, Compose config still parses, and docs/package quality gates pass. Run `timeout 300s ./bin/demo smoke` when Docker and Compose are available; if Docker is unavailable in the execution environment, capture the exact Docker/Compose availability output and still run all non-Docker gates. Do not change runtime or wrapper behavior to make docs pass; docs must follow the already-verified Phase 53 and Phase 54 contracts.
  </action>
  <acceptance_criteria>
    - `mix test test/cairnloop/docs/docker_first_docs_test.exs --warnings-as-errors` passes.
    - `./bin/demo help` prints the documented wrapper command vocabulary.
    - `docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet` passes when Docker Compose is available.
    - `timeout 300s ./bin/demo smoke` passes when Docker is available, or the summary records Docker unavailability with command output.
    - `mix ci.fast` and `mix ci.quality` pass before phase close.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/docs/docker_first_docs_test.exs --warnings-as-errors</automated>
    <automated>./bin/demo help</automated>
    <automated>docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet</automated>
    <automated>timeout 300s ./bin/demo smoke</automated>
    <automated>mix ci.fast</automated>
    <automated>mix ci.quality</automated>
  </verify>
  <done>Final Phase 55 validation proves docs consistency, package docs quality, wrapper command alignment, and Docker smoke where the local environment allows it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| adopter to troubleshooting docs | Users follow failure recovery advice after local demo startup problems. |
| docs to logs | Troubleshooting may ask users to inspect logs that can contain local details. |
| docs to credentials | Troubleshooting may mention `OPENAI_API_KEY` and provider configuration. |
| docs source-scan test to repo files | Test reads docs and wrapper source without executing runtime paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-55-08 | Information Disclosure | Troubleshooting logs guidance | mitigate | Per D-09, route users to bounded `./bin/demo logs`, `status`, `reset`, failing route, and `/health` guidance; do not request unbounded Compose output or credentials. |
| T-55-09 | Spoofing | OpenAI credential guidance | mitigate | Per D-11 and D-12, state `OPENAI_API_KEY` is optional for local first-run/smoke/seeded click-through while preserving production provider configuration truth. |
| T-55-10 | Tampering | Smoke route documentation | mitigate | Per D-13 through D-15, align route coverage to Phase 54 and verify against source-scan tests plus `./bin/demo help`. |
| T-55-11 | Denial of Service | Port/reset troubleshooting | mitigate | Per D-08 through D-10, document dynamic ports and volume semantics so adopters use `reset` only when they intend to reseed. |
| T-55-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan; package legitimacy checkpoint is not required. |
</threat_model>

<verification>
Run these after completing all Phase 55 plans:
- `mix test test/cairnloop/docs/docker_first_docs_test.exs --warnings-as-errors`
- `./bin/demo help`
- `docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet`
- `timeout 300s ./bin/demo smoke`
- `mix ci.fast`
- `mix ci.quality`
- `rg -n 'localhost:4000' README.md guides/01-quickstart.md examples/cairnloop_example/README.md` and confirm remaining hits are manual-local Phoenix context.
- `sh -c 'if rg -n "~> 0\\.1\\.0" README.md guides/01-quickstart.md; then exit 1; fi'`
</verification>

<success_criteria>
DOC-03 and DOC-04 are satisfied. All DOC-01 through DOC-04 behaviors have source or command proof. D-01 through D-17 are covered across the three plans. No schema push is required, no new external packages are installed, and Phase 56 CI workflow wiring remains outside this phase.
</success_criteria>

<output>
Create `.planning/phases/55-docker-first-adopter-docs/55-03-SUMMARY.md` when done.
</output>
