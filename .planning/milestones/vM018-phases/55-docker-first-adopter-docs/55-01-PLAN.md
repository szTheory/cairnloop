---
phase: 55-docker-first-adopter-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/01-quickstart.md
autonomous: true
requirements:
  - DOC-01
  - DOC-04
user_setup: []
must_haves:
  truths:
    - "DOC-01 / D-01: README and Quickstart make ./bin/demo the first fresh-clone evaluation command."
    - "D-02: Manual local setup, Igniter installation, host integration, and mix phx.server remain secondary and clearly labeled."
    - "D-03 / D-05: Docker-facing copy tells adopters to open the printed URL and avoids hard-coded localhost:4000 outside manual-local context."
    - "D-07: README and Quickstart mirror the Phase 54 wrapper command vocabulary instead of inventing commands."
    - "D-17: Touched dependency snippets use the current mix.exs version 0.5.1."
  artifacts:
    - path: README.md
      provides: "Root adopter entry point and HexDocs README source."
      contains: "./bin/demo"
    - path: guides/01-quickstart.md
      provides: "HexDocs Quickstart with Docker-first first-run path."
      contains: "Fastest path: Docker demo"
  key_links:
    - from: README.md
      to: bin/demo
      via: "README command and URL copy mirrors ./bin/demo help and printed URLs."
      pattern: "\\./bin/demo"
    - from: guides/01-quickstart.md
      to: guides/04-troubleshooting.md
      via: "Quickstart points first-run failure recovery toward the troubleshooting guide."
      pattern: "Troubleshooting"
---

<objective>
Make the root README and HexDocs Quickstart tell the same Docker-first first-run story.

Purpose: DOC-01 requires adopters who enter through GitHub or HexDocs to start with `./bin/demo`, not manual local setup.
Output: Updated `README.md` and `guides/01-quickstart.md` that align first-run order, command vocabulary, printed URL guidance, and touched dependency versions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-docker-first-adopter-docs/55-CONTEXT.md
@.planning/phases/55-docker-first-adopter-docs/55-RESEARCH.md
@.planning/phases/55-docker-first-adopter-docs/55-VALIDATION.md
@.planning/phases/55-docker-first-adopter-docs/55-PATTERNS.md
@.planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
@.planning/phases/54-demo-wrapper-experience/54-VERIFICATION.md
@.planning/phases/53-demo-runtime-contract/53-PATTERNS.md
@.planning/phases/53-demo-runtime-contract/53-VERIFICATION.md
@CLAUDE.md
@AGENTS.md
@README.md
@guides/01-quickstart.md
@mix.exs
@bin/demo
</context>

## Artifacts This Phase Produces

- Modified by this plan: `README.md`, `guides/01-quickstart.md`.
- New files created by this plan: none.
- Phase-level new file created by Plan 55-03: `test/cairnloop/docs/docker_first_docs_test.exs`.
- Phase-level new test module created by Plan 55-03: `Cairnloop.Docs.DockerFirstDocsTest`.
- No schema push is required: scoped Phase 55 files are documentation plus a DB-free source-scan test; no ORM schema, Ecto schema module, or migration file is modified.

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Make first-run story consistent anywhere an adopter enters repo or HexDocs. | 55-01, 55-02, 55-03 | COVERED | This plan covers root README and Quickstart entry points. |
| REQ | DOC-01 | Docker demo first in README and Quickstart; manual setup secondary. | 55-01 | COVERED | Tasks cite D-01 and D-02. |
| REQ | DOC-04 | Explain local smoke and route coverage without external credentials. | 55-01, 55-03 | COVERED | This plan mirrors command vocabulary; Plan 55-03 adds full smoke/OpenAI coverage. |
| RESEARCH | - | README and Quickstart already Docker-first but need command vocabulary, troubleshooting link text, and stale version cleanup. | 55-01 | COVERED | Uses `bin/demo` and `mix.exs` as source of truth. |
| CONTEXT | D-01 | Docker demo is the primary evaluation path. | 55-01 | COVERED | Implemented in Task 1 and Task 2. |
| CONTEXT | D-02 | Manual local and host paths remain secondary. | 55-01 | COVERED | Implemented in Task 1 and Task 2. |
| CONTEXT | D-03 | Calm adopter-facing copy. | 55-01 | COVERED | Implemented in both tasks. |
| CONTEXT | D-04 | `./bin/demo` output/help are command and URL source of truth. | 55-01 | COVERED | Verified with `./bin/demo help`. |
| CONTEXT | D-05 | Docker docs use printed URL, not hard-coded localhost:4000. | 55-01 | COVERED | Implemented in both docs. |
| CONTEXT | D-07 | Wrapper command vocabulary and volume semantics. | 55-01 | COVERED | Implemented in Task 2. |
| CONTEXT | D-16 | Only DOC-01 through DOC-04 docs plus adjacent contradiction cleanup. | 55-01 | COVERED | Files are limited to README and Quickstart. |
| CONTEXT | D-17 | Touched stale dependency snippets may be corrected to current repo version. | 55-01 | COVERED | Uses `mix.exs` version 0.5.1. |

<tasks>

<task type="auto">
  <name>Task 1: Keep README Docker-first and current-version consistent</name>
  <files>README.md</files>
  <read_first>
    README.md
    mix.exs
    bin/demo
    .planning/phases/55-docker-first-adopter-docs/55-CONTEXT.md
    .planning/phases/55-docker-first-adopter-docs/55-RESEARCH.md
    .planning/phases/54-demo-wrapper-experience/54-VERIFICATION.md
  </read_first>
  <action>
    Update `README.md` so the root adopter entry keeps `### Try the live demo first` as the first evaluation path and keeps `./bin/demo` as the first command for a fresh clone per D-01. Preserve Igniter/manual install guidance after the Docker demo and label it as host-app or manual setup per D-02. Use calm outcome-focused wording per D-03: run the demo, open the printed URL, reset the seeded database, view logs. Mirror `bin/demo` as the command and URL truth per D-04 and D-05; Docker-facing copy must say to use the printed URL and must not point to `localhost:4000`. If the touched README dependency snippets still show an older Hex requirement, update them to `{:cairnloop, "~> 0.5.1"}` from `mix.exs` per D-17. Update the Troubleshooting guide link copy so Docker demo failures, dynamic ports, reset, and logs are included in the link description. Keep scope to DOC-01 through DOC-04 adjacent copy per D-16; do not edit runtime, wrapper, CI, or product UI files.
  </action>
  <acceptance_criteria>
    - `README.md` presents `./bin/demo` before Igniter, manual install, and host-app setup guidance.
    - Docker-facing README text says to open the URL printed by `./bin/demo`.
    - README manual/host-app installation remains available but secondary.
    - README dependency snippets touched by this task use `{:cairnloop, "~> 0.5.1"}`.
    - README troubleshooting link text mentions Docker demo failure recovery, not only legacy install or migration issues.
  </acceptance_criteria>
  <verify>
    <automated>elixir -e 's=File.read!("README.md"); {d,_}=:binary.match(s,"### Try the live demo first"); {i,_}=:binary.match(s,"### Install in your app"); if d >= i, do: raise("README Docker demo heading must precede install heading")'</automated>
    <automated>rg -n '\./bin/demo' README.md</automated>
    <automated>rg -n '\{:cairnloop, "~> 0\.5\.1"\}' README.md</automated>
    <automated>sh -c 'if rg -n "~> 0\\.1\\.0" README.md; then exit 1; fi'</automated>
  </verify>
  <done>README gives Docker-first first-run guidance, keeps manual setup secondary, avoids Docker hard-coded localhost guidance, and uses the current package version where touched.</done>
</task>

<task type="auto">
  <name>Task 2: Align Quickstart command vocabulary and manual-path boundary</name>
  <files>guides/01-quickstart.md</files>
  <read_first>
    guides/01-quickstart.md
    README.md
    guides/04-troubleshooting.md
    mix.exs
    bin/demo
    .planning/phases/55-docker-first-adopter-docs/55-CONTEXT.md
    .planning/phases/55-docker-first-adopter-docs/55-PATTERNS.md
    .planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
  </read_first>
  <action>
    Update `guides/01-quickstart.md` so the `Fastest path: Docker demo` section remains first and uses `./bin/demo` as the evaluation command per D-01. Keep `Prerequisites`, Igniter install, host integration, and `Manual boot` clearly scoped to manual local or production host work per D-02. Expand the useful wrapper commands to the Phase 54 vocabulary per D-07: default `start`/`up`, `urls`, `logs`, `status`/`ps`, `stop`, `down`, `reset`, `smoke`, and `help`. Preserve the semantic distinction that `stop` and `down` keep named volumes, while `reset` removes volumes and reseeds. Keep Docker URL guidance tied to the printed URL and dynamic `4100-4199` port behavior per D-04 and D-05; reserve `localhost:4000` for manual local Phoenix boot per D-06. If touched dependency snippets still show the stale package requirement, update them to `{:cairnloop, "~> 0.5.1"}` from `mix.exs` per D-17. Update the final Troubleshooting link to describe Docker demo issues, pgvector/manual Postgres confusion, and reset/log flows. Keep copy calm and adopter-facing per D-03 and scope-limited per D-16.
  </action>
  <acceptance_criteria>
    - Quickstart Docker demo section appears before manual prerequisites and manual boot.
    - Quickstart command vocabulary includes `start`/`up`, `urls`, `logs`, `status`/`ps`, `stop`, `down`, `reset`, `smoke`, and `help`.
    - Quickstart explains stop/down/reset volume behavior.
    - Any remaining `localhost:4000` reference is explicitly tied to manual local Phoenix boot.
    - Quickstart touched dependency snippets use `{:cairnloop, "~> 0.5.1"}`.
  </acceptance_criteria>
  <verify>
    <automated>./bin/demo help</automated>
    <automated>elixir -e 's=File.read!("guides/01-quickstart.md"); {d,_}=:binary.match(s,"## Fastest path: Docker demo"); {p,_}=:binary.match(s,"## Prerequisites"); if d >= p, do: raise("Quickstart Docker demo section must precede prerequisites")'</automated>
    <automated>rg -n 'start|up|urls|logs|status|ps|stop|down|reset|smoke|help' guides/01-quickstart.md</automated>
    <automated>sh -c 'if rg -n "~> 0\\.1\\.0" guides/01-quickstart.md; then exit 1; fi'</automated>
    <automated>mix ci.fast</automated>
  </verify>
  <done>Quickstart mirrors README and `bin/demo`, keeps manual local setup secondary, and points first-run recovery toward troubleshooting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| adopter to docs | Untrusted adopter follows README or HexDocs commands on a local machine. |
| docs to local shell | Documentation can cause adopters to run wrapper, Mix, or Docker commands. |
| docs to secrets | Documentation may mention environment variables such as `OPENAI_API_KEY`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-55-01 | Tampering | README / Quickstart first-run path | mitigate | Per D-01, D-02, and DOC-01, keep Docker demo first and manual setup secondary so docs do not send evaluators into host Elixir/Postgres setup. |
| T-55-02 | Spoofing | Wrapper command vocabulary | mitigate | Per D-04 and D-07, verify command names against `./bin/demo help` and do not introduce alternate primary commands. |
| T-55-03 | Information Disclosure | Logs and credentials in docs | mitigate | Keep README/Quickstart diagnostics bounded to `./bin/demo logs`, reset, status, and printed URLs; do not instruct users to paste credentials or unbounded Compose output. |
| T-55-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan; package legitimacy checkpoint is not required. |
</threat_model>

<verification>
Run these after completing both tasks:
- `./bin/demo help`
- `mix ci.fast`
- `mix ci.quality`
- `rg -n '\./bin/demo|CAIRNLOOP_WEB_PORT|Troubleshooting' README.md guides/01-quickstart.md`
- `sh -c 'if rg -n "~> 0\\.1\\.0" README.md guides/01-quickstart.md; then exit 1; fi'`
</verification>

<success_criteria>
DOC-01 is satisfied for README and Quickstart. DOC-04 command-language groundwork is in place for Plan 55-03. No schema push is required, no external packages are added, and no Phase 56 CI workflow or runtime behavior is changed.
</success_criteria>

<output>
Create `.planning/phases/55-docker-first-adopter-docs/55-01-SUMMARY.md` when done.
</output>
