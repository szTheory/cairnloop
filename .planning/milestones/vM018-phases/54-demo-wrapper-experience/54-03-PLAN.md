---
phase: 54-demo-wrapper-experience
plan: "03"
type: execute
wave: 2
depends_on:
  - "54-02"
files_modified: []
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
  - BOOT-03
  - BOOT-04
  - VER-01
  - VER-02
user_setup: []
must_haves:
  truths:
    - "D-16: `mix ci.fast` passes before Phase 54 closes."
    - "D-04/D-05/D-16: `docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet` passes and the demo DB remains private while Phoenix publishes on localhost."
    - "D-08/D-09/D-10/D-11: `./bin/demo smoke` boots an isolated stack, waits for `/health`, checks the locked HTTP routes, and cleans up containers plus volumes."
    - "D-03/D-14: operational wrapper commands remain available after smoke verification: urls, logs, status/ps, stop, down, reset, and help."
    - "D-12/D-13: if smoke fails during execution, the failure report includes the failing URL/command and recent web logs."
  artifacts:
    - path: "bin/demo"
      provides: "Final adopter-facing demo wrapper behavior under Docker smoke"
      exports: ["start_demo", "smoke_demo", "print_urls", "wait_for_health", "smoke_route"]
    - path: "test/cairnloop/demo_wrapper_contract_test.exs"
      provides: "DB-free regression coverage run inside `mix ci.fast`"
      exports: ["Cairnloop.DemoWrapperContractTest"]
    - path: "examples/cairnloop_example/compose.demo.yml"
      provides: "Validated Compose contract for private DB and dynamic localhost web port"
      contains: "target: 4000"
  key_links:
    - from: "bin/demo"
      to: "examples/cairnloop_example/compose.demo.yml"
      via: "final Compose config and smoke execution"
      pattern: "compose\\.demo\\.yml"
    - from: "bin/demo"
      to: "examples/cairnloop_example/lib/cairnloop_example_web/router.ex"
      via: "HTTP route smoke hits mounted routes"
      pattern: "smoke_route.*support/settings"
    - from: "test/cairnloop/demo_wrapper_contract_test.exs"
      to: "mix.exs"
      via: "DB-free ExUnit test runs under root `mix ci.fast`"
      pattern: "test --exclude integration --warnings-as-errors"
---

<objective>
Run the final Phase 54 validation gate against the hardened wrapper.

Purpose: prove the adopter-facing wrapper works from the repo root with Docker Compose, private DB networking, discovered URLs, isolated smoke, and actionable diagnostics.
Output: command evidence in the plan summary; no production source edits unless verification reveals a defect that belongs to Phase 54.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
@.planning/phases/54-demo-wrapper-experience/54-RESEARCH.md
@.planning/phases/54-demo-wrapper-experience/54-PATTERNS.md
@.planning/phases/54-demo-wrapper-experience/54-VALIDATION.md
@.planning/phases/54-demo-wrapper-experience/54-02-SUMMARY.md
@bin/demo
@test/cairnloop/demo_wrapper_contract_test.exs
@examples/cairnloop_example/compose.demo.yml
@examples/cairnloop_example/Dockerfile.demo
@examples/cairnloop_example/lib/cairnloop_example_web/router.ex
</context>

## Plan Assumptions

- Schema Push Detection Gate: no schema files are modified or detected in Phase 54 scope; no schema push task is planned.
- Package Legitimacy Gate: no package installs.
- This is an automated verification plan, not human UAT. If Docker smoke fails, fix the Phase 54 wrapper defect and rerun; do not create a manual checkpoint.

## Artifacts This Phase Produces

- No new source files or shell symbols are created by this plan.
- Verification evidence is recorded in `.planning/phases/54-demo-wrapper-experience/54-03-SUMMARY.md`.
- Phase artifacts verified: `bin/demo`, `test/cairnloop/demo_wrapper_contract_test.exs`, `examples/cairnloop_example/compose.demo.yml`, and `examples/cairnloop_example/Dockerfile.demo`.
- Existing CLI subcommands/aliases verified: `start`, `up`, `urls`, `logs`, `status`, `ps`, `stop`, `down`, `reset`, `smoke`, `help`, `-h`, `--help`.

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | - | Make `./bin/demo` the adopter-facing operational surface for the local demo | 54-01, 54-02, 54-03 | COVERED | This plan proves the full result.
REQ | BOOT-01 | Docker Compose v2 only local runtime prerequisite | 54-01, 54-03 | COVERED | Final smoke runs from repo root through Docker Compose.
REQ | BOOT-02 | Dynamic/configured localhost Phoenix port and private Compose Postgres | 54-01, 54-03 | COVERED | Compose config and smoke prove the contract.
REQ | BOOT-03 | Exact running URLs after health | 54-01, 54-02, 54-03 | COVERED | Smoke uses discovered base URL after health; wrapper output can be inspected from command evidence.
REQ | BOOT-04 | Wrapper operational subcommands | 54-01, 54-02, 54-03 | COVERED | Help/contract tests plus final command checks.
REQ | VER-01 | Isolated smoke stack and cleanup | 54-01, 54-03 | COVERED | `./bin/demo smoke` is the final gate.
REQ | VER-02 | Failure output includes route/logs | 54-01, 54-02, 54-03 | COVERED | Contract test pins source; final smoke failure path is reported if triggered.
RESEARCH | - | Final gate is `mix ci.fast`, Compose config, `./bin/demo smoke` | 54-03 | COVERED | Implemented here.
CONTEXT | D-01 | Single canonical `./bin/demo` | 54-03 | COVERED | Smoke uses wrapper.
CONTEXT | D-02 | Preserve command vocabulary | 54-03 | COVERED | Help/source tests already green; final plan checks help/status.
CONTEXT | D-03 | stop/down/reset semantics | 54-03 | COVERED | Contract tests and smoke cleanup prove scoped semantics.
CONTEXT | D-04 | DB private | 54-03 | COVERED | Compose config validation.
CONTEXT | D-05 | Dynamic localhost Phoenix port | 54-03 | COVERED | Compose config validation and smoke.
CONTEXT | D-06 | Compose port URL discovery | 54-03 | COVERED | Smoke and source tests.
CONTEXT | D-07 | `urls` route block/fail closed | 54-03 | COVERED | Contract tests and optional running-stack `urls` check.
CONTEXT | D-08 | `/health` readiness | 54-03 | COVERED | Smoke waits for health.
CONTEXT | D-09 | Locked HTTP route list | 54-03 | COVERED | Smoke checks routes.
CONTEXT | D-10 | Isolated smoke namespace | 54-03 | COVERED | Smoke command behavior and contract tests.
CONTEXT | D-11 | Optional smoke port override | 54-03 | COVERED | Contract tests.
CONTEXT | D-12 | Calm diagnostics | 54-03 | COVERED | Source tests and failure output if smoke fails.
CONTEXT | D-13 | Logs on health/route failure | 54-03 | COVERED | Source tests and failure output if smoke fails.
CONTEXT | D-14 | Logs command covers web/db | 54-03 | COVERED | Contract tests.
CONTEXT | D-15 | Automated proof | 54-03 | COVERED | No human UAT.
CONTEXT | D-16 | Compose config, smoke, `mix ci.fast` before close | 54-03 | COVERED | This plan runs all three.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Run the full Phase 54 command gate</name>
  <read_first>
    bin/demo
    test/cairnloop/demo_wrapper_contract_test.exs
    examples/cairnloop_example/compose.demo.yml
    examples/cairnloop_example/Dockerfile.demo
    .planning/phases/54-demo-wrapper-experience/54-VALIDATION.md
    .planning/phases/54-demo-wrapper-experience/54-02-SUMMARY.md
  </read_first>
  <files>No files modified unless verification exposes a Phase 54 defect.</files>
  <action>Run the full Phase 54 gate from the repository root: `mix ci.fast`, `docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet`, and `./bin/demo smoke`. Let `./bin/demo smoke` use its isolated `_smoke` project namespace; do not set a fixed smoke port unless the command reports a real local collision, and then use `CAIRNLOOP_SMOKE_WEB_PORT` only for the rerun and record that in the summary. If smoke fails, capture the failing route/readiness URL or failed command plus recent web logs from wrapper output, fix the Phase 54 wrapper issue, and rerun all three commands.</action>
  <verify>
    <automated>mix ci.fast && docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet && ./bin/demo smoke</automated>
  </verify>
  <acceptance_criteria>
    - `mix ci.fast` passes with the new DB-free contract test included.
    - Compose config validation passes.
    - `./bin/demo smoke` passes and prints `Docker demo smoke passed.`
    - Smoke checks `/`, `/support`, `/support/inbox`, `/chat`, `/support/knowledge-base`, `/support/knowledge-base/gaps`, `/support/knowledge-base/suggestions`, `/support/audit-log`, and `/support/settings`.
    - Smoke cleanup does not remove the normal non-smoke demo project.
  </acceptance_criteria>
  <done>Full Phase 54 automated validation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove operational commands remain discoverable after smoke</name>
  <read_first>
    bin/demo
    .planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
    .planning/phases/54-demo-wrapper-experience/54-VALIDATION.md
  </read_first>
  <files>No files modified unless verification exposes a Phase 54 defect.</files>
  <action>After the full gate, run lightweight wrapper checks that do not mutate data outside Compose semantics: `./bin/demo help` and `./bin/demo status`. If a normal demo stack is still running from local work, `./bin/demo urls` may also be run to confirm the route block; otherwise do not start a non-smoke stack just for this task because `./bin/demo smoke` already proved route reachability. Record command output facts in the summary. If `status` or help fails, fix `bin/demo` and rerun Task 1.</action>
  <verify>
    <automated>./bin/demo help && ./bin/demo status</automated>
  </verify>
  <acceptance_criteria>
    - Help prints every supported command/alias named in D-02.
    - `status` invokes Compose status for the scoped project and exits without shell syntax/runtime wrapper errors.
    - No global Docker cleanup command is run.
  </acceptance_criteria>
  <done>Operational command surface remains discoverable after smoke verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell -> Docker Compose | Final verification runs lifecycle commands against Docker. |
| Compose stack -> host network | Smoke publishes Phoenix to a loopback dynamic/configured port. |
| wrapper diagnostics -> terminal | Failure details are exposed to maintainer/adopter terminal output. |
| smoke cleanup -> Docker resources | Smoke removes containers and volumes inside its project namespace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Information Disclosure | Compose DB service | mitigate | Final Compose config gate must pass; source test already forbids DB host ports. |
| T-54-02 | Information Disclosure | web host binding | mitigate | Final Compose config and smoke verify web is reachable through the intended loopback published port. |
| T-54-03 | Tampering/Denial of Service | smoke cleanup | mitigate | Smoke uses `_smoke` namespace and project-scoped `down -v --remove-orphans`; no global prune. |
| T-54-04 | Tampering | command/route strings | mitigate | Source test pins literal route list and explicit command case branches before smoke. |
| T-54-05 | Information Disclosure | failure logs | mitigate | If smoke fails, summary must include wrapper's bounded failure output and no env/inspect JSON dump. |
| T-54-06 | Elevation of Privilege | Docker operations | accept | Docker Compose is the required runtime; verification uses normal Compose operations only. |
| T-54-SC | Tampering | package installs | accept | No package-manager install tasks exist. |
</threat_model>

<verification>
Final gate: `mix ci.fast && docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet && ./bin/demo smoke`.
</verification>

<success_criteria>
- All Phase 54 requirements BOOT-01, BOOT-02, BOOT-03, BOOT-04, VER-01, and VER-02 have automated evidence.
- No human UAT checkpoint is needed.
- No schema push, package install, docs narrative, CI workflow, UI, CSS, router, or Dockerfile work is introduced by this plan.
</success_criteria>

<output>
Create `.planning/phases/54-demo-wrapper-experience/54-03-SUMMARY.md` when done.
</output>
