---
phase: 54-demo-wrapper-experience
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - test/cairnloop/demo_wrapper_contract_test.exs
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
  - BOOT-03
  - BOOT-04
  - VER-01
  - VER-02
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: `./bin/demo` is the single canonical wrapper and advertises start/up, urls, logs, status/ps, stop, down, reset, smoke, and help."
    - "D-04/D-05: Compose keeps Postgres private and publishes only Phoenix on localhost with `CAIRNLOOP_WEB_PORT` defaulting to `4100-4199`."
    - "D-06/D-07/D-08: wrapper URL output is sourced from `docker compose port web 4000`; no Docker demo URL assumes `localhost:4000`."
    - "D-09/D-10/D-11: smoke uses the locked HTTP route list, isolated `_smoke` Compose project, optional `CAIRNLOOP_SMOKE_WEB_PORT`, and project-scoped cleanup."
    - "D-12/D-13/D-14: failure diagnostics include the failing readiness/route/command boundary and recent web logs without broad raw dumps."
    - "D-15/D-16: Phase 54 starts with DB-free source/contract proof before wrapper hardening and keeps Docker smoke as an explicit phase gate."
  artifacts:
    - path: "test/cairnloop/demo_wrapper_contract_test.exs"
      provides: "DB-free Phase 54 source/contract coverage for wrapper, Compose, URLs, smoke, diagnostics, and shell syntax"
      exports: ["Cairnloop.DemoWrapperContractTest"]
    - path: "bin/demo"
      provides: "Existing wrapper source under test; implementation changes land in dependent Plan 54-02"
      contains: "compose port web \"$CONTAINER_PORT\""
    - path: "examples/cairnloop_example/compose.demo.yml"
      provides: "Existing private DB and dynamic localhost web Compose contract under test"
      contains: "host_ip: \"${CAIRNLOOP_BIND_HOST:-127.0.0.1}\""
  key_links:
    - from: "test/cairnloop/demo_wrapper_contract_test.exs"
      to: "bin/demo"
      via: "File.read!/1 source assertions and bash syntax check"
      pattern: "System\\.cmd\\(\"bash\", \\[\"-n\", \"bin/demo\"\\]"
    - from: "test/cairnloop/demo_wrapper_contract_test.exs"
      to: "examples/cairnloop_example/compose.demo.yml"
      via: "File.read!/1 source assertions for private DB and dynamic web port"
      pattern: "CAIRNLOOP_WEB_PORT:-4100-4199"
    - from: "bin/demo"
      to: "examples/cairnloop_example/lib/cairnloop_example_web/router.ex"
      via: "Smoke route list mirrors mounted demo routes"
      pattern: "smoke_route.*support/settings"
---

<objective>
Create the Phase 54 Wave 0 contract test before wrapper hardening.

Purpose: lock the wrapper/Compose/security contract from BOOT-01 through BOOT-04 and VER-01 through VER-02 before changing `bin/demo`.
Output: one DB-free ExUnit test file that initially goes RED on the Phase 54 gaps and becomes green in Plan 54-02.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
@.planning/phases/54-demo-wrapper-experience/54-RESEARCH.md
@.planning/phases/54-demo-wrapper-experience/54-PATTERNS.md
@.planning/phases/54-demo-wrapper-experience/54-VALIDATION.md
@.planning/phases/53-demo-runtime-contract/53-VERIFICATION.md
@bin/demo
@examples/cairnloop_example/compose.demo.yml
@examples/cairnloop_example/Dockerfile.demo
@examples/cairnloop_example/lib/cairnloop_example_web/router.ex
@lib/cairnloop/router.ex
@test/cairnloop/web/collateral_wiring_test.exs
@test/cairnloop/web/brand_token_gate_test.exs
@test/integration/dashboard_wiring_test.exs
</context>

## Plan Assumptions

- Schema Push Detection Gate: no Payload CMS, Prisma, Drizzle, Supabase, or TypeORM schema files appear in the Phase 54 scope artifacts, so no schema push task is planned.
- Package Legitimacy Gate: Phase 54 installs no npm, pip, cargo, or Mix packages.
- UI/doc narrative work is out of scope for this plan; do not edit `lib/cairnloop/web/**`, `priv/static/cairnloop.css`, README, Quickstart, or troubleshooting docs.

## Artifacts This Phase Produces

- New test file: `test/cairnloop/demo_wrapper_contract_test.exs`.
- New test module: `Cairnloop.DemoWrapperContractTest`.
- New test helpers in that module: `wrapper_source/0`, `compose_source/0`, `help_output/0`, `assert_contains/2`, `assert_ordered_route_block/1`, and a DB-private Compose block helper if useful.
- Planned shell helper names asserted for Plan 54-02: `recent_web_logs`, `fail_with_web_logs`, `run_compose_or_explain`, and `wait_for_web_endpoint`.
- Existing CLI subcommands/aliases preserved by the phase: `start`, `up`, `urls`, `logs`, `status`, `ps`, `stop`, `down`, `reset`, `smoke`, `help`, `-h`, `--help`.
- Existing shell variables preserved by the phase: `CAIRNLOOP_WEB_PORT`, `CAIRNLOOP_SMOKE_WEB_PORT`, `CAIRNLOOP_BIND_HOST`, `CAIRNLOOP_COMPOSE_PROJECT`, `OPENAI_API_KEY`, `LOCAL_UID`, `LOCAL_GID`, `COMPOSE_FILE`, `CONTAINER_PORT`.

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | - | Make `./bin/demo` the adopter-facing operational surface for the local demo | 54-01, 54-02, 54-03 | COVERED | Wave 0 tests, wrapper hardening, and final smoke gate cover the operational surface.
REQ | BOOT-01 | Docker Compose v2 is the only local runtime prerequisite for `./bin/demo` | 54-01, 54-03 | COVERED | Source test asserts wrapper uses Docker Compose and Dockerfile owns Elixir runtime; smoke proves behavior.
REQ | BOOT-02 | Dynamic/configured localhost Phoenix port and private Compose Postgres | 54-01, 54-03 | COVERED | Source test and Compose config gate assert private DB and loopback web publishing.
REQ | BOOT-03 | Exact URL block after health from running Compose stack | 54-01, 54-02, 54-03 | COVERED | Tests pin route labels and Compose port discovery; wrapper hardening preserves health wait.
REQ | BOOT-04 | urls/logs/status/stop/down/reset/help wrapper commands | 54-01, 54-02, 54-03 | COVERED | Tests pin command surface and help; wrapper updates help drift.
REQ | VER-01 | `./bin/demo smoke` boots isolated stack, checks routes, cleans up | 54-01, 54-03 | COVERED | Source test pins `_smoke`, route list, and `down -v`; final smoke runs.
REQ | VER-02 | Smoke failures include failing route and recent web logs | 54-01, 54-02 | COVERED | Tests pin diagnostic helpers; wrapper uses them at health, route, and compose-up boundaries.
RESEARCH | - | Harden existing `bin/demo` in place, no replacement wrapper | 54-01, 54-02 | COVERED | Test reads `bin/demo`; Plan 54-02 edits only the existing wrapper.
RESEARCH | - | Add DB-free source/contract test before Docker behavior gate | 54-01 | COVERED | This plan creates the Wave 0 test file.
RESEARCH | - | Keep final verification to `mix ci.fast`, Compose config, and `./bin/demo smoke` | 54-03 | COVERED | Final plan runs those commands.
CONTEXT | D-01 | `./bin/demo` remains single canonical entry point | 54-01, 54-02 | COVERED | Tests assert no competing primary path is introduced by this phase.
CONTEXT | D-02 | Preserve command vocabulary and aliases | 54-01, 54-02 | COVERED | Tests assert case aliases and help copy.
CONTEXT | D-03 | stop/down/reset volume semantics | 54-01, 54-02 | COVERED | Tests assert `stop`, `down --remove-orphans`, and reset `down -v` plus `start_demo`.
CONTEXT | D-04 | Keep Postgres private to Compose | 54-01, 54-03 | COVERED | Tests assert no DB host port; Compose config gate validates YAML.
CONTEXT | D-05 | Phoenix localhost port via `CAIRNLOOP_WEB_PORT=4100-4199` default | 54-01, 54-03 | COVERED | Tests assert env default and Compose host IP.
CONTEXT | D-06 | Printed links discovered by `docker compose port web 4000` | 54-01, 54-02 | COVERED | Tests assert source discovery and no hard-coded Docker URL.
CONTEXT | D-07 | `urls` prints same route block and fails closed when web absent | 54-01, 54-02 | COVERED | Tests assert shared route block and fail-closed copy.
CONTEXT | D-08 | start/smoke wait for real `/health` | 54-01, 54-02, 54-03 | COVERED | Tests assert `/health` readiness; final smoke proves behavior.
CONTEXT | D-09 | Smoke checks locked HTTP route list only | 54-01, 54-03 | COVERED | Tests assert each route and final smoke runs the command.
CONTEXT | D-10 | Smoke isolated Compose project and no disturbance to normal stack | 54-01, 54-03 | COVERED | Tests assert `_smoke` and final smoke cleanup.
CONTEXT | D-11 | `CAIRNLOOP_SMOKE_WEB_PORT` optional override, no fixed smoke port | 54-01 | COVERED | Tests assert env inheritance expression.
CONTEXT | D-12 | Calm actionable diagnostics, no broad raw dumps | 54-01, 54-02 | COVERED | Tests assert helper/copy boundaries.
CONTEXT | D-13 | Health/route failure prints recent web logs | 54-01, 54-02 | COVERED | Tests assert recent web logs at both boundaries.
CONTEXT | D-14 | `logs` covers web and db | 54-01 | COVERED | Tests assert `compose logs -f web db`.
CONTEXT | D-15 | Automated shell/source and Docker smoke proof | 54-01, 54-03 | COVERED | Wave 0 test plus final smoke.
CONTEXT | D-16 | Continue Compose config, smoke, and `mix ci.fast` before close | 54-03 | COVERED | Final plan runs all three.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the red Phase 54 wrapper contract test</name>
  <read_first>
    .planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
    .planning/phases/54-demo-wrapper-experience/54-RESEARCH.md
    .planning/phases/54-demo-wrapper-experience/54-PATTERNS.md
    .planning/phases/54-demo-wrapper-experience/54-VALIDATION.md
    bin/demo
    examples/cairnloop_example/compose.demo.yml
    test/cairnloop/web/collateral_wiring_test.exs
    test/cairnloop/web/brand_token_gate_test.exs
    test/integration/dashboard_wiring_test.exs
  </read_first>
  <files>test/cairnloop/demo_wrapper_contract_test.exs</files>
  <behavior>
    - Test bash syntax: `bash -n bin/demo` returns exit 0.
    - Test command surface: source includes `start|up`, `ps|status`, `help|-h|--help`, and help output advertises both `ps` and `status` per D-02.
    - Test route block: `print_urls` includes Demo index, Operator cockpit, Inbox, Customer chat, Knowledge Base, Gaps, Suggestions, Audit log, Settings, and Health labels with the D-09 paths.
    - Test URL discovery: source contains `compose port web "$CONTAINER_PORT"` and does not contain Docker demo assumptions for port 4000 in printed browser URLs.
    - Test Compose privacy: Compose source keeps `db` without a host `ports` block and `web` with `host_ip: "${CAIRNLOOP_BIND_HOST:-127.0.0.1}"`, `published: "${CAIRNLOOP_WEB_PORT:-4100-4199}"`, and `target: 4000`.
    - Test smoke isolation: source contains `_smoke`, `CAIRNLOOP_SMOKE_WEB_PORT`, the locked smoke routes, and `compose down -v --remove-orphans`.
    - Test diagnostics: source contains helper names or equivalent source strings proving compose-up, health timeout, and route failure diagnostics include failed boundary text and recent `compose logs --tail=80 web`.
  </behavior>
  <action>Create `test/cairnloop/demo_wrapper_contract_test.exs` as a DB-free ExUnit module named `Cairnloop.DemoWrapperContractTest`, using `ExUnit.Case, async: true`. Read `bin/demo` and `examples/cairnloop_example/compose.demo.yml` with `File.read!/1`; run `System.cmd("bash", ["-n", "bin/demo"], stderr_to_stdout: true)` for syntax. Use focused assertions and small helpers modeled on `Cairnloop.Web.CollateralWiringTest` and `Cairnloop.Web.BrandTokenGateTest`. Make the test intentionally RED against the current Phase 54 gaps: help must advertise `status`, delayed web-port/readiness code must be represented by a `wait_for_web_endpoint` helper or equivalent, and compose-up diagnostics must be represented by `run_compose_or_explain` or equivalent. Do not call Docker, start Phoenix, require Repo, or run `./bin/demo smoke` from this test file.</action>
  <verify>
    <automated>set +e; mix test test/cairnloop/demo_wrapper_contract_test.exs > /tmp/phase54-red.log 2>&1; code=$?; set -e; test "$code" -ne 0 && rg -n "status|wait_for_web_endpoint|run_compose_or_explain|Recent web logs" /tmp/phase54-red.log</automated>
  </verify>
  <acceptance_criteria>
    - `test/cairnloop/demo_wrapper_contract_test.exs` exists and defines `defmodule Cairnloop.DemoWrapperContractTest`.
    - The test file contains `use ExUnit.Case, async: true`.
    - The test file reads `bin/demo` and `examples/cairnloop_example/compose.demo.yml` directly.
    - The test file does not run Docker, `./bin/demo smoke`, Phoenix, Repo, or browser tooling.
    - The automated verify command passes by proving the new test goes RED for the known Phase 54 hardening gaps.
  </acceptance_criteria>
  <done>Wave 0 source/contract test coverage exists and fails for the wrapper gaps that Plan 54-02 must close.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the red coverage is scoped to Phase 54 contracts</name>
  <read_first>
    test/cairnloop/demo_wrapper_contract_test.exs
    .planning/phases/54-demo-wrapper-experience/54-VALIDATION.md
    .planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
  </read_first>
  <files>test/cairnloop/demo_wrapper_contract_test.exs</files>
  <action>Review the red test output and adjust only the test file if it fails for syntax mistakes, broad unrelated assertions, Docker availability, Repo availability, browser tooling, docs narrative, or UI/CSS expectations. Keep the failures tied to Phase 54 D-02, D-08, D-12, and D-13 wrapper hardening. Preserve source assertions for BOOT-01, BOOT-02, BOOT-03, BOOT-04, VER-01, and VER-02.</action>
  <verify>
    <automated>bash -n bin/demo && set +e; mix test test/cairnloop/demo_wrapper_contract_test.exs > /tmp/phase54-red-confirm.log 2>&1; code=$?; set -e; test "$code" -ne 0 && rg -n "status|wait_for_web_endpoint|run_compose_or_explain|Recent web logs" /tmp/phase54-red-confirm.log</automated>
  </verify>
  <acceptance_criteria>
    - RED failures mention only missing Phase 54 wrapper contract strings/behaviors, not missing Docker, missing Postgres, Phoenix boot, or UI/browser assertions.
    - `bash -n bin/demo` still passes.
    - The test file covers all Phase 54 requirement IDs listed in frontmatter.
  </acceptance_criteria>
  <done>The test suite is ready for Plan 54-02 to turn RED to GREEN without expanding Phase 54 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter shell -> `bin/demo` | User-controlled command/environment values enter the wrapper. |
| `bin/demo` -> Docker Compose | Wrapper translates commands into Docker Compose lifecycle operations. |
| Docker Compose -> host network | Compose publishes Phoenix to the host while keeping Postgres internal. |
| Docker Compose logs -> terminal | Container logs become adopter/maintainer diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Information Disclosure | `examples/cairnloop_example/compose.demo.yml` db service | mitigate | Contract test asserts `db` has no host `ports`; Plan 54-03 runs Compose config validation. |
| T-54-02 | Information Disclosure | Compose web port binding | mitigate | Contract test asserts loopback `host_ip` default and dynamic `CAIRNLOOP_WEB_PORT` range. |
| T-54-03 | Tampering/Denial of Service | smoke cleanup | mitigate | Contract test asserts `_smoke` project suffix and `compose down -v --remove-orphans`, never global Docker prune. |
| T-54-04 | Tampering | command and route strings | mitigate | Contract test asserts explicit command case branches and literal smoke route list; Plan 54-02 must quote variables. |
| T-54-05 | Information Disclosure | diagnostic output | mitigate | Contract test asserts recent web logs and failure boundary text without env dumps or inspect JSON. |
| T-54-06 | Elevation of Privilege | Docker access | accept | Docker access is the phase prerequisite; tests and plans forbid extra Docker socket/API side effects beyond normal Compose commands. |
| T-54-SC | Tampering | package installs | accept | No package-manager install tasks exist; Package Legitimacy Audit says none. |
</threat_model>

<verification>
Plan 54-01 intentionally stops at RED coverage. Automated proof is the RED confirmation command, not a green `mix test` run. Plan 54-02 must make `mix test test/cairnloop/demo_wrapper_contract_test.exs` pass.
</verification>

<success_criteria>
- Wave 0 test file exists and is DB-free.
- RED output identifies the planned wrapper hardening gaps.
- No Docker, Postgres, browser, UI, docs narrative, or package-install scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/54-demo-wrapper-experience/54-01-SUMMARY.md` when done.
</output>
