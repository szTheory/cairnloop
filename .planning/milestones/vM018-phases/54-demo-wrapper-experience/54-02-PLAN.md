---
phase: 54-demo-wrapper-experience
plan: "02"
type: execute
wave: 1
depends_on:
  - "54-01"
files_modified:
  - bin/demo
autonomous: true
requirements:
  - BOOT-03
  - BOOT-04
  - VER-02
user_setup: []
must_haves:
  truths:
    - "D-02/D-07: `./bin/demo help` advertises both `ps` and `status`, and `./bin/demo urls` uses the same route block as start."
    - "D-06/D-08: start and smoke wait for `/health` through a URL discovered from `docker compose port web 4000`, including delayed port discovery."
    - "D-12/D-13: compose-up, health timeout, and smoke route failures name the failing command/URL boundary and print recent web logs."
    - "D-14: `./bin/demo logs` continues to follow both web and db logs."
    - "D-15: the Wave 0 contract test from Plan 54-01 passes after wrapper hardening."
  artifacts:
    - path: "bin/demo"
      provides: "Hardened adopter-facing demo wrapper"
      exports: ["recent_web_logs", "fail_with_web_logs", "run_compose_or_explain", "wait_for_web_endpoint", "start_demo", "smoke_demo"]
    - path: "test/cairnloop/demo_wrapper_contract_test.exs"
      provides: "Plan 54-01 contract test consumed as the green gate"
      exports: ["Cairnloop.DemoWrapperContractTest"]
  key_links:
    - from: "bin/demo"
      to: "examples/cairnloop_example/compose.demo.yml"
      via: "`compose()` helper always uses `$COMPOSE_FILE`"
      pattern: "docker compose -f \"\\$COMPOSE_FILE\""
    - from: "bin/demo"
      to: "examples/cairnloop_example/lib/cairnloop_example_web/router.ex"
      via: "`print_urls` and `smoke_route` retain the mounted route list"
      pattern: "support/knowledge-base/suggestions"
    - from: "test/cairnloop/demo_wrapper_contract_test.exs"
      to: "bin/demo"
      via: "green source/contract assertions"
      pattern: "run_compose_or_explain"
---

<objective>
Harden `bin/demo` so the wrapper contracts from Plan 54-01 pass.

Purpose: make URLs, help, readiness, and failure diagnostics behave like an adopter-facing operational surface without changing the Phase 53 runtime contract.
Output: updated `bin/demo` with delayed-port readiness, shared failure-log helpers, compose-up diagnostics, and corrected help copy.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
@.planning/phases/54-demo-wrapper-experience/54-RESEARCH.md
@.planning/phases/54-demo-wrapper-experience/54-PATTERNS.md
@.planning/phases/54-demo-wrapper-experience/54-VALIDATION.md
@.planning/phases/54-demo-wrapper-experience/54-01-SUMMARY.md
@bin/demo
@test/cairnloop/demo_wrapper_contract_test.exs
@examples/cairnloop_example/compose.demo.yml
@examples/cairnloop_example/lib/cairnloop_example_web/router.ex
</context>

## Plan Assumptions

- Schema Push Detection Gate: no schema files are modified, so no schema push task is planned.
- Package Legitimacy Gate: no package installs.
- Keep all changes in `bin/demo`; do not edit Compose, Dockerfile, router, docs, UI, or CSS unless an automated gate proves the wrapper cannot satisfy the contract otherwise.

## Artifacts This Phase Produces

- Updated shell helpers in `bin/demo`: `recent_web_logs`, `fail_with_web_logs`, `run_compose_or_explain`, and `wait_for_web_endpoint`.
- Preserved shell helpers/functions in `bin/demo`: `default_project_name`, `compose`, `require_docker`, `web_endpoint`, `base_url`, `print_urls`, `wait_for_health`, `smoke_route`, `start_demo`, `smoke_demo`, `usage`.
- Preserved CLI subcommands/aliases: `start`, `up`, `urls`, `logs`, `status`, `ps`, `stop`, `down`, `reset`, `smoke`, `help`, `-h`, `--help`.
- Preserved environment variables: `CAIRNLOOP_WEB_PORT`, `CAIRNLOOP_SMOKE_WEB_PORT`, `CAIRNLOOP_BIND_HOST`, `CAIRNLOOP_COMPOSE_PROJECT`, `OPENAI_API_KEY`, `LOCAL_UID`, `LOCAL_GID`.

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | - | Make `./bin/demo` the adopter-facing operational surface for the local demo | 54-01, 54-02, 54-03 | COVERED | This plan owns the wrapper behavior changes.
REQ | BOOT-01 | Docker Compose v2 only local runtime prerequisite | 54-01, 54-03 | COVERED | This plan must not introduce local Elixir/Postgres checks.
REQ | BOOT-02 | Dynamic/configured localhost Phoenix port and private Compose Postgres | 54-01, 54-03 | COVERED | This plan must preserve Compose-derived URLs.
REQ | BOOT-03 | Exact route URLs after health | 54-01, 54-02, 54-03 | COVERED | This plan hardens discovery and readiness.
REQ | BOOT-04 | Wrapper subcommands for operational actions | 54-01, 54-02, 54-03 | COVERED | This plan fixes help drift and preserves aliases.
REQ | VER-01 | Isolated smoke stack and cleanup | 54-01, 54-03 | COVERED | This plan preserves smoke implementation while adding diagnostics.
REQ | VER-02 | Actionable failure output with route/logs | 54-01, 54-02 | COVERED | This plan owns diagnostic helpers.
RESEARCH | - | Use existing wrapper, shared diagnostics, delayed port handling | 54-02 | COVERED | Implemented here.
CONTEXT | D-01 | Single canonical `./bin/demo` | 54-02 | COVERED | No competing entry points.
CONTEXT | D-02 | Preserve command vocabulary | 54-02 | COVERED | Help and case aliases align.
CONTEXT | D-03 | stop/down/reset semantics | 54-02 | COVERED | Existing compose commands retained.
CONTEXT | D-04 | Postgres private | 54-03 | COVERED | No Compose DB edits in this plan.
CONTEXT | D-05 | Dynamic localhost Phoenix port | 54-02 | COVERED | Discovery remains Compose-based.
CONTEXT | D-06 | Use `docker compose port web 4000` | 54-02 | COVERED | Readiness and URLs derive from `web_endpoint`.
CONTEXT | D-07 | `urls` fail closed when web absent | 54-02 | COVERED | Keep `base_url` fail-closed for `urls`.
CONTEXT | D-08 | start/smoke wait for `/health` | 54-02 | COVERED | `wait_for_health` uses discovered URL.
CONTEXT | D-09 | Smoke locked route list | 54-02 | COVERED | Route list preserved.
CONTEXT | D-10 | Smoke isolated project | 54-02 | COVERED | `_smoke` suffix preserved.
CONTEXT | D-11 | Smoke port override | 54-02 | COVERED | `CAIRNLOOP_SMOKE_WEB_PORT` inheritance preserved.
CONTEXT | D-12 | Calm diagnostics | 54-02 | COVERED | Shared diagnostics added.
CONTEXT | D-13 | Web logs on health/route failure | 54-02 | COVERED | `fail_with_web_logs` used.
CONTEXT | D-14 | Logs for web and db | 54-02 | COVERED | `compose logs -f web db` retained.
CONTEXT | D-15 | Automated proof | 54-02 | COVERED | Contract test turns green.
CONTEXT | D-16 | Final verification commands | 54-03 | COVERED | Deferred to final plan.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden URL discovery, health wait, and help copy</name>
  <read_first>
    bin/demo
    test/cairnloop/demo_wrapper_contract_test.exs
    .planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
    .planning/phases/54-demo-wrapper-experience/54-PATTERNS.md
    .planning/phases/54-demo-wrapper-experience/54-VALIDATION.md
    .planning/phases/54-demo-wrapper-experience/54-01-SUMMARY.md
  </read_first>
  <files>bin/demo</files>
  <behavior>
    - `./bin/demo help` includes both `ps` and `status`.
    - `./bin/demo urls` still fails closed when `compose port web 4000` returns no endpoint.
    - `start_demo` and `smoke_demo` tolerate delayed `compose port web 4000` output before health polling.
    - Wildcard bind addresses from Compose output still normalize to `127.0.0.1`.
  </behavior>
  <action>Update `bin/demo` only. Keep `base_url` as the fail-closed path for `urls` per D-07. Add a separate `wait_for_web_endpoint` path for `start_demo` and `smoke_demo` so those flows retry `web_endpoint` before curling `/health`. If needed, extract endpoint parsing into a helper such as `url_from_endpoint` so `base_url` and `wait_for_health` share the same wildcard-host normalization. Update `usage` so the visible command list advertises `ps, status` or an equivalent line that names both aliases. Preserve `compose port web "$CONTAINER_PORT"`, the existing route block, `CAIRNLOOP_WEB_PORT="${CAIRNLOOP_WEB_PORT:-4100-4199}"`, and `CAIRNLOOP_SMOKE_WEB_PORT` behavior per D-05, D-06, and D-11.</action>
  <verify>
    <automated>bash -n bin/demo && ./bin/demo help && mix test test/cairnloop/demo_wrapper_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n bin/demo` exits 0.
    - `./bin/demo help` exits 0 and displays both `ps` and `status`.
    - `mix test test/cairnloop/demo_wrapper_contract_test.exs` no longer fails for help/status or delayed endpoint discovery assertions.
    - `base_url` retains the existing "web service is not running" fail-closed message for `urls`.
  </acceptance_criteria>
  <done>URL discovery and help copy satisfy BOOT-03 and BOOT-04 without changing Compose or route scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add calm Compose and smoke failure diagnostics</name>
  <read_first>
    bin/demo
    test/cairnloop/demo_wrapper_contract_test.exs
    .planning/phases/54-demo-wrapper-experience/54-CONTEXT.md
    .planning/phases/54-demo-wrapper-experience/54-RESEARCH.md
    .planning/phases/54-demo-wrapper-experience/54-PATTERNS.md
  </read_first>
  <files>bin/demo</files>
  <behavior>
    - `compose up -d --build` failures in `start_demo` and `smoke_demo` include the failed command boundary and recent web logs when available.
    - Health timeout failures include the readiness URL and recent web logs.
    - Smoke route failures include the full failing URL and recent web logs.
    - Diagnostics stay web-focused and do not dump raw env, Docker inspect JSON, or long unrelated Compose output.
  </behavior>
  <action>Add small Bash helpers in `bin/demo`: `recent_web_logs` to print the "Recent web logs:" header and `compose logs --tail=80 web`, `fail_with_web_logs` to print a caller-supplied calm error message plus recent web logs before exiting nonzero, and `run_compose_or_explain` to execute important Compose commands while naming the failed command on error. Use those helpers for `compose up -d --build` in `start_demo` and `smoke_demo`, for `/health` timeout in `wait_for_health`, and for route failure in `smoke_route`. Keep `./bin/demo logs` as `compose logs -f web db` per D-14. Quote URL and route variables in curl calls; keep route strings literal inside the script.</action>
  <verify>
    <automated>bash -n bin/demo && mix test test/cairnloop/demo_wrapper_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source contains helper names `recent_web_logs`, `fail_with_web_logs`, and `run_compose_or_explain` or equivalent helper names already asserted by the Wave 0 test.
    - `compose up -d --build` is no longer a bare unchecked command in `start_demo` or `smoke_demo`.
    - Health and route failures use the same recent-web-log helper.
    - The contract test passes.
  </acceptance_criteria>
  <done>VER-02 failure diagnostics are implemented and pinned by the DB-free contract test.</done>
</task>

<task type="auto">
  <name>Task 3: Run the fast wrapper and Compose contract gate</name>
  <read_first>
    bin/demo
    test/cairnloop/demo_wrapper_contract_test.exs
    examples/cairnloop_example/compose.demo.yml
    .planning/phases/54-demo-wrapper-experience/54-VALIDATION.md
  </read_first>
  <files>bin/demo</files>
  <action>Run the fast validation commands after the wrapper edits. If a command fails, fix only `bin/demo` or the new contract test unless the failure proves an existing Phase 53 contract file is wrong. Do not broaden into docs, UI, CI, router, Dockerfile, or package changes.</action>
  <verify>
    <automated>bash -n bin/demo && ./bin/demo help && mix test test/cairnloop/demo_wrapper_contract_test.exs && docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - Shell syntax, help, contract test, and Compose config all pass.
    - `examples/cairnloop_example/compose.demo.yml` remains unmodified unless the executor records a concrete Compose validation defect.
    - No package install, schema push, UI, docs narrative, or CI workflow change is introduced by this plan.
  </acceptance_criteria>
  <done>Wrapper hardening is green under the fast Phase 54 validation gate and ready for full smoke in Plan 54-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter shell -> `bin/demo` | Command names and environment variables influence wrapper behavior. |
| `bin/demo` -> Docker Compose | Wrapper invokes Compose for lifecycle, logs, status, and port discovery. |
| Compose port output -> browser URL | Wrapper converts Compose output into user-facing URLs. |
| web logs -> terminal | Recent web logs are exposed as diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Information Disclosure | Compose DB service | transfer | Plan 54-03 validates Compose; this plan must not edit DB port exposure. |
| T-54-02 | Information Disclosure | URL discovery and host binding | mitigate | Keep discovered URL normalization separate from binding defaults; do not change `CAIRNLOOP_BIND_HOST` default. |
| T-54-03 | Tampering/Denial of Service | `reset` and smoke cleanup | mitigate | Preserve Compose-project scoped `down` commands; no global Docker cleanup. |
| T-54-04 | Tampering | route and command variables | mitigate | Use explicit `case`, literal smoke routes, quoted curl arguments, and no eval. |
| T-54-05 | Information Disclosure | diagnostics | mitigate | Print failing boundary plus `compose logs --tail=80 web`; do not dump env, inspect JSON, or verbose curl traces. |
| T-54-06 | Elevation of Privilege | Docker operations | accept | Docker Compose is the required local runtime; wrapper stays on normal `docker compose -f "$COMPOSE_FILE"` operations. |
| T-54-SC | Tampering | package installs | accept | No package-manager install tasks exist. |
</threat_model>

<verification>
Fast gate: `bash -n bin/demo && ./bin/demo help && mix test test/cairnloop/demo_wrapper_contract_test.exs && docker compose -f examples/cairnloop_example/compose.demo.yml config --quiet`.
</verification>

<success_criteria>
- Plan 54-01 contract test is green.
- `bin/demo` help, URL discovery, readiness, smoke route diagnostics, and logs behavior satisfy D-02, D-06, D-07, D-08, D-12, D-13, and D-14.
- No changes are made outside `bin/demo` by this plan.
</success_criteria>

<output>
Create `.planning/phases/54-demo-wrapper-experience/54-02-SUMMARY.md` when done.
</output>
