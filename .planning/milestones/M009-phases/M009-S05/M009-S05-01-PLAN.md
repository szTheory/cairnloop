---
phase: M009-S05
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/router.ex
  - lib/cairnloop/web/inbox_live.ex
  - lib/cairnloop/web/settings_live.ex
  - lib/cairnloop/web/conversation_live.ex
  - lib/cairnloop/web/search_modal_component.ex
  - lib/cairnloop/retrieval.ex
  - lib/cairnloop/retrieval/providers/knowledge_base.ex
  - lib/cairnloop/retrieval/providers/resolved_cases.ex
  - test/cairnloop/web/inbox_live_test.exs
  - test/cairnloop/web/settings_live_test.exs
  - test/cairnloop/web/conversation_live_test.exs
  - test/cairnloop/web/search_modal_component_test.exs
  - test/cairnloop/retrieval_test.exs
autonomous: true
requirements:
  - M009-REQ-04
  - M009-REQ-05
must_haves:
  truths:
    - "Non-conversation search surfaces either provide real scope context or fail closed before returning partially filtered results."
    - "Provider filtering and visibility enforcement happen before `Ranker.merge/3` consumes candidates."
    - "Search keeps the existing `Knowledge Base` versus `Similar resolved cases` trust contract while adding explicit scoped-unavailable behavior."
  artifacts:
    - path: "lib/cairnloop/web/search_modal_component.ex"
      provides: "Scoped retrieval boundary and fail-closed operator-search state"
    - path: "lib/cairnloop/router.ex"
      provides: "Dashboard live-session seam that defines the `host_user_id` session contract for non-conversation surfaces"
    - path: "lib/cairnloop/retrieval/providers/resolved_cases.ex"
      provides: "Tenant-filtered resolved-case candidate queries"
    - path: "test/cairnloop/retrieval_test.exs"
      provides: "Proof that filtering happens before ranking and result merging"
  key_links:
    - from: "lib/cairnloop/web/inbox_live.ex"
      to: "lib/cairnloop/web/search_modal_component.ex"
      via: "mount passes explicit scope contract or renders fail-closed search state"
      pattern: "host_user_id|SearchModalComponent"
    - from: "lib/cairnloop/retrieval.ex"
      to: "lib/cairnloop/retrieval/providers/resolved_cases.ex"
      via: "search opts carry scope into provider queries before ranking"
      pattern: "search_resolved_cases|host_user_id"
---

<objective>
Close the live runtime search-safety gap for M009 Phase 5.

Purpose: make every mounted search surface explicitly scoped, keep retrieval server-owned, and ensure provider filtering happens before ranking or presentation.
Output: fail-closed non-conversation mounts, retrieval/provider enforcement, and targeted tests that prove the remaining operator-search blocker is closed in code.

Scope note: this plan touches 13 files because the mount contract, retrieval guard, provider enforcement, and proof tests form one tightly coupled runtime slice. Splitting the test work away would weaken the fail-closed guarantee by separating the safety contract from the evidence that it actually holds.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M009-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/vM009-vM009-MILESTONE-AUDIT.md
@.planning/milestones/M009-phases/M009-S05/M009-S05-RESEARCH.md
@.planning/milestones/M009-phases/M009-S05/M009-S05-PATTERNS.md
@.planning/milestones/M009-phases/M009-S05/M009-S05-VALIDATION.md
@.planning/milestones/M009-phases/M009-S02-CONTEXT.md
@.planning/milestones/M009-phases/M009-S02/M009-S02-VALIDATION.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Make every search mount explicitly scoped or fail closed</name>
  <files>
    - lib/cairnloop/router.ex
    - lib/cairnloop/web/inbox_live.ex
    - lib/cairnloop/web/settings_live.ex
    - lib/cairnloop/web/conversation_live.ex
    - lib/cairnloop/web/search_modal_component.ex
  </files>
  <read_first>
    - lib/cairnloop/router.ex
    - lib/cairnloop/web/inbox_live.ex
    - lib/cairnloop/web/settings_live.ex
    - lib/cairnloop/web/conversation_live.ex
    - lib/cairnloop/web/search_modal_component.ex
    - .planning/milestones/M009-phases/M009-S05/M009-S05-RESEARCH.md
    - .planning/milestones/M009-phases/M009-S05/M009-S05-PATTERNS.md
  </read_first>
  <action>
    Add an explicit scope contract to every LiveView that mounts `SearchModalComponent`. `ConversationLive` already passes `host_user_id` and should remain the reference path. For `InboxLive` and `SettingsLive`, use the LiveView session payload as the exact non-conversation scope source: read `session["host_user_id"]` in `mount/3`, store it on the socket, and pass it into `SearchModalComponent`. Update `lib/cairnloop/router.ex` or its dashboard contract comments so the `cairnloop_dashboard` live session clearly documents that host apps must supply `host_user_id` in session for scoped operator search on non-conversation surfaces.

    If `session["host_user_id"]` is absent, do not perform a partially filtered retrieval request; instead, render a calm fail-closed search state that explains scoped search is unavailable on that surface until operator scope is present. This keeps the implementation executable inside the current library boundary while making the host-app integration contract explicit rather than assumed.

    Keep the shared component contract narrow: `host_surface`, `host_user_id`, `current_path`, and `preserve_reply_form`. Do not fork the search component per surface. Preserve conversation draft/reply safety while adding the new fail-closed branch.
  </action>
  <acceptance_criteria>
    - `lib/cairnloop/web/inbox_live.ex` and `lib/cairnloop/web/settings_live.ex` read `session["host_user_id"]` in `mount/3` and pass it into `SearchModalComponent`
    - `lib/cairnloop/router.ex` documents or encodes the non-conversation `host_user_id` live-session contract for the dashboard routes
    - `lib/cairnloop/web/search_modal_component.ex` contains a fail-closed branch for missing required scope rather than falling through to unscoped retrieval
    - `lib/cairnloop/web/conversation_live.ex` still passes `host_user_id={@conversation.host_user_id}`
    - the search component still renders `Knowledge Base` and `Similar resolved cases` states without introducing per-surface forks
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/web/inbox_live_test.exs test/cairnloop/web/settings_live_test.exs test/cairnloop/web/conversation_live_test.exs test/cairnloop/web/search_modal_component_test.exs</automated>
  </verify>
  <done>
    Inbox, Settings, and conversation all expose one explicit search-scope contract, and non-conversation surfaces either pass `session["host_user_id"]` or render a fail-closed scoped-unavailable state proven by the targeted LiveView/component tests.
  </done>
</task>

<task type="execute">
  <name>Task 2: Enforce provider-specific filtering before ranking</name>
  <files>
    - lib/cairnloop/retrieval.ex
    - lib/cairnloop/retrieval/providers/knowledge_base.ex
    - lib/cairnloop/retrieval/providers/resolved_cases.ex
    - test/cairnloop/retrieval_test.exs
  </files>
  <read_first>
    - lib/cairnloop/retrieval.ex
    - lib/cairnloop/retrieval/providers/knowledge_base.ex
    - lib/cairnloop/retrieval/providers/resolved_cases.ex
    - lib/cairnloop/retrieval/ranker.ex
    - test/cairnloop/retrieval_test.exs
    - .planning/milestones/M009-phases/M009-S05/M009-S05-RESEARCH.md
  </read_first>
  <action>
    Make the filtering contract explicit at the retrieval boundary and in both providers before `Ranker.merge/3` runs. Preserve the current schema-aware distinction:
    - `ResolvedCases` must keep enforcing `host_user_id` tenant scope on keyword and semantic paths.
    - `KnowledgeBase` should keep published-only visibility enforcement and should not gain an invented tenant predicate without a backing column.

    If the retrieval boundary receives a search request from a surface that requires tenant scope but lacks `host_user_id`, handle that as a bounded fail-closed condition before unsafe candidates can reach the ranker. Preserve existing trust/source metadata and do not move filtering into presenters or UI helpers.
  </action>
  <acceptance_criteria>
    - `lib/cairnloop/retrieval/providers/resolved_cases.ex` still filters both candidate paths by `host_user_id`
    - `lib/cairnloop/retrieval/providers/knowledge_base.ex` preserves published-only visibility enforcement and removes or documents the misleading no-op host-user filter seam
    - `lib/cairnloop/retrieval.ex` contains an explicit guard or helper that prevents unsafe unscoped search requests from reaching ranking on surfaces that require scope
    - `test/cairnloop/retrieval_test.exs` proves the ranker never merges candidates from an unsafe unscoped path
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/retrieval_test.exs</automated>
  </verify>
  <done>
    Retrieval rejects or bounds unsafe unscoped search requests before ranking, `ResolvedCases` keeps tenant filtering on every candidate path, and `KnowledgeBase` preserves only schema-backed visibility enforcement as proven by the retrieval test slice.
  </done>
</task>

<task type="execute">
  <name>Task 3: Backfill runtime closure tests for scoped, unscoped, and trust-state behavior</name>
  <files>
    - test/cairnloop/web/search_modal_component_test.exs
    - test/cairnloop/web/inbox_live_test.exs
    - test/cairnloop/web/settings_live_test.exs
    - test/cairnloop/web/conversation_live_test.exs
    - test/cairnloop/retrieval_test.exs
  </files>
  <read_first>
    - test/cairnloop/web/search_modal_component_test.exs
    - test/cairnloop/web/inbox_live_test.exs
    - test/cairnloop/web/settings_live_test.exs
    - test/cairnloop/web/conversation_live_test.exs
    - test/cairnloop/retrieval_test.exs
    - lib/cairnloop/web/search_modal_component.ex
  </read_first>
  <action>
    Extend the existing test slice so it proves the actual phase goal instead of only the happy-path conversation search. Add assertions for:
    - Inbox and Settings mounting the shared search component with explicit scope semantics
    - scoped-unavailable behavior when a non-conversation surface lacks `host_user_id`
    - conversation search still passing `host_user_id`
    - retrieval/provider behavior that enforces filtering before ranking
    - trust-language continuity so canonical `Knowledge Base` and assistive `Similar resolved cases` cues still render correctly after the fail-closed changes

    Keep the tests host-local and retrieval-stubbed where appropriate. Do not introduce remote dependencies.
  </action>
  <acceptance_criteria>
    - `test/cairnloop/web/search_modal_component_test.exs` contains assertions for a scoped-unavailable path in addition to no-hit and retrieval-error
    - `test/cairnloop/web/inbox_live_test.exs` and `test/cairnloop/web/settings_live_test.exs` assert explicit scope semantics, not only `data-host-surface`
    - `test/cairnloop/retrieval_test.exs` contains at least one assertion proving unsafe unscoped search is rejected or filtered before merge
    - the targeted test slice exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/web/search_modal_component_test.exs test/cairnloop/web/inbox_live_test.exs test/cairnloop/web/settings_live_test.exs test/cairnloop/web/conversation_live_test.exs test/cairnloop/retrieval_test.exs</automated>
  </verify>
  <done>
    The runtime closure regression slice covers scoped, unscoped, and trust-state behavior across component, mount, conversation-safety, and retrieval-boundary tests, and the targeted suite passes without remote dependencies.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView mount -> shared search component | Surface scope must be explicit before any retrieval request is allowed |
| Retrieval facade -> providers -> ranker | Filtering must complete before ranking or result merging |
| Search UI state -> operator trust | Scoped-unavailable, no-hit, and retrieval-error states must remain distinct and non-misleading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M009-S05-01 | Information Disclosure | Non-conversation search mounts | mitigate | Require explicit scope or fail closed before any resolved-case retrieval path runs |
| T-M009-S05-02 | Tampering | Provider/ranker contract | mitigate | Keep visibility and tenant filters in provider queries before `Ranker.merge/3` |
| T-M009-S05-03 | Spoofing | Search trust-state copy | mitigate | Distinguish scoped-unavailable, no-hit, and retrieval-error states without blurring canonical vs assistive cues |
</threat_model>

<verification>
- `mix test test/cairnloop/web/search_modal_component_test.exs test/cairnloop/web/inbox_live_test.exs test/cairnloop/web/settings_live_test.exs test/cairnloop/web/conversation_live_test.exs test/cairnloop/retrieval_test.exs`
- `rg -n 'host_user_id|scope|scoped|Knowledge Base|Similar resolved cases|published|Ranker' lib/cairnloop test/cairnloop`
</verification>

<success_criteria>
- Inbox, Settings, and conversation surfaces all carry explicit scope semantics into the shared search boundary.
- Unsafe unscoped search requests cannot reach result ranking on surfaces that require tenant filtering.
- Existing operator trust cues remain intact while the remaining search-safety blocker is closed in code.
</success_criteria>

<output>
After completion, create `.planning/milestones/M009-phases/M009-S05/M009-S05-01-SUMMARY.md`
</output>
