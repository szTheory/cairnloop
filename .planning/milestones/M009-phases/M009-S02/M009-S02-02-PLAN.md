---
phase: M009-S02
plan: "02"
type: execute
wave: 2
depends_on:
  - M009-S02-01
files_modified:
  - lib/cairnloop/web/search_modal_component.ex
  - lib/cairnloop/web/inbox_live.ex
  - lib/cairnloop/web/conversation_live.ex
  - lib/cairnloop/web/settings_live.ex
  - test/cairnloop/web/search_modal_component_test.exs
  - test/cairnloop/web/inbox_live_test.exs
  - test/cairnloop/web/conversation_live_test.exs
  - test/cairnloop/web/settings_live_test.exs
autonomous: true
requirements:
  - M009-REQ-04
  - M009-REQ-05
must_haves:
  truths:
    - "The palette previews on focus and opens on confirm without route churn during selection."
    - "Keyboard and mouse share one active-row state machine, and result movement does not trigger new retrieval requests."
    - "Search mounts safely across Inbox, Conversation, and Settings without discarding conversation draft state."
  artifacts:
    - path: "lib/cairnloop/web/search_modal_component.ex"
      provides: "Preview/open keyboard contract and source-aware destination handling"
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "Conversation-safe search mount behavior"
    - path: "test/cairnloop/web/settings_live_test.exs"
      provides: "Host-surface coverage for the third search mount"
  key_links:
    - from: "lib/cairnloop/web/search_modal_component.ex"
      to: "lib/cairnloop/web/conversation_live.ex"
      via: "palette open/close behavior must not disturb conversation assigns"
      pattern: "SearchModalComponent"
    - from: "lib/cairnloop/web/search_modal_component.ex"
      to: "lib/cairnloop/web/inbox_live.ex"
      via: "shared palette mount contract"
      pattern: "live_component module=\\{Cairnloop\\.Web\\.SearchModalComponent\\}"
---

<objective>
Finish the operator search interaction model for M009 Phase 2.

Purpose: add preview-on-focus, source-aware open actions, bounded keyboard behavior, and host-surface verification on top of the retrieval-backed palette foundation from Plan 01.
Output: one active-row state machine, explicit open behavior, safe host integration, and multi-surface LiveView coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M009-ROADMAP.md
@.planning/milestones/M009-phases/M009-S02-CONTEXT.md
@.planning/milestones/M009-phases/M009-S02/M009-S02-RESEARCH.md
@.planning/milestones/M009-phases/M009-S02/M009-S02-PATTERNS.md
@.planning/milestones/M009-phases/M009-S02/M009-S02-VALIDATION.md
@.planning/milestones/M009-phases/M009-S02/M009-S02-UI-SPEC.md
@.planning/milestones/M009-phases/M009-S02/M009-S02-01-PLAN.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Implement preview-on-focus and explicit open actions</name>
  <files>
    - lib/cairnloop/web/search_modal_component.ex
  </files>
  <read_first>
    - lib/cairnloop/web/search_modal_component.ex
    - lib/cairnloop/web/search_result_presenter.ex
    - lib/cairnloop/retrieval/result.ex
    - .planning/milestones/M009-phases/M009-S02-CONTEXT.md
    - .planning/milestones/M009-phases/M009-S02/M009-S02-UI-SPEC.md
  </read_first>
  <action>
    Extend the palette state machine so the active row updates an inline preview pane without navigation. Add explicit events for:
    - moving active selection up/down locally
    - hovering/clicking a row while keeping the same active-row contract
    - opening the active result on `Enter`
    - optionally opening in a new tab/window on `Cmd/Ctrl+Enter` when the destination supports it
    - clearing query on first `Escape` and closing on the second

    Use explicit destination data from Plan 01 rather than building routes from guessed IDs. Knowledge Base preview should show canonical article context and action copy `Open article`. Resolved-case preview should show structured evidence blocks in this order: `Issue summary`, `Resolution note`, `Actions taken`, `Outcome`, then citation/backreference metadata, with action copy `Open resolved case`.
  </action>
  <acceptance_criteria>
    - `lib/cairnloop/web/search_modal_component.ex` contains `ArrowDown` and `ArrowUp` handling or equivalent active-row events
    - the component contains `Open article` and `Open resolved case`
    - the component contains `Issue summary`, `Resolution note`, `Actions taken`, and `Outcome`
    - the component contains an `Escape` behavior path that distinguishes clear vs close
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'ArrowDown|ArrowUp|Open article|Open resolved case|Issue summary|Resolution note|Actions taken|Outcome|Escape' lib/cairnloop/web/search_modal_component.ex</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Tighten keyboard handling and host-surface mounts</name>
  <files>
    - lib/cairnloop/web/search_modal_component.ex
    - lib/cairnloop/web/inbox_live.ex
    - lib/cairnloop/web/conversation_live.ex
    - lib/cairnloop/web/settings_live.ex
  </files>
  <read_first>
    - lib/cairnloop/web/search_modal_component.ex
    - lib/cairnloop/web/inbox_live.ex
    - lib/cairnloop/web/conversation_live.ex
    - lib/cairnloop/web/settings_live.ex
    - README.md
    - .planning/milestones/M009-phases/M009-S02/M009-S02-RESEARCH.md
  </read_first>
  <action>
    Replace the current unfiltered global shortcut behavior with a bounded event contract that avoids noisy server traffic and accidental capture. If the repo still has no host-app JS bootstrap, use the safest LiveView-native fallback: filter the global shortcut so only `cmd/ctrl+k` reaches the server and ordinary typing elsewhere is ignored. If a bootstrap point exists by execution time, introduce a minimal hook there instead.

    Update the Inbox, Conversation, and Settings mounts only as needed to pass host-specific context into the shared search component, such as `host_user_id`, current route/destination helpers, or a state-preservation hint. In `ConversationLive`, keep the reply/draft form assigns intact while the palette opens, previews, and closes.
  </action>
  <acceptance_criteria>
    - `lib/cairnloop/web/search_modal_component.ex` does not contain the original unfiltered `phx-window-keydown=\"toggle_search\"` contract without any narrowing logic
    - `lib/cairnloop/web/inbox_live.ex`, `conversation_live.ex`, and `settings_live.ex` still mount `Cairnloop.Web.SearchModalComponent`
    - `lib/cairnloop/web/conversation_live.ex` does not lose the existing reply-form assign path
    - at least one host LiveView passes context into the search component beyond the bare `id=\"search-modal\"` mount
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'SearchModalComponent|host_user_id|toggle_search|phx-window-keydown|reply-form|live_component module=\\{Cairnloop\\.Web\\.SearchModalComponent\\}' lib/cairnloop/web/search_modal_component.ex lib/cairnloop/web/inbox_live.ex lib/cairnloop/web/conversation_live.ex lib/cairnloop/web/settings_live.ex</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 3: Add multi-surface tests for preview, open, and state safety</name>
  <files>
    - test/cairnloop/web/search_modal_component_test.exs
    - test/cairnloop/web/inbox_live_test.exs
    - test/cairnloop/web/conversation_live_test.exs
    - test/cairnloop/web/settings_live_test.exs
  </files>
  <read_first>
    - test/cairnloop/web/search_modal_component_test.exs
    - test/cairnloop/web/inbox_live_test.exs
    - test/cairnloop/web/conversation_live_test.exs
    - lib/cairnloop/web/search_modal_component.ex
    - lib/cairnloop/web/conversation_live.ex
  </read_first>
  <action>
    Add test coverage for:
    - active-row movement updates preview locally without triggering a fresh search request
    - `Enter` opens the active result destination while focus movement alone does not navigate
    - `Escape` clears then closes
    - Inbox and Settings still mount the shared palette
    - Conversation search interactions do not reset draft/reply form state

    Add `test/cairnloop/web/settings_live_test.exs` if no settings coverage exists yet. Keep the tests retrieval-stubbed and host-local; do not depend on remote services.
  </action>
  <acceptance_criteria>
    - `test/cairnloop/web/settings_live_test.exs` exists
    - `test/cairnloop/web/search_modal_component_test.exs` contains assertions for `Enter` and `Escape`
    - `test/cairnloop/web/conversation_live_test.exs` contains at least one assertion path covering preserved reply or draft form state during search interaction
    - `mix test test/cairnloop/web/search_modal_component_test.exs test/cairnloop/web/inbox_live_test.exs test/cairnloop/web/conversation_live_test.exs test/cairnloop/web/settings_live_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/web/search_modal_component_test.exs test/cairnloop/web/inbox_live_test.exs test/cairnloop/web/conversation_live_test.exs test/cairnloop/web/settings_live_test.exs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard shortcut -> shared dashboard LiveViews | Shortcut capture must stay narrow and must not interfere with unrelated typing |
| Preview state -> navigation state | Focus movement must not silently route away or discard conversation work |
| Assistive evidence preview -> canonical article preview | Different source types must remain distinct in both content blocks and action copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M009-S02-03 | Denial of Service | Global shortcut/event flow | mitigate | Narrow the open shortcut contract so ordinary typing does not create noisy server traffic |
| T-M009-S02-04 | Tampering | Navigation + preview behavior | mitigate | Use explicit destinations and preview-only focus changes so rows cannot navigate unexpectedly or blur source semantics |
</threat_model>

<verification>
- `mix test test/cairnloop/web/search_modal_component_test.exs test/cairnloop/web/inbox_live_test.exs test/cairnloop/web/conversation_live_test.exs test/cairnloop/web/settings_live_test.exs`
- `rg -n 'Open article|Open resolved case|Issue summary|Resolution note|Actions taken|Outcome|Enter|Escape|SearchModalComponent' lib/cairnloop/web/search_modal_component.ex test/cairnloop/web/search_modal_component_test.exs test/cairnloop/web/conversation_live_test.exs`
</verification>

<success_criteria>
- The palette previews on focus and opens only on explicit confirmation.
- Keyboard and mouse interactions share one active-row model without extra retrieval round-trips.
- Search remains safe across Inbox, Conversation, and Settings, including conversation draft/reply state.
</success_criteria>

<output>
After completion, create `.planning/milestones/M009-phases/M009-S02/M009-S02-02-SUMMARY.md`
</output>
