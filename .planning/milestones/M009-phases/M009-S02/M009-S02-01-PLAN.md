---
phase: M009-S02
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/web/search_modal_component.ex
  - lib/cairnloop/web/search_result_presenter.ex
  - lib/cairnloop/retrieval/result.ex
  - lib/cairnloop/retrieval/providers/knowledge_base.ex
  - lib/cairnloop/retrieval/providers/resolved_cases.ex
  - test/cairnloop/web/search_modal_component_test.exs
autonomous: true
requirements:
  - M009-REQ-04
  - M009-REQ-05
must_haves:
  truths:
    - "Operator search uses `Cairnloop.Retrieval` instead of direct remote HTTP calls from the UI."
    - "Results render in fixed `Knowledge Base` and `Similar resolved cases` sections while preserving backend ranking within each section."
    - "Each row has explicit source/trust cues plus enough metadata for preview and open actions without route guessing."
  artifacts:
    - path: "lib/cairnloop/web/search_modal_component.ex"
      provides: "Retrieval-backed palette shell with sectioned result state"
    - path: "lib/cairnloop/web/search_result_presenter.ex"
      provides: "UI presenter over `Cairnloop.Retrieval.Result`"
    - path: "test/cairnloop/web/search_modal_component_test.exs"
      provides: "Real interaction coverage for the search palette"
  key_links:
    - from: "lib/cairnloop/web/search_modal_component.ex"
      to: "lib/cairnloop/retrieval.ex"
      via: "search event uses retrieval facade"
      pattern: "Cairnloop\\.Retrieval\\.search"
    - from: "lib/cairnloop/web/search_modal_component.ex"
      to: "lib/cairnloop/web/search_result_presenter.ex"
      via: "render path formats result metadata through presenter"
      pattern: "SearchResultPresenter"
---

<objective>
Build the retrieval-backed foundation of the operator search palette for M009 Phase 2.

Purpose: replace the old direct-HTTP modal with a shared palette shell that queries `Cairnloop.Retrieval`, groups results by source, and renders trust-aware rows from an explicit presenter contract.
Output: retrieval metadata enrichment, one presenter layer, a sectioned palette shell, and non-placeholder component tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M009-ROADMAP.md
@.planning/milestones/M009-phases/M009-S02-CONTEXT.md
@.planning/milestones/M009-phases/M009-S02/M009-S02-RESEARCH.md
@.planning/milestones/M009-phases/M009-S02/M009-S02-PATTERNS.md
@.planning/milestones/M009-phases/M009-S02/M009-S02-VALIDATION.md
@.planning/milestones/M009-phases/M009-S02/M009-S02-UI-SPEC.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Enrich retrieval results for explicit palette destinations and trust cues</name>
  <files>
    - lib/cairnloop/retrieval/result.ex
    - lib/cairnloop/retrieval/providers/knowledge_base.ex
    - lib/cairnloop/retrieval/providers/resolved_cases.ex
  </files>
  <read_first>
    - lib/cairnloop/retrieval/result.ex
    - lib/cairnloop/retrieval/providers/knowledge_base.ex
    - lib/cairnloop/retrieval/providers/resolved_cases.ex
    - lib/cairnloop/knowledge_base/article.ex
    - lib/cairnloop/knowledge_base/revision.ex
    - lib/cairnloop/conversation.ex
    - .planning/milestones/M009-phases/M009-S02-CONTEXT.md
    - .planning/milestones/M009-phases/M009-S02/M009-S02-UI-SPEC.md
  </read_first>
  <action>
    Extend the retrieval result contract so the UI can open and preview results without guessing routes from loose IDs. Add concrete metadata fields or nested metadata keys for:
    - Knowledge Base destination data: `article_id`, `revision_id`, `chunk_index`, and a timestamp or recency source
    - Resolved-case destination data: `conversation_id`, `chunk_index`, `resolved_at`, `issue_summary`, `resolution_note`, `actions_taken`, and `outcome`
    - A stable action label or destination tuple the presenter can turn into `Open article` or `Open resolved case`

    Update `Cairnloop.Retrieval.Providers.KnowledgeBase` to join whatever source tables are needed so KB rows expose article/revision identifiers and recency metadata alongside existing trust fields. Update `Cairnloop.Retrieval.Providers.ResolvedCases` so resolved-case rows expose preview-safe structured evidence and `resolved_at`.

    Preserve current provider ranking order and trust semantics. Do not move ranking or filtering logic into the UI.
  </action>
  <acceptance_criteria>
    - `lib/cairnloop/retrieval/result.ex` contains either `:metadata` keys or direct fields for `article_id` and `resolved_at`
    - `lib/cairnloop/retrieval/providers/knowledge_base.ex` contains `article_id` in its selected payload
    - `lib/cairnloop/retrieval/providers/resolved_cases.ex` contains at least one of `issue_summary`, `resolution_note`, `actions_taken`, or `outcome` in its selected payload
    - Both provider files still contain `source_type` and `trust_level`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'article_id|resolved_at|issue_summary|resolution_note|actions_taken|outcome|source_type|trust_level' lib/cairnloop/retrieval/result.ex lib/cairnloop/retrieval/providers/knowledge_base.ex lib/cairnloop/retrieval/providers/resolved_cases.ex</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Replace the old modal with a retrieval-backed, sectioned palette shell</name>
  <files>
    - lib/cairnloop/web/search_modal_component.ex
    - lib/cairnloop/web/search_result_presenter.ex
  </files>
  <read_first>
    - lib/cairnloop/web/search_modal_component.ex
    - lib/cairnloop/retrieval.ex
    - lib/cairnloop/retrieval/result.ex
    - .planning/milestones/M009-phases/M009-S02/M009-S02-UI-SPEC.md
    - .planning/milestones/M009-phases/M009-S02-CONTEXT.md
  </read_first>
  <action>
    Rebuild `Cairnloop.Web.SearchModalComponent` around the M009-S02 UI contract. Remove the direct `Req.post` search path and call `Cairnloop.Retrieval.search(query, opts)` instead, using a minimum query length of `2` and a debounce of `250ms`. Track palette assigns for open/closed state, query, loading/error state, active result identity, grouped `knowledge_base` and `resolved_case` sections, and a neutral preview state.

    Add `Cairnloop.Web.SearchResultPresenter` as a thin presenter over `Cairnloop.Retrieval.Result`. It should derive:
    - section name (`Knowledge Base` or `Similar resolved cases`)
    - source label + trust label
    - row snippet and preview copy
    - recency string (`Updated ...` or `Resolved ...`)
    - explicit open action label (`Open article` or `Open resolved case`)

    Render the two-pane desktop shell and stacked mobile shell from the UI spec, including section-empty states, neutral preview state, and fixed section order. Do not re-rank results client-side.
  </action>
  <acceptance_criteria>
    - `lib/cairnloop/web/search_modal_component.ex` contains `Cairnloop.Retrieval.search`
    - `lib/cairnloop/web/search_modal_component.ex` no longer contains `Req.post`
    - `lib/cairnloop/web/search_modal_component.ex` contains `Knowledge Base` and `Similar resolved cases`
    - `lib/cairnloop/web/search_result_presenter.ex` defines `defmodule Cairnloop.Web.SearchResultPresenter`
    - `lib/cairnloop/web/search_modal_component.ex` contains a query length guard of `2`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'Cairnloop\\.Retrieval\\.search|Req\\.post|Knowledge Base|Similar resolved cases|byte_size\\(query\\) < 2|SearchResultPresenter' lib/cairnloop/web/search_modal_component.ex lib/cairnloop/web/search_result_presenter.ex</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 3: Replace placeholder search tests with retrieval-backed component coverage</name>
  <files>
    - test/cairnloop/web/search_modal_component_test.exs
  </files>
  <read_first>
    - test/cairnloop/web/search_modal_component_test.exs
    - test/cairnloop/web/knowledge_base_live_test.exs
    - lib/cairnloop/web/search_modal_component.ex
    - lib/cairnloop/web/search_result_presenter.ex
  </read_first>
  <action>
    Rewrite `test/cairnloop/web/search_modal_component_test.exs` so it no longer contains the placeholder `assert true`. Add component-oriented coverage for:
    - closed by default and opens through the keyboard toggle contract
    - queries shorter than 2 characters keep results empty
    - retrieval-backed searches render both fixed sections in the correct order
    - source/trust labels render distinctly for KB and resolved-case rows
    - retrieval errors surface the non-destructive error state

    Stub or inject retrieval results through a test seam instead of hitting external HTTP.
  </action>
  <acceptance_criteria>
    - `test/cairnloop/web/search_modal_component_test.exs` does not contain `assert true`
    - the test file contains `Knowledge Base` and `Similar resolved cases`
    - the test file contains assertions referencing both `Canonical guidance` and `Supporting evidence`
    - the test file exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/web/search_modal_component_test.exs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retrieval facade -> operator palette | Search UI must consume trusted server-owned ranking and metadata |
| Canonical KB hits -> assistive resolved-case hits | UI must preserve truth hierarchy in labels, section order, and preview copy |
| Keyboard shortcut -> active host LiveView | Global open behavior must not capture typing broadly or disrupt unrelated state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M009-S02-01 | Information Disclosure | Search query path | mitigate | Route search through `Cairnloop.Retrieval` so tenant/visibility filters stay server-owned |
| T-M009-S02-02 | Tampering | Result presentation | mitigate | Render explicit source/trust labels from constrained metadata instead of loose strings or route guessing |
</threat_model>

<verification>
- `mix test test/cairnloop/web/search_modal_component_test.exs`
- `rg -n 'Cairnloop\\.Retrieval\\.search|Req\\.post|Knowledge Base|Similar resolved cases|Canonical guidance|Supporting evidence' lib/cairnloop/web/search_modal_component.ex lib/cairnloop/web/search_result_presenter.ex test/cairnloop/web/search_modal_component_test.exs`
</verification>

<success_criteria>
- Operator search no longer depends on direct remote HTTP calls from the modal.
- Results render in fixed source-aware sections with explicit trust/source cues.
- The UI receives enough retrieval metadata to support preview and open flows without route guessing.
</success_criteria>

<output>
After completion, create `.planning/milestones/M009-phases/M009-S02/M009-S02-01-SUMMARY.md`
</output>
