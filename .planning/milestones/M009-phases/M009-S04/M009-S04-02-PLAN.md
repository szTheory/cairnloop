---
phase: M009-S04
plan: "02"
type: execute
wave: 2
depends_on:
  - M009-S04-01
files_modified:
  - lib/cairnloop/retrieval/gap_event.ex
  - lib/cairnloop/retrieval/gap_event_snapshot.ex
  - lib/cairnloop/retrieval/gap_recorder.ex
  - lib/cairnloop/retrieval/workers/prune_gap_events.ex
  - priv/repo/migrations/*_add_retrieval_gap_events.exs
  - test/cairnloop/retrieval/gap_recorder_test.exs
  - test/cairnloop/retrieval/workers/prune_gap_events_test.exs
autonomous: true
requirements:
  - M009-REQ-09
must_haves:
  truths:
    - "No-hit, failed-search, and weak-grounding outcomes can be persisted durably without depending on telemetry handlers or worker lifecycle semantics."
    - "Gap events retain enough sanitized seed text and canonical-versus-assistive evidence counts for future clustering work while remaining append-only and bounded."
    - "Retention is explicit and lightweight: evidence rows live for 90 days by default and are pruned by explicit follow-up work rather than silently disappearing."
  artifacts:
    - path: "lib/cairnloop/retrieval/gap_event.ex"
      provides: "Append-only retrieval gap event schema with typed envelope fields"
    - path: "lib/cairnloop/retrieval/gap_recorder.ex"
      provides: "Synchronous boundary-owned recorder API using Ecto.Multi"
    - path: "lib/cairnloop/retrieval/workers/prune_gap_events.ex"
      provides: "Explicit Oban retention worker for the 90-day default pruning policy"
    - path: "priv/repo/migrations/*_add_retrieval_gap_events.exs"
      provides: "Durable retrieval gap event table and indexes"
  key_links:
    - from: "lib/cairnloop/retrieval/gap_recorder.ex"
      to: "lib/cairnloop/retrieval/gap_event.ex"
      via: "boundary-owned synchronous persistence inserts typed events through Ecto.Multi"
      pattern: "GapEvent|Ecto\\.Multi"
    - from: "lib/cairnloop/retrieval/gap_recorder.ex"
      to: "lib/cairnloop/retrieval/workers/prune_gap_events.ex"
      via: "retention follow-up is scheduled explicitly after successful persistence"
      pattern: "Oban"
---

<objective>
Establish the Phase 4 durable gap-event store, recorder, and explicit retention policy.

Purpose: make retrieval gap evidence durable and inspectable from the correct application boundaries while keeping retention and follow-up work boring and explicit.
Output: append-only gap-event schema, synchronous `Ecto.Multi` recorder seam, 90-day retention policy, and an Oban-backed pruning hook for follow-up maintenance.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M009-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-CONTEXT.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-RESEARCH.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-PATTERNS.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-VALIDATION.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-01-PLAN.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Create the append-only retrieval gap-event schema and synchronous recorder seam</name>
  <files>
    - lib/cairnloop/retrieval/gap_event.ex
    - lib/cairnloop/retrieval/gap_event_snapshot.ex
    - lib/cairnloop/retrieval/gap_recorder.ex
    - priv/repo/migrations/*_add_retrieval_gap_events.exs
    - test/cairnloop/retrieval/gap_recorder_test.exs
  </files>
  <read_first>
    - lib/cairnloop/retrieval/resolved_case_evidence.ex
    - lib/cairnloop/retrieval.ex
    - .planning/milestones/M009-phases/M009-S04/M009-S04-CONTEXT.md
    - .planning/milestones/M009-phases/M009-S04/M009-S04-RESEARCH.md
    - .planning/milestones/M009-phases/M009-S04/M009-S04-PATTERNS.md
  </read_first>
  <action>
    Add the dedicated append-only retrieval gap event store per D-10 and D-11. Use one focused table plus small embedded snapshot structs or bounded JSON payloads rather than a normalized lineage graph. The envelope must include occurrence time, surface, outcome class, reason, host or tenant scope, query fingerprint, sanitized query excerpt, canonical hit count, assistive hit count, clarification attempts, and attempted evidence snapshots per D-13 and D-14. Preserve canonical versus assistive semantics everywhere per D-29.

    Implement a `GapRecorder` boundary API that performs the durable row insert synchronously through `Ecto.Multi` per D-12 and D-28. This plan resolves the persistence-path decision from research: the evidence row itself is written synchronously at the application boundary, not enqueued as the primary path through Oban, so audit and debugging reads can observe the row immediately after the no-hit or weak-grounding outcome occurs. Keep storage distinct from telemetry per D-09, add redaction and deduplication discipline per D-15, and do not wire persistence from telemetry handlers.
  </action>
  <acceptance_criteria>
    - a migration creates one append-only retrieval gap-events table with indexes on occurrence time, surface, outcome, reason, and host scope
    - the Ecto schema uses bounded enums or equivalent typed fields for surface, outcome class, and reason atoms
    - the recorder API persists sanitized excerpts, query fingerprints, canonical versus assistive counts, and evidence snapshots without storing raw metric-label payloads
    - the recorder API performs the primary row write synchronously through `Ecto.Multi`, not by enqueuing Oban as the primary persistence path
    - tests assert the inserted row is immediately available after the recorder call returns
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/retrieval/gap_recorder_test.exs</automated>
  </verify>
  <done>
    Durable retrieval gap evidence can be inserted synchronously, deduplicated, and inspected immediately through explicit backend seams, and the tests prove the stored contract is append-only, sanitized, and separated from telemetry.
  </done>
</task>

<task type="execute">
  <name>Task 2: Add the explicit 90-day retention worker and maintenance contract</name>
  <files>
    - lib/cairnloop/retrieval/workers/prune_gap_events.ex
    - lib/cairnloop/retrieval/gap_recorder.ex
    - test/cairnloop/retrieval/workers/prune_gap_events_test.exs
  </files>
  <read_first>
    - lib/cairnloop/retrieval/workers/index_resolved_conversation.ex
    - lib/cairnloop/retrieval.ex
    - .planning/milestones/M009-phases/M009-S04/M009-S04-RESEARCH.md
  </read_first>
  <action>
    Implement the retention discipline from research explicitly: retrieval gap events are kept for 90 days by default, and pruning or replay maintenance uses Oban only as an explicit follow-up seam per D-15 and D-20. Do not let worker state redefine product outcome semantics. If the recorder schedules maintenance, keep it best-effort and clearly secondary to the synchronous evidence row write.

    Keep the maintenance contract simple and host-comprehensible per D-03 and D-30. The worker should prune by `occurred_at` or equivalent time field, preserve recent evidence untouched, and stay decoupled from retrieval telemetry. Do not expand this into a historical analytics subsystem or retention-policy UI.
  </action>
  <acceptance_criteria>
    - the plan implements a 90-day default retention policy for retrieval gap events
    - pruning runs through an explicit Oban worker or equivalent explicit maintenance seam rather than implicit deletes in read paths
    - any recorder-side follow-up scheduling is clearly secondary to the synchronous row write
    - the retention tests prove older rows are prune-eligible while recent rows are retained
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/retrieval/workers/prune_gap_events_test.exs</automated>
  </verify>
  <done>
    The gap-event store has an explicit 90-day retention contract with Oban used only for follow-up maintenance, and tests prove the lifecycle behavior without changing product semantics.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retrieval/search/draft boundary -> gap store | Durable evidence must be sanitized and typed before persistence |
| Gap recorder -> database transaction | The product-trust event row must exist before any maintenance follow-up is considered complete |
| Gap store -> retention worker | Lifecycle cleanup must not mutate or reinterpret product outcome semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M009-S04-03 | Denial of Service | Retrieval gap-event payload growth | mitigate | Enforce redaction, payload-size discipline, dedupe, and explicit 90-day pruning per D-15 |
| T-M009-S04-04 | Repudiation | Gap recorder primary write path | mitigate | Keep the evidence row write synchronous through `Ecto.Multi` so boundary outcomes are durably recorded immediately and test that behavior directly |
| T-M009-S04-05 | Tampering | Retention maintenance | mitigate | Limit Oban to explicit pruning of old rows and keep worker behavior independent from retrieval outcome classification per D-20 |
</threat_model>

<verification>
- `mix test test/cairnloop/retrieval/gap_recorder_test.exs test/cairnloop/retrieval/workers/prune_gap_events_test.exs`
- `rg -n 'query_fingerprint|sanitized_query_excerpt|canonical_hit_count|assistive_hit_count|90|prune|retention' lib/cairnloop test/cairnloop`
</verification>

<success_criteria>
- Durable retrieval gap evidence exists independently of telemetry and preserves enough sanitized context for later clustering work.
- The primary evidence row is written synchronously from application boundaries, with Oban reserved for explicit retention or replay follow-up only.
- Retrieval gap events are retained for 90 days by default and pruned through an explicit maintenance seam.
</success_criteria>

<output>
After completion, create `.planning/milestones/M009-phases/M009-S04/M009-S04-02-SUMMARY.md`
</output>
