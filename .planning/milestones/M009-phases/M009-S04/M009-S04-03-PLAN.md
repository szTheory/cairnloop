---
phase: M009-S04
plan: "03"
type: execute
wave: 3
depends_on:
  - M009-S04-01
  - M009-S04-02
files_modified:
  - lib/cairnloop/automation/workers/draft_worker.ex
  - lib/cairnloop/web/search_modal_component.ex
  - lib/cairnloop/web/search_result_presenter.ex
  - lib/cairnloop/web/conversation_live.ex
  - test/cairnloop/automation/workers/draft_worker_test.exs
  - test/cairnloop/web/search_modal_component_test.exs
  - test/cairnloop/web/conversation_live_test.exs
autonomous: true
requirements:
  - M009-REQ-08
  - M009-REQ-09
must_haves:
  truths:
    - "Search and draft surfaces propagate host scope into retrieval, telemetry, and gap recording instead of emitting misleading unscoped signals."
    - "No-hit, retrieval-error, and weak-grounding outcomes are recorded from the real application boundaries that know the surface and operator-facing trust state."
    - "Operators can inspect lightweight retrieval quality cues in existing search and draft surfaces without a new routed admin console."
  artifacts:
    - path: "lib/cairnloop/web/search_modal_component.ex"
      provides: "Search-boundary wiring for scoped retrieval, error or no-hit persistence, and calm operator copy"
    - path: "lib/cairnloop/automation/workers/draft_worker.ex"
      provides: "Draft-boundary wiring for structured telemetry and durable weak-grounding gap recording"
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "Draft rail cues for explicit grounding weakness and trust-state inspectability inside the existing conversation surface"
  key_links:
    - from: "lib/cairnloop/web/search_modal_component.ex"
      to: "lib/cairnloop/retrieval.ex"
      via: "search passes host scope and surface metadata into retrieval"
      pattern: "host_user_id|host_surface|Retrieval\\.search"
    - from: "lib/cairnloop/automation/workers/draft_worker.ex"
      to: "lib/cairnloop/retrieval/gap_recorder.ex"
      via: "weak-grounding and retrieval-error outcomes persist durable evidence from the draft boundary"
      pattern: "GapRecorder"
    - from: "lib/cairnloop/web/conversation_live.ex"
      to: "lib/cairnloop/web/search_result_presenter.ex"
      via: "operator trust cues reuse existing evidence presentation semantics"
      pattern: "SearchResultPresenter"
---

<objective>
Wire the Phase 4 contracts into search and drafting, then surface calm retrieval quality cues in the existing UI.

Purpose: make the new telemetry and gap-event contracts real at the application boundaries and keep operator inspectability lightweight, source-aware, and inside current surfaces.
Output: scoped search and draft integration with gap recording, plus updated search and draft UI copy and tests for no-hit, retrieval-error, and weak-grounding cases.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M009-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-CONTEXT.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-RESEARCH.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-PATTERNS.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-VALIDATION.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-01-PLAN.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-02-PLAN.md
@.planning/milestones/M009-phases/M009-S03/M009-S03-01-PLAN.md
@.planning/milestones/M009-phases/M009-S03/M009-S03-02-PLAN.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Wire scoped search and draft boundaries into retrieval telemetry and gap recording</name>
  <files>
    - lib/cairnloop/automation/workers/draft_worker.ex
    - lib/cairnloop/web/search_modal_component.ex
    - test/cairnloop/automation/workers/draft_worker_test.exs
    - test/cairnloop/web/search_modal_component_test.exs
  </files>
  <read_first>
    - lib/cairnloop/automation/workers/draft_worker.ex
    - lib/cairnloop/web/search_modal_component.ex
    - lib/cairnloop/retrieval.ex
    - lib/cairnloop/retrieval/gap_recorder.ex
    - test/cairnloop/automation/workers/draft_worker_test.exs
    - test/cairnloop/web/search_modal_component_test.exs
    - .planning/milestones/M009-phases/M009-S04/M009-S04-CONTEXT.md
  </read_first>
  <action>
    Fix the known scope-propagation risk by threading `host_user_id`, `host_surface`, and any retrieval boundary metadata through the real search and draft call sites per D-12, D-21, D-27, and D-29. `SearchModalComponent` should stop calling retrieval with an empty opts list, and `DraftWorker` should pass the host scope and draft surface metadata needed for accurate retrieval telemetry and durable evidence. Boundary code, not telemetry handlers, must decide when to record failed searches, no-hit search attempts, retrieval errors, assistive-only results, clarification-limit escalations, and canonical-insufficient-detail cases per D-09, D-12, D-17, D-18, and D-20.

    Use the new `GapRecorder` and telemetry helpers from Plans 01 and 02 directly from these application boundaries. This plan also resolves the persistence-path decision from research: search no-hit and retrieval-error rows, plus draft weak-grounding rows, should exist immediately after the boundary action completes because the primary write path is synchronous `Ecto.Multi` persistence. Keep retry semantics in Oban separate from operator-facing reasoning per D-20, and assert in tests that a no-hit or weak-grounding branch records durable evidence with stable bounded reasons instead of one generic fallback bucket.
  </action>
  <acceptance_criteria>
    - `SearchModalComponent` passes host scope and surface metadata into retrieval instead of always calling `search(query, [])`
    - `DraftWorker` passes scoped retrieval opts into `ground_for_draft/2` and records weak-grounding or retrieval-error events from the worker boundary
    - search no-hit and retrieval-error cases persist gap events synchronously from the UI boundary without depending on telemetry handlers or Oban as the primary write path
    - draft weak-grounding cases persist gap events with stable reason atoms such as `:assistive_only_results`, `:clarification_limit_reached`, or `:canonical_insufficient_detail`
    - the updated tests assert scope propagation and durable gap recording for at least one search miss and one draft weak-grounding case
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/automation/workers/draft_worker_test.exs test/cairnloop/web/search_modal_component_test.exs</automated>
  </verify>
  <done>
    Search and draft boundaries emit scoped retrieval telemetry and durable gap evidence from the correct application seams, and automated tests prove the known scope-propagation risk is closed.
  </done>
</task>

<task type="execute">
  <name>Task 2: Add lightweight retrieval quality cues inside existing search and draft surfaces</name>
  <files>
    - lib/cairnloop/web/search_modal_component.ex
    - lib/cairnloop/web/search_result_presenter.ex
    - lib/cairnloop/web/conversation_live.ex
    - test/cairnloop/web/search_modal_component_test.exs
    - test/cairnloop/web/conversation_live_test.exs
  </files>
  <read_first>
    - lib/cairnloop/web/search_modal_component.ex
    - lib/cairnloop/web/search_result_presenter.ex
    - lib/cairnloop/web/conversation_live.ex
    - test/cairnloop/web/search_modal_component_test.exs
    - test/cairnloop/web/conversation_live_test.exs
    - .planning/milestones/M009-phases/M009-S04/M009-S04-CONTEXT.md
    - .planning/milestones/M009-phases/M009-S04/M009-S04-PATTERNS.md
  </read_first>
  <action>
    Reuse the existing search and draft evidence surfaces per D-03, D-21, and D-22. Update `SearchModalComponent`, `SearchResultPresenter`, and `ConversationLive` so operators can see calm, evidence-first retrieval quality cues for no-hit, retrieval-error, and weak-grounding cases without a new routed console. Keep the cues lightweight per D-23 and D-24: source type, trust label, recency, explicit no-hit or escalation copy, and the reason the system could not safely ground a reply. Do not show raw scores, debug spans, or dense internals to operators.

    Search should distinguish a temporary retrieval failure from a true no-hit state, and the draft rail should make weak-grounding or escalation reasons inspectable in the existing evidence card without pretending the answer quality is stronger than it is. Preserve canonical-first semantics in copy and layout per D-29, and keep the UI work bounded to current surfaces per the deferred-ideas list. The tests must cover both the rendering contract and the trust-language outcomes, not just raw text presence.
  </action>
  <acceptance_criteria>
    - the search UI distinguishes no-hit, retrieval-error, and regular-results states with calm existing-surface copy
    - `ConversationLive` exposes weak-grounding or escalation reasons in the draft rail while keeping canonical versus assistive evidence labels intact
    - `SearchResultPresenter` remains the shared source or trust vocabulary for these surfaces
    - no new routed admin or debugger surface is introduced
    - tests cover one no-hit or retrieval-error search state and one weak-grounding draft rail state
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/web/search_modal_component_test.exs test/cairnloop/web/conversation_live_test.exs</automated>
  </verify>
  <done>
    Operators can inspect retrieval quality signals directly inside the existing search and draft surfaces, and automated tests prove no-hit and weak-grounding cases remain explicit without exposing noisy internals.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search UI -> retrieval boundary | Host scope and surface metadata must stay intact so retrieval signals and results remain tenant-safe |
| Draft worker -> gap recorder | Weak-grounding persistence must reflect product trust semantics, not worker retry state |
| Draft rail -> operator decision | UI must preserve canonical-versus-assistive trust hierarchy and explicit weak-grounding reasons |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M009-S04-06 | Information Disclosure | Search and draft scope propagation | mitigate | Pass `host_user_id` and surface metadata from current call sites into retrieval and persistence, then assert the scoped behavior in component and worker tests |
| T-M009-S04-07 | Spoofing | Operator-facing weak-grounding cues | mitigate | Render explicit no-hit, clarification-limit, and escalation copy inside existing surfaces so weak evidence cannot masquerade as canonical confidence |
| T-M009-S04-08 | Tampering | Gap recording from application boundaries | mitigate | Record durable evidence from `SearchModalComponent` and `DraftWorker` using typed reason atoms and the synchronous recorder seam rather than deriving product semantics indirectly from telemetry or Oban state |
</threat_model>

<verification>
- `mix test test/cairnloop/automation/workers/draft_worker_test.exs test/cairnloop/web/search_modal_component_test.exs test/cairnloop/web/conversation_live_test.exs`
- `rg -n 'host_user_id|host_surface|GapRecorder|no-hit|no hit|retrieval error|weak grounding|clarification_limit_reached|assistive_only_results' lib/cairnloop test/cairnloop`
</verification>

<success_criteria>
- Search and drafting propagate host scope into retrieval, telemetry, and durable gap recording so the signals describe the real trust boundary.
- Failed search, no-hit, and weak-grounding outcomes are persisted from the real search or draft boundaries using stable reason atoms and synchronous boundary-owned writes.
- Existing search and draft surfaces expose lightweight inspectability cues without a new admin console or raw-score chrome.
</success_criteria>

<output>
After completion, create `.planning/milestones/M009-phases/M009-S04/M009-S04-03-SUMMARY.md`
</output>
