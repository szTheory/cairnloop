---
phase: M009-S04
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/retrieval.ex
  - lib/cairnloop/retrieval/result.ex
  - lib/cairnloop/retrieval/ranker.ex
  - lib/cairnloop/retrieval/telemetry.ex
  - lib/cairnloop/telemetry.ex
  - test/cairnloop/retrieval_test.exs
  - test/cairnloop/retrieval/telemetry_test.exs
autonomous: true
requirements:
  - M009-REQ-08
must_haves:
  truths:
    - "Retrieval and draft-grounding attempts emit stable Cairnloop telemetry with bounded dimensions for latency, hit/miss, ranking outcomes, and grounding decisions."
    - "Structured subordinate diagnostics exist beneath the coarse grounding state machine so telemetry can distinguish infra failures, empty recall, weak grounding, and policy limits."
    - "Canonical Knowledge Base evidence and assistive resolved-case evidence stay distinct in telemetry labels and retrieval outcome metadata."
  artifacts:
    - path: "lib/cairnloop/retrieval/telemetry.ex"
      provides: "Stable bounded retrieval/search/grounding telemetry helpers over Cairnloop.Telemetry"
    - path: "lib/cairnloop/retrieval.ex"
      provides: "Structured diagnostic taxonomy and telemetry emission at the retrieval seam"
    - path: "lib/cairnloop/retrieval/ranker.ex"
      provides: "Ranking-outcome summaries that feed bounded retrieval telemetry"
  key_links:
    - from: "lib/cairnloop/retrieval.ex"
      to: "lib/cairnloop/retrieval/telemetry.ex"
      via: "retrieval/search and grounding spans emit bounded metadata"
      pattern: "Retrieval\\.Telemetry|Cairnloop\\.Telemetry"
    - from: "lib/cairnloop/retrieval/ranker.ex"
      to: "lib/cairnloop/retrieval.ex"
      via: "ranking outcome summaries remain part of the normalized retrieval bundle"
      pattern: "Ranker|match_reasons"
---

<objective>
Establish the Phase 4 retrieval telemetry contract and diagnostic taxonomy.

Purpose: give search and grounded drafting one stable, host-owned telemetry seam and one explicit diagnostic taxonomy before persistence and surface integration start.
Output: bounded retrieval telemetry helpers, structured diagnostic taxonomy under the existing grounding state machine, and contract-level verification scaffolding.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/M009-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-CONTEXT.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-RESEARCH.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-PATTERNS.md
@.planning/milestones/M009-phases/M009-S04/M009-S04-VALIDATION.md
@.planning/milestones/M009-phases/M009-S03/M009-S03-PLAN-CHECK.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add the stable retrieval telemetry contract and structured diagnostic taxonomy</name>
  <files>
    - lib/cairnloop/retrieval.ex
    - lib/cairnloop/retrieval/result.ex
    - lib/cairnloop/retrieval/ranker.ex
    - lib/cairnloop/retrieval/telemetry.ex
    - lib/cairnloop/telemetry.ex
    - test/cairnloop/retrieval_test.exs
    - test/cairnloop/retrieval/telemetry_test.exs
  </files>
  <read_first>
    - lib/cairnloop/retrieval.ex
    - lib/cairnloop/retrieval/result.ex
    - lib/cairnloop/retrieval/ranker.ex
    - lib/cairnloop/telemetry.ex
    - .planning/milestones/M009-phases/M009-S04/M009-S04-CONTEXT.md
    - .planning/milestones/M009-phases/M009-S04/M009-S04-RESEARCH.md
    - .planning/milestones/M009-phases/M009-S04/M009-S04-PATTERNS.md
  </read_first>
  <action>
    Extend the retrieval boundary so the public observability seam is Cairnloop-native telemetry per D-04, D-05, D-06, and D-26, not direct OpenInference events. Add a retrieval telemetry helper module that wraps `Cairnloop.Telemetry` and standardizes root events such as retrieval search and draft grounding with bounded metadata only: surface, source mix, result bucket, grounding status, diagnostic class, reason, canonical hit count, assistive hit count, and ranking outcome summaries per D-07, D-08, D-17, and D-18. Keep the contract explicit and readable for the next maintainer per D-30.

    Update `Cairnloop.Retrieval`, `Result`, and the ranker so the normalized retrieval bundle carries the structured diagnostic taxonomy under the existing coarse product disposition instead of collapsing every failure into one generic `:retrieval_error` bucket. Preserve the current operator-facing `strong | clarification | escalation` state machine per D-16, but add bounded subordinate reasons such as `:provider_timeout`, `:index_unavailable`, `:assistive_only_results`, `:no_canonical_results`, and `:canonical_insufficient_detail` per D-18 and D-19. Address the known coarse retrieval-error-classification risk explicitly by capturing or deriving the bounded error class before the current rescue fallback. Do not add raw query text, result IDs, or citation payloads to metric labels per D-08, and do not introduce a new public telemetry contract outside `[:cairnloop, ...]`.
  </action>
  <acceptance_criteria>
    - `lib/cairnloop/retrieval/telemetry.ex` defines bounded event-name and metadata helpers rooted under `[:cairnloop, :retrieval, ...]`
    - retrieval search and draft-grounding paths can report result bucket, source mix, grounding status, diagnostic class, and stable reason atoms without using raw query text as labels
    - `Cairnloop.Retrieval` preserves the coarse `strong | clarification | escalation` contract while also exposing structured subordinate diagnostics
    - the diagnostic taxonomy distinguishes infra errors, empty recall, weak grounding, and policy limits rather than one flat `failed` bucket
    - any OpenInference trace emission remains an adapter concern, not the primary public contract
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/retrieval_test.exs test/cairnloop/retrieval/telemetry_test.exs</automated>
  </verify>
  <done>
    The retrieval facade emits stable Cairnloop retrieval events with bounded metadata, and the tests prove search and grounding outcomes carry structured diagnostics without breaking the existing product-state contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retrieval boundary -> telemetry contract | Only bounded, low-cardinality metadata may cross into public metrics and traces |
| Retrieval outcome bundle -> downstream persistence | Telemetry metadata and diagnostic taxonomy must stay bounded and separate from later durable evidence writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M009-S04-01 | Information Disclosure | Retrieval telemetry metadata | mitigate | Keep event metadata bounded per D-08, strip raw query text and record IDs from metric labels, and test the emitted metadata shape directly |
| T-M009-S04-02 | Tampering | Diagnostic classification in `Cairnloop.Retrieval` | mitigate | Preserve the coarse product disposition while adding typed subordinate diagnostics so infra failures, empty recall, and weak grounding cannot be merged into one misleading bucket |
| T-M009-S04-03 | Repudiation | Retrieval outcome diagnostics | mitigate | Keep the taxonomy explicit in `Cairnloop.Retrieval` and cover stable reason atoms in tests so downstream persistence and host metrics can rely on one authoritative contract |
</threat_model>

<verification>
- `mix test test/cairnloop/retrieval_test.exs test/cairnloop/retrieval/telemetry_test.exs`
- `rg -n 'diagnostic|grounding_status|result_bucket|source_mix|canonical_hit_count|assistive_hit_count' lib/cairnloop test/cairnloop`
</verification>

<success_criteria>
- Retrieval search and draft-grounding attempts emit stable Cairnloop telemetry with safe bounded metadata for latency, hit/miss, ranking outcomes, and grounding decisions.
- Structured subordinate diagnostics make infra failures, empty recall, weak grounding, and policy limits distinguishable without changing the coarse product-state contract.
- Canonical and assistive evidence remain distinct across telemetry labels and retrieval diagnostics.
</success_criteria>

<output>
After completion, create `.planning/milestones/M009-phases/M009-S04/M009-S04-01-SUMMARY.md`
</output>
