---
phase: 1
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [TLM-01, TLM-02, EXT-01]
must_haves:
  truths:
    - "Developers can observe a [:cairnloop, :conversation, :resolved] telemetry event firing whenever a conversation is marked as resolved"
    - "The event payload reliably contains the conversation ID and metadata (like duration)"
    - "Developers can implement a Cairnloop.Notifier behaviour to execute transactionally-bound side-effects"
    - "Developers can consult documentation and a reference handler to hook external actions to the resolution event"
    - "Developers can scaffold the notifier behaviour using `mix cairnloop.gen.notifier`"
  artifacts:
    - path: "lib/cairnloop/chat.ex"
      provides: "Telemetry and Oban job execution logic"
    - path: "lib/cairnloop/workers/notify_resolved_worker.ex"
      provides: "Transactional Oban worker for the Notifier behaviour"
    - path: "lib/cairnloop/notifier.ex"
      provides: "Behaviour definition for host extensibility"
    - path: "lib/mix/tasks/cairnloop.gen.notifier.ex"
      provides: "DX code generator for scaffolding the notifier"
    - path: "test/cairnloop/chat_test.exs"
      provides: "Test coverage asserting telemetry and worker behavior"
    - path: "README.md"
      provides: "Documentation and reference handler for host extensibility"
  key_links: []
---

<objective>
Implement and verify the Foundation (Telemetry & Events) "Dual Emission" architecture and Host Extensibility documentation.

Purpose: Ensure the system natively emits rich observability signals using a complete dual-emission approach: an ephemeral domain event (`[:cairnloop, :conversation, :resolved]`) for tracing/UI reactivity, and a transactionally guaranteed `Cairnloop.Notifier` behaviour backed by Oban for durable business logic.
Output: Verified dual telemetry emission capabilities, a complete Notifier behaviour implementation, DX code generator, and verified documentation.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/1/RESEARCH.md
@.planning/phases/1/1-PATTERNS.md
@.planning/phases/1/ARCH_ANALYSIS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Dual Emission for Telemetry (TLM-01, TLM-02)</name>
  <files>
    - lib/cairnloop/chat.ex
    - test/cairnloop/chat_test.exs
  </files>
  <action>Update `Cairnloop.Chat.resolve_conversation/2` to explicitly execute the `[:cairnloop, :conversation, :resolved]` telemetry event inside the success branch of the transaction, while preserving the surrounding `[:cairnloop, :conversation, :resolve]` span. Update `chat_test.exs` to verify both the span and the specific domain event are emitted.</action>
  <verify>
    <automated>mix test test/cairnloop/chat_test.exs</automated>
  </verify>
  <done>Code correctly emits the domain event and tests pass successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Transactional Notifier Behaviour</name>
  <files>
    - lib/cairnloop/notifier.ex
    - lib/cairnloop/workers/notify_resolved_worker.ex
    - lib/cairnloop/chat.ex
  </files>
  <action>Define the `Cairnloop.Notifier` behaviour. Create an Oban worker (`Cairnloop.Workers.NotifyResolvedWorker`) that executes this behaviour. Enqueue this worker inside the `Ecto.Multi` transaction in `Cairnloop.Chat.resolve_conversation/2` to ensure guaranteed execution.</action>
  <verify>
    <automated>grep -q "defmodule Cairnloop.Notifier" lib/cairnloop/notifier.ex</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Create DX Code Generator</name>
  <files>
    - lib/mix/tasks/cairnloop.gen.notifier.ex
  </files>
  <action>Implement a Mix task `mix cairnloop.gen.notifier` that scaffolds a clean `lib/my_app/cairnloop_notifier.ex` file and injects the configuration into the host app's `config/config.exs` to maximize developer ergonomics.</action>
  <verify>
    <automated>mix help cairnloop.gen.notifier</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Update Host Extensibility Documentation (EXT-01)</name>
  <files>
    - README.md
  </files>
  <action>Update the `README.md` to clearly document the "Dual Emission" architecture. Explain when to use Telemetry (ephemeral observation) vs the Notifier behaviour (durable mutation). Provide concrete code examples for both.</action>
  <verify>
    <automated>grep -q "cairnloop, :conversation, :resolved" README.md</automated>
  </verify>
  <done>Documentation exists demonstrating how to consume both signals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application -> Observers | Domain events are emitted to attached telemetry handlers |
| Application -> Workers | Notifier behaviour is executed via internal Oban queues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | lib/cairnloop/chat.ex | accept | Telemetry payload contains system IDs (conversation_id, host_user_id) and metadata. It's emitted internally to trusted handlers and doesn't explicitly expose sensitive PII. |
</threat_model>

<verification>
mix test test/cairnloop/chat_test.exs
mix help cairnloop.gen.notifier
grep -q "cairnloop, :conversation, :resolved" README.md
</verification>

<success_criteria>
1. Developers can observe a `[:cairnloop, :conversation, :resolved]` telemetry event firing whenever a conversation is marked as resolved.
2. The event payload reliably contains the conversation ID and metadata (like duration).
3. Developers can use `Cairnloop.Notifier` for transactionally guaranteed side-effects.
4. A Mix task exists to automatically scaffold the notifier for developers.
5. Developers can consult documentation and a reference handler to hook external actions to the resolution event.
</success_criteria>

<output>
After completion, create `.planning/phases/1/1-01-SUMMARY.md`
</output>