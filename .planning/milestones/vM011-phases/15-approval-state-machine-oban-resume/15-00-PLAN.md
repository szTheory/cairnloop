---
phase: 15-approval-state-machine-oban-resume
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/cairnloop/governance/tool_approval_test.exs
  - test/cairnloop/workers/approval_resume_worker_test.exs
  - test/cairnloop/workers/approval_expiry_worker_test.exs
  - test/cairnloop/governance_test.exs
  - test/cairnloop/governance/tool_action_event_test.exs
  - test/cairnloop/governance/preview_test.exs
  - test/cairnloop/web/tool_proposal_presenter_test.exs
autonomous: true
requirements: [FLOW-03, APRV-01, APRV-02, APRV-03, APRV-04]

must_haves:
  truths:
    - "`mix compile --warnings-as-errors` stays clean and `mix test` runs to completion with the 3 new + 4 extended approval test files present (red/skipped allowed for not-yet-built behavior; broken-compile is not)."
    - "`test/cairnloop/governance/tool_approval_test.exs` exists with describe blocks for changeset validity, decision_changeset reason-required (FLOW-03), and the append-only intent (no update/1, no delete/1)."
    - "`test/cairnloop/workers/approval_resume_worker_test.exs` exists with a MockRepo + describe blocks for validate-pass→:execution_pending, validate-fail→:invalidated, lazy expires_at guard, and idempotent no-op."
    - "`test/cairnloop/workers/approval_expiry_worker_test.exs` exists with a MockRepo + describe block for the scheduled :pending→:expired flip."
    - "`test/cairnloop/governance_test.exs` is extended with describe blocks for approve-enqueues-never-executes (APRV-01), WR-01 humanization (no `#Ecto.Changeset<` substring), append-only multi-decision trail, and transition-guarded-on-:pending."
    - "`test/cairnloop/governance/tool_action_event_test.exs` is extended to assert the new approval `@event_type_values` atoms are accepted by the changeset."
    - "`test/cairnloop/governance/preview_test.exs` is extended with a snapshotted-vs-live divergence describe block (D15-14)."
    - "`test/cairnloop/web/tool_proposal_presenter_test.exs` is extended with approval status_group, approval_outlook, and history_line describe blocks (D15-16)."
  artifacts:
    - path: "test/cairnloop/governance/tool_approval_test.exs"
      provides: "Headless ToolApproval changeset + decision_changeset + append-only behavior contract"
      contains: "Cairnloop.Governance.ToolApproval"
    - path: "test/cairnloop/workers/approval_resume_worker_test.exs"
      provides: "Resume worker re-validate + seam + lazy-expiry + idempotency contract (MockRepo)"
      contains: "ApprovalResumeWorker"
    - path: "test/cairnloop/workers/approval_expiry_worker_test.exs"
      provides: "Scheduled expiry flip contract (MockRepo)"
      contains: "ApprovalExpiryWorker"
  key_links:
    - from: "test/cairnloop/workers/approval_resume_worker_test.exs"
      to: "Cairnloop.Workers.ApprovalResumeWorker.perform/1"
      via: "module under test (does not exist until Wave 2 — tests red/skipped initially)"
      pattern: "ApprovalResumeWorker.perform"
    - from: "test/cairnloop/governance/tool_approval_test.exs"
      to: "Cairnloop.Governance.ToolApproval"
      via: "module under test (does not exist until Wave 1)"
      pattern: "ToolApproval"
---

<objective>
Create the Phase 15 test infrastructure (Wave 0) BEFORE any implementation exists, satisfying the
Nyquist validation contract in 15-VALIDATION.md. Three new headless test files
(`tool_approval_test.exs`, `approval_resume_worker_test.exs`, `approval_expiry_worker_test.exs`)
and four extensions (`governance_test.exs`, `tool_action_event_test.exs`, `preview_test.exs`,
`tool_proposal_presenter_test.exs`) establish the per-task `<automated>` verification surface that
Waves 1-4 turn green. This matches the Phase 14 plan structure (14-00-PLAN.md).

The COMMON path is headless: changeset validation, pure presenter functions, and worker logic via
the established `MockRepo` + `Application.put_env(:cairnloop, :repo, MockRepo)` idiom. The rare
Postgres-only legs (partial unique index INSERT, JSONB round-trip, `expires_at` column type) are
written but marked `# REPO-UNAVAILABLE` per CLAUDE.md and 15-VALIDATION.md Manual-Only rows.

Purpose: Front-load the test surface so every downstream task has a concrete `<automated>` gate
(Nyquist Dimension 8). The modules these tests reference (`Cairnloop.Governance.ToolApproval`,
`Cairnloop.Workers.ApprovalResumeWorker`, `Cairnloop.Workers.ApprovalExpiryWorker`, the new
`Governance.approve/reject/defer/expire/get_active_approval` facade functions) do NOT exist yet —
Wave 0 tests are written red/skipped and turn green across Waves 1-4.
Output: Three new + four extended test files committed; `mix compile --warnings-as-errors` clean;
`mix test` runs (new approval describe blocks may be red/skipped pending implementation — correct
for Wave 0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-approval-state-machine-oban-resume/15-CONTEXT.md
@.planning/phases/15-approval-state-machine-oban-resume/15-RESEARCH.md
@.planning/phases/15-approval-state-machine-oban-resume/15-PATTERNS.md
@.planning/phases/15-approval-state-machine-oban-resume/15-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Modules these tests will reference. They do NOT exist yet — Wave 0 tests are red/skipped -->
<!-- until Waves 1-4 implement them. Use these exact names so the tests bind cleanly later. -->

Wave 1 will create (test against these contracts):
- `Cairnloop.Governance.ToolApproval` — Ecto schema, table `cairnloop_tool_approvals`. Fields:
  `status` (`:pending|:approved|:execution_pending|:rejected|:deferred|:expired|:invalidated`),
  `decided_by` (:string), `last_decision` (:string), `decided_at` (:utc_datetime_usec),
  `reason` (:string), `expires_at` (:utc_datetime_usec), `tool_proposal_id` (belongs_to).
  Functions: `changeset/2`, `decision_changeset/6` (approval, status, decision, reason, actor_id,
  decided_at), `status_values/0`.
- `Cairnloop.Governance.get_active_approval/1` — takes a tool_proposal_id, returns the single
  `:pending` `%ToolApproval{}` or nil (added to the narrow facade).
- `ToolActionEvent.@event_type_values` extended with `:approval_requested`, `:approved`,
  `:rejected`, `:deferred`, `:expired`, `:invalidated`, `:resume_scheduled`,
  `:revalidation_passed`, `:revalidation_failed`.
- `ToolProposal` gains nullable `rendered_consequence` (:string) + `title` (:string) columns and
  `has_one(:approval, ToolApproval)`.

Wave 2 will create (test against these contracts):
- `Cairnloop.Workers.ApprovalResumeWorker.perform(%Oban.Job{args: %{"approval_id" => id}})` —
  re-calls `Governance.validate/3`; pass → :execution_pending + :revalidation_passed event; fail →
  :invalidated + :revalidation_failed event; lazy `expires_at < now` guard → :expired; never run/3.
- `Cairnloop.Workers.ApprovalExpiryWorker.perform(%Oban.Job{args: %{"approval_id" => id}})` —
  flips `:pending → :expired` + emits :expired event; no-op if already resolved.

Wave 3 will create (test against these contracts):
- `Cairnloop.Governance.approve/3`, `reject/3`, `defer/3`, `expire/2` — `opts` includes
  `enqueue_fn` (defaulting to a `&safe_enqueue/1` wrapping `Oban.insert/1`) for headless test
  isolation; approve persists decision + event THEN enqueues resume; never calls run/3.

Existing structs/idioms the tests build fixtures from (CONFIRMED in source):
- `Cairnloop.Governance.ToolProposal` — `status`, `risk_tier`, `approval_mode`, `tool_ref`,
  `actor_id`, `input_snapshot`, `scope_snapshot`, `policy_snapshot`, `events` (has_many),
  `rendered_consequence`/`title` (Wave 1), `approval` (has_one, Wave 1).
- `Cairnloop.Governance.ToolActionEvent` — `event_type`, `from_status`, `to_status`, `actor_id`,
  `reason`, `metadata`. NOTE: changeset currently `validate_required([..., :to_status, ...])` at
  tool_action_event.ex L57 — approval events leave from_status/to_status nil (Wave 1 relaxes the
  required-for-approval-events case); tests should document this expectation.
- MockRepo idiom: `Application.put_env(:cairnloop, :repo, MockRepo)` in setup +
  `on_exit(&Application.delete_env/2)` (see test/cairnloop/workers/sla_countdown_worker_test.exs).
- `enqueue_fn` opt injection idiom (see lib/cairnloop/knowledge_automation.ex ~L1068-1074).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 3 new headless test files (ToolApproval changeset + the 2 Oban workers)</name>
  <files>test/cairnloop/governance/tool_approval_test.exs, test/cairnloop/workers/approval_resume_worker_test.exs, test/cairnloop/workers/approval_expiry_worker_test.exs</files>
  <read_first>
    - test/cairnloop/governance/tool_proposal_test.exs (the changeset-test idiom + inline `%ToolProposal{}` fixture style these new tests mirror)
    - test/cairnloop/governance/tool_action_event_test.exs (append-only invariant test idiom — `refute function_exported?(Mod, :update, 1)` style; new approval event_type validity)
    - test/cairnloop/workers/sla_countdown_worker_test.exs (the COMPLETE MockRepo + `Application.put_env(:cairnloop, :repo, MockRepo)` + `on_exit` worker-test template; `get → check status → assert_receive {:repo_update, cs}`)
    - lib/cairnloop/knowledge_automation/review_task.ex (the `@status_values` + `decision_changeset` + denormalized last-decision shape ToolApproval clones — FLOW-03 reason-required source)
    - lib/cairnloop/workers/sla_countdown_worker.ex (the worker structure under test)
    - .planning/phases/15-approval-state-machine-oban-resume/15-PATTERNS.md (Test Patterns section — the exact MockRepo fixtures with approval ids 1/2/3/4)
    - .planning/phases/15-approval-state-machine-oban-resume/15-VALIDATION.md (rows 15-01-a/b/c, 15-02-b, 15-03-a/b/c/d/e — the behaviors these files must encode)
  </read_first>
  <action>
    Create `test/cairnloop/governance/tool_approval_test.exs` (module
    `Cairnloop.Governance.ToolApprovalTest`, `use ExUnit.Case, async: true`) covering
    `Cairnloop.Governance.ToolApproval` headless (no Repo), building inline `%ToolApproval{}`
    fixtures (no shared factory — repo idiom). Cover (15-VALIDATION rows 15-01-a/b, 15-02-b):
    (1) `changeset/2` valid with required `tool_proposal_id` + `status`; `status` defaults to
    `:pending`;
    (2) `decision_changeset/6` requires `reason` for `:rejected` AND `:deferred` (FLOW-03) — assert
    `refute cs.valid?` and a `:reason` error; `:approved` does NOT require reason;
    (3) `decision_changeset/6` captures `decided_by`/`decided_at`/`last_decision` (denormalized
    ReviewTask idiom);
    (4) the one-active-lane `unique_constraint(:tool_proposal_id, name:
    :cairnloop_tool_approvals_one_active_lane_index)` is declared on the changeset (assert the
    changeset has the constraint registered; the actual DB INSERT rejection is a `# REPO-UNAVAILABLE`
    stub — add the comment and `@tag :skip` on the live-INSERT case);
    (5) append-only intent: `refute function_exported?(ToolApproval, :update, 1)` and `:delete`.
    Mark any reference to the not-yet-existing module with `@tag :skip` so the file COMPILES now and
    skips are removed in Wave 1.

    Create `test/cairnloop/workers/approval_resume_worker_test.exs` (module
    `Cairnloop.Workers.ApprovalResumeWorkerTest`, `use ExUnit.Case, async: false`) cloning the
    sla_countdown_worker_test.exs MockRepo template. Define a MockRepo with: approval id 1
    (status `:pending`, expires_at nil), id 2 (status `:approved` — already transitioned), id 3
    (nil — deleted), id 4 (status `:pending`, `expires_at: ~U[2020-01-01 00:00:00Z]` — expired);
    `get!(ToolProposal, 10)` returning a proposal whose `tool_ref` re-validates to `{:ok, _}`;
    `update/1` and `insert/1` `send(self(), {:repo_update|:repo_insert, cs})`. Cover
    (15-VALIDATION rows 15-03-a/b/c/d):
    (1) `:pending` + validate pass → `assert_receive {:repo_update, cs}` with
    `get_change(cs, :status) == :execution_pending` (and an event insert) — assert NO call resolves
    `run/3` (source-assert the worker file contains no `.run(` / `run/3` reference, OR a refute);
    (2) `:pending` + validate fail → `:invalidated` with an operator-visible reason event;
    (3) already-transitioned (id 2) and deleted (id 3) → `:ok` no-op, `refute_receive {:repo_update, _}`;
    (4) lazy `expires_at` guard (id 4) → `get_change(cs, :status) == :expired` BEFORE re-validation runs.
    Add a `# REPO-UNAVAILABLE` comment for any test that would need a live validate against a real
    tool module; use a test-only tool ref / `@tag :skip` where the validate path cannot be exercised
    headlessly yet.

    Create `test/cairnloop/workers/approval_expiry_worker_test.exs` (module
    `Cairnloop.Workers.ApprovalExpiryWorkerTest`, `use ExUnit.Case, async: false`) cloning the same
    MockRepo template. Cover (15-VALIDATION row 15-03-e):
    (1) `:pending` approval → flips `:pending → :expired` (`get_change(cs, :status) == :expired`)
    and inserts an `:expired` event;
    (2) already-resolved approval (status `:approved`) and nil approval → `:ok` no-op.
    Mark not-yet-existing module references `@tag :skip` so the file compiles now.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/governance/tool_approval_test.exs test/cairnloop/workers/approval_resume_worker_test.exs test/cairnloop/workers/approval_expiry_worker_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test/cairnloop/governance/tool_approval_test.exs` exists and references `Cairnloop.Governance.ToolApproval` (`grep -c "Cairnloop.Governance.ToolApproval" test/cairnloop/governance/tool_approval_test.exs` ≥ 1).
    - `tool_approval_test.exs` contains a `decision_changeset` reject-without-reason test asserting `refute cs.valid?` (FLOW-03), and an approve-without-reason test asserting validity.
    - `tool_approval_test.exs` contains a `# REPO-UNAVAILABLE` comment on the live partial-unique-index INSERT stub.
    - `test/cairnloop/workers/approval_resume_worker_test.exs` exists, sets up `Application.put_env(:cairnloop, :repo, MockRepo)` in `setup`, and references `ApprovalResumeWorker.perform`.
    - `approval_resume_worker_test.exs` has a test asserting the lazy `expires_at` guard yields `status == :expired` and a test asserting validate-pass yields `status == :execution_pending`.
    - `test/cairnloop/workers/approval_expiry_worker_test.exs` exists and references `ApprovalExpiryWorker.perform`.
    - `mix compile --warnings-as-errors` is clean; `mix test {three files}` runs to completion (pass/skip/red allowed; compile failure NOT allowed — wrap undefined-module references in runtime calls / `@tag :skip`, never compile-time macros).
  </acceptance_criteria>
  <done>Three new headless test files exist, compile warnings-clean, and run; they encode the FLOW-03/APRV-02/APRV-03/APRV-04 + D15-12 behavior contracts with skip-tags + REPO-UNAVAILABLE markers where implementation does not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the 4 existing test files for the approval surface (facade, events, snapshot divergence, presenter)</name>
  <files>test/cairnloop/governance_test.exs, test/cairnloop/governance/tool_action_event_test.exs, test/cairnloop/governance/preview_test.exs, test/cairnloop/web/tool_proposal_presenter_test.exs</files>
  <read_first>
    - test/cairnloop/governance_test.exs (existing MockRepo with `insert/1`, `get_by/2`, `Process.get(:tool_proposals, [])`; the source-assertion + inline-fixture + describe-block idiom; existing `propose/3` blocked-path tests)
    - test/cairnloop/governance/tool_action_event_test.exs (the existing event_type validity + append-only tests — extend with the new approval atoms)
    - test/cairnloop/governance/preview_test.exs (the structured-vs-live render test idiom — extend with the D15-14 divergence fixture)
    - test/cairnloop/web/tool_proposal_presenter_test.exs (the pure-presenter describe-block idiom; existing `status_group`/`approval_outlook`/`history_line` tests to mirror)
    - lib/cairnloop/governance.ex L309-320 (the `insert_blocked_proposal` WR-01 site — `reason_str = inspect(reason)` at L313 the D15-15 test asserts is gone)
    - lib/cairnloop/web/tool_proposal_presenter.ex L55-66, L111-114, L259-269 (existing status_group / approval_outlook / history_line + catch-all the extensions target)
    - .planning/phases/15-approval-state-machine-oban-resume/15-PATTERNS.md (Test Patterns — the exact extension blocks for tool_action_event_test + presenter_test)
    - .planning/phases/15-approval-state-machine-oban-resume/15-VALIDATION.md (rows 15-01-c/d, 15-02-a/c/d, 15-04-a/b, 15-05-a/b)
  </read_first>
  <action>
    Extend `test/cairnloop/governance_test.exs` with new describe blocks (do not modify existing
    passing tests). Cover (15-VALIDATION rows 15-01-d, 15-02-a/c/d, 15-05-b):
    (1) `get_active_approval/1` returns the single `:pending` approval (or nil) for a proposal id —
    extend MockRepo with a `get_by`/`all` path returning a seeded `:pending` `%ToolApproval{}`;
    (2) `approve/3` persists the approval decision + event AND enqueues a resume job via an injected
    `enqueue_fn` opt that captures the enqueued job (assert the job carries `%{"approval_id" => id}`),
    and asserts the worker is `Cairnloop.Workers.ApprovalResumeWorker`; assert NO `run/3` is called
    (source-assert governance.ex contains no `.run(` in the approve path);
    (3) record-before-enqueue ordering: the approval `:repo_update`/`:repo_insert` is observed BEFORE
    the captured enqueue (use ordered `assert_receive` or capture order);
    (4) append-only multi-decision trail: a request→approve sequence yields ≥ 2 distinct
    `ToolActionEvent` inserts (no update of prior events);
    (5) transition guarded on `:pending`: `approve/reject/defer` on an already-`:approved`/`:rejected`
    approval returns an error / no-op and does NOT write a new decision (T-force-resolved);
    (6) **D15-15 / WR-01:** the `:needs_input` blocked path (changeset reason) persists a humanized
    reason — assert the persisted `policy_snapshot` AND the `ToolActionEvent.reason` contain NO
    `"#Ecto.Changeset<"` substring (`refute String.contains?(persisted, "#Ecto.Changeset<")`), and
    `:needs_input` STILL persists a row.
    Mark sub-cases needing the not-yet-existing facade `@tag :skip` so the file compiles and runs now.

    Extend `test/cairnloop/governance/tool_action_event_test.exs` (15-VALIDATION row 15-01-c): add a
    describe block asserting the 9 new approval `@event_type_values` atoms (`:approval_requested`,
    `:approved`, `:rejected`, `:deferred`, `:expired`, `:invalidated`, `:resume_scheduled`,
    `:revalidation_passed`, `:revalidation_failed`) are accepted by `ToolActionEvent.changeset/2`
    when `from_status: nil, to_status: nil` and the transition is carried in
    `metadata: %{approval_status: ..., new_approval_status: ...}`. Document the expectation that
    `to_status` is NOT required for approval event_types (Wave 1 relaxes the changeset). Keep the
    existing append-only invariant tests intact.

    Extend `test/cairnloop/governance/preview_test.exs` (15-VALIDATION row 15-05-a, D15-14): add a
    describe block "snapshotted prose vs live divergence". Build two `%ToolProposal{}` fixtures with
    a populated `rendered_consequence`/`title` that DIFFER from what live `Preview.render/1` would
    produce for the same `tool_ref`; assert the approval surface (presenter / card precompute) uses
    the snapshotted `proposal.rendered_consequence`/`proposal.title` columns, NOT the live render.
    Also assert a pre-Phase-15 NULL-snapshot row falls back to the structured-summary card (P14
    D-17), never live prose. `@tag :skip` the parts needing the not-yet-added columns.

    Extend `test/cairnloop/web/tool_proposal_presenter_test.exs` (15-VALIDATION rows 15-04-a/b,
    D15-16): add describe blocks asserting
    (1) `status_group/1` maps approval atoms with ZERO relabeling — `:pending_approval`→`:awaiting`,
    `:execution_pending`→`:active`, `:rejected`/`:deferred`/`:expired`/`:invalidated`→`:done`;
    (2) `approval_outlook` (or `approval_outlook_for_approval/1`) returns real "Pending approval"
    calm copy for an active `:pending` approval (no future-tense "Will require");
    (3) `history_line/1` returns humanized, non-`"Workflow updated"` lines for every new approval
    event_type, showing `actor_id` and `reason` where applicable, with NO raw Elixir terms (refute
    the output contains `"#Ecto"`, `":"`-prefixed atoms, or `"%{"`).
    Mark not-yet-implemented clauses `@tag :skip` so the file compiles and runs now.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/governance_test.exs test/cairnloop/governance/tool_action_event_test.exs test/cairnloop/governance/preview_test.exs test/cairnloop/web/tool_proposal_presenter_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "get_active_approval" test/cairnloop/governance_test.exs` returns ≥ 1.
    - `test/cairnloop/governance_test.exs` contains a test asserting the persisted reason for `:needs_input` has no `#Ecto.Changeset<` substring (D15-15).
    - `test/cairnloop/governance_test.exs` contains a test asserting `approve/3` enqueues a job carrying `%{"approval_id" => ...}` via an injected `enqueue_fn` (APRV-01).
    - `grep -c ":revalidation_failed\|:revalidation_passed\|:approval_requested" test/cairnloop/governance/tool_action_event_test.exs` returns ≥ 1.
    - `test/cairnloop/governance/preview_test.exs` contains a divergence test referencing `rendered_consequence`.
    - `test/cairnloop/web/tool_proposal_presenter_test.exs` contains a `status_group` test for `:execution_pending` → `:active` and `:rejected` → `:done`.
    - Existing previously-passing tests in all four files still pass (no regressions).
    - `mix compile --warnings-as-errors` clean; `mix test {four files}` runs to completion (skip/red allowed for not-yet-built behavior; no compile failures).
  </acceptance_criteria>
  <done>All four existing test files are extended with the approval-surface describe blocks; the suite compiles warnings-clean and runs; new behavior tests are red/skipped pending Waves 1-4, existing tests unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → modules-under-test | Wave 0 tests reference modules/functions that do not exist yet; references must not break the `--warnings-as-errors` build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-W0-01 | Tampering | Test files referencing undefined modules/functions | mitigate | Reference not-yet-existing modules only via runtime aliases / `@tag :skip` tests so `mix compile --warnings-as-errors` stays clean; never via compile-time macro expansion. |
| T-15-W0-02 | Information Disclosure | Repo-bound tests run against an unavailable Postgres in this workspace | accept | DB-round-trip legs (partial unique index, JSONB, expires_at column) are written as `# REPO-UNAVAILABLE` stubs per CLAUDE.md; headless MockRepo path is the merge-blocking coverage. |
| T-15-W0-SC | Tampering | mix/external installs | accept | No package installs in Phase 15 (15-RESEARCH "Package Legitimacy Audit: Not applicable"). No new deps introduced by Wave 0. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean across the repo after both tasks.
- `mix test test/cairnloop/governance/tool_approval_test.exs test/cairnloop/workers/approval_resume_worker_test.exs test/cairnloop/workers/approval_expiry_worker_test.exs test/cairnloop/governance_test.exs test/cairnloop/governance/tool_action_event_test.exs test/cairnloop/governance/preview_test.exs test/cairnloop/web/tool_proposal_presenter_test.exs` runs to completion.
- Pre-existing baseline test failures / `*.Repo` boot noise (per CLAUDE.md + STATE.md) are NOT introduced or worsened by this plan; report them honestly if present.
</verification>

<success_criteria>
- Seven test files exist (3 new, 4 extended) encoding the FLOW-03 + APRV-01..04 + D15-12/14/15/16 behavior contracts from 15-VALIDATION.md.
- All seven compile warnings-clean and the suite runs (Wave-0 red/skip is expected and correct).
- No regression to existing passing tests.
</success_criteria>

<output>
Create `.planning/phases/15-approval-state-machine-oban-resume/15-00-SUMMARY.md` when done.
</output>
</content>
</invoke>
