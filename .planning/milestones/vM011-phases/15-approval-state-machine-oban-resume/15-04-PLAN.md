---
phase: 15-approval-state-machine-oban-resume
plan: 04
type: execute
wave: 4
depends_on: ["15-01", "15-03"]
files_modified:
  - lib/cairnloop/web/tool_proposal_presenter.ex
  - lib/cairnloop/web/conversation_live.ex
autonomous: true
requirements: [FLOW-03, APRV-01, APRV-03]

must_haves:
  truths:
    - "The presenter maps approval states into the EXISTING four display groups with zero relabeling (D15-16): Awaiting = active `:pending`; Active = `:approved`/`:execution_pending`; Done = `:rejected`/`:deferred`/`:expired`/`:invalidated`; Blocked unchanged."
    - "`approval_outlook` becomes the real 'Pending approval' status (calm, present-tense) when an active `:pending` approval exists, replacing the Phase-14 future-tense honesty-seam copy."
    - "`history_line/1` produces humanized, non-`'Workflow updated'` lines for every new approval event type, showing `decided_by`/reason where applicable, with NO raw Elixir terms/JSON (D15-17)."
    - "The Phase-14 footer action slot renders Approve / Reject / Defer affordances (with reason capture for reject/defer); status is conveyed by text AND color, never color-alone (brand §7.5)."
    - "`handle_event(\"approve\"/\"reject\"/\"defer\")` only calls the Governance facade (persist + enqueue) then reloads via the existing thin-notification → `reload_conversation_with_context` path — plain-assign, no streams, no inline execution (APRV-01, P14 D-02)."
    - "The approval card reads the snapshotted `proposal.rendered_consequence`/`proposal.title`, NEVER live `Preview.render/1` (D15-14); NULL-snapshot rows fall back to the structured-summary card."
  artifacts:
    - path: "lib/cairnloop/web/tool_proposal_presenter.ex"
      provides: "Approval status_group/approval_outlook/history_line mappings (D15-16), zero relabeling"
      contains: "def status_group(:execution_pending)"
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "Footer-slot Approve/Reject/Defer handlers + reload reflection (FLOW-03/APRV-01)"
      contains: "handle_event(\"approve"
  key_links:
    - from: "lib/cairnloop/web/conversation_live.ex"
      to: "Cairnloop.Governance.approve/3"
      via: "footer-slot handle_event persists + enqueues, then reload (never inline execute)"
      pattern: "Cairnloop.Governance.(approve|reject|defer)"
    - from: "lib/cairnloop/web/conversation_live.ex"
      to: "proposal.rendered_consequence"
      via: "card reads snapshotted prose, never live Preview.render"
      pattern: "rendered_consequence"
    - from: "lib/cairnloop/web/tool_proposal_presenter.ex"
      to: "the four display groups"
      via: "status_group mapping for approval states (zero relabeling)"
      pattern: "status_group\\(:"
---

<objective>
Reflect approval, expiry, and resume outcomes back into the in-thread operator workflow
(M011-S03-04) by extending the existing Phase-14 read-only surface — NOT building a second console.
The presenter maps the new approval states into the existing Awaiting/Active/Done/Blocked groups
with ZERO relabeling (D15-16), repurposes the deliberately-named `approval_outlook` honesty seam into
the real "Pending approval" status, and humanizes every approval event in `history_line/1`. The
ConversationLive footer action slot (reserved empty in Phase 14, D-05) gains Approve / Reject / Defer
affordances that call the Governance facade and reflect outcomes via the existing thin-notification →
`reload_conversation_with_context` reload — plain-assign, no streams (P14 D-02), no inline execution
(APRV-01). The card reads the snapshotted prose columns, never live `Preview.render/1` (D15-14).

Purpose: This is where the durable approval machinery becomes operator-usable. It depends on plan
15-01 (snapshot columns, has_one(:approval), get_active_approval) and plan 15-03 (the
approve/reject/defer facade the handlers call). Output: presenter + LiveView extensions; Wave 0
presenter + (extended) conversation_live tests turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-approval-state-machine-oban-resume/15-CONTEXT.md
@.planning/phases/15-approval-state-machine-oban-resume/15-PATTERNS.md
@.planning/phases/14-operator-timeline-preview-surface/14-CONTEXT.md
@prompts/cairnloop_brand_book.md
@CLAUDE.md

<interfaces>
<!-- Contracts this plan CONSUMES (Wave-1 record + Wave-3 facade) + DEFINES (presenter/UI). -->

This plan consumes:
- `Cairnloop.Governance.approve/3`, `reject/3` (`reason:` opt), `defer/3` (`reason:` opt) — the
  Wave-3 facade the footer handlers call; each persists + (approve) enqueues, never executes inline.
- `Cairnloop.Governance.get_active_approval/1` — the single `:pending` approval (or nil) for a
  proposal; `list_proposals_for_conversation/1` preloads `:approval` (extend its preload in this plan
  OR access the active approval per-proposal — keep it through the narrow facade, D15-17).
- `proposal.rendered_consequence` / `proposal.title` — snapshotted prose columns (plan 15-01); the
  card reads these, NEVER live `Preview.render/1` (D15-14).
- `proposal.approval` (has_one, plan 15-01) — the approval record the card/presenter dispatches on.

This plan extends (CONFIRMED seams — read before editing):
- `lib/cairnloop/web/tool_proposal_presenter.ex` — `status_group/1` L55-59 (add approval atoms ABOVE
  the catch-all, NO relabeling); `approval_outlook/1` L111-114 (add real `:pending`-approval copy /
  an `approval_outlook_for_approval/1` taking the approval struct); `history_line/1` clauses L259-269
  (add approval event_type clauses ABOVE the `"Workflow updated"` catch-all at L269 — D-24).
- `lib/cairnloop/web/conversation_live.ex` — `handle_event("execute_tool")` L175-199 (the
  facade-call→flash→noreply pattern to mirror); `reload_conversation_with_context/2` L214-227 (the
  reload path that loads governed_actions via the facade — preserve; no streams); the
  `governed_action_card/1` precompute block L843-859 (add approval-aware values: active approval,
  snapshotted prose); the EMPTY footer slot at L1034-1036 (Phase-15 affordances land here).
- 15-PATTERNS.md — Pattern 9 (presenter extensions: status_group, approval_outlook_for_approval,
  history_line clauses) + Pattern 10 (the approve/reject/defer handle_event handlers).
- brand book §13.2 ("Approval required"/"Blocked by policy" register), §7.5 (never color-alone — pair
  chip color WITH text), §10.2 (rail layout), §2.2/§7 (brand tokens `var(--cl-primary, #A94F30)`
  over hex), §5.3/§5.6 (reason-forward, no raw terms).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ToolProposalPresenter for approval states (status_group, approval_outlook, history_line)</name>
  <files>lib/cairnloop/web/tool_proposal_presenter.ex</files>
  <read_first>
    - lib/cairnloop/web/tool_proposal_presenter.ex (the WHOLE presenter: status_group/1 L55-59, status_group_label/1 L62-66, approval_outlook/1 L111-114, reason_label/1 L138-156, history_line/1 + catch-all L259-269)
    - lib/cairnloop/governance/tool_approval.ex (the approval status enum + decided_by/reason fields the presenter renders)
    - lib/cairnloop/governance/tool_action_event.ex (the approval event types history_line dispatches on)
    - .planning/phases/14-operator-timeline-preview-surface/14-CONTEXT.md (D-10 four groups zero-relabeling; D-12 approval_outlook honesty seam; D-13 status⊥risk⊥approval axes; D-24 history_line catch-all)
    - prompts/cairnloop_brand_book.md (§13.2 approval register; §5.3/§5.6 reason-forward, no raw terms; §7.5 never color-alone)
    - .planning/phases/15-approval-state-machine-oban-resume/15-PATTERNS.md (Pattern 9 — the exact status_group / approval_outlook_for_approval / history_line blocks)
  </read_first>
  <behavior>
    - status_group(:pending_approval) → :awaiting; status_group(:execution_pending) → :active; status_group(:rejected|:deferred|:expired|:invalidated) → :done. Existing proposal-status mappings unchanged.
    - approval_outlook_for_approval(%{status: :pending}) returns calm present-tense "Pending approval" copy (no "Will require").
    - approval_outlook_for_approval covers approved/execution_pending/rejected/deferred/expired/invalidated with reason-forward copy; unknown → nil (total).
    - history_line/1 returns humanized non-"Workflow updated" lines for all 9 approval event types, showing actor/reason where present; the D-24 catch-all remains LAST.
    - No presenter output contains raw Elixir terms (no "#Ecto", ":atom"-style, "%{").
  </behavior>
  <action>
    Extend `lib/cairnloop/web/tool_proposal_presenter.ex` (pure, total, no markup — the existing
    presenter idiom). All additions go ABOVE the relevant catch-alls so existing clauses are
    untouched (D-13 status⊥risk⊥approval axes; D-10 zero relabeling).

    (1) `status_group/1`: add `def status_group(:pending_approval), do: :awaiting`,
    `def status_group(:execution_pending), do: :active`, and `:rejected`/`:deferred`/`:expired`/
    `:invalidated` → `:done` — ABOVE the existing `def status_group(_), do: :blocked` at L59. Do NOT
    change `status_group_label/1` (the four group names are fixed — D15-16).

    (2) Add `approval_outlook_for_approval/1` taking a `%ToolApproval{}` (or a map with `:status`/
    `:reason`): `:pending` → calm present-tense "Pending approval — an operator must approve, reject,
    or defer this action." (brand §13.2); `:approved`/`:execution_pending` → "Approved — re-checking
    policy before it runs." / "Approved — ready to run."; `:rejected`/`:deferred` → reason-forward
    "Rejected: <reason>" / "Deferred: <reason>" (§5.6); `:expired` → "Approval request expired.";
    `:invalidated` → "Approval invalidated — policy or scope changed since approval."; `_` → nil
    (total). Leave the existing `approval_outlook/1` (the `approval_mode` future-tense seam) in place
    for proposals with NO active approval; the card chooses which to show.

    (3) `history_line/1`: add clauses for `:approval_requested`, `:approved`, `:rejected`,
    `:deferred`, `:expired`, `:invalidated`, `:resume_scheduled`, `:revalidation_passed`,
    `:revalidation_failed` ABOVE the `def history_line(%ToolActionEvent{}), do: "Workflow updated"`
    catch-all at L269. Show `actor_id` and `reason` where present (reason-forward, §5.6); use
    `reason_label/1` or the operator-supplied reason string — NEVER raw terms (§5.3). Keep the D-24
    catch-all LAST.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/web/tool_proposal_presenter_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def status_group(:execution_pending)" lib/cairnloop/web/tool_proposal_presenter.ex` returns 1, mapping to `:active`.
    - `grep -cE "def status_group\\(:(rejected|deferred|expired|invalidated)\\)" lib/cairnloop/web/tool_proposal_presenter.ex` returns ≥ 4, all → `:done`.
    - `grep -c "approval_outlook_for_approval" lib/cairnloop/web/tool_proposal_presenter.ex` returns ≥ 1.
    - `grep -cE "event_type: :(approval_requested|approved|rejected|deferred|expired|invalidated|revalidation_passed|revalidation_failed|resume_scheduled)" lib/cairnloop/web/tool_proposal_presenter.ex` returns ≥ 9.
    - The `def history_line(%ToolActionEvent{}), do: "Workflow updated"` catch-all is still the LAST history_line clause (still present).
    - `mix test test/cairnloop/web/tool_proposal_presenter_test.exs` — status_group, approval_outlook, and history_line approval describe blocks pass; `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>The presenter maps approval states into the existing four groups (zero relabeling), repurposes approval_outlook into real "Pending approval" copy, and humanizes every approval event in history_line (no raw terms); Wave 0 presenter tests green; build warnings-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Approve/Reject/Defer into the ConversationLive footer slot + read snapshotted prose</name>
  <files>lib/cairnloop/web/conversation_live.ex</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex (the `handle_event("execute_tool")` facade-call→flash→noreply at L175-199; `reload_conversation_with_context/2` L214-227; the governed_action_card/1 precompute L843-859; the EMPTY footer slot L1034-1036; the `.governed-action-footer` style L502; the approval_outlook assign L854/L915/L952)
    - lib/cairnloop/web/tool_proposal_presenter.ex (the Task-1 approval_outlook_for_approval/1 + status_group the card now calls)
    - lib/cairnloop/governance.ex (approve/3, reject/3, defer/3, get_active_approval/1, list_proposals_for_conversation/1 — the facade the handlers + reload use; preload :approval)
    - lib/cairnloop/governance/tool_proposal.ex (rendered_consequence/title columns + has_one(:approval) the card reads)
    - prompts/cairnloop_brand_book.md (§7.5 never color-alone — chip color + text; §10.2 rail layout; §2.2/§7 brand token var(--cl-primary, #A94F30) over hex; §13.2 calm approval copy)
    - .planning/phases/15-approval-state-machine-oban-resume/15-PATTERNS.md (Pattern 10 — the approve/reject/defer handle_event handlers; the reload_conversation_with_context preload note)
    - .planning/phases/14-operator-timeline-preview-surface/14-CONTEXT.md (D-05 footer slot; D-02 no streams; D-17 NULL-snapshot structured-summary fallback)
  </read_first>
  <behavior>
    - The card precompute reads the active approval (proposal.approval / get_active_approval) and snapshotted proposal.rendered_consequence/title — NEVER calls live Preview.render/1.
    - When an active :pending approval exists, the footer slot renders Approve / Reject / Defer affordances (reject/defer capture a reason); status chip shows color AND text (never color-alone).
    - handle_event("approve") calls Cairnloop.Governance.approve/3 then reloads; success flashes + reflects new state; never executes inline.
    - handle_event("reject"/"defer") call reject/3 / defer/3 with the captured reason; missing reason → error flash, nothing executed (FLOW-03).
    - All reflection goes through reload_conversation_with_context (plain-assign, no streams).
  </behavior>
  <action>
    In `lib/cairnloop/web/conversation_live.ex`:

    (1) **Card precompute (L843-859):** add approval-aware values — resolve the active approval
    (`proposal.approval` if preloaded, else `Cairnloop.Governance.get_active_approval(proposal.id)`
    through the facade; D15-17) and compute `approval_outlook =
    ToolProposalPresenter.approval_outlook_for_approval(approval)` when an approval exists, falling
    back to the existing `approval_outlook(proposal.approval_mode)` when none. Read the consequence/
    title prose from the SNAPSHOTTED columns `proposal.rendered_consequence` / `proposal.title`
    (D15-14) — NEVER call live `Preview.render/1` from the card. When the snapshot column is NULL
    (pre-Phase-15 rows), fall back to the structured-summary card already built from the snapshot
    (P14 D-17) — NOT live prose.

    (2) **Footer slot (L1034-1036):** replace the empty Phase-15-reserved slot with Approve / Reject
    / Defer affordances, rendered ONLY when an active `:pending` approval exists (use the resolved
    approval + `status_group`). Approve is a `phx-click="approve"` button with
    `phx-value-approval-id`. Reject and Defer capture a reason (a small inline form / textarea posting
    `reason`) — reason-required is enforced server-side by the facade (FLOW-03); show the requirement
    in calm copy (§13.2). Convey status by chip color AND text label (never color-alone, §7.5); use
    the brand token `var(--cl-primary, #A94F30)` for primary affordance color, not hardcoded hex
    (§2.2/§7). Keep rail layout per §10.2.

    (3) **handle_event handlers:** add `handle_event("approve", %{"approval-id" => id}, socket)`,
    `handle_event("reject", %{"approval-id" => id, "reason" => reason}, socket)`,
    `handle_event("defer", %{"approval-id" => id, "reason" => reason}, socket)` mirroring the
    `execute_tool` handler shape (Pattern 10). Each: derive `actor_id` from the conversation/host
    context, call `Cairnloop.Governance.approve/reject/defer` (reject/defer pass `reason:`), on
    `{:ok, _}` → `put_flash(:info, ...)` + `reload_conversation_with_context(socket,
    socket.assigns.conversation.id)`, on `{:error, _}` → calm `put_flash(:error, ...)` (for
    reject/defer, "A reason is required."). NEVER call `run/3` / execute inline (APRV-01) — the
    handler only persists + (approve) enqueues via the facade, then reloads.

    (4) **Reload preload:** ensure `reload_conversation_with_context/2` (L214-227) loads
    governed_actions with the active `:approval` available (extend
    `list_proposals_for_conversation/1`'s preload in plan 15-01's facade OR resolve per-proposal via
    `get_active_approval/1` here — keep it through the narrow facade). Stay plain-assign — NO
    `Phoenix.LiveView.stream/3` (P14 D-02).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/web/conversation_live_test.exs test/cairnloop/web/tool_proposal_presenter_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "handle_event\\(\"(approve|reject|defer)\"" lib/cairnloop/web/conversation_live.ex` returns ≥ 3.
    - `grep -cE "Cairnloop.Governance.(approve|reject|defer)" lib/cairnloop/web/conversation_live.ex` returns ≥ 3.
    - The approve/reject/defer handlers contain NO inline execution: `grep -cE "\\.run\\(|run/3|Governance.execute" lib/cairnloop/web/conversation_live.ex` returns 0.
    - `grep -c "rendered_consequence" lib/cairnloop/web/conversation_live.ex` returns ≥ 1 (card reads the snapshot).
    - The card does NOT call live Preview.render: `grep -c "Preview.render" lib/cairnloop/web/conversation_live.ex` returns 0.
    - No streams introduced: `grep -c "LiveView.stream\\|stream(" lib/cairnloop/web/conversation_live.ex` returns 0.
    - Brand token used for the affordance color: `grep -c "var(--cl-primary" lib/cairnloop/web/conversation_live.ex` returns ≥ 1 (no new hardcoded hex for the footer affordances).
    - `mix test test/cairnloop/web/conversation_live_test.exs test/cairnloop/web/tool_proposal_presenter_test.exs` — footer-render, approve/reject/defer handler, color+text, and reload-reflection tests pass; `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>The footer slot renders Approve/Reject/Defer (reason-captured) with color+text status; the handlers call the facade (persist + enqueue, never inline execute) and reflect via plain-assign reload (no streams); the card reads snapshotted prose, never live Preview.render; Wave 0 conversation_live + presenter tests green; build warnings-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (browser) → ConversationLive handle_event | Approve/Reject/Defer clicks cross from the UI into durable decisions; handlers must persist + enqueue, never execute inline. |
| approval surface → displayed prose | The card could re-read live registry prose that drifted from propose-time; must read the snapshot only. |
| approval state → operator perception | State conveyed by color alone is inaccessible/ambiguous; must pair color with text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 (T-inline-exec) | Availability | `handle_event("approve")` | mitigate | Handler only calls `Cairnloop.Governance.approve/3` (persist + enqueue) then reloads; NO `run/3`, no synchronous resume — all execution async (APRV-01). |
| T-15-16 (T-trust-drift) | Spoofing / Integrity | Approval card prose | mitigate | Card reads snapshotted `proposal.rendered_consequence`/`title` columns; NEVER live `Preview.render/1`; NULL rows fall back to the structured-summary card (D15-14 / P14 D-17). Divergence asserted by the preview_test fixture (plan 15-01 / Wave 0). |
| T-15-17 (T-raw-terms) | Information Disclosure | history_line / approval_outlook copy | mitigate | All operator copy is humanized via the presenter (reason_label / operator reason strings); no raw Elixir terms/JSON rendered (§5.3); presenter test refutes raw-term substrings. |
| T-15-18 (T-color-alone) | Information Disclosure (accessibility) | Status chip / affordances | mitigate | Status conveyed by text label AND color (brand §7.5); brand token `var(--cl-primary, #A94F30)` over hardcoded hex (§2.2/§7); asserted by the LiveView render test. |
| T-15-19 (T-reason-required) | Tampering | Reject/Defer without a reason | mitigate | Facade `reject/3`/`defer/3` enforce reason via decision_changeset (FLOW-03); handler surfaces a calm "A reason is required." error; nothing persisted/executed. |
| T-15-SC | Tampering | mix/external installs | accept | No package installs in Phase 15 (15-RESEARCH Package Legitimacy Audit: not applicable). No new deps. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean across the repo.
- `mix test test/cairnloop/web/conversation_live_test.exs test/cairnloop/web/tool_proposal_presenter_test.exs` — footer affordances, approve/reject/defer handlers (persist+enqueue, no inline exec), color+text status, snapshot-read, and plain-assign reload behaviors green.
- No streams, no live Preview.render on the card, no inline execution (all source-asserted).
- Pre-existing `*.Repo` boot noise (CLAUDE.md/STATE.md) not introduced or worsened; report honestly.
- Visual brand compliance (footer layout, calm copy, chip color+text pairing) is a Manual-Only verification (15-VALIDATION.md) — note it for the operator, do not block on it here.
</verification>

<success_criteria>
- Approval states reflect into the existing four groups with zero relabeling; approval_outlook is the real "Pending approval" status (D15-16).
- Footer slot renders Approve/Reject/Defer (reason-captured, color+text); handlers persist + enqueue via the facade and never execute inline (APRV-01, FLOW-03).
- Card reads snapshotted prose, never live Preview.render (D15-14); reflection is plain-assign reload, no streams (P14 D-02).
- No regression to existing passing presenter/LiveView tests.
</success_criteria>

<output>
Create `.planning/phases/15-approval-state-machine-oban-resume/15-04-SUMMARY.md` when done.
</output>
</content>
