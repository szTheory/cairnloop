---
phase: 14-operator-timeline-preview-surface
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - lib/cairnloop/web/conversation_live.ex
autonomous: true
requirements: [FLOW-02]
user_setup: []

must_haves:
  truths:
    - "`governed_action_card/1` is a function component (not a LiveComponent) that renders any of the four Phase-13 statuses without crashing."
    - "The status chip pairs a color tone WITH a text label — never color-alone (brand §7.5)."
    - "Status, risk_tier, and approval_mode render as SEPARATE display axes — never fused into one badge (D-13)."
    - "Inline content is humanized; raw snapshot maps appear ONLY behind an explicit expander (D-22); the consequence/structured-summary comes from `Preview.render/1`."
    - "The card has a footer action slot that is structurally present but EMPTY in Phase 14 (read-only — D-05); no approve/reject/defer affordances exist."
    - "An empty or not-loaded event trail renders a calm 'No history yet' rather than raising (`Ecto.assoc_loaded?` guard, D-24)."
  artifacts:
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "governed_action_card/1 function component"
      contains: "def governed_action_card"
  key_links:
    - from: "lib/cairnloop/web/conversation_live.ex (governed_action_card/1)"
      to: "Cairnloop.Web.ToolProposalPresenter"
      via: "status/risk/approval/reason/input/scope/policy/history label calls"
      pattern: "ToolProposalPresenter\\."
    - from: "lib/cairnloop/web/conversation_live.ex (governed_action_card/1)"
      to: "Cairnloop.Governance.Preview.render/1"
      via: "consequence prose vs structured summary branch"
      pattern: "Preview.render"
---

<objective>
Build the `governed_action_card/1` function component — the read-only preview card that renders one
`ToolProposal` (with its preloaded event trail) in the right evidence rail. It mirrors the existing
`quick_fix_card/1` / `draft_audit_card/1` rail idiom but, unlike `draft_audit_card`, has NO action
buttons (Phase 14 is read-only — D-05); a footer action slot is reserved structurally for Phase 15.

The card consumes the Wave-1 layer ONLY: all labels/tones/rows come from
`Cairnloop.Web.ToolProposalPresenter`, the consequence-vs-structured-summary decision comes from
`Cairnloop.Governance.Preview.render/1`. The card never branches on footgun internals, never reads
live config for trust fields, and never dumps raw maps inline.

Purpose: Deliver FLOW-02's human-readable preview card surface (risk label, actor scope, target,
consequence summary, durable event trail) as a stateless render of immutable snapshot data.
Output: One new function component in `conversation_live.ex`, turning the Wave-0 card-render tests
green.

NOTE: This plan and Wave 3 both modify `lib/cairnloop/web/conversation_live.ex`. They run in
SEPARATE waves (2 then 3) precisely because they share this file — never run them in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-operator-timeline-preview-surface/14-CONTEXT.md
@.planning/phases/14-operator-timeline-preview-surface/14-RESEARCH.md
@CLAUDE.md
@prompts/cairnloop_brand_book.md

<interfaces>
<!-- Confirmed contracts. The card consumes Wave-1 outputs and existing rail/humanize helpers. -->

Consume (built in Wave 1 — 14-01):
- `Cairnloop.Web.ToolProposalPresenter` total functions: `status_label/1`, `status_group/1`,
  `status_group_label/1`, `status_meaning/1`, `risk_tier_label/1`, `risk_tier_tone/1` (atom →
  LiveView maps to color), `approval_mode_label/1`, `approval_outlook/1`, `reason_label/1`,
  `input_rows/1`, `scope_summary/1`, `policy_explanation/1`, `block_reason_copy/1`, `history_line/1`,
  `event_timestamp_label/1`, `trace_metadata/1`.
- `Cairnloop.Governance.Preview.render(proposal) :: {:preview, String.t()} | {:structured, map()}`.

Mirror / reuse (existing in conversation_live.ex):
- `quick_fix_card/1` (L457) — rail-card idiom: `rail-card` CSS class, eyebrow, h3, summary, status rail.
- `draft_audit_card/1` (L582) — HAS action buttons; the governed card must NOT (D-05). Use only its layout shape.
- `context_section/1` (L552), `context_field/1` (L569, `hide_label` assign), `humanize_context_label/1` (L670), `normalize_context_value/1` (L679-691, "Unsupported value" sentinel) — humanize-don't-dump renderers.
- `last_module_part/1` + `humanize_context_label/1` (used in `tool_renderer/1` L521-523) — tool-ref humanization for the title fallback.
- `search_modal_component.ex` badge/chip idiom (`source_badge_style/1`, ~L131-136 + ~L300) — inline-style chip pattern; brand primary `var(--cl-primary, #A94F30)`.

Card input:
- `attr :proposal, :map, required: true` — a `%Cairnloop.Governance.ToolProposal{}` with `events`
  preloaded (may be `[]` or `%Ecto.Association.NotLoaded{}` — guard with `Ecto.assoc_loaded?/1`).

Render source (built in Wave 0, asserted here):
- `test/cairnloop/web/conversation_live_test.exs` — `render_component(&governed_action_card/1, ...)` tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: governed_action_card/1 function component (status chip, risk/approval meta, outlook sub-line, input rows, event mini-timeline, scope/policy, trace, empty footer slot)</name>
  <files>lib/cairnloop/web/conversation_live.ex</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex L457-501 (quick_fix_card/1 — the rail-card idiom to clone), L582-668 (draft_audit_card/1 — layout shape; note its action buttons must NOT be copied, D-05), L552-578 (context_section/1 + context_field/1), L670-691 (humanize_context_label/1 + normalize_context_value/1), L503-548 (tool_renderer/1 — last_module_part + chip placement reference)
    - lib/cairnloop/web/search_modal_component.ex (badge/chip inline-style idiom; brand-token usage; the quick-fix CSS at L346 `background: #a94f30` and chip-with-label pattern at L353)
    - lib/cairnloop/web/tool_proposal_presenter.ex (Wave-1 — every label/tone/row helper the card calls)
    - lib/cairnloop/governance/preview.ex (Wave-1 — `render/1` return shapes `{:preview, str}` / `{:structured, map}`)
    - prompts/cairnloop_brand_book.md §7.5 (never state-by-color-alone), §10.2 (rail layout), §5.6 (no raw terms)
    - test/cairnloop/web/conversation_live_test.exs (the Wave-0 card-render tests this task turns green)
  </read_first>
  <action>
    Add a `governed_action_card/1` function component to `conversation_live.ex` (alongside
    `quick_fix_card/1` / `draft_audit_card/1`), declared with `attr :proposal, :map, required: true`.
    It is a FUNCTION component (not a LiveComponent — D-03), a stateless render of immutable snapshot
    data; the parent LiveView owns all events. Precompute presenter values into assigns before the
    `~H` block (the in-repo pattern) so the template stays declarative.

    Card structure (D-21 display scope; exact markup/CSS/expander mechanism are executor discretion
    within brand tokens, D-27):
    - eyebrow + headline: the consequence preview from `Preview.render/1` — on `{:preview, str}` show
      `str` labelled as a "current description"; on `{:structured, summary}` show the structured
      title (the title fallback chain) — never a raw module atom, never a raw map.
    - status chip: render `ToolProposalPresenter.status_label/1` TEXT plus a color derived from the
      status group / risk tone — the chip element ALWAYS contains the text label (never color-alone,
      brand §7.5). Map the presenter's tone ATOM (`:info|:warning|:danger`) to a brand-token color in
      the LiveView; do NOT push color decisions into the presenter.
    - meta line: `risk_tier_label/1` and `approval_mode_label/1` as SEPARATE axes from the status chip
      (D-13 — never fuse risk/approval/status into one badge).
    - `approval_outlook/1` sub-line: render only when non-nil; future-tense, non-actionable (D-12).
    - input snapshot: render `ToolProposalPresenter.input_rows/1` via the humanize-don't-dump
      `context_field/1` idiom; the raw `input_snapshot` map appears ONLY inside a `<details>` expander
      (D-22 — inline humanized, raw behind opt-in expander).
    - event mini-timeline: iterate the preloaded `proposal.events` (guard `Ecto.assoc_loaded?/1`;
      render a calm "No history yet" when empty/not-loaded — D-24) as compact one-line entries via
      `history_line/1` + `event_timestamp_label/1`; full per-event detail (reason, metadata) behind a
      `<details>` expander.
    - scope snapshot: render `scope_summary/1`; on `status == :scope_invalid`, surface the missing
      scopes prominently (D-21).
    - policy snapshot: render `policy_explanation/1` as one calm "why this gate" sentence; the raw
      policy map only behind a `<details>` expander (D-22). This is display only — NOT OBS-02
      attribution.
    - trace metadata: render `trace_metadata/1` de-emphasized (mono, copyable): proposal id,
      tool_ref/tool_version, short idempotency key.
    - footer action slot: include a structurally-present but EMPTY footer region (e.g. a
      `<div class="governed-action-footer">` with a comment marking it as the Phase-15 affordance
      slot) — NO approve/reject/defer buttons in Phase 14 (D-05). Do NOT copy `draft_audit_card`'s
      action buttons.

    Add minimal scoped CSS for the new card classes following the existing rail-card / quick-fix CSS
    idiom; use brand tokens (`var(--cl-primary, #A94F30)`), never new hardcoded SaaS hex. Do NOT wire
    the card into the rail in this plan — that is Wave 3 (`reload_conversation_with_context` assign +
    rail section). This plan only DEFINES the component and its render tests.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/web/conversation_live_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def governed_action_card" lib/cairnloop/web/conversation_live.ex` ≥ 1.
    - The component references the Wave-1 layer: `grep -c "ToolProposalPresenter\." lib/cairnloop/web/conversation_live.ex` ≥ 1 and `grep -c "Preview.render" lib/cairnloop/web/conversation_live.ex` ≥ 1.
    - No LiveComponent introduced for the card: `grep -c "use Phoenix.LiveComponent" lib/cairnloop/web/conversation_live.ex` returns `0`.
    - The card defines a footer action slot but renders NO approve/reject/defer button: the Wave-0 render test asserts the footer region exists and contains no `phx-click` approve/reject/defer affordance (D-05).
    - Render tests pass for all four statuses without crashing, including the empty/not-loaded events "No history yet" path (`Ecto.assoc_loaded?` guard).
    - The status chip render includes the status LABEL text (color-plus-text, never color-alone) — asserted in the Wave-0 render test.
    - No new hardcoded `#2563eb`-style SaaS hex introduced: `grep -c "#2563eb" lib/cairnloop/web/conversation_live.ex` returns `0` (the Execute-button instance is removed in Wave 3, not added here).
    - `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>`governed_action_card/1` exists as a read-only function component consuming the Wave-1 presenter + Preview, rendering all four statuses with separate risk/approval/status axes, humanized inline content (raw behind expanders), a guarded event mini-timeline, and an empty Phase-15 footer slot — Wave-0 card-render tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot maps → rendered DOM | `input_snapshot` / `scope_snapshot` / `policy_snapshot` values (incl. potential PII) flow into HTML. |
| host prose (`{:preview, str}`) → DOM | live `preview/1` output rendered to the operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 (T-pii) | Information Disclosure (PII) | inline rendering of snapshot maps | mitigate | Inline content is rendered ONLY via `ToolProposalPresenter.input_rows/1` (the Wave-1 masking choke point) and `context_field/1` humanizers; the raw `input_snapshot`/`policy_snapshot` maps appear ONLY behind an explicit `<details>` expander (D-22). The template never iterates an arbitrary snapshot map directly. |
| T-14-02-02 | Information Disclosure / brand | raw Elixir terms / module atoms in DOM | mitigate | Title comes from the `Preview` fallback chain (never raw module atom); reasons via `reason_label/1`; policy via `policy_explanation/1`. No `inspect`/raw map rendered inline. |
| T-14-02-03 | Tampering / phishing affordance | a misleading "Approve" affordance before Phase 15 exists | mitigate | Footer slot is structurally empty; no approve/reject/defer button rendered (D-05). Render test asserts absence of those affordances — prevents naming a non-existent action (D-12 footgun). |
| T-14-02-04 (XSS) | Tampering | host `preview/1` prose rendered to DOM | mitigate | Render prose via standard HEEx interpolation (auto-HTML-escaped); never `raw/1` on host-supplied strings. Phoenix template escaping is the control. |
| T-14-SC | Tampering | mix/external installs | accept | No package installs in Phase 14 (RESEARCH Package Legitimacy Audit: Not applicable). |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/cairnloop/web/conversation_live_test.exs` green for the card-render describe blocks.
- Visual brand compliance (rail placement, calm tone, chip color+text) is a manual-only check per 14-VALIDATION.md — not gating the automated suite.
- Pre-existing baseline failures / `*.Repo` boot noise reported honestly and not worsened.
</verification>

<success_criteria>
- `governed_action_card/1` is a read-only function component rendering FLOW-02's preview card from the Wave-1 layer.
- Separate risk/approval/status axes; humanized inline content; raw maps behind expanders; empty Phase-15 footer slot; guarded event trail.
- Wave-0 card-render tests green; no LiveComponent; no new SaaS-blue hex.
</success_criteria>

<output>
Create `.planning/phases/14-operator-timeline-preview-surface/14-02-SUMMARY.md` when done.
</output>
