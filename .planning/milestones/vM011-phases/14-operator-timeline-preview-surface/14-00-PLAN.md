---
phase: 14-operator-timeline-preview-surface
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/cairnloop/governance/preview_test.exs
  - test/cairnloop/web/tool_proposal_presenter_test.exs
  - test/cairnloop/governance_test.exs
  - test/cairnloop/web/conversation_live_test.exs
autonomous: true
requirements: [FLOW-01, FLOW-02]

must_haves:
  truths:
    - "Running `mix test` compiles all four governed-action test files without error (red/skipped is fine; broken-compile is not)."
    - "`test/cairnloop/governance/preview_test.exs` exists with describe blocks for the structured-summary common path, the four live-leg fallback triggers, and a string-keyed (simulated JSONB) snapshot variant."
    - "`test/cairnloop/web/tool_proposal_presenter_test.exs` exists with describe blocks for every total presenter function named in D-25."
    - "`test/cairnloop/governance_test.exs` is extended with describe blocks for `list_proposals_for_conversation/1`, conversation_id-on-both-paths, and conversation_id-excluded-from-idempotency-key."
    - "`test/cairnloop/web/conversation_live_test.exs` is extended with a MockRepo that can return governed_actions and describe blocks for governed_action_card rendering + blocked-proposal visibility."
  artifacts:
    - path: "test/cairnloop/governance/preview_test.exs"
      provides: "Headless Preview.render/1 behavior contract (common structured path + four fallback triggers + string-key variant)"
      contains: "Cairnloop.Governance.Preview"
    - path: "test/cairnloop/web/tool_proposal_presenter_test.exs"
      provides: "Pure presenter behavior contract for all D-25 helpers"
      contains: "Cairnloop.Web.ToolProposalPresenter"
    - path: "test/cairnloop/governance_test.exs"
      provides: "Facade + idempotency + conversation-scoping behavior contract"
      contains: "list_proposals_for_conversation"
    - path: "test/cairnloop/web/conversation_live_test.exs"
      provides: "Card render + rail visibility behavior contract"
      contains: "governed_action"
  key_links:
    - from: "test/cairnloop/governance/preview_test.exs"
      to: "Cairnloop.Governance.Preview.render/1"
      via: "module under test (does not exist until Wave 1 — tests are red/skipped initially)"
      pattern: "Preview.render"
    - from: "test/cairnloop/web/tool_proposal_presenter_test.exs"
      to: "Cairnloop.Web.ToolProposalPresenter"
      via: "module under test (does not exist until Wave 1)"
      pattern: "ToolProposalPresenter\\."
---

<objective>
Create the Phase 14 test infrastructure (Wave 0) BEFORE any implementation exists, satisfying the
Nyquist validation contract in 14-VALIDATION.md. Two new headless test files (`preview_test.exs`,
`tool_proposal_presenter_test.exs`) and two extensions (`governance_test.exs`,
`conversation_live_test.exs`) establish the per-task `<automated>` verification surface that Waves
1-3 will turn green.

This phase is a read-only Phoenix LiveView display surface over durable Phase-13 `ToolProposal` +
`ToolActionEvent` records. Nothing executes. The COMMON path (no tool implements `preview/1` yet —
D-17) is the snapshot-built structured summary; tests are designed around that reality, not the
rare live leg.

Purpose: Front-load the test surface so every downstream task has a concrete `<automated>` gate
(Nyquist Dimension 8). The modules these tests reference (`Cairnloop.Governance.Preview`,
`Cairnloop.Web.ToolProposalPresenter`, `Governance.list_proposals_for_conversation/1`) do NOT exist
yet — Wave 0 tests are written red/skipped and turn green in Waves 1-3.
Output: Four test files committed; `mix compile --warnings-as-errors` clean; `mix test` runs (new
governed-action describe blocks may be red/skipped pending implementation — that is correct for
Wave 0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-operator-timeline-preview-surface/14-CONTEXT.md
@.planning/phases/14-operator-timeline-preview-surface/14-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Modules these tests will reference. They do NOT exist yet — Wave 0 tests are red/skipped -->
<!-- until Waves 1-3 implement them. Use these exact names so the tests bind cleanly later. -->

Wave 1 will create (test against these contracts):
- `Cairnloop.Governance.Preview.render(proposal) :: {:preview, String.t()} | {:structured, map()}`
- `Cairnloop.Web.ToolProposalPresenter` — total functions returning strings/atoms (no markup):
  `status_label/1`, `status_group/1`, `status_group_label/1`, `status_meaning/1`,
  `risk_tier_label/1`, `risk_tier_tone/1`, `approval_mode_label/1`, `approval_outlook/1`,
  `reason_label/1`, `input_rows/1`, `scope_summary/1`, `policy_explanation/1`,
  `block_reason_copy/1`, `history_line/1`, `event_timestamp_label/1`, `trace_metadata/1`
- `Cairnloop.Governance.list_proposals_for_conversation/1` (added to existing facade)

Existing structs the tests build fixtures from (confirmed in source):
- `Cairnloop.Governance.ToolProposal` — fields: `status` (`:proposed|:needs_input|:scope_invalid|:policy_denied`),
  `risk_tier` (`:read_only|:low_write|:high_write|:destructive`),
  `approval_mode` (`:auto|:requires_approval|:always_block`), `tool_ref`, `tool_version`,
  `actor_id`, `account_id`, `input_snapshot` (`:map`), `scope_snapshot` (`:map`),
  `policy_snapshot` (`:map`), `events` (has_many), `inserted_at`
- `Cairnloop.Governance.ToolActionEvent` — fields: `event_type`
  (`:proposal_created|:proposal_blocked`), `from_status`, `to_status`, `actor_id`, `reason`,
  `metadata`, `inserted_at`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create headless Preview.render/1 and presenter test files</name>
  <files>test/cairnloop/governance/preview_test.exs, test/cairnloop/web/tool_proposal_presenter_test.exs</files>
  <read_first>
    - test/cairnloop/governance_test.exs (existing MockRepo + inline-fixture idiom; `use ExUnit.Case, async: false`)
    - lib/cairnloop/web/review_task_presenter.ex (the presenter idiom these tests target: `status_label/1`, `history_line/1` + catch-all at L130, `reason_label/1` at L132-138, `metadata_value/2` dual-key lookup at L162-166)
    - lib/cairnloop/web/gap_candidate_presenter.ex (`@reason_labels` map + `humanize_atom/1` fallback at L75-79 — the `reason_label/1` pattern)
    - lib/cairnloop/web/conversation_live.ex L744-753 (`to_result/1` — the in-repo JSONB string-key rehydration precedent the Preview tests simulate)
    - .planning/phases/14-operator-timeline-preview-surface/14-RESEARCH.md (Pattern 1 + Pattern 2 — the exact module shapes; Pitfall 2 — string-key invisibility)
  </read_first>
  <action>
    Create `test/cairnloop/governance/preview_test.exs` (module `Cairnloop.Governance.PreviewTest`,
    `use ExUnit.Case, async: true`) covering `Cairnloop.Governance.Preview.render/1` headless (no
    Repo). Build `%Cairnloop.Governance.ToolProposal{}` fixtures inline (no shared factory — repo
    idiom). Cover, per 14-VALIDATION.md rows 14-01-c / 14-01-d:
    (1) common path — a proposal whose `tool_ref` resolves to a tool that does NOT export `preview/1`
    returns `{:structured, _}` (D-17);
    (2) fallback when `tool_ref` is an unregistered/unknown tool → `{:structured, _}` (D-19 footgun 4);
    (3) fallback when a (hypothetical) live `preview/1` raises → `{:structured, _}` (D-19 try/rescue);
    (4) fallback when live `preview/1` returns a non-string → `{:structured, _}`;
    (5) string-keyed `input_snapshot` variant (e.g. `%{"order_id" => "123"}`) produces the SAME
    structured result as the atom-keyed variant `%{order_id: "123"}` — partial JSONB-round-trip
    simulation (D-19 footgun 1).
    Add a `# REPO-UNAVAILABLE: full JSONB round-trip requires Postgres; string-keyed fixture is
    partial coverage` comment on the string-key test (per CLAUDE.md + 14-VALIDATION.md Manual-Only row).
    Where the live leg cannot yet be exercised because no tool exports `preview/1`, mark those
    sub-cases `@tag :skip` with a note, so the file COMPILES and RUNS now and the skip is removed
    in Wave 1 when a test-only tool with `preview/1` can be defined.

    Create `test/cairnloop/web/tool_proposal_presenter_test.exs` (module
    `Cairnloop.Web.ToolProposalPresenterTest`, `use ExUnit.Case, async: true`) with one describe
    block per D-25 total function, asserting against the locked D-11 mappings:
    `status_label/1` → "Proposed" / "Needs input" / "Not available here" / "Blocked by policy";
    `status_group/1` → `:awaiting` (proposed, needs_input), `:blocked` (scope_invalid, policy_denied);
    `reason_label/1` humanizes `{:missing_scopes, [:admin_scope]}` into a readable string with NO
    `inspect`-style output (assert the result does not contain `":missing_scopes"` or `"[:"`);
    `history_line/1` catch-all returns `"Workflow updated"` for an unrecognized `event_type` (D-24);
    `input_rows/1` on a map containing a nested map / tuple value returns humanized rows or the
    "Unsupported value" sentinel and NEVER the raw nested structure (D-22 masking choke point);
    `approval_outlook/1` returns a calm future-tense sentence for `:requires_approval`, `nil` for
    `:auto`, and a "cannot be approved or run" sentence for `:always_block` (D-12).
    Mark any function not yet implementable `@tag :skip` so the file compiles and runs now.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/governance/preview_test.exs test/cairnloop/web/tool_proposal_presenter_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test/cairnloop/governance/preview_test.exs` exists and references `Cairnloop.Governance.Preview.render`.
    - `test/cairnloop/web/tool_proposal_presenter_test.exs` exists and references `Cairnloop.Web.ToolProposalPresenter`.
    - Both files COMPILE (`mix compile --warnings-as-errors` clean) — undefined modules are tolerated only inside `@tag :skip` tests or tests that have not yet run; if a referenced module is undefined at compile time, wrap the reference so compilation still succeeds (alias + runtime call, not a compile-time macro).
    - `preview_test.exs` contains a string-keyed snapshot test variant (`grep -c '=>' test/cairnloop/governance/preview_test.exs` returns ≥ 1 line with a `"..." =>` map key).
    - `preview_test.exs` contains a `# REPO-UNAVAILABLE` comment.
    - `tool_proposal_presenter_test.exs` asserts the `{:missing_scopes, ...}` case produces no raw-term output (a refute on `=~ "missing_scopes"` against the raw-tuple form).
    - `mix test {both files}` runs to completion (pass/skip/red allowed; compile failure NOT allowed).
  </acceptance_criteria>
  <done>Both new headless test files exist, compile warnings-clean, and run; they encode the D-11/D-12/D-17/D-19/D-22/D-24 behavior contracts with skip-tags where the implementation does not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Extend governance_test.exs and conversation_live_test.exs for the governed-action surface</name>
  <files>test/cairnloop/governance_test.exs, test/cairnloop/web/conversation_live_test.exs</files>
  <read_first>
    - test/cairnloop/governance_test.exs (existing MockRepo with `insert/1`, `get_by/2`, `Process.get(:tool_proposals, [])`; inline fixtures; describe-block idiom)
    - test/cairnloop/web/conversation_live_test.exs (existing MockRepo with `get!/2` for Conversation 1 / 321, `get/2`, `get_by/2`, `insert/1`; source-assertion idiom near L1019-1044 referenced in 14-RESEARCH.md)
    - lib/cairnloop/governance.ex L105-119 (`derive_idempotency_key/4` canonical map — D-08 exclusion target), L197-227 (`insert_new_proposal` valid path), L303-320 (`insert_blocked_proposal` blocked path)
    - lib/cairnloop/web/conversation_live.ex L173-185 (`handle_event("execute_tool")` — conversation_id threading target), L188-198 (`failure_reason_message/1` — inspect-replacement target), L200 (`reload_conversation_with_context/2` — governed_actions assign target)
    - .planning/phases/14-operator-timeline-preview-surface/14-VALIDATION.md (rows 14-01-a/b, 14-02-a/b, 14-03-a/b — exact behaviors these extensions must assert)
  </read_first>
  <action>
    Extend `test/cairnloop/governance_test.exs` with new describe blocks (do not modify existing
    passing tests):
    (1) `list_proposals_for_conversation/1` returns proposals ordered `desc: inserted_at` with
    `events` preloaded `asc: inserted_at`, and returns `[]` for a conversation with no proposals —
    extend MockRepo to support an `all/1` (or query) path returning seeded `:tool_proposals`
    filtered by `conversation_id` (row 14-01-a);
    (2) `conversation_id` is written on the VALID path AND the BLOCKED path of `propose/3` (D-07) —
    assert the persisted `ToolProposal` (via `Process.get(:last_inserted)` / `:tool_proposals`)
    carries the conversation_id passed in the context (row 14-01-b);
    (3) `conversation_id` is EXCLUDED from the idempotency key (D-08) — assert that two `propose/3`
    calls with identical action params but DIFFERENT `conversation_id` produce the SAME
    `idempotency_key` (i.e. the second is deduped to the first). Mark sub-cases that need the not-yet-
    existing facade/threading `@tag :skip` so the file compiles and runs now.

    Extend `test/cairnloop/web/conversation_live_test.exs`:
    (1) extend MockRepo so the LiveView's `governed_actions` load path can return a list of
    `%ToolProposal{}` structs (and `[]` by default) — mirror the existing `get!/2` per-id pattern;
    (2) `governed_action_card/1` renders all four statuses without crashing via
    `Phoenix.LiveViewTest.render_component/2` (row 14-02-a) and the rendered HTML pairs a status
    chip color WITH a text label (never color-alone — assert the status label string appears in the
    output, brand §7.5);
    (3) empty `events` (or `%Ecto.Association.NotLoaded{}`) renders a calm "No history yet" rather
    than raising (D-24 `Ecto.assoc_loaded?` guard);
    (4) blocked proposals (`:needs_input`, `:scope_invalid`, `:policy_denied`) appear in the rail
    (Support-Truth Gate — row 14-03-b);
    (5) a source-assertion test that `failure_reason_message/1` no longer calls `inspect` on the
    scope/policy reason path (mirror the existing source-assertion idiom; read the file and refute
    `inspect(reason)` inside the `failure_reason_message` clauses).
    Mark card-render and rail tests `@tag :skip` until `governed_action_card/1` exists (Wave 2),
    keeping the file compiling and running now.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/cairnloop/governance_test.exs test/cairnloop/web/conversation_live_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "list_proposals_for_conversation" test/cairnloop/governance_test.exs` returns ≥ 1.
    - `test/cairnloop/governance_test.exs` contains a test asserting identical-action / different-conversation_id `propose/3` calls yield the same idempotency key (D-08).
    - `grep -c "governed_action" test/cairnloop/web/conversation_live_test.exs` returns ≥ 1.
    - `test/cairnloop/web/conversation_live_test.exs` MockRepo can return a list of `%ToolProposal{}` for the governed_actions load path.
    - A source-assertion test for the `inspect`-removal in `failure_reason_message/1` is present.
    - Existing previously-passing tests in both files still pass (no regressions introduced by the extension).
    - `mix compile --warnings-as-errors` is clean; `mix test {both files}` runs to completion (skip/red allowed for not-yet-implemented behavior; no compile failures).
  </acceptance_criteria>
  <done>Both existing test files are extended with the governed-action describe blocks and MockRepo support; the suite compiles warnings-clean and runs; new behavior tests are red/skipped pending Waves 1-3, existing tests unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → modules-under-test | Wave 0 tests reference modules that do not exist yet; references must not break the build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-W0-01 | Tampering | Test files referencing undefined modules | mitigate | Reference not-yet-existing modules only via runtime aliases / `@tag :skip` tests so `mix compile --warnings-as-errors` stays clean; never via compile-time macro expansion. |
| T-14-W0-SC | Tampering | npm/mix/external installs | accept | No package installs in Phase 14 (RESEARCH "Package Legitimacy Audit: Not applicable"). No new deps introduced by Wave 0. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean across the repo after both tasks.
- `mix test test/cairnloop/governance/preview_test.exs test/cairnloop/web/tool_proposal_presenter_test.exs test/cairnloop/governance_test.exs test/cairnloop/web/conversation_live_test.exs` runs to completion.
- Pre-existing baseline test failures / `*.Repo` boot noise (per CLAUDE.md + STATE.md) are NOT introduced or worsened by this plan; report them honestly if present.
</verification>

<success_criteria>
- Four test files exist (2 new, 2 extended) encoding the FLOW-01 + FLOW-02 behavior contracts from 14-VALIDATION.md.
- All four compile warnings-clean and the suite runs (Wave-0 red/skip is expected and correct).
- No regression to existing passing tests.
</success_criteria>

<output>
Create `.planning/phases/14-operator-timeline-preview-surface/14-00-SUMMARY.md` when done.
</output>
