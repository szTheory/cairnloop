---
phase: 61-ci-cd-efficiency-and-release-confidence
plan: "03"
type: execute
wave: 1
depends_on:
  - "61-01"
files_modified:
  - ".github/workflows/demo-smoke.yml"
  - "test/cairnloop/demo_smoke_workflow_contract_test.exs"
autonomous: true
requirements:
  - CI-02
  - CI-03
  - CI-04
  - CI-05
user_setup: []
must_haves:
  truths:
    - "D-02/D-03: Demo smoke action posture is refreshed and locked in the existing DB-free source-contract test."
    - "D-04/D-05/D-06: Demo smoke remains read-only, credential-free, and unrelated to release secrets or write permissions."
    - "D-09/D-10: Docker demo smoke remains outside default `mix ci` and runs for wrapper/demo/docs/example signal, schedule, manual dispatch, and trusted main contexts."
    - "D-11/D-18: Demo smoke emits bounded Docker/runtime/duration summary evidence without secrets or support content."
  artifacts:
    - path: ".github/workflows/demo-smoke.yml"
      provides: "Demo smoke path-filter tightening, action refresh, and bounded summary evidence"
      exports: ["jobs.demo-smoke", "on.pull_request.paths", "on.push.paths"]
    - path: "test/cairnloop/demo_smoke_workflow_contract_test.exs"
      provides: "Updated source-contract assertions for demo smoke path filters and action posture"
      exports: ["Cairnloop.DemoSmokeWorkflowContractTest"]
  key_links:
    - from: "test/cairnloop/demo_smoke_workflow_contract_test.exs"
      to: ".github/workflows/demo-smoke.yml"
      via: "path filter and permission assertions"
      pattern: "assert_path_filters"
  prohibitions:
    - "No pull_request_target workflow trigger."
    - "No release credentials for untrusted PR code."
    - "No stale action runtime variables such as ACTIONS_RUNNER_NODE_VERSION."
    - "No ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION opt-out."
    - "No empty artifact uploads accepted as useful evidence."
    - "No Docker smoke inside default mix ci."
---

<!-- planner-discipline-allow: pull_request_target -->
<!-- planner-discipline-allow: ACTIONS_RUNNER_NODE_VERSION -->
<!-- planner-discipline-allow: ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION -->

<objective>
Keep Docker demo smoke as a high-signal adoption proof without broad PR overlap or privileged workflow posture.

Purpose: Demo smoke should continue proving the adopter Docker wrapper path, but Phase 61 should stop using it as a duplicate check for every library/static change normal CI and E2E already cover.
Output: Updated `.github/workflows/demo-smoke.yml` and existing demo-smoke source-contract test assertions for CI-02, CI-03, CI-04, and CI-05.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-CONTEXT.md
@.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-RESEARCH.md
@.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-VALIDATION.md
@docs/ci-cd-audit.md
@.github/workflows/demo-smoke.yml
@test/cairnloop/demo_smoke_workflow_contract_test.exs
@README.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh demo smoke action posture</name>
  <files>.github/workflows/demo-smoke.yml, test/cairnloop/demo_smoke_workflow_contract_test.exs</files>
  <read_first>
  - @.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-CONTEXT.md
  - @.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-RESEARCH.md
  - @.github/workflows/demo-smoke.yml
  - @test/cairnloop/demo_smoke_workflow_contract_test.exs
  </read_first>
  <action>Per D-01/D-02, refresh the official `actions/checkout` release source at implementation time and, if no incompatibility is found, update `.github/workflows/demo-smoke.yml` from `actions/checkout@v6` to `actions/checkout@v7`. Preserve workflow-level `permissions: contents: read`, PR-only concurrency cancellation, `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"`, `persist-credentials: false`, and wrapper delegation to `./bin/demo smoke`. Update `Cairnloop.DemoSmokeWorkflowContractTest` to assert the selected checkout major and the same least-privilege invariants per D-03/D-04/D-05/D-06.</action>
  <verify>
    <automated>mix test test/cairnloop/demo_smoke_workflow_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>Demo smoke uses the chosen maintained checkout major, keeps official Node 24 posture, remains read-only, and every checkout opts out of persisted credentials.</acceptance_criteria>
  <done>Demo smoke action posture is source-locked.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow demo smoke path filters to adoption-signal changes</name>
  <files>.github/workflows/demo-smoke.yml, test/cairnloop/demo_smoke_workflow_contract_test.exs</files>
  <read_first>
  - @.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-CONTEXT.md
  - @docs/ci-cd-audit.md
  - @.github/workflows/demo-smoke.yml
  - @test/cairnloop/demo_smoke_workflow_contract_test.exs
  - @README.md
  - @CONTRIBUTING.md
  </read_first>
  <action>Per D-09/D-10, keep `workflow_dispatch`, the weekly schedule, and `push`/`pull_request` path filters, but remove broad `lib/**` and `priv/**` from demo-smoke PR/push filters unless the current source reveals a demo-only risk not covered by normal CI or E2E. Keep filters for `.dockerignore`, `bin/demo`, `examples/cairnloop_example/**`, `.github/workflows/demo-smoke.yml`, README/adopter guide paths, `mix.exs`, `mix.lock`, and `config/**`. Keep Docker smoke outside `mix.exs` aliases and outside `.github/workflows/ci.yml`. Update `@path_filters` and negative assertions in `Cairnloop.DemoSmokeWorkflowContractTest` to match the new filter list and prove planning-only files do not trigger the workflow.</action>
  <verify>
    <automated>mix test test/cairnloop/demo_smoke_workflow_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>Demo smoke still runs for wrapper/demo/docs/example/config/Mix changes, schedule, and manual dispatch, but no longer duplicates normal CI for broad library or static changes.</acceptance_criteria>
  <done>Demo smoke runner time is reduced without weakening adopter proof.</done>
</task>

<task type="auto">
  <name>Task 3: Add bounded demo smoke summary evidence</name>
  <files>.github/workflows/demo-smoke.yml, test/cairnloop/demo_smoke_workflow_contract_test.exs</files>
  <read_first>
  - @.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-CONTEXT.md
  - @.github/workflows/demo-smoke.yml
  - @test/cairnloop/demo_smoke_workflow_contract_test.exs
  </read_first>
  <action>Per D-11/D-18, append a bounded `$GITHUB_STEP_SUMMARY` section from the demo smoke job containing Docker version, Docker Compose version, workflow event, ref, and elapsed seconds for `./bin/demo smoke`. Do not print secrets, support content, container logs, or unbounded command output into the summary. Keep the workflow as a thin delegator and do not inline Compose or route-smoke internals that belong to `./bin/demo`. Update `Cairnloop.DemoSmokeWorkflowContractTest` to assert the summary heading, duration label, Docker version probes, wrapper command, and absence of duplicated wrapper internals.</action>
  <verify>
    <automated>mix test test/cairnloop/demo_smoke_workflow_contract_test.exs --warnings-as-errors && mix ci.fast</automated>
  </verify>
  <acceptance_criteria>Demo smoke summaries expose enough bounded evidence to diagnose runtime and duration, and the workflow still delegates behavior to `./bin/demo smoke`.</acceptance_criteria>
  <done>Demo smoke evidence is useful without expanding workflow-owned logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| PR source -> Docker smoke workflow | Untrusted source can trigger smoke checks but must not gain write or release authority. |
| Demo smoke output -> maintainer summary | Runtime evidence must avoid secret or support-content leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-61-07 | Denial of Service | `demo-smoke.yml` path filters | mitigate | Narrow broad path filters to demo/adoption signal paths per D-09. |
| T-61-08 | Elevation of Privilege | Demo smoke permissions | mitigate | Keep read-only permissions and checkout credential opt-out asserted by source-contract tests. |
| T-61-09 | Information Disclosure | Step summary | mitigate | Summarize bounded runtime/duration facts only. |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | This workflow runs Docker smoke through the existing wrapper and adds no package-manager dependency. |
</threat_model>

<verification>
Run `mix test test/cairnloop/demo_smoke_workflow_contract_test.exs --warnings-as-errors` after each task and `mix ci.fast` before closing the plan.
</verification>

<success_criteria>
- Demo smoke uses maintained action/runtime posture and least-privilege defaults.
- Demo smoke path filters target Docker/demo/docs/example/config/Mix changes, not broad duplicate library changes.
- Demo smoke stays outside default `mix ci`.
- Demo smoke summary evidence is bounded and wrapper-owned behavior remains in `./bin/demo`.
</success_criteria>

## Artifacts this phase produces

- Changed file: `.github/workflows/demo-smoke.yml` with updated `actions/checkout` major, narrowed `on.push.paths` / `on.pull_request.paths`, and bounded `$GITHUB_STEP_SUMMARY` evidence.
- Changed file: `test/cairnloop/demo_smoke_workflow_contract_test.exs` with updated `@path_filters`, action posture assertions, summary assertions, and forbidden release/write behavior assertions.
- Intentionally unchanged symbol: `./bin/demo smoke` remains the only Docker smoke implementation entry point.
- Intentionally unchanged symbol: `mix.exs` default `ci` alias remains free of Docker smoke.

<output>
Create `.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-03-SUMMARY.md` when done.
</output>
