---
phase: 61-ci-cd-efficiency-and-release-confidence
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - "test/cairnloop/ci_workflow_contract_test.exs"
  - "test/cairnloop/release_workflow_contract_test.exs"
autonomous: true
requirements:
  - CI-02
  - CI-03
  - CI-04
  - CI-05
  - CI-06
user_setup: []
must_haves:
  truths:
    - "D-01/D-02/D-03: DB-free workflow source-contract tests exist before workflow mutations and lock current action/runtime facts, including official Node 24 posture."
    - "D-04/D-05/D-06: Contract tests prove read-only workflow defaults, scoped release write permissions, checkout credential opt-out, and absence of privileged untrusted PR release triggers."
    - "D-07/D-08/D-09/D-10: Contract tests name the current fast/quality/integration/e2e/release_gate/demo-smoke topology and preserve the local Mix lane split."
    - "D-11/D-12/D-13/D-17/D-18: Contract tests give later plans concrete assertions for timing/cache/runtime evidence and useful bounded artifacts."
    - "D-14/D-15/D-16: Release workflow tests identify exact-SHA checkout, Hex token preflight, dry run, package inspection, publish gate, Hex info, and HexDocs verification."
  artifacts:
    - path: "test/cairnloop/ci_workflow_contract_test.exs"
      provides: "Cairnloop.CIWorkflowContractTest source-contract module"
      exports: ["workflow_source/0", "assert_contains/2", "event_block!/2", "job_block!/2", "path_filters/1"]
    - path: "test/cairnloop/release_workflow_contract_test.exs"
      provides: "Cairnloop.ReleaseWorkflowContractTest source-contract module"
      exports: ["workflow_source/0", "assert_contains/2", "job_block!/2", "step_block!/2"]
  key_links:
    - from: "test/cairnloop/ci_workflow_contract_test.exs"
      to: ".github/workflows/ci.yml"
      via: "File.read! source scan"
      pattern: "File\\.read!\\(.*ci\\.yml"
    - from: "test/cairnloop/release_workflow_contract_test.exs"
      to: ".github/workflows/release-please.yml"
      via: "File.read! source scan"
      pattern: "File\\.read!\\(.*release-please\\.yml"
  prohibitions:
    - "No pull_request_target workflow trigger."
    - "No release credentials for untrusted PR code."
    - "No stale action runtime variables such as ACTIONS_RUNNER_NODE_VERSION."
    - "No ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION opt-out."
    - "No empty artifact uploads accepted as useful evidence."
    - "No Docker smoke inside default mix ci."
---

<!-- planner-discipline-allow: pull_request_target -->
<!-- planner-discipline-allow: ACTIONS_RUNNER_NODE_VERSION -->
<!-- planner-discipline-allow: ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION -->

<objective>
Create Wave 0 DB-free workflow source-contract tests for CI-02 through CI-06 before mutating workflow behavior.

Purpose: Phase 61 changes must be guarded by fast local tests that read workflow YAML as source, mirroring `test/cairnloop/demo_smoke_workflow_contract_test.exs`, without starting Docker, Phoenix, Repo, browser tooling, or GitHub Actions.
Output: `Cairnloop.CIWorkflowContractTest` and `Cairnloop.ReleaseWorkflowContractTest` capturing the current dirty-worktree baseline and the invariant safety contracts that later plans update as workflow source changes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-CONTEXT.md
@.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-RESEARCH.md
@.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-VALIDATION.md
@docs/ci-cd-audit.md
@docs/software-quality-evaluation.md
@.github/workflows/ci.yml
@.github/workflows/release-please.yml
@.github/workflows/demo-smoke.yml
@test/cairnloop/demo_smoke_workflow_contract_test.exs
</context>

## Source Coverage Audit

| Source | ID | Requirement / decision | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 61 | Keep CI fast, deterministic, least-privilege, and useful while preserving release trust. | 61-01 through 61-05 | COVERED |
| REQ | CI-02 | Current first-party action/runtime posture without stale variables. | 61-01, 61-02, 61-03, 61-04, 61-05 | COVERED |
| REQ | CI-03 | Least-privilege token permissions and non-persisted checkout credentials. | 61-01, 61-02, 61-03, 61-04 | COVERED |
| REQ | CI-04 | Maintainer-visible wall-clock, cache, runtime, slow-test, compile, and failure evidence. | 61-01, 61-02, 61-03, 61-04, 61-05 | COVERED |
| REQ | CI-05 | Keep E2E/demo/integration/quality/release checks where signal is clear. | 61-01, 61-02, 61-03, 61-05 | COVERED |
| REQ | CI-06 | Release automation proves package/docs/dry-run readiness before publish without exposing secrets to PR code. | 61-01, 61-04, 61-05 | COVERED |
| RESEARCH | Action majors | Refresh checkout/cache/upload-artifact to current maintained majors where compatible. | 61-02, 61-03, 61-04 | COVERED |
| RESEARCH | Workflow contracts | Add CI and release source-contract tests modeled on demo smoke. | 61-01 | COVERED |
| RESEARCH | Path gates | Path-gate E2E/demo smoke and preserve aggregate semantics. | 61-02, 61-03 | COVERED |
| RESEARCH | Summaries | Add bounded timing/cache/runtime/failure evidence. | 61-02, 61-03, 61-04 | COVERED |
| RESEARCH | Release preflight | Run quality/package/docs proof on exact release SHA before publish. | 61-04 | COVERED |
| CONTEXT | D-01..D-18 | Locked CI/CD decisions from discuss-phase. | 61-01 through 61-05 with D-ID citations | COVERED |

<tasks>

<task type="auto">
  <name>Task 1: Add CI workflow source contract</name>
  <files>test/cairnloop/ci_workflow_contract_test.exs</files>
  <read_first>
  - @CLAUDE.md
  - @.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-CONTEXT.md
  - @.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-VALIDATION.md
  - @.github/workflows/ci.yml
  - @test/cairnloop/demo_smoke_workflow_contract_test.exs
  - test/cairnloop/ci_workflow_contract_test.exs (new file; if it exists at execution time, read it and preserve user changes)
  </read_first>
  <action>Create `test/cairnloop/ci_workflow_contract_test.exs` with module `Cairnloop.CIWorkflowContractTest`, `use ExUnit.Case, async: true`, and helper functions modeled on the demo-smoke contract test. Per D-01/D-03, assert the current source contract for `.github/workflows/ci.yml`: `push` to `main` and `master`, `pull_request`, `workflow_dispatch`, workflow-level `permissions: contents: read`, PR-only `cancel-in-progress`, `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"`, no stale Node transition or insecure opt-out env vars, jobs `fast`, `quality`, `integration`, `e2e`, and `release_gate`, lane commands `mix ci.fast`, `mix ci.quality`, `mix ci.integration`, and `mix test.e2e`, all current checkouts with `persist-credentials: false`, and the current action majors found in the dirty worktree. Per D-07/D-10, assert Docker demo smoke is not part of `mix ci` or `.github/workflows/ci.yml`. Per D-11/D-17, include assertions that locate existing cache/version summary blocks and the current Playwright artifact posture so Plan 61-02 can intentionally tighten those expectations.</action>
  <verify>
    <automated>mix format --check-formatted test/cairnloop/ci_workflow_contract_test.exs && mix test test/cairnloop/ci_workflow_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>`Cairnloop.CIWorkflowContractTest` passes against the current on-disk `ci.yml`, fails if CI stops using official Node 24 opt-in, fails if checkout credentials are persisted, fails if Docker smoke appears in default CI, and reads only workflow source.</acceptance_criteria>
  <done>CI workflow source-contract coverage exists and is DB-free.</done>
</task>

<task type="auto">
  <name>Task 2: Add release workflow source contract</name>
  <files>test/cairnloop/release_workflow_contract_test.exs</files>
  <read_first>
  - @CLAUDE.md
  - @.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-CONTEXT.md
  - @.github/workflows/release-please.yml
  - @test/cairnloop/demo_smoke_workflow_contract_test.exs
  - test/cairnloop/release_workflow_contract_test.exs (new file; if it exists at execution time, read it and preserve user changes)
  </read_first>
  <action>Create `test/cairnloop/release_workflow_contract_test.exs` with module `Cairnloop.ReleaseWorkflowContractTest`. Per D-04/D-05/D-06, assert `.github/workflows/release-please.yml` is trusted-ref only, defaults to `permissions: contents: read`, grants `contents: write` and `pull-requests: write` only inside the `release-please` job, keeps `publish-hex` at `contents: read`, uses checkout with `persist-credentials: false`, and does not expose publish credentials to PR-triggered code. Per D-14/D-15/D-16, assert `publish-hex` checks out `needs.release-please.outputs.sha`, gates on `release_created == 'true'`, preflights `HEX_API_KEY`, runs `mix hex.publish --dry-run`, inspects the built tarball for `lib`, `priv`, `guides`, `mix.exs`, `README.md`, `LICENSE`, `CHANGELOG.md`, runs `mix hex.publish --yes`, `mix hex.info`, and `mix hex.docs fetch`. Leave a focused failing assertion for the release quality preflight only if Plan 61-04 is executed in a TDD red/green flow; otherwise assert the current baseline and name the Plan 61-04 target in test names or messages without making Wave 0 fail.</action>
  <verify>
    <automated>mix format --check-formatted test/cairnloop/release_workflow_contract_test.exs && mix test test/cairnloop/release_workflow_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>`Cairnloop.ReleaseWorkflowContractTest` passes against the current on-disk release workflow and protects trusted triggers, scoped permissions, exact SHA checkout, Hex token preflight, dry run, package inspection, publish, Hex info, and HexDocs fetch.</acceptance_criteria>
  <done>Release workflow source-contract coverage exists and is DB-free.</done>
</task>

<task type="auto">
  <name>Task 3: Prove workflow contracts run as a fast local gate</name>
  <files>test/cairnloop/ci_workflow_contract_test.exs, test/cairnloop/release_workflow_contract_test.exs</files>
  <read_first>
  - @.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-VALIDATION.md
  - @test/cairnloop/demo_smoke_workflow_contract_test.exs
  - @test/cairnloop/ci_workflow_contract_test.exs
  - @test/cairnloop/release_workflow_contract_test.exs
  </read_first>
  <action>Run the Phase 61 quick workflow-contract command from `61-VALIDATION.md`. If existing dirty-worktree changes in the workflows cause a mismatch, update only the new contract tests to reflect current source truth or the locked D-01 through D-18 decisions; do not revert workflow files in this Wave 0 plan.</action>
  <verify>
    <automated>mix test test/cairnloop/demo_smoke_workflow_contract_test.exs test/cairnloop/ci_workflow_contract_test.exs test/cairnloop/release_workflow_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>The three workflow source-contract test files pass together without requiring Docker, Repo, browser tooling, GitHub Actions, or network access.</acceptance_criteria>
  <done>Wave 0 test scaffold is ready for workflow mutation plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Contributor source -> workflow YAML | Untrusted edits can weaken CI, permissions, or release gates. |
| Workflow source -> release credentials | Source contract must prevent release secrets from entering untrusted PR paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-61-01 | Tampering | `ci.yml` and `release-please.yml` | mitigate | DB-free source-contract tests assert action runtime posture and reject stale or unsafe runtime knobs per D-01. |
| T-61-02 | Elevation of Privilege | GitHub token permissions | mitigate | Tests assert read-only workflow defaults, scoped release write permissions, and checkout credential opt-out per D-04/D-05. |
| T-61-03 | Information Disclosure | Publish secrets and artifacts | mitigate | Tests assert publish secrets remain in trusted release jobs and artifact evidence is bounded per D-06/D-18. |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | This plan adds no package-manager install task or dependency. |
</threat_model>

<verification>
Run `mix test test/cairnloop/demo_smoke_workflow_contract_test.exs test/cairnloop/ci_workflow_contract_test.exs test/cairnloop/release_workflow_contract_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
- CI and release workflow source-contract test files exist.
- The tests pass against the current dirty-worktree workflow baseline.
- The tests cite and guard D-01 through D-18 decisions that later plans intentionally change.
- No source files outside `test/cairnloop/*workflow_contract_test.exs` are modified by this plan.
</success_criteria>

## Artifacts this phase produces

- New file: `test/cairnloop/ci_workflow_contract_test.exs` with module `Cairnloop.CIWorkflowContractTest` and helpers `workflow_source/0`, `assert_contains/2`, `event_block!/2`, `job_block!/2`, `path_filters/1`.
- New file: `test/cairnloop/release_workflow_contract_test.exs` with module `Cairnloop.ReleaseWorkflowContractTest` and helpers `workflow_source/0`, `assert_contains/2`, `job_block!/2`, `step_block!/2`.
- Changed workflow keys planned in later phase plans: `.github/workflows/ci.yml` jobs `changes`, `fast`, `quality`, `integration`, `e2e`, `release_gate`; `.github/workflows/demo-smoke.yml` job `demo-smoke`; `.github/workflows/release-please.yml` jobs `release-please` and `publish-hex`.
- Changed action pins planned in later phase plans: `actions/checkout@v7`, `actions/cache@v6`, `actions/upload-artifact@v7`; retained pins `actions/setup-node@v6`, `googleapis/release-please-action@v5`, `erlef/setup-beam@v1`.
- Changed docs planned in Plan 61-05: `docs/ci-cd-audit.md`, `README.md`, `CONTRIBUTING.md`.
- Intentionally unchanged symbol: `mix.exs` aliases `ci`, `ci.fast`, `ci.integration`, `ci.quality`, and `ci.full`; default `mix ci` remains free of Docker smoke per D-10.

<output>
Create `.planning/phases/61-ci-cd-efficiency-and-release-confidence/61-01-SUMMARY.md` when done.
</output>
