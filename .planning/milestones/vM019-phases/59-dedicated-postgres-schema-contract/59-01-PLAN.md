---
phase: 59-dedicated-postgres-schema-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/schema_prefix.ex
  - test/cairnloop/schema_prefix_test.exs
  - config/test.exs
  - test/integration/schema_prefix_contract_test.exs
  - test/integration/public_schema_compatibility_test.exs
  - test/integration/schema_prefix_runtime_test.exs
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-06
must_haves:
  truths:
    - "DB-01/D-01: default test and runtime contract resolves new installs to the cairnloop schema."
    - "DB-02/D-03: explicit public compatibility can be selected with public and legacy nil."
    - "D-02/D-04: Cairnloop.SchemaPrefix remains the only prefix abstraction and does not support per-request switching."
  artifacts:
    - path: "lib/cairnloop/schema_prefix.ex"
      provides: "canonical prefix normalization, repo opts, and safe identifier helpers"
      exports: ["default/0", "configured/1", "repo_opts/1", "quoted_table/2", "quote_identifier!/1", "normalize!/1"]
    - path: "config/test.exs"
      provides: "dedicated-schema default test compile config with explicit CAIRNLOOP_SCHEMA_PREFIX=public compile/test override"
    - path: "test/integration/schema_prefix_contract_test.exs"
      provides: "DB-backed dedicated-schema contract assertions"
    - path: "test/integration/public_schema_compatibility_test.exs"
      provides: "DB-backed explicit public compatibility assertions"
    - path: "test/integration/schema_prefix_runtime_test.exs"
      provides: "DB-backed runtime prefix/collision assertions"
  key_links:
    - from: "config/test.exs"
      to: "lib/cairnloop/schema_prefix.ex"
      via: "config :cairnloop, :schema_prefix"
      pattern: "schema_prefix"
    - from: "test/integration/*schema*_test.exs"
      to: "priv/test_host/migrations and priv/repo/migrations"
      via: "mix test.setup and information_schema/pg_catalog assertions"
      pattern: "table_schema|schemaname"
---

<objective>
Establish the canonical prefix helper and the red DB-backed contract tests for Phase 59.

Purpose: Make the default dedicated-schema decision executable before migration/runtime work starts, while preserving explicit public compatibility per D-01 through D-05 and DB-01/DB-02/DB-06.
Output: Prefix helper semantics, test compile config, and concrete integration tests that downstream plans must make pass.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-CONTEXT.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-RESEARCH.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-VALIDATION.md
@docs/postgres-schema-prefix.md
@lib/cairnloop/schema_prefix.ex
@test/cairnloop/schema_prefix_test.exs
@config/test.exs
@test/support/data_case.ex
</context>

## Artifacts this phase produces

- `Cairnloop.SchemaPrefix.default/0`
- `Cairnloop.SchemaPrefix.configured/1`
- `Cairnloop.SchemaPrefix.repo_opts/1`
- `Cairnloop.SchemaPrefix.quoted_table/2`
- `Cairnloop.SchemaPrefix.quote_identifier!/1`
- `Cairnloop.SchemaPrefix.normalize!/1`
- `config :cairnloop, :schema_prefix, "cairnloop"` as the test/default compile path
- `CAIRNLOOP_SCHEMA_PREFIX=public` public-mode compile/test path plus documented nil/empty legacy compatibility behavior
- `test/integration/schema_prefix_contract_test.exs`
- `test/integration/public_schema_compatibility_test.exs`
- `test/integration/schema_prefix_runtime_test.exs`
- Commands: `mix test test/cairnloop/schema_prefix_test.exs --warnings-as-errors`, `mix test --include integration test/integration/schema_prefix_contract_test.exs test/integration/public_schema_compatibility_test.exs test/integration/schema_prefix_runtime_test.exs --warnings-as-errors`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock prefix helper semantics and test compile default</name>
  <files>lib/cairnloop/schema_prefix.ex, test/cairnloop/schema_prefix_test.exs, config/test.exs</files>
  <read_first>
  - lib/cairnloop/schema_prefix.ex
  - test/cairnloop/schema_prefix_test.exs
  - config/test.exs
  - .planning/phases/59-dedicated-postgres-schema-contract/59-CONTEXT.md
  - .planning/phases/59-dedicated-postgres-schema-contract/59-RESEARCH.md
  - .planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
  - docs/postgres-schema-prefix.md
  </read_first>
  <behavior>
  - Deleting `:cairnloop, :schema_prefix` resolves to `"cairnloop"` per D-01.
  - Setting the config to `"public"` is accepted as explicit public compatibility per D-03.
  - Setting the config to nil or empty string remains accepted as legacy public compatibility per D-03.
  - Unsafe prefix values containing punctuation, whitespace, semicolons, or multiple identifiers raise `ArgumentError` per D-02.
  </behavior>
  <action>Update `Cairnloop.SchemaPrefix` only, not facade-local helpers, per D-02. Keep `default/0` returning `"cairnloop"`. Ensure `configured/1`, `repo_opts/1`, and `quoted_table/2` behave consistently for `"cairnloop"`, `"public"`, nil, and empty-string compatibility. Update `config/test.exs` so tests compile with the dedicated default unless `CAIRNLOOP_SCHEMA_PREFIX=public` is set for the explicit public compatibility proof; do not leave an unconditional `config :cairnloop, :schema_prefix, nil`. Define the exact public-mode command as `CAIRNLOOP_SCHEMA_PREFIX=public MIX_ENV=test mix do clean, compile --force --warnings-as-errors, test --include integration test/integration/public_schema_compatibility_test.exs --warnings-as-errors` and use it in the final verification plan for DB-02/DB-06/D-05/D-15. Extend `test/cairnloop/schema_prefix_test.exs` to assert `"public"` behavior as the preferred explicit compatibility spelling, nil legacy behavior, unsafe identifier rejection, and the existing `@schema_prefix Application.compile_env(:cairnloop, :schema_prefix, "cairnloop")` schema source scan per D-05.</action>
  <acceptance_criteria>
  - `Cairnloop.SchemaPrefix.configured(schema_prefix: "public")` returns `"public"`.
  - `Cairnloop.SchemaPrefix.quoted_table("cairnloop_chunks", schema_prefix: "public")` returns a schema-qualified public table name, not a raw unvalidated string.
  - `Cairnloop.SchemaPrefix.configured(schema_prefix: nil)` returns nil.
  - `config/test.exs` contains the dedicated default, reads `CAIRNLOOP_SCHEMA_PREFIX=public` for explicit public-mode compilation, and has no unconditional test override to nil.
  - No module other than `Cairnloop.SchemaPrefix` introduces a new prefix abstraction.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/schema_prefix_test.exs --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix compile --force --warnings-as-errors</automated>
    <automated>CAIRNLOOP_SCHEMA_PREFIX=public MIX_ENV=test mix do clean, compile --force --warnings-as-errors, test test/cairnloop/schema_prefix_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Helper/config tests pass, warnings-clean compile passes, and D-01/D-02/D-03/D-04/D-05 are represented in the helper contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create concrete DB-backed contract tests for downstream waves</name>
  <files>test/integration/schema_prefix_contract_test.exs, test/integration/public_schema_compatibility_test.exs, test/integration/schema_prefix_runtime_test.exs</files>
  <read_first>
  - test/support/data_case.ex
  - test/integration/outbound_bulk_envelopes_migration_test.exs
  - test/integration/partial_unique_index_test.exs
  - .planning/phases/59-dedicated-postgres-schema-contract/59-VALIDATION.md
  - .planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
  - docs/postgres-schema-prefix.md
  </read_first>
  <behavior>
  - `schema_prefix_contract_test.exs` asserts physical object placement in `cairnloop` with `information_schema` or `pg_catalog`.
  - `public_schema_compatibility_test.exs` proves explicit public compatibility under the actual compile/config mode, not only runtime `Application.put_env/3`.
  - `schema_prefix_runtime_test.exs` creates misleading same-name `public.cairnloop_*` objects while the configured prefix is `"cairnloop"` and proves runtime paths ignore the collision.
  </behavior>
  <action>Create real integration tests, not skipped tests, for D-14/D-15/D-16. Use `Cairnloop.DataCase`, include `@moduletag :integration`, and make every catalog query filter by `table_schema`, `schemaname`, or `pronamespace` so stale public tables cannot satisfy DB-01/DB-03. The tests may be red at this point because later plans have not yet converted migrations/runtime, but they must compile and fail only on the missing contract behavior. Do not hide them behind skip tags or test-only conditional branches.</action>
  <acceptance_criteria>
  - All three test files exist and compile.
  - Each test file contains concrete assertions tied to DB-01 through DB-06.
  - `schema_prefix_contract_test.exs` checks support-domain tables, indexes, FKs, trigger functions, and absence of public collision masking.
  - `public_schema_compatibility_test.exs` names `"public"` and nil legacy compatibility as separate supported modes.
  - `schema_prefix_runtime_test.exs` names Chat, KnowledgeBase/Retrieval, Governance/Outbound/MCP, worker bulk operations, doctor checks, and Oban boundary coverage.
  </acceptance_criteria>
  <verify>
    <automated>set +e; output=$(mix test --include integration test/integration/schema_prefix_contract_test.exs test/integration/public_schema_compatibility_test.exs test/integration/schema_prefix_runtime_test.exs --warnings-as-errors 2>&1); status=$?; printf '%s\n' "$output"; test "$status" -ne 0; printf '%s\n' "$output" | grep -E '(Assertion|Expected|schema prefix|table_schema|public compatibility|collision)'; ! printf '%s\n' "$output" | grep -E '(CompilationError|SyntaxError|TokenMissingError|UndefinedFunctionError)'</automated>
    <automated>mix test test/cairnloop/schema_prefix_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Wave-0 DB-backed tests are present, executable, and red for missing implementation rather than syntax or compile failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host config -> SchemaPrefix | Host-controlled `:schema_prefix` is converted into SQL identifiers and Ecto options. |
| test DB -> contract assertions | Existing public tables could mask dedicated-schema failures if tests do not scope catalog queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering / Elevation of Privilege | `Cairnloop.SchemaPrefix.quote_identifier!/1` | mitigate | Validate single identifiers before quoting; reject multi-token SQL values in tests. |
| T-59-02 | Tampering | DB-backed contract tests | mitigate | Require table_schema/schemaname/procatalog assertions so `public.cairnloop_*` cannot satisfy dedicated-schema tests. |
| T-59-03 | Information Disclosure / Integrity | public/dedicated mode split | mitigate | Create explicit public compatibility tests and collision tests before runtime conversion. |
| T-59-04 | Denial of Service | `vector` extension rollback | transfer | Covered in migration plans; this plan seeds tests that will assert no rollback drops shared vector. |
| T-59-05 | Denial of Service / Integrity | Oban boundary | transfer | Covered in runtime/test-host plans; tests must keep `oban_jobs` host-owned. |
</threat_model>

<verification>
Run the focused helper tests and compile gate. Run the new integration tests with an expected nonzero status to prove the red contract exists and compiles.
</verification>

<success_criteria>
- Prefix helper is canonical and safe.
- New test default is dedicated-schema oriented.
- Concrete DB-backed contract tests exist for dedicated, public, runtime/collision, and Oban-boundary proof.
</success_criteria>

<output>
Create `.planning/phases/59-dedicated-postgres-schema-contract/59-01-SUMMARY.md` when done.
</output>
