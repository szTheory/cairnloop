---
phase: 59-dedicated-postgres-schema-contract
plan: "03"
type: execute
wave: 2
depends_on:
  - "59-01"
files_modified:
  - lib/cairnloop/chat.ex
  - lib/cairnloop/knowledge_base.ex
  - lib/cairnloop/mcp.ex
  - test/cairnloop/chat_test.exs
  - test/cairnloop/knowledge_base_test.exs
  - test/cairnloop/mcp_test.exs
  - test/integration/schema_prefix_runtime_test.exs
autonomous: true
requirements:
  - DB-05
  - DB-06
must_haves:
  truths:
    - "DB-05/D-06: Chat, KnowledgeBase, MCP, representative preloads, and writes honor the configured support prefix."
    - "D-15: collision tests prove public tables cannot mask dedicated-schema runtime behavior."
  artifacts:
    - path: "lib/cairnloop/chat.ex"
      provides: "prefix-aware conversation/message reads, writes, and preloads"
    - path: "lib/cairnloop/knowledge_base.ex"
      provides: "prefix-aware KB reads, writes, and vector searches"
    - path: "lib/cairnloop/mcp.ex"
      provides: "prefix-aware MCP token persistence"
    - path: "test/integration/schema_prefix_runtime_test.exs"
      provides: "runtime prefix/collision proof for core facades"
  key_links:
    - from: "lib/cairnloop/*"
      to: "lib/cairnloop/schema_prefix.ex"
      via: "Cairnloop.SchemaPrefix.configured/0, repo_opts/1, and quoted_table/2"
      pattern: "SchemaPrefix"
---

<objective>
Make Chat, KnowledgeBase, and MCP core runtime facades honor the configured Cairnloop support prefix.

Purpose: DB-05 is not true until shipped core facade reads/writes and preloads hit the same schema that migrations create.
Output: Prefix-aware Chat/KnowledgeBase/MCP runtime paths and focused tests proving no silent public/dedicated split.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-CONTEXT.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-RESEARCH.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-VALIDATION.md
@docs/postgres-schema-prefix.md
@lib/cairnloop/schema_prefix.ex
@lib/cairnloop/chat.ex
@lib/cairnloop/knowledge_base.ex
@lib/cairnloop/mcp.ex
@test/integration/schema_prefix_runtime_test.exs
</context>

## Artifacts this phase produces

- Prefix-aware `Chat` query/write/preload behavior.
- Prefix-aware `KnowledgeBase` article/revision/chunk queries, inserts, updates, and vector searches.
- Prefix-aware `MCP` token insert/read/update paths.
- Runtime collision assertions in `test/integration/schema_prefix_runtime_test.exs` for Chat, KnowledgeBase, and MCP paths.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prefix Chat, KnowledgeBase, and MCP public APIs without signature churn</name>
  <files>lib/cairnloop/chat.ex, lib/cairnloop/knowledge_base.ex, lib/cairnloop/mcp.ex, test/cairnloop/chat_test.exs, test/cairnloop/knowledge_base_test.exs, test/cairnloop/mcp_test.exs, test/integration/schema_prefix_runtime_test.exs</files>
  <read_first>
  - lib/cairnloop/chat.ex
  - lib/cairnloop/knowledge_base.ex
  - lib/cairnloop/mcp.ex
  - test/cairnloop/chat_test.exs
  - test/cairnloop/knowledge_base_test.exs
  - test/cairnloop/mcp_test.exs
  - test/integration/schema_prefix_runtime_test.exs
  - lib/cairnloop/schema_prefix.ex
  - .planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
  </read_first>
  <behavior>
  - Existing public function signatures remain unchanged per project sealed-contract invariant.
  - Query sources and preloads use explicit configured prefixes where `@schema_prefix` is insufficient for public compatibility.
  - Schema writes pass `Cairnloop.SchemaPrefix.repo_opts/1` where Ecto write opts are needed for compatibility.
  </behavior>
  <action>Audit every `repo().all`, `repo().one`, `repo().get`, `repo().get!`, `repo().insert`, `repo().update`, `repo().transaction`, and `repo().preload` call in these modules. Preserve existing public APIs and local `repo/0` indirection. Use `prefix = Cairnloop.SchemaPrefix.configured()` on `from`/`join` sources when query prefix precedence requires it, and use `Cairnloop.SchemaPrefix.repo_opts()` for schema write calls that need explicit opts. Update mock-repo tests to assert prefix opts/query prefixes and extend `schema_prefix_runtime_test.exs` to cover representative Chat conversation/message flows, KB article/revision/chunk flows, and MCP token insert/read/revoke paths per D-06 and D-15.</action>
  <acceptance_criteria>
  - No public function signature in `Chat`, `KnowledgeBase`, or `MCP` changes.
  - Tests fail if `Repo.all/one/get/insert/update/preload` paths drop the configured prefix where required.
  - Runtime integration test creates public collision rows and proves these public APIs read/write the configured `cairnloop` schema.
  - Public compatibility mode remains covered by `public_schema_compatibility_test.exs`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/chat_test.exs test/cairnloop/knowledge_base_test.exs test/cairnloop/mcp_test.exs --warnings-as-errors</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Core facade/runtime paths are prefix-aware without sealed API churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cairnloop runtime -> host Repo | Runtime facades execute reads/writes against a host-owned Ecto repo. |
| core facades -> host Repo | Chat, KnowledgeBase, and MCP reads/writes execute against host-owned Ecto repos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-11 | Tampering | core facade query prefixing | mitigate | Apply explicit prefix handling to Chat, KnowledgeBase, and MCP query/write paths where Ecto precedence requires it. |
| T-59-12 | Tampering | query prefix precedence | mitigate | Apply explicit from/join prefixes where `@schema_prefix` or Repo opts would be insufficient. |
| T-59-13 | Information Disclosure / Integrity | silent public/dedicated split | mitigate | Add collision tests with same-name public rows. |
</threat_model>

<verification>
Run focused unit tests and compile after the runtime task. DB-backed runtime integration is completed in Plan 59-07 after migrations and test-host setup are available.
</verification>

<success_criteria>
- Chat, KnowledgeBase, and MCP runtime paths use the configured support prefix.
- Public collisions cannot mask dedicated-schema runtime behavior for core facades.
</success_criteria>

<output>
Create `.planning/phases/59-dedicated-postgres-schema-contract/59-03-SUMMARY.md` when done.
</output>
