---
phase: 59-dedicated-postgres-schema-contract
plan: "04"
type: execute
wave: 4
depends_on:
  - "59-09"
files_modified:
  - lib/cairnloop/governance.ex
  - lib/cairnloop/outbound.ex
  - lib/cairnloop/doctor.ex
  - lib/mix/tasks/cairnloop.doctor.ex
  - test/cairnloop/governance_test.exs
  - test/cairnloop/outbound_test.exs
  - test/cairnloop/doctor_test.exs
  - test/integration/schema_prefix_runtime_test.exs
autonomous: true
requirements:
  - DB-05
  - DB-06
must_haves:
  truths:
    - "DB-05/D-06: governance and outbound persistence honor the configured Cairnloop support prefix."
    - "D-07: Oban enqueues and checks stay host-owned."
    - "D-16: doctor prefix/readiness truth is backed by tests and does not replace DB-backed proof."
  artifacts:
    - path: "lib/cairnloop/governance.ex"
      provides: "prefix-aware governance proposal, approval, event, and audit reads/writes"
    - path: "lib/cairnloop/outbound.ex"
      provides: "prefix-aware outbound envelope/message persistence without Oban redirection"
    - path: "lib/cairnloop/doctor.ex"
      provides: "honest configured-prefix diagnostics"
  key_links:
    - from: "lib/cairnloop/doctor.ex"
      to: "lib/cairnloop/schema_prefix.ex"
      via: "configured prefix display and optional DB structural checks"
      pattern: "SchemaPrefix"
---

<objective>
Complete DB-05 across governance, outbound, and doctor diagnostics.

Purpose: Dedicated-schema correctness must include governance/outbound persistence and readiness truth, not only core chat/retrieval paths.
Output: Prefix-aware governance/outbound modules and doctor diagnostics that report schema state honestly.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-CONTEXT.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-RESEARCH.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
@.planning/phases/58-identity-ingress-and-side-effect-trust/58-07-SUMMARY.md
@docs/postgres-schema-prefix.md
@lib/cairnloop/schema_prefix.ex
@lib/cairnloop/governance.ex
@lib/cairnloop/outbound.ex
@lib/cairnloop/doctor.ex
</context>

## Artifacts this phase produces

- Prefix-aware governance proposal/approval/event reads and writes.
- Prefix-aware outbound message and bulk envelope transactions.
- Preservation of the existing `test/cairnloop/governance_test.exs` no-`Ecto.Multi` invariant.
- `Cairnloop.Doctor.checks/2` configured-prefix findings using Ready/Blocked/Not checked here style.
- Commands: `mix test test/cairnloop/governance_test.exs test/cairnloop/outbound_test.exs test/cairnloop/doctor_test.exs --warnings-as-errors`, `mix compile --warnings-as-errors`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prefix governance and outbound persistence</name>
  <files>lib/cairnloop/governance.ex, lib/cairnloop/outbound.ex, test/cairnloop/governance_test.exs, test/cairnloop/outbound_test.exs, test/integration/schema_prefix_runtime_test.exs</files>
  <read_first>
  - lib/cairnloop/governance.ex
  - lib/cairnloop/outbound.ex
  - test/cairnloop/governance_test.exs
  - test/cairnloop/outbound_test.exs
  - lib/cairnloop/schema_prefix.ex
  - .planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
  </read_first>
  <behavior>
  - Existing sealed governance/outbound APIs and transaction styles remain intact.
  - Governance and outbound runtime persistence hit the configured Cairnloop support prefix where schema/query/write behavior requires explicit prefixing.
  - Oban jobs remain host-owned; outbound enqueue calls are not redirected to the Cairnloop prefix.
  </behavior>
  <action>Audit governance and outbound for direct repo calls, preloads, transactions, query sources, and `delete_all` usage. Add `Cairnloop.SchemaPrefix.configured()` or `repo_opts/1` only where needed to make DB-05 true; do not introduce a second helper module. Preserve the governance source guard that forbids introducing `Ecto.Multi` into `lib/cairnloop/governance.ex`. Do not route `Oban.Job`, `Oban.insert`, Oban unique keys, or `oban_jobs` through the Cairnloop prefix per D-07. Extend existing tests and `schema_prefix_runtime_test.exs` so governance proposals/approvals/events and outbound bulk envelopes/messages are covered by prefix/collision assertions.</action>
  <acceptance_criteria>
  - Governance no-`Ecto.Multi` invariant still passes.
  - Existing governance and outbound focused tests pass.
  - Runtime integration test names governance and outbound paths as DB-05 coverage.
  - No code adds `Cairnloop.SchemaPrefix` to Oban table placement or Oban migration behavior.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/governance_test.exs test/cairnloop/outbound_test.exs --warnings-as-errors</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Governance and outbound persistence paths honor the configured support prefix without changing sealed public APIs or Oban ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add doctor configured-prefix diagnostics</name>
  <files>lib/cairnloop/doctor.ex, lib/mix/tasks/cairnloop.doctor.ex, test/cairnloop/doctor_test.exs, test/integration/schema_prefix_runtime_test.exs</files>
  <read_first>
  - lib/cairnloop/doctor.ex
  - lib/mix/tasks/cairnloop.doctor.ex
  - test/cairnloop/doctor_test.exs
  - test/integration/schema_prefix_runtime_test.exs
  - .planning/phases/58-identity-ingress-and-side-effect-trust/58-07-SUMMARY.md
  - lib/cairnloop/schema_prefix.ex
  - .planning/phases/59-dedicated-postgres-schema-contract/59-CONTEXT.md
  </read_first>
  <behavior>
  - Doctor reports the configured Cairnloop support prefix and explicit public compatibility state.
  - Doctor uses calm Ready/Blocked/Not checked here language and does not claim DB proof it did not run.
  - `/health` remains liveness-only; do not edit UI or health plug files for this phase.
  </behavior>
  <action>Extend `Cairnloop.Doctor.checks/2` and `mix cairnloop.doctor` output documentation with a configured-prefix finding. If a Repo is available and the check queries catalogs, it must use `Cairnloop.SchemaPrefix` and scoped catalog queries; if not querying the DB, it must explicitly say the schema/tables were not checked here. Include public-compatibility wording for `"public"` and nil legacy mode. Do not edit `lib/cairnloop/web/**`, `priv/static/cairnloop.css`, or `/health` behavior.</action>
  <acceptance_criteria>
  - `test/cairnloop/doctor_test.exs` asserts dedicated prefix, explicit public compatibility, and unsafe prefix failure messages.
  - Doctor output does not expose raw stack traces or raw config inspection for invalid prefixes.
  - Doctor does not claim Oban queue or pgvector index checks unless it actually queries them.
  - No UI files are modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/doctor_test.exs --warnings-as-errors</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Doctor carries prefix/readiness truth consistent with D-16 while preserving Phase 58 liveness boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime facades -> host Repo | Governance/outbound write into host-owned Postgres through Cairnloop schemas. |
| doctor -> operator output | Diagnostic text communicates prefix state without overstating checks or leaking raw exceptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-16 | Tampering | runtime query prefixing | mitigate | Apply explicit prefix handling to query sources/write opts where Ecto precedence requires it. |
| T-59-17 | Integrity | governance/outbound transactions | mitigate | Preserve existing transaction/co-commit styles and add prefix behavior without public API churn. |
| T-59-18 | Information Disclosure / Integrity | public/dedicated split | mitigate | Add runtime collision assertions for governance/outbound paths. |
| T-59-19 | Denial of Service / Integrity | Oban enqueue/table ownership | mitigate | Do not route Oban inserts/jobs/tables through Cairnloop prefix; assert boundary in tests. |
| T-59-20 | Repudiation | doctor readiness claims | mitigate | Doctor distinguishes checked, blocked, and not-checked states in tests. |
</threat_model>

<verification>
Run focused governance/outbound/doctor tests and compile. Full DB-backed runtime verification is deferred to Plan 59-07 after migration and host/example setup.
</verification>

<success_criteria>
- Governance and outbound persistence honors configured prefix.
- Oban boundary remains intact.
- Doctor reports prefix state honestly without changing `/health` or UI.
</success_criteria>

<output>
Create `.planning/phases/59-dedicated-postgres-schema-contract/59-04-SUMMARY.md` when done.
</output>
