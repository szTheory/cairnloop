---
phase: 59-dedicated-postgres-schema-contract
plan: "10"
type: execute
wave: 5
depends_on:
  - "59-04"
  - "59-05"
files_modified:
  - lib/cairnloop/automation.ex
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_automation/candidate_builder.ex
  - lib/cairnloop/retrieval/gap_recorder.ex
  - lib/cairnloop/retrieval/workers/prune_gap_events.ex
  - lib/cairnloop/workers/tool_execution_worker.ex
  - lib/cairnloop/workers/approval_expiry_worker.ex
  - lib/cairnloop/workers/approval_resume_worker.ex
  - lib/cairnloop/workers/outbound_worker.ex
  - lib/cairnloop/workers/sla_countdown_worker.ex
  - test/integration/schema_prefix_runtime_test.exs
autonomous: true
requirements:
  - DB-05
  - DB-06
must_haves:
  truths:
    - "DB-05/D-06: automation, KnowledgeAutomation, gap recording, pruning, approval/tool/outbound/SLA workers, and worker state transitions honor the configured support prefix."
    - "D-07: Oban enqueues and job structs stay host-owned while worker persistence targets Cairnloop support tables."
    - "D-15: collision tests prove misleading public support tables cannot mask dedicated-schema worker behavior."
  artifacts:
    - path: "lib/cairnloop/automation.ex"
      provides: "prefix-aware automation draft/SLA persistence"
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "prefix-aware knowledge automation candidate/review persistence"
    - path: "lib/cairnloop/retrieval/gap_recorder.ex"
      provides: "prefix-aware retrieval gap event persistence"
    - path: "lib/cairnloop/workers/*.ex"
      provides: "prefix-aware worker state transitions without moving Oban"
    - path: "test/integration/schema_prefix_runtime_test.exs"
      provides: "runtime prefix/collision proof for automation and worker paths"
  key_links:
    - from: "lib/cairnloop/**/*worker*.ex"
      to: "lib/cairnloop/schema_prefix.ex"
      via: "Cairnloop.SchemaPrefix.configured/0 and repo_opts/1"
      pattern: "SchemaPrefix"
---

<objective>
Make automation, gap recording, and worker persistence honor the configured Cairnloop support prefix.

Purpose: DB-05 includes worker-driven writes and state transitions, not only direct facade calls; those paths must target support-domain tables while Oban remains host-owned.
Output: Prefix-aware automation/worker runtime paths and DB-backed runtime collision coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-CONTEXT.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-RESEARCH.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-04-SUMMARY.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-05-SUMMARY.md
@docs/postgres-schema-prefix.md
@lib/cairnloop/schema_prefix.ex
@lib/cairnloop/automation.ex
@lib/cairnloop/knowledge_automation.ex
@lib/cairnloop/retrieval/gap_recorder.ex
@test/integration/schema_prefix_runtime_test.exs
</context>

## Artifacts this phase produces

- Prefix-aware automation draft, SLA, and knowledge automation persistence where required.
- Prefix-aware retrieval gap event recording and pruning.
- Prefix-aware approval, tool execution, outbound, and SLA worker state transitions where they touch Cairnloop support-domain tables.
- Oban enqueue/job table placement preserved outside `Cairnloop.SchemaPrefix`.
- Runtime collision assertions in `test/integration/schema_prefix_runtime_test.exs` for automation and worker paths.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prefix automation, gap recording, and worker persistence</name>
  <files>lib/cairnloop/automation.ex, lib/cairnloop/knowledge_automation.ex, lib/cairnloop/knowledge_automation/candidate_builder.ex, lib/cairnloop/retrieval/gap_recorder.ex, lib/cairnloop/retrieval/workers/prune_gap_events.ex, lib/cairnloop/workers/tool_execution_worker.ex, lib/cairnloop/workers/approval_expiry_worker.ex, lib/cairnloop/workers/approval_resume_worker.ex, lib/cairnloop/workers/outbound_worker.ex, lib/cairnloop/workers/sla_countdown_worker.ex, test/integration/schema_prefix_runtime_test.exs</files>
  <read_first>
  - lib/cairnloop/automation.ex
  - lib/cairnloop/knowledge_automation.ex
  - lib/cairnloop/knowledge_automation/candidate_builder.ex
  - lib/cairnloop/retrieval/gap_recorder.ex
  - lib/cairnloop/retrieval/workers/prune_gap_events.ex
  - lib/cairnloop/workers/tool_execution_worker.ex
  - lib/cairnloop/workers/approval_expiry_worker.ex
  - lib/cairnloop/workers/approval_resume_worker.ex
  - lib/cairnloop/workers/outbound_worker.ex
  - lib/cairnloop/workers/sla_countdown_worker.ex
  - test/integration/schema_prefix_runtime_test.exs
  - lib/cairnloop/schema_prefix.ex
  - .planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
  </read_first>
  <behavior>
  - Automation and worker reads/writes target the configured Cairnloop support prefix where schema/query/write behavior requires explicit prefixing.
  - Worker enqueue calls, Oban jobs, Oban unique keys, and `oban_jobs` placement are not redirected to the Cairnloop prefix.
  - Runtime integration coverage names automation drafts/SLA, gap recording/pruning, approval/tool/outbound/SLA workers, and worker state transitions.
  </behavior>
  <action>Audit each listed module for direct repo calls, preloads, transactions, query sources, `insert_all`, `update_all`, `delete_all`, and worker state persistence. Add `Cairnloop.SchemaPrefix.configured()` or `repo_opts/1` only where needed to make DB-05 true; do not introduce a second helper module. Keep `Oban.Job`, `Oban.insert`, Oban unique keys, and `oban_jobs` outside the Cairnloop prefix per D-07. Extend `schema_prefix_runtime_test.exs` so automation drafts/SLA, retrieval gap recorder/pruner, approval/tool/outbound/SLA worker state transitions, and collision behavior are covered by DB-backed assertions.</action>
  <acceptance_criteria>
  - Automation and worker persistence paths use the configured prefix where required by Ecto query/write behavior.
  - Runtime integration test names automation, gap recorder/pruner, approval/tool/outbound/SLA worker paths as DB-05 coverage.
  - Tests assert Oban enqueue/job placement is not routed through `Cairnloop.SchemaPrefix`.
  - No public function signature changes are introduced.
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
    <automated>mix test.integration --only integration test/integration/schema_prefix_runtime_test.exs</automated>
  </verify>
  <done>Automation and worker persistence paths honor the configured support prefix without changing sealed public APIs or Oban ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker runtime -> host Repo | Automation and workers write into host-owned Postgres through Cairnloop support schemas. |
| worker enqueue -> Oban | Worker scheduling crosses into host-owned Oban tables that must not be schema-prefixed by Cairnloop config. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-45 | Tampering | automation/worker query prefixing | mitigate | Apply explicit prefix handling to query sources/write opts where Ecto precedence requires it. |
| T-59-46 | Integrity | worker state transitions | mitigate | Preserve existing worker behavior while adding prefix behavior to support-domain persistence. |
| T-59-47 | Information Disclosure / Integrity | public/dedicated split | mitigate | Add runtime collision assertions for automation and worker paths. |
| T-59-48 | Denial of Service / Integrity | Oban enqueue/table ownership | mitigate | Do not route Oban inserts/jobs/tables through Cairnloop prefix; assert boundary in tests. |
</threat_model>

<verification>
Run warnings-clean compile and the runtime integration test after Plan 59-05 has created the test-host support schema substrate.
</verification>

<success_criteria>
- Automation, gap recording, and worker persistence honor configured prefix.
- Oban boundary remains intact.
- Runtime DB-backed proof covers worker-driven support-domain writes.
</success_criteria>

<output>
Create `.planning/phases/59-dedicated-postgres-schema-contract/59-10-SUMMARY.md` when done.
</output>
