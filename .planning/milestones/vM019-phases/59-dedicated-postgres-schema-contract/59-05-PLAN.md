---
phase: 59-dedicated-postgres-schema-contract
plan: "05"
type: execute
wave: 4
depends_on:
  - "59-01"
  - "59-02"
  - "59-08"
files_modified:
  - lib/mix/tasks/cairnloop/install.ex
  - test/cairnloop/tasks/install_test.exs
  - test/cairnloop/migrations_test.exs
  - priv/test_host/migrations/20260101000000_create_host_owned_tables.exs
  - priv/test_host/migrations/20260525000001_add_run_key_to_messages.exs
  - priv/test_host/migrations/20260526100000_add_outbound_to_messages.exs
  - priv/test_host/migrations/20260527070000_add_conversation_slas.exs
  - UPGRADING.md
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-06
must_haves:
  truths:
    - "DB-01/D-13: installer and integration test host create support-domain tables in the dedicated prefix by default."
    - "DB-02/D-03: public-schema compatibility is explicit and documented narrowly for existing installs."
    - "D-07/D-14: Oban setup remains separate and host-owned."
  artifacts:
    - path: "lib/mix/tasks/cairnloop/install.ex"
      provides: "prefix-aware installer-generated host-support migration and setup copy"
    - path: "priv/test_host/migrations/*.exs"
      provides: "integration host support-domain migrations in configured prefix"
    - path: "UPGRADING.md"
      provides: "narrow public compatibility and dedicated-schema move notes"
  key_links:
    - from: "lib/mix/tasks/cairnloop/install.ex"
      to: "priv/test_host/migrations/20260101000000_create_host_owned_tables.exs"
      via: "same support table prefix pattern"
      pattern: "prefix = Cairnloop.SchemaPrefix.configured"
---

<objective>
Make installer output and the integration test host match the dedicated-schema contract.

Purpose: New installs and DB-backed tests need the same prefix-aware host-support substrate before library migrations and runtime proof can be trusted.
Output: Prefix-aware installer/test-host migrations, narrow upgrade wording, and a clean test DB setup command that cannot be satisfied by stale public tables.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-CONTEXT.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-RESEARCH.md
@.planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
@.planning/phases/58-identity-ingress-and-side-effect-trust/58-01-SUMMARY.md
@docs/postgres-schema-prefix.md
@lib/mix/tasks/cairnloop/install.ex
@test/cairnloop/tasks/install_test.exs
@priv/test_host/migrations/20260101000000_create_host_owned_tables.exs
@priv/test_host/migrations/20260101000001_add_oban_jobs.exs
@UPGRADING.md
</context>

## Artifacts this phase produces

- Installer-generated host migration using `prefix = Cairnloop.SchemaPrefix.configured()`.
- Installer copy that names `config :cairnloop, :schema_prefix, "cairnloop"` for new installs.
- Installer copy that names `config :cairnloop, :schema_prefix, "public"` for explicit public compatibility and mentions nil as legacy accepted config.
- Removal of any claim that `mix ecto.migrate --prefix cairnloop` alone completes the contract.
- Prefix-aware test-host support migrations for conversations, messages, drafts, run keys, outbound message fields, and conversation SLAs.
- `UPGRADING.md` narrow notes for existing public-schema users.
- Commands: `mix test test/cairnloop/tasks/install_test.exs test/cairnloop/migrations_test.exs --warnings-as-errors`, `MIX_ENV=test mix ecto.drop --quiet -r Cairnloop.Repo -r Chimeway.Repo || true`, `mix test.setup`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update installer substrate and narrow upgrade wording</name>
  <files>lib/mix/tasks/cairnloop/install.ex, test/cairnloop/tasks/install_test.exs, UPGRADING.md</files>
  <read_first>
  - lib/mix/tasks/cairnloop/install.ex
  - test/cairnloop/tasks/install_test.exs
  - UPGRADING.md
  - .planning/phases/59-dedicated-postgres-schema-contract/59-CONTEXT.md
  - .planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
  - docs/postgres-schema-prefix.md
  </read_first>
  <behavior>
  - Installer-generated host migration creates the configured schema and support-domain tables with `prefix: prefix`.
  - Installer wording keeps Oban setup separate and does not frame CLI `--prefix` as sufficient.
  - Existing public installs have explicit compatibility wording for `"public"` and legacy nil.
  </behavior>
  <action>Update `mix cairnloop.install` per D-13. Generated migration source must use `prefix = Cairnloop.SchemaPrefix.configured()`, `CREATE SCHEMA IF NOT EXISTS`, `create table(..., prefix: prefix)`, `references(..., prefix: prefix)`, and `create index(..., prefix: prefix)` for host-scaffolded Cairnloop support-domain tables. Replace installer copy that implies `mix ecto.migrate --prefix cairnloop` completes the contract with wording that source-qualified migrations plus `config :cairnloop, :schema_prefix, "cairnloop"` are the default. Add explicit public compatibility copy using `"public"` and mention nil as accepted legacy. Keep `UPGRADING.md` edits narrow to DB-02: public compatibility, dedicated-schema move outline, rollback posture, and no shared `vector`/Oban drops.</action>
  <acceptance_criteria>
  - Installer tests assert default `"cairnloop"` config, explicit `"public"` compatibility, and legacy nil acceptance.
  - Installer tests reject copy that describes `--prefix` as the complete contract.
  - Installer-generated migration includes prefix-aware conversations, messages, `customer_ref`, and indexes.
  - `UPGRADING.md` mentions public compatibility and dedicated-schema move steps without broad Phase 60 docs cleanup.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/tasks/install_test.exs --warnings-as-errors</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Installer and upgrade copy satisfy DB-01/DB-02 without overstepping into Phase 60 broad docs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert integration test-host support migrations</name>
  <files>priv/test_host/migrations/20260101000000_create_host_owned_tables.exs, priv/test_host/migrations/20260525000001_add_run_key_to_messages.exs, priv/test_host/migrations/20260526100000_add_outbound_to_messages.exs, priv/test_host/migrations/20260527070000_add_conversation_slas.exs, test/cairnloop/migrations_test.exs</files>
  <read_first>
  - priv/test_host/migrations/20260101000000_create_host_owned_tables.exs
  - priv/test_host/migrations/20260101000001_add_oban_jobs.exs
  - priv/test_host/migrations/20260525000001_add_run_key_to_messages.exs
  - priv/test_host/migrations/20260526100000_add_outbound_to_messages.exs
  - priv/test_host/migrations/20260527070000_add_conversation_slas.exs
  - test/cairnloop/migrations_test.exs
  - lib/cairnloop/schema_prefix.ex
  - .planning/phases/59-dedicated-postgres-schema-contract/59-PATTERNS.md
  </read_first>
  <behavior>
  - Test-host Cairnloop support tables are created and altered under the configured support prefix.
  - `priv/test_host/migrations/20260101000001_add_oban_jobs.exs` remains host-owned and unprefixed.
  - Source scans cover test-host support migrations without failing on the deliberate Oban migration.
  </behavior>
  <action>Convert test-host support-domain migrations to the same prefix pattern as the installer per D-14. Add `prefix = Cairnloop.SchemaPrefix.configured()`, create the schema when needed, pass `prefix: prefix` to support-domain table/index/reference/alter/drop operations, and keep `AddObanJobs` unchanged except for any comments needed to preserve the host-owned boundary. Extend `test/cairnloop/migrations_test.exs` to scan `priv/test_host/migrations/*.exs` for Cairnloop support-domain prefix drift while allowlisting the Oban migration by filename and purpose, not by broad text matching.</action>
  <acceptance_criteria>
  - Test-host conversations/messages/drafts/SLA support tables and alters use `prefix: prefix`.
  - `priv/test_host/migrations/20260101000001_add_oban_jobs.exs` still calls `Oban.Migration.up()`/`down()` without `Cairnloop.SchemaPrefix`.
  - Source scan fails if future test-host `cairnloop_*` DDL is unprefixed.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/cairnloop/migrations_test.exs test/cairnloop/tasks/install_test.exs --warnings-as-errors</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Integration host setup can create prefixed support tables while Oban remains public/host-owned.</done>
</task>

<task type="auto">
  <name>Task 3: Run clean test-host DB setup proof</name>
  <files>priv/test_host/migrations/20260101000000_create_host_owned_tables.exs, priv/test_host/migrations/20260525000001_add_run_key_to_messages.exs, priv/test_host/migrations/20260526100000_add_outbound_to_messages.exs, priv/test_host/migrations/20260527070000_add_conversation_slas.exs, test/integration/schema_prefix_contract_test.exs</files>
  <read_first>
  - mix.exs
  - config/test.exs
  - test/integration/schema_prefix_contract_test.exs
  - .planning/phases/59-dedicated-postgres-schema-contract/59-VALIDATION.md
  </read_first>
  <action>Run a clean test database setup after migration/config changes so stale public tables cannot mask missing dedicated-schema placement. Reset only the test databases. Then run the setup alias and the dedicated schema contract test subset that asserts host-support tables and library objects exist in `cairnloop` while `oban_jobs` remains host-owned.</action>
  <acceptance_criteria>
  - Clean setup creates schema `cairnloop`.
  - `cairnloop.cairnloop_conversations`, `cairnloop.cairnloop_messages`, `cairnloop.cairnloop_drafts`, and `cairnloop.cairnloop_conversation_slas` exist.
  - `public.oban_jobs` exists or Oban host ownership is asserted according to the existing test-host migration.
  - Same-name public support tables do not satisfy the contract assertions.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix ecto.drop --quiet -r Cairnloop.Repo -r Chimeway.Repo || true</automated>
    <automated>mix test.setup</automated>
    <automated>mix test.integration --only integration test/integration/schema_prefix_contract_test.exs</automated>
  </verify>
  <done>DB setup/migration proof passes after dedicated-schema library and test-host migrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer output -> adopter database | Generated migration/copy shapes real host database setup. |
| test-host migrations -> local/CI DB | Test-host migrations can mask or expose dedicated-schema failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-21 | Tampering | generated migration identifiers | mitigate | Use `Cairnloop.SchemaPrefix` helper in generated migration source. |
| T-59-22 | Integrity | installer guidance | mitigate | Tests reject wording that treats `--prefix` as sufficient and require explicit config. |
| T-59-23 | Information Disclosure / Integrity | stale public tables | mitigate | Clean test DB setup plus schema-scoped catalog assertions. |
| T-59-24 | Denial of Service | shared vector extension | mitigate | Upgrade wording and migration scans prohibit dropping shared `vector`. |
| T-59-25 | Denial of Service / Integrity | Oban setup | mitigate | Keep Oban migration separate and source-scan the boundary. |
</threat_model>

<verification>
Run focused installer/source-scan tests, compile, then clean test DB setup and dedicated-schema integration contract assertions.
</verification>

<success_criteria>
- Installer/test-host substrate defaults to `cairnloop`.
- Existing public installs have explicit compatibility guidance.
- DB setup proof cannot pass due to stale public tables.
</success_criteria>

<output>
Create `.planning/phases/59-dedicated-postgres-schema-contract/59-05-SUMMARY.md` when done.
</output>
