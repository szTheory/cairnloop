---
phase: 58-identity-ingress-and-side-effect-trust
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/ingress/email_webhook_plug.ex
  - lib/cairnloop/ingress/email_webhook_verifier.ex
  - lib/cairnloop/web/mcp/auth_plug.ex
  - lib/cairnloop/web/mcp/router.ex
  - guides/05-mcp-clients.md
  - test/cairnloop/ingress/email_webhook_plug_test.exs
  - test/cairnloop/web/mcp/auth_plug_test.exs
  - test/cairnloop/web/mcp/router_test.exs
  - test/cairnloop/docs_trust_test.exs
autonomous: true
requirements:
  - TRUST-03
  - TRUST-04
  - OPS-04
must_haves:
  truths:
    - "TRUST-03 / D-04: email webhook ingress authenticates before body parse and enqueue"
    - "TRUST-03 / D-05: email webhook has no literal default secret and supports an explicit host verifier/token seam"
    - "TRUST-03 validation: test/cairnloop/ingress/email_webhook_plug_test.exs covers missing, wrong, and configured auth plus halted unauthorized responses"
    - "TRUST-04 / D-06: MCP initialize, tools/list, and tools/call fail closed without valid Bearer token"
    - "TRUST-04 validation: test/cairnloop/web/mcp/router_test.exs covers missing-token initialize/tools-list/tools-call before metadata or write exposure"
    - "OPS-04: MCP guide names the actual AuthPlug module, raw token shape, and failure domains"
  prohibitions:
    - "Email webhook must not read request body or call Oban.insert after unauthorized response"
    - "MCP tools/list must not expose configured tool metadata without valid Bearer token"
    - "MCP initialize must not expose capability metadata without valid Bearer token"
    - "MCP well-known OAuth/resource metadata remains the only public discovery surface"
  artifacts:
    - path: lib/cairnloop/ingress/email_webhook_verifier.ex
      provides: "host-configured email webhook verifier seam"
      exports: ["verify/1"]
    - path: lib/cairnloop/ingress/email_webhook_plug.ex
      provides: "halted fail-closed webhook auth before parse/enqueue"
    - path: lib/cairnloop/web/mcp/router.ex
      provides: "token-required JSON-RPC method gate"
    - path: guides/05-mcp-clients.md
      provides: "MCP auth docs matching code"
    - path: test/cairnloop/ingress/email_webhook_plug_test.exs
      provides: "TRUST-03 regression coverage"
    - path: test/cairnloop/web/mcp/router_test.exs
      provides: "TRUST-04 regression coverage"
    - path: test/cairnloop/docs_trust_test.exs
      provides: "MCP auth docs source-scan coverage"
  key_links:
    - from: lib/cairnloop/ingress/email_webhook_plug.ex
      to: lib/cairnloop/ingress/email_webhook_verifier.ex
      via: "call/2 verifies request before read_body/1"
      pattern: "EmailWebhookVerifier.verify"
    - from: lib/cairnloop/web/mcp/router.ex
      to: lib/cairnloop/web/mcp/auth_plug.ex
      via: "Router calls AuthPlug and gates token-required methods"
      pattern: "mcp_token"
    - from: guides/05-mcp-clients.md
      to: lib/cairnloop/web/mcp/auth_plug.ex
      via: "docs name AuthPlug and Bearer behavior"
      pattern: "AuthPlug"
---

<objective>
Make email webhook and MCP ingress fail closed before sensitive parsing, tool metadata exposure, or governed write surfaces.

Purpose: close inbound-auth gaps without adding provider-specific email product behavior or replacing the existing MCP router.
Output: halted email auth, explicit email verifier seam, token-required MCP JSON-RPC methods, and docs/tests that match live code.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-evidence-and-trust-audit/57-SUMMARY.md
@.planning/phases/58-identity-ingress-and-side-effect-trust/58-CONTEXT.md
@.planning/phases/58-identity-ingress-and-side-effect-trust/58-RESEARCH.md
@.planning/phases/58-identity-ingress-and-side-effect-trust/58-VALIDATION.md
@lib/cairnloop/ingress/email_webhook_plug.ex
@lib/cairnloop/web/mcp/auth_plug.ex
@lib/cairnloop/web/mcp/router.ex
@guides/05-mcp-clients.md
</context>

## Artifacts this phase produces

- New module `Cairnloop.Ingress.EmailWebhookVerifier` with public `verify/1`.
- New config key `:email_webhook_verifier` for host request-auth callbacks.
- Retained config key `:email_webhook_token` as the simple shared-token seam.
- New private MCP method gate in `Cairnloop.Web.MCP.Router`.
- Updated `guides/05-mcp-clients.md` auth, module, token, and fail-closed behavior.
- Extended tests for email webhook, MCP auth plug, and MCP router.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Halt email webhook auth failures before parse and enqueue</name>
  <files>lib/cairnloop/ingress/email_webhook_plug.ex, lib/cairnloop/ingress/email_webhook_verifier.ex, test/cairnloop/ingress/email_webhook_plug_test.exs</files>
  <read_first>
  - .planning/phases/58-identity-ingress-and-side-effect-trust/58-CONTEXT.md
  - .planning/phases/58-identity-ingress-and-side-effect-trust/58-VALIDATION.md
  - lib/cairnloop/ingress/email_webhook_plug.ex
  - test/cairnloop/ingress/email_webhook_plug_test.exs
  - lib/cairnloop/workers/process_message.ex
  </read_first>
  <action>Add `Cairnloop.Ingress.EmailWebhookVerifier.verify/1` so `EmailWebhookPlug.call/2` can authenticate through an explicit host verifier or the existing `:email_webhook_token`. Verify before `Plug.Conn.read_body/1`, return JSON 401, and call `halt/1` on unauthorized responses. Preserve the existing successful queued email stub and do not add provider-specific email-to-conversation behavior per D-04 and D-05.</action>
  <acceptance_criteria>
  - No configured verifier/token rejects with 401 and halted conn.
  - Wrong token rejects with 401 and halted conn.
  - Configured token or verifier allows the existing JSON parse and `ProcessMessage` enqueue path.
  - Unauthorized tests prove `read_body/1` and `Oban.insert/1` are not reached after rejection.
  </acceptance_criteria>
  <done>Email webhook auth rejects and halts unsafe requests before body parsing or enqueue while preserving configured success behavior.</done>
  <verify>
    <automated>mix test test/cairnloop/ingress/email_webhook_plug_test.exs --warnings-as-errors</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require Bearer auth for MCP token-required methods</name>
  <files>lib/cairnloop/web/mcp/auth_plug.ex, lib/cairnloop/web/mcp/router.ex, test/cairnloop/web/mcp/auth_plug_test.exs, test/cairnloop/web/mcp/router_test.exs</files>
  <read_first>
  - .planning/phases/58-identity-ingress-and-side-effect-trust/58-CONTEXT.md
  - .planning/phases/58-identity-ingress-and-side-effect-trust/58-RESEARCH.md
  - lib/cairnloop/web/mcp/auth_plug.ex
  - lib/cairnloop/web/mcp/router.ex
  - lib/cairnloop/web/mcp/well_known_plug.ex
  - test/cairnloop/web/mcp/auth_plug_test.exs
  - test/cairnloop/web/mcp/router_test.exs
  </read_first>
  <action>Keep `AuthPlug` as the token validator that assigns `:mcp_token`, then update `Router.call/2` or method dispatch to require that assign before `initialize`, `tools/list`, and `tools/call`. Return HTTP 401 with `WWW-Authenticate: Bearer` for missing/invalid tokens on those methods. Preserve JSON-RPC 200 error envelopes for malformed JSON and unsupported methods when the request is not trying to access a token-required method. Leave `WellKnownPlug` public.</action>
  <acceptance_criteria>
  - Missing/invalid token on `initialize` returns 401 and no `capabilities.tools`.
  - Missing/invalid token on `tools/list` returns 401 and no tool array.
  - Missing/invalid token on `tools/call` still returns 401 before Governance is called.
  - Valid token keeps existing initialize/list/call success behavior.
  - Malformed JSON and unsupported method tests keep their current JSON-RPC error shape unless the method is token-required.
  </acceptance_criteria>
  <done>MCP token-required methods return 401 without a valid Bearer token and preserve valid-token JSON-RPC behavior.</done>
  <verify>
    <automated>mix test test/cairnloop/web/mcp/auth_plug_test.exs test/cairnloop/web/mcp/router_test.exs --warnings-as-errors</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align MCP/email auth docs with live behavior</name>
  <files>guides/05-mcp-clients.md, test/cairnloop/docs_trust_test.exs</files>
  <read_first>
  - .planning/phases/58-identity-ingress-and-side-effect-trust/58-CONTEXT.md
  - .planning/phases/58-identity-ingress-and-side-effect-trust/58-VALIDATION.md
  - guides/05-mcp-clients.md
  - lib/cairnloop/web/mcp/auth_plug.ex
  - lib/cairnloop/mcp.ex
  - lib/cairnloop/ingress/email_webhook_plug.ex
  </read_first>
  <action>Create or extend `test/cairnloop/docs_trust_test.exs` to source-scan the MCP guide for the actual `Cairnloop.Web.MCP.AuthPlug` module, Bearer-token wording, no stale token prefix claim, and fail-closed `initialize` / `tools/list` / `tools/call` behavior. Update `guides/05-mcp-clients.md` with host-owned router wiring, public well-known discovery scope, and the actual Settings token copy behavior. Mention email webhook auth responsibility only if the guide cross-links troubleshooting; keep broad docs overhaul out of this phase.</action>
  <acceptance_criteria>
  - MCP guide no longer names a nonexistent auth module.
  - MCP guide no longer claims raw tokens have a fixed prefix.
  - MCP guide states `initialize`, `tools/list`, and `tools/call` require a valid Bearer token.
  - Docs source-scan test fails on stale module/token/auth claims.
  </acceptance_criteria>
  <done>MCP/email auth documentation matches live modules, token behavior, and fail-closed method requirements with source-scan coverage.</done>
  <verify>
    <automated>mix test test/cairnloop/docs_trust_test.exs test/cairnloop/web/mcp/router_test.exs test/cairnloop/ingress/email_webhook_plug_test.exs --warnings-as-errors</automated>
    <automated>mix compile --warnings-as-errors</automated>
    <automated>mix ci.fast</automated>
    <automated>mix ci.integration</automated>
    <automated>mix ci.quality</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| email provider -> EmailWebhookPlug | untrusted HTTP request could spoof inbound email |
| MCP client -> MCP Router | untrusted JSON-RPC request could enumerate tools or create proposals |
| docs -> host adopter | stale docs could cause unsafe mounting/auth assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04-01 | Spoofing | EmailWebhookPlug | mitigate | verifier/token check before body parse and enqueue; halt unauthorized conn |
| T-58-04-02 | Information Disclosure | MCP tools/list and initialize | mitigate | Bearer token required before capability/tool metadata |
| T-58-04-03 | Tampering | MCP tools/call | mitigate | valid token required before Governance.propose/3 |
| T-58-04-04 | Repudiation | docs/auth setup | mitigate | source-scan docs test pins module/token/auth truth |
| T-58-04-SC | Tampering | package installs | accept | no new npm/pip/cargo package installs in this plan |
</threat_model>

<verification>
- Focused: `mix test test/cairnloop/ingress/email_webhook_plug_test.exs test/cairnloop/web/mcp/auth_plug_test.exs test/cairnloop/web/mcp/router_test.exs test/cairnloop/docs_trust_test.exs --warnings-as-errors`
- Build: `mix compile --warnings-as-errors`
- Wave gate: `mix ci.fast`
- DB-backed MCP token gate: `mix ci.integration`
- Docs/package gate: `mix ci.quality`
</verification>

<success_criteria>
- TRUST-03 is satisfied for email webhook auth and docs truth.
- TRUST-04 is satisfied for MCP token-required methods.
- OPS-04 gains enough MCP/email troubleshooting truth to isolate host config versus Cairnloop behavior.
</success_criteria>

<output>
Create `.planning/phases/58-identity-ingress-and-side-effect-trust/58-04-SUMMARY.md` when done.
</output>
