---
phase: 38-shared-page-shell-migration
plan: 04
type: execute
wave: 2
depends_on: ["38-02", "38-03"]
files_modified:
  - lib/cairnloop/web/knowledge_base_live/editor.ex
  - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex
  - test/cairnloop/web/knowledge_base_live_test.exs
  - test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs
autonomous: true
requirements: [SHELL-02]
must_haves:
  truths:
    - "Opening the editor from a conversation renders an origin-aware breadcrumb with ≥2 crumbs and a working navigate= back link to the conversation"
    - "The editor origin crumb shows a humanized label ('Conversation' / 'Suggestions') — never the raw return_to path"
    - "The editor's last breadcrumb crumb carries aria-current=page and no href"
    - "Suggestion review now renders a cl_breadcrumb (it had none) with a Knowledge link and a current Suggestions crumb"
  artifacts:
    - path: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      provides: "Editor :breadcrumb slot fed by BreadcrumbPresenter.editor_items/2 (origin-aware)"
      contains: "BreadcrumbPresenter.editor_items"
    - path: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      provides: "New :breadcrumb slot fed by BreadcrumbPresenter (static lane crumbs)"
      contains: "BreadcrumbPresenter"
  key_links:
    - from: "editor.ex :breadcrumb slot"
      to: "Cairnloop.Web.BreadcrumbPresenter.editor_items/2"
      via: "items={BreadcrumbPresenter.editor_items(@review_context.return_to, @article.title)}"
      pattern: "BreadcrumbPresenter\\.editor_items"
    - from: "suggestion_review.ex :breadcrumb slot"
      to: "Cairnloop.Web.BreadcrumbPresenter"
      via: "items={BreadcrumbPresenter.suggestions_items(...)}"
      pattern: "BreadcrumbPresenter"
---

<objective>
Wire the `BreadcrumbPresenter` (plan 38-03) into the editor and suggestion_review `:breadcrumb`
slots (slots established in plan 38-02). This delivers SHELL-02: the orphaned `cl_breadcrumb`
primitive becomes a live, origin-aware "you are here" trail.

- Editor: replace the static breadcrumb items with `BreadcrumbPresenter.editor_items/2`, so when
  `return_to` is present the trail prepends a humanized origin back crumb (≥2 crumbs + a working
  `navigate=` back link → satisfies SHELL-02 / success-criterion 2).
- Suggestion review: ADD a `cl_breadcrumb` it currently lacks (static lane crumbs), per D-01.

Purpose: Operators always know where they are on the deep KB-from-conversation path, with a real
back link (D-01). The origin label is humanized (never the raw path) per the copy rule + V5.
Output: Two wired `:breadcrumb` slots + render assertions proving the ≥2-crumbs / back-link /
humanized-label contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38-shared-page-shell-migration/38-CONTEXT.md
@.planning/phases/38-shared-page-shell-migration/38-RESEARCH.md
@.planning/phases/38-shared-page-shell-migration/38-PATTERNS.md
@.planning/phases/38-shared-page-shell-migration/38-02-SUMMARY.md
@.planning/phases/38-shared-page-shell-migration/38-03-SUMMARY.md

<interfaces>
<!-- BreadcrumbPresenter — built in plan 38-03 (lib/cairnloop/web/breadcrumb_presenter.ex) -->
editor_items(return_to, title)
  # binary return_to => [%{label: origin, href: return_to}, %{label: "Knowledge", href: "/knowledge-base"}, %{label: "Editing: #{title}"}]
  #   origin = "Suggestions" when return_to starts with "/knowledge-base", else "Conversation"
  # nil/non-binary return_to => [%{label: "Knowledge", href: "/knowledge-base"}, %{label: "Editing: #{title}"}]
  # last item ALWAYS omits :href (current crumb)
suggestions_items(...)  # static lane crumbs: [%{label: "Knowledge", href: "/knowledge-base"}, %{label: "Suggestions"}]
                        #   (+ optional current task crumb when a selected task title is supplied)

<!-- cl_breadcrumb/1 — components.ex:385-401: items WITH href => <.link navigate=>; no-href => <span aria-current="page"> -->

<!-- Verified origin assign (editor) — editor.ex:145-153: @review_context.return_to is the signed-token-verified navigable path -->
<!-- Suggestion review lane assigns — @queue_filter, @selected_task (suggestion_review.ex :13-57) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire origin-aware breadcrumb into the KB editor</name>
  <files>lib/cairnloop/web/knowledge_base_live/editor.ex, test/cairnloop/web/knowledge_base_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/knowledge_base_live/editor.ex (the :breadcrumb slot established by 38-02; @review_context.return_to assign at :145-153; @article.title)
    - lib/cairnloop/web/breadcrumb_presenter.ex (editor_items/2 — built in 38-03)
    - lib/cairnloop/web/components.ex (cl_breadcrumb/1 :385-401)
    - lib/cairnloop/web/conversation_live.ex (handoff producer :180-206 — confirms a conversation return_to is the bare "/#{id}" path)
    - test/cairnloop/web/knowledge_base_live_test.exs (editor render harness; ≥2-crumb assertion example :716-721)
    - .planning/phases/38-shared-page-shell-migration/38-RESEARCH.md (§"Code Examples" — ≥2-crumbs + back-link render assertion; §"Common Pitfalls" 1 & 2)
  </read_first>
  <behavior>
    - Test 1 (from a conversation): with `@review_context.return_to = "/42"`, the editor render contains `class="cl-breadcrumb"`, a `navigate="/42"` (or `href="/42"`) back link, a `class="cl-breadcrumb__sep"` separator (≥2 crumbs ⇒ ≥1 separator), and an `aria-current="page"` current crumb. The origin crumb text is "Conversation" — and the raw string "/42" does NOT appear as crumb LABEL text (it appears only inside the href).
    - Test 2 (from the suggestion lane): with `@review_context.return_to = "/knowledge-base/suggestions?task=7"`, the origin crumb label is "Suggestions" with a working back link.
    - Test 3 (no origin): with `@review_context.return_to = nil`, the static fallback renders ("Knowledge" link + current "Editing: …" crumb), still a valid trail.
  </behavior>
  <action>
    In `editor.ex`, ensure `Cairnloop.Web.BreadcrumbPresenter` is aliased/imported, then replace the
    static items list in the `:breadcrumb` slot (the slot 38-02 established) with
    `<.cl_breadcrumb items={Cairnloop.Web.BreadcrumbPresenter.editor_items(@review_context.return_to, @article.title)} />`.
    This makes the trail origin-aware: a conversation `return_to` prepends a "Conversation" back
    crumb (≥2 crumbs + working `navigate=` link → success-criterion 2); a lane `return_to` prepends
    "Suggestions"; nil falls back to the static `Knowledge / Editing` trail. Do NOT branch on
    `@review_origin?` (Pitfall 1) — the presenter derives the label from the path shape. Do NOT
    re-decode or re-sign the token — reuse the already-verified `@review_context.return_to` assign.

    Extend `knowledge_base_live_test.exs` with the three <behavior> render assertions using the
    existing headless `rendered_to_string(Editor.render(assigns))` harness; build assigns with each
    `return_to` value. Mark any genuinely Repo-needing assertion `# REPO-UNAVAILABLE` (none expected
    — the breadcrumb render is pure).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/knowledge_base_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "BreadcrumbPresenter.editor_items" lib/cairnloop/web/knowledge_base_live/editor.ex` returns ≥1; the static inline items list is no longer hardcoded in the slot.
    - Render (conversation origin): rendered string contains `cl-breadcrumb`, `navigate="/42"` (or `href="/42"`), `cl-breadcrumb__sep`, `aria-current="page"`, and the label text "Conversation"; the substring `>/42<` (raw path as a crumb LABEL) does NOT appear.
    - Render (lane origin): rendered string contains the label text "Suggestions" with a working back link.
    - Render (no origin): rendered string contains the static "Knowledge" link + a current "Editing: " crumb.
    - Command: `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/web/knowledge_base_live_test.exs` exits 0.
  </acceptance_criteria>
  <done>The editor breadcrumb is origin-aware via `BreadcrumbPresenter.editor_items/2`: ≥2 crumbs + a working back link when `return_to` is present, humanized label (never the raw path), `aria-current` on the last crumb, static fallback when absent; warnings-clean; editor render tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the static lane breadcrumb to suggestion_review</name>
  <files>lib/cairnloop/web/knowledge_base_live/suggestion_review.ex, test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</files>
  <read_first>
    - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex (the cl_page :breadcrumb slot is currently unused after 38-02; @queue_filter / @selected_task assigns :13-57; task_patch/2 :461, queue_patch/2 :466 helpers)
    - lib/cairnloop/web/breadcrumb_presenter.ex (suggestions_items — built in 38-03)
    - lib/cairnloop/web/components.ex (cl_breadcrumb/1 :385-401)
    - test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs (render harness near :872)
    - .planning/phases/38-shared-page-shell-migration/38-RESEARCH.md (§"Breadcrumb Origin Plumbing" (c) — suggestion_review has NO conversation origin; static lane crumb only; do NOT invent a conversation→suggestion_review handoff — that's P42)
  </read_first>
  <behavior>
    - Test 1: the suggestion_review render now contains `class="cl-breadcrumb"` (it had none before), a "Knowledge" back link (`navigate`/`href="/knowledge-base"`), and a current "Suggestions" crumb with `aria-current="page"` and no href.
    - Test 2 (if a selected-task title is supplied): a 3rd current crumb renders the task title and "Suggestions" becomes a back link to "/knowledge-base/suggestions".
  </behavior>
  <action>
    In `suggestion_review.ex`, ensure `Cairnloop.Web.BreadcrumbPresenter` is aliased/imported, then
    add a `<:breadcrumb>` slot to the `cl_page` (the slot 38-02 left unused) containing
    `<.cl_breadcrumb items={Cairnloop.Web.BreadcrumbPresenter.suggestions_items(...)} />`. Pass the
    lane signals it has (`@queue_filter` / `@selected_task`) per the presenter's arity from 38-03;
    when a task is selected, the presenter appends a current task crumb and links "Suggestions" back
    to "/knowledge-base/suggestions". This is a STATIC/lane-derived crumb — do NOT receive or invent
    a conversation `return_to` (suggestion_review is a producer of editor handoffs, not a receiver of
    a conversation origin; inventing one is P42 scope creep — Pitfall in RESEARCH (c)).

    Extend `suggestion_review_test.exs` with the <behavior> assertions using the existing headless
    render harness. Mark Repo-needing parts `# REPO-UNAVAILABLE` if any.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "BreadcrumbPresenter" lib/cairnloop/web/knowledge_base_live/suggestion_review.ex` returns ≥1; `grep -c "<:breadcrumb>" lib/cairnloop/web/knowledge_base_live/suggestion_review.ex` returns ≥1.
    - Render: rendered string contains `cl-breadcrumb`, a "/knowledge-base" back link, "Suggestions", and `aria-current="page"` (the screen now has a breadcrumb it previously lacked).
    - Render: no conversation/`return_to` path is rendered (the crumb is purely lane-derived — no invented handoff).
    - Command: `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs` exits 0.
  </acceptance_criteria>
  <done>Suggestion review renders a static lane `cl_breadcrumb` (it had none) via `BreadcrumbPresenter`: Knowledge back link + current Suggestions crumb (+ task crumb when selected); no invented conversation origin; warnings-clean; suggestion_review render tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `return_to` path (editor) → rendered back-link href + derived label | The `return_to` is the only externally-influenced value rendered. It is ALREADY verified at mount (signed `handoff` token, `editor.ex:147,207-214`) and is already rendered as a `navigate=` href today. This plan reuses that verified value via the presenter — it does NOT decode raw params, re-sign, or accept an unverified origin. suggestion_review introduces NO new origin (static lane crumb only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-07 | Tampering (open-redirect via back crumb) | editor `:breadcrumb` href = `return_to` | mitigate | The href reuses `@review_context.return_to`, which is honored ONLY when it decodes from the signed `handoff` token (`verified_return_to_from_token/1`, `editor.ex:207-214`). No unverified param is rendered; no new token plumbing. Same verified value already used at `editor.ex:287`. |
| T-38-08 | Information disclosure / Spoofing (raw path reflected to operator) | editor origin crumb label | mitigate | Label is the humanized noun from `BreadcrumbPresenter.editor_items/2` (derived from path shape) — never the raw `return_to`. Asserted by the render test checking the raw `/42` does not appear as crumb LABEL text (copy rule + ASVS V5). |
| T-38-09 | Spoofing (mislabeled origin) | origin label derivation | mitigate | Label derives from `return_to` path shape, NOT `@review_origin?` (Pitfall 1 — that flag is true for any lane open). suggestion_review fabricates no conversation origin (static lane crumb). |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — pure in-repo wiring of an existing presenter + component. No install task in this plan. |

No `high`-severity threats: the only externally-influenced value (`return_to`) is verified upstream and reused, never re-trusted; the operator-visible label is a derived humanized noun (mitigated + test-enforced); suggestion_review adds no new origin input.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean after both tasks (Pitfall 3).
- Editor + suggestion_review render tests green, including the ≥2-crumbs / back-link / `aria-current` / humanized-label assertions (SHELL-02 / success-criterion 2).
- Full suite gate before `/gsd:verify-work`: `mix compile --warnings-as-errors && mix test` green (success-criterion 3); brand-token gate green; screenshot pipeline runs as a clean smoke (full baseline acceptance is P45 — do NOT block on baselines here).
</verification>

<success_criteria>
- The editor breadcrumb is origin-aware: ≥2 crumbs + a working `navigate=` back link when `return_to` is present, humanized label (never the raw path), `aria-current` on the current crumb; static fallback otherwise (SHELL-02 / success-criterion 2).
- Suggestion review renders a static lane `cl_breadcrumb` it previously lacked (D-01).
- The orphaned `cl_breadcrumb` primitive is now genuinely exercised on the deep KB path.
- `mix compile --warnings-as-errors` and the KB render tests pass; the full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/38-shared-page-shell-migration/38-04-SUMMARY.md` when done.
</output>
