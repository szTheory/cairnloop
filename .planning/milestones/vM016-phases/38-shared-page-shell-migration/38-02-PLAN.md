---
phase: 38-shared-page-shell-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/web/knowledge_base_live/index.ex
  - lib/cairnloop/web/knowledge_base_live/editor.ex
  - lib/cairnloop/web/knowledge_base_live/gaps.ex
  - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex
  - test/cairnloop/web/knowledge_base_live_test.exs
  - test/cairnloop/web/knowledge_base_live/gaps_test.exs
  - test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs
autonomous: true
requirements: [SHELL-01]
must_haves:
  truths:
    - "KB Index renders inside .cl-page--wide with title 'Knowledge Base', kb_nav in :subnav, and 'New article' in :actions"
    - "KB Editor renders inside .cl-page--wide with title 'Editing: {title}', kb_nav in :subnav, and its existing breadcrumb moved into the :breadcrumb slot"
    - "KB Gaps renders inside .cl-page--wide with title 'Knowledge gaps' and kb_nav in :subnav"
    - "KB Suggestion review renders inside .cl-page--wide with title 'Suggestion review' and kb_nav in :subnav"
  artifacts:
    - path: "lib/cairnloop/web/knowledge_base_live/index.ex"
      provides: "KB Index body wrapped in cl_page; kb_nav→:subnav; New article→:actions"
      contains: "<.cl_page"
    - path: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      provides: "KB Editor body wrapped in cl_page; breadcrumb in :breadcrumb slot; kb_nav→:subnav"
      contains: "<.cl_page"
    - path: "lib/cairnloop/web/knowledge_base_live/gaps.ex"
      provides: "KB Gaps body wrapped in cl_page; kb_nav→:subnav"
      contains: "<.cl_page"
    - path: "lib/cairnloop/web/knowledge_base_live/suggestion_review.ex"
      provides: "KB Suggestion review body wrapped in cl_page; kb_nav→:subnav"
      contains: "<.cl_page"
  key_links:
    - from: "KB screens"
      to: "cl_page :subnav slot"
      via: "kb_nav moved into <:subnav>"
      pattern: "<:subnav>"
    - from: "editor.ex"
      to: "cl_page :breadcrumb slot"
      via: "existing cl_breadcrumb moved into <:breadcrumb>"
      pattern: "<:breadcrumb>"
---

<objective>
Migrate the four KB sub-screens — Index, Editor, Gaps, Suggestion review — to render through
`cl_page` (D-02/D-03/D-04). This delivers SHELL-01 for the KB half of the cockpit: `kb_nav` tabs
move into the `:subnav` slot, single primary actions move into `:actions`, and the editor's
existing free-floating `cl_breadcrumb` moves into the `:breadcrumb` slot.

This plan establishes the editor's `:breadcrumb` slot with the CURRENT STATIC items shape (a
structural move only). The origin-aware breadcrumb logic + the new suggestion_review breadcrumb
are wired in plan 38-04 (wave 2) once `BreadcrumbPresenter` exists (plan 38-03).

Purpose: Uniform header/width across all KB detail screens (D-02/D-03). Pure structural lift (D-05).
Output: Four migrated KB render functions + extended headless render assertions proving each
renders inside `.cl-page--wide`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38-shared-page-shell-migration/38-CONTEXT.md
@.planning/phases/38-shared-page-shell-migration/38-RESEARCH.md
@.planning/phases/38-shared-page-shell-migration/38-PATTERNS.md

<interfaces>
<!-- cl_page/1 — VERIFIED at lib/cairnloop/web/components.ex:321-346 -->
attr :title, :string, required: true
attr :subtitle, :string, default: nil
attr :width, :string, values: ~w(wide reading), default: "wide"
slot :actions ; slot :breadcrumb ; slot :subnav ; slot :inner_block, required: true
DOM markers: class="cl-page cl-page--wide", cl-page__title, cl-page__subtitle, cl-page__subnav

<!-- cl_breadcrumb/1 — VERIFIED at lib/cairnloop/web/components.ex:385-401 -->
attr :items, :list, required: true  # [%{label, href}]; LAST item current (NO :href key)
# items WITH href => <.link navigate=>; the one WITHOUT href => <span aria-current="page">

<!-- editor.ex's CURRENT static breadcrumb (the items shape to carry verbatim in this plan) -->
items={[
  %{label: "Knowledge", href: "/knowledge-base"},
  %{label: "Editing: #{@article.title}"}   # last item: NO href => current crumb
]}

cl_page, cl_breadcrumb, kb_nav are all already imported on every KB screen — no new imports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate KB Index + Gaps into cl_page</name>
  <files>lib/cairnloop/web/knowledge_base_live/index.ex, lib/cairnloop/web/knowledge_base_live/gaps.ex, test/cairnloop/web/knowledge_base_live_test.exs, test/cairnloop/web/knowledge_base_live/gaps_test.exs</files>
  <read_first>
    - lib/cairnloop/web/knowledge_base_live/index.ex (header region :51-109; <h1>Knowledge Base</h1> at :57 inside cl-row--between; kb_nav at :54; "New article" cl_button; gap-candidates link)
    - lib/cairnloop/web/knowledge_base_live/gaps.ex (header region :62-156; <h1>Knowledge gaps</h1> at :68 inside <header class="cl-mb-7"> + subtitle; kb_nav at :65)
    - lib/cairnloop/web/components.ex (cl_page/1 def :321-346 — four declared slots only)
    - lib/cairnloop/web/knowledge_base_live/nav_component.ex (kb_nav/1 — the sub-tab strip → :subnav)
    - test/cairnloop/web/knowledge_base_live_test.exs (headless render harness; KB render assertion example at :716-721)
    - test/cairnloop/web/knowledge_base_live/gaps_test.exs (gaps render harness)
    - .planning/phases/38-shared-page-shell-migration/38-PATTERNS.md (`index.ex` and `gaps.ex` per-screen blocks; Pattern 1 minimal + Pattern 2 primary-action)
  </read_first>
  <behavior>
    - Test 1 (Index): rendered HTML contains `class="cl-page cl-page--wide"`, `class="cl-page__title"`, "Knowledge Base", a `class="cl-page__subnav"` region containing the kb_nav tabs, and the "New article" button rendered in the header actions region.
    - Test 2 (Gaps): rendered HTML contains `class="cl-page cl-page--wide"`, `class="cl-page__title"`, "Knowledge gaps", its subtitle text, and a `class="cl-page__subnav"` region containing kb_nav.
  </behavior>
  <action>
    KB Index: inside `<.cl_shell current={:knowledge} …>`, wrap the body in `<.cl_page title="Knowledge Base" width="wide">`. Move `<.kb_nav current={:index} />` into `<:subnav>`. Move the "New article" `<.cl_button variant="primary" phx-click="new_article" phx-disable-with="Creating...">New article</.cl_button>` (VERBATIM) into `<:actions>`. The gap-candidates `<p>…<.link navigate="/knowledge-base/gaps">…</.link></p>` and the articles `cl_card` stay in `inner_block`.

    KB Gaps: inside `<.cl_shell current={:knowledge} …>`, replace the `<header class="cl-mb-7">` wrapper with `<.cl_page title="Knowledge gaps" subtitle="{the existing muted subtitle text verbatim}" width="wide">`. Move `<.kb_nav current={:gaps} />` into `<:subnav>`. Gap-candidates card stays in `inner_block`. No `:actions`, no `:breadcrumb` (gaps is a lane index, not a detail screen — per CONTEXT discretion it IS included as the 5th KB screen).

    Note the INTENTIONAL kb_nav reorder (Pitfall 5): kb_nav renders ABOVE the h1 today but BELOW the header in `:subnav` — flag this in the SUMMARY for the P45 screenshot diff so it is not mistaken for a regression.

    Extend the KB render tests with the assertions from <behavior> using the existing headless harness. Mark any genuinely Repo-needing assertion `# REPO-UNAVAILABLE` (none expected).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/knowledge_base_live_test.exs test/cairnloop/web/knowledge_base_live/gaps_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "<.cl_page" lib/cairnloop/web/knowledge_base_live/index.ex lib/cairnloop/web/knowledge_base_live/gaps.ex` returns ≥1 per file.
    - Source: `grep -c "<:subnav>" lib/cairnloop/web/knowledge_base_live/index.ex lib/cairnloop/web/knowledge_base_live/gaps.ex` returns ≥1 per file; `grep -c "<:actions>" lib/cairnloop/web/knowledge_base_live/index.ex` returns ≥1.
    - Render (Index): rendered string contains `cl-page cl-page--wide`, `cl-page__title`, "Knowledge Base", `cl-page__subnav`, and "New article".
    - Render (Gaps): rendered string contains `cl-page cl-page--wide`, `cl-page__title`, "Knowledge gaps", and `cl-page__subnav`.
    - Command: `mix compile --warnings-as-errors` exits 0 (Pitfall 3); the two test files exit 0.
  </acceptance_criteria>
  <done>KB Index and Gaps render inside `.cl-page--wide` with verbatim titles, kb_nav in `:subnav`, and (Index) New article in `:actions`; warnings-clean; tests pass; kb_nav reorder noted for P45.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate KB Editor + Suggestion review into cl_page (breadcrumb slot established with current static items)</name>
  <files>lib/cairnloop/web/knowledge_base_live/editor.ex, lib/cairnloop/web/knowledge_base_live/suggestion_review.ex, test/cairnloop/web/knowledge_base_live_test.exs, test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</files>
  <read_first>
    - lib/cairnloop/web/knowledge_base_live/editor.ex (header region :260-342; existing <.cl_breadcrumb> at :263 free-floating above kb_nav; kb_nav at :268; <h1>Editing: {@article.title}</h1> at :271; 2-col grid with token style= at :294 — KEEP unchanged; source-gap card at :328)
    - lib/cairnloop/web/knowledge_base_live/suggestion_review.ex (header region :186-355; <h1>Suggestion review</h1> at :192 inside <header class="cl-mb-7"> + subtitle; kb_nav at :189; filter card + queue table + detail cards)
    - lib/cairnloop/web/components.ex (cl_page/1 def :321-346; cl_breadcrumb/1 def :385-401 — last item omits :href)
    - test/cairnloop/web/knowledge_base_live_test.exs (KB editor render harness; breadcrumb assertion example :716-721)
    - test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs (suggestion_review render harness; render example near :872)
    - .planning/phases/38-shared-page-shell-migration/38-PATTERNS.md (`editor.ex` + `suggestion_review.ex` per-screen blocks; "Best Already-Correct Exemplar")
  </read_first>
  <behavior>
    - Test 1 (Editor): rendered HTML contains `class="cl-page cl-page--wide"`, `class="cl-page__title"`, "Editing: " + the article title, a `class="cl-page__subnav"` region with kb_nav, and a `class="cl-breadcrumb"` nav rendered inside the page header (breadcrumb is no longer free-floating).
    - Test 2 (Editor): the breadcrumb still contains the current static crumbs ("Knowledge" link + the current "Editing: …" crumb with `aria-current="page"`) — this plan does NOT yet add the origin-aware crumb (that is 38-04).
    - Test 3 (Suggestion review): rendered HTML contains `class="cl-page cl-page--wide"`, `class="cl-page__title"`, "Suggestion review", its subtitle text, and a `class="cl-page__subnav"` region with kb_nav. (No breadcrumb yet — added in 38-04.)
  </behavior>
  <action>
    KB Editor: inside `<.cl_shell current={:knowledge} …>`, wrap the body in `<.cl_page title={"Editing: #{@article.title}"} width="wide">`. Move the existing `<.cl_breadcrumb items={[...]} />` call (currently at :263, free-floating above kb_nav) into the `<:breadcrumb>` slot — carry its CURRENT static items list VERBATIM in this plan (the origin-aware items come in 38-04). Move `<.kb_nav current={:editor} />` into `<:subnav>`. The banners (:274-292), the 2-col markdown/preview grid at :294 (KEEP its token-based `style=` UNCHANGED — do NOT retheme), and the source-gap card (:328) move wholesale into `inner_block`. No `:actions`.

    KB Suggestion review: inside `<.cl_shell current={:knowledge} …>`, replace the `<header class="cl-mb-7">` wrapper with `<.cl_page title="Suggestion review" subtitle="{the existing muted subtitle text verbatim}" width="wide">`. Move `<.kb_nav current={:suggestions} />` into `<:subnav>`. The filter card + queue table + detail cards stay in `inner_block`. Do NOT add a breadcrumb here yet — the new lane breadcrumb is wired in 38-04 (it currently has none; leaving the `:breadcrumb` slot unused now keeps this plan a pure structural lift).

    Pitfall 3: after the move, confirm no assign became orphaned and only the 4 declared cl_page slots are used. Pitfall 5: note the kb_nav above→below reorder in the SUMMARY for P45.

    Extend the KB editor and suggestion_review render tests with the <behavior> assertions using the existing headless harness. Mark Repo-needing parts `# REPO-UNAVAILABLE` if any.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/knowledge_base_live_test.exs test/cairnloop/web/knowledge_base_live/suggestion_review_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "<.cl_page" lib/cairnloop/web/knowledge_base_live/editor.ex lib/cairnloop/web/knowledge_base_live/suggestion_review.ex` returns ≥1 per file.
    - Source: `grep -c "<:breadcrumb>" lib/cairnloop/web/knowledge_base_live/editor.ex` returns ≥1 (breadcrumb moved into the slot); `grep -c "<:subnav>" lib/cairnloop/web/knowledge_base_live/editor.ex lib/cairnloop/web/knowledge_base_live/suggestion_review.ex` returns ≥1 per file.
    - Render (Editor): rendered string contains `cl-page cl-page--wide`, `cl-page__title`, "Editing: ", `cl-page__subnav`, `cl-breadcrumb`, and `aria-current="page"`.
    - Render (Suggestion review): rendered string contains `cl-page cl-page--wide`, `cl-page__title`, "Suggestion review", and `cl-page__subnav`.
    - Command: `mix compile --warnings-as-errors` exits 0; the two test files exit 0.
  </acceptance_criteria>
  <done>KB Editor and Suggestion review render inside `.cl-page--wide`; editor's breadcrumb is in the `:breadcrumb` slot (still static items); both have kb_nav in `:subnav`; warnings-clean; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Render-layer structural lift of existing KB markup. No new untrusted input, auth, session, or data flow. The editor's `return_to` origin is NOT touched in this plan (it is still rendered via the existing static breadcrumb items; origin plumbing lands in 38-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | Tampering (XSS via title) | `cl_page` title `Editing: #{@article.title}` | accept | `@article.title` is a stored, already-trusted KB article field rendered through the existing `<h1>` today; HEEx auto-escapes `{...}`; no `raw/1`. The migration changes the wrapper, not the data source. |
| T-38-04 | Tampering | editor static breadcrumb items moved into `:breadcrumb` slot | accept | Items carried VERBATIM from the existing static call — same crumb data, same hrefs, only the DOM position changes. No new origin/path data is introduced in this plan. |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — pure in-repo component adoption. No install task in this plan. |

No `high`-severity threats: structural lift only; the security-relevant breadcrumb-origin work is deferred to 38-04 where it is modeled explicitly.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean after both tasks (Pitfall 3: orphaned assigns / only 4 declared slots).
- KB render tests green for all four KB screens.
- Brand-token gate stays green — the editor 2-col grid's token-based `style=` at :294 is kept UNCHANGED (no hex reintroduced).
- SUMMARY records the intentional kb_nav above→below reorder (Pitfall 5) for the P45 screenshot diff.
</verification>

<success_criteria>
- KB Index, Editor, Gaps, Suggestion review each render inside `<.cl_page … width="wide">` nested in `<.cl_shell>` (SHELL-01).
- Each carries its verbatim former `<h1>` text as the title (D-05); kb_nav is in `:subnav`; Index's New article is in `:actions`.
- Editor's existing breadcrumb is in the `:breadcrumb` slot (still static items — origin-awareness is 38-04).
- `mix compile --warnings-as-errors` and the KB render tests pass.
</success_criteria>

<output>
Create `.planning/phases/38-shared-page-shell-migration/38-02-SUMMARY.md` when done.
</output>
