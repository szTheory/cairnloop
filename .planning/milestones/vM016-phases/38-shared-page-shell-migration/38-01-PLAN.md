---
phase: 38-shared-page-shell-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/web/home_live.ex
  - lib/cairnloop/web/inbox_live.ex
  - lib/cairnloop/web/audit_log_live.ex
  - lib/cairnloop/web/settings_live.ex
  - test/cairnloop/web/home_live_test.exs
  - test/cairnloop/web/inbox_live_test.exs
  - test/cairnloop/web/settings_live_test.exs
autonomous: true
requirements: [SHELL-01]
must_haves:
  truths:
    - "Home renders inside .cl-page--wide with the verbatim 'Welcome back' title"
    - "Inbox renders inside .cl-page--wide with the verbatim 'Inbox' title and the search modal stays in the body"
    - "Audit Log renders inside .cl-page--wide with the verbatim 'Audit Log' title and its filter bar stays in the card body"
    - "Settings renders inside .cl-page--wide with the verbatim 'Settings' title and the Toggle dark mode button in the :actions slot"
  artifacts:
    - path: "lib/cairnloop/web/home_live.ex"
      provides: "Home body wrapped in cl_page width=wide"
      contains: "<.cl_page"
    - path: "lib/cairnloop/web/inbox_live.ex"
      provides: "Inbox body wrapped in cl_page width=wide"
      contains: "<.cl_page"
    - path: "lib/cairnloop/web/audit_log_live.ex"
      provides: "Audit Log body wrapped in cl_page width=wide"
      contains: "<.cl_page"
    - path: "lib/cairnloop/web/settings_live.ex"
      provides: "Settings body wrapped in cl_page width=wide; Toggle dark mode in :actions"
      contains: "<.cl_page"
  key_links:
    - from: "home_live.ex / inbox_live.ex / audit_log_live.ex / settings_live.ex"
      to: "cl_page"
      via: "HEEx component call nested inside cl_shell"
      pattern: "<\\.cl_page"
---

<objective>
Migrate the four non-KB operator screens — Home, Inbox, Audit Log, Settings — to render their
body through the locked `cl_page` primitive (built in P37), replacing each screen's hand-rolled
`<h1>` + width chrome. This delivers SHELL-01 for the non-KB half of the cockpit: uniform header
height, inner content width, and page title across all four screens.

Purpose: Stop the cockpit feeling like four different apps (per D-02/D-03/D-04). Pure structural
lift — no body markup is rewritten (D-05).
Output: Four migrated LiveView render functions + extended headless render assertions proving each
renders inside `.cl-page--wide` with its verbatim title.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38-shared-page-shell-migration/38-CONTEXT.md
@.planning/phases/38-shared-page-shell-migration/38-RESEARCH.md
@.planning/phases/38-shared-page-shell-migration/38-PATTERNS.md

<interfaces>
<!-- cl_page/1 — VERIFIED at lib/cairnloop/web/components.ex:321-346. Use directly, no exploration. -->
attr :title, :string, required: true
attr :subtitle, :string, default: nil
attr :width, :string, values: ~w(wide reading), default: "wide"
slot :actions      # right-aligned in header's cl-row--between
slot :breadcrumb   # rendered ABOVE the title row (header top)
slot :subnav       # rendered BETWEEN header and body
slot :inner_block, required: true  # the whole former body, minus <h1> and its wrapper

Rendered DOM markers (what render tests assert against):
  class="cl-page cl-page--wide"   class="cl-page__title"   class="cl-page__subtitle"
  class="cl-page__body"           class="cl-page__subnav"  (only when subnav non-empty)

cl_page is already imported via `import Cairnloop.Web.Components` on every screen — no new import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate Home + Inbox into cl_page</name>
  <files>lib/cairnloop/web/home_live.ex, lib/cairnloop/web/inbox_live.ex, test/cairnloop/web/home_live_test.exs, test/cairnloop/web/inbox_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/home_live.ex (header region :63-122; <h1>Welcome back</h1> at :67 inside <header style=…> with subtitle "What needs you today?")
    - lib/cairnloop/web/inbox_live.ex (header region :112-279; <h1>Inbox</h1> at :124; search-modal live_component sibling at :115)
    - lib/cairnloop/web/components.ex (cl_page/1 def :321-346 — the four declared slots only)
    - test/cairnloop/web/home_live_test.exs (headless harness: `rendered_to_string(Cairnloop.Web.HomeLive.render(assigns(%{})))` at :26-27)
    - test/cairnloop/web/inbox_live_test.exs (render harness; note :625 references "<h1>Inbox</h1>" only in a test NAME string asserting "No conversations yet." — does NOT break)
    - .planning/phases/38-shared-page-shell-migration/38-PATTERNS.md (`home_live.ex` and `inbox_live.ex` per-screen blocks)
  </read_first>
  <behavior>
    - Test 1 (Home): rendered HTML contains `class="cl-page cl-page--wide"` and `class="cl-page__title"` and the verbatim text "Welcome back" and the subtitle text "What needs you today?".
    - Test 2 (Inbox): rendered HTML contains `class="cl-page cl-page--wide"` and `class="cl-page__title"` and the verbatim text "Inbox".
    - Test 3 (Inbox): the search-modal component still renders inside the page body (existing search-modal markup/id present in the rendered string).
  </behavior>
  <action>
    Home: inside the existing `<.cl_shell current={:home} …>`, replace the `<header style="margin-bottom: var(--cl-space-7);">` wrapper (containing `<h1>Welcome back</h1>` and `<p class="cl-text-muted">What needs you today?</p>`) with `<.cl_page title="Welcome back" subtitle="What needs you today?" width="wide">`. Move the `.cl-home-grid` body inside `cl_page`'s inner_block UNCHANGED. Carry "Welcome back" VERBATIM per D-05 (P39 redesigns Home copy — not here). No `:actions`, no `:subnav`.

    Inbox: inside `<.cl_shell current={:inbox} …>`, wrap the body in `<.cl_page title="Inbox" width="wide">`. Per Pitfall 4 (D-04), keep the `<.live_component module={Cairnloop.Web.SearchModalComponent} id="search-modal" …>` as the FIRST CHILD of `inner_block` (it is a fixed-overlay; its position is CSS-driven). Remove only the bare `<h1>Inbox</h1>`; the `<div class="cairnloop-inbox">` wrapper plus the list, bulk-header, sticky bulk bar, and confirm modal all stay in the body per D-04 (substantial UI does NOT move to `:actions`). No `:actions`, no `:subnav`.

    Then extend `home_live_test.exs` and `inbox_live_test.exs` with the render assertions from <behavior> using the existing `rendered_to_string(Module.render(assigns(...)))` harness. Do NOT introduce a Repo round-trip; mark any genuinely Repo-needing assertion `# REPO-UNAVAILABLE` (none expected — headless render needs no Repo).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/home_live_test.exs test/cairnloop/web/inbox_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "<.cl_page" lib/cairnloop/web/home_live.ex lib/cairnloop/web/inbox_live.ex` returns ≥1 per file.
    - Render (Home): rendered string contains `cl-page cl-page--wide`, `cl-page__title`, "Welcome back", and "What needs you today?".
    - Render (Inbox): rendered string contains `cl-page cl-page--wide`, `cl-page__title`, "Inbox", and the `id="search-modal"` component markup (modal stays in body).
    - Command: `mix compile --warnings-as-errors` exits 0 (no orphaned-assign / undefined-slot warnings — Pitfall 3); `mix test test/cairnloop/web/home_live_test.exs test/cairnloop/web/inbox_live_test.exs` exits 0.
  </acceptance_criteria>
  <done>Home and Inbox both render inside `.cl-page--wide` with verbatim titles; the inbox search modal stays in the body; warnings-clean; both screens' render tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Audit Log + Settings into cl_page</name>
  <files>lib/cairnloop/web/audit_log_live.ex, lib/cairnloop/web/settings_live.ex, test/cairnloop/web/settings_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/audit_log_live.ex (header region :87-165; <h1>Audit Log</h1> at :91 inside <header class="cl-mb-7"> + subtitle; the search/filter form lives in the cl_card :header at :97-123 — a substantial filter bar)
    - lib/cairnloop/web/settings_live.ex (header region :154-280; <h1>Settings</h1> at :166 inside <div class="cl-row cl-row--between"> with a "Toggle dark mode" button; search-modal live_component at :157)
    - lib/cairnloop/web/components.ex (cl_page/1 def :321-346)
    - test/cairnloop/web/settings_live_test.exs (render harness; SettingsLive has a known order-flake per memory baseline — verify in isolation, not a regression)
    - .planning/phases/38-shared-page-shell-migration/38-PATTERNS.md (`audit_log_live.ex` and `settings_live.ex` per-screen blocks; D-04 "filter bars stay in body" + "single primary button → :actions")
  </read_first>
  <behavior>
    - Test 1 (Audit Log): rendered HTML contains `class="cl-page cl-page--wide"`, `class="cl-page__title"`, the verbatim text "Audit Log", and its subtitle text; the search/filter form markup is still present (stayed in the card body, NOT in :actions).
    - Test 2 (Settings): rendered HTML contains `class="cl-page cl-page--wide"`, `class="cl-page__title"`, the verbatim text "Settings", and the "Toggle dark mode" button text rendered within the page header actions region.
  </behavior>
  <action>
    Audit Log: inside `<.cl_shell current={:audit} …>`, replace the `<header class="cl-mb-7">` wrapper with `<.cl_page title="Audit Log" subtitle="{the existing muted subtitle text verbatim}" width="wide">`. The `cl_card` (whose `:header` slot holds the substantial search + action-filter form at :97-123) plus its table and pagination move WHOLE into `inner_block` — the filter bar STAYS in the body (D-04 forbids cramming filters into `:actions`). No `:actions`, no `:subnav`.

    Settings: inside `<.cl_shell current={:settings} …>`, wrap the body in `<.cl_page title="Settings" width="wide">`. Place the existing "Toggle dark mode" `<button … class="cl-button cl-button--ghost">` (with its `onclick` VERBATIM) into the `<:actions>` slot — this is the textbook single-primary-action case (D-04). Per Pitfall 4, keep the `<.live_component … id="search-modal" …>` as the FIRST CHILD of `inner_block`. Flash banners + health/MCP/policy cards stay in the body.

    Extend `settings_live_test.exs` with the Settings render assertion from <behavior>. For Audit Log, add the render assertion to the appropriate existing audit-log render test file if one exists; if Audit Log lacks a dedicated render test, add a minimal headless render assertion (mark Repo-needing parts `# REPO-UNAVAILABLE` if any). Do NOT rewrite body markup.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/settings_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "<.cl_page" lib/cairnloop/web/audit_log_live.ex lib/cairnloop/web/settings_live.ex` returns ≥1 per file.
    - Source: Settings' "Toggle dark mode" button appears inside a `<:actions>` slot block (grep for `<:actions>` in settings_live.ex returns ≥1).
    - Render (Audit Log): rendered string contains `cl-page cl-page--wide`, `cl-page__title`, "Audit Log", and the filter-form markup (filter bar stayed in body).
    - Render (Settings): rendered string contains `cl-page cl-page--wide`, `cl-page__title`, "Settings", and "Toggle dark mode".
    - Command: `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/web/settings_live_test.exs` exits 0 (a SettingsLive order-flake is pre-existing baseline — re-run in isolation to confirm not a P38 regression).
  </acceptance_criteria>
  <done>Audit Log and Settings render inside `.cl-page--wide` with verbatim titles; Audit Log's filter bar stays in the body; Settings' Toggle-dark-mode button is in `:actions`; warnings-clean; Settings render test passes in isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan introduces NO new trust boundary — it is a render-layer structural lift of existing, already-trusted markup. No new untrusted input, auth, session, or data flow is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering (XSS via title/subtitle) | `cl_page` title/subtitle on Home/Inbox/Audit/Settings | accept | Titles/subtitles are static literal strings carried verbatim from existing `<h1>`/`<p>` (D-05); HEEx auto-escapes `{...}` interpolation; no `raw/1` used. No attacker-controlled input reaches these slots. |
| T-38-02 | Information disclosure | Inbox/Settings search-modal placement | accept | The search modal is the same already-shipped `live_component` moved within the same DOM subtree (Pitfall 4); no change to its data scope, host_surface, or host_user_id assigns. |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — pure in-repo component adoption (RESEARCH "Package Legitimacy Audit: Not applicable — zero external packages"). No install task exists in this plan. |

No `high`-severity threats: this plan adds no input, no auth, no data flow, and installs nothing.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` is clean after both tasks (Pitfall 3: watch for orphaned assigns / only the 4 declared slots).
- `mix test test/cairnloop/web/` is green for the four migrated screens' render tests.
- Brand-token gate (`test/cairnloop/web/brand_token_gate_test.exs`) stays green — no hardcoded hex reintroduced (the Home/Settings/Audit markup carries token-based styles only).
- Before declaring the plan done: re-run any SettingsLive flake in isolation to confirm it is the pre-existing baseline order-flake, not a P38 regression.
</verification>

<success_criteria>
- Home, Inbox, Audit Log, Settings each render inside `<.cl_page … width="wide">` nested in `<.cl_shell>` (SHELL-01).
- Each carries its verbatim former `<h1>` text as the `cl_page` title (D-05).
- Inbox/Settings search modals remain in the body; Audit Log filter bar stays in the body; Settings Toggle-dark-mode button is in `:actions`.
- `mix compile --warnings-as-errors` and the four screens' render tests pass.
</success_criteria>

<output>
Create `.planning/phases/38-shared-page-shell-migration/38-01-SUMMARY.md` when done.
</output>
