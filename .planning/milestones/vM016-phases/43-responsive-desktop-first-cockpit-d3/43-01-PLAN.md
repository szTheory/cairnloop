---
phase: 43-responsive-desktop-first-cockpit-d3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/static/cairnloop.css
  - test/cairnloop/web/cairnloop_css_test.exs
autonomous: true
requirements: [RESP-01]
must_haves:
  truths:
    - "No @media block in cairnloop.css uses a max-width width condition (all width queries are min-width)"
    - "The CSS documents three literal breakpoint constants (640 / 768 / 1024) in ONE comment block"
    - "No @media condition contains var() (breakpoints are literal pixels, never tokenized)"
    - "768px appears as a real min-width consumer rule, not comment-only"
    - "Behavior is unchanged at every viewport (the two converted blocks are behaviorally equivalent)"
  artifacts:
    - path: "priv/static/cairnloop.css"
      provides: "Mobile-first min-width media queries + breakpoint comment block + 768 tablet rule"
      contains: "BREAKPOINTS"
    - path: "test/cairnloop/web/cairnloop_css_test.exs"
      provides: "RESP-01 CSS-presence drift-proofing assertions"
      contains: "RESP-01"
  key_links:
    - from: "test/cairnloop/web/cairnloop_css_test.exs"
      to: "priv/static/cairnloop.css"
      via: "File.read! string scan in setup_all"
      pattern: "max-width"
---

<objective>
Normalize `priv/static/cairnloop.css` to mobile-first authoring per D3-01/02/03: convert the two `max-width:640` blocks to `min-width` equivalents, add ONE standardized breakpoint comment block documenting the three literal constants (640/768/1024), introduce the new 768 tablet breakpoint as a real consumer rule, align the existing `.cl-home-grid` min-width queries to the documented constants, and extend the CSS-presence test with RESP-01 drift-proofing assertions.

Purpose: RESP-01 — the cockpit CSS becomes mobile-first (`min-width`) throughout with standardized, documented, literal-only breakpoints, so future responsive work has one discoverable contract and cannot silently no-op a tokenized breakpoint.
Output: Edited `cairnloop.css` (mobile-first), extended `cairnloop_css_test.exs` (RESP-01 cases green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-responsive-desktop-first-cockpit-d3/43-CONTEXT.md
@.planning/phases/43-responsive-desktop-first-cockpit-d3/43-RESEARCH.md
@.planning/phases/43-responsive-desktop-first-cockpit-d3/43-VALIDATION.md

<interfaces>
<!-- Current responsive state of cairnloop.css (verified by line-cited reads). -->
<!-- The executor must read the file itself for full context; these are the load-bearing anchors. -->

Two width-conditioned max-width @media blocks exist (the ONLY two to convert):
  - L263:  @media (max-width: 640px) { .cl-main { padding: var(--cl-space-5, 16px); } }
           (base .cl-main at L261 has padding: var(--cl-space-7, 24px))
  - L516-519: @media (max-width: 640px) {
                .cl-nav { gap: var(--cl-space-3, 8px); padding: 0 var(--cl-space-4, 12px); }
                .cl-nav__link { padding: var(--cl-space-2, 4px) var(--cl-space-3, 8px); }
              }
           NOTE: this is the .cl-nav block, NOT a modal. The modal selectors
           (.cl-modal-* at L522-524) have NO media query and use width: min(640px, 92vw).
           Base .cl-nav is at L264; base .cl-nav__link is defined separately above L516.

Already-conformant min-width queries (mobile-first; only ALIGN to constants, do not restructure):
  - L426: @media (min-width: 640px)  { .cl-home-grid { grid-template-columns: repeat(2, 1fr); } }
  - L427: @media (min-width: 1024px) { .cl-home-grid { grid-template-columns: repeat(3, 1fr); } }
  - L529-533: @media (min-width: 1024px) { .conversation-layout flex-direction: row; ... }  (DO NOT TOUCH — owned by Plan 02 verification)

Existing responsive section header comment at L513-515.
Layout tokens already defined at L137-140 (--cl-content-max / --cl-rail-width / --cl-page-gutter) — consume, do not redefine.
prefers-reduced-motion @media at L197 has NO width condition — leave it; it is not a width query.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the two max-width:640 blocks to mobile-first min-width + add breakpoint comment block + 768 tablet consumer + align home-grid constants</name>
  <files>priv/static/cairnloop.css</files>
  <read_first>
    - priv/static/cairnloop.css (the file being normalized — read L130-156 tokens, L261-264 .cl-main + .cl-nav base, L425-427 home-grid, L513-533 responsive section, and L197 reduced-motion to confirm it has no width condition)
    - .planning/phases/43-responsive-desktop-first-cockpit-d3/43-RESEARCH.md (Pattern 1 exact before/after for both blocks; Pattern 2 comment-block example; Pattern 3 + Open Question 1 for the 768 consumer; Pitfall 1 the L516-is-nav-not-modal correction)
    - .planning/phases/43-responsive-desktop-first-cockpit-d3/43-CONTEXT.md (D3-01, D3-02, D3-03 locked decisions)
  </read_first>
  <action>
    Per D3-01: invert Block A (`.cl-main`). Change the base `.cl-main` rule (currently L261) so its `padding` becomes the small-viewport value `var(--cl-space-5, 16px)`; remove the `@media (max-width: 640px)` override at L263 and replace it with `@media (min-width: 640px) { .cl-main { padding: var(--cl-space-7, 24px); } }`. Leave every other `.cl-main` property (flex, width, max-width, margin) on the base rule unchanged. Behavioral equivalence: <640px renders 16px, ≥640px renders 24px — identical to today.

    Per D3-01 + Pitfall 1: invert Block B (`.cl-nav`, NOT a modal). Edit the BASE `.cl-nav` rule (L264) so its `gap` becomes `var(--cl-space-3, 8px)` and its `padding` becomes `0 var(--cl-space-4, 12px)` (the small values); edit the BASE `.cl-nav__link` rule so its padding becomes `var(--cl-space-2, 4px) var(--cl-space-3, 8px)`; keep all non-overridden base properties (height: 56px, background, border) intact. Then convert the L516-519 block to `@media (min-width: 640px)` carrying the DESKTOP values: `.cl-nav { gap: var(--cl-space-5, 16px); padding: 0 var(--cl-space-7, 24px); }` and `.cl-nav__link { padding: var(--cl-space-2, 4px) var(--cl-space-4, 12px); }`. Do NOT search for a `.cl-modal` media query — none exists.

    Per D3-02 + D3-03: add ONE breakpoint comment block at the top of the responsive section (replace/extend the L513-515 `Responsive — mobile-first` header) documenting exactly three literal constants — `sm = 640px`, `md = 768px (tablet; introduced Phase 43)`, `lg = 1024px` — and stating in prose that `var()` is ILLEGAL inside `@media` conditions (silent no-op) so breakpoints are never tokenized, while layout VALUES (`--cl-content-max`, `--cl-rail-width`, `--cl-page-gutter`) stay tokens. This is the single discoverable location for the constants.

    Per D3-02 + Pattern 3 + Open Question 1 (recommended resolution): introduce 768 as a REAL `@media (min-width: 768px)` consumer — a genuine tablet gutter step on `.cl-main` padding between the 640 and 1024 tiers (e.g. an intermediate padding/gutter value larger than the 640 step). It must produce a real change, not a no-op rule. Place it adjacent to the converted Block A so the 640→768 progression is readable. Do NOT add an empty/comment-only 768 rule (that would itself be drift).

    Per D3-03 (alignment, not restructure): leave the `.cl-home-grid` min-width:640 / min-width:1024 queries (L426/427) as mobile-first; only ensure their literal pixel values exactly match the documented constants (640 and 1024). Do not convert them — they already conform.

    Constraint: do NOT touch the conversation-layout block (L528-533) — Plan 02 verifies it. Do NOT introduce any `var()` inside an `@media` condition. Do NOT tokenize any breakpoint. The `.css` file is structurally excluded from the brand-token gate, so these edits cannot trip it.
  </action>
  <verify>
    <automated>grep -n '@media (max-width' priv/static/cairnloop.css; test $(grep -c '@media (max-width' priv/static/cairnloop.css) -eq 0 && grep -q 'min-width: 768px' priv/static/cairnloop.css && test "$(awk '/@media \(min-width: 768px\)/{f=1} f&&/padding/{print \"REAL\"; exit}' priv/static/cairnloop.css)" = "REAL" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@media (max-width' priv/static/cairnloop.css` returns 0 (no max-width width conditions remain)
    - `grep -c 'min-width: 768px' priv/static/cairnloop.css` returns at least 1 AND the `@media (min-width: 768px)` block contains a real declaration (a `padding`/gutter property on `.cl-main`) — a token no-op or comment-only 768 rule must NOT pass. Verify with the portable awk scan: `awk '/@media \(min-width: 768px\)/{f=1} f&&/padding/{print "REAL"; exit}' priv/static/cairnloop.css` prints `REAL`
    - The file contains a single comment block listing the literal strings `640px`, `768px`, and `1024px` together (the standardized breakpoint constants)
    - `grep -nE '@media\s*\([^)]*var\(' priv/static/cairnloop.css` returns no matches (no var() in any @media condition)
    - `.cl-main` base padding is `var(--cl-space-5, 16px)` and a `@media (min-width: 640px)` restores `var(--cl-space-7, 24px)`
    - The conversation-layout block (`.conversation-layout` + `@media (min-width: 1024px)`) is byte-identical to before (untouched)
  </acceptance_criteria>
  <done>All width-conditioned @media blocks use min-width; one breakpoint comment block documents 640/768/1024 as literals; a real 768 rule exists; no var() in @media; conversation-layout untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend cairnloop_css_test.exs with RESP-01 CSS-presence drift-proofing assertions</name>
  <files>test/cairnloop/web/cairnloop_css_test.exs</files>
  <read_first>
    - test/cairnloop/web/cairnloop_css_test.exs (the existing DB-free CSS-presence test — reuse its `setup_all` css read; mirror its `describe`/`assert css =~` style)
    - .planning/phases/43-responsive-desktop-first-cockpit-d3/43-RESEARCH.md (Code Examples: "CSS-presence drift-proofing test" — the exact describe block to mirror)
    - .planning/phases/43-responsive-desktop-first-cockpit-d3/43-VALIDATION.md (Wave 0 Requirements — the RESP-01 cases this test must cover)
    - priv/static/cairnloop.css (to confirm the literal strings the assertions target now exist after Task 1)
  </read_first>
  <action>
    Add a new `describe "responsive normalization (D3 / RESP-01)"` block to the existing test module, reusing the `setup_all` `css` binding (do not add a second file read). Add four assertions:
    1. No max-width width media conditions remain: `refute css =~ ~r/@media\s*\(\s*max-width/` with a message that all media queries must be min-width (mobile-first).
    2. The three standardized breakpoints are present as literal constants: `assert css =~ "640px"`, `assert css =~ "768px"`, `assert css =~ "1024px"` — and assert the documentation marker (e.g. `assert css =~ "BREAKPOINTS"`) so the comment block's presence is pinned, not just incidental pixel values.
    3. Breakpoints are NOT tokenized: `refute css =~ ~r/@media\s*\([^)]*var\(/` with a message that var() in @media silently no-ops.
    4. `.cl-table-scroll` still defined (no regression from the normalization): `assert css =~ ".cl-table-scroll"`.
    Keep the test `async: true` and DB-free (pure string scan — no Repo, no `# REPO-UNAVAILABLE` marker needed since no Postgres round-trip occurs).
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/cairnloop_css_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/web/cairnloop_css_test.exs` exits 0
    - The module contains a `describe` block whose name includes `RESP-01`
    - The new block contains a `refute css =~ ~r/@media\s*\(\s*max-width/` assertion
    - The new block contains a `refute css =~ ~r/@media\s*\([^)]*var\(/` assertion
    - The new block asserts presence of the literal strings `640px`, `768px`, and `1024px`
  </acceptance_criteria>
  <done>The CSS-presence test has a RESP-01 describe block asserting no max-width conditions, three literal breakpoints, no var() in @media, and table-scroll preservation — and the full file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits a static stylesheet and a test file. No untrusted input crosses any boundary; no data, auth, or network surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | priv/static/cairnloop.css (CSS injection) | accept | All values are static / token-valued literals; no user-controlled style strings are introduced. CSS is served as a static asset. No new input surface. |
| T-43-02 | (a11y/governance-adjacent, not STRIDE) | media-query normalization | mitigate | Behavioral-equivalence reasoning per converted block (research Pattern 1) + the new CSS-presence test pins no-max-width / literal-breakpoints / no-var-in-@media so drift cannot silently re-enter. |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs this plan (zero external dependencies; no `mix deps` / `npm install` changes). |

**Honest assessment:** No new input/output/auth/data-access surface. The only real risk is accessibility regression and visual drift from the min-width conversion — mitigated by behavioral-equivalence-preserving edits and the CSS-presence drift-proofing test. ASVS V2/V3/V4/V5/V6 do not apply (no auth/session/access-control/input/crypto surface).
</threat_model>

<verification>
- `mix test test/cairnloop/web/cairnloop_css_test.exs` is green (RESP-01 cases pass).
- `grep -c '@media (max-width' priv/static/cairnloop.css` returns 0.
- `grep -c 'min-width: 768px' priv/static/cairnloop.css` returns ≥1.
- `mix compile --warnings-as-errors` passes (no .ex changes here, but confirm clean tree).
- Manual (deferred to phase gate per VALIDATION Manual-Only table): at ~600px and 768px, `.cl-main` padding and `.cl-nav` layout match pre-conversion behavior.
</verification>

<success_criteria>
- All width-conditioned `@media` blocks in `cairnloop.css` are `min-width` (mobile-first).
- One comment block documents 640/768/1024 as literal constants and warns against tokenizing breakpoints.
- A real `@media (min-width: 768px)` consumer rule exists (not comment-only).
- No `var()` appears in any `@media` condition.
- The conversation-layout stacking block is untouched (owned by Plan 02 verification).
- `cairnloop_css_test.exs` RESP-01 assertions are green.
</success_criteria>

<output>
Create `.planning/phases/43-responsive-desktop-first-cockpit-d3/43-01-SUMMARY.md` when done.
</output>
