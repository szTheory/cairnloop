---
phase: 37-component-primitives
plan: 04
type: execute
wave: 3
depends_on: ["37-03"]
files_modified:
  - lib/cairnloop/web/components.ex
  - test/cairnloop/web/components_test.exs
autonomous: true
requirements: [UIC-04]
must_haves:
  truths:
    - "cl_source_card renders a status surface keyed by source_variant (success/info/neutral/...) pairing color + a distinct-silhouette cl_icon + a title (never color alone)"
    - "cl_status_cell renders a table-cell-sized cl_chip from variant + an always-visible label (never color alone), with no dependency on a non-existent action_tone/1"
    - "cl_switch is a real <button role=switch aria-checked=...> with a string aria-checked, an always-present label, and :rest passthrough wiring phx-click / phx-value-*"
  artifacts:
    - path: "lib/cairnloop/web/components.ex"
      provides: "cl_source_card/1, cl_status_cell/1, cl_switch/1 components"
      contains: "def cl_switch"
    - path: "test/cairnloop/web/components_test.exs"
      provides: "role=switch + string aria-checked + label + :rest tests; source-card icon test; status-cell chip+label test"
      exports: []
  key_links:
    - from: "lib/cairnloop/web/components.ex (cl_switch)"
      to: "WAI-ARIA switch pattern"
      via: "aria-checked={to_string(@checked)} (string, not boolean attr)"
      pattern: "aria-checked=\\{to_string"
    - from: "lib/cairnloop/web/components.ex (cl_status_cell)"
      to: "cl_chip"
      via: "delegation (no re-authored chip markup)"
      pattern: "<.cl_chip"
---

<objective>
Add the three status/toggle primitives to `Cairnloop.Web.Components`: `cl_source_card/1`
(UIC-04 / D-06 — a status surface keyed by `source_variant`, reusing the chip variant triplet +
`cl_icon` silhouette), `cl_status_cell/1` (UIC-04 / D-07 — a thin table-cell wrapper delegating to
`cl_chip`, tone-agnostic, NO dependency on the non-existent `action_tone/1`), and `cl_switch/1`
(UIC-04 / D-04 — a real `role="switch"` button with string `aria-checked`, an always-present label,
and `:rest` toggle wiring). This plan shares `components.ex` + `components_test.exs` with Plan 03,
so it runs in Wave 3 after it; it is parallel-safe with the table-wrap plan (different files).

Purpose: These three complete the UIC-04 primitive set — all status-bearing, all "never color
alone" (color + icon/shape + text). `cl_source_card` is the clean replacement for the search-modal
inline-hex drift that P40 remediates.
Output: `cl_source_card/1`, `cl_status_cell/1`, `cl_switch/1` + their headless render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-component-primitives/37-UI-SPEC.md
@.planning/phases/37-component-primitives/37-PATTERNS.md
@.planning/phases/37-component-primitives/37-RESEARCH.md

<interfaces>
<!-- Existing analogs the executor builds against. -->
From lib/cairnloop/web/components.ex:
- `cl_chip/1` (line 76) — `<span class={["cl-chip", "cl-chip--#{@variant}", @class]} {@rest}>` with `cl_icon` + label; `cl_status_cell` DELEGATES to this, `cl_source_card` mirrors its variant idiom.
- `cl_banner/1` (line 95) — `assign_new(:resolved_icon, fn -> assigns.icon || status_icon(assigns.variant) end)` + `<.cl_icon name={@resolved_icon} ...>` + slot body; the shape `cl_source_card` mirrors.
- `cl_button/1` (line 41) — `attr(:rest, :global, include: ~w(disabled form name value phx-click phx-value-id phx-disable-with data-confirm))`; the `:rest` template for `cl_switch`.
- `@status_variants ~w(success info warning danger ai neutral)` (line 27) — the single source enum; reuse, don't redefine.
- `status_icon/1` map (lines 245-250): success→check-circle, info→info, warning→alert-triangle, danger→x-circle, ai→waypoint, _→clock. REUSE; do not hand-author SVG.
- `cl_icon/1` (line 209) — self-contained inline SVG set; uses `Phoenix.HTML.raw/1` on a FIXED internal allowlist only — never pass caller input to raw/1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cl_switch/1 role=switch toggle (UIC-04 / D-04)</name>
  <files>lib/cairnloop/web/components.ex, test/cairnloop/web/components_test.exs</files>
  <read_first>
    - lib/cairnloop/web/components.ex — `cl_button/1` (line 35-37, 41) for the `<button>` + `:rest` global `include:` template; the moduledoc token-only rule.
    - .planning/phases/37-component-primitives/37-PATTERNS.md — "cl_switch/1" section (the exact `<button role="switch" aria-checked={to_string(@checked)} {@rest}>` skeleton + Pitfalls 3 & 5).
    - .planning/phases/37-component-primitives/37-UI-SPEC.md — §8 cl_switch (structure, attributes, interaction/visual contract, 44px tap target).
    - .planning/phases/37-component-primitives/37-RESEARCH.md — Pattern 4, Pitfall 3 (aria-checked must be a STRING via to_string/1), Pitfall 5 (phx-value-* are not default globals — must be in include:).
  </read_first>
  <behavior>
    - Test 1: `cl_switch checked={false} label="Draft mode" phx-click="toggle"` renders `role="switch"`, the literal `aria-checked="false"` (STRING, not a boolean attr), the visible `Draft mode` label, and the passed-through `phx-click="toggle"`.
    - Test 2: `cl_switch checked={true} label="Notifications"` renders `aria-checked="true"`.
    - Test 3: rendered HTML contains no `#hex`.
  </behavior>
  <action>
    Add `cl_switch/1` to `components.ex` per D-04 / PATTERNS (a real `role="switch"` button, NOT a checkbox): attrs `checked :boolean, required: true`, `label :string, required: true` (always-visible — never color alone, §7.5), and `attr(:rest, :global, include: ~w(phx-click phx-value-id phx-value-key disabled form name value))` (CRITICAL — `phx-value-*` are not default globals; omitting them silently drops the toggle binding, RESEARCH Pitfall 5). Emit `<button type="button" class="cl-switch" role="switch" aria-checked={to_string(@checked)} {@rest}>` containing `<span class="cl-switch__track"><span class="cl-switch__thumb"></span></span>` and `<span class="cl-switch__label">{@label}</span>`. Render `aria-checked` via `to_string(@checked)` so it is the literal string `"true"`/`"false"` (RESEARCH Pitfall 3 — a raw boolean would emit a present/absent boolean attribute, which assistive tech cannot announce as "off"). Token-only; the 44px tap target + checked visual live in the Plan 01 CSS (`[aria-checked="true"]` selector). Add the three tests above.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def cl_switch(' lib/cairnloop/web/components.ex` matches.
    - The emitted markup contains `role="switch"` and `aria-checked={to_string(@checked)}`; the `:rest` global `include:` list contains `phx-click`, `phx-value-id`, `phx-value-key`.
    - A test asserts `html =~ ~s(role="switch")`, `html =~ ~s(aria-checked="false")` (string), `html =~ "Draft mode"`, and `html =~ ~s(phx-click="toggle")`; another asserts `aria-checked="true"` when checked.
    - Every cl_switch test asserts `refute html =~ ~r/#[0-9a-fA-F]{3,6}/`.
    - `mix compile --warnings-as-errors` and `mix test test/cairnloop/web/components_test.exs` both exit 0.
  </acceptance_criteria>
  <done>`cl_switch/1` is a real `role="switch"` button with string `aria-checked`, an always-present label, and `:rest` toggle wiring (`phx-click`/`phx-value-*`); tests confirm the ARIA contract, passthrough, and token-purity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cl_status_cell/1 table-cell chip wrapper (UIC-04 / D-07)</name>
  <files>lib/cairnloop/web/components.ex, test/cairnloop/web/components_test.exs</files>
  <read_first>
    - lib/cairnloop/web/components.ex — `cl_chip/1` (line 76-90) which `cl_status_cell` delegates to directly; the `@status_variants` enum (line 27).
    - .planning/phases/37-component-primitives/37-PATTERNS.md — "cl_status_cell/1" section (thin wrapper; the D-07 grounding correction — `action_tone/1` does NOT exist; takes `variant`+`label` directly).
    - .planning/phases/37-component-primitives/37-UI-SPEC.md — §7 cl_status_cell (structure, attributes, the `action_tone/1` note).
    - lib/cairnloop/web/audit_log_presenter.ex — confirm it has `action_label/1` and LACKS `action_tone/1`; `cl_status_cell` MUST NOT call a non-existent fn.
  </read_first>
  <behavior>
    - Test 1: `cl_status_cell variant="success" label="Approved"` renders a `.cl-status-cell` span wrapping a `.cl-chip.cl-chip--success` with the visible `Approved` label and an `<svg` icon (never color alone).
    - Test 2: `cl_status_cell` with default `variant` ("neutral") and a label renders without error and shows the label.
    - Test 3: rendered HTML contains no `#hex`.
  </behavior>
  <action>
    Add `cl_status_cell/1` to `components.ex` per D-07 / PATTERNS as a thin wrapper that DELEGATES to `cl_chip` (do NOT re-author chip markup): attrs `variant :string, values: @status_variants, default: "neutral"`, `label :string, required: true` (always-visible — never color alone), `icon :string, default: nil` (overrides the default chip icon when provided). Emit `<span class="cl-status-cell"><.cl_chip variant={@variant} label={@label} icon={@icon} /></span>`. The wrapping span is a minimal table-column-alignment container (CSS added by Plan 01). It MUST NOT call `AuditLogPresenter.action_tone/1` — that function does not exist (verified); the tone-mapping is added in P38/P40 at adoption time. Add the three tests above.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def cl_status_cell(' lib/cairnloop/web/components.ex` matches; `grep -F 'action_tone' lib/cairnloop/web/components.ex` does NOT match.
    - The component emits `<span class="cl-status-cell">` wrapping `<.cl_chip ...>` (delegation, not re-authored markup).
    - A test asserts `html =~ "cl-status-cell"`, `html =~ "cl-chip--success"`, the visible `Approved` label, and `html =~ "<svg"` (icon present — never color alone).
    - Every cl_status_cell test asserts `refute html =~ ~r/#[0-9a-fA-F]{3,6}/`.
    - `mix compile --warnings-as-errors` and `mix test test/cairnloop/web/components_test.exs` both exit 0.
  </acceptance_criteria>
  <done>`cl_status_cell/1` delegates to `cl_chip` with a required always-visible label and a distinct-silhouette icon; it depends on no non-existent function; tests confirm chip+label+icon and token-purity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add cl_source_card/1 status surface (UIC-04 / D-06)</name>
  <files>lib/cairnloop/web/components.ex, test/cairnloop/web/components_test.exs</files>
  <read_first>
    - lib/cairnloop/web/components.ex — `cl_banner/1` (line 95-104, the `assign_new(:resolved_icon ...)` + `<.cl_icon>` + slot-body shape to mirror); `cl_chip/1` variant idiom (line 81); the `@status_variants` enum (line 27) and `status_icon/1` map (lines 245-250).
    - .planning/phases/37-component-primitives/37-PATTERNS.md — "cl_source_card/1" section (the `source_variant` API, the variant→class + resolved-icon pattern, the drift-map contract: success replaces #4A6238, info replaces #3F6F80).
    - .planning/phases/37-component-primitives/37-UI-SPEC.md — §6 cl_source_card (structure, attributes, visual contract, drift-map alignment, CSS classes introduced).
  </read_first>
  <behavior>
    - Test 1: `cl_source_card source_variant="success"` with a `:title` slot renders `<div class="cl-source-card cl-source-card--success">`, an `<svg` header icon (the success silhouette), and the title text.
    - Test 2: `cl_source_card source_variant="info"` renders `cl-source-card--info` and resolves the info icon; an optional `:meta` slot, when provided, renders `.cl-source-card__meta`.
    - Test 3: rendered HTML contains no `#hex`.
  </behavior>
  <action>
    Add `cl_source_card/1` to `components.ex` per D-06 / PATTERNS, mirroring `cl_banner`'s variant+icon+slot shape: attr `source_variant :string, values: ~w(success info neutral warning danger ai), default: "neutral"`; slots `:title, required: true`, `:meta` (optional), `:inner_block` (body). Resolve the header icon from the variant via the existing `status_icon/1` map using the `assign_new(:resolved_icon, fn -> assigns[:icon] || status_icon(assigns.source_variant) end)` idiom (REUSE the map — do not hand-author SVG). Emit `<div class={["cl-source-card", "cl-source-card--#{@source_variant}"]}>` with a `.cl-source-card__header` containing `<.cl_icon name={@resolved_icon} class="cl-source-card__icon" />` + the title slot, a `.cl-source-card__body` with the inner_block, and an optional `<div :if={@meta != []} class="cl-source-card__meta">{render_slot(@meta)}</div>`. The icon MUST always be present (never color alone, §7.5). The variant maps to the existing chip token triplet via the Plan 01 CSS (`success` → the `#4A6238` drift replacement, `info` → the `#3F6F80` replacement — P40 swap contract). Token-only; render title/body via auto-escaped `{...}`. Add the three tests above.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def cl_source_card(' lib/cairnloop/web/components.ex` matches.
    - The component declares `source_variant` with `values: ~w(success info neutral warning danger ai)` and resolves its header icon via the existing `status_icon/1` map.
    - A test asserts `html =~ "cl-source-card--success"`, `html =~ "<svg"` (icon present — never color alone), and the `:title` slot text; another asserts `cl-source-card--info` and the optional `:meta` slot.
    - Every cl_source_card test asserts `refute html =~ ~r/#[0-9a-fA-F]{3,6}/`.
    - `mix compile --warnings-as-errors` and `mix test test/cairnloop/web/components_test.exs` both exit 0.
  </acceptance_criteria>
  <done>`cl_source_card/1` renders a variant-keyed status surface with a required title, a distinct-silhouette icon resolved from the existing map, and an optional meta slot; success/info align to the P40 drift-swap contract; tests confirm icon presence and token-purity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller assigns → rendered HTML | Caller (P38+) supplies `label`/title/meta/body text and `checked`/`variant`. Output-encoding + the `:rest` allowlist are the controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08 | Tampering / Elevation (XSS) | `cl_source_card`/`cl_status_cell`/`cl_switch` caller text | mitigate | All caller text rendered via HEEx auto-escaped `{...}`; NO caller input reaches `Phoenix.HTML.raw/1`. `cl_icon`'s `raw/1` stays on its FIXED internal icon-path allowlist — `cl_source_card` resolves the icon name from the trusted `status_icon/1` map, never from caller input (V5 output-encoding). |
| T-37-09 | Spoofing / Tampering (attr injection) | `cl_switch` `:rest` global | mitigate | `:rest` uses an explicit `include:` allowlist (`phx-click`/`phx-value-*`/`disabled`/form attrs) — only known-safe attributes pass through; arbitrary attributes are dropped by Phoenix.Component. |
| T-37-10 | Information disclosure (state-by-color-alone, a11y) | status-bearing primitives | mitigate | Every variant pairs color + distinct-silhouette `cl_icon` + a visible text label (§7.5); `aria-checked` is a string; verified by render tests asserting `<svg` + label presence + `aria-checked="false"`. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: not applicable). |

No HIGH-severity threats.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/cairnloop/web/components_test.exs` green (existing + new).
- `mix test test/cairnloop/web/brand_token_gate_test.exs` green.
- `grep -F 'action_tone' lib/cairnloop/web/components.ex` returns nothing (D-07 grounding honored).
</verification>

<success_criteria>
- `cl_source_card/1` (source_variant + icon), `cl_status_cell/1` (chip + label), `cl_switch/1` (role="switch" + string aria-checked + label) all exist, token-pure, never color alone (UIC-04).
- No primitive depends on the non-existent `action_tone/1`.
- Build warnings-clean; all render tests token-pure.
</success_criteria>

<output>
Create `.planning/phases/37-component-primitives/37-04-SUMMARY.md` when done.
</output>
