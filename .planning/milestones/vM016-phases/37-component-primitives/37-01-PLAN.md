---
phase: 37-component-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/static/cairnloop.css
  - test/cairnloop/web/cairnloop_css_test.exs
autonomous: true
requirements: [UIC-05]
must_haves:
  truths:
    - "The three layout tokens --cl-content-max / --cl-rail-width / --cl-page-gutter are defined in cairnloop.css and usable by cl_page and cl_hero"
    - "The three inert utilities .cl-gap-2 / .cl-align-center / .cl-justify-between are defined (no longer silent no-ops in markup)"
    - ".cl-table-scroll provides overflow-x:auto + a focus ring, so a wrapped table scrolls accessibly"
    - "All visual CSS for the eight primitives (.cl-page*, .cl-hero*, .cl-fact-list*, .cl-source-card*, .cl-switch*) exists as bare var(--cl-*) tokens with no hardcoded hex"
  artifacts:
    - path: "priv/static/cairnloop.css"
      provides: "Layout tokens, inert utilities, .cl-table-scroll, and all primitive visual classes"
      contains: "--cl-content-max"
    - path: "test/cairnloop/web/cairnloop_css_test.exs"
      provides: "Machine-verification that the CSS literals are present"
      exports: []
  key_links:
    - from: "priv/static/cairnloop.css"
      to: ".cl-page--wide / .cl-page--reading"
      via: "max-width references to the new layout tokens"
      pattern: "max-width:\\s*var\\(--cl-content-max"
---

<objective>
Add every CSS deliverable for Phase 37 to `priv/static/cairnloop.css`: the three layout
tokens (D-09), the three inert utility definitions (D-10), the `.cl-table-scroll` accessible
scroll-wrapper class (D-11), and the full visual stylesheet for the eight new primitives
(`.cl-page*`, `.cl-hero*`, `.cl-fact-list*`, `.cl-source-card*`, `.cl-switch*`). This plan owns
`cairnloop.css` exclusively so it can run in parallel with the first component plan (which owns
`components.ex`). It also adds a small CSS-presence test so UIC-05's CSS half is machine-verified.

Purpose: The component plans emit `.cl-*` class names only; this plan is the single source of
visual truth those classes resolve to. Token-purity is enforced here — all color/spacing is bare
`var(--cl-*)`, never hardcoded hex.
Output: Extended `cairnloop.css` + new `test/cairnloop/web/cairnloop_css_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-component-primitives/37-UI-SPEC.md
@.planning/phases/37-component-primitives/37-PATTERNS.md
@.planning/phases/37-component-primitives/37-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add layout tokens + three inert utilities + .cl-table-scroll</name>
  <read_first>
    - priv/static/cairnloop.css — the `:root` control-sizing token block (cairnloop.css:130-133), the `--cl-space-*` ladder (cairnloop.css:86-101), the `.cl-row`/`.cl-stack` family (cairnloop.css:423-427), and the `.cl-focusable:focus-visible` focus-ring rule (cairnloop.css:236-239) — these are the conventions to mirror.
    - .planning/phases/37-component-primitives/37-PATTERNS.md — "Pattern F — CSS token-block" and the "3 inert utilities" / ".cl-table-scroll wrapper" sections (exact literal definitions to copy).
    - .planning/phases/37-component-primitives/37-UI-SPEC.md — "Spacing Scale" (token values) and "§9 .cl-table-scroll wrapper" (CSS definition + focus-visible rule).
  </read_first>
  <action>
    In the existing `:root` token block of `cairnloop.css`, placed near the control-sizing tokens (around line 130-133), add exactly three layout tokens per D-09 / UI-SPEC Spacing: `--cl-content-max: 1200px;` (grounded in the existing `.cl-main` max-width at cairnloop.css:254), `--cl-rail-width: 352px;` (grounded in `.evidence-rail` width at cairnloop.css:509), and `--cl-page-gutter: var(--cl-space-5);` (= 16px). Do NOT tokenize any breakpoint — these tokens drive `max-width` values only, never `@media` conditions (var() is illegal in media queries — that work is P43).
    Then add exactly three inert-utility classes per D-10 (no more — do NOT grow a utility framework; `.cl-row`/`.cl-stack` remain the preferred composites): `.cl-gap-2 { gap: var(--cl-space-2); }`, `.cl-align-center { align-items: center; }`, `.cl-justify-between { justify-content: space-between; }`. Place them adjacent to the existing `.cl-row`/`.cl-stack` family.
    Then add the `.cl-table-scroll` wrapper per D-11 / UI-SPEC §9: `.cl-table-scroll { overflow-x: auto; -webkit-overflow-scrolling: touch; }` plus a focus ring `.cl-table-scroll:focus-visible { box-shadow: var(--cl-focus-ring); border-radius: var(--cl-radius-xs); }` (mirror the existing `.cl-focusable:focus-visible` convention). All values are bare `var(--cl-*)` tokens or literal pixel values — never hardcoded color hex.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*/\*' priv/static/cairnloop.css | grep -c -e '--cl-content-max' -e '--cl-rail-width' -e '--cl-page-gutter' -e '\.cl-gap-2' -e '\.cl-align-center' -e '\.cl-justify-between' -e '\.cl-table-scroll'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F -- '--cl-content-max: 1200px' priv/static/cairnloop.css` matches.
    - `grep -F -- '--cl-rail-width: 352px' priv/static/cairnloop.css` matches.
    - `grep -F -- '--cl-page-gutter: var(--cl-space-5)' priv/static/cairnloop.css` matches.
    - `grep -F '.cl-gap-2' priv/static/cairnloop.css`, `.cl-align-center`, and `.cl-justify-between` each match exactly once as a selector.
    - `grep -F '.cl-table-scroll { overflow-x: auto' priv/static/cairnloop.css` matches.
    - No `#` hex literal appears in any line added by this task (`overflow-x`, `gap`, `box-shadow` all use tokens/keywords).
  </acceptance_criteria>
  <done>The three layout tokens, three inert utilities, and `.cl-table-scroll` (with focus-visible ring) are defined in `cairnloop.css` using only bare `var(--cl-*)` tokens and pixel literals.</done>
</task>

<task type="auto">
  <name>Task 2: Add primitive visual CSS (.cl-page*, .cl-hero*, .cl-fact-list*, .cl-source-card*, .cl-switch*)</name>
  <read_first>
    - priv/static/cairnloop.css — `.cl-stat__count` + `--calm` (cairnloop.css:413-414, the copper display numeral to clone heavier into `.cl-hero__count`); the `cl_chip` variant triplet rules (cairnloop.css:344-348, surface/border/text per variant — copy the shape for `.cl-source-card--{variant}`); `.cl-button:disabled` (cairnloop.css:304, `opacity:0.5; cursor:not-allowed` for the switch disabled state); the `.cl-details dt`/`dd` color/size rules (cairnloop.css:483-485, the muted-micro-label idiom for `.cl-fact-list`).
    - .planning/phases/37-component-primitives/37-UI-SPEC.md — the per-component "CSS classes introduced" lists for cl_page (§1), cl_hero (§2), cl_fact_list (§5), cl_source_card (§6), cl_switch (§8), the Typography table (hero count 48px Fraunces; dt 12px/500 muted; dd 13px/400), and the Color "Semantic status color contract" variant triplet table.
    - .planning/phases/37-component-primitives/37-PATTERNS.md — the `cl_hero`, `cl_fact_list`, `cl_source_card`, `cl_switch` "CSS analog" excerpts.
  </read_first>
  <action>
    Add the net-new visual CSS for the primitives, bare `var(--cl-*)` tokens only, following the BEM `.cl-block__element--modifier` convention already in the file:
    - `.cl-page` + `.cl-page--wide { max-width: var(--cl-content-max); margin: 0 auto; padding: 0 var(--cl-page-gutter); }` + `.cl-page--reading { max-width: var(--cl-rail-width); margin: 0 auto; padding: 0 var(--cl-page-gutter); }` + `.cl-page__header`, `.cl-page__title` (28px / `--cl-weight-semibold` / `--cl-font-title`), `.cl-page__subtitle` (`--cl-font-body` / `--cl-text-muted`), `.cl-page__subnav`, `.cl-page__body`.
    - `.cl-hero` (full-width block, `--cl-space-7` vertical padding) + `.cl-hero__job` (18px / 600 / `--cl-text`) + `.cl-hero__count` (Fraunces `--cl-font-display`, 48px, line-height 1, `color: var(--cl-primary)`) + `.cl-hero__count--calm { color: var(--cl-success); }` + `.cl-hero__detail` (13px / `--cl-text-muted`) + `.cl-hero__cta`. The 48px copper count clones `.cl-stat__count` heavier — this is the SC-2 "~2-3× weight" target.
    - `.cl-fact-list` (dedicated, NOT a `.cl-details dl` reuse per UI-SPEC §5 rationale) + `.cl-fact-list__row` (`--cl-space-4` row padding) + `.cl-fact-list__label` (`dt`: 12px / 500 / `--cl-text-muted`) + `.cl-fact-list__value` (`dd`: 13px / 400 / `--cl-text`, `margin:0`).
    - `.cl-source-card` (surface `--cl-surface`, `1px solid var(--cl-border)`, `--cl-radius-sm`, `--cl-space-4` body padding) + the six `.cl-source-card--{success,info,warning,danger,ai,neutral}` modifiers, each using the SAME status-token triplet shape as `cl_chip` (`background: var(--cl-{v}-surface); border-color: var(--cl-{v}-border); color: var(--cl-{v}-text);`) + `.cl-source-card__header`, `.cl-source-card__icon` (16px), `.cl-source-card__body` (13px / `--cl-text-muted`), `.cl-source-card__meta` (12px / `--cl-text-muted`).
    - `.cl-switch { display: inline-flex; align-items: center; gap: var(--cl-space-2); min-height: 44px; min-width: 44px; }` (44px tap target — preemptive RESP-02) + `.cl-switch:disabled { opacity: 0.5; cursor: not-allowed; }` + `.cl-switch__track` (`--cl-border` bg, 24px tall, 44px wide, `--cl-radius-full`; `[aria-checked="true"]` state → `background: var(--cl-primary)`) + `.cl-switch__thumb` (20px circle, `--cl-surface-raised`, `transition: transform var(--cl-dur-instant, 100ms) var(--cl-ease-out)`) + `.cl-switch__label` (13px / 500 / `--cl-text`). Use the `[aria-checked="true"]` attribute selector to drive the checked visual — never bind it to color alone in markup (the label text already carries state, §7.5).
    Emit ZERO hardcoded hex and ZERO `var(--cl-token, #hex)` fallback forms anywhere in this block — bare tokens only.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*/\*' priv/static/cairnloop.css | grep -c -e '\.cl-page--wide' -e '\.cl-hero__count' -e '\.cl-fact-list__label' -e '\.cl-source-card--success' -e '\.cl-switch__track'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '.cl-page--wide' priv/static/cairnloop.css` and `.cl-page--reading` both match, each referencing a layout token in its `max-width`.
    - `grep -F '.cl-hero__count' priv/static/cairnloop.css` matches and the rule references `var(--cl-primary)`; `.cl-hero__count--calm` references `var(--cl-success)`.
    - `.cl-fact-list__label` and `.cl-fact-list__value` selectors exist (dedicated, not under `.cl-details`).
    - All six `.cl-source-card--{success,info,warning,danger,ai,neutral}` selectors exist; each uses `var(--cl-*-surface/border/text)` tokens.
    - `.cl-switch`, `.cl-switch__track`, `.cl-switch__thumb`, `.cl-switch__label` selectors exist; `.cl-switch` declares `min-height: 44px`.
    - `grep -nE '#[0-9a-fA-F]{3,6}' <newly-added lines>` finds zero hex in any line this task added (verified in Task 3's presence test scope or by eyeballing the diff — the brand gate also enforces no `var(--cl-x,#hex)` fallback).
  </acceptance_criteria>
  <done>All net-new primitive visual CSS exists, BEM-named, token-only (bare `var(--cl-*)`), with the 44px switch tap target and the six source-card variant triplets aligned to the existing chip triplets.</done>
</task>

<task type="auto">
  <name>Task 3: Add CSS-presence test (machine-verify UIC-05's CSS half)</name>
  <read_first>
    - test/cairnloop/web/components_test.exs — the `use ExUnit.Case, async: true` harness header (lines 1-10) for the test-module idiom to mirror.
    - .planning/phases/37-component-primitives/37-VALIDATION.md — "Wave 0 Requirements" (the optional, recommended CSS-presence test) and "Validation Levels" CSS-presence row.
    - .planning/phases/37-component-primitives/37-RESEARCH.md — Validation Architecture "CSS presence (3 tokens + 3 utilities + .cl-table-scroll)" row.
  </read_first>
  <action>
    Create `test/cairnloop/web/cairnloop_css_test.exs` as a new Repo-free ExUnit module (`use ExUnit.Case, async: true`). In a `setup_all` (or a module attribute), read `priv/static/cairnloop.css` once into a string via `File.read!/1` (resolve the path relative to the app priv dir or the repo root — use `Path.join` from `File.cwd!()` or `Application.app_dir/2`; prefer the cwd-relative `priv/static/cairnloop.css` to keep it simple and Repo-free). Then assert with `=~` that the file contains each required literal: the three tokens (`--cl-content-max`, `--cl-rail-width`, `--cl-page-gutter`), the three utilities (`.cl-gap-2`, `.cl-align-center`, `.cl-justify-between`), and `.cl-table-scroll`. Add one assertion that the primitive classes `.cl-hero__count`, `.cl-fact-list`, `.cl-source-card--success`, and `.cl-switch__track` are present so the CSS half of all primitives is covered. This is a pure file-content test — no DB, no `# REPO-UNAVAILABLE` marker needed.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/cairnloop_css_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/web/cairnloop_css_test.exs` exits 0 with all assertions passing.
    - The test reads `priv/static/cairnloop.css` and asserts presence of all three layout tokens, all three inert utilities, and `.cl-table-scroll` by literal `=~` substring.
    - The test asserts `.cl-hero__count`, `.cl-fact-list`, `.cl-source-card--success`, `.cl-switch__track` literals are present.
    - `mix compile --warnings-as-errors` exits 0 (no test-module warnings).
  </acceptance_criteria>
  <done>`cairnloop_css_test.exs` machine-verifies every UIC-05 CSS literal plus the primitive CSS classes; the test is green and Repo-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | This plan adds CSS + a file-content test only. No untrusted input crosses any boundary; CSS is static design-system text, the test reads a checked-in file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering (style drift) | `cairnloop.css` primitive classes | mitigate | All color/spacing emitted as bare `var(--cl-*)` tokens — zero hardcoded hex / `rgba()` / `var(--cl-x,#hex)` fallback; the brand-token gate (`brand_token_gate_test.exs`) and the new CSS-presence test enforce token-only output (D-13). |
| T-37-02 | Information disclosure | CSS file-read in test | accept | The test reads only the project's own checked-in `priv/static/cairnloop.css`; no secret, no PII, no external path. Low risk. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: "not applicable — zero external packages"). No slopcheck required. |

No HIGH-severity threats. The single real control (V5 output-encoding / token-purity) is structural and gate-enforced.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/cairnloop/web/cairnloop_css_test.exs` green.
- `mix test test/cairnloop/web/brand_token_gate_test.exs` green (the new CSS introduces no `.ex` hex; the gate scans `.ex` files, `.css` stays unscanned per the gate design).
- `grep -nE '#[0-9a-fA-F]{3,6}' priv/static/cairnloop.css` may match pre-existing token fallbacks elsewhere in the file, but NO line added by this plan contains a hex literal.
</verification>

<success_criteria>
- Three layout tokens, three inert utilities, `.cl-table-scroll`, and all primitive visual CSS exist in `cairnloop.css`, token-only.
- `cairnloop_css_test.exs` passes, machine-verifying UIC-05's CSS half.
- Build is warnings-clean.
</success_criteria>

<output>
Create `.planning/phases/37-component-primitives/37-01-SUMMARY.md` when done.
</output>
