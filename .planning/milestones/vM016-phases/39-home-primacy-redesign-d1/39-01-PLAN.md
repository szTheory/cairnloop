---
phase: 39-home-primacy-redesign-d1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/chat.ex
  - test/cairnloop/chat_test.exs
autonomous: true
requirements: [HOME-02, HOME-05]
must_haves:
  truths:
    - "The Chat facade can return conversations scoped to a single status without an in-memory filter (D-02, backs HOME-02 deep-link landing)"
    - "The Chat facade can return a scoped count via a SELECT count(*) WHERE, not a full list load + Enum.count (D-09, HOME-05)"
    - "An unknown/garbage status atom degrades to the unfiltered query, never a crash (D-03 defense-in-depth at the query layer)"
    - "The sealed 0-arity list_conversations/0 contract is preserved verbatim as a distinct clause"
  artifacts:
    - path: "lib/cairnloop/chat.ex"
      provides: "list_conversations/1 + count_conversations/1 + private scope_status/2 (additive siblings)"
      contains: "def count_conversations"
    - path: "test/cairnloop/chat_test.exs"
      provides: "scope_status/2 query-shape assertions + # REPO-UNAVAILABLE count round-trip tests"
      contains: "scope_status"
  key_links:
    - from: "lib/cairnloop/chat.ex count_conversations/1"
      to: "lib/cairnloop/chat.ex scope_status/2"
      via: "shared private where-builder so list and count can never disagree"
      pattern: "scope_status\\(query"
    - from: "scope_status/2"
      to: "Conversation.status enum"
      via: "whitelist guard [:open, :resolved, :archived]"
      pattern: "status in \\[:open, :resolved, :archived\\]"
---

<objective>
Extend the `Cairnloop.Chat` read facade with additive, status-scoped query functions that back
both the resolved deep-link landing (HOME-02) and the scoped Home counts (HOME-05) — without
touching the sealed 0-arity `list_conversations/0` contract.

Purpose: Implements D-02 (filter pushed into the facade as a scoped Ecto `where`, not
`Enum.filter`) and D-09 (cheap `SELECT count(*)` scoped counts). These functions are the shared
foundation both InboxLive (Plan 02) and HomeLive (Plan 03) consume, so they ship first.

Output: `list_conversations/1`, `count_conversations/1`, and private `scope_status/2` added to
`lib/cairnloop/chat.ex`; query-shape + `# REPO-UNAVAILABLE` tests in `test/cairnloop/chat_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/39-home-primacy-redesign-d1/39-CONTEXT.md
@.planning/phases/39-home-primacy-redesign-d1/39-RESEARCH.md
@.planning/phases/39-home-primacy-redesign-d1/39-PATTERNS.md
@.planning/phases/39-home-primacy-redesign-d1/39-VALIDATION.md

<interfaces>
<!-- Verified in-repo this planning pass. Executor uses these directly, no exploration needed. -->

Conversation schema (lib/cairnloop/conversation.ex:6) — the ONLY real status values:
  field(:status, Ecto.Enum, values: [:open, :resolved, :archived], default: :open)
  There is NO :awaiting_customer / :new — whitelist only [:open, :resolved, :archived].

Chat facade current shape (lib/cairnloop/chat.ex):
  - line 2:  import Ecto.Query
  - line 6:  defp repo do  Application.fetch_env!(:cairnloop, :repo)  end
  - line 10: def list_conversations do  (SEALED 0-arity — preserve verbatim)
               Conversation |> order_by(desc: :updated_at) |> repo().all()
  In-file scoped-read precedent (chat.ex ~118): `from(... where: ... == ^...)` — mirror this idiom.

Test harness (test/cairnloop/chat_test.exs): MockRepo module (~line 6) + setup that does
  Application.put_env(:cairnloop, :repo, MockRepo) with on_exit delete_env (~line 106). MockRepo
  currently has all/1 + insert/1; it needs an aggregate/3 clause for count_conversations/1 tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — scope_status/2 query-shape + count tests (RED)</name>
  <files>test/cairnloop/chat_test.exs</files>
  <read_first>
    - test/cairnloop/chat_test.exs (existing MockRepo at ~line 6, setup put_env at ~line 106, existing list_conversations/0 tests)
    - .planning/phases/39-home-primacy-redesign-d1/39-PATTERNS.md (chat_test.exs analog block, ~lines 390-404)
    - .planning/phases/39-home-primacy-redesign-d1/39-RESEARCH.md (§ Validation Architecture rows HOME-02c, HOME-05a; § "What needs a live Repo")
    - lib/cairnloop/conversation.ex (status enum, line 6)
  </read_first>
  <behavior>
    - scope_status query-shape (no Repo): a query scoped to :resolved inspects to a where clause on `status == ^:resolved`; scoped to nil is unchanged from the base query; scoped to an unknown atom (e.g. :bogus) is unchanged (defense-in-depth). Assert by building the query through `Chat.list_conversations(status: ...)` against a MockRepo whose `all/1` captures and returns/inspects the received `%Ecto.Query{}`, OR by asserting `Macro.to_string`/`inspect` of the query contains the where fragment. No live Repo.
    - count_conversations/1 issues a single count aggregate over the scoped query (assert MockRepo.aggregate/3 is called with `:count, :id`, not a list load + Enum.count). Use a MockRepo aggregate/3 stub that records its args.
    - # REPO-UNAVAILABLE round-trip: count_conversations(status: :resolved) returns the real resolved row count; list_conversations(status: :resolved) returns only resolved rows. Mark these `# REPO-UNAVAILABLE` (run in CI :integration lane only).
  </behavior>
  <action>
    Extend `test/cairnloop/chat_test.exs` reusing the existing `MockRepo` + `setup` put_env harness.
    Add an `aggregate/3` clause to MockRepo recording `{query, agg, field}` (or returning a fixture
    count) so `count_conversations/1` is exercisable headlessly. Add a `describe "scope_status/2 (via
    list_conversations/1 + count_conversations/1)"` block with the three query-shape assertions per
    `<behavior>` (resolved → where on `status == ^:resolved`; nil → unscoped; unknown atom → unscoped,
    proving D-03 defense-in-depth at the query layer). Add the two `# REPO-UNAVAILABLE` round-trip
    tests for real count/list contents. These tests MUST fail now (functions do not exist yet) — this
    is the RED step. Do not implement chat.ex in this task. Headless tests use `async: false` to match
    the existing module (it swaps a global `:repo` env).
  </action>
  <verify>
    <automated>mix test test/cairnloop/chat_test.exs 2>&1 | grep -qE "undefined function|UndefinedFunctionError|no function clause|test.*scope_status" && echo "RED: new tests present and failing as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `test/cairnloop/chat_test.exs` contains a `describe` referencing `scope_status` with ≥3 assertions (resolved / nil / unknown-atom).
    - The headless query-shape + aggregate-call tests FAIL (RED) because `list_conversations/1` and `count_conversations/1` do not yet exist.
    - The two round-trip tests are present and carry a literal `# REPO-UNAVAILABLE` comment.
    - MockRepo has an `aggregate/3` clause.
  </acceptance_criteria>
  <done>New scope_status / count_conversations tests exist and fail for the right reason (missing functions), with round-trip tests marked # REPO-UNAVAILABLE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add list_conversations/1 + count_conversations/1 + scope_status/2 (GREEN)</name>
  <files>lib/cairnloop/chat.ex</files>
  <read_first>
    - lib/cairnloop/chat.ex (lines 1-14: module, import Ecto.Query, repo/0, sealed list_conversations/0; in-file where precedent ~line 118)
    - .planning/phases/39-home-primacy-redesign-d1/39-PATTERNS.md (chat.ex pattern block, lines 33-89 — exact idiom-matched shapes)
    - .planning/phases/39-home-primacy-redesign-d1/39-RESEARCH.md (Pattern 2, lines 250-294)
    - lib/cairnloop/conversation.ex (status enum, line 6)
    - test/cairnloop/chat_test.exs (the tests written in Task 1 — implement to satisfy them)
  </read_first>
  <action>
    Add three additive members to `Cairnloop.Chat`, mirroring the in-file query-pipe idiom and the
    `from ... where ... == ^...` precedent. Keep the sealed 0-arity `list_conversations/0` clause
    UNTOUCHED and as a DISTINCT head clause (do NOT collapse into a single `\\ []` default — A3
    sealed-contract grep): (1) `list_conversations(opts) when is_list(opts)` — `order_by(desc:
    :updated_at)` piped through `scope_status(Keyword.get(opts, :status))` then `repo().all()`;
    (2) `count_conversations(opts \\ [])` — `scope_status(Keyword.get(opts, :status))` then
    `repo().aggregate(:count, :id)` (cheap SELECT count(*), NOT load + Enum.count, per D-09);
    (3) private `scope_status/2` with three clauses — `scope_status(query, nil) -> query`;
    `scope_status(query, status) when status in [:open, :resolved, :archived] -> where(query, [c],
    c.status == ^status)`; `scope_status(query, _other) -> query` (defense-in-depth: ignore unknown
    atoms — never crash, mirrors D-03 at the query layer). Whitelist ONLY the three real enum values;
    do NOT add :awaiting_customer / :new. No `String.to_existing_atom` anywhere. Confirm warnings-clean.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/chat_test.exs --exclude integration</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0.
    - `grep -c 'def list_conversations do' lib/cairnloop/chat.ex` returns 1 (sealed 0-arity preserved as its own clause).
    - `grep -q 'def count_conversations' lib/cairnloop/chat.ex` and `grep -q 'defp scope_status' lib/cairnloop/chat.ex` both succeed.
    - `grep -q 'aggregate(:count, :id)' lib/cairnloop/chat.ex` succeeds (count is an aggregate, not a list load).
    - `grep -q 'status in \[:open, :resolved, :archived\]' lib/cairnloop/chat.ex` succeeds; `grep -q 'to_existing_atom' lib/cairnloop/chat.ex` returns nothing.
    - The headless (non-integration) chat_test.exs assertions from Task 1 now PASS.
  </acceptance_criteria>
  <done>Scoped facade queries exist, are warnings-clean, share one scope_status/2, preserve the sealed 0-arity contract, and turn the Task 1 headless tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InboxLive `?status=` → Chat facade | A status value (already normalized by Plan 02's `normalize_status/1`) crosses into a query builder; this plan is the second line of defense. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Denial of Service | `scope_status/2` | mitigate | Guard whitelists only the three real enum atoms; any other atom falls through to the `_other -> query` clause (unfiltered), so a malformed status never reaches the DB as a bad predicate and never crashes the query builder. Defense-in-depth behind Plan 02's `normalize_status/1`. |
| T-39-02 | Tampering / Injection | `scope_status/2` Ecto `where` | mitigate | Status bound via parameterized `^status` pin in `where(query, [c], c.status == ^status)` — no string interpolation of user input into SQL. Verified by the query-shape test (Task 1). |
| T-39-SC | Tampering | hex/npm/cargo installs | accept | No packages installed this plan (RESEARCH § Package Legitimacy Audit: not applicable). No registry surface. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` passes.
- `mix test test/cairnloop/chat_test.exs --exclude integration` passes (headless scope_status + aggregate-call assertions green).
- Round-trip count/list-contents tests present and marked `# REPO-UNAVAILABLE` (validated in CI `mix test.integration`, not locally — Repo may be unavailable per CLAUDE.md).
- Sealed contract intact: exactly one `def list_conversations do` 0-arity clause.
</verification>

<success_criteria>
- `Chat.list_conversations(status: :resolved)` and `Chat.count_conversations(status: :resolved)` exist and route through one private `scope_status/2`.
- Unknown/nil status → unfiltered query (no crash) at the facade layer (D-03 defense-in-depth).
- `count_conversations/1` uses `repo().aggregate(:count, :id)` (D-09 cheap count).
- Sealed 0-arity `list_conversations/0` preserved verbatim as a distinct clause.
- Warnings-clean build; headless tests green.
</success_criteria>

<output>
Create `.planning/phases/39-home-primacy-redesign-d1/39-01-SUMMARY.md` when done.
</output>
