---
phase: 45-seed-enrichment-screenshot-regen-verification-sweep
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/screenshots/capture.mjs
  - examples/cairnloop_example/screenshots/README.md
autonomous: true
requirements:
  - VERIFY-01
must_haves:
  truths:
    - "VERIFY-01: Screenshot capture supports explicit light and dark operator/admin captures for every required Phase 45 screen."
    - "CONTEXT D-07: Capture remains an evidence pack and does not add a CI pixel baseline, new vendor visual review, or snapshot gate."
    - "CONTEXT D-08: Phase 45 acceptance capture scope is operator/admin only; demo index, marketing, and customer chat do not count toward the ledger."
    - "CONTEXT D-09: Dark and light captures force both Playwright browser color scheme and Cairnloop app theme state."
    - "CONTEXT D-10: Static screenshots remain motion-stabilized; live motion stays covered by E2E."
    - "UI-SPEC: Capture includes a true operator/admin empty state by searching audit events for the seed-proven unmatched sentinel `phase45-empty-audit-filter` and waiting for `No audit events found`."
  artifacts:
    - path: "examples/cairnloop_example/screenshots/capture.mjs"
      provides: "Dual-theme Playwright screenshot pipeline"
      contains: "THEMES"
    - path: "examples/cairnloop_example/screenshots/README.md"
      provides: "Non-gating dual-theme screenshot usage instructions"
      contains: "guides/assets/light"
  key_links:
    - from: "examples/cairnloop_example/screenshots/capture.mjs"
      to: "priv/static/cairnloop.css"
      via: "browser captures render the final vM017 runtime tokens through the example app"
      pattern: "colorScheme"
    - from: "examples/cairnloop_example/screenshots/capture.mjs"
      to: "lib/cairnloop/web/settings_live.ex"
      via: "theme forcing uses the app's `phx:theme` and `data-theme` mechanism"
      pattern: "phx:theme|dataset\\.theme"
---

<objective>
Make the existing Playwright screenshot pipeline produce explicit final-brand light and dark operator/admin evidence.

Purpose: Prepare screenshot generation for VERIFY-01 without changing product behavior, CI policy, or visual tooling ownership.
Output: `capture.mjs` dual-theme capture support and README instructions that preserve the non-gating evidence posture.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-CONTEXT.md
@.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-RESEARCH.md
@.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-PATTERNS.md
@.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-UI-SPEC.md
@examples/cairnloop_example/screenshots/capture.mjs
@examples/cairnloop_example/screenshots/README.md
@examples/cairnloop_example/screenshots/package.json
@lib/cairnloop/web/settings_live.ex
@priv/static/cairnloop.css
@.planning/milestones/vM017-phases/52-collateral-wiring-qa-validation-sweep/52-02-SUMMARY.md
</context>

## Artifacts This Phase Produces

| Kind | Name / Path | Created By |
|------|-------------|------------|
| Constant | `THEMES` in `examples/cairnloop_example/screenshots/capture.mjs` | Task 1 |
| Function | `themeOutputDir(themeName)` in `examples/cairnloop_example/screenshots/capture.mjs` | Task 1 |
| Function | `applyThemeState(page, themeName)` in `examples/cairnloop_example/screenshots/capture.mjs` | Task 1 |
| Data entry | `SHOTS` entries for `06b-action-rejected.png` and `06c-action-deferred.png` | Task 1 |
| Data entry | `SHOTS` entry for `14-audit-empty-state.png` using audit search sentinel `phase45-empty-audit-filter` | Task 1 |
| Documentation section | `Light and dark captures` in `examples/cairnloop_example/screenshots/README.md` | Task 2 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend capture.mjs for explicit light and dark themes</name>
  <files>examples/cairnloop_example/screenshots/capture.mjs</files>
  <read_first>
    @examples/cairnloop_example/screenshots/capture.mjs
    @examples/cairnloop_example/screenshots/package.json
    @lib/cairnloop/web/settings_live.ex
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-UI-SPEC.md
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-CONTEXT.md
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-PATTERNS.md
  </read_first>
  <behavior>
    - Every Phase 45 operator/admin `SHOTS` entry is captured under both `guides/assets/light/` and `guides/assets/dark/` per D-08 and D-09.
    - The light pass sets Playwright `colorScheme: "light"`, `document.documentElement.dataset.theme = "light"`, and `localStorage["phx:theme"] = "light"`.
    - The dark pass sets Playwright `colorScheme: "dark"`, `document.documentElement.dataset.theme = "dark"`, and `localStorage["phx:theme"] = "dark"`.
    - Static capture keeps reduced motion and the existing stabilization stylesheet per D-10.
    - The capture script remains non-gating and does not add visual assertion infrastructure per D-07.
  </behavior>
  <action>Refactor `capture.mjs` around a `THEMES` array containing `light` and `dark`, a `themeOutputDir(themeName)` helper that writes to `guides/assets/{theme}/`, and an `applyThemeState(page, themeName)` helper used before each route hydration. Preserve `BASE_URL`, `VIEWPORT`, `DEVICE_SCALE`, `STABILIZE_CSS`, `waitForLiveViewConnected/1`, and screen-specific `waitFor` selectors. Add or update `SHOTS` so the operator/admin matrix includes cockpit home, inbox/list triage, conversation workspace, AI draft approval, pending action, executed action, rejected action, deferred action, resolved conversation, outbound recovery, bulk recovery, knowledge index, knowledge gaps, KB suggestions, KB editor draft, audit log, settings, and `14-audit-empty-state.png`. Implement the empty-state shot against `/support/audit-log`: after the page is live, fill the search input with `phase45-empty-audit-filter`, submit or allow `phx-change` to settle, and wait for the real empty copy `No audit events found`. Keep any root-level light-copy compatibility only for existing docs references; Phase 45 acceptance paths must be the theme directories. Do not add package upgrades, screenshot assertions, vendor visual tooling, CI wiring, demo-index acceptance, marketing pages, or customer chat acceptance.</action>
  <acceptance_criteria>
    - Source assertion: `capture.mjs` declares `THEMES` with `light` and `dark`.
    - Source assertion: `browser.newContext` uses `colorScheme: theme.colorScheme`.
    - Source assertion: `capture.mjs` sets both `localStorage.setItem("phx:theme", themeName)` and `document.documentElement.dataset.theme = themeName`.
    - Source assertion: screenshot paths include `guides/assets/light` and `guides/assets/dark` directories.
    - Source assertion: `SHOTS` includes rejected and deferred governed-action entries with stable file names `06b-action-rejected.png` and `06c-action-deferred.png`.
    - Source assertion: `SHOTS` includes `14-audit-empty-state.png`, `phase45-empty-audit-filter`, and waits for `No audit events found`.
    - CLI assertion: JavaScript syntax parses successfully.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/cairnloop_example/screenshots && node --check capture.mjs && rg -n 'THEMES|colorScheme: theme\\.colorScheme|phx:theme|dataset\\.theme|06b-action-rejected|06c-action-deferred|14-audit-empty-state|phase45-empty-audit-filter|No audit events found' capture.mjs</automated>
  </verify>
  <done>The screenshot script can generate the full Phase 45 operator/admin matrix in explicit light and dark themes without changing product behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Update screenshot README for non-gating dual-theme evidence</name>
  <files>examples/cairnloop_example/screenshots/README.md</files>
  <read_first>
    @examples/cairnloop_example/screenshots/README.md
    @examples/cairnloop_example/screenshots/package.json
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-CONTEXT.md
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-UI-SPEC.md
  </read_first>
  <action>Update the README to state that `npm run capture` produces explicit `guides/assets/light/NN-name.png` and `guides/assets/dark/NN-name.png` outputs, with any root-level light copies retained only for existing guide compatibility. Preserve the existing capture-only/non-gating wording and the `BASE_URL=http://localhost:4000 npm run capture` workflow. Add concise notes that Phase 45 acceptance covers operator/admin states only, static captures are motion-stabilized, behavior remains gated by ExUnit/integration/E2E, and dependency upgrades are outside this phase unless separately reviewed.</action>
  <acceptance_criteria>
    - Source assertion: README mentions `guides/assets/light/` and `guides/assets/dark/`.
    - Source assertion: README still says the tool is capture-only and non-gating.
    - Source assertion: README includes the `BASE_URL=http://localhost:4000 npm run capture` command.
    - Source assertion: README describes screenshots as evidence assets and tests as behavior gates.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'guides/assets/light|guides/assets/dark|capture-only|non-gating|BASE_URL=http://localhost:4000 npm run capture|behavior' examples/cairnloop_example/screenshots/README.md</automated>
  </verify>
  <done>Screenshot workflow docs explain the dual-theme evidence pack without turning screenshots into a release gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser -> example app | Browser automation loads operator/admin pages and can expose theme or seeded-state gaps. |
| screenshot script -> filesystem | Capture code writes generated images under `guides/assets/`. |
| local tooling -> dependency lockfiles | Screenshot workflow uses existing Node tooling and must not drift package ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05 | Spoofing | theme evidence | mitigate | Force both browser `colorScheme` and app `data-theme`/`phx:theme` state before capture per D-09. |
| T-45-06 | Repudiation | screenshot evidence | mitigate | Keep deterministic filenames and theme directories so Plan 45-03 can ledger every captured screen/theme pair. |
| T-45-07 | Information Disclosure | screenshots | mitigate | Scope accepted captures to operator/admin seeded proof and keep raw token material out of visible Settings state. |
| T-45-08 | Denial of Service | screenshot capture | accept | Local capture may be slow but remains non-gating evidence; behavior gates run separately. |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | No package or lockfile changes are planned; use existing Playwright workflow and stop before any dependency upgrade. |
</threat_model>

<verification>
Plan 45-02 verifies source-level dual-theme capture support. Plan 45-03 performs the actual seeded capture and visual ledger.
</verification>

<success_criteria>
VERIFY-01 is ready for capture when `capture.mjs` deterministically loops all required operator/admin shots through light and dark browser/app theme state and the README documents the evidence workflow.
</success_criteria>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| REQ | VERIFY-01 | Light and dark screenshots regenerated through Playwright pipeline | 45-02, 45-03 | COVERED | This plan prepares the pipeline; Plan 45-03 generates assets and ledger. |
| CONTEXT | D-07 | Evidence pack, not pixel gate | 45-02 | COVERED | Tasks preserve capture-only posture and no new visual gate. |
| CONTEXT | D-08 | Operator/admin states only | 45-02 | COVERED | Task 1 matrix is scoped to operator/admin. |
| CONTEXT | D-09 | Force Playwright and app theme state | 45-02 | COVERED | Task 1 sets both browser and app theme state. |
| CONTEXT | D-10 | Static captures motion-stabilized | 45-02 | COVERED | Task 1 preserves reduced motion and stabilization CSS. |
| RESEARCH | — | Use existing locked Playwright workflow, no upgrades | 45-02 | COVERED | Threat model and actions forbid dependency drift. |
| UI-SPEC | — | Theme directory layout, screenshot matrix, and empty-state proof | 45-02 | COVERED | Task 1 implements `guides/assets/{light,dark}` paths and `14-audit-empty-state.png`. |
</source_audit>

<output>
Create `.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-02-SUMMARY.md` when done.
</output>
