---
phase: 45-seed-enrichment-screenshot-regen-verification-sweep
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/priv/repo/seeds.exs
  - examples/cairnloop_example/test/cairnloop_example/seeds_test.exs
  - examples/cairnloop_example/lib/cairnloop_example/tools/high_risk_demo_action.ex
  - examples/cairnloop_example/config/config.exs
autonomous: true
requirements:
  - SEED-01
must_haves:
  truths:
    - "SEED-01: Running the example seed twice leaves stable row counts for conversations, governed approvals/events, ReviewTask states, articles/revisions, and MCP tokens."
    - "SEED-01: Operator screens have seeded rejected, deferred, pending, executed, and higher-risk approval-required governed-action evidence."
    - "SEED-01: Knowledge review proof uses ReviewTask statuses for rejected, deferred, approved-ready, and published states."
    - "SEED-01: Settings has one or two active MCP token rows whose visible handle is masked as cl_mcp_*** and never exposes raw token material."
    - "SEED-01/VERIFY-01: Seed output leaves the audit-search sentinel `phase45-empty-audit-filter` unmatched so Plan 45-03 captures a true operator empty state, not a hand-authored ledger row."
    - "CONTEXT D-01..D-06: Seed behavior is facade-first, deterministic, re-runnable, and direct DB updates are limited to passive presentation fields."
  artifacts:
    - path: "examples/cairnloop_example/priv/repo/seeds.exs"
      provides: "Phase 45 deterministic seed builders and passive timestamp/reason presentation tweaks"
      contains: "build_phase45_evidence_states"
    - path: "examples/cairnloop_example/test/cairnloop_example/seeds_test.exs"
      provides: "DB-backed seed coverage and idempotency contract"
      contains: "phase45_seed_contract"
    - path: "examples/cairnloop_example/lib/cairnloop_example/tools/high_risk_demo_action.ex"
      provides: "Example-app-only approval-required high-risk governed tool"
      exports: ["CairnloopExample.Tools.HighRiskDemoAction"]
    - path: "examples/cairnloop_example/config/config.exs"
      provides: "Example-app tool registry including the Phase 45 demo tool"
      contains: "CairnloopExample.Tools.HighRiskDemoAction"
  key_links:
    - from: "examples/cairnloop_example/priv/repo/seeds.exs"
      to: "lib/cairnloop/governance.ex"
      via: "Governance.propose/request_approval/reject/defer/approve for behaviorful governed-action state"
      pattern: "Governance\\.(propose|request_approval|reject|defer|approve)"
    - from: "examples/cairnloop_example/priv/repo/seeds.exs"
      to: "lib/cairnloop/knowledge_automation.ex"
      via: "ReviewTask facade transitions for KB review status proof"
      pattern: "KnowledgeAutomation\\.(ensure_review_task_for_suggestion|reject_review_task|defer_review_task|approve_review_task|publish_review_task)"
    - from: "examples/cairnloop_example/priv/repo/seeds.exs"
      to: "lib/cairnloop/mcp.ex"
      via: "MCP.issue_token/1 creates hashed token rows while raw values are discarded"
      pattern: "MCP\\.issue_token"
---

<objective>
Add the deterministic Phase 45 seed coverage required by SEED-01.

Purpose: Make the final-brand operator UI exercise happy, empty, error, dense, and boundary states without manual setup.
Output: Seed tests, an example-app-only high-risk demo tool, and facade-first seed helpers that create the missing governed-action, KB review, draft article, and masked MCP token evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-CONTEXT.md
@.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-RESEARCH.md
@.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-PATTERNS.md
@examples/cairnloop_example/priv/repo/seeds.exs
@examples/cairnloop_example/test/cairnloop_example/seeds_test.exs
@examples/cairnloop_example/config/config.exs
@lib/cairnloop/tools/internal_note.ex
@lib/cairnloop/tool.ex
@lib/cairnloop/governance.ex
@lib/cairnloop/knowledge_automation.ex
@lib/cairnloop/knowledge_automation/review_task.ex
@lib/cairnloop/knowledge_base.ex
@lib/cairnloop/mcp.ex
@lib/cairnloop/mcp/token.ex
@lib/cairnloop/web/settings_live.ex
</context>

## Artifacts This Phase Produces

| Kind | Name / Path | Created By |
|------|-------------|------------|
| Module | `CairnloopExample.Tools.HighRiskDemoAction` | Task 2 |
| Function | `CairnloopExample.Tools.HighRiskDemoAction.changeset/2` | Task 2 |
| Function | `CairnloopExample.Tools.HighRiskDemoAction.scope/0` | Task 2 |
| Function | `CairnloopExample.Tools.HighRiskDemoAction.authorize/2` | Task 2 |
| Function | `CairnloopExample.Tools.HighRiskDemoAction.run/3` | Task 2 |
| Seed helper | `build_phase45_evidence_states/0` | Task 3 |
| Seed helper | `ensure_phase45_review_task_state/3` | Task 3 |
| Seed helper | `ensure_phase45_governed_decision/4` | Task 3 |
| Seed helper | `ensure_phase45_demo_mcp_token/1` | Task 3 |
| Seed helper | `ensure_phase45_draft_article/0` | Task 3 |
| Seed helper | `backdate_phase45_audit_events!/0` | Task 3 |
| Test tag | `:phase45_seed_contract` | Task 1 |
| New file | `examples/cairnloop_example/lib/cairnloop_example/tools/high_risk_demo_action.ex` | Task 2 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Phase 45 seed contract tests</name>
  <files>examples/cairnloop_example/test/cairnloop_example/seeds_test.exs</files>
  <read_first>
    @examples/cairnloop_example/test/cairnloop_example/seeds_test.exs
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-CONTEXT.md
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-PATTERNS.md
    @lib/cairnloop/knowledge_automation/review_task.ex
    @lib/cairnloop/mcp/token.ex
    @lib/cairnloop/governance.ex
  </read_first>
  <behavior>
    - Running the seed exposes rejected and deferred governed-action approvals/events with human-readable reasons per D-02.
    - Running the seed exposes ReviewTask statuses `:rejected`, `:deferred`, `:approved_ready_to_publish`, and `:published` per D-03.
    - Running the seed creates one draft article with an unpublished draft revision per D-02.
    - Running the seed creates active MCP token rows named `Demo docs integration` and `Demo audit export`, while assertions only check masked UI/source-safe handles per D-04.
    - Running the seed creates an approval-required higher-risk demo proposal whose tool module is under the example app per D-05.
    - Running the seed leaves no ToolActionEvent actor, action label, reason, or metadata value matching `phase45-empty-audit-filter`, so the audit-log search shot can render the real `.cl_empty` state `No audit events found`.
    - Running the seed twice keeps row counts stable, including tokens, ReviewTasks, approvals, action events, articles, and revisions per D-01.
  </behavior>
  <action>Add `@tag :phase45_seed_contract` tests before implementation. Extend aliases/imports only as needed for `ToolApproval`, `ToolActionEvent`, `ReviewTask`, `Cairnloop.MCP.Token`, `Article`, and `Revision`. Add one coverage test and extend the existing idempotency count map. Include a seed-backed empty-state assertion that no governed audit event text/metadata contains the sentinel `phase45-empty-audit-filter`; Plan 45-02 uses that sentinel to drive the audit log into the visible `No audit events found` empty state. The tests must cite SEED-01 and D-01 through D-05 in comments or assertion messages for traceability, and they must fail against the current seed before Tasks 2-3 are implemented.</action>
  <acceptance_criteria>
    - Source assertion: `seeds_test.exs` contains `@tag :phase45_seed_contract`.
    - Source assertion: tests assert the exact ReviewTask status atoms `:rejected`, `:deferred`, `:approved_ready_to_publish`, and `:published`.
    - Source assertion: tests reference `Demo docs integration`, `Demo audit export`, and `cl_mcp_***`, but do not assert any raw token value.
    - Source assertion: tests reference `phase45-empty-audit-filter` and prove it matches zero seeded audit events before screenshot capture.
    - Behavior assertion: the focused test command fails before seed implementation and passes after Task 3.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/cairnloop_example && PGPORT=5432 MIX_ENV=test mix test test/cairnloop_example/seeds_test.exs --only phase45_seed_contract</automated>
  </verify>
  <done>The Phase 45 seed tests define all missing SEED-01 evidence and fail for real missing seed behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the example-only high-risk governed demo tool</name>
  <files>examples/cairnloop_example/lib/cairnloop_example/tools/high_risk_demo_action.ex, examples/cairnloop_example/config/config.exs</files>
  <read_first>
    @lib/cairnloop/tools/internal_note.ex
    @lib/cairnloop/tool.ex
    @examples/cairnloop_example/config/config.exs
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-CONTEXT.md
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-PATTERNS.md
  </read_first>
  <behavior>
    - The demo tool uses `Cairnloop.Tool` with `risk_tier: :high_write`, so approval mode derives as approval-required per D-05.
    - The module lives only in the example app namespace and is registered only in `examples/cairnloop_example/config/config.exs`.
    - `run/3` has a benign no-op or single harmless demo result and never performs a destructive side effect.
  </behavior>
  <action>Create `CairnloopExample.Tools.HighRiskDemoAction` by mirroring the `InternalNote` behavior shape: `use Ecto.Schema`, `use Cairnloop.Tool` with title `High-risk demo action`, description `Shows an approval-required boundary state without performing a destructive side effect.`, `risk_tier: :high_write`, embedded fields `conversation_id` and `reason`, required changeset validation, `scope/0`, `authorize/2`, and `run/3`. Register the module beside `Cairnloop.Tools.InternalNote` in the example app `tools:` list only. Do not add a library-root module, public API, new route, migration, package, or behavior outside the example app per D-05.</action>
  <acceptance_criteria>
    - Source assertion: the new module path is `examples/cairnloop_example/lib/cairnloop_example/tools/high_risk_demo_action.ex`.
    - Source assertion: the module uses `risk_tier: :high_write` and title `High-risk demo action`.
    - Source assertion: root `lib/cairnloop/**` has no `HighRiskDemoAction` module.
    - Source assertion: `examples/cairnloop_example/config/config.exs` registers both `Cairnloop.Tools.InternalNote` and `CairnloopExample.Tools.HighRiskDemoAction`.
    - Behavior assertion: example-app compilation is warnings-clean.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/cairnloop_example && mix compile --warnings-as-errors</automated>
  </verify>
  <done>The higher-risk tool exists only as example-app evidence and can be proposed through the existing Governance facade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement facade-first Phase 45 seed enrichment</name>
  <files>examples/cairnloop_example/priv/repo/seeds.exs, examples/cairnloop_example/test/cairnloop_example/seeds_test.exs</files>
  <read_first>
    @examples/cairnloop_example/priv/repo/seeds.exs
    @examples/cairnloop_example/test/cairnloop_example/seeds_test.exs
    @lib/cairnloop/governance.ex
    @lib/cairnloop/knowledge_automation.ex
    @lib/cairnloop/knowledge_base.ex
    @lib/cairnloop/mcp.ex
    @lib/cairnloop/web/settings_live.ex
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-CONTEXT.md
    @.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-PATTERNS.md
  </read_first>
  <behavior>
    - `build_phase45_evidence_states/0` is called from `run/0` after existing base articles/conversations/suggestion setup so required subjects exist.
    - Governed-action rejected/deferred/pending/executed evidence is created through `Cairnloop.Governance` transitions, with direct updates limited to timestamp/order presentation per D-01, D-02, and D-06.
    - KB review states are created by suggestions plus ReviewTask facade transitions, not by adding statuses to ArticleSuggestion per D-03.
    - MCP tokens are created through `Cairnloop.MCP.issue_token/1`, raw values are bound to ignored variables and never printed per D-04.
    - A draft article remains draft with an unpublished draft revision through `KnowledgeBase.save_draft/2` per D-02.
  </behavior>
  <action>Add grouped helpers before `get_or_insert!/3`: `build_phase45_evidence_states/0`, `ensure_phase45_review_task_state/3`, `ensure_phase45_governed_decision/4`, `ensure_phase45_demo_mcp_token/1`, `ensure_phase45_draft_article/0`, and `backdate_phase45_audit_events!/0`. Use stable natural keys and subject strings such as `[demo-phase45] governed rejection`, `[demo-phase45] governed deferral`, and `demo:article_suggestion:phase45:*`. Use public facade calls first: `Governance.propose/3`, `Governance.request_approval/2`, `Governance.reject/3`, `Governance.defer/3`, `KnowledgeAutomation.ensure_review_task_for_suggestion/2`, `reject_review_task/2`, `defer_review_task/2`, `approve_review_task/2`, `publish_review_task/2`, `KnowledgeBase.save_draft/2`, and `MCP.issue_token/1`. Add short comments only where direct `Repo.update_all` or `Ecto.Changeset.change` touches passive timestamps/reason display for screenshot chronology per D-06. Keep all fixture copy calm and user-facing, using the UI-SPEC reason strings where visible.</action>
  <acceptance_criteria>
    - Source assertion: `seeds.exs` contains `build_phase45_evidence_states()` inside `run/0`.
    - Source assertion: no new `ArticleSuggestion` status atom is introduced for rejected/deferred/published states.
    - Source assertion: MCP raw token bindings are ignored and never passed to `IO.puts`, `inspect`, assertions, or ledger files.
    - Behavior assertion: seed tests pass with stable counts after two runs.
    - Behavior assertion: the final seed data contains varied audit timestamps across roughly 14 days and human-readable reasons for rejected/deferred governed events.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/cairnloop_example && PGPORT=5432 MIX_ENV=test mix test test/cairnloop_example/seeds_test.exs</automated>
  </verify>
  <done>SEED-01 is satisfied through deterministic facade-first seed data and DB-backed tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script -> database | Deterministic local seed code writes persisted demo state used by operator/admin UI proof. |
| seed script -> Governance/Knowledge/MCP facades | Seed builders cross behaviorful domain boundaries that emit audit events, hash tokens, enqueue jobs, and validate state transitions. |
| example app -> library config | Example-only tool registration must not widen the public library API or root package surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | `examples/cairnloop_example/priv/repo/seeds.exs` | mitigate | Use behaviorful facades for Governance, KnowledgeAutomation, KnowledgeBase, and MCP state; direct DB writes are limited to passive timestamp/order presentation per D-06. |
| T-45-02 | Repudiation | governed-action seed evidence | mitigate | Create rejected/deferred/approved/executed states through Governance transitions so ToolActionEvent audit history exists. |
| T-45-03 | Information Disclosure | MCP seed tokens | mitigate | Use `MCP.issue_token/1`, discard raw token values immediately, assert only masked `cl_mcp_***` display. |
| T-45-04 | Elevation of Privilege | high-risk demo tool registration | mitigate | Keep `CairnloopExample.Tools.HighRiskDemoAction` under the example app and register it only in example config; no root library API or destructive effect. |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | No package install or dependency upgrade is planned; if implementation attempts one, stop for package legitimacy review before changing lockfiles. |
</threat_model>

<verification>
Run focused seed contract tests during this plan and leave the full milestone sweep to Plan 45-04.
</verification>

<success_criteria>
SEED-01 is complete when the example seed reruns idempotently and DB tests prove governed decisions, ReviewTask states, draft article state, masked MCP tokens, and the example-only higher-risk governed tool proposal.
</success_criteria>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| REQ | SEED-01 | Enriched demo seed covers audit events, KB states, drafts, MCP tokens, and higher-risk governed action evidence | 45-01 | COVERED | Tasks 1-3 implement and test SEED-01. |
| CONTEXT | D-01 | Existing seed script with facade-first incremental builders | 45-01 | COVERED | Task 3 adds grouped helpers in `seeds.exs`. |
| CONTEXT | D-02 | Fill only missing state coverage | 45-01 | COVERED | Task 3 targets only rejected/deferred/published KB, draft, MCP, audit chronology, and higher-risk action gaps. |
| CONTEXT | D-03 | ReviewTask owns knowledge suggestion states | 45-01 | COVERED | Tasks 1 and 3 assert and implement ReviewTask statuses. |
| CONTEXT | D-04 | MCP tokens through MCP facade and masked display | 45-01 | COVERED | Tasks 1 and 3 assert issue-token use and masked handles. |
| CONTEXT | D-05 | Higher-risk tool stays example-app-only | 45-01 | COVERED | Task 2 creates and registers only the example-app module. |
| CONTEXT | D-06 | Direct DB writes only for passive seed presentation | 45-01 | COVERED | Task 3 limits direct updates to timestamps/order/reasons with comments. |
| RESEARCH | — | No new packages or fixture DSL | 45-01 | COVERED | Plan uses existing seed entry point and example app. |
</source_audit>

<output>
Create `.planning/phases/45-seed-enrichment-screenshot-regen-verification-sweep/45-01-SUMMARY.md` when done.
</output>
