---
phase: 42-cross-screen-threading
plan: 03
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - lib/cairnloop/web/audit_log_live.ex
  - test/cairnloop/web/audit_log_live_test.exs
autonomous: true
requirements: [THREAD-02, THREAD-03]
must_haves:
  truths:
    - "Every audit row with a resolved conversation_id renders a scope-relative 'View conversation' link to /#{id}"
    - "An audit row whose proposal has no conversation degrades to plain text — never a broken link"
    - "Visiting /audit-log?proposal=<id> filters the log to that proposal's trail"
    - "handle_params/2 tolerates a missing or malformed proposal param without crashing (fail-closed, full honest view)"
  artifacts:
    - path: "lib/cairnloop/web/audit_log_live.ex"
      provides: "handle_params/2 proposal filter + per-row subject link"
      contains: "def handle_params"
  key_links:
    - from: "audit_log_live.ex row template"
      to: "AuditLogPresenter.subject_href/1 → /#{conversation_id}"
      via: "<.link navigate> with explicit accessible name"
      pattern: "subject_href"
    - from: "audit_log_live.ex handle_params/2"
      to: "load_events → list_action_events(proposal_id:)"
      via: "parsed ?proposal param threaded into the auditor read"
      pattern: "proposal"
---

<objective>
Wire the Audit Log web layer: each row links to its subject conversation (THREAD-02), and a
`?proposal=<id>` deep-link filters the log to one governed action's trail (THREAD-03a). Builds
directly on Plan 01's enriched auditor map, `proposal_id:` facade opt, and `subject_href/1`.

Purpose: turn the audit log from a dead-end leaf into a navigable surface, and make a governed
action's audit trail deep-linkable/shareable. `AuditLogLive` has NO `handle_params/2` today and
the read had no `proposal_id` filter — both are net-new here, sitting on Plan 01's backing reads.

Output: `handle_params/2` (tolerant proposal filter) + a per-row scope-relative subject link with
an explicit accessible name + fail-closed plain-text fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-cross-screen-threading/42-CONTEXT.md
@.planning/phases/42-cross-screen-threading/42-RESEARCH.md
@.planning/phases/42-cross-screen-threading/42-PATTERNS.md
@.planning/phases/42-cross-screen-threading/42-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (consumed here) + the live AuditLogLive shape (verified this session). -->

From Plan 01:
- Auditor.Governance.list_events/1 map now includes conversation_id: + proposal_id:.
- Governance.list_action_events/1 accepts proposal_id: opt.
- AuditLogPresenter.subject_href/1 → "/#{conversation_id}" | nil.

Current AuditLogLive (lib/cairnloop/web/audit_log_live.ex) — has mount/3, handle_event/3
("search"/"filter"/"load_more"), defp load_events/1, defp recompute/1. NO handle_params/2 today.
load_events/1:
```elixir
defp load_events(socket) do
  auditor = Application.get_env(:cairnloop, :auditor, Cairnloop.Auditor.Governance)
  events = auditor.list_events(limit: socket.assigns.limit)
  socket |> assign(events: events, maybe_more?: length(events) >= socket.assigns.limit) |> recompute()
end
```
Row template (audit_log_live.ex:133): `<tr :for={event <- @visible_events}>` with
`P.timestamp_label`/`P.actor_label`/`P.action_label`/`P.reason_label`/`P.metadata_rows` cells.
Inbox a11y idiom to mirror (inbox_live.ex:215-216): `<.link navigate={"/#{conv.id}"}` +
`aria-label={"Select conversation: ..."}`. Scope-relative ONLY (Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: handle_params/2 proposal filter threaded through load_events</name>
  <files>lib/cairnloop/web/audit_log_live.ex, test/cairnloop/web/audit_log_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/audit_log_live.ex:22-70 (mount/3, handle_event/3, load_events/1, recompute/1 — the assign→load flow to mirror)
    - 42-PATTERNS.md §"lib/cairnloop/web/audit_log_live.ex" + §"No Analog Found" (handle_params/2 shell is net-new; mirror mount→assign→load_events for the assign/load half)
    - 42-RESEARCH.md Pattern 3 + Security Domain (Integer.parse tolerant fallback; pin with ^id; never interpolate; scoped read, no Repo)
    - 42-PATTERNS.md §"Tolerant param parsing"
  </read_first>
  <behavior>
    - handle_params(%{"proposal" => raw}, _, socket): parses raw with Integer.parse/1; valid → assign proposal_filter: id and load filtered events; invalid/garbage → proposal_filter: nil, full honest view (no crash).
    - handle_params(_, _, socket) catch-all: proposal_filter: nil, full view.
    - load_events threads proposal_filter into the auditor read via list_action_events(proposal_id:) (or the auditor opt) — only when non-nil.
    - Existing events/visible_events assign shape and recompute/1 are unchanged (D-09).
  </behavior>
  <action>
    Add two `handle_params/2` clauses to `Cairnloop.Web.AuditLogLive` (net-new — none exist today):
    a `%{"proposal" => raw}` clause that parses `raw` with `Integer.parse/1` returning the integer or
    nil (tolerant — V5; NEVER string-interpolate, Pitfall: param tampering), assigns `proposal_filter`,
    and pipes into `load_events/1`; and a catch-all clause that assigns `proposal_filter: nil` and
    loads (D-10). Resolve Claude's-discretion: query param name is `proposal` (not `proposal_id`),
    assign key is `proposal_filter`. Add `proposal_filter: nil` to the `mount/3` assigns so the assign
    always exists. Modify `load_events/1` to pass `proposal_id: socket.assigns.proposal_filter` into
    the auditor/`list_action_events/1` read — only including the opt when non-nil so the unfiltered
    path is byte-identical to today. Keep the `events`/`visible_events`/`maybe_more?` assign shape and
    `recompute/1` untouched (D-09). No `Cairnloop.Repo` in the LiveView (criterion-4 / D-03).

    In `test/cairnloop/web/audit_log_live_test.exs`, extend the headless render suite:
    handle_params with proposal="bogus" renders without crashing (full view); handle_params with a
    valid id assigns the filter; missing param → full view. Use the base_assigns/render idiom (no Repo).
  </action>
  <verify>
    <automated>grep -q 'def handle_params' lib/cairnloop/web/audit_log_live.ex && grep -q 'Integer.parse' lib/cairnloop/web/audit_log_live.ex && grep -Lq 'Cairnloop.Repo' lib/cairnloop/web/audit_log_live.ex; ! grep -q 'Cairnloop.Repo\.' lib/cairnloop/web/audit_log_live.ex && mix compile --warnings-as-errors && mix test test/cairnloop/web/audit_log_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `audit_log_live.ex` defines `def handle_params(` with a `%{"proposal" => ...}` clause AND a catch-all clause.
    - The proposal param is parsed with `Integer.parse/1` (tolerant) — no direct string interpolation into a query.
    - `grep 'Cairnloop.Repo\.' lib/cairnloop/web/audit_log_live.ex` returns no matches (criterion-4: no raw Repo in the LiveView).
    - `mix test test/cairnloop/web/audit_log_live_test.exs` exits 0, including a `proposal="bogus"` no-crash case.
  </acceptance_criteria>
  <done>handle_params/2 tolerantly reads ?proposal, threads a parameterized filter through load_events, preserves the assign shape, and never crashes on bad input; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-row subject link with accessible name + fail-closed fallback</name>
  <files>lib/cairnloop/web/audit_log_live.ex, test/cairnloop/web/audit_log_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/audit_log_live.ex:128-160 (the `<tr :for={event <- @visible_events}>` table body to extend)
    - lib/cairnloop/web/inbox_live.ex:212-216 (the canonical row link + aria-label a11y idiom to mirror)
    - 42-PATTERNS.md §"lib/cairnloop/web/audit_log_live.ex — Per-row subject link" + §"Scope-root-relative declarative nav"
    - 42-RESEARCH.md Pitfall 3 (scope-relative), Pitfall 4 (no dead link on absence), Pitfall 6 (a11y accessible name); D-08, D-14
  </read_first>
  <behavior>
    - For an event whose subject_href/1 is non-nil, the row renders an explicit `<.link navigate={href}>` with a discernible accessible name (visible text "View conversation" and/or aria-label) to a scope-relative /#{id} path.
    - For an event whose subject_href/1 is nil, the row renders plain text (an em-dash or muted label) — NO link, no broken/empty navigate.
    - The link is NEVER mount-prefixed (no /support/...).
  </behavior>
  <action>
    Add a subject cell (new column or an addition to an existing cell) to the audit table body row at
    `audit_log_live.ex` (`<tr :for={event <- @visible_events}>`). Compute the href via
    `P.subject_href(event)` (Plan 01). Resolve Claude's-discretion in favor of an EXPLICIT link
    element (not whole-row-as-link) per RESEARCH Open Q2 / Pitfall 6: when href is non-nil render
    `<.link navigate={href}>View conversation</.link>` with an `aria-label` mirroring the inbox idiom
    (inbox_live.ex:216); when href is nil render a plain muted cell (em-dash) — fail-closed, never a
    broken link (D-08, Pitfall 4). The path is scope-root-relative `/#{id}` (it comes from
    subject_href/1) — verify no mount prefix leaks (Pitfall 3). Add a `<th>` header for the new column.
    Status/affordance conveyed by text, never color alone (brand §7.5).

    In `test/cairnloop/web/audit_log_live_test.exs`, extend the headless render suite: an event with
    `conversation_id: 5` renders a `navigate="/5"` link with the "View conversation" accessible name and
    NO `/support` prefix; an event with `conversation_id: nil` renders no link (plain cell). Use the
    base_assigns/render idiom with built `visible_events` (no Repo).
  </action>
  <verify>
    <automated>grep -q 'subject_href' lib/cairnloop/web/audit_log_live.ex && ! grep -q '/support/' lib/cairnloop/web/audit_log_live.ex && mix compile --warnings-as-errors && mix test test/cairnloop/web/audit_log_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The audit row uses `P.subject_href(event)` and renders `<.link navigate=` only when href is non-nil.
    - A built event with `conversation_id: 5` produces HTML containing `navigate="/5"` and the accessible name "View conversation" (test asserts).
    - A built event with `conversation_id: nil` produces a row with NO `<.link navigate` for the subject cell (test asserts).
    - `grep '/support/' lib/cairnloop/web/audit_log_live.ex` returns no matches (scope-relative only).
    - `mix test test/cairnloop/web/audit_log_live_test.exs` exits 0.
  </acceptance_criteria>
  <done>Audit rows link to their subject conversation with an accessible name, degrade to plain text on absence, and use scope-relative paths only; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → AuditLogLive | The `?proposal=<id>` query param is the one new external input this phase introduces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | Tampering | `?proposal=<id>` param | mitigate | Integer.parse/1 tolerant fallback to nil; pinned with ^id via the facade read; bad input → full honest view, never a crash |
| T-42-08 | Information Disclosure (IDOR) | operator forging a foreign/garbage proposal id | mitigate | Read routes through the operator-scoped facade (Plan 01); a non-matching id yields an empty/honest filtered view, never a cross-tenant leak |
| T-42-09 | Information Disclosure | broken/`/nil` link to a non-existent subject | mitigate | subject_href/1 returns nil → plain-text cell (fail-closed, D-08); no `navigate=` with nil/empty segment (Pitfall 4) |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | None — Phase 42 installs no packages (RESEARCH: Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` passes.
- `mix test test/cairnloop/web/audit_log_live_test.exs` exits 0.
- `grep 'Cairnloop.Repo\.' lib/cairnloop/web/audit_log_live.ex` → no matches (criterion-4).
- `grep '/support/' lib/cairnloop/web/audit_log_live.ex` → no matches (scope-relative).
- Manual: visiting /audit-log?proposal=<id> shows only that proposal's events; /audit-log?proposal=bogus shows the full log without error.
</verification>

<success_criteria>
- Every audit row links to its subject conversation (or degrades to plain text) — the audit log is no longer a dead end (THREAD-02).
- /audit-log?proposal=<id> filters to that action's trail; the param handler is total (THREAD-03a).
- No raw Cairnloop.Repo in the LiveView; scope-relative links only; build warnings-clean; tests green.
</success_criteria>

<output>
Create `.planning/phases/42-cross-screen-threading/42-03-SUMMARY.md` when done.
</output>
