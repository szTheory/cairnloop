---
phase: 42-cross-screen-threading
plan: 04
type: execute
wave: 2
depends_on: ["42-02"]
files_modified:
  - lib/cairnloop/web/conversation_live.ex
  - test/cairnloop/web/conversation_live_test.exs
autonomous: true
requirements: [THREAD-01, THREAD-03]
must_haves:
  truths:
    - "When a conversation is resolved, a calm 'Next in queue' affordance links to the next open conversation"
    - "When the queue is clear (next id is nil), a calm 'Queue clear' state links to /inbox — never a dead/disabled Next or a stale-id navigate"
    - "A governed-action card's Tier-3 trace group links to /audit-log?proposal=<id> for that action's audit trail"
    - "All threading links are scope-root-relative and declarative (<.link navigate>)"
  artifacts:
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "Next-in-queue affordance + Queue-clear state in resolved region; audit deep-link in trace group"
      contains: "Next in queue"
  key_links:
    - from: "conversation_live.ex reload_conversation_with_context/2"
      to: "Chat.next_open_conversation/1 → @next_open_id assign"
      via: "render-time scoped read threaded into the resolved-region affordance"
      pattern: "next_open_conversation"
    - from: "conversation_live.ex Tier-3 trace group"
      to: "/audit-log?proposal=#{@trace.proposal_id}"
      via: "<.link navigate> deep-link"
      pattern: "audit-log\\?proposal"
---

<objective>
Wire the conversation-rail web layer: a "Next in queue" affordance in the resolved-state region
(THREAD-01) and an audit-trail deep-link in the governed-action card's Tier-3 trace group
(THREAD-03a). Builds on Plan 02's `Chat.next_open_conversation/1`. Both edits live in
`conversation_live.ex`, so they ship together (single file owner).

Purpose: from a resolved conversation, the operator advances to the next open item without
bouncing through the inbox (D-04/D-05); from a governed-action card they jump straight to that
action's audit trail (D-10). Both reuse existing rail regions — no new affordance machinery.

Output: Next-in-queue + Queue-clear states keyed on `@conversation.status == :resolved`, and a
"View audit trail" deep-link in the Tier-3 "Identifiers & trace" group.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-cross-screen-threading/42-CONTEXT.md
@.planning/phases/42-cross-screen-threading/42-RESEARCH.md
@.planning/phases/42-cross-screen-threading/42-PATTERNS.md
@.planning/phases/42-cross-screen-threading/42-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (consumed here) + the live conversation_live.ex shape (verified this session). -->

From Plan 02: Chat.next_open_conversation(current_id) → next-open conversation id | nil.

conversation_live.ex mount/3 (:16) calls reload_conversation_with_context/2 to set the
@conversation assign. The resolved region is the function component outbound_recovery_card/1
(:531), invoked at :480 as `<.outbound_recovery_card conversation={@conversation} />`, gated on
`@conversation.status == :resolved`. Status enum [:open, :resolved, :archived].

Tier-3 trace group (conversation_live.ex:1084-1092) — `@trace.proposal_id` is in scope:
```elixir
<.cl_disclosure id={"ga-#{@proposal.id}-trace"}>
  <:summary>Identifiers &amp; trace</:summary>
  <.cl_fact_list facts={[%{label: "Proposal", value: "##{@trace.proposal_id}"}, ...]} />
</.cl_disclosure>
```
Scope-relative nav idioms to mirror: back-link `<.link navigate="/inbox">` (:438); inbox row
`<.link navigate={"/#{conv.id}"}>` (inbox_live.ex:215). NEVER mount-prefixed (Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Next-in-queue affordance + Queue-clear state in the resolved region</name>
  <files>lib/cairnloop/web/conversation_live.ex, test/cairnloop/web/conversation_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex:16-30 (mount/3), the reload_conversation_with_context/2 helper (sets @conversation), and :475-485 (the outbound_recovery_card call site)
    - lib/cairnloop/web/conversation_live.ex:529-548 (outbound_recovery_card/1 — the resolved-state component to attach to)
    - lib/cairnloop/web/conversation_live.ex:438 (back-to-inbox scope-relative nav idiom)
    - 42-PATTERNS.md §"conversation_live.ex — Next-in-queue in resolved region" (exact placement + Queue-clear excerpt)
    - 42-RESEARCH.md "Affordance placement in the resolved region" code example; D-04, D-05, D-06, D-14
  </read_first>
  <behavior>
    - When @conversation.status == :resolved and @next_open_id is an id → render a calm "Next in queue" `<.link navigate={"/#{id}"}>` (cl-button styled).
    - When resolved and @next_open_id is nil → render a calm "Queue clear — no more open conversations" state with a `<.link navigate="/inbox">` back link.
    - The affordance never renders a disabled/dead "Next" and never navigates to a stale id (D-06).
    - PubSub reload (re-render) recomputes @next_open_id and re-renders the correct state.
  </behavior>
  <action>
    Compute `next_open_id` via `Chat.next_open_conversation(@conversation.id)` and assign it as
    `@next_open_id` inside `reload_conversation_with_context/2` (alongside the existing @conversation
    assign) so it is recomputed on every PubSub reload (Claude's-discretion resolved: assign-based,
    set in the reload helper). Pass it into `outbound_recovery_card/1` by adding a
    `next_open_id` attr to that component and updating the call site (:480) to
    `<.outbound_recovery_card conversation={@conversation} next_open_id={@next_open_id} />`. Inside the
    component's `@conversation.status == :resolved` block, add a `case @next_open_id` (D-06):
    nil → a calm "Queue clear — no more open conversations" line + `<.link navigate="/inbox">`
    back-to-inbox; id → `<.link navigate={"/#{id}"}>Next in queue &rarr;</.link>` (cl-button class).
    Copy is calm, reason-forward, brand §7.5 — text+tone, never color alone. Links are
    scope-root-relative declarative `navigate` (D-14, Pitfall 3) — NEVER `/support/...`. Do not add
    a "mark resolved" action (deferred — D-05 keys off the existing resolved state).

    In `test/cairnloop/web/conversation_live_test.exs`, add headless render cases (built assigns,
    no Repo): resolved + next_open_id=42 renders `navigate="/42"` "Next in queue"; resolved +
    next_open_id=nil renders the "Queue clear" copy + `navigate="/inbox"`; non-resolved status renders
    neither. Mirror the audit_log_live_test base_assigns/render idiom.
  </action>
  <verify>
    <automated>grep -q 'next_open_conversation' lib/cairnloop/web/conversation_live.ex && grep -q 'Next in queue' lib/cairnloop/web/conversation_live.ex && grep -q 'Queue clear' lib/cairnloop/web/conversation_live.ex && ! grep -q '/support/' lib/cairnloop/web/conversation_live.ex && mix compile --warnings-as-errors && mix test test/cairnloop/web/conversation_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `@next_open_id` is assigned via `Chat.next_open_conversation(@conversation.id)` in the reload helper (grep + read).
    - `outbound_recovery_card/1` takes a `next_open_id` attr and the call site passes it.
    - Resolved + next_open_id=42 render contains `navigate="/42"` and "Next in queue" (test asserts; no `/support` prefix).
    - Resolved + next_open_id=nil render contains "Queue clear" copy and `navigate="/inbox"` (test asserts) — no disabled/dead Next.
    - `mix test test/cairnloop/web/conversation_live_test.exs` exits 0; `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Resolved conversations show Next-in-queue (or a calm Queue-clear state) via scope-relative declarative nav, recomputed on reload; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit-trail deep-link in the governed-action Tier-3 trace group</name>
  <files>lib/cairnloop/web/conversation_live.ex, test/cairnloop/web/conversation_live_test.exs</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex:1082-1095 (the Tier-3 "Identifiers & trace" cl_disclosure group where @trace.proposal_id is in scope)
    - 42-PATTERNS.md §"conversation_live.ex — audit deep-link in Tier-3 trace group" (exact placement + link form)
    - 42-RESEARCH.md THREAD-03a; D-10 (link to the action's filtered trail via ?proposal=<id>), D-14, Pitfall 3
  </read_first>
  <behavior>
    - The Tier-3 trace group renders a `<.link navigate={"/audit-log?proposal=#{@trace.proposal_id}"}>View audit trail</.link>` with explicit link text.
    - The link is scope-root-relative (/audit-log?proposal=...), never mount-prefixed.
    - It is declarative navigate (not push_navigate / push_patch).
  </behavior>
  <action>
    In the governed-action card's Tier-3 "Identifiers & trace" `cl_disclosure` group
    (conversation_live.ex:1084-1092), add a `<.link navigate={"/audit-log?proposal=#{@trace.proposal_id}"}>View audit trail</.link>`
    inside or immediately after the `cl_fact_list` (D-10 — link to the action's full audit trail, not
    one event row; placement in the Tier-3 trace group per Claude's-discretion / P41 D-02). Scope-root-
    relative path (`/audit-log?proposal=...`), declarative `navigate` (D-14), explicit link text for
    the accessible name (brand §7.5 — never color alone, mono only for the raw id). Do not introduce a
    signed return-token (D-11 — plain declarative nav). No raw Cairnloop.Repo.

    In `test/cairnloop/web/conversation_live_test.exs`, add a headless render case (built assigns with
    a `@trace` carrying a proposal_id, no Repo) asserting the rendered HTML contains
    `navigate="/audit-log?proposal=<id>"` and "View audit trail", with no `/support` prefix. Render the
    governed-action card component directly if the trace group is in a separate function component.
  </action>
  <verify>
    <automated>grep -q 'audit-log?proposal=' lib/cairnloop/web/conversation_live.ex && grep -q 'View audit trail' lib/cairnloop/web/conversation_live.ex && ! grep -q '/support/' lib/cairnloop/web/conversation_live.ex && mix compile --warnings-as-errors && mix test test/cairnloop/web/conversation_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The Tier-3 trace group renders `navigate="/audit-log?proposal=#{@trace.proposal_id}"` with the text "View audit trail" (grep + test).
    - The path is scope-relative (`/audit-log?...`) — `grep '/support/'` returns no matches.
    - Uses `<.link navigate=` (declarative), not `push_navigate`/`push_patch`.
    - `mix test test/cairnloop/web/conversation_live_test.exs` exits 0; `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The governed-action card links to its audit trail via a scope-relative declarative deep-link with an accessible name; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render-time read → rail | next_open_conversation/1 resolves a navigational id at render time — a structural read, not a trust-fact re-read (D-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-10 | Information Disclosure | Next link navigating to a stale/foreign conversation id | mitigate | next_open_conversation/1 is operator-scoped (Plan 02) and recomputed each reload; nil → Queue-clear state, never a stale-id navigate (D-06) |
| T-42-11 | Information Disclosure | dead/`/nil` link when queue empty | mitigate | case @next_open_id branches: nil → calm Queue-clear + /inbox, never a disabled/dead Next (Pitfall 4) |
| T-42-12 | Tampering | mount-prefixed link breaking under host mount | mitigate | Scope-root-relative paths only (`/#{id}`, `/inbox`, `/audit-log?...`); verified by grep gate (Pitfall 3) |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` passes.
- `mix test test/cairnloop/web/conversation_live_test.exs` exits 0.
- `grep '/support/' lib/cairnloop/web/conversation_live.ex` → no matches (scope-relative).
- `grep 'Cairnloop.Repo\.' lib/cairnloop/web/conversation_live.ex` → no new direct Repo use (criterion-4).
- Manual: a resolved conversation shows "Next in queue" (or "Queue clear"); a governed-action card's trace group shows "View audit trail".
</verification>

<success_criteria>
- After resolving, a Next-in-queue affordance advances to the next open conversation without returning to the inbox; end-of-queue is calm and honest (THREAD-01).
- A governed-action card links to its audit trail (THREAD-03a).
- All links scope-relative + declarative; no raw Repo; build warnings-clean; tests green.
</success_criteria>

<output>
Create `.planning/phases/42-cross-screen-threading/42-04-SUMMARY.md` when done.
</output>
