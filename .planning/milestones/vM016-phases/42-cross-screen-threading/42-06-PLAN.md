---
phase: 42-cross-screen-threading
plan: 06
type: execute
wave: 3
depends_on: ["42-03", "42-04", "42-05"]
files_modified:
  - examples/cairnloop_example/test/e2e/thread_navigation_test.exs
autonomous: true
requirements: [THREAD-01, THREAD-02, THREAD-03]
must_haves:
  truths:
    - "A real browser navigating from a resolved conversation's 'Next in queue' link lands on the next open conversation (not the inbox)"
    - "Clicking an audit-row subject link lands on the subject conversation"
    - "Clicking a governed-action card's 'View audit trail' link lands on /audit-log filtered to that proposal"
    - "Clicking a conversation-originated article's origin crumb lands on the originating conversation"
  artifacts:
    - path: "examples/cairnloop_example/test/e2e/thread_navigation_test.exs"
      provides: "real-browser navigation proof for all four threads"
      contains: "@moduletag :e2e"
  key_links:
    - from: "thread_navigation_test.exs"
      to: "the four threading links rendered by Plans 03/04/05"
      via: "PhoenixTest.Playwright visit + click + URL/landing assertions"
      pattern: "visit"
---

<objective>
Prove the four threading links actually transition in a real browser (the one blind spot
`Phoenix.LiveViewTest` cannot cover — it runs no JS). Adds a single E2E spec mirroring the
existing `rail_disclosure_test.exs` harness, exercising all four threads end to end.

Purpose: headless render tests (Plans 03/04/05) assert the link markup, param tolerance, and
fail-closed absence; only the live JS `<.link navigate>` transition needs a browser. This spec is
the automated proof of THREAD-01/02/03 navigation, gated in the CI `e2e` lane.

Output: `examples/cairnloop_example/test/e2e/thread_navigation_test.exs` covering all four threads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-cross-screen-threading/42-RESEARCH.md
@.planning/phases/42-cross-screen-threading/42-PATTERNS.md
@.planning/phases/42-cross-screen-threading/42-03-SUMMARY.md
@.planning/phases/42-cross-screen-threading/42-04-SUMMARY.md
@.planning/phases/42-cross-screen-threading/42-05-SUMMARY.md

<interfaces>
<!-- Harness shape verified this session. -->

E2E harness (examples/cairnloop_example/test/e2e/rail_disclosure_test.exs):
- `use PhoenixTest.Playwright.Case, async: false`
- `@moduletag :e2e`
- fixtures via `import CairnloopExample.RailFixtures` (e.g. pending_governed_action_conversation/0
  returns %{conv_id:, proposal_id:}); the Ecto sandbox is managed by the Case and the dashboard
  live_session joins it via CairnloopExampleWeb.LiveAcceptance.
- nav: `conn |> visit("/support/#{conv_id}") |> assert_has("body .phx-connected")`
- helpers: `assert_has/2,3`, `PhoenixTest.Playwright.reload_page/1`.

IMPORTANT: E2E visits use the HOST mount prefix `/support/...` (the example app mounts the library
at /support — examples/cairnloop_example/lib/cairnloop_example_web/router.ex:42). The LIBRARY links
are scope-relative (`/#{id}`); under the /support mount they resolve to `/support/#{id}`. Assert the
landed URL accordingly (e.g. ends with `/support/<id>`), NOT a doubled `/support/support/...`.

Run command: `cd examples/cairnloop_example && mix test.e2e`.
Threads to exercise (from Plans 03/04/05):
- THREAD-01: resolved conversation → "Next in queue" → next open conversation.
- THREAD-02: /support/audit-log row "View conversation" → subject conversation.
- THREAD-03a: governed-action card "View audit trail" → /support/audit-log?proposal=<id>.
- THREAD-03b: conversation-originated article editor "From conversation" crumb → conversation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E thread-navigation spec for all four threads</name>
  <files>examples/cairnloop_example/test/e2e/thread_navigation_test.exs</files>
  <read_first>
    - examples/cairnloop_example/test/e2e/rail_disclosure_test.exs:1-45 (exact harness: use PhoenixTest.Playwright.Case, @moduletag :e2e, fixtures import, visit + .phx-connected gate, assert_has)
    - examples/cairnloop_example/lib/cairnloop_example_web/router.ex:42 (library mounted at /support — visit paths are /support-prefixed; landed library links resolve under /support)
    - 42-RESEARCH.md §"Wave 0 Gaps" (thread_navigation_test.exs requirement) + §"Validation Architecture" (E2E is the phase gate proving real transitions)
    - 42-PATTERNS.md §"Test files" (E2E mirrors rail_disclosure_test.exs)
    - The Plan 03/04/05 SUMMARYs for the exact link text/accessible names shipped ("View conversation", "Next in queue", "View audit trail", "From conversation")
  </read_first>
  <action>
    Create `examples/cairnloop_example/test/e2e/thread_navigation_test.exs` mirroring
    `rail_disclosure_test.exs`: `use PhoenixTest.Playwright.Case, async: false`, `@moduletag :e2e`,
    fixtures imported from the example app's fixtures module (reuse
    `pending_governed_action_conversation/0` or the closest existing fixture; add a small fixture only
    if none provides a resolved conversation / a conversation-originated article — prefer reuse). Add
    a `describe` per thread with at least one `test` each:

    1. THREAD-01: seed/visit a RESOLVED conversation with another open conversation present; visit
       `/support/#{resolved_id}`, gate on `.phx-connected`, click the "Next in queue" link, assert the
       landed URL is the next open conversation (e.g. `assert_has` a conversation-view marker /
       assert the path is `/support/#{next_open_id}` and NOT `/support/inbox`). If the fixture set has
       no second open conversation, also cover the Queue-clear branch (link to `/support/inbox`).
    2. THREAD-02: visit `/support/audit-log`, click a row's "View conversation" link, assert landing on
       the subject conversation view.
    3. THREAD-03a: visit a conversation with a governed-action card, open the Tier-3 "Identifiers &
       trace" group, click "View audit trail", assert landing on `/support/audit-log?proposal=<id>`
       (URL contains the proposal filter).
    4. THREAD-03b: visit a conversation-ORIGINATED article editor (`:conversation_quick_fix`), click
       the "From conversation" breadcrumb crumb, assert landing on the originating conversation.

    Assert landed URLs against the HOST `/support/...` prefix (the library links are scope-relative;
    under the mount they resolve to `/support/...`) — never assert a doubled `/support/support/...`
    (Pitfall 3 regression guard). Keep the spec deterministic; reuse the existing sandbox/LiveAcceptance
    wiring. Do not modify any library code — this plan is test-only.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example && mix test.e2e test/e2e/thread_navigation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/cairnloop_example/test/e2e/thread_navigation_test.exs` exists with `@moduletag :e2e` and `use PhoenixTest.Playwright.Case`.
    - It has a describe/test for each of THREAD-01, THREAD-02, THREAD-03a, THREAD-03b.
    - URL assertions use the `/support/...` host prefix and contain no `/support/support/` (scope-relative regression guard).
    - `cd examples/cairnloop_example && mix test.e2e test/e2e/thread_navigation_test.exs` exits 0.
    - No library `lib/` file is modified by this plan (test-only).
  </acceptance_criteria>
  <done>A real-browser E2E spec proves all four threads navigate to the correct landing record under the host mount; the gated e2e lane passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → browser | Test-only plan; exercises the host-mounted routes in a real browser. No new product trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-16 | Tampering | scope-relative links breaking under the /support mount | mitigate | E2E asserts landed URLs under `/support/...` with no doubled prefix — the regression guard for Pitfall 3 |
| T-42-17 | (N/A — test-only) | no library code changed | accept | Plan modifies only a test file; no new runtime surface |
</threat_model>

<verification>
- `cd examples/cairnloop_example && mix test.e2e test/e2e/thread_navigation_test.exs` exits 0.
- The spec covers all four threads (grep for the four link texts / four describe blocks).
- No `lib/` change introduced by this plan (`git diff --name-only` shows only the e2e test file).
</verification>

<success_criteria>
- All four threading transitions are proven in a real browser under the host mount (THREAD-01/02/03).
- URL assertions guard against the scope-relative/mount-prefix regression (Pitfall 3).
- Test-only; gated e2e lane green.
</success_criteria>

<output>
Create `.planning/phases/42-cross-screen-threading/42-06-SUMMARY.md` when done.
</output>
