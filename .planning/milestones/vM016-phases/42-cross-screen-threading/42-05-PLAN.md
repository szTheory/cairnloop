---
phase: 42-cross-screen-threading
plan: 05
type: execute
wave: 2
depends_on: ["42-02"]
files_modified:
  - lib/cairnloop/web/knowledge_base_live/editor.ex
  - lib/cairnloop/web/breadcrumb_presenter.ex
  - test/cairnloop/web/breadcrumb_presenter_test.exs
  - test/cairnloop/web/knowledge_base_live/editor_test.exs
autonomous: true
requirements: [THREAD-03]
must_haves:
  truths:
    - "A KB article that originated from a conversation (:conversation_quick_fix) shows a breadcrumb/header link back to that conversation"
    - "A KB article with no originating conversation (gap/revision) shows no such link — honest absence, never a dead/placeholder crumb"
    - "The origin link reuses cl_breadcrumb + BreadcrumbPresenter and is scope-root-relative"
    - "Resolution works on a DIRECT visit to the editor (not only via the suggestion-handoff param)"
  artifacts:
    - path: "lib/cairnloop/web/knowledge_base_live/editor.ex"
      provides: "origin-conversation resolved at mount + threaded into the breadcrumb"
      contains: "originating_conversation_id"
    - path: "lib/cairnloop/web/breadcrumb_presenter.ex"
      provides: "editor_items variant that prepends an origin-conversation crumb when present"
  key_links:
    - from: "editor.ex mount/3"
      to: "KnowledgeAutomation.originating_conversation_id(article.id, scope_filters)"
      via: "assign threaded into BreadcrumbPresenter.editor_items"
      pattern: "originating_conversation_id"
    - from: "breadcrumb_presenter.ex editor_items"
      to: "/#{conversation_id} crumb (only when non-nil)"
      via: "conditional origin crumb"
      pattern: "From conversation"
---

<objective>
Wire the KB article → originating-conversation back-link (THREAD-03b) into the editor breadcrumb,
reusing the P38 `cl_breadcrumb` + `BreadcrumbPresenter` orientation layer. Builds on Plan 02's
`KnowledgeAutomation.originating_conversation_id/2`.

Purpose: trace a KB article back to the conversation where the gap was first surfaced —
bi-directional causal threading. The resolution must work on a DIRECT editor visit, because the
in-scope `suggestion` is nil unless the URL carries the handoff param (RESEARCH correction #2 /
Pitfall 2); Plan 02's read is keyed on `article_id` precisely to handle that.

Output: an origin-conversation crumb rendered ONLY for `:conversation_quick_fix`-originated
articles (honest absence otherwise), reusing the existing breadcrumb infrastructure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-cross-screen-threading/42-CONTEXT.md
@.planning/phases/42-cross-screen-threading/42-RESEARCH.md
@.planning/phases/42-cross-screen-threading/42-PATTERNS.md
@.planning/phases/42-cross-screen-threading/42-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (consumed here) + the live editor/presenter shape (verified this session). -->

From Plan 02: KnowledgeAutomation.originating_conversation_id(article_id, opts) →
conversation id | nil (only for :conversation_quick_fix-originated articles).

editor.ex mount/3 (:12) computes `scope_filters = scope_filters(session)` and assigns
`article` (article.id in scope), `review_context` (has :return_to), etc. Render breadcrumb slot
(editor.ex:264-266):
```elixir
<:breadcrumb>
  <.cl_breadcrumb items={BreadcrumbPresenter.editor_items(@review_context.return_to, @article.title)} />
</:breadcrumb>
```

BreadcrumbPresenter.editor_items/2 (breadcrumb_presenter.ex:45) — total fn, builds a list of
`%{label, href}` maps; last item omits :href. Docstring (:82) already notes "cross-screen
threading lands in Phase 42". cl_breadcrumb/1 (components.ex:395): items=[%{label, href}], renders
`<.link :if={item[:href]} navigate={item.href}>`. Scope-relative paths only (Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BreadcrumbPresenter origin-crumb variant (conditional, total)</name>
  <files>lib/cairnloop/web/breadcrumb_presenter.ex, test/cairnloop/web/breadcrumb_presenter_test.exs</files>
  <read_first>
    - lib/cairnloop/web/breadcrumb_presenter.ex:45-99 (editor_items/2 total-fn idiom + the :82 "Phase 42" docstring note)
    - lib/cairnloop/web/components.ex:389-405 (cl_breadcrumb items contract: %{label, href}; last omits :href)
    - 42-PATTERNS.md §"knowledge_base_live/editor.ex — article→conversation crumb" (presenter excerpt + scope-relative crumb href; omit when nil)
    - 42-RESEARCH.md D-12, D-13, Pitfall 3, Pitfall 4
  </read_first>
  <behavior>
    - When given a non-nil originating conversation id, editor_items prepends (or includes) an origin crumb %{label: "From conversation", href: "/#{id}"}.
    - When the origin id is nil, the crumb list is exactly today's output (no origin crumb — honest absence, D-12).
    - The origin crumb href is scope-root-relative (/#{id}), never mount-prefixed.
    - The function stays total (multi-clause head, explicit nil/_ fallback).
  </behavior>
  <action>
    Add an origin-conversation crumb to `BreadcrumbPresenter` for the editor breadcrumb. Resolve
    Claude's-discretion: extend with a new arity (e.g. `editor_items/3` taking
    `(origin_conversation_id, return_to, title)`) OR add an optional origin arg — keep the existing
    `editor_items/2` clauses intact (sealed; other call sites must not break). When the origin id is a
    present integer, include a crumb `%{label: "From conversation", href: "/#{id}"}` as the first crumb
    (scope-root-relative — Pitfall 3; NEVER `/support/...`); when nil, return exactly the existing
    `editor_items/2` output (honest absence — D-12, Pitfall 4). Mirror the existing total-fn idiom:
    explicit clause for the present-id case, fallback delegating to the current `editor_items/2`.
    Return data only (list of `%{label, href}` maps), never markup.

    In `test/cairnloop/web/breadcrumb_presenter_test.exs`, add pure total-fn cases: origin id present →
    first crumb is `%{label: "From conversation", href: "/<id>"}` (no `/support` prefix); origin id nil →
    identical to the existing editor_items/2 output (no extra crumb). No Repo.
  </action>
  <verify>
    <automated>grep -q 'From conversation' lib/cairnloop/web/breadcrumb_presenter.ex && ! grep -q '/support/' lib/cairnloop/web/breadcrumb_presenter.ex && mix compile --warnings-as-errors && mix test test/cairnloop/web/breadcrumb_presenter_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `breadcrumb_presenter.ex` produces a `%{label: "From conversation", href: "/<id>"}` crumb when an origin id is supplied (test asserts; href scope-relative, no `/support`).
    - With a nil origin id, the crumb list equals the existing `editor_items/2` output (test asserts — no extra crumb; honest absence).
    - Existing `editor_items/2` clauses are unchanged (sealed).
    - `mix test test/cairnloop/web/breadcrumb_presenter_test.exs` exits 0; `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The presenter conditionally prepends a scope-relative origin-conversation crumb, omitting it on absence, as a total function; pure tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve origin at editor mount + thread into the breadcrumb</name>
  <files>lib/cairnloop/web/knowledge_base_live/editor.ex, test/cairnloop/web/knowledge_base_live/editor_test.exs</files>
  <read_first>
    - lib/cairnloop/web/knowledge_base_live/editor.ex:12-40 (mount/3 — scope_filters(session) + the assign(...) chain to add the read alongside)
    - lib/cairnloop/web/knowledge_base_live/editor.ex:107-115 (load_suggestion/3 — proof the in-scope suggestion is nil on a direct visit; DO NOT use it for origin resolution)
    - lib/cairnloop/web/knowledge_base_live/editor.ex:263-267 (the breadcrumb slot to update)
    - 42-PATTERNS.md §"knowledge_base_live/editor.ex" (mount-thread excerpt; reuse cl_breadcrumb, don't build a new affordance)
    - 42-RESEARCH.md Pitfall 2 (NEW article_id-keyed lookup, not the handoff suggestion), D-12, D-13
  </read_first>
  <behavior>
    - At mount, the editor resolves the origin conversation id via KnowledgeAutomation.originating_conversation_id(article.id, scope_filters) and assigns it.
    - The breadcrumb renders the origin crumb when the id is present, omits it when nil.
    - Resolution works on a DIRECT visit (no "suggestion_id" param) — keyed on article.id, not the in-scope suggestion.
  </behavior>
  <action>
    In `editor.ex` `mount/3`, after `article` and `scope_filters` are computed, resolve
    `origin_conversation_id = KnowledgeAutomation.originating_conversation_id(article.id, scope_filters)`
    (Plan 02) and add it as an assign (e.g. `assign(origin_conversation_id: ...)`) alongside the other
    assigns. Use the article_id-keyed facade read — NOT the in-scope `suggestion` (nil on direct
    visits — Pitfall 2). No raw `Cairnloop.Repo` (criterion-4 / D-03). Update the breadcrumb slot
    (editor.ex:264-266) to pass the origin id into the presenter (the `editor_items/3` form from
    Task 1) so the origin crumb renders only when present (D-12/D-13). Reuse `cl_breadcrumb` — do not
    build a new affordance. Ensure the read is also resolved on the error/rescue mount path only if an
    article is in scope there (otherwise leave the rescue path untouched).

    In `test/cairnloop/web/knowledge_base_live/editor_test.exs`, add a headless render case (built
    assigns including `origin_conversation_id`, no Repo): present id → breadcrumb HTML contains
    `navigate="/<id>"` and "From conversation"; nil → no origin crumb. The DB-backed mount resolution
    itself is covered by Plan 02's REPO-UNAVAILABLE integration tests.
  </action>
  <verify>
    <automated>grep -q 'originating_conversation_id' lib/cairnloop/web/knowledge_base_live/editor.ex && ! grep -q 'Cairnloop.Repo\.' lib/cairnloop/web/knowledge_base_live/editor.ex && mix compile --warnings-as-errors && mix test test/cairnloop/web/knowledge_base_live/editor_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `editor.ex` mount calls `KnowledgeAutomation.originating_conversation_id(article.id, ...)` and assigns the result (grep + read).
    - The origin resolution uses `article.id`, NOT the in-scope `suggestion` (read confirms; works on direct visit).
    - With a present origin id, the rendered breadcrumb contains `navigate="/<id>"` + "From conversation"; with nil, no origin crumb (test asserts).
    - `grep 'Cairnloop.Repo\.' editor.ex` → no matches (criterion-4).
    - `mix test test/cairnloop/web/knowledge_base_live/editor_test.exs` exits 0; `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The editor resolves the origin conversation by article id at mount and renders a scope-relative back-link crumb only when present; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| facade → editor render | The origin conversation id (a structural FK) is resolved via a scoped facade read and rendered as a link target — not a trust fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-13 | Information Disclosure | origin crumb linking to a cross-tenant conversation | mitigate | originating_conversation_id/2 pipes through apply_scope/2 (Plan 02); scope_filters(session) passed from mount (V4) |
| T-42-14 | Information Disclosure | dead/placeholder crumb when no origin exists | mitigate | nil id → crumb omitted entirely (honest absence, D-12, Pitfall 4); no `/nil` href |
| T-42-15 | Tampering | mount-prefixed crumb href | mitigate | Crumb href is scope-root-relative `/#{id}`; grep gate enforces no `/support/` (Pitfall 3) |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` passes.
- `mix test test/cairnloop/web/breadcrumb_presenter_test.exs test/cairnloop/web/knowledge_base_live/editor_test.exs` exits 0.
- `grep 'Cairnloop.Repo\.' lib/cairnloop/web/knowledge_base_live/editor.ex` → no matches (criterion-4).
- `grep '/support/' lib/cairnloop/web/breadcrumb_presenter.ex lib/cairnloop/web/knowledge_base_live/editor.ex` → no matches.
- Manual: a conversation-originated article shows a "From conversation" crumb; a gap/revision article shows none.
</verification>

<success_criteria>
- A KB article links back to its originating conversation when one exists, and shows no link otherwise (THREAD-03b).
- Resolution works on a direct editor visit (article_id-keyed, not handoff-param-gated).
- Reuses cl_breadcrumb/BreadcrumbPresenter; scope-relative; no raw Repo; build warnings-clean; tests green.
</success_criteria>

<output>
Create `.planning/phases/42-cross-screen-threading/42-05-SUMMARY.md` when done.
</output>
