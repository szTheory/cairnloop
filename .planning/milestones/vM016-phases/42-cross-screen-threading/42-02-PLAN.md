---
phase: 42-cross-screen-threading
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/chat.ex
  - lib/cairnloop/knowledge_automation.ex
  - test/cairnloop/chat_test.exs
  - test/cairnloop/knowledge_automation_test.exs
autonomous: true
requirements: [THREAD-01, THREAD-03]
must_haves:
  truths:
    - "Chat.next_open_conversation/1 returns the id of the next open conversation in inbox order, excluding the current id, or nil when the queue is clear"
    - "next_open_conversation/1 is a cheap select(:id)-only scoped read — never a full list load + Enum"
    - "KnowledgeAutomation.originating_conversation_id/2 returns the conversation id for a :conversation_quick_fix-originated article, or nil otherwise"
    - "Both reads route through the facade repo() indirection — no raw Cairnloop.Repo"
  artifacts:
    - path: "lib/cairnloop/chat.ex"
      provides: "next_open_conversation/1 cheap scoped read"
      contains: "def next_open_conversation"
    - path: "lib/cairnloop/knowledge_automation.ex"
      provides: "originating_conversation_id/2 article->conversation read"
      contains: "def originating_conversation_id"
  key_links:
    - from: "lib/cairnloop/chat.ex next_open_conversation/1"
      to: "Conversation (status==:open, id != current, updated_at desc)"
      via: "scoped select(:id) query via repo()"
      pattern: "select.*c.id"
    - from: "lib/cairnloop/knowledge_automation.ex originating_conversation_id/2"
      to: "ArticleSuggestion.entrypoint_id"
      via: "where article_id and entrypoint_type==:conversation_quick_fix, select(:entrypoint_id)"
      pattern: "conversation_quick_fix"
---

<objective>
Build the two facade reads that back Next-in-queue (THREAD-01) and the KB article →
originating-conversation back-link (THREAD-03b). Domain layer only — Plans 04 and 05 wire the
LiveView affordances on top of these reads.

Purpose: "Next" must mirror the inbox's existing canonical order without inventing a queue field
(D-04), and the article→conversation origin must be resolved via a NEW article→suggestion lookup
because the editor's in-scope `suggestion` is nil on a direct visit and `Article` has no
`belongs_to(:suggestion)` (RESEARCH correction #2 / Pitfall 2).

Output: `Chat.next_open_conversation/1` (cheap, select-id-only, deterministic tiebreak) and
`KnowledgeAutomation.originating_conversation_id/2` (scoped, conversation-quick-fix-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-cross-screen-threading/42-CONTEXT.md
@.planning/phases/42-cross-screen-threading/42-RESEARCH.md
@.planning/phases/42-cross-screen-threading/42-PATTERNS.md

<interfaces>
<!-- Verified in-tree this session. -->

Chat facade idiom (lib/cairnloop/chat.ex:1-33) — alias Cairnloop.{Conversation, Message};
repo() = Application.fetch_env!(:cairnloop, :repo); ordering order_by(desc: :updated_at);
cheap-read pattern count_conversations/1 uses repo().aggregate, never Enum. Status enum
[:open, :resolved, :archived] (conversation.ex:5-6).

KnowledgeAutomation facade (lib/cairnloop/knowledge_automation.ex):
```elixir
defp repo, do: Application.fetch_env!(:cairnloop, :repo)
defp apply_scope(query, opts) do
  query
  |> maybe_where_equal(:tenant_scope, Keyword.get(opts, :tenant_scope))
  |> maybe_where_equal(:host_user_id, Keyword.get(opts, :host_user_id))
end
```
ArticleSuggestion (article_suggestion.ex:29-31): entrypoint_type is
Ecto.Enum [:gap_candidate, :article_revision, :conversation_quick_fix]; entrypoint_id :integer;
article_id :integer. Only :conversation_quick_fix carries a conversation id (D-12).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chat.next_open_conversation/1 cheap scoped read</name>
  <files>lib/cairnloop/chat.ex, test/cairnloop/chat_test.exs</files>
  <read_first>
    - lib/cairnloop/chat.ex:1-46 (repo() indirection, order_by desc: :updated_at, scope_status/2 parameterized pin, count_conversations/1 cheap-read pattern)
    - 42-PATTERNS.md §"lib/cairnloop/chat.ex — next_open_conversation/1" (exact new-fn shape + provenance)
    - 42-RESEARCH.md Pattern 1 + D-04 + D-06 + D-07 (mirror inbox order; select id only; deterministic id tiebreak; nil when clear)
  </read_first>
  <behavior>
    - next_open_conversation(current_id) returns the id of the first open conversation ordered updated_at desc (tiebreak id desc), excluding current_id.
    - Returns nil when no other open conversation exists (queue clear, D-06).
    - select(:id) only — does NOT load the full conversation row (D-07, cheap-read).
  </behavior>
  <action>
    Add `def next_open_conversation(current_id)` to `Cairnloop.Chat` as an additive sibling to
    `list_conversations/1` (Claude's-discretion name resolved: `next_open_conversation/1`, arg =
    current conversation id). Query `Conversation` `where([c], c.status == :open and c.id != ^current_id)`,
    `order_by([c], desc: c.updated_at, desc: c.id)` (D-04 mirrors inbox order; `desc: :id` is the
    deterministic tiebreak per D-07), `limit(1)`, `select([c], c.id)`, `repo().one()`. Reference the
    aliased `Conversation`, not the fully-qualified name. Parameterize `current_id` with `^` (never
    interpolate). Do NOT load the full row — select the id only (D-07). Do not modify the sealed
    `list_conversations`/`count_conversations`/`scope_status` clauses.

    In `test/cairnloop/chat_test.exs`, add cases for next_open_conversation/1 (next-open id; excludes
    current; nil when queue clear; tiebreak determinism). These need a Postgres round-trip — write
    them and mark `# REPO-UNAVAILABLE` (run under mix test.integration). Keep the file compiling so
    plain `mix test` stays green (REPO-UNAVAILABLE cases skipped/tagged, not failing).
  </action>
  <verify>
    <automated>grep -q 'def next_open_conversation' lib/cairnloop/chat.ex && grep -A8 'def next_open_conversation' lib/cairnloop/chat.ex | grep -q 'select' && mix compile --warnings-as-errors && mix test test/cairnloop/chat_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/cairnloop/chat.ex` contains `def next_open_conversation(`.
    - The new fn body contains a `select([c], c.id)` (id-only read, NOT a full-row load) and an `order_by` with `desc: c.updated_at`.
    - `current_id` is parameterized with `^current_id` (no string interpolation).
    - `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/chat_test.exs` exits 0 (REPO-UNAVAILABLE cases not failing).
  </acceptance_criteria>
  <done>next_open_conversation/1 is a cheap select-id scoped read mirroring inbox order with a deterministic tiebreak; build warnings-clean; tests green/marked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KnowledgeAutomation.originating_conversation_id/2 article->conversation read</name>
  <files>lib/cairnloop/knowledge_automation.ex, test/cairnloop/knowledge_automation_test.exs</files>
  <read_first>
    - lib/cairnloop/knowledge_automation.ex (repo() indirection, list_article_suggestions/1, apply_scope/2 at :1965)
    - lib/cairnloop/knowledge_automation/article_suggestion.ex:29-31 (entrypoint_type enum, entrypoint_id, article_id)
    - 42-PATTERNS.md §"lib/cairnloop/knowledge_automation.ex — originating_conversation_id/2" (exact new-fn shape + provenance)
    - 42-RESEARCH.md Pattern 4 + Pitfall 2 + D-12 (NEW lookup keyed on article_id; only :conversation_quick_fix carries a conv id; earliest origin if multiple; nil → omit link)
  </read_first>
  <behavior>
    - originating_conversation_id(article_id) returns entrypoint_id for the earliest ArticleSuggestion where article_id matches AND entrypoint_type == :conversation_quick_fix.
    - Returns nil for articles whose suggestions are :gap_candidate / :article_revision (honest absence, D-12).
    - Returns nil for an unknown article_id.
    - Goes through apply_scope/2 (operator scope honored — V4 access control).
  </behavior>
  <action>
    Add `def originating_conversation_id(article_id, opts \\ [])` to `Cairnloop.KnowledgeAutomation`
    (Claude's-discretion name resolved: `originating_conversation_id/2`). Query `ArticleSuggestion`,
    pipe through the existing `apply_scope(opts)` (operator scope — DO NOT add an unscoped query;
    V4 access control / cross-tenant deep-link mitigation), `where([s], s.article_id == ^article_id
    and s.entrypoint_type == :conversation_quick_fix)` (D-12 — only this entrypoint carries a
    conversation id), `order_by([s], asc: s.inserted_at)` (earliest origin if multiple, assumption A2),
    `limit(1)`, `select([s], s.entrypoint_id)`, `repo().one()`. This is a NEW read keyed on
    `article_id` — do NOT reuse the editor's in-scope `suggestion` (nil on direct visits, Pitfall 2);
    `Article` has no `belongs_to(:suggestion)`. Parameterize all bindings with `^`.

    In `test/cairnloop/knowledge_automation_test.exs`, add cases: :conversation_quick_fix → entrypoint_id;
    :gap_candidate / :article_revision → nil; unknown article_id → nil; scope honored. These need a
    Postgres round-trip — write them and mark `# REPO-UNAVAILABLE` (run under mix test.integration);
    keep the file compiling so plain `mix test` stays green.
  </action>
  <verify>
    <automated>grep -q 'def originating_conversation_id' lib/cairnloop/knowledge_automation.ex && grep -A10 'def originating_conversation_id' lib/cairnloop/knowledge_automation.ex | grep -q 'conversation_quick_fix' && mix compile --warnings-as-errors && mix test test/cairnloop/knowledge_automation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/cairnloop/knowledge_automation.ex` contains `def originating_conversation_id(`.
    - The fn body filters on `entrypoint_type == :conversation_quick_fix`, pipes through `apply_scope`, and `select`s `entrypoint_id` (id-only).
    - All query bindings parameterized with `^` (no interpolation).
    - `mix compile --warnings-as-errors` exits 0; `mix test test/cairnloop/knowledge_automation_test.exs` exits 0 (REPO-UNAVAILABLE cases not failing).
  </acceptance_criteria>
  <done>originating_conversation_id/2 resolves the conversation-quick-fix origin id via a scoped article_id lookup, nil otherwise; build warnings-clean; tests green/marked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| facade → web layer | Both reads return only ids consumed by Plans 04/05 as link targets — structural FKs, not trust facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Information Disclosure | originating_conversation_id/2 returning a cross-tenant conversation | mitigate | Pipes through apply_scope/2 (tenant_scope + host_user_id); never an unscoped query (V4) |
| T-42-05 | Tampering | current_id / article_id binding | mitigate | Parameterized `^` Ecto pins only; total reads return nil on unknown ids, never crash |
| T-42-06 | Information Disclosure | leaking the full conversation/suggestion row | accept→mitigate | select(:id)/select(:entrypoint_id) only — minimal field exposure by construction (D-07) |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` passes.
- `mix test test/cairnloop/chat_test.exs test/cairnloop/knowledge_automation_test.exs` exits 0.
- `grep -rn 'Cairnloop.Repo' lib/cairnloop/chat.ex lib/cairnloop/knowledge_automation.ex` shows no direct Repo use (only `repo()` indirection).
- No schema change in either module.
</verification>

<success_criteria>
- `next_open_conversation/1` returns the next-open id in inbox order (or nil), cheaply (THREAD-01 backing).
- `originating_conversation_id/2` resolves the conversation-quick-fix origin id (or nil) (THREAD-03b backing).
- Both reads are scoped facade reads via repo() — no raw Cairnloop.Repo; no migration; build warnings-clean.
</success_criteria>

<output>
Create `.planning/phases/42-cross-screen-threading/42-02-SUMMARY.md` when done.
</output>
