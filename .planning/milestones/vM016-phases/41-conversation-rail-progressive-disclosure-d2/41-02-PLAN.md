---
phase: 41-conversation-rail-progressive-disclosure-d2
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - lib/cairnloop/web/components.ex
autonomous: true
requirements: [RAIL-03]
user_setup: []

must_haves:
  truths:
    - "cl_disclosure/1 passes data-tier='2' (and any data-* global) through to the rendered <details>"
    - "Existing cl_disclosure call sites render identically (additive change — no existing caller passes :rest)"
    - "open is still bound ONLY as the static initial-render boolean (D2/D-09 invariant untouched)"
  artifacts:
    - path: "lib/cairnloop/web/components.ex"
      provides: "cl_disclosure/1 with attr(:rest, :global) spread onto <details>"
      contains: "attr(:rest, :global)"
  key_links:
    - from: "lib/cairnloop/web/components.ex cl_disclosure/1"
      to: "the rendered <details> element"
      via: "{@rest} spread carrying data-tier/data-density"
      pattern: "phx-update=\"ignore\" open=\\{@open\\} \\{@rest\\}"
---

<objective>
Add an additive `attr(:rest, :global)` passthrough to the Phase-37 `cl_disclosure/1` primitive so the
card template (Plan 03) and rail controls (Plan 04) can scope `data-tier="2"` to the `<details>`
element. Without this, the Expand-all/Collapse-all `[data-tier="2"]` selector matches nothing
(RESEARCH Pitfall 1).

Purpose: Unblock `data-tier` scoping for RAIL-03. This is the single primitive edit the phase needs.
Output: `cl_disclosure/1` carries `{@rest}` on its `<details>`; the Wave 0 components_test.exs
`data-tier` test flips GREEN.

NOTE — SEAL-SAFE additive change: Phase 37 is sealed (CLAUDE.md "seal completed phases"). Adding a
`:global` passthrough is purely additive — no existing call site passes extra attributes, so no
existing render changes. This is the recommended option from CONTEXT D-03 / RESEARCH A3, mirroring
the `cl_switch/1` idiom in the same file. Flagged here so the owner can cheaply veto if they would
rather add an explicit `attr(:tier, :string)` instead.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/41-conversation-rail-progressive-disclosure-d2/41-PATTERNS.md
@.planning/phases/41-conversation-rail-progressive-disclosure-d2/41-RESEARCH.md

<interfaces>
<!-- The canonical :global passthrough idiom in THIS file — copy it. -->

From lib/cairnloop/web/components.ex:243-252 (cl_switch/1):
  attr(:rest, :global, include: ~w(phx-click phx-value-id phx-value-key disabled form name value))
  def cl_switch(assigns) do
    ~H"""
    <button type="button" class="cl-switch" role="switch" aria-checked={to_string(@checked)} {@rest}>

From lib/cairnloop/web/components.ex:193-205 (cl_disclosure/1 as-written — NO :rest today):
  attr(:id, :string, required: true)
  attr(:open, :boolean, default: false)
  slot(:summary, required: true)
  slot(:inner_block, required: true)
  def cl_disclosure(assigns) do
    ~H"""
    <details class="cl-details cl-disclosure" id={@id} phx-update="ignore" open={@open}>
      <summary class="cl-details__summary">{render_slot(@summary)}</summary>
      {render_slot(@inner_block)}
    </details>
    """
  end
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add attr(:rest, :global) to cl_disclosure/1 and spread {@rest} onto <details></name>
  <read_first>
    - lib/cairnloop/web/components.ex:180-205 (cl_disclosure/1 @doc + attrs + function body)
    - lib/cairnloop/web/components.ex:243-252 (cl_switch/1 — the canonical :global idiom to copy)
    - test/cairnloop/web/components_test.exs:256-313 (the cl_disclosure tests incl. the Wave 0
      data-tier test that must flip GREEN, and the existing open positive/negative tests that must
      STAY GREEN — proving the change is additive and the static-open invariant is intact)
  </read_first>
  <behavior>
    - Rendering `<.cl_disclosure id="x" open={true} data-tier="2">` emits `data-tier="2"` on the
      `<details>` (Wave 0 test flips RED→GREEN).
    - Rendering `<.cl_disclosure id="x">` (no extra attrs) renders byte-for-byte as before — the two
      existing positive (open=true → `open` present) and negative (default → no `open`) tests stay green.
    - `open` remains bound only to `@open` (the static boolean); `{@rest}` carries data-* only, never
      a dynamic `open` binding.
  </behavior>
  <action>
    Add `attr(:rest, :global)` immediately AFTER `attr(:open, :boolean, default: false)` (no `include:`
    list needed — `data-tier`/`data-density` are plain `data-*` globals that pass by default; do NOT
    add `phx-*` to an `include:` here because the disclosure must never receive a dynamic open binding).
    Spread `{@rest}` onto the existing `<details>` tag AFTER `open={@open}`, so the element reads
    `<details class="cl-details cl-disclosure" id={@id} phx-update="ignore" open={@open} {@rest}>`.
    Extend the existing `@doc` by ONE line noting `:rest` carries `data-tier`/`data-density` scoping
    hooks (per the existing P41 forward-compat guardrail already in that @doc). Do NOT touch the
    `phx-update="ignore"`, the `open={@open}` binding, the summary slot, or any other attr — this is
    a strictly additive passthrough.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5</automated>
    <automated>mix test test/cairnloop/web/components_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cl_disclosure/1` attr list contains `attr(:rest, :global)` and the `<details>` carries `{@rest}`.
    - `mix compile --warnings-as-errors` exits 0.
    - The Wave 0 `data-tier="2"` passthrough test (Plan 01 Task 2) now PASSES.
    - The two existing cl_disclosure `open` positive/negative tests still PASS (additive proof).
    - No new `open`/dynamic binding introduced — `open` is still `={@open}` only.
  </acceptance_criteria>
  <done>cl_disclosure/1 carries {@rest}; data-tier test green; existing open tests green; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → cl_disclosure markup | A `:global` passthrough now spreads caller-provided attributes onto `<details>`. Callers are first-party templates (no untrusted attribute source). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02 | Tampering | `{@rest}` global passthrough on cl_disclosure | accept | All callers are first-party HEEx templates; no untrusted/user-supplied attribute map flows into `:rest`. `attr(:rest, :global)` is Phoenix.Component's standard, already used by cl_switch/cl_button/cl_card in the same file. No new XSS surface (V5): HEEx escapes attribute values. |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH: not applicable; all deps in mix.lock). |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/cairnloop/web/components_test.exs` green (data-tier test flipped, open tests intact).
</verification>

<success_criteria>
- `cl_disclosure/1` is additively extended with `:global` passthrough; the `data-tier` scoping hook works.
- No existing call-site render changes; the static-only `open` invariant is untouched.
</success_criteria>

<output>
Create `.planning/phases/41-conversation-rail-progressive-disclosure-d2/41-02-SUMMARY.md` when done.
</output>
