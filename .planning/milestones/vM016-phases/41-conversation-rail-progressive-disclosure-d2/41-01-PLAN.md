---
phase: 41-conversation-rail-progressive-disclosure-d2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/cairnloop/web/conversation_live_test.exs
  - test/cairnloop/web/components_test.exs
autonomous: true
requirements: [RAIL-01, RAIL-02, RAIL-03]
user_setup: []

must_haves:
  truths:
    - "A failing (RED) test asserts the pending footer + safety quartet render OUTSIDE any <details> (RAIL-01)"
    - "A failing (RED) test asserts 3 Tier-2 cl_disclosure groups (data-tier=2) + a standalone non-data-tier Trace group exist (RAIL-02)"
    - "A failing (RED) test asserts every rail <details> count equals the phx-update=ignore count (RAIL-02 mechanism)"
    - "A failing (RED) test asserts pending/blocked cards emit a static open on Inputs & scope; policy_denied also opens Policy (D-08); non-pending/non-blocked emits no open (D-08 negative)"
    - "A test asserts no handle_event clause head names a disclosure/open-state toggle (allow-listing the two known navigation events), so D-09 render-purity holds (GREEN against the current codebase, and stays GREEN after the phase ships)"
    - "A failing (RED) test asserts Expand/Collapse-all phx-click carries JS set_attribute/remove_attribute scoped to [data-tier='2'] and the rail emits data-density='comfortable' default + control markup (RAIL-03)"
    - "A failing (RED) test asserts cl_disclosure passes data-tier='2' through to the <details> (the :global passthrough Wave 1 adds)"
  artifacts:
    - path: "test/cairnloop/web/conversation_live_test.exs"
      provides: "describe 'governed_action_card/1 — Phase 41 rail disclosure' with the 7 RAIL-* assertions"
      contains: "Phase 41 rail disclosure"
    - path: "test/cairnloop/web/components_test.exs"
      provides: "extended cl_disclosure block asserting data-tier='2' passthrough"
      contains: "data-tier"
  key_links:
    - from: "test/cairnloop/web/conversation_live_test.exs"
      to: "Cairnloop.Web.ConversationLive.governed_action_card/1"
      via: "render_component(Function.capture(...), proposal: tool_proposal_fixture(...))"
      pattern: "render_component\\(card_fn"
---

<objective>
Create the Wave 0 RED test scaffold for Phase 41. The production code does not yet exist, so
every test added here MUST fail initially (RED) — that is the point. These tests encode the
target structure from VALIDATION.md so Waves 1–3 have a concrete, automated definition of done.

Purpose: Nyquist sampling floor. Every downstream plan verifies against these tests; no rail
behavior ships without a corresponding assertion landed here first.
Output: A new `describe` block in `conversation_live_test.exs` covering RAIL-01/02/03 + D-08/D-09,
and an extended `cl_disclosure` block in `components_test.exs` asserting the `data-tier` passthrough.

NOTE on the D-09 render-purity test (behavior 6): it is the ONE test in this block that is GREEN
from the start and STAYS green — it proves the invariant "no `handle_event` toggles disclosure open
state" both against the current codebase AND after the phase ships. The other six tests are RED now
and turn GREEN as Waves 1–3 land. (See behavior 6 for why the broad `~r/handle_event.*open/s`
DOTALL substring grep is FORBIDDEN — it false-matches the legitimate navigation events
`open_review_task`/`open_manual_draft`.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-conversation-rail-progressive-disclosure-d2/41-VALIDATION.md
@.planning/phases/41-conversation-rail-progressive-disclosure-d2/41-RESEARCH.md
@.planning/phases/41-conversation-rail-progressive-disclosure-d2/41-PATTERNS.md

<interfaces>
<!-- Existing headless test idiom — COPY this exactly. No Repo, no live mount. -->

GUARDRAIL — module placement (do NOT create a fresh module):
  The new `describe` block MUST live INSIDE the existing `Cairnloop.Web.ConversationLiveTest`
  module (`conversation_live_test.exs:1` — the module whose `setup`/config wires `MockRepo` at
  :406). The "DB-free / no `# REPO-UNAVAILABLE`" guarantee holds ONLY inside this module: a
  bare or `:proposed`/`policy_denied` `tool_proposal_fixture` leaves `proposal.approval` as
  `NotLoaded`, so `active_approval` resolution (conversation_live.ex:850-866) routes through
  `Cairnloop.Governance.get_latest_approval/1 → MockRepo.get_latest_approval/1 → nil` — a pure
  in-memory stub, never Postgres. Creating a NEW test module would lose the MockRepo wiring and
  hit a live Repo. Append the new `describe` to the END of this module's governed_action_card
  region; do not start a new `defmodule`.

From test/cairnloop/web/conversation_live_test.exs:1622 (tool_proposal_fixture/1):
  base struct fields: id, tool_ref, tool_version, status (:proposed default), risk_tier (:read_only),
  approval_mode (:auto), actor_id, account_id, input_snapshot, scope_snapshot, policy_snapshot,
  events ([]). Overrides merged via Map.merge. The base fixture has NO :approval key — add one in
  the override map for the pending case: `%{approval: %Cairnloop.Governance.Approval{id: 7, status: :pending}}`.
  (Confirm the Approval struct field shape against lib/cairnloop/governance/ — RESEARCH A1 flags this
  as the one field-name risk; if `:approval` is an assoc, the card resolves it via Ecto.assoc_loaded?
  at conversation_live.ex:850-866, so a bare struct override is loaded-and-present.)

From test/cairnloop/web/conversation_live_test.exs:1654-1656 (the render idiom):
  card_fn = Function.capture(Cairnloop.Web.ConversationLive, :governed_action_card, 1)
  html = render_component(card_fn, proposal: proposal)

From test/cairnloop/web/components_test.exs:260-313 (existing cl_disclosure block — extend, do not rewrite):
  uses `rendered_to_string(~H"...")` with `assigns = %{}`; positive open=true → "open" present;
  negative default → `refute html =~ ~r/\bopen\b/`. These two ALREADY satisfy criterion 4 ("no
  server assign controls open"); the only ADD is one test asserting data-tier="2" reaches <details>.

KNOWN handle_event clause heads in conversation_live.ex (for the behavior-6 allow-list):
  The only two clause heads whose event name contains "open" are LEGITIMATE NAVIGATION events,
  NOT disclosure toggles:
    handle_event("open_review_task", ...)   # conversation_live.ex:157 — navigates to a review task
    handle_event("open_manual_draft", ...)  # conversation_live.ex:168 — opens the manual-draft composer
  Neither touches a `<details>` open-state. The behavior-6 assertion MUST allow-list exactly these
  two and assert NO OTHER clause head names a disclosure/open-state toggle.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Phase 41 rail-disclosure describe block (RED) to conversation_live_test.exs</name>
  <read_first>
    - test/cairnloop/web/conversation_live_test.exs (the full file head + the existing
      `governed_action_card/1 — renders all four statuses` describe at :1645-1702 and
      `tool_proposal_fixture/1` at :1622-1639 — copy the runtime-dispatch idiom verbatim;
      CONFIRM the surrounding `defmodule Cairnloop.Web.ConversationLiveTest` (:1) and the
      MockRepo wiring (:406) so the new block lands INSIDE this module)
    - .planning/phases/41-conversation-rail-progressive-disclosure-d2/41-VALIDATION.md
      (§"Per-Task Verification Map" + §"Wave 0 Requirements" — the authoritative test list)
    - .planning/phases/41-conversation-rail-progressive-disclosure-d2/41-RESEARCH.md
      (§"Validation Architecture" — the honest server/client boundary; D-09 is a PROXY
      render-purity assertion: "no `handle_event`/assign flips `open`")
    - lib/cairnloop/web/conversation_live.ex (run `grep -n 'def handle_event(' …` to enumerate
      the clause heads so behavior 6 allow-lists the two navigation events exactly)
    - lib/cairnloop/web/conversation_live.ex:957-1112 (current card template — so assertions
      target the POST-restructure target shape, not the current bespoke markup)
  </read_first>
  <action>
    Add ONE new `describe "governed_action_card/1 — Phase 41 rail disclosure"` block at the end of
    the existing governed_action_card test region, INSIDE the `Cairnloop.Web.ConversationLiveTest`
    module (the one wiring MockRepo at :406 — do NOT create a new `defmodule`; a fresh module would
    lose the MockRepo stub and hit a live Repo). Use `Function.capture(Cairnloop.Web.ConversationLive,
    :governed_action_card, 1)` + `render_component(card_fn, proposal: ...)` (DB-free) for every test;
    use `tool_proposal_fixture/1` for fixtures. Do NOT add `# REPO-UNAVAILABLE` markers (RESEARCH §423
    confirms none of these touch Repo — bare/`:proposed`/`policy_denied` fixtures leave `approval`
    as NotLoaded, so resolution routes through MockRepo → nil, never Postgres).

    Six of these seven tests MUST fail RED now (production markup does not exist yet) — that is
    correct. Behavior 6 (D-09 render-purity) is the exception: it is GREEN from the start and stays
    green (it proves an invariant that already holds and must keep holding).

    Cover exactly these seven behaviors (one test each, names traceable to the req):
    1. RAIL-01 Tier-1 isolation — pending fixture (`approval: %Approval{status: :pending}`); assert
       `html =~ "Approval required"` AND assert the footer is NOT inside a `<details>`: split the
       rendered html on `"governed-action-footer"`; the segment from the card start up to the footer
       marker must contain an equal number of `"<details"` and `"</details>"` substrings (every
       disclosure opened before the footer is also closed before it — footer is a Tier-1 sibling).
    2. RAIL-02 group structure — `:proposed` fixture; assert exactly three `data-tier="2"` occurrences;
       assert the three Tier-2 summaries `"Inputs &amp; scope"`, `"History"`, `"Policy explanation"`
       are present; assert `"Identifiers &amp; trace"` summary is present; assert the trace group is
       NOT data-tier (count `data-tier="2"` stays at 3, not 4).
    3. RAIL-02 mechanism — `:proposed` fixture; compute details_count = (html split on `"<details"`
       length - 1) and ignore_count = (html split on `phx-update="ignore"` length - 1); assert
       `details_count == ignore_count` and `details_count >= 4`.
    4. D-08 positive — pending fixture → assert the Inputs group `<details>` carries `open`
       (regex allowing `data-tier="2" ... open` in either attribute order, e.g.
       `~r/data-tier="2"[^>]*\bopen\b|\bopen\b[^>]*data-tier="2"/`). Add a second `policy_denied`
       fixture (`%{status: :policy_denied}`) → assert BOTH Inputs and Policy groups emit `open`.
    5. D-08 negative — `:proposed` fixture with `approval: nil` (no pending, no block) → assert no
       Tier-2 group emits a static open: `refute html =~ ~r/data-tier="2"[^>]*\bopen\b/`.
    6. D-09 render-purity — assert "no `handle_event` clause toggles `<details>` disclosure open
       state," using the HONEST mechanism framing RESEARCH endorses. Do NOT use the broad DOTALL
       substring grep `refute src =~ ~r/handle_event.*open/s` — the `/s` flag makes `.*` span the
       whole file, so the legitimate navigation events `handle_event("open_review_task")`
       (conversation_live.ex:157) and `handle_event("open_manual_draft")` (:168) match and the
       `refute` FAILS unconditionally against the current codebase (guaranteed false positive).
       Instead, assert the POSITIVE mechanism over `handle_event` CLAUSE HEADS only:
         a. `src = File.read!("lib/cairnloop/web/conversation_live.ex")`.
         b. Extract every event-name string from a `handle_event(` clause head via
            `Regex.scan(~r/def handle_event\(\s*"([^"]+)"/, src)` → flatten to the list of event names.
         c. Define `allowed_open_events = ["open_review_task", "open_manual_draft"]` (the two known
            NAVIGATION events — they navigate, they do NOT toggle a `<details>`).
         d. Assert that the set of event names whose name suggests a disclosure/open-state toggle —
            i.e. any name matching `~r/^(open|close|toggle|expand|collapse|density)/` — is a SUBSET of
            `allowed_open_events`. Concretely:
              `disclosure_like = Enum.filter(event_names, &(&1 =~ ~r/^(open|close|toggle|expand|collapse|density)/))`
              `assert disclosure_like -- allowed_open_events == []`
            (i.e. after removing the allow-listed navigation events, NO disclosure-toggle event remains).
         e. As a companion belt-and-suspenders check (still over clause-head scope, never DOTALL),
            assert the card template binds `open` only to a STATIC assign/boolean, never to a
            post-event assign: assert there is NO `open={@` binding to a name other than the two
            static auto-open booleans — `refute src =~ ~r/open=\{@(?!auto_open_inputs|auto_open_policy)\w+\}/`.
            (Inputs/Policy use `open={@auto_open_inputs}` / `open={@auto_open_policy}`, both computed
            ONCE in the assign block; any OTHER `open={@...}` would be a post-mount disclosure assign
            and must not exist.)
       Lean on the existing `cl_disclosure` static-only-open unit test (`components_test.exs:283`)
       as the companion proof that the primitive itself never assign-drives `open`. This test is
       GREEN now (the two navigation events are allow-listed; no disclosure-toggle event exists) and
       MUST stay GREEN after the phase ships (Plan 04 adds NO `handle_event` for open/density —
       Expand/Collapse-all are pure `Phoenix.LiveView.JS` client DOM ops; density is a client hook).
    7. RAIL-03 controls — the rail header is not part of the card fn, so assert the RAIL-LEVEL
       control markup via a clean source assertion against `conversation_live.ex` (the source-grep
       approach is correct; state it cleanly). Read the source once
       (`src = File.read!("lib/cairnloop/web/conversation_live.ex")`) and assert each of these
       control-bar strings is present:
         - `src =~ "Expand all"`
         - `src =~ "Collapse all"`
         - `src =~ ~s|set_attribute({"open", ""}|`
         - `src =~ ~s|remove_attribute("open"|`
         - `src =~ ~s|to: "[data-tier='2']"|`
         - `src =~ ~s|data-density="comfortable"|`
       These are RED until Plan 04 lands the control bar + density default.

    Behaviors 1–5 and 7 target the POST-restructure shape and will fail until Waves 1–3 ship.
    Behavior 6 is GREEN now and stays green. Run the suite at the end and CONFIRM: behavior-6 test
    PASSES (green); the other six FAIL RED (not erroring-on-compile) — the runtime-dispatch idiom
    (`Function.capture`) keeps `--warnings-as-errors` clean even though the target markup does not
    exist yet.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5</automated>
    <automated>mix test test/cairnloop/web/conversation_live_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 (runtime dispatch keeps the build clean).
    - The new `describe "governed_action_card/1 — Phase 41 rail disclosure"` block exists INSIDE the
      `Cairnloop.Web.ConversationLiveTest` module (no new `defmodule`), with 7 tests.
    - The D-09 render-purity test (behavior 6) PASSES (green) against the current codebase — it
      allow-lists `open_review_task`/`open_manual_draft` and finds no disclosure-toggle `handle_event`
      and no non-static `open={@...}` binding. It does NOT use a `~r/.../s` whole-file substring match.
    - The other six tests (RAIL-01/02/03 structure/mechanism/D-08/controls) FAIL RED — NOT skipped,
      NOT erroring at compile. (Pre-existing tests stay green.)
    - Every new test uses `render_component(Function.capture(...), proposal: ...)` or `File.read!` —
      zero Repo round-trips, zero `# REPO-UNAVAILABLE` markers.
  </acceptance_criteria>
  <done>The 7 RAIL-01/02/03 + D-08/D-09 tests exist; behavior 6 is GREEN, the other six are RED; build is warnings-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the cl_disclosure block (RED) in components_test.exs for the data-tier passthrough</name>
  <read_first>
    - test/cairnloop/web/components_test.exs:256-313 (the existing cl_disclosure describe — the
      positive open=true / negative default / token-pure tests; COPY the `rendered_to_string(~H...)` idiom)
    - lib/cairnloop/web/components.ex:193-205 (cl_disclosure/1 as-written — NO :rest today, so the
      new assertion is RED until Wave 1 adds `attr(:rest, :global)` + `{@rest}`)
    - .planning/phases/41-conversation-rail-progressive-disclosure-d2/41-PATTERNS.md
      (§"components_test.exs — extend cl_disclosure block" — the exact one-test addition)
  </read_first>
  <action>
    Add exactly ONE test to the existing cl_disclosure region. Using the existing
    `rendered_to_string(~H"...")` / `assigns = %{}` idiom, render
    `<.cl_disclosure id="inputs-scope" open={true} data-tier="2">` with a `:summary` slot and body,
    then `assert html =~ ~s(data-tier="2")`. This proves the `:global` passthrough Wave 1 will add;
    it is RED now (the attribute is silently dropped because `cl_disclosure/1` has no `:rest`). Do NOT
    modify the existing positive/negative `open` tests or the token-pure test — the static-only-open
    pair already satisfies criterion 4 ("no server assign controls open") and is the companion proof
    referenced by 41-01 behavior 6. Keep the existing `refute html =~ ~r/#[0-9a-fA-F]{3,6}/` hex-purity
    style intact for the new test too.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/components_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - A new test asserting `data-tier="2"` reaches the rendered `<details>` exists in the
      cl_disclosure region of components_test.exs.
    - That test FAILS RED now (the attribute is dropped pre-Wave-1).
    - The pre-existing `open` positive/negative + token-pure tests are unchanged and still green.
  </acceptance_criteria>
  <done>The data-tier passthrough test exists and fails RED; existing cl_disclosure tests unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds tests only; no runtime trust boundary is introduced or moved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering/Info-disclosure | Future rail `<details>` snapshot bodies (asserted-against here) | mitigate | Tests assert the structure that keeps raw snapshots behind default-closed expanders (D-02/D-22); no `raw/1`, `inspect/2`-escaped strings only. This plan only encodes the assertion; mitigation lands in Plan 03. |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §"Package Legitimacy Audit": not applicable; all deps pre-existing in mix.lock). |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/cairnloop/web/conversation_live_test.exs test/cairnloop/web/components_test.exs`
  runs; the D-09 render-purity test is GREEN, the other Phase 41 tests are RED (expected —
  production code lands Waves 1–3); pre-existing tests remain green.
</verification>

<success_criteria>
- New `describe "governed_action_card/1 — Phase 41 rail disclosure"` block (inside
  `Cairnloop.Web.ConversationLiveTest`) with 7 tests: behavior 6 GREEN, the other six RED.
- One new `data-tier` passthrough test in the cl_disclosure block, failing RED.
- Build warnings-clean; no `# REPO-UNAVAILABLE` markers; no Repo round-trip in any new test.
</success_criteria>

<output>
Create `.planning/phases/41-conversation-rail-progressive-disclosure-d2/41-01-SUMMARY.md` when done.
</output>
