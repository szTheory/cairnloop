---
phase: 40-drift-remediation-brand-token-gate-hardening
plan: 03
type: execute
wave: 2
depends_on: [40-01, 40-02]
files_modified:
  - test/cairnloop/web/brand_token_gate_test.exs
  - lib/cairnloop/credo_checks/no_hardcoded_color.ex
  - .credo.exs
autonomous: true
requirements: [GATE-01, GATE-02]
must_haves:
  truths:
    - "The ExUnit gate fails on inline style=...#hex..., raw rgba()/hsl(), and helper-returned hex in render .ex files"
    - "A # cl-allow-color magic-comment on the offending or previous line suppresses a violation (opt-in, auditable)"
    - "The gate passes on token-valued var(--cl-...) styles, .cl- utility classes, anchor hrefs, and interpolation"
    - "A complementary Credo check (Cairnloop.CredoChecks.NoHardcodedColor) flags hardcoded color in render files at dev time, advisory only"
    - "Gate scope stays lib/cairnloop/web/**/*.ex + the example app live dir (globs unchanged, .css unscanned) and the ExUnit gate remains the CI source of truth (D-06)"
    - "mix test (incl. the hardened gate over the now-remediated files) is green; mix compile --warnings-as-errors passes"
  artifacts:
    - path: "test/cairnloop/web/brand_token_gate_test.exs"
      provides: "Hardened brand-token CI gate with new patterns + allowlist + FAIL/PASS fixtures"
      contains: "cl-allow-color"
    - path: "lib/cairnloop/credo_checks/no_hardcoded_color.ex"
      provides: "Advisory dev-time Credo color check"
      contains: "use Credo.Check"
    - path: ".credo.exs"
      provides: "requires: + enabled: wiring for the custom check"
      contains: "NoHardcodedColor"
  key_links:
    - from: ".credo.exs requires:"
      to: "lib/cairnloop/credo_checks/no_hardcoded_color.ex"
      via: "Credo custom-check load path"
      pattern: "credo_checks/no_hardcoded_color"
---

<objective>
Harden the existing brand-token ExUnit gate to catch inline `style="…#hex…"`, raw `rgba()`/`hsl()`,
and helper-returned hex in render `.ex` files — anchored on `#`+color context to avoid false
positives, with a `# cl-allow-color` magic-comment allowlist — and add a complementary advisory
Credo check. The ExUnit gate stays the CI source of truth; Credo is dev-time only (GATE-01, GATE-02).

Purpose: Lock the drift door. After 40-01/40-02 made the two surfaces clean, this prevents the same
drift from silently re-entering any render file.
Output: An extended gate test, a new Credo check module, and the `.credo.exs` wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-drift-remediation-brand-token-gate-hardening/40-CONTEXT.md
@.planning/phases/40-drift-remediation-brand-token-gate-hardening/40-RESEARCH.md

<interfaces>
<!-- Verified live this session. -->

Existing gate (test/cairnloop/web/brand_token_gate_test.exs, 60 lines):
  Pure File.read! + String.split + per-line Regex.match? (NO DB — keep DB-free, Repo may be
  unavailable). Today: single pattern @hex_fallback_pattern ~r/var\(--cl-[a-z-]+,\s*#/ (BRAND-04).
  @web_dir = lib/cairnloop/web ; @example_live_dir = examples/.../live ; globs **/*.ex.
  .css is already excluded structurally (only .ex globbed) — D-05/D-06 satisfied; do not change globs.

.credo.exs (verified): top-level requires: [] (empty); checks.enabled list under "## Warnings".
  Credo version 1.7.18 (mix.lock). strict: true, 120-col.

Custom check (RESEARCH §D): module Cairnloop.CredoChecks.NoHardcodedColor at
  lib/cairnloop/credo_checks/no_hardcoded_color.ex. use Credo.Check with id:, base_priority: :low,
  category: :warning, explanations:. run/2 over %Credo.SourceFile{}, restrict to web render dirs,
  reuse the same # cl-allow-color sentinel, emit Credo issues, NEVER raise a failing exit_status
  (advisory — ExUnit gate is authoritative, D-07).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the ExUnit gate — hex/rgba/hsl detection + allowlist + FAIL/PASS fixtures</name>
  <files>test/cairnloop/web/brand_token_gate_test.exs</files>
  <read_first>
    - test/cairnloop/web/brand_token_gate_test.exs (current harness — extend, do NOT fork; D-04)
    - .planning/phases/40-drift-remediation-brand-token-gate-hardening/40-RESEARCH.md §C (regex mechanics, anchoring, sentinel, exact FAIL/PASS fixture strings)
    - lib/cairnloop/web/conversation_live.ex + lib/cairnloop/web/search_modal_component.ex (must already be clean from 40-01/40-02 — the hardened scan must find zero violations in them)
  </read_first>
  <behavior>
    - FAIL: `<div style="color:#abc">` (3-digit inline hex)
    - FAIL: `<div style="color: #4A6238;">` (6-digit inline hex)
    - FAIL: `  "background: rgba(0,0,0,0.5);"` (raw rgba in helper return string)
    - FAIL: `  "color: hsl(20, 50%, 40%);"` (raw hsl)
    - FAIL: `  defp badge, do: "#8b1a1a"` (helper-returned hex)
    - FAIL: `<div style="border:1px solid rgba(64,51,43,0.08)">` (raw rgba in attr)
    - PASS: `<div style="color: var(--cl-text)">` (token-valued inline)
    - PASS: `<button class="cl-button cl-button--primary">` (utility class)
    - PASS: `<a href="#supporting-evidence">` (anchor — non-hex char after #)
    - PASS: `phx-value-dom_id={presenter.dom_id}` (no hex; interpolation stripped)
    - PASS: `# a comment mentioning issue #1234` (#1 is 1 digit, not 3/6 — no match)
    - PASS (allowlisted): `<div style="color:#abc"> <%!-- cl-allow-color --%>` and the prev-line variant
    - SCAN-CLEAN: the two real remediated files produce ZERO violations under the hardened scan
  </behavior>
  <action>
    Extend brand_token_gate_test.exs in place (D-04 — one gate, one file). Add two new patterns: a
    bare-hex pattern `~r/#[0-9a-fA-F]{6}\b|#[0-9a-fA-F]{3}\b/` (catches inline-style hex AND
    helper-returned hex — both are just source text; D-04 a+c) and a function-color pattern
    `~r/\b(?:rgba?|hsla?)\(/` (D-04 b). Anchor to avoid false positives (D-05): (1) strip the EEx
    interpolation form on each line first via `String.replace(line, ~r/\#\{[^}]*\}/, "")` so it
    never trips the 3-hex rule; (2) the `\b` word boundary after 3/6 hex digits keeps long DOM ids
    and `#section` anchors from matching; (3) skip color-free comment-only lines (trimmed start is
    `#` and contains none of style=/color/background/rgba/hsl). Keep the existing
    @hex_fallback_pattern test intact (double-flagging the var(--cl-x,#hex) form is fine). Implement
    the `# cl-allow-color` allowlist (D-05, REQUIRED): before collecting violations, build a per-file
    set of allowed line numbers = lines containing the sentinel; a violation on line N is suppressed
    if line N OR line N-1 is in that set (covers same-line trailing comment and prev-line block
    comment). The sentinel must be opt-in per line/block — never a blanket file disable (auditable;
    threat T-40-SC). Keep the whole test a pure File.read!/string-scan — NO DB, NO Repo (Repo may be
    unavailable; add a `# REPO-UNAVAILABLE` note only if some assertion would need Postgres — none
    should). Add an explicit fixtures test: run the new pattern+anchor+allowlist logic against the
    literal FAIL/PASS strings in <behavior> as in-test string fixtures (NOT on-disk .ex files —
    those would themselves be scanned by the real gate). Assert every FAIL string is flagged, every
    PASS string is clean, and the allowlisted strings are suppressed. Keep the real-file scan
    asserting violations == [] over @web_dir + @example_live_dir (now clean post-40-01/40-02).
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/brand_token_gate_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/web/brand_token_gate_test.exs` exits 0 (real files clean + fixtures pass)
    - The fixtures reference the spec strings: grep finds 'rgba(0,0,0,0.5)', '#4A6238', 'cl-allow-color', and 'var(--cl-text)' in the test file
    - New patterns present: the test contains both a `rgba?` / `hsla?` function pattern and a bare `#[0-9a-fA-F]{6}` hex pattern
    - Pure (no DB): `grep -c 'File.read!' test/cairnloop/web/brand_token_gate_test.exs` > 0 ; `grep -c 'Repo' test/cairnloop/web/brand_token_gate_test.exs` == 0
    - @web_dir / @example_live_dir globs unchanged (.css stays unscanned — D-06)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>The gate fails on inline hex, raw rgba/hsl, and helper-returned hex; the # cl-allow-color sentinel suppresses opt-in; token/anchor/interpolation cases pass; the two remediated files scan clean; the test stays DB-free.</done>
</task>

<task type="auto">
  <name>Task 2: Add the advisory Credo check + wire it into .credo.exs</name>
  <files>lib/cairnloop/credo_checks/no_hardcoded_color.ex, .credo.exs</files>
  <read_first>
    - .credo.exs (top-level requires: [] and the checks.enabled list under "## Warnings")
    - .planning/phases/40-drift-remediation-brand-token-gate-hardening/40-RESEARCH.md §D (Credo 1.7.x use Credo.Check boilerplate, wiring, advisory config, pitfall #4)
    - deps/credo (confirm the installed 1.7.18 use Credo.Check API supports id:/base_priority:/category:/explanations: — RESEARCH A5)
    - test/cairnloop/web/brand_token_gate_test.exs (mirror the same patterns + # cl-allow-color sentinel for parity)
  </read_first>
  <action>
    Create lib/cairnloop/credo_checks/no_hardcoded_color.ex, module
    Cairnloop.CredoChecks.NoHardcodedColor: `use Credo.Check, id: "CL_NoHardcodedColor",
    base_priority: :low, category: :warning, explanations: [check: "..."]`. In run/2 over the
    %Credo.SourceFile{}, restrict to render dirs (filter source_file.filename =~ "lib/cairnloop/web/"
    or the example live dir) so it never flags non-render .ex or the canonical cairnloop.css (CSS is
    not a Credo source anyway). Reuse the SAME two regexes and the SAME `# cl-allow-color`
    line/prev-line suppression + interpolation-strip + comment-skip anchoring as the ExUnit gate
    (Task 1), so the two never diverge. Emit one Credo issue per flagged line via the standard
    IssueMeta / format_issue path. Keep it ADVISORY: base_priority: :low and DEFAULT exit_status —
    do NOT set a failing exit_status (pitfall #4 / D-07: it must not become a divergent second hard
    gate; the ExUnit gate is authoritative; duplicate coverage is acceptable/harmless). Wire into
    .credo.exs: (1) set top-level `requires: ["lib/cairnloop/credo_checks/no_hardcoded_color.ex"]`
    (Credo loads custom checks via requires:); (2) add
    `{Cairnloop.CredoChecks.NoHardcodedColor, [priority: :low]}` to the checks.enabled list under the
    "## Warnings" group. The module is ordinary Elixir — must survive mix compile
    --warnings-as-errors (no unused vars/aliases). The full default `mix credo --strict` must remain
    green (the two render files are already remediated; the check finds nothing to flag there).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -2; mix credo list --only Cairnloop.CredoChecks.NoHardcodedColor 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - lib/cairnloop/credo_checks/no_hardcoded_color.ex exists and contains `use Credo.Check` + `base_priority: :low`
    - .credo.exs `requires:` lists `lib/cairnloop/credo_checks/no_hardcoded_color.ex` and the enabled list contains `Cairnloop.CredoChecks.NoHardcodedColor`
    - `mix compile --warnings-as-errors` exits 0
    - `mix credo --strict` exits 0 (clean; the check is advisory and finds nothing in the remediated files) — confirm the new check name appears in `mix credo list`
    - The check does NOT set a failing exit_status of its own (grep the module: no `exit_status:` raising config; only base_priority/category)
  </acceptance_criteria>
  <done>A Cairnloop.CredoChecks.NoHardcodedColor check exists, is wired into .credo.exs via requires: + enabled:, surfaces under mix credo --strict as advisory (low priority, no hard fail), and the build stays warnings-clean with credo green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate / dev lint -> codebase | The gate and Credo check are CI/dev tooling that scan source text; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-SC | Tampering | `# cl-allow-color` allowlist sentinel | mitigate | The sentinel is a fail-OPEN escape hatch. Mitigation: it suppresses only the line it is on OR the immediately-preceding line (opt-in, per-line/block) — never a blanket file/module disable. A reviewer can grep `cl-allow-color` to audit every exception. The plan adds ZERO sentinels to the remediated files (D-05: it exists for future intentional cases, not to grandfather current drift), so abuse is visible in diff review. |

No package installs in this phase (ExUnit + Credo already present) — no Package Legitimacy Gate
required. The gate test is a pure File.read!/string scan: no DB, network, auth, or input-handling
surface. The Credo check is ordinary dev-time Elixir.
</threat_model>

<verification>
- `mix test test/cairnloop/web/brand_token_gate_test.exs` → exit 0 (fixtures + real-file scan)
- `mix test` → green overall (no commit boundary leaves it red — the files were remediated in wave 1 first)
- `mix compile --warnings-as-errors` → exit 0
- `mix credo --strict` → exit 0; `mix credo list` shows Cairnloop.CredoChecks.NoHardcodedColor
- `grep -rn 'cl-allow-color' lib/cairnloop/web/` → empty (no sentinel used to grandfather drift)
</verification>

<success_criteria>
The ExUnit gate fails on inline hex / raw rgba/hsl / helper-returned hex, passes on
token/anchor/interpolation cases, supports an auditable `# cl-allow-color` opt-in allowlist, and
scans the two remediated files clean. A complementary advisory Credo check exists and is wired into
`.credo.exs` without becoming a second hard gate. `mix test`, `mix compile --warnings-as-errors`,
and `mix credo --strict` all pass.
</success_criteria>

<output>
Create `.planning/phases/40-drift-remediation-brand-token-gate-hardening/40-03-SUMMARY.md` when done
</output>
