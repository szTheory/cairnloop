---
phase: 40-drift-remediation-brand-token-gate-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/web/conversation_live.ex
autonomous: true
requirements: [DRIFT-01, DRIFT-02]
must_haves:
  truths:
    - "conversation_live.ex contains zero off-palette hardcoded hex (no #hex, no rgba/hsl)"
    - "The approve/reject/defer footer renders via cl_button variants (primary/danger/default) and .cl-textarea"
    - "Remediated text/border colors respond to dark-mode (token-valued, not hex-locked)"
    - "mix compile --warnings-as-errors passes after the rebuild"
  artifacts:
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "On-palette conversation rail + token/primitive-based approval footer"
      contains: "cl_button"
  key_links:
    - from: "approve/reject/defer footer"
      to: "cl_button / .cl-textarea primitives"
      via: "Phoenix.Component function-component calls"
      pattern: "<\\.cl_button variant=\"(primary|danger|default)\""
---

<objective>
Remediate every off-palette hardcoded hex in `lib/cairnloop/web/conversation_live.ex` per the D-02
hex→token map, and rebuild the hand-rolled approve/reject/defer footer with `cl_button` variants +
the shared `.cl-textarea` class, migrating bespoke inline-layout `style=` to existing `.cl-`
utilities (DRIFT-01, DRIFT-02).

Purpose: This is one of the two largest drift surfaces. Tokenizing it makes the conversation view
respond to dark-mode and lets the hardened gate (40-03) land green.
Output: A fully on-palette `conversation_live.ex` with a primitive-built approval footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-drift-remediation-brand-token-gate-hardening/40-CONTEXT.md
@.planning/phases/40-drift-remediation-brand-token-gate-hardening/40-RESEARCH.md

<interfaces>
<!-- Verified live this session — use directly, no codebase exploration needed. -->

cl_button (lib/cairnloop/web/components.ex:30):
  attr :variant — values: ~w(default primary danger ghost)   ← NO "warning" variant exists.
  attr :size    — values: ~w(sm md lg), default "md"
  Renders <button class="cl-button cl-button--{variant} ..."> with token colors.
  Pass through phx-click / phx-value-* / type via :rest global attr.

.cl-textarea (priv/static/cairnloop.css:384): shared input class. Carries token border
  (var(--cl-border-strong) on hover), aria-invalid danger state, padding. Replaces both bespoke
  <textarea style="…border:#c38f57…">.

Layout utilities (priv/static/cairnloop.css:430-442):
  .cl-stack            display:flex; flex-direction:column; gap:8px (--cl-space-3)
  .cl-stack--lg        gap:16px (--cl-space-5)
  .cl-row              display:flex; align-items:center; gap:8px
  .cl-row--wrap        flex-wrap:wrap
  Spacing tokens: --cl-space-2=4px, --cl-space-3=8px, --cl-space-4=12px, --cl-space-5=16px.
  Text color utilities: ONLY .cl-text-muted (color:var(--cl-text-muted)); .cl-text-small/-micro
  set font-size only. There is NO .cl-text class — body color must use token-valued inline style.

on-primary token (priv/static/cairnloop.css:48-49): --cl-primary-text=#FFFFFF; alias --cl-on-primary.
  cl_button variant="primary" already paints color:var(--cl-primary-text) — #fffdf8 is absorbed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize the conversation rail prose + divider hex (non-footer)</name>
  <files>lib/cairnloop/web/conversation_live.ex</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex (read the current file — line numbers below are from
      a 2026-06-04 grep and may have drifted; work from current state)
    - .planning/phases/40-drift-remediation-brand-token-gate-hardening/40-RESEARCH.md §B (conversation hex inventory table)
    - priv/static/cairnloop.css (canonical token list — confirm --cl-text / --cl-text-muted / --cl-border exist)
  </read_first>
  <action>
    Apply the D-02 hex→token map to every non-footer hex in conversation_live.ex. Per RESEARCH §B:
    line ~792 `border-top: 1px solid #e5e7eb` → `border-top: 1px solid var(--cl-border)` (keep the
    margin/padding inline, they are non-color → gate-PASS; do NOT invent a utility). Every
    `color: #8b7355` (summaries/no-history/helper prose at ~1002, 1019, 1033, 1049) → migrate the
    element to `class="cl-text-muted"` (preferred; it sets color:var(--cl-text-muted)) and keep
    any font-size as `.cl-text-small`/`.cl-text-micro` where it maps, OR if a class migration is
    awkward inline, use token-valued `style="color:var(--cl-text-muted)"`. Every body `color: #4c4033`
    (event detail body ~1020, scope prose ~1040, policy prose ~1046) → token-valued inline
    `style="color:var(--cl-text)"` (there is NO .cl-text class — do not write `class="cl-text"`, it
    would silently no-op and drop the color; per RESEARCH §F gap #2). `font-style:italic` /
    `font-weight` / `cursor:pointer` / `font-size` are non-color and gate-PASS — leave them inline
    or fold font-size into a `.cl-text-*` class as convenient. Do NOT touch the footer block
    (~1070–1114) — that is Task 2. Do NOT add tokens or CSS classes (D-01).
  </action>
  <verify>
    <automated>grep -nE '#[0-9a-fA-F]{3,6}|rgba\(|hsl\(' lib/cairnloop/web/conversation_live.ex | grep -vE '^[0-9]+:.*107[0-9]|^[0-9]+:.*108[0-9]|^[0-9]+:.*109[0-9]|^[0-9]+:.*110[0-9]|^[0-9]+:.*111[0-9]'</automated>
  </verify>
  <acceptance_criteria>
    - Above grep returns no matches OUTSIDE the footer region (only footer hex ~1070-1114 may remain pre-Task-2)
    - `mix compile --warnings-as-errors` exits 0
    - No `class="cl-text"` appears anywhere (grep -c 'class="cl-text"' returns 0) — the body-color path is inline var()
    - No new `--cl-` token or `.cl-` class definition was added to priv/static/cairnloop.css (git diff shows that file untouched)
  </acceptance_criteria>
  <done>All non-footer hex in conversation_live.ex is replaced by var(--cl-…) tokens or token utility classes; build is warnings-clean; cairnloop.css is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild approve/reject/defer footer with cl_button + .cl-textarea + .cl- utilities</name>
  <files>lib/cairnloop/web/conversation_live.ex</files>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex (the footer block — currently ~1070-1114; read current state)
    - lib/cairnloop/web/components.ex (cl_button attrs ~line 20-63 — confirm variant values + :rest passthrough)
    - priv/static/cairnloop.css:384 (.cl-textarea), :430-435 (.cl-row/.cl-stack)
    - .planning/phases/40-drift-remediation-brand-token-gate-hardening/40-RESEARCH.md §A (conversation footer → utility map) + §F gaps #1, #3
  </read_first>
  <action>
    Replace the hand-rolled footer markup with primitives. Approve: delete the inline `<button
    style="…#fffdf8…">` and render `<.cl_button variant="primary" phx-click="approve_action"
    phx-value-…={…}>Approve</.cl_button>` — preserve the EXACT existing phx-click event name and
    phx-value-* attrs the LiveView already handles (read them off the current button; do not rename
    events). Reject button → `<.cl_button variant="danger" type="submit">Reject</.cl_button>`
    inside its existing `<form>`; the maroon `#8b1a1a`/`#fdecea` colors come from
    `.cl-button--danger` tokens — delete the inline color style. Defer button →
    `<.cl_button variant="default" type="submit">Defer</.cl_button>` — there is NO `warning`
    variant (RESEARCH §F gap #1; passing variant="warning" raises a compile-time attr error and
    fails --warnings-as-errors). D-03 explicitly sanctions "default/warning = defer"; use
    `variant="default"`. Both `<textarea style="…border:#c38f57…">` controls → `class="cl-textarea"`,
    dropping all inline style (keep `name`, `phx-*`, `placeholder`, `required` attrs). Migrate the
    footer container `display:flex;flex-direction:column;gap:12px` → `class="cl-stack"` (8px) or
    accept token-valued `style="gap:var(--cl-space-4)"` for exact 12px; the button row
    `display:flex;flex-wrap:wrap;gap:8px;align-items:flex-start` → `class="cl-row cl-row--wrap"`
    (note: `.cl-row` centers items — if flex-start matters visually, add token-safe
    `style="align-items:flex-start"`, a non-color style that is gate-PASS). The "Approval required"
    label `color:#4c4033` → token-valued `style="color:var(--cl-text)"` (no .cl-text class). The
    trailing helper text `color:#8b7355;font-style:italic` → `class="cl-text-muted cl-text-small"`
    keeping `font-style:italic` inline (non-color, gate-PASS). Do NOT invent utility classes
    (D-03); use token-valued inline only where no existing utility fits.
  </action>
  <verify>
    <automated>grep -nE '#[0-9a-fA-F]{3,6}|rgba\(|hsl\(' lib/cairnloop/web/conversation_live.ex; echo "exit:$?"; grep -c '<\.cl_button' lib/cairnloop/web/conversation_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,6}|rgba\(|hsl\(' lib/cairnloop/web/conversation_live.ex` returns ZERO matches (whole file now clean — Task 1 + Task 2 complete)
    - `grep -c '<\.cl_button' lib/cairnloop/web/conversation_live.ex` returns at least 3 (approve/reject/defer)
    - `grep -c 'class="cl-textarea"' lib/cairnloop/web/conversation_live.ex` returns at least 2
    - No `variant="warning"` appears (grep -c 'variant="warning"' returns 0)
    - `mix compile --warnings-as-errors` exits 0
    - The approve/reject/defer phx-click / form submit event names are unchanged from the pre-edit file (diff shows markup-only change, same event atoms)
  </acceptance_criteria>
  <done>Footer renders via cl_button (primary/danger/default) + .cl-textarea; whole file is hex/rgba-free; existing approval events preserved; build warnings-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → approve/reject/defer form | Existing form POST — unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | approve/reject/defer footer | accept | Markup-only refactor: phx-click event names, phx-value-* params, form `name`/`required` attrs are preserved verbatim. No change to server-side handle_event/handle_params authz — acceptance criteria assert event atoms are unchanged. No new input handling introduced. |

No new threat surface: this plan swaps CSS color strings and replaces hand-styled buttons/textareas
with already-tested primitives. No new dependency, DB, network, auth, or input-validation path.
</threat_model>

<verification>
- `grep -nE '#[0-9a-fA-F]{3,6}|rgba\(|hsl\(' lib/cairnloop/web/conversation_live.ex` → empty
- `mix compile --warnings-as-errors` → exit 0
- `mix test test/cairnloop/web/brand_token_gate_test.exs` → still green (existing gate)
- Manual dark-mode spot check (RESEARCH §E): set `<html data-theme="dark">`; footer + rail prose flip to dark palette.
</verification>

<success_criteria>
conversation_live.ex carries zero off-palette hex/rgba; the approval footer is built from
cl_button + .cl-textarea + .cl- utilities; priv/static/cairnloop.css is untouched (D-01);
`mix compile --warnings-as-errors` and the existing gate both pass.
</success_criteria>

<output>
Create `.planning/phases/40-drift-remediation-brand-token-gate-hardening/40-01-SUMMARY.md` when done
</output>
