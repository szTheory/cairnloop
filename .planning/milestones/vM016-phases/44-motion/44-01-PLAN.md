---
phase: 44-motion
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/cairnloop/web/motion_css_test.exs
  - examples/cairnloop_example/test/e2e/motion_test.exs
autonomous: true
requirements: [MOTION-01, MOTION-02]
nyquist_compliant: true

must_haves:
  truths:
    - "A DB-free unit test asserts no new motion rule names a forbidden layout property (width/height/top/left/max-height/max-width)"
    - "The unit test asserts every new @keyframes / .cl-motion-* / .cl-toast block exists in BOTH stylesheets (mirror parity)"
    - "The unit test asserts .cl-hero__count / .cl-stat__count rules carry no transition-property"
    - "An @moduletag :e2e Playwright test asserts hero entrance animation-name + duration <180ms, rail reveal transform/opacity only, reply-send has no cl-motion-* class, and reduced-motion zeroes transforms while .cl-motion-state stays 120ms"
  artifacts:
    - path: "test/cairnloop/web/motion_css_test.exs"
      provides: "DB-free CSS string-scan gate for motion rules (forbidden-property + mirror-parity + count-tick no-transition)"
      contains: "defmodule Cairnloop.Web.MotionCssTest"
    - path: "examples/cairnloop_example/test/e2e/motion_test.exs"
      provides: "Playwright E2E for computed motion behavior + reduced-motion"
      contains: "@moduletag :e2e"
  key_links:
    - from: "test/cairnloop/web/motion_css_test.exs"
      to: "priv/static/cairnloop.css + examples/cairnloop_example/priv/static/assets/css/app.css"
      via: "File.read! string scan of both files"
      pattern: "File.read!"
    - from: "examples/cairnloop_example/test/e2e/motion_test.exs"
      to: "rendered cockpit DOM"
      via: "PhoenixTest.Playwright evaluate/3 getComputedStyle"
      pattern: "getComputedStyle"
---

<objective>
Create the two Wave-0 Nyquist test files BEFORE any motion CSS or markup lands. The unit test is a DB-free `File.read!` string scan modeled on `brand_token_gate_test.exs`; the E2E is a Playwright computed-style test modeled on `rail_disclosure_test.exs`. Both start RED (no motion rules exist yet) and turn GREEN as Waves 1–2 ship.

Purpose: Per the phase Validation Strategy, every implementation task that touches motion CSS verifies against these scans. Authoring them first makes the feedback loop real instead of retrofitted.
Output: `test/cairnloop/web/motion_css_test.exs` (unit) and `examples/cairnloop_example/test/e2e/motion_test.exs` (E2E).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-motion/44-VALIDATION.md
@.planning/phases/44-motion/44-PATTERNS.md
@.planning/phases/44-motion/44-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the DB-free motion CSS string-scan unit test (RED until Wave 1)</name>
  <read_first>
    - test/cairnloop/web/brand_token_gate_test.exs (lines 1-48 module head + path-resolution + read-and-scan idiom; lines ~166-198 the per-file `File.read! |> String.split("\n") |> Enum.with_index(1)` violation-collection loop). COPY this DB-free pure-File.read! shape exactly, including the `# REPO-UNAVAILABLE` moduledoc note.
    - .planning/phases/44-motion/44-PATTERNS.md (section "test/cairnloop/web/motion_css_test.exs (NEW test...)" — exact path-resolution + assertions to encode)
    - .planning/phases/44-motion/44-RESEARCH.md ("Wave 0 Gaps" + "Two-File CSS Mirror" + "Negative Assertions" tables for the exact forbidden-property list and the two stylesheet paths/line divergence: lib 855 lines vs example 4166 lines)
    - priv/static/cairnloop.css (lines 142-211 — confirm the token block + reduced-motion block already exist so the test scans only NEW motion rule names, not the shipped tokens)
  </read_first>
  <action>
    Create `test/cairnloop/web/motion_css_test.exs` as `defmodule Cairnloop.Web.MotionCssTest` using `use ExUnit.Case, async: true`, with a `# REPO-UNAVAILABLE` moduledoc note (pure File.read!, no Postgres). Resolve both stylesheet paths with `Path.expand(..., __DIR__)`: `@lib_css` → `../../../priv/static/cairnloop.css`, `@example_css` → `../../../examples/cairnloop_example/priv/static/assets/css/app.css`.

    Encode three assertion groups. (a) FORBIDDEN-LAYOUT-PROPERTY scan: for each NEW motion rule name (`cl-enter-up`, `cl-reveal-x`, `cl-toast-enter`, `cl-toast-exit`, `.cl-motion-enter`, `.cl-motion-reveal`, `.cl-toast`, `.cl-list-stagger`), assert the lines belonging to those blocks contain none of `width`/`height`/`top`/`left`/`max-height`/`max-width` as animated/transitioned properties — scope the scan to the new motion blocks (e.g. slice from the block opener to its closing brace), never the whole file, so pre-existing layout CSS is not flagged. (b) MIRROR-PARITY: for each new keyframe/class TOKEN string, assert it is present in BOTH `@lib_css` and `@example_css` (substring presence in each). (c) COUNT-TICK NO-TRANSITION: assert the `.cl-hero__count` and `.cl-stat__count` rule bodies contain no `transition-property` (and no `transition:` shorthand) — keeping count ticks instant per the criterion-3 negative assertion.

    The test is authored against the FINAL rule names (those Wave 1 will create), so it is RED now and turns GREEN when Plan 02 lands the CSS. Do NOT weaken assertions to pass against the current file. Mark group-(b) parity assertions so a missing mirror copy fails loudly.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/motion_css_test.exs 2>&1 | tail -20 # EXPECTED RED at Wave 0 (motion rules not yet authored); confirms the test exists, compiles, and asserts against the final rule names</automated>
  </verify>
  <acceptance_criteria>
    - File `test/cairnloop/web/motion_css_test.exs` exists, compiles warnings-clean, defines `Cairnloop.Web.MotionCssTest`.
    - Uses `Path.expand(..., __DIR__)` to resolve BOTH `priv/static/cairnloop.css` AND `examples/cairnloop_example/priv/static/assets/css/app.css`; reads them with `File.read!` (no Repo, no DB).
    - Contains exactly the three assertion groups: forbidden-layout-property scan over the NEW motion blocks only; cross-file mirror parity for each new keyframe/class token; `.cl-hero__count`/`.cl-stat__count` carry no `transition-property`/`transition:`.
    - Running it at Wave 0 fails RED (motion rules absent) — NOT a false green from over-broad matching.
  </acceptance_criteria>
  <done>The motion CSS unit test exists, compiles, and asserts the three groups against the final rule names; it is RED pending Wave 1 CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Playwright motion E2E (computed-style + reduced-motion) — @moduletag :e2e</name>
  <read_first>
    - examples/cairnloop_example/test/e2e/rail_disclosure_test.exs (lines 1-45 — module head, `use PhoenixTest.Playwright.Case, async: false`, `@moduletag :e2e`, `import CairnloopExample.RailFixtures`, `setup` fixture, the `visit/2 |> assert_has("body .phx-connected")` connect gate; and the `evaluate/3` computed-style shape used later in the file). COPY header + fixture + connect-gate exactly.
    - .planning/phases/44-motion/44-PATTERNS.md (section "examples/cairnloop_example/test/e2e/motion_test.exs (NEW E2E)" — the evaluate/getComputedStyle shape and the exact assertions to encode)
    - .planning/phases/44-motion/44-RESEARCH.md ("Validation Architecture" → Phase Requirements → Test Map rows for MOTION-01 / MOTION-01(negative) / MOTION-02; "Negative Assertions" table for the reply-send/⌘K nuance — assert ABSENCE of `cl-motion-*` classes, NOT zero transitions)
    - lib/cairnloop/web/conversation_live.ex (lines 458, 480, 485 — the `.message-status-chip`, the "Send Reply" `<.cl_button type="submit">`, and the `.evidence-rail` selectors the E2E will query)
    - lib/cairnloop/web/home_live.ex + lib/cairnloop/web/components.ex:171 (the `.cl-hero__count` selector for the hero animation assertion)
  </read_first>
  <action>
    Create `examples/cairnloop_example/test/e2e/motion_test.exs` as `defmodule CairnloopExampleWeb.MotionE2ETest`, `use PhoenixTest.Playwright.Case, async: false`, `@moduletag :e2e`, importing the appropriate fixture (model on `rail_disclosure_test.exs`'s `import CairnloopExample.RailFixtures` + `setup` returning a `conv_id`; reuse `pending_governed_action_conversation/0` so the conversation page with `.evidence-rail` and a `.message-status-chip` renders).

    Encode these tests, each visiting the relevant route and gating on `assert_has("body .phx-connected")` before any `evaluate/3` computed-style read. (1) HERO ENTRANCE: on the Home route, `getComputedStyle(.cl-hero__count).animationName` == `"cl-enter-up"` and `animationDuration` parses to < 180ms; and the count text node has empty/`all 0s`-equivalent `transitionProperty` (no real transition). (2) RAIL REVEAL: on the conversation route, the `.evidence-rail` computed `animationName` == `"cl-reveal-x"` and its motion uses transform/opacity only (assert `animationName` present; assert no `width`/`max-height` in any transition on the element). (3) STAGGER ≤5: assert the inbox `.cl-list-stagger > li:nth-child(1..5)` carry an `animationDelay` and that `:nth-child(6)` does not (delay 0). (4) NEGATIVE: the "Send Reply" submit button's `className` contains NONE of `cl-motion-enter`/`cl-motion-reveal`/`cl-toast-enter`/`cl-toast-exit`/`cl-list-stagger` — assert absence of NEW motion classes only, do NOT assert it has zero transitions (the universal `.cl-button` micro-affordance is shipped and allowed). (5) REDUCED-MOTION (MOTION-02): call the Playwright `emulateMedia({reducedMotion: 'reduce'})` equivalent, then assert transform animations resolve to ~0.01ms while `.cl-motion-state` `transitionDuration` stays `120ms`.

    Author against the FINAL selectors/class names. The E2E verifies at wave-merge (gated `e2e` lane); it may not run locally without Postgres — that is expected per the repo E2E memory.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example && mix compile --warnings-as-errors 2>&1 | tail -5 # E2E file compiles; full run happens on the gated e2e lane at wave merge (mix test.e2e), Postgres not required to compile</automated>
  </verify>
  <acceptance_criteria>
    - File `examples/cairnloop_example/test/e2e/motion_test.exs` exists, compiles warnings-clean, defines `CairnloopExampleWeb.MotionE2ETest` with `use PhoenixTest.Playwright.Case` and `@moduletag :e2e`.
    - Header/fixture/connect-gate (`assert_has("body .phx-connected")`) copied from `rail_disclosure_test.exs`.
    - Contains the five tests: hero `animation-name: cl-enter-up` + duration <180ms + no count-tick transition; rail `cl-reveal-x` transform/opacity-only; stagger on ≤5 `<li>`; reply-send button carries NONE of the new `cl-motion-*` classes (absence assertion, not zero-transition); reduced-motion zeroes transforms but keeps `.cl-motion-state` at 120ms.
    - The negative test asserts ABSENCE of the new `cl-motion-*` classes (not "zero transitions"), matching the criterion-3 nuance.
  </acceptance_criteria>
  <done>The Playwright motion E2E exists, compiles, and encodes the five computed-style + reduced-motion assertions against the final selectors; it verifies on the gated e2e lane at wave merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave-0 test files only. No runtime surface, no input handling, no rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | motion_css_test / motion_test (test assets only) | accept | Test files execute only in CI/dev; no production surface, no user input, no secrets. Static `File.read!`/browser-eval against own repo files. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (ExUnit + Playwright lane already present). RESEARCH "Package Legitimacy Audit" = N/A; no registry interaction. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean for the lib test; `cd examples/cairnloop_example && mix compile --warnings-as-errors` clean for the E2E.
- `mix test test/cairnloop/web/motion_css_test.exs` is RED at Wave 0 (motion rules not yet authored) — this is expected and correct (it goes GREEN at Plan 02).
- The E2E runs on the gated `e2e` lane (`mix test.e2e`) at wave merge; local Postgres not required to compile it.
</verification>

<success_criteria>
- Both Wave-0 test files exist and compile warnings-clean.
- The unit test encodes forbidden-layout-property + mirror-parity + count-tick-no-transition assertions against the FINAL motion rule names (RED now, GREEN after Plan 02).
- The E2E encodes the five computed-style + reduced-motion assertions, with the negative assertion checking ABSENCE of new `cl-motion-*` classes.
</success_criteria>

<output>
Create `.planning/phases/44-motion/44-01-SUMMARY.md` when done. Note that the unit test is intentionally RED at Wave 0 and the E2E is verified on the gated e2e lane at wave merge.
</output>
