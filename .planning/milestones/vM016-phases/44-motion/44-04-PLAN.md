---
phase: 44-motion
plan: 04
type: execute
wave: 2
depends_on: ["44-02"]
files_modified:
  - lib/cairnloop/web/inbox_live.ex
  - lib/cairnloop/web/conversation_live.ex
autonomous: true
requirements: [MOTION-01, MOTION-02]
nyquist_compliant: true

must_haves:
  truths:
    - "The inbox list applies a ≤5-item first-paint stagger (cl-list-stagger on the <ul>); it does NOT replay on attribute-only patches (bulk-select/filter)"
    - "The evidence-rail reveals via a one-shot mount entrance (cl-motion-reveal, translateX+opacity, 260ms) — additive, P41 rail not restructured"
    - "The outbound message-status-chip carries .cl-motion-state so pending→sent/failed cross-fades at 180ms (survives reduced-motion at 120ms)"
    - "The status chip renders DISTINCT label text per state (Pending/Sent/Failed) — motion supplements, never replaces, the textual state (§7.5)"
    - "The reply-send button carries NO new cl-motion-* class (no new send-specific entrance/state motion)"
  artifacts:
    - path: "lib/cairnloop/web/inbox_live.ex"
      provides: "cl-list-stagger container class on the inbox <ul>"
      contains: "cl-list-stagger"
    - path: "lib/cairnloop/web/conversation_live.ex"
      provides: "cl-motion-reveal on .evidence-rail (phx-mounted) + cl-motion-state on .message-status-chip"
      contains: "cl-motion-reveal"
  key_links:
    - from: "lib/cairnloop/web/inbox_live.ex <ul>"
      to: ".cl-list-stagger > li:nth-child(-n+5) (Plan 02)"
      via: "additive class on the existing <ul class=\"cl-stack ...\">"
      pattern: "cl-list-stagger"
    - from: "lib/cairnloop/web/conversation_live.ex .evidence-rail"
      to: ".cl-motion-reveal (Plan 02)"
      via: "phx-mounted={JS.transition(\"cl-motion-reveal\", time: 260)}"
      pattern: "cl-motion-reveal"
    - from: "lib/cairnloop/web/conversation_live.ex .message-status-chip"
      to: ".cl-motion-state cross-fade (Plan 02) + outbound_status_label/1 distinct text"
      via: "additive cl-motion-state class on the chip"
      pattern: "cl-motion-state"
---

<objective>
Wire the three LiveView attach points that Plan 02's CSS feeds: (1) the inbox list `<ul>` gets `cl-list-stagger` for a ≤5-item first-paint stagger; (2) the `.evidence-rail` gets a one-shot `phx-mounted` reveal entrance (the LANDMINE: there is no `.cl-drawer` render — the real rail is `.evidence-rail`, mounted fresh per conversation); (3) the outbound `.message-status-chip` gets `.cl-motion-state` for the meaning-bearing gate cross-fade, with a §7.5 confirmation that the chip's label text already differs per state. All changes are ADDITIVE — the P41 rail and the sealed conversation markup are not restructured. The reply-send button must NOT receive any new motion class.

Purpose: These are the three surfaces whose motion is class-attach-only on existing markup (D-04 stagger, D-07 rail reveal, D-06 gate flip). The §7.5 check is the open research item A2 — already-satisfied per source inspection, encoded here as a guarding assertion.
Output: stagger, rail reveal, and gate cross-fade wired; reply-send confirmed motion-free; the §7.5 label distinctness asserted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-motion/44-UI-SPEC.md
@.planning/phases/44-motion/44-PATTERNS.md
@.planning/phases/44-motion/44-RESEARCH.md

<interfaces>
<!-- Extracted from the codebase; the executor should use these directly. -->

inbox_live.ex:204-206 — inbox list (PLAIN `for` comprehension, NOT a stream — verified):
  <ul class="cl-stack cl-inbox-list--bulk-clearance">
    <%= for conv <- @conversations do %>
      <li class="cl-row cl-list-row"> ... </li>

conversation_live.ex:457-460 — outbound status chip:
  <%= if outbound_status = outbound_status_label(msg) do %>
    <span class={["message-status-chip", outbound_status_class(msg)]}>
      <%= outbound_status %>
    </span>

conversation_live.ex:735-744 — outbound_status_label/1 returns DISTINCT text per state:
  "pending" -> "Pending" ; "sent" -> "Sent" ; "failed" -> "Failed" ; _ -> nil
  (so §7.5 distinct-label requirement is ALREADY satisfied — the chip renders <%= outbound_status %>)

conversation_live.ex:485 — the real rail (NO .cl-drawer is rendered anywhere):
  <div class="evidence-rail" data-density="comfortable" phx-hook=".RailDensity" id="evidence-rail-density">

conversation_live.ex:480 — reply-send: <.cl_button type="submit"> "Send Reply" — MUST stay motion-class-free.

Phoenix.LiveView.JS must be aliased/available in conversation_live.ex for JS.transition on the rail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cl-list-stagger to the inbox list (first-paint ≤5 stagger; accept navigate-back replay)</name>
  <read_first>
    - lib/cairnloop/web/inbox_live.ex lines 204-221 (the `<ul class="cl-stack cl-inbox-list--bulk-clearance">` + the `<%= for conv <- @conversations %>` comprehension + `<li class="cl-row cl-list-row">`; the bulk-select checkbox at ~211 — confirm the list is a plain comprehension, NOT `stream/3`, so morphdom replays animation only on real DOM insert not on attr-only patches)
    - priv/static/cairnloop.css (confirm `.cl-list-stagger > li:nth-child(-n+5)` + per-index delays exist from Plan 02)
    - .planning/phases/44-motion/44-PATTERNS.md "lib/cairnloop/web/inbox_live.ex:204-221 — list stagger" (additive class only; the D-05 INSERT-ONLY trade-off to carry into the summary)
    - .planning/phases/44-motion/44-RESEARCH.md "Pattern 2" + "Open Questions" #2 (the auto-decision: ship `nth-child` first-paint stagger; ACCEPT navigate-back replay as calm/bounded ≤200ms/≤5 items; do NOT convert to streams — over-engineering churns the sealed list)
  </read_first>
  <action>
    Add the `cl-list-stagger` class to the existing inbox `<ul class="cl-stack cl-inbox-list--bulk-clearance">` (additive — keep the existing classes). The CSS `nth-child(-n+5)` delay rules (Plan 02) drive the ≤5-item stagger on the existing `<li>` rows; the animation fires on DOM insert and morphdom does not replay it on attribute-only patches (bulk-select toggle, filter, count change). Do NOT convert the list to a `stream/3` and do NOT restructure the markup — the list is a sealed working surface and stream conversion is explicitly out of scope (over-engineering).

    ACCEPTED TRADE-OFF (D-05, surface in the summary for cheap owner veto): because the list is a plain `for` comprehension, LiveView re-mounts the whole list on navigate-away-and-back, so the first-paint stagger WILL replay on return. This is accepted as calm and bounded (≤5 items, ≤200ms total) rather than converting to streams. Do not add a guard that churns the list.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && grep -n 'cl-list-stagger' lib/cairnloop/web/inbox_live.ex && mix test test/cairnloop/web/brand_token_gate_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The inbox `<ul>` carries `cl-list-stagger` IN ADDITION to its existing `cl-stack cl-inbox-list--bulk-clearance` classes.
    - The `<li>` markup and the `for` comprehension are unchanged (no stream conversion, no restructure).
    - `mix compile --warnings-as-errors` clean; brand-token gate GREEN (no hex introduced).
    - The accepted navigate-back replay trade-off is documented in the plan summary.
  </acceptance_criteria>
  <done>Inbox `<ul>` carries `cl-list-stagger`; ≤5-item first-paint stagger active; list not converted to streams; trade-off noted.</done>
</task>

<task type="auto">
  <name>Task 2: Add the one-shot reveal entrance to .evidence-rail (no .cl-drawer exists)</name>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex line 485 (`<div class="evidence-rail" data-density="comfortable" phx-hook=".RailDensity" id="evidence-rail-density">` — the real rail; `.cl-drawer` is rendered NOWHERE so the reveal is a one-shot mount entrance, additive only)
    - lib/cairnloop/web/conversation_live.ex module head (confirm `Phoenix.LiveView.JS` is aliased/available; if not, add `alias Phoenix.LiveView.JS` — model on search_modal_component.ex:9)
    - lib/cairnloop/web/search_modal_component.ex line 65 (the existing `phx-mounted={JS.focus()}` one-shot — `JS.transition/2` is the same family)
    - priv/static/cairnloop.css (confirm `.cl-motion-reveal` = `cl-reveal-x` translateX+opacity at `--cl-dur-panel`/`--cl-ease-drawer` exists from Plan 02)
    - .planning/phases/44-motion/44-RESEARCH.md "Rail/Drawer Reveal (D-07) — landmine" + .planning/phases/44-motion/44-PATTERNS.md "(b) Rail reveal — one-shot mount" (additive `phx-mounted`; do NOT restructure the P41-sealed rail; do NOT add width/max-height transitions)
  </read_first>
  <action>
    Ensure `Phoenix.LiveView.JS` is aliased in `conversation_live.ex` (add `alias Phoenix.LiveView.JS` if absent). Add `phx-mounted={JS.transition("cl-motion-reveal", time: 260)}` to the existing `.evidence-rail` div (line 485) — additive attribute, keep `data-density`, `phx-hook=".RailDensity"`, and `id="evidence-rail-density"` intact. The `.cl-motion-reveal` class (Plan 02) runs the `cl-reveal-x` keyframe (translateX(16px)→0 + opacity) at 260ms `--cl-dur-panel` / `--cl-ease-drawer`. This is the auto-decided interpretation of D-07's "rail/drawer reveal" (no `.cl-drawer` is rendered anywhere; the rail mounts fresh per conversation). Do NOT add any `width`/`max-height`/`top`/`left` transition and do NOT restructure the P41-sealed rail. Flag the interpretation in the summary.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && grep -n 'phx-mounted=.*JS.transition."cl-motion-reveal"' lib/cairnloop/web/conversation_live.ex && grep -n 'phx-hook=".RailDensity"' lib/cairnloop/web/conversation_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - The `.evidence-rail` div carries `phx-mounted={JS.transition("cl-motion-reveal", time: 260)}`, with its existing `data-density`/`phx-hook=".RailDensity"`/`id` attributes intact.
    - `Phoenix.LiveView.JS` is aliased/available in the module.
    - No `width`/`max-height`/`top`/`left` transition or rail restructure introduced.
    - `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>The evidence-rail reveals via a 260ms one-shot `cl-motion-reveal` mount entrance; P41 rail structure and density hook untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add .cl-motion-state to the status chip + assert §7.5 distinct labels + confirm reply-send motion-free</name>
  <read_first>
    - lib/cairnloop/web/conversation_live.ex lines 457-460 (`<span class={["message-status-chip", outbound_status_class(msg)]}><%= outbound_status %></span>` — the gate-flip attach point; add `cl-motion-state` to the class list)
    - lib/cairnloop/web/conversation_live.ex lines 735-744 (`outbound_status_label/1` — VERIFY it returns distinct text: "pending"→"Pending", "sent"→"Sent", "failed"→"Failed"; this satisfies §7.5 distinct-label-per-state, so motion only supplements)
    - lib/cairnloop/web/conversation_live.ex line 480 (the "Send Reply" `<.cl_button type="submit">` — confirm it carries NO new `cl-motion-*` class; the universal `.cl-button` micro-affordance is shipped and stays — criterion-3 nuance: assert ABSENCE of NEW motion, not zero transitions)
    - priv/static/cairnloop.css (confirm the `.cl-motion-state` base cross-fade transition exists from Plan 02 + the reduced-motion re-enable at 120ms is shipped at 197-211)
    - .planning/phases/44-motion/44-RESEARCH.md "Pattern 3" + "Open Questions" #1 (§7.5 A2) + "Negative Assertions" (reply-send nuance)
  </read_first>
  <action>
    Add `"cl-motion-state"` to the `.message-status-chip` class list at line 458 (additive — keep `"message-status-chip"` and the `outbound_status_class(msg)` result). On the `status-pending`→`status-sent`/`status-failed` morphdom class swap, the Plan 02 `.cl-motion-state` base transition cross-fades opacity/color/background-color/border-color at 180ms (`--cl-dur-ui`); under reduced-motion the shipped re-enable keeps it at 120ms (the one meaning-bearing survivor).

    §7.5 CONFIRMATION (research A2): verify `outbound_status_label/1` returns distinct visible text per state ("Pending"/"Sent"/"Failed") — it does (lines 735-744), and the chip renders `<%= outbound_status %>`, so the state is conveyed by TEXT, not color alone; motion only supplements. If for any reason the label text were identical across states, distinct labels MUST be added — but per source inspection no copy change is required; encode this as a guarding source-grep assertion in verify.

    NEGATIVE assertion: confirm the "Send Reply" `<.cl_button type="submit">` (line 480) carries NO `cl-motion-*` class. Do NOT add motion to it; do NOT strip its shipped universal `.cl-button` affordance. The criterion-3 assertion is "no NEW send-specific motion," not "zero transitions."
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && grep -n 'cl-motion-state' lib/cairnloop/web/conversation_live.ex && grep -nE '"pending" -> "Pending"|"sent" -> "Sent"|"failed" -> "Failed"' lib/cairnloop/web/conversation_live.ex && ! grep -nE 'cl_button type="submit".*cl-motion-|cl-motion-.*Send Reply' lib/cairnloop/web/conversation_live.ex && mix test test/cairnloop/web/brand_token_gate_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The `.message-status-chip` span carries `cl-motion-state` IN ADDITION to `message-status-chip` and the `outbound_status_class(msg)` result.
    - `outbound_status_label/1` returns distinct text per state (grep confirms "Pending"/"Sent"/"Failed") — §7.5 satisfied by text, not color alone.
    - The "Send Reply" submit button carries NO `cl-motion-*` class (negative assertion); the shipped `.cl-button` affordance is untouched.
    - `mix compile --warnings-as-errors` clean; brand-token gate GREEN.
  </acceptance_criteria>
  <done>The status chip cross-fades on state change via `.cl-motion-state` (180ms / 120ms reduced-motion); distinct per-state labels confirmed (§7.5); reply-send confirmed motion-class-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Additive class/attribute attachment on existing rendered markup. No new input, no new data flow, no auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Tampering | inbox_live / conversation_live class attachments (gate-scanned .ex) | mitigate | All edits are `.cl-*` class additions / `phx-mounted` attributes — no hex, no raw color. Verified by `mix test test/cairnloop/web/brand_token_gate_test.exs`. |
| T-44-07 | Repudiation/Info-disclosure | §7.5 state-by-color-alone | mitigate | Status chip conveys state via distinct TEXT (Pending/Sent/Failed) — motion supplements, never replaces, the textual state (brand §7.5). Asserted by source grep. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs. RESEARCH "Package Legitimacy Audit" = N/A. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/cairnloop/web/brand_token_gate_test.exs` GREEN (only `.cl-*` classes added).
- grep confirms: `cl-list-stagger` on the inbox `<ul>`; `cl-motion-reveal` on `.evidence-rail`; `cl-motion-state` on the chip; distinct status labels; NO `cl-motion-*` on the reply-send button.
- E2E (Plan 01) verifies rail reveal transform/opacity-only, stagger ≤5, reply-send absence-of-motion, and reduced-motion behavior on the gated `e2e` lane at wave merge.
</verification>

<success_criteria>
- Inbox list: ≤5-item first-paint stagger (no stream conversion; accepted navigate-back replay).
- Rail: one-shot 260ms reveal entrance, additive, P41 structure intact.
- Gate chip: 180ms cross-fade via `.cl-motion-state` (120ms reduced-motion); distinct per-state labels (§7.5).
- Reply-send: no new motion class.
</success_criteria>

<output>
Create `.planning/phases/44-motion/44-04-SUMMARY.md` when done. Record the two auto-decisions for owner visibility: (1) rail "reveal" interpreted as a one-shot `.evidence-rail` mount entrance (no `.cl-drawer` exists); (2) inbox stagger ships as `nth-child` first-paint with ACCEPTED navigate-back replay (no stream conversion). Confirm §7.5 labels were already distinct (no copy change needed).
</output>
