---
phase: 44-motion
plan: 03
type: execute
wave: 2
depends_on: ["44-02"]
files_modified:
  - lib/cairnloop/web/components.ex
  - examples/cairnloop_example/lib/cairnloop_example_web/components/layouts.ex
autonomous: true
requirements: [MOTION-01, MOTION-02]
nyquist_compliant: true

must_haves:
  truths:
    - "The hero count animates once on mount (cl-motion-enter via phx-mounted) and does NOT replay or transition on live count ticks"
    - "A reusable lib component cl_flash/1 renders flash as a brand .cl-toast that enters on mount and exits faster on dismiss"
    - "cl_flash/1 renders flash text auto-escaped (HEEx {...}, never raw/1) and carries a distinct per-kind icon (not color-alone, §7.5)"
    - "The example shell flash_group renders the lib cl_flash so the toast actually mounts in the running app"
    - "cl_flash/1 and layouts.ex stay hex-free — no bare hex, no var(--cl-*, #hex) fallback, no raw rgba()/hsl() (brand-token gate passes)"
  artifacts:
    - path: "lib/cairnloop/web/components.ex"
      provides: "phx-mounted hero entrance in cl_hero/1 + new cl_flash/1 toast component"
      contains: "def cl_flash"
    - path: "examples/cairnloop_example/lib/cairnloop_example_web/components/layouts.ex"
      provides: "flash_group wired to render the lib cl_flash"
      contains: "cl_flash"
  key_links:
    - from: "lib/cairnloop/web/components.ex cl_hero/1 .cl-hero__count"
      to: ".cl-motion-enter animation (Plan 02)"
      via: "phx-mounted={JS.transition(\"cl-motion-enter\", time: 140)}"
      pattern: "phx-mounted=\\{JS.transition"
    - from: "lib/cairnloop/web/components.ex cl_flash/1"
      to: ".cl-toast / .cl-toast-enter / .cl-toast-exit (Plan 02)"
      via: "class=\"cl-toast\" + phx-mounted/phx-remove JS.transition"
      pattern: "cl-toast"
    - from: "examples layouts.ex flash_group"
      to: "Cairnloop.Web.Components.cl_flash"
      via: "import + <.cl_flash .../> call replacing/augmenting the DaisyUI flash"
      pattern: "cl_flash"
---

<objective>
Wire the component layer for two surfaces: (1) the hero count one-shot mount entrance in `cl_hero/1`, and (2) a NEW reusable lib `cl_flash/1` toast component with `.cl-toast` enter/exit motion, rendered in the example shell's `flash_group`. All new color comes via `var(--cl-*)`/`.cl-*` classes (Plan 02's CSS) — this file is brand-token-gate-scanned, so it must stay hex-free. Flash text is auto-escaped (no `raw/1`) and carries a distinct per-kind icon (§7.5).

Purpose: The hero entrance (D-03) and toast treatment (D-08, lib-elevation per component posture) both live in the lib component layer. The example `flash_group` must invoke `cl_flash` so the toast actually renders and the E2E can observe it (RESEARCH A4).
Output: `cl_hero/1` mounts the hero entrance; `cl_flash/1` exists and is rendered by the example shell; build is warnings-clean and the brand-token gate passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-motion/44-UI-SPEC.md
@.planning/phases/44-motion/44-PATTERNS.md
@.planning/phases/44-motion/44-RESEARCH.md

<interfaces>
<!-- Extracted from the codebase; the executor should use these directly. -->

cl_hero/1 (lib/cairnloop/web/components.ex:160-179) renders the hero count:
  <span class={["cl-hero__count", @calm? && "cl-hero__count--calm"]}>{@count}</span>
  attrs: count (integer, required), job, href, cta, calm? ; slots: detail, cta_slot

Cairnloop.Web.Components module head (components.ex:25): `use Phoenix.Component` ONLY —
  cl_flash/1 needs `JS.transition`, so the module MUST add `alias Phoenix.LiveView.JS`.

Analog: example flash/1 (core_components.ex:56-86) — kind/flash/id attrs, the
  `phx-click={JS.push("lv:clear-flash", value: %{key: @kind}) |> hide("##{@id}")}` dismiss idiom,
  `Phoenix.Flash.get(@flash, @kind)` to read the message string.

Analog: lib cl_banner/1 (components.ex:88-105) — attr/slot conventions, `.cl-*` classes + role
  + auto-escaped {...} + distinct per-status icon via a status_icon/1-style helper (§7.5).

Example flash_group (layouts.ex:85-105): currently calls <.flash kind={:info}/> + <.flash kind={:error}/>
  then two phx-disconnected/phx-connected reconnection flashes (KEEP those as-is).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the hero count one-shot mount entrance in cl_hero/1</name>
  <read_first>
    - lib/cairnloop/web/components.ex lines 25-30 (module head — currently only `use Phoenix.Component`; the `alias Phoenix.LiveView.JS` you add here is shared with Task 2)
    - lib/cairnloop/web/components.ex lines 160-179 (`cl_hero/1` — the `.cl-hero__count` span at line 171 is the attach point)
    - lib/cairnloop/web/search_modal_component.ex line 65 (the existing `phx-mounted={JS.focus()}` one-shot pattern; `JS.transition/2` is the same family — line 9 shows `alias Phoenix.LiveView.JS`)
    - .planning/phases/44-motion/44-PATTERNS.md "lib/cairnloop/web/home_live.ex — hero count entrance" + "Shared Patterns → One-shot mount entrance"
    - priv/static/cairnloop.css (confirm `.cl-motion-enter` exists from Plan 02 — this is the class the entrance applies)
  </read_first>
  <action>
    Add `alias Phoenix.LiveView.JS` to the `Cairnloop.Web.Components` module head (after `use Phoenix.Component`). In `cl_hero/1`, add `phx-mounted={JS.transition("cl-motion-enter", time: 140)}` to the `.cl-hero__count` span (line 171). The 140ms matches the `--cl-dur-micro` animation Plan 02 defined on `.cl-motion-enter` (unambiguously <180ms).

    Do NOT add any `transition-property` to the count span and do NOT introduce a number-roll/count-up tween — the entrance is a one-shot `animation` that morphdom does not replay on in-place text patches, so live count ticks stay instant (criterion-3 negative assertion). Apply the same `phx-mounted` to the `cl_stat/1` `.cl-stat__count` span only if `cl_stat/1` renders a count node that warrants the secondary-stat entrance; if so, keep it identical (140ms, no transition). Per D-03, the count text node carries no transition.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && grep -n 'phx-mounted=.*JS.transition."cl-motion-enter"' lib/cairnloop/web/components.ex && mix test test/cairnloop/web/brand_token_gate_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `Cairnloop.Web.Components` module head includes `alias Phoenix.LiveView.JS`.
    - The `.cl-hero__count` span carries `phx-mounted={JS.transition("cl-motion-enter", time: 140)}`.
    - No `transition-property`/`transition:` and no count-up/number-roll added to the count node.
    - `mix compile --warnings-as-errors` clean; `mix test test/cairnloop/web/brand_token_gate_test.exs` GREEN (no hex introduced).
  </acceptance_criteria>
  <done>The hero count animates once on mount via `.cl-motion-enter`; count ticks remain instant; build + brand gate clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the lib cl_flash/1 toast component (hex-free, escaped, §7.5 icon)</name>
  <read_first>
    - lib/cairnloop/web/components.ex lines 76-105 (`cl_chip/1` + `cl_banner/1` — the attr/slot conventions, `.cl-*` classes, `role`, distinct per-status icon via a `status_icon/1`-style helper, auto-escaped `{...}`; copy this shape)
    - examples/cairnloop_example/lib/cairnloop_example_web/components/core_components.ex lines 31, 48-86 (the `flash/1` analog — `kind`/`flash`/`id` attrs, `Phoenix.Flash.get(@flash, @kind)`, the `lv:clear-flash` dismiss idiom, the `phx-mounted={show(...)}` placement; `alias Phoenix.LiveView.JS` at line 31)
    - .planning/phases/44-motion/44-PATTERNS.md "cl_flash/1 in lib/cairnloop/web/components.ex" (attr/slot + dismiss + enter/exit wiring + §7.5/escaping guards + GATE-CRITICAL note)
    - .planning/phases/44-motion/44-RESEARCH.md "Brand-Token Gate Safety" (the three forbidden patterns: `var(--cl-x, #hex)`, bare hex, raw `rgba()/hsl()`) + "Security Domain" (use `{msg}` auto-escaped, never `raw/1`)
    - priv/static/cairnloop.css (confirm `.cl-toast`, `.cl-toast-enter`, `.cl-toast-exit` exist from Plan 02)
  </read_first>
  <behavior>
    - Renders a `.cl-toast` element only when a flash message exists for `@kind` (`Phoenix.Flash.get(@flash, @kind)` non-nil); renders nothing otherwise.
    - Carries `phx-mounted={JS.transition("cl-toast-enter", time: 180)}` (enter) and `phx-remove={JS.transition("cl-toast-exit", time: 160)}` (exit faster than enter).
    - Dismiss control uses `JS.push("lv:clear-flash", value: %{key: @kind}) |> hide("##{@id}")`, has `aria-label="Dismiss"`, and a distinct per-kind icon (info vs error) — not color-alone (§7.5).
    - Flash text is interpolated with auto-escaped `{...}`; `raw/1` is never used → an attacker-influenced flash string `<script>` is rendered as inert escaped text (XSS-safe).
    - The component markup contains zero hex, zero `var(--cl-*, #hex)` fallbacks, zero raw `rgba()/rgb()/hsl()/hsla()` — color comes only from `.cl-toast`/`.cl-*` classes / bare `var(--cl-*)`.
  </behavior>
  <action>
    Add `cl_flash/1` to `lib/cairnloop/web/components.ex` (reusing the `alias Phoenix.LiveView.JS` added in Task 1). Declare attrs `id` (string), `flash` (map, default `%{}`), `kind` (atom, `values: [:info, :error]`), `title` (string, default nil), `rest` (`:global`), and an optional `inner_block` slot — mirror the example `flash/1` attrs and the lib `cl_banner/1` declaration style. Render a `.cl-toast` container (class drives the brand surface from Plan 02) with `role="status"` (info) / `role="alert"` (error as appropriate), a distinct per-kind icon (reuse the `cl_icon`/`status_icon`-style helper so info and error have different silhouettes — §7.5), the auto-escaped message via `{Phoenix.Flash.get(@flash, @kind)}` (and/or `{render_slot(@inner_block)}`), and a dismiss button with `aria-label="Dismiss"` using the `lv:clear-flash` idiom. Attach `phx-mounted={JS.transition("cl-toast-enter", time: 180)}` and `phx-remove={JS.transition("cl-toast-exit", time: 160)}`.

    GATE-CRITICAL: this file is scanned by the brand-token gate. Use ONLY `.cl-*` classes / bare `var(--cl-*)`. No bare hex, no `var(--cl-x, #hex)` fallback form, no raw color functions. NEVER `raw/1` on flash content. Keep `mix compile --warnings-as-errors` clean — declare and use every attr/slot.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/cairnloop/web/brand_token_gate_test.exs && grep -n 'def cl_flash' lib/cairnloop/web/components.ex && ! grep -nE 'raw\(' lib/cairnloop/web/components.ex</automated>
  </verify>
  <acceptance_criteria>
    - `lib/cairnloop/web/components.ex` defines `def cl_flash` with the attrs above and renders a `.cl-toast` element with `phx-mounted={JS.transition("cl-toast-enter", time: 180)}` and `phx-remove={JS.transition("cl-toast-exit", time: 160)}`.
    - Flash text rendered via auto-escaped `{...}` (no `raw/1` anywhere in the file).
    - A distinct per-kind icon is present (info ≠ error silhouette) and a dismiss button carries `aria-label="Dismiss"` + the `lv:clear-flash` idiom.
    - `mix test test/cairnloop/web/brand_token_gate_test.exs` GREEN — the new component introduces no hex / `var(--cl-x,#hex)` / raw color function.
    - `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>`cl_flash/1` exists as a hex-free, escaped, §7.5-iconed `.cl-toast` with mount-enter / faster-exit transitions; brand gate + build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the example shell flash_group to render the lib cl_flash</name>
  <read_first>
    - examples/cairnloop_example/lib/cairnloop_example_web/components/layouts.ex lines 80-114 (`flash_group/1` — replace/augment the two `<.flash kind={:info}/>`+`<.flash kind={:error}/>` calls; KEEP the two `phx-disconnected`/`phx-connected` reconnection flashes at 91-113 as-is)
    - lib/cairnloop/web/components.ex (the `cl_flash/1` just added — confirm its public arity/attrs for the call site)
    - .planning/phases/44-motion/44-PATTERNS.md "examples/.../components/layouts.ex:85-114 — flash_group wiring" (swap callee; import cl_flash from Cairnloop.Web.Components)
    - .planning/phases/44-motion/44-RESEARCH.md "Open Questions" #3 + A4 (the shell MUST invoke cl_flash or the toast never renders and the E2E can't see it)
    - test/cairnloop/web/brand_token_gate_test.exs (this file scans the example live dir + lib web; layouts.ex must stay hex-free)
  </read_first>
  <action>
    In `examples/cairnloop_example/lib/cairnloop_example_web/components/layouts.ex`, make `cl_flash` available (import `Cairnloop.Web.Components` if not already imported in that module's scope) and replace the two operator-facing `<.flash kind={:info} flash={@flash}/>` / `<.flash kind={:error} flash={@flash}/>` calls in `flash_group/1` with `<.cl_flash id="flash-info" kind={:info} flash={@flash}/>` / `<.cl_flash id="flash-error" kind={:error} flash={@flash}/>`. Keep the `aria-live="polite"` wrapper and the two `phx-disconnected`/`phx-connected` reconnection flashes unchanged (those are framework reconnection toasts, out of scope). Keep `layouts.ex` hex-free (it is gate-scanned). Confirm the example app still compiles warnings-clean.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example && mix compile --warnings-as-errors 2>&1 | tail -5 && grep -n 'cl_flash' lib/cairnloop_example_web/components/layouts.ex</automated>
  </verify>
  <acceptance_criteria>
    - `flash_group/1` renders `<.cl_flash kind={:info} .../>` and `<.cl_flash kind={:error} .../>` for operator flashes (the lib toast actually mounts).
    - The two `phx-disconnected`/`phx-connected` reconnection flashes are unchanged.
    - `cd examples/cairnloop_example && mix compile --warnings-as-errors` is clean.
    - `layouts.ex` introduces no hex (brand-token gate still passes for the example live/web scope).
  </acceptance_criteria>
  <done>The example shell renders the lib `cl_flash` toast for operator flashes; reconnection flashes untouched; example app compiles warnings-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView → flash render | `put_flash` strings (operator-authored, but treat as untrusted) cross into `cl_flash/1` rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | Tampering / Info-disclosure | cl_flash/1 flash content rendering | mitigate | Render flash text with HEEx `{Phoenix.Flash.get(...)}` auto-escaping; NEVER `raw/1`. An attacker-influenced flash string (e.g. `<script>`) renders as inert escaped text — no stored/reflected XSS via the toast. Verified by `! grep -nE 'raw\(' components.ex`. |
| T-44-05 | Tampering | cl_flash/1 + layouts.ex (brand-token gate scope) | mitigate | Both files are gate-scanned `.ex`; keep all color via `.cl-*`/bare `var(--cl-*)`. No hex, no `var(--cl-x,#hex)`, no raw `rgba()/hsl()`. Verified by `mix test test/cairnloop/web/brand_token_gate_test.exs`. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs. RESEARCH "Package Legitimacy Audit" = N/A. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean (lib) and `cd examples/cairnloop_example && mix compile --warnings-as-errors` clean (example).
- `mix test test/cairnloop/web/brand_token_gate_test.exs` GREEN (cl_flash + layouts hex-free).
- `! grep -nE 'raw\(' lib/cairnloop/web/components.ex` (no `raw/1` on flash content — XSS-safe).
- E2E hero-entrance + toast assertions verify on the gated `e2e` lane at wave merge.
</verification>

<success_criteria>
- Hero count mounts the `.cl-motion-enter` entrance once; count ticks stay instant.
- `cl_flash/1` is a reusable, hex-free, escaped, §7.5-iconed `.cl-toast` with enter/faster-exit motion, rendered by the example shell.
- Brand-token gate + warnings-clean builds pass across lib and example.
</success_criteria>

<output>
Create `.planning/phases/44-motion/44-03-SUMMARY.md` when done. Record the lib-elevation decision (toast shipped as reusable `cl_flash/1` per component-elevation posture) and that the example DaisyUI `flash/1` now coexists / is superseded for operator flashes.
</output>
