---
phase: 44-motion
plan: 02
type: execute
wave: 1
depends_on: ["44-01"]
files_modified:
  - priv/static/cairnloop.css
  - examples/cairnloop_example/priv/static/assets/css/app.css
autonomous: true
requirements: [MOTION-01, MOTION-02]
nyquist_compliant: true

must_haves:
  truths:
    - "New @keyframes (cl-enter-up, cl-reveal-x, cl-toast-enter, cl-toast-exit) and utility classes (.cl-motion-enter, .cl-motion-reveal, .cl-list-stagger, .cl-toast) exist under .cl-app using only transform + opacity"
    - "The gate cross-fade rule for .cl-motion-state exists (transition-property: opacity, color, background-color, border-color at --cl-dur-ui)"
    - "Every new motion rule is mirrored verbatim into the example app app.css (mirror parity)"
    - ".cl-hero__count / .cl-stat__count carry no transition-property (count ticks stay instant)"
    - "Reduced-motion is honored by the existing global .cl-app block (new entrances sit under .cl-app); .cl-motion-state survives at 120ms"
  artifacts:
    - path: "priv/static/cairnloop.css"
      provides: "Canonical motion @keyframes + .cl-motion-* + .cl-toast rules + .cl-motion-state cross-fade"
      contains: "cl-enter-up"
    - path: "examples/cairnloop_example/priv/static/assets/css/app.css"
      provides: "Verbatim mirror of the new .cl-* motion rules in the .cl-* region"
      contains: "cl-enter-up"
  key_links:
    - from: "priv/static/cairnloop.css new motion rules"
      to: "existing token block (--cl-dur-*, --cl-ease-*, --cl-stagger)"
      via: "var() references — no new duration values invented"
      pattern: "var\\(--cl-(dur|ease|stagger)"
    - from: "new entrance animations"
      to: "global @media (prefers-reduced-motion) block under .cl-app"
      via: "all rules scoped under .cl-app so the existing block zeroes them"
      pattern: "\\.cl-app"
---

<objective>
Author the new motion CSS — `@keyframes` + `.cl-motion-*` utility classes + the `.cl-toast` surface + the `.cl-motion-state` gate cross-fade — in the canonical `priv/static/cairnloop.css`, then mirror every new rule verbatim into the example app `app.css`. All rules reuse the SHIPPED token block (`--cl-dur-*` / `--cl-ease-*` / `--cl-stagger`) and sit under `.cl-app` so the existing `prefers-reduced-motion` block zeroes them. This wave turns Plan 01's unit test GREEN.

Purpose: The five surfaces (hero count, rail reveal, gate flip, list stagger, toast) all consume these classes in Wave 2. Defining the CSS contract first means the Wave-2 markup tasks attach classes that already exist.
Output: New hex-free, transform/opacity-only motion rules present and mirrored in BOTH stylesheets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-motion/44-UI-SPEC.md
@.planning/phases/44-motion/44-PATTERNS.md
@.planning/phases/44-motion/44-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author new motion @keyframes + utility classes in canonical cairnloop.css</name>
  <read_first>
    - priv/static/cairnloop.css lines 142-157 (the SHIPPED motion token block — `--cl-dur-instant/micro/ui/panel/exit`, `--cl-ease-out/in-out/drawer/linear`, `--cl-stagger`; REUSE, do not redefine)
    - priv/static/cairnloop.css lines 197-211 (the SHIPPED global `@media (prefers-reduced-motion)` block + the `.cl-motion-state` 120ms re-enable — new rules must sit under `.cl-app` so this zeroes them; the `.cl-motion-state` survivor is reused, not redefined)
    - priv/static/cairnloop.css lines 420-423 + 769-773 (`.cl-stat__count` and `.cl-hero__count` — confirm they carry NO `transition-property` today; keep it that way)
    - .planning/phases/44-motion/44-RESEARCH.md "Code Examples" (illustrative hex-free rule shapes) + "Standard Stack" duration note (use `--cl-dur-micro` 140ms for the hero so it is unambiguously <180ms; `--cl-dur-panel` 260ms for rail; `--cl-dur-ui` 180ms for gate flip + list stagger; `--cl-dur-exit` 160ms for toast exit)
    - .planning/phases/44-motion/44-UI-SPEC.md "Surface-by-Surface Motion Specification" (translate distances, easing per surface, toast surface tokens: var(--cl-surface-raised)/var(--cl-border)/var(--cl-shadow-modal)/var(--cl-radius-md)/var(--cl-z-toast))
    - .planning/phases/44-motion/44-PATTERNS.md "priv/static/cairnloop.css" section (Anti-patterns: no width/height/top/left/max-height/max-width; no transition-property on count nodes; no !important outside .cl-app)
  </read_first>
  <action>
    In `priv/static/cairnloop.css`, after the `.cl-app` block (alongside existing `.cl-*` rules), add the new motion rules — ALL hex-free, ALL under `.cl-app`, transform/opacity ONLY, reusing existing tokens (never invent `--cl-dur-*`/`--cl-stagger` values):

    Keyframes: `@keyframes cl-enter-up` (opacity 0→1, translateY(4px)→0); `@keyframes cl-reveal-x` (opacity 0→1, translateX(16px)→0); `@keyframes cl-toast-enter` (opacity 0→1, translateY(-8px)→0); `@keyframes cl-toast-exit` (opacity 1→0, translateY(0)→translateY(-4px)).

    Utility classes (all `.cl-app .cl-*`): `.cl-motion-enter` → `animation: cl-enter-up var(--cl-dur-micro) var(--cl-ease-out) both` (140ms, hero/stat count); `.cl-motion-reveal` → `animation: cl-reveal-x var(--cl-dur-panel) var(--cl-ease-drawer) both` (260ms, rail); `.cl-list-stagger > li:nth-child(-n+5)` → `animation: cl-enter-up var(--cl-dur-ui) var(--cl-ease-out) both`, plus per-index `animation-delay` rules for `:nth-child(1..5)` stepping by `var(--cl-stagger)` (item1=0ms, item2=1×, item3=2×, item4=3×, item5=4× via `calc()`); `.cl-toast-enter` → `animation: cl-toast-enter var(--cl-dur-ui) var(--cl-ease-out) both`; `.cl-toast-exit` → `animation: cl-toast-exit var(--cl-dur-exit) var(--cl-ease-in-out) both`.

    Gate cross-fade: `.cl-app .cl-motion-state` base transition → `transition: opacity/color/background-color/border-color var(--cl-dur-ui) var(--cl-ease-linear)` (the existing reduced-motion block already re-enables this class at 120ms — do NOT touch that block; just add the base 180ms transition so the chip cross-fades on class swap).

    Toast surface: `.cl-app .cl-toast` → `background: var(--cl-surface-raised); border: 1px solid var(--cl-border); box-shadow: var(--cl-shadow-modal); border-radius: var(--cl-radius-md); z-index: var(--cl-z-toast)` (and any positioning needed — top/right placement via `position`/`inset`-family is acceptable layout setup, but NEVER put width/height/top/left/max-height/max-width inside a transition or keyframe).

    Do NOT add any `transition-property` to `.cl-hero__count` / `.cl-stat__count`. Do NOT add new `--cl-dur-*` tokens. Do NOT touch the existing reduced-motion block at 197-211.
  </action>
  <verify>
    <automated>grep -nE 'cl-enter-up|cl-reveal-x|cl-toast-enter|cl-toast-exit|cl-motion-enter|cl-motion-reveal|cl-list-stagger|cl-motion-state|cl-toast' priv/static/cairnloop.css | grep -v '^[0-9]*:#' | head -40 # rules present; then mix test (mirror parity asserts at Task 2)</automated>
  </verify>
  <acceptance_criteria>
    - `priv/static/cairnloop.css` contains `@keyframes cl-enter-up`, `cl-reveal-x`, `cl-toast-enter`, `cl-toast-exit` and classes `.cl-motion-enter`, `.cl-motion-reveal`, `.cl-list-stagger > li:nth-child(-n+5)` (+ per-index delays), `.cl-toast-enter`, `.cl-toast-exit`, `.cl-toast`, and a base `.cl-motion-state` transition.
    - Every new rule is under `.cl-app` and uses only `var(--cl-dur-*)` / `var(--cl-ease-*)` / `var(--cl-stagger)` — `grep -nE 'cl-dur-(instant|micro|ui|panel|exit)' priv/static/cairnloop.css` shows NO newly-introduced literal `ms` duration on the new motion rules.
    - No new motion rule (keyframe or `.cl-motion-*`/`.cl-toast`) contains `width`/`height`/`top`/`left`/`max-height`/`max-width` as an animated/transitioned property.
    - `.cl-hero__count` / `.cl-stat__count` still carry NO `transition-property` (grep confirms absence).
    - The reduced-motion block (`cairnloop.css:197-211`) is unchanged.
    - Hero entrance uses `--cl-dur-micro` (140ms, unambiguously <180ms); rail uses `--cl-dur-panel`; gate flip + stagger use `--cl-dur-ui`; toast exit uses `--cl-dur-exit`.
  </acceptance_criteria>
  <done>Canonical `cairnloop.css` carries all new hex-free, transform/opacity-only motion rules under `.cl-app`, reusing shipped tokens; count nodes remain transition-free; reduced-motion block untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the new motion rules verbatim into example app app.css; turn the unit test GREEN</name>
  <read_first>
    - priv/static/cairnloop.css (the new motion rules just authored in Task 1 — these are the verbatim source to copy)
    - examples/cairnloop_example/priv/static/assets/css/app.css lines 122-132 (mirror token block — identical to lib 142-153) and lines 180-191 (mirror reduced-motion block — identical to lib 197-211); the `.cl-*` region is where the new rules go
    - .planning/phases/44-motion/44-RESEARCH.md "Runtime State Inventory" + "Two-File CSS Mirror" (the example file is 4166 lines and contains a SEPARATE Tailwind/DaisyUI section with its own `@media (prefers-reduced-motion)` blocks at lines ~3092 and ~3593 — DO NOT touch those; place new rules in the `.cl-*` region after the `.cl-app` block alongside existing `.cl-*` rules)
    - test/cairnloop/web/motion_css_test.exs (the Wave-0 unit test — Task 2 must make ALL its assertions GREEN, especially mirror-parity)
  </read_first>
  <action>
    Copy every new `@keyframes` / `.cl-motion-*` / `.cl-list-stagger` / `.cl-toast` / `.cl-motion-state` rule authored in Task 1 VERBATIM into `examples/cairnloop_example/priv/static/assets/css/app.css`, placing them in the `.cl-*` region (after the `.cl-app` block, alongside the existing mirrored `.cl-*` rules — the same region that already mirrors the token block at 122-132). Do NOT modify the Tailwind/DaisyUI compiled section or its `@media (prefers-reduced-motion)` blocks at ~3092 / ~3593. The two files must agree on the new motion rule text so the unit test's mirror-parity assertions pass.

    After mirroring, run the Wave-0 unit test — it must now be GREEN (forbidden-property scan passes, mirror-parity passes, count-tick no-transition passes). If any parity assertion fails, the copy diverged — fix the divergence, do not relax the test.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/motion_css_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `examples/cairnloop_example/priv/static/assets/css/app.css` contains the SAME new `@keyframes`/`.cl-motion-*`/`.cl-list-stagger`/`.cl-toast`/`.cl-motion-state` rules as `priv/static/cairnloop.css`, in its `.cl-*` region.
    - The DaisyUI/Tailwind section and its `@media (prefers-reduced-motion)` blocks (~3092, ~3593) are unmodified.
    - `mix test test/cairnloop/web/motion_css_test.exs` is GREEN (all three assertion groups pass, including cross-file mirror parity).
    - `mix compile --warnings-as-errors` is clean.
  </acceptance_criteria>
  <done>Both stylesheets carry identical new motion rules in their `.cl-*` regions; the Wave-0 unit test is GREEN; build is warnings-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure static-CSS additions. No input, no auth, no rendering logic, no `.ex` change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02 | Tampering | cairnloop.css / app.css motion rules | accept | CSS is not scanned by the brand-token gate structurally; rules are hex-free and reuse shipped tokens. No executable surface, no secrets, no user input. |
| T-44-03 | Denial of Service | animation rules | accept | Durations bounded by shipped tokens (≤260ms), transform/opacity only (GPU-cheap, no layout thrash); `prefers-reduced-motion` zeroes them. No infinite/pulse animations (brand §15.3 forbidden). |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs. RESEARCH "Package Legitimacy Audit" = N/A. |
</threat_model>

<verification>
- `mix test test/cairnloop/web/motion_css_test.exs` GREEN (forbidden-property + mirror-parity + count-tick-no-transition).
- `mix compile --warnings-as-errors` clean.
- `grep` confirms new rules reuse `var(--cl-dur-*)`/`var(--cl-ease-*)`/`var(--cl-stagger)` and introduce no literal duration values.
</verification>

<success_criteria>
- All new motion `@keyframes` + `.cl-motion-*` + `.cl-toast` + `.cl-motion-state` rules present in BOTH stylesheets, hex-free, transform/opacity-only, under `.cl-app`.
- Hero entrance is 140ms (`--cl-dur-micro`, unambiguously <180ms); count nodes carry no transition.
- The Wave-0 unit test flips RED→GREEN.
</success_criteria>

<output>
Create `.planning/phases/44-motion/44-02-SUMMARY.md` when done. Record that the unit test is now GREEN and the two stylesheets are mirror-consistent for the new motion rules.
</output>
