---
phase: M005-S01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cairnloop/auditor.ex
  - lib/cairnloop/automation.ex
  - lib/cairnloop/chat.ex
  - test/cairnloop/automation_test.exs
  - test/cairnloop/chat_test.exs
autonomous: true
requirements: 
  - M005-REQ-01
  - M005-REQ-02
must_haves:
  truths:
    - "Host application can configure a custom Auditor module."
    - "Operator actions immutably log evidence within the same Ecto.Multi database transaction."
  artifacts:
    - path: "lib/cairnloop/auditor.ex"
      provides: "Cairnloop.Auditor behaviour and NoOp implementation"
    - path: "lib/cairnloop/automation.ex"
      provides: "Multi chains injected with auditor calls"
    - path: "lib/cairnloop/chat.ex"
      provides: "Multi chains injected with auditor calls"
  key_links:
    - from: "lib/cairnloop/automation.ex"
      to: "lib/cairnloop/auditor.ex"
      via: "auditor.audit/4 invocation"
    - from: "lib/cairnloop/chat.ex"
      to: "lib/cairnloop/auditor.ex"
      via: "auditor.audit/4 invocation"
---

<objective>
Implement the `Cairnloop.Auditor` behavior and inject it into critical operator Ecto.Multi workflows to enable compliance-grade, durable auditing.

Purpose: Guarantee that critical actions (approving/discarding drafts, replying, resolving) generate immutable audit records transactionally.
Output: Auditor behavior, NoOp fallback, and updated core modules passing their multi chains through the auditor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/M005-phases/M005-S01/M005-S01-PATTERNS.md
@.planning/milestones/M005-phases/M005-S01/M005-S01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Auditor behavior and fallback</name>
  <files>lib/cairnloop/auditor.ex</files>
  <action>
    Create `Cairnloop.Auditor` defining `@callback audit(multi :: Ecto.Multi.t(), action :: atom(), actor :: map() | String.t() | nil, metadata :: map()) :: Ecto.Multi.t()`.
    Define `Cairnloop.Auditor.NoOp` which implements the behavior and simply returns the `multi` unmodified.
    Do NOT build a standalone auditing schema (per locked decision - we rely on the host application for storage).
    Analog Reference: Follow the behavior definition pattern of `lib/cairnloop/context_provider.ex` (see M005-S01-PATTERNS.md).
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>File compiles with behaviour and NoOp module defined.</done>
</task>

<task type="auto">
  <name>Task 2: Inject Auditor into Automation flows</name>
  <files>lib/cairnloop/automation.ex, test/cairnloop/automation_test.exs</files>
  <action>
    Update `approve_draft/1`, `discard_draft/1`, and `mark_draft_edited/1` to accept a second argument `opts \\ []`.
    Inside each function, extract `actor = Keyword.get(opts, :actor)` and `auditor = Keyword.get(opts, :auditor, Application.get_env(:cairnloop, :auditor, Cairnloop.Auditor.NoOp))`.
    Inject `auditor.audit(multi, :action_name, actor, %{draft_id: draft_id})` into the `Ecto.Multi` pipeline *before* `repo().transaction()`. (e.g. `multi |> auditor.audit(:approve_draft, actor, %{draft_id: draft_id})`)
    Update `automation_test.exs` to ensure existing tests still pass with the new arities and opts.
    Explicitly add an isolated test in `automation_test.exs` using a mock or test auditor to verify that `Ecto.Multi` chain interception works correctly (the test auditor should successfully append an operation to the multi chain and it should be executed alongside the main transaction).
    Analog Reference: Follow the `Ecto.Multi` usage pattern of `lib/cairnloop/automation.ex` (see M005-S01-PATTERNS.md).
  </action>
  <verify>
    <automated>mix test test/cairnloop/automation_test.exs</automated>
  </verify>
  <done>Automation workflows successfully pass their Multi chain through the Auditor, and tests verify chain interception.</done>
</task>

<task type="auto">
  <name>Task 3: Inject Auditor into Chat flows</name>
  <files>lib/cairnloop/chat.ex, test/cairnloop/chat_test.exs</files>
  <action>
    Update `reply_to_conversation/3` to accept `opts \\ []` as the 4th argument (e.g. `reply_to_conversation(id, content, role \\ :agent, opts \\ [])`). 
    Update `resolve_conversation/2` to also use the auditor.
    Extract the actor from `opts[:actor]` (for reply) or `opts[:resolved_by]` (for resolve). 
    Resolve the auditor from `opts` or Application env as in Task 2.
    Inject `auditor.audit/4` into the respective `Ecto.Multi` pipelines before `repo().transaction()`.
    Update `chat_test.exs` to pass.
    Explicitly add an isolated test in `chat_test.exs` using a mock or test auditor to verify that `Ecto.Multi` chain interception works correctly.
    Analog Reference: Follow the `Ecto.Multi` usage pattern of `lib/cairnloop/chat.ex` (see M005-S01-PATTERNS.md).
  </action>
  <verify>
    <automated>mix test test/cairnloop/chat_test.exs</automated>
  </verify>
  <done>Chat workflows successfully pass their Multi chain through the Auditor, and tests verify chain interception.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lib -> Host DB | Cairnloop allows host application to execute arbitrary DB operations within Cairnloop's transactional scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M005-S01-01 | Tampering | Auditor Callback | mitigate | Define rigid `@callback` ensuring it receives and returns an `Ecto.Multi.t()`, forcing host to use Ecto's safe transactional constraints rather than raw SQL. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test
</verification>

<success_criteria>
- `Cairnloop.Auditor` behavior exists.
- Core operator actions in `Automation` and `Chat` pipe their `Ecto.Multi` chains through the auditor before execution.
- Transactional integrity is preserved (auditor cannot break the `Ecto.Multi` flow without a compilation/runtime error if it doesn't return `Ecto.Multi.t()`).
</success_criteria>

<output>
After completion, create `.planning/milestones/M005-phases/M005-S01/M005-S01-01-SUMMARY.md`
</output>