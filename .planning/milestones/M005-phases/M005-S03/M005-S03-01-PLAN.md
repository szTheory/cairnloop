---
phase: M005-S03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/cairnloop/install.slos.ex
  - test/cairnloop/tasks/install.slos_test.exs
autonomous: true
requirements:
  - M005-REQ-05
  - M005-REQ-06
must_haves:
  truths:
    - Adopters can run a mix task to scaffold SLOs and runbooks.
    - SLOs are defined in the host application as explicit code, not hidden macros.
    - Runbooks are generated into priv/runbooks/ directory as markdown files.
    - Doctor checks are scaffolded to diagnose Cairnloop health.
  artifacts:
    - path: lib/mix/tasks/cairnloop/install.slos.ex
      provides: Mix task for scaffolding observability configuration
  key_links:
    - from: lib/mix/tasks/cairnloop/install.slos.ex
      to: priv/runbooks/cairnloop_system_health.md
      via: Igniter.create_new_file
---

<objective>
Implement the generator task to explicitly scaffold SLO definitions, Doctor checks, and runbooks directly into the host application.

Purpose: Provide SRE-grade observability and runbook scaffolding out-of-the-box, ensuring operators have visibility without hidden macros.
Output: A new Igniter Mix task `mix cairnloop.install.slos`.
</objective>

<context>
@.planning/milestones/M005-phases/M005-S03-CONTEXT.md
@.planning/milestones/M005-phases/M005-S03/M005-S03-PATTERNS.md
@.planning/milestones/M005-phases/M005-S03/M005-S03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Igniter Task for SLO and Runbook Scaffolding</name>
  <files>lib/mix/tasks/cairnloop/install.slos.ex, test/cairnloop/tasks/install.slos_test.exs</files>
  <behavior>
    - Test 1: igniter() creates MyApp.Cairnloop.SLOs module
    - Test 2: igniter() creates MyApp.Cairnloop.Doctor module
    - Test 3: igniter() creates priv/runbooks/cairnloop_system_health.md (and others) using Igniter.create_new_file
  </behavior>
  <action>
    Create `Mix.Tasks.Cairnloop.Install.Slos` as an Igniter task. 
    1. Resolve the `app_module` from the igniter.
    2. Generate `MyApp.Cairnloop.SLOs` containing `Parapet.SLO.define(...)` placeholders or templates for TTFR, Resolution Time, and System Health.
    3. Generate `MyApp.Cairnloop.Doctor` module.
    4. Scaffold markdown runbooks (`cairnloop_ttfr_breach.md`, `cairnloop_resolution_breach.md`, `cairnloop_system_health.md`) into `priv/runbooks/` using `Igniter.create_new_file/3`.
    Use `lib/mix/tasks/cairnloop/install.parapet.ex` as a pattern for creating modules and Igniter test setup.
  </action>
  <verify>
    <automated>mix test test/cairnloop/tasks/install.slos_test.exs</automated>
  </verify>
  <done>Task successfully scaffolds modules and markdown files via Igniter tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System | The generator writes files into the host application structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M005-S03-01 | Tampering | install.slos.ex | mitigate | Use Igniter.create_new_file to safely avoid overwriting user customizations in existing files. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test test/cairnloop/tasks/install.slos_test.exs
</verification>

<success_criteria>
- Igniter task successfully generates `MyApp.Cairnloop.SLOs`.
- Igniter task successfully generates `MyApp.Cairnloop.Doctor`.
- Igniter task correctly places Markdown runbooks in `priv/runbooks/`.
- No use of hidden macros (e.g. `use Cairnloop.SLOs`).
</success_criteria>

<output>
After completion, create `.planning/milestones/M005-phases/M005-S03/M005-S03-01-SUMMARY.md`
</output>
