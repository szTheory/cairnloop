---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/cairnloop/embedder.ex
  - lib/cairnloop/embedder/external_api.ex
  - lib/cairnloop/knowledge_base/markdown_parser.ex
  - lib/cairnloop/knowledge_base/workers/chunk_revision.ex
  - lib/cairnloop/knowledge_base.ex
  - .planning/M008-ROADMAP.md
autonomous: true
requirements:
  - M008-REQ-03
  - M008-REQ-04
  - M008-REQ-05
  - M008-REQ-06
must_haves:
  truths:
    - Publishing a revision automatically queues a background chunking job
    - Markdown content is parsed into distinct sections based on H2/H3 headers
    - Each chunk gets a vector embedding using the external API
    - Idempotency is maintained by clearing old chunks for a revision before inserting new ones
    - AI triage systems can search chunks using vector distance
  artifacts:
    - path: lib/cairnloop/embedder.ex
      provides: Behaviour for generating embeddings
    - path: lib/cairnloop/knowledge_base/markdown_parser.ex
      provides: AST-based markdown parsing for chunk extraction
    - path: lib/cairnloop/knowledge_base/workers/chunk_revision.ex
      provides: Oban worker orchestration
  key_links:
    - from: lib/cairnloop/knowledge_base.ex
      to: lib/cairnloop/knowledge_base/workers/chunk_revision.ex
      via: Oban.insert/2 inside publish_revision/1 Ecto.Multi
---

<objective>
Automatically prepare and index Markdown content transparently in the background for optimal AI retrieval via RAG.

Purpose: Offload the CPU-intensive markdown AST parsing and I/O-heavy embedding API calls to an asynchronous background worker, ensuring the operator's authoring workflow remains fast and unblocked. Provide a foundation for AI features to retrieve semantic context from the knowledge base natively via pgvector.
Output: Working Embedder behaviour, Earmark AST parser, Oban worker, and an updated Knowledge Base context.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/M008-phases/M008-S03/PATTERNS.md
@.planning/milestones/M008-phases/M008-S03/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Req and Setup Embedder Behaviour</name>
  <files>mix.exs, lib/cairnloop/embedder.ex, lib/cairnloop/embedder/external_api.ex</files>
  <action>
    - In `mix.exs`, add `{:req, "~> 0.5"}` to the dependencies. Run `mix deps.get`.
    - Create `lib/cairnloop/embedder.ex` as a Behaviour with a `@callback generate_embeddings(chunks :: [String.t()], opts :: keyword()) :: {:ok, [[float()]]} | {:error, term()}`.
    - Create `lib/cairnloop/embedder/external_api.ex` implementing the `Cairnloop.Embedder` behaviour. Use `Req.post` to call a dummy/mock embedding endpoint (e.g., `https://api.openai.com/v1/embeddings` utilizing `System.get_env("OPENAI_API_KEY")`). If the key is missing, mock a valid response array or gracefully return `{:error, :missing_api_key}` for development safety, but implement the structural map over chunks so it actually works when configured.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Req is installed, Embedder Behaviour is defined, and ExternalApi implements it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the AST-based Markdown Parser</name>
  <files>lib/cairnloop/knowledge_base/markdown_parser.ex, test/cairnloop/knowledge_base/markdown_parser_test.exs</files>
  <behavior>
    - Test 1: Parses markdown with H2 headers into separate text chunks.
    - Test 2: Parses markdown with H3 headers into separate text chunks.
    - Test 3: Falls back gracefully if chunk length exceeds token limits (e.g., splits or trims).
    - Test 4: Ignores layout HTML or groups it correctly using Earmark.as_ast/1.
  </behavior>
  <action>
    Implement `Cairnloop.KnowledgeBase.MarkdownParser.parse(markdown)`.
    - Use `Earmark.as_ast(markdown)` to get the AST list.
    - Fold over the AST nodes. Group text nodes under the most recent H2 (`{"h2", _, _, _}`) or H3 tag encountered.
    - Extract raw text from the nodes. 
    - Return a list of string chunks. Enforce a maximum length fallback (e.g., ~4000 chars per chunk) to avoid embedding token limit exhaustion.
  </action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_base/markdown_parser_test.exs</automated>
  </verify>
  <done>Parser successfully converts raw markdown strings into a list of chunked strings based on structural headers.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Oban Worker and Wire Context API</name>
  <files>lib/cairnloop/knowledge_base/workers/chunk_revision.ex, lib/cairnloop/knowledge_base.ex</files>
  <action>
    1. Create `Cairnloop.KnowledgeBase.Workers.ChunkRevision`.
       - `use Oban.Worker, queue: :default, unique: [period: 60]`.
       - In `perform/1`, fetch the `Revision` along with its `content` (from `Article` if content is stored there, or `Revision` schema if it has a `body` field - check schema, the context implies `revision.content` or we just parse the markdown).
       - Add telemetry: `:telemetry.execute([:openinference, :span, :start], ...)` and `stop` wrapping the process.
       - Use `MarkdownParser.parse/1` to get chunks.
       - Use `Cairnloop.Embedder.ExternalApi.generate_embeddings/1` to get vectors.
       - **Idempotency**: Use `Ecto.Multi.delete_all(:delete, from(c in Chunk, where: c.revision_id == ^revision_id))` followed by `Ecto.Multi.insert_all` to persist the chunks mapping their string and vector directly to the `cairnloop_chunks` table using `Pgvector.Ecto.Vector`.
    
    2. Update `lib/cairnloop/knowledge_base.ex`.
       - In `publish_revision(revision)`, append an `Ecto.Multi` step to insert the Oban job: `Oban.insert(:chunk_job, Cairnloop.KnowledgeBase.Workers.ChunkRevision.new(%{revision_id: revision.id}))`.
       - Expose a new function `search_chunks(embedding_vector, limit \\ 5)` that queries the `Chunk` schema using `order_by([c], fragment("? <-> ?", c.embedding, ^embedding_vector))`.
  </action>
  <verify>
    <automated>mix test test/cairnloop/knowledge_base_test.exs</automated>
  </verify>
  <done>Job is enqueued upon publish, worker is idempotent and traces execution, and search API is exposed.</done>
</task>

<task type="auto">
  <name>Task 4: Update M008-ROADMAP.md Progress</name>
  <files>.planning/M008-ROADMAP.md</files>
  <action>
    - Update the `M008-ROADMAP.md` Progress table. Mark "Phase 3: Semantic Chunking & pgvector Embeddings (Oban)" as "Complete" and set the "Completed" date to today.
  </action>
  <verify>
    <automated>grep -q "Complete" .planning/M008-ROADMAP.md</automated>
  </verify>
  <done>M008-ROADMAP.md Progress table is updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal -> External API | API requests send internal text chunks to an external 3rd-party LLM endpoint for embeddings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Embedder.ExternalApi | accept | The articles sent for embedding are knowledge base articles intended for organizational or public consumption. No PII is embedded. |
| T-03-02 | Denial of Service | MarkdownParser | mitigate | Enforce maximum chunk size parsing and string length safety checks to prevent memory exhaustion or token limit errors at the external API. |
| T-03-03 | Tampering | ChunkRevision Worker | mitigate | Use Ecto.Multi to idempotently clear old chunks associated with the revision ID before inserting new ones, preventing corrupted duplicated state on retries. |
</threat_model>

<verification>
- `mix.exs` compiles cleanly with the new `Req` dependency.
- Earmark correctly yields lists of strings grouped by headers.
- Publish `Revision` workflow spawns a queued Oban job.
- `search_chunks/2` compiles and produces a valid Ecto query using the pgvector `<->` operator.
</verification>

<success_criteria>
- The RAG substrate is fully wired. Background Oban workers chunk markdown and generate pgvector embeddings automatically upon Knowledge Base article publish actions, without blocking the Phoenix request cycle.
</success_criteria>

<output>
After completion, create `.planning/phases/03-Semantic-Chunking/03-01-SUMMARY.md`
</output>