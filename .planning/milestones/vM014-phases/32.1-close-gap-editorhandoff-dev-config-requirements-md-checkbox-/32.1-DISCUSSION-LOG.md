# Phase 32.1: Close gap — EditorHandoff dev config + REQUIREMENTS.md checkbox cleanup + BRAND-04 gate fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-29
**Phase:** 32.1-close-gap-editorhandoff-dev-config-requirements-md-checkbox-
**Areas discussed:** Phase 30 code warning scope, mix.lock version bump handling

---

## Phase 30 code warning scope

| Option | Description | Selected |
|--------|-------------|----------|
| Tight scope only | Phase 32.1 delivers exactly the 3 named fixes. CR-02 and WR-03 go to the deferred list for vM015. | ✓ |
| Fold in CR-02 only | Add error-handling to open_for_manual_edit for calm flash on DB/scope error. | |
| Fold in both CR-02 + WR-03 | Also fix Editor.mount/3 direct repo() call (arch-invariant #5 violation). | |

**User's choice:** Tight scope only
**Notes:** Phase 32.1 is bounded to the 3 tasks named in the phase title. CR-02 and WR-03 deferred to vM015.

---

## mix.lock version bump handling

| Option | Description | Selected |
|--------|-------------|----------|
| Commit with Phase 32.1 | Include mix.lock in the Phase 32.1 commit — keeps example app deps current. | ✓ |
| Commit separately first | Commit mix.lock as a standalone chore commit before Phase 32.1 changes. | |
| Hold out, skip | Leave mix.lock unstaged. | |

**User's choice:** Commit with Phase 32.1
**Notes:** Pre-existing version bumps (decimal 3.1.1, oban 2.23.0, swoosh 1.25.3) committed alongside the gap-closure changes to prevent lingering unstaged drift.

---

## Claude's Discretion

- **BRAND-04 wildcard fix scope:** Applied `**/*.ex` to both `@web_dir` (line 32) and
  `@example_live_dir` (line 33) for consistent recursive scanning. The audit only flagged
  `@web_dir`, but applying it to both is more robust and harmless.
- **Troubleshooting guide placement:** New `CAIRNLOOP_HANDOFF_SECRET_KEY_BASE` section appends
  at the end of `guides/04-troubleshooting.md` as its own `##` block, following the existing
  Symptom/Cause/Fix pattern.
- **runtime.exs placement:** `fetch_env!` config goes inside the `config_env() == :prod`
  block, mirroring existing prod-only secret patterns.
- **dev.exs placement:** Config block appended after the existing Endpoint config, before
  any `import_config` calls.

## Deferred Ideas

- **CR-02 → vM015:** `open_for_manual_edit` bare-match crash path in suggestion_review.ex —
  non-blocking warning, deferred to vM015.
- **WR-03 → vM015:** `Editor.mount/3` direct `repo()` call violates arch-invariant #5 —
  fix: add `KnowledgeBase.get_article/2` READ facade. Deferred to vM015.
