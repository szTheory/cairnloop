---
phase: 27-realistic-demo-fixtures
plan: 07
type: execute
wave: 6
depends_on:
  - 27-06
files_modified:
  - examples/cairnloop_example/priv/repo/seeds.exs
autonomous: true
requirements:
  - FIX-02
tags:
  - seeds
  - elixir
  - oban
  - pgvector
  - m008-substrate
must_haves:
  truths:
    - "the SeedRun.run/0 main flow at the bottom of seeds.exs invokes the 5 builders in correct dependency order: build_articles/0 → build_conversations/1 → build_gaps/1 → build_suggestion/2 → drain_embedding_pipeline/0"
    - "drain_embedding_pipeline/0 calls Oban.drain_queue(queue: :default, with_recursion: true) and pattern-matches the returned %{success, failure, snoozed, cancelled, discard} map"
    - "drain_embedding_pipeline/0 emits IO.warn when failure > 0 and IO.puts the success count on the happy path (already shipped as the real body in plan 27-01)"
    - "the script's final IO output indicates row counts so an adopter running `mix setup` sees concrete evidence of what was seeded (e.g. 'Seeded 16 conversations, 5 articles, 3 gap candidates, 1 article suggestion; drained N embedding jobs.')"
    - "running `cd examples/cairnloop_example && mix ecto.reset` (which invokes `ecto.setup` → `mix run priv/repo/seeds.exs`) completes without raising and leaves cairnloop_chunks non-empty"
  artifacts:
    - path: "examples/cairnloop_example/priv/repo/seeds.exs"
      provides: "Wired main flow with end-to-end seed + drain orchestration and adopter-facing IO summary"
      contains: "drain_embedding_pipeline"
  key_links:
    - from: "SeedRun.run/0"
      to: "Cairnloop.KnowledgeBase.Workers.ChunkRevision"
      via: "drain_embedding_pipeline → Oban.drain_queue → ChunkRevision.perform"
      pattern: "Oban.drain_queue"
    - from: "ChunkRevision.perform"
      to: "cairnloop_chunks (pgvector table)"
      via: "Embedder.ExternalApi.generate_embeddings (zero-vector fallback if no OPENAI_API_KEY)"
      pattern: "cairnloop_chunks"
---

<objective>
Finalize seeds.exs by wiring the SeedRun.run/0 main flow that orchestrates all 5 builders in dependency order, and adopter-facing IO output that summarizes what was seeded. drain_embedding_pipeline/0's body already shipped in plan 27-01's skeleton; this plan adds the call-site wiring (return-value threading) and the adopter-facing summary line so `mix setup` produces a visible self-test signal.

Purpose: FIX-02's second half — "the live ChunkRevision Oban worker drives embeddings through pgvector". The 6 enqueued ChunkRevision jobs (5 first-publish + 1 v2 republish from plan 27-03) are drained synchronously, landing chunks in cairnloop_chunks. This is what makes FIX-02 a substrate self-test rather than a fixture shortcut.

Output: A complete, end-to-end-runnable seeds.exs that an adopter can execute via `mix ecto.reset` and walk away with a populated dashboard + non-empty cairnloop_chunks table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md
@.planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md
@.planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md
@CLAUDE.md
@examples/cairnloop_example/priv/repo/seeds.exs

<interfaces>
<!-- Oban.drain_queue/1 (verified deps/oban/lib/oban.ex:920-928):

       %{success: success, failure: failure, snoozed: snoozed, cancelled: cancelled, discard: discard} =
         Oban.drain_queue(queue: :default, with_recursion: true)

     :default queue config in example app (examples/cairnloop_example/config/config.exs:54-57):
       queues: [default: 10]

     :testing mode in MIX_ENV=test (examples/cairnloop_example/config/test.exs:2):
       testing: :manual — drain_queue still works (it explicitly runs available jobs from the table).

     Builder call sequence (already prefigured by plan 27-01's skeleton):
       articles      = build_articles()
       conversations = build_conversations(articles)
       _gaps         = build_gaps(conversations)
       {sugg, task}  = build_suggestion(articles, conversations)
       drain_result  = drain_embedding_pipeline()
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire the SeedRun.run/0 main flow + adopter-facing IO summary</name>
  <files>examples/cairnloop_example/priv/repo/seeds.exs</files>
  <read_first>
    - examples/cairnloop_example/priv/repo/seeds.exs (final state after plans 27-01, 27-03, 27-04, 27-05, 27-06 — all 5 builders should now have real bodies; the run/0 entry point was stubbed in plan 27-01)
    - .planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md D-08 (drain at end), D-21 (warnings-clean script)
    - .planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md (Code Examples §Oban drain call shape; Pitfall 7 testing: :manual is fine for drain_queue/1)
    - .planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md (seeds.exs section — Oban drain pattern)
  </read_first>
  <action>
Modify the SeedRun.run/0 function (defined in plan 27-01's skeleton) inside examples/cairnloop_example/priv/repo/seeds.exs to:

1. **Capture each builder's return value with named bindings** (not underscored — we'll use them for the summary):

       def run() do
         IO.puts("Seeding Cairnloop example app demo data...")

         articles      = build_articles()
         conversations = build_conversations(articles)
         gaps          = build_gaps(conversations)
         {suggestion, _review_task} = build_suggestion(articles, conversations)

         drain_summary = drain_embedding_pipeline()

         emit_seed_summary(articles, conversations, gaps, suggestion, drain_summary)
         :ok
       end

2. **Modify drain_embedding_pipeline/0 (already real-bodied in plan 27-01) to RETURN the drain map** instead of just emitting IO. The new body:

       defp drain_embedding_pipeline() do
         IO.puts("Draining embedding pipeline (Oban :default queue)...")
         %{success: success, failure: failure} = result =
           Oban.drain_queue(queue: :default, with_recursion: true)

         if failure > 0 do
           IO.warn("Seed embedding pipeline drained with #{failure} failures. " <>
                   "Inspect oban_jobs.errors for details.")
         end

         IO.puts("Drained #{success} embedding job(s).")
         result
       end

   (The change vs plan 27-01: return `result` instead of returning the last IO.puts expression, and capture `result` in the destructuring with `= result`. This is additive — the existing IO output is preserved.)

3. **Add a new private helper emit_seed_summary/5** that prints a single concise summary line for the adopter:

       defp emit_seed_summary(articles, conversations, gaps, suggestion, drain_summary) do
         article_count = map_size(articles)
         conversation_count = length(conversations)
         gap_count = length(gaps)
         suggestion_count = if suggestion, do: 1, else: 0
         drained = drain_summary.success
         failures = drain_summary.failure

         IO.puts(
           "Seeded #{conversation_count} conversation(s), #{article_count} article(s), " <>
           "#{gap_count} gap candidate(s), #{suggestion_count} article suggestion(s); " <>
           "drained #{drained} embedding job(s) (#{failures} failure(s))."
         )
       end

4. **Ensure the script's bottom invokes CairnloopExample.SeedRun.run()** as the final top-level expression. The plan 27-01 skeleton already includes this line; verify it is still present and exactly reads:

       CairnloopExample.SeedRun.run()

   (No surrounding `if Mix.env() ... end` gate — `mix ecto.setup` always runs the seed in dev/prod; the integration test in plan 27-08 will eval_file this script under MIX_ENV=test where Oban testing: :manual makes drain_queue still safe per Pitfall 7.)

5. **No additional builder changes** — this plan only touches the main flow + drain_embedding_pipeline's return value + the new emit_seed_summary helper. Builders 03–06 remain untouched.

Warnings hygiene (D-21, C-02):
- The `result` capture in the new drain body must NOT shadow any earlier binding (it doesn't — the function-scope binding is fresh).
- The `if suggestion, do: 1, else: 0` truthiness check is correct because plan 27-06's build_suggestion/2 returns `{%ArticleSuggestion{}, %ReviewTask{}}` (truthy) on success — if the suggestion insert ever returns nil (it shouldn't; plan 27-06 uses `{:ok, s} = …`), the summary correctly reports 0.
- No IO.inspect/1.
- No `Logger.*` (script uses IO).
  </action>
  <verify>
    <automated>cd examples/cairnloop_example &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tee /tmp/p27-07-compile.log; echo '---'; echo 'run/0-orchestration-present:'; grep -cE 'def run\(\)' examples/cairnloop_example/priv/repo/seeds.exs; echo 'drain-call-present:'; grep -c 'Oban.drain_queue(queue: :default, with_recursion: true)' examples/cairnloop_example/priv/repo/seeds.exs; echo 'emit_seed_summary-defined:'; grep -c 'defp emit_seed_summary' examples/cairnloop_example/priv/repo/seeds.exs; echo 'script-final-line-invokes-SeedRun.run:'; tail -5 examples/cairnloop_example/priv/repo/seeds.exs | grep -c 'CairnloopExample.SeedRun.run()'; echo 'builder-call-order-in-run:'; awk '/def run\(\)/,/^  end$/' examples/cairnloop_example/priv/repo/seeds.exs | grep -E 'build_(articles|conversations|gaps|suggestion)|drain_embedding_pipeline' | head -10</automated>
    <human-check>Open seeds.exs; read SeedRun.run/0 from top to bottom and confirm the call order is: build_articles → build_conversations → build_gaps → build_suggestion → drain_embedding_pipeline → emit_seed_summary. Confirm the script's last top-level expression is `CairnloopExample.SeedRun.run()` with no conditional wrapping.</human-check>
  </verify>
  <acceptance_criteria>
- cd examples/cairnloop_example && mix compile --warnings-as-errors exits 0.
- grep -cE 'def run\(\)' examples/cairnloop_example/priv/repo/seeds.exs returns 1.
- grep -c 'Oban.drain_queue(queue: :default, with_recursion: true)' returns 1 (single drain call, in drain_embedding_pipeline/0).
- grep -c 'defp emit_seed_summary' returns 1.
- The script's final non-blank, non-comment line is exactly `CairnloopExample.SeedRun.run()`.
- The 5 builder calls appear in dependency order within run/0 (awk-scoped grep returns build_articles before build_conversations before build_gaps before build_suggestion before drain_embedding_pipeline).
- End-to-end FIX-02 assertion (`Repo.aggregate(Cairnloop.KnowledgeBase.Chunk, :count) > 0` after run) ships in plan 27-08's integration test.
  </acceptance_criteria>
  <done>
All acceptance_criteria conditions hold; seeds.exs is end-to-end runnable; the adopter-facing IO summary makes the seed's effect visible.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeds.exs run/0 → Oban supervisor → ChunkRevision worker → pgvector | The drain at end is the FIX-02 self-test path; skipping it or running it before all publish_revision calls would leave cairnloop_chunks empty |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-20 | Tampering | drain called BEFORE build_articles publishes revisions (no jobs to drain → empty chunks) | mitigate | run/0 enforces strict call order: build_articles → … → drain_embedding_pipeline. Acceptance criterion pins this order via awk-scoped grep. |
| T-27-21 | Denial of Service | drain raises if Oban supervisor is not up (e.g. mix run --no-start) | accept | The seeds.exs header comment from plan 27-01 documents that `--no-start` is incompatible with the drain. mix run (default) and mix ecto.setup both start the Application. Adopter-facing constraint, not a runtime check. |
| T-27-22 | Repudiation | adopter cannot tell whether the seed ran or what it produced | mitigate | emit_seed_summary/5 prints a one-line summary with concrete counts, making the seed's effect visible in mix setup output. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | No new packages. |
</threat_model>

<verification>
After this task:
1. mix compile --warnings-as-errors exits 0.
2. The script's call order is correct.
3. The adopter-facing summary line is emitted.
4. End-to-end execution proof (running mix ecto.reset against a real Postgres and observing cairnloop_chunks non-empty) is the responsibility of plan 27-08's integration test; this plan ships the wiring.
</verification>

<success_criteria>
- seeds.exs is end-to-end runnable: builders 03–06 supply the data, drain_embedding_pipeline turns enqueued jobs into pgvector rows, emit_seed_summary signals success to the adopter.
- The script's main flow is one cohesive, top-to-bottom-readable function.
- No library code modified.
- Warnings-clean.
</success_criteria>

<output>
Create .planning/phases/27-realistic-demo-fixtures/27-07-SUMMARY.md when done — record:
- The verbatim final line of seeds.exs (should be `CairnloopExample.SeedRun.run()`).
- The expected drain success count for a fresh DB (should equal the number of publish_revision/1 calls from plan 27-03 — 5 first-publishes + 1 v2 republish = 6 jobs, all draining to :success).
- A copy of the adopter-facing summary line as it appears in the IO.puts call.
- Any deviations from this plan.
</output>
