---
phase: 27-realistic-demo-fixtures
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/lib/cairnloop_example/demo_context_provider.ex
  - examples/cairnloop_example/config/config.exs
  - examples/cairnloop_example/test/cairnloop_example/demo_context_provider_test.exs
autonomous: true
requirements:
  - FIX-01
tags:
  - elixir
  - phoenix
  - context-provider
  - behaviour-impl
  - headless-test
must_haves:
  truths:
    - "CairnloopExample.DemoContextProvider implements Cairnloop.ContextProvider and returns deterministic per-actor maps keyed by conversation.host_user_id"
    - "the provider fail-opens to {:ok, %{}} for unknown actors (mirrors DefaultContextProvider behaviour-doc contract)"
    - "examples/cairnloop_example/config/config.exs wires context_provider: CairnloopExample.DemoContextProvider inside the existing config :cairnloop, … keyword list block (no duplicate-key block)"
    - "headless test asserts documented shape for at least 5 known demo actors + 1 unknown actor; runs in <1 sec without Repo"
  artifacts:
    - path: "examples/cairnloop_example/lib/cairnloop_example/demo_context_provider.ex"
      provides: "Demo Cairnloop.ContextProvider implementation"
      contains: "@behaviour Cairnloop.ContextProvider"
    - path: "examples/cairnloop_example/test/cairnloop_example/demo_context_provider_test.exs"
      provides: "Pure headless test of get_context/2 shape + fail-open"
      contains: "use ExUnit.Case, async: true"
    - path: "examples/cairnloop_example/config/config.exs"
      provides: "Configured-adapter wire for the demo provider"
      contains: "context_provider: CairnloopExample.DemoContextProvider"
  key_links:
    - from: "examples/cairnloop_example/config/config.exs"
      to: "examples/cairnloop_example/lib/cairnloop_example/demo_context_provider.ex"
      via: "configured-adapter pattern (Application.get_env(:cairnloop, :context_provider, DefaultContextProvider))"
      pattern: "context_provider: CairnloopExample.DemoContextProvider"
    - from: "examples/cairnloop_example/lib/cairnloop_example/demo_context_provider.ex"
      to: "Cairnloop.ContextProvider"
      via: "behaviour"
      pattern: "@behaviour Cairnloop.ContextProvider"
---

<objective>
Ship the example-app demo Cairnloop.ContextProvider implementation (CairnloopExample.DemoContextProvider), its headless test, and the one-line wire in config.exs — per D-10, D-11, D-12, D-20. After this plan, the operator inbox renders categorized per-actor context sections for the demo customers seeded in subsequent plans, instead of the empty {:ok, %{}} map the DefaultContextProvider returns.

Purpose: FIX-01's "ContextProvider snippets" criterion. This plan is fully independent of seeds.exs (different files, no shared state) so it can run parallel to plan 27-01 in Wave 1.

Output: A 50–80 LOC behaviour-impl module with ~5 pattern-matched actor clauses + a fail-open default; a ~30-line headless test covering known + unknown actors + section-key string contract; one additive line in config.exs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md
@.planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md
@.planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md
@CLAUDE.md
@prompts/cairnloop_brand_book.md
@lib/cairnloop/context_provider.ex
@lib/cairnloop/default_context_provider.ex
@examples/cairnloop_example/config/config.exs
@test/cairnloop/context_provider_test.exs

<interfaces>
<!-- Cairnloop.ContextProvider behaviour contract (verified lib/cairnloop/context_provider.ex):

       @callback get_context(actor_id :: String.t(), opts :: keyword()) ::
                   {:ok, map()} | {:error, term()}

     Documented return shape (lib/cairnloop/context_provider.ex:14-21):

       {:ok, %{
         "User Details" => %{name: "Alice", lifetime_value: "$450"},
         "Active Plan" => %{tier: "Pro", status: "past_due"}
       }}

     Key contract points:
       - Top-level keys MUST be strings (rendered as section headers in operator UI).
       - Inner-map values are simple terms (strings, integers, booleans, dates) — never raw atoms.
       - Unknown actors return {:ok, %{}} ("fail-open"). Behaviour doc mandates UI graceful degradation.
       - Library lookup site: lib/cairnloop/web/conversation_live.ex:358 — Application.get_env(:cairnloop, :context_provider, DefaultContextProvider) |> provider.get_context(conversation.host_user_id, []).

     Configured-adapter slot to extend in config.exs (verified examples/cairnloop_example/config/config.exs:59-61):

       config :cairnloop,
         repo: CairnloopExample.Repo,
         tools: [Cairnloop.Tools.InternalNote]

     becomes:

       config :cairnloop,
         repo: CairnloopExample.Repo,
         tools: [Cairnloop.Tools.InternalNote],
         context_provider: CairnloopExample.DemoContextProvider
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CairnloopExample.DemoContextProvider with brand-voice per-actor clauses + headless test</name>
  <files>examples/cairnloop_example/lib/cairnloop_example/demo_context_provider.ex, examples/cairnloop_example/test/cairnloop_example/demo_context_provider_test.exs</files>
  <read_first>
    - lib/cairnloop/context_provider.ex (the behaviour, full file)
    - lib/cairnloop/default_context_provider.ex (the 13-LOC default impl — the shape to mimic)
    - test/cairnloop/context_provider_test.exs (the 14-LOC headless test pattern)
    - .planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md D-10, D-11, D-12 (per-actor return contract + brand-voice §7.5)
    - .planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md (Pattern Assignments section for demo_context_provider.ex and demo_context_provider_test.exs — mimic + divergence rules)
    - .planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md (Pitfall 3 — host_user_id is overloaded; provider key MUST be conversation.host_user_id customer identity like "demo_user_acme_billing"; NOT operator-session identity "demo_operator")
    - prompts/cairnloop_brand_book.md (sections 5.5 UX microcopy register, 7.5 no raw Elixir terms; calm/fail-closed/reason-forward)
  </read_first>
  <behavior>
    - Test "get_context/2 returns documented shape for demo_user_acme_billing": asserts {:ok, %{"User Details" => ud, "Active Plan" => ap}} = DemoContextProvider.get_context("demo_user_acme_billing", []); asserts is_binary(ud[:email]), is_binary(ap[:tier]).
    - Test "get_context/2 returns at least 2 sections for each known demo actor": parameterized loop over ["demo_user_acme_billing", "demo_user_globex_seats", "demo_user_initech_billing", "demo_user_umbrella_ci", "demo_user_hooli_tokens"]; for each, asserts {:ok, ctx} = DemoContextProvider.get_context(actor, []) and map_size(ctx) >= 2.
    - Test "get_context/2 fail-opens for unknown actors": assert {:ok, %{}} == DemoContextProvider.get_context("totally_random_unknown_user_xyz", []).
    - Test "all section keys are strings (no raw atoms surfaced)": for each known actor's returned map, asserts Enum.all?(Map.keys(ctx), &is_binary/1).
    - Test "all inner-map values are simple terms (no Elixir atoms in values)": for each known actor's returned map, walks each section's inner map and asserts that every value is either a binary, integer, float, boolean, Date, DateTime, or nil — explicitly NOT an atom (other than nil).
  </behavior>
  <action>
Create examples/cairnloop_example/lib/cairnloop_example/demo_context_provider.ex with:

Module name: CairnloopExample.DemoContextProvider.

@moduledoc (multi-line): explains this is the example-app demo Cairnloop.ContextProvider implementation, returns plausible per-actor context for the 16 seeded demo conversations, and fail-opens to {:ok, %{}} for unknown actors so the operator inbox degrades gracefully. Tone follows brand book section 7.5 (calm, factual, no raw Elixir terms in the docstring prose).

@behaviour Cairnloop.ContextProvider.

Module-level def get_context(actor_id, opts \\ []) head followed by pattern-matched clauses (D-10 says ~30–50 LOC; ship 5 clauses):

- "demo_user_acme_billing" — Acme Corp, billing-past-due persona. Sections: "User Details" (name "Riya Chen", email "riya@acme.example", joined "2025-08-14"), "Active Plan" (tier "Team", seats 8, status "past due"), "Recent Charges" (last_charge_at "2026-05-12", last_amount "$48.00", currency "USD").
- "demo_user_globex_seats" — Globex Inc, growing-team persona. Sections: "User Details" (name "Mateo Alvarez", email "mateo@globex.example", joined "2025-11-02"), "Active Plan" (tier "Team", seats 4, status "active"), "Seats" (seats_used 4, seats_total 4, pending_invites 1).
- "demo_user_initech_billing" — Initech LLC, billing-email-update persona. Sections: "User Details" (name "Sora Lin", email "sora@initech.example", joined "2024-09-30"), "Active Plan" (tier "Starter", seats 2, status "active"), "Billing" (billing_email "accounts@initech.example", payment_method "card ending 4242").
- "demo_user_umbrella_ci" — Umbrella Co, CI-diagnostic persona. Sections: "User Details" (name "Priya Sharma", email "priya@umbrella.example", joined "2026-01-15"), "Active Plan" (tier "Pro", seats 12, status "active"), "Recent CI Runs" (last_run_at "2026-05-25T18:22:00Z", last_run_status "skipped", consecutive_skips 3).
- "demo_user_hooli_tokens" — Hooli Industries, token-rotation persona. Sections: "User Details" (name "Jonas Weber", email "jonas@hooli.example", joined "2025-04-08"), "Active Plan" (tier "Team", seats 6, status "active"), "API Keys" (active_keys 2, last_rotated_at "2026-02-10", expiring_within_30_days 1).

Final catch-all clause: def get_context(_actor_id, _opts), do: {:ok, %{}} — D-10 fail-open.

Brand-voice constraints (C-08, D-12, D-18) — these apply to every string value in the returned maps:
- No raw Elixir atoms in any string value. Status strings are human-readable ("past due", "active", "skipped") NOT :past_due / :active / :skipped.
- No raw JSON-shaped strings; values are plain text or numeric.
- Email addresses use .example TLD (RFC 2606 reserved — safe for demo).
- Dates as ISO 8601 strings (rendered as-is in the operator UI; D-12 says "calm, factual").

Add @impl true on every def get_context/2 clause (including the catch-all). Use the multi-clause-with-default-on-head idiom (PATTERNS.md — do NOT put \\ [] on each clause individually; put it on the head only, or omit the default since conversation_live.ex:361 always passes an explicit opts list).

Create examples/cairnloop_example/test/cairnloop_example/demo_context_provider_test.exs with:
- defmodule CairnloopExample.DemoContextProviderTest do
- use ExUnit.Case, async: true (no Repo per D-20 / PATTERNS.md)
- alias CairnloopExample.DemoContextProvider
- The 5 tests listed in <behavior> above. Use describe "DemoContextProvider" do … end to group.
- DO NOT alias or use CairnloopExample.DataCase — this test must remain pure to satisfy CLAUDE.md C-04 (Cairnloop.Repo may be unavailable; the same caveat applies to CairnloopExample.Repo).
  </action>
  <verify>
    <automated>cd examples/cairnloop_example &amp;&amp; mix test test/cairnloop_example/demo_context_provider_test.exs --color 2&gt;&amp;1 | tee /tmp/p27-02-test.log; grep -E '(0 failures|failed)' /tmp/p27-02-test.log</automated>
    <human-check>Read the new module and confirm: (a) no raw Elixir atoms appear in any string value (grep -E ':[a-z_]+' on the file body, excluding the @behaviour/@impl/key atoms — should show only :ok, {:ok, …} and standard atoms, never demo-data atoms), (b) all 5 demo actor ids exactly match the strings used in plan 27-04's seeded Conversation.host_user_id values (cross-reference once that plan is written).</human-check>
  </verify>
  <acceptance_criteria>
- examples/cairnloop_example/lib/cairnloop_example/demo_context_provider.ex exists with @behaviour Cairnloop.ContextProvider, 5 known-actor clauses, and a fail-open catch-all.
- examples/cairnloop_example/test/cairnloop_example/demo_context_provider_test.exs exists with 5 tests, all green.
- cd examples/cairnloop_example && mix test test/cairnloop_example/demo_context_provider_test.exs exits 0 in <2 seconds.
- cd examples/cairnloop_example && mix compile --warnings-as-errors exits 0.
- grep -c "demo_user_" examples/cairnloop_example/lib/cairnloop_example/demo_context_provider.ex returns exactly 5 distinct id clauses (use grep -E '"demo_user_[a-z_]+"' | sort -u | wc -l == 5 as the exact gate).
  </acceptance_criteria>
  <done>
All acceptance_criteria conditions hold; the provider module + test ship green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the demo provider in examples/cairnloop_example/config/config.exs</name>
  <files>examples/cairnloop_example/config/config.exs</files>
  <read_first>
    - examples/cairnloop_example/config/config.exs (the full file — read lines 1–65 to confirm the existing config :cairnloop, … keyword list block at lines 59–61 has not drifted)
    - .planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md D-11 (additive wire in example app only)
    - .planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md (config/config.exs section — extend the existing block; do NOT add a second config :cairnloop, :context_provider, … line)
  </read_first>
  <action>
Edit examples/cairnloop_example/config/config.exs:
- Locate the existing config :cairnloop, repo: CairnloopExample.Repo, tools: [Cairnloop.Tools.InternalNote] block (currently lines ~59–61; verify by reading before editing).
- Extend the keyword list with one new key: context_provider: CairnloopExample.DemoContextProvider. Final block reads:

      config :cairnloop,
        repo: CairnloopExample.Repo,
        tools: [Cairnloop.Tools.InternalNote],
        context_provider: CairnloopExample.DemoContextProvider

Do NOT add a separate config :cairnloop, :context_provider, … line (PATTERNS.md — duplicate-key block triggers config-merge warnings and breaks scannability).

Do NOT modify examples/cairnloop_example/config/test.exs or any library-side config (D-11: example-app-only wire; the library's own tests rely on DefaultContextProvider as the unconfigured fallback at lib/cairnloop/web/conversation_live.ex:358).
  </action>
  <verify>
    <automated>cd examples/cairnloop_example &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tee /tmp/p27-02-cfg-compile.log; echo "wire-line:"; grep -c 'context_provider: CairnloopExample.DemoContextProvider' examples/cairnloop_example/config/config.exs; echo "config-cairnloop-blocks:"; grep -c '^config :cairnloop' examples/cairnloop_example/config/config.exs</automated>
    <human-check>Open config.exs and visually confirm: the wire is inside the same keyword list as :repo and :tools (no second config :cairnloop, … line was added).</human-check>
  </verify>
  <acceptance_criteria>
- grep -c 'context_provider: CairnloopExample.DemoContextProvider' examples/cairnloop_example/config/config.exs returns exactly 1.
- grep -c '^config :cairnloop' examples/cairnloop_example/config/config.exs returns exactly 1 (no duplicate block was introduced).
- cd examples/cairnloop_example && mix compile --warnings-as-errors exits 0.
- File examples/cairnloop_example/config/test.exs is unchanged (git status confirms).
  </acceptance_criteria>
  <done>
All acceptance_criteria conditions hold; the configured-adapter wire is in place and the example app's compile is warnings-clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host system (operator UI) → CairnloopExample.DemoContextProvider | The provider receives an arbitrary actor_id :: String.t() from ConversationLive.assign_context_data/1; must not crash, must not leak host data on unknown ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Information Disclosure | provider returning sensitive host data over an unscoped actor lookup | mitigate | DemoContextProvider returns ONLY hard-coded literal data for the 5 known demo actors; fail-opens to {:ok, %{}} for any other id. No host-system query, no string interpolation into queries, no atom creation. Safe by construction (ASVS V5). |
| T-27-05 | Denial of Service | provider raising on unknown actor (would crash ConversationLive) | mitigate | The catch-all def get_context(_actor_id, _opts), do: {:ok, %{}} clause prevents FunctionClauseError. Behaviour-doc contract at lib/cairnloop/context_provider.ex:21-24 mandates this fail-open shape; the headless test asserts it. |
| T-27-06 | Tampering | atom creation from arbitrary host input (atom-table exhaustion) | mitigate | The provider does no String.to_atom/1 or String.to_existing_atom/1 — all branching is pattern-matched on string literals. ASVS V5 satisfied by construction. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | Phase 27 introduces NO new package dependencies. RESEARCH.md Package Legitimacy Audit confirms zero new installs. |
</threat_model>

<verification>
After both tasks:
1. cd examples/cairnloop_example && mix test test/cairnloop_example/demo_context_provider_test.exs exits 0 in <2 sec.
2. cd examples/cairnloop_example && mix compile --warnings-as-errors exits 0.
3. grep -c 'context_provider: CairnloopExample.DemoContextProvider' examples/cairnloop_example/config/config.exs returns 1.
4. The example app's compile output mentions CairnloopExample.DemoContextProvider with no warnings.
</verification>

<success_criteria>
- The configured-adapter slot for :context_provider is now filled in the example app (D-11).
- The demo provider returns the documented per-actor shape (D-10) for all 5 demo customer ids that plan 27-04 will use, and fail-opens for unknown actors.
- Headless test asserts the brand-voice no-raw-atoms invariant (C-08, D-12) for every section value.
- No library code touched (C-07).
- Plan 02 ships independently of seeds.exs (different files), so a Plan-01-blocked plan 03 cannot block Plan 02's review.
</success_criteria>

<output>
Create .planning/phases/27-realistic-demo-fixtures/27-02-SUMMARY.md when done — record:
- The 5 confirmed demo customer ids (which plan 27-04 must match exactly on Conversation.host_user_id).
- Confirmation that the headless test runs in <2 sec without Repo.
- Confirmation that the config.exs wire extends the existing keyword list (single config :cairnloop block, not a second one).
- Any deviations from this plan (should be none).
</output>
