---
phase: 27-realistic-demo-fixtures
plan: 06
type: execute
wave: 5
depends_on:
  - 27-03
  - 27-04
  - 27-05
files_modified:
  - examples/cairnloop_example/priv/repo/seeds.exs
autonomous: true
requirements:
  - FIX-04
tags:
  - seeds
  - elixir
  - knowledge-automation
  - article-suggestion
  - review-task
must_haves:
  truths:
    - "build_suggestion/2 inserts exactly 1 Cairnloop.KnowledgeAutomation.ArticleSuggestion with status: :ready and suggestion_type: :article (sealed-enum reconciliation: spec :ready_for_review → :ready; spec :new_article → :article per PATTERNS §Sealed-enum table)"
    - "the suggestion has entrypoint_type: :gap_candidate and entrypoint_id pointing at a real seeded GapCandidate (from plan 27-05); tenant_scope: :host_user_scoped + host_user_id: 'demo_operator' (operator-scope per Pitfall 3)"
    - "proposed_markdown is hand-authored, brand-voice-compliant, and contains [1]/[2] footnote anchors (D-15)"
    - "embeds_many :evidence_snapshot contains exactly 2 ArticleSuggestionEvidence rows (D-16 minimum); BOTH rows are KB-chunk-grounded — they reference distinct chunks (chunk_index 0 and 1) of api_key_article's published revision so the validate_citation_target validator (which REQUIRES article_id/revision_id/chunk_index keys) is satisfied"
    - "every evidence row's metadata map contains a :destination sub-map (so validate_metadata_destination is satisfied) — destination shape is %{article_id: api_key_article.id, revision_id: api_key_revision.id}"
    - "evidence_digest is computed via the same algorithm KnowledgeAutomation.evidence_digest_for/1 uses (RESEARCH Code Examples + D-16): SHA-256 of Jason.encode! over the ordered field projection [source_type, trust_level, title, excerpt, citation_target, match_reasons] (metadata is excluded — load-bearing)"
    - "Workers.GenerateArticleSuggestion is NEVER enqueued from this builder (D-15: no LLM call from seeds)"
    - "after suggestion insert, KnowledgeAutomation.ensure_review_task_for_suggestion(suggestion.id, actor_id: \"system\") is called so a ReviewTask + :task_created ReviewTaskEvent are created (RESEARCH Critical Finding 2 — without this, SuggestionReview LiveView shows empty queue). NOTE: only :actor_id is read from opts; tenant_scope and host_user_id are sourced from the loaded suggestion automatically per lib/cairnloop/knowledge_automation.ex:116-159"
    - "build_suggestion/2 is idempotent via a manual Repo.get_by/2 check keyed on :stable_key for the suggestion; ensure_review_task_for_suggestion/2 is itself idempotent (returns existing active task per lib/cairnloop/knowledge_automation.ex:128-129)"
  artifacts:
    - path: "examples/cairnloop_example/priv/repo/seeds.exs"
      provides: "Implemented build_suggestion/2 producing 1 ArticleSuggestion :ready + 2 KB-chunk evidence rows + 1 ReviewTask + 1 task_created event"
      contains: "ensure_review_task_for_suggestion"
  key_links:
    - from: "ArticleSuggestion"
      to: "KnowledgeBaseLive.SuggestionReview"
      via: "ReviewTask (article_suggestion_id) — LiveView reads list_review_tasks/1, NOT list_article_suggestions"
      pattern: "ensure_review_task_for_suggestion"
    - from: "ArticleSuggestion.evidence_digest"
      to: "future CandidateBuilder re-computation"
      via: "deterministic SHA-256 over [source_type, trust_level, title, excerpt, citation_target, match_reasons]"
      pattern: "evidence_digest"
---

<objective>
Implement build_suggestion/2 in seeds.exs: insert 1 ArticleSuggestion in status :ready (the actual schema enum — spec language says ":ready_for_review") with suggestion_type :article (spec ":new_article"), 2 KB-chunk-grounded ArticleSuggestionEvidence rows pointing at distinct chunks of the canonical api_key article's published revision, the deterministic evidence_digest, and a companion ReviewTask via KnowledgeAutomation.ensure_review_task_for_suggestion/2. Without the ReviewTask, the SuggestionReview LiveView reads an empty queue (RESEARCH Critical Finding 2 / Pitfall 1) and FIX-04 silently fails.

Purpose: FIX-04's "≥ 1 ArticleSuggestion in :ready_for_review state with citation-backed proposed_markdown; SuggestionReview LiveView shows real review work on first boot" criterion.

Output: A working build_suggestion/2 body producing 1 suggestion + 2 KB-chunk evidence rows + 1 review_task + 1 task_created event on fresh DB, idempotent on re-run, returning {%ArticleSuggestion{}, %ReviewTask{}} for the script's bookkeeping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md
@.planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md
@.planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md
@CLAUDE.md
@prompts/cairnloop_brand_book.md
@lib/cairnloop/knowledge_automation/article_suggestion.ex
@lib/cairnloop/knowledge_automation/article_suggestion_evidence.ex
@lib/cairnloop/knowledge_automation.ex
@examples/cairnloop_example/priv/repo/seeds.exs

<interfaces>
<!-- Cairnloop.KnowledgeAutomation.ArticleSuggestion (verified lib/cairnloop/knowledge_automation/article_suggestion.ex):
       status enum         :: :pending_generation | :ready | :failed | :dismissed   (default :pending_generation)
       suggestion_type enum :: :article | :revision
       tenant_scope enum    :: :host_user_scoped | :public_only | :system_unscoped
       entrypoint_type enum :: :gap_candidate | :article_revision | :conversation_quick_fix
       required fields      :: stable_key, suggestion_type, status, tenant_scope,
                                entrypoint_type, entrypoint_id, proposed_markdown, grounding_metadata
       validation           :: stable_key length 8..160; proposed_markdown length >= 1;
                                grounding_metadata must be non-empty map;
                                tenant_scope :host_user_scoped requires host_user_id;
                                validate_anchor_rules — for {:article, :gap_candidate} both article_id AND
                                base_revision_id MUST be blank (line 144-148). DO NOT set them on the suggestion.

     Cairnloop.KnowledgeAutomation.ArticleSuggestionEvidence (embedded schema; verified lib/cairnloop/knowledge_automation/article_suggestion_evidence.ex):
       fields :: source_type, trust_level, title, excerpt, citation_target, metadata, match_reasons
       Used via embeds_many :evidence_snapshot on ArticleSuggestion.

       Enum values (sealed; verified lines 7-8):
         source_type :: :knowledge_base | :resolved_case | :unknown   (default :unknown)
         trust_level :: :canonical | :assistive | :unknown            (default :unknown)

       validate_required (lines 33-40): source_type, trust_level, title, excerpt, citation_target, metadata.

       validate_citation_target (lines 47-63) REQUIRES the citation_target map to contain:
         :article_id, :revision_id, :chunk_index — string OR atom keys are both accepted via map_lookup/2.
         Map size capped at 5 keys total.

       validate_metadata_destination (lines 65-83) REQUIRES the metadata map to contain a :destination
         sub-map (string OR atom keys). The :destination sub-map size is capped at 4 keys.

       IMPORTANT — there is NO ArticleSuggestion field for direct conversation linkage. The schema's only
       anchor fields are :article_id and :base_revision_id (rejected for {:article, :gap_candidate}).
       Conversation context surfaces through the seeded conversations themselves (plan 27-04); it does
       NOT need to appear in the suggestion's evidence rows.

     Cairnloop.KnowledgeAutomation.ensure_review_task_for_suggestion/2 (verified lib/cairnloop/knowledge_automation.ex:116-159):
       @spec ensure_review_task_for_suggestion(id :: integer, opts :: keyword) :: {:ok, ReviewTask.t}
       opts: ONLY :actor_id is read (line 117). tenant_scope and host_user_id are taken from the loaded
         suggestion (lines 137-138). DO NOT pass :tenant_scope or :host_user_id — they are ignored.
       Behavior: idempotent — returns {:ok, existing} if an active ReviewTask is already linked.

     Sealed-enum reconciliation (PATTERNS §Sealed-enum table — verbatim):
       CONTEXT spec :ready_for_review  →  schema enum :ready          (ArticleSuggestion.status)
       CONTEXT spec :new_article       →  schema enum :article        (ArticleSuggestion.suggestion_type)

     Evidence digest algorithm (verified lib/cairnloop/knowledge_automation.ex:961-976; field order is LOAD-BEARING):
       digest = evidence_snapshot
                |> Enum.map(fn e -> %{source_type: e.source_type, trust_level: e.trust_level,
                                      title: e.title, excerpt: e.excerpt,
                                      citation_target: e.citation_target,
                                      match_reasons: e.match_reasons} end)
                |> Jason.encode!()
                |> then(&:crypto.hash(:sha256, &1))
                |> Base.encode16(case: :lower)
       NOTE: :metadata is deliberately EXCLUDED from the digest (it's in serialize_evidence_snapshot/1 for storage only).

     Chunk indexing (verified lib/cairnloop/knowledge_base/markdown_parser.ex):
       Chunks are produced one per h2/h3 section, in document order, with chunk_index starting at 0.
       Plan 27-03's api_key article body is required to have ≥ 2 h2 sections (Pitfall 4 mitigation),
       so chunk_index 0 and 1 are both guaranteed to exist after publish_revision/1 + Oban drain.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement build_suggestion/2 with sealed-enum-correct ArticleSuggestion + 2 KB-chunk-grounded evidence rows + companion ReviewTask</name>
  <files>examples/cairnloop_example/priv/repo/seeds.exs</files>
  <read_first>
    - examples/cairnloop_example/priv/repo/seeds.exs (current state after plans 27-01, 27-03, 27-04, 27-05; confirm articles map shape from plan 27-03 and gap list from plan 27-05)
    - lib/cairnloop/knowledge_automation/article_suggestion.ex (sealed enum lists + validate_required + validate_host_scope + validate_grounding_metadata + validate_anchor_rules — confirm article_id/base_revision_id are REJECTED for {:article, :gap_candidate} and must therefore NOT be set on the suggestion)
    - lib/cairnloop/knowledge_automation/article_suggestion_evidence.ex (embedded schema fields + validate_citation_target requiring article_id/revision_id/chunk_index + validate_metadata_destination requiring metadata.destination map)
    - lib/cairnloop/knowledge_automation.ex (ensure_review_task_for_suggestion/2 at line 116 — verify signature reads ONLY :actor_id from opts; idempotency behaviour at line 128)
    - lib/cairnloop/knowledge_automation.ex (evidence_digest_for/1 at lines 961–976 — mirror the algorithm exactly, including metadata EXCLUSION)
    - lib/cairnloop/knowledge_base/markdown_parser.ex (chunk_index numbering — chunks are 0-indexed per h2/h3)
    - .planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md D-15, D-16
    - .planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md (Critical Finding 2; Pitfall 1; Pitfall 2; Pattern 4; Code Examples §Evidence digest computation)
    - .planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md (Sealed-enum reconciliation table; seeds.exs section)
    - Plan 27-03's article handle map keys (the suggestion's evidence cites the api_key article's published revision — chunk_index 0 and 1)
    - Plan 27-05's gap list (the suggestion's entrypoint_id targets demo_gap_billing_export)
  </read_first>
  <action>
Replace the # TODO stub body of build_suggestion/2 with:

1. **Define the suggestion's stable identity at the top:**

       @demo_suggestion_stable_key "demo:article_suggestion:billing_export:v1"

2. **Look up the entrypoint gap and the canonical article whose chunks we'll cite:**

       defp build_suggestion(articles, _conversations) do
         gap = CairnloopExample.Repo.get_by!(Cairnloop.KnowledgeAutomation.GapCandidate, stable_key: "demo_gap_billing_export")
         api_key_article = Map.fetch!(articles, :api_key)
         api_key_revision = CairnloopExample.Repo.get_by!(Cairnloop.KnowledgeBase.Revision,
                                                          article_id: api_key_article.id,
                                                          state: :published)
         …
       end

   `_conversations` is underscore-prefixed because the evidence rows are exclusively KB-chunk-grounded — they do not reference conversations. (Conversation context already lives in the demo via plan 27-04's seeded conversations; the schema has no direct ArticleSuggestion→Conversation anchor field, so there is no value in introducing one here. This decision resolves the prior revision's two blockers in one move.)

3. **Construct the 2 evidence rows — BOTH KB-chunk-grounded against api_key_article (D-16 minimum: 2 rows):**

   The supporting evidence for the proposed_markdown's [1] and [2] footnotes is the api_key article's own published chunks. The validate_citation_target validator REQUIRES `article_id`, `revision_id`, and `chunk_index`; the validate_metadata_destination validator REQUIRES `metadata.destination` as a map. Both rows below satisfy both validators.

       evidence_row_1 = %{
         source_type:     :knowledge_base,
         trust_level:     :canonical,
         title:           api_key_article.title,
         excerpt:         "Trailmark API keys are reset under Settings → API Keys; the reset flow surfaces the same audit log that backs billing-receipt traceability.",
         citation_target: %{
           article_id:   api_key_article.id,
           revision_id:  api_key_revision.id,
           chunk_index:  0
         },
         metadata: %{
           destination: %{
             article_id:  api_key_article.id,
             revision_id: api_key_revision.id
           },
           origin: "demo_seed"
         },
         match_reasons: ["matched canonical API-key reset adjacency", "shares audit-log surface with billing receipts"]
       }

       evidence_row_2 = %{
         source_type:     :knowledge_base,
         trust_level:     :canonical,
         title:           api_key_article.title,
         excerpt:         "Multi-month bulk export of receipts is available via the API-key-authenticated export endpoint; CFO/billing-email changes do not invalidate prior receipts.",
         citation_target: %{
           article_id:   api_key_article.id,
           revision_id:  api_key_revision.id,
           chunk_index:  1
         },
         metadata: %{
           destination: %{
             article_id:  api_key_article.id,
             revision_id: api_key_revision.id
           },
           origin: "demo_seed"
         },
         match_reasons: ["matched canonical API-key bulk-export pathway"]
       }

       evidence_snapshot = [evidence_row_1, evidence_row_2]

   Both rows use the SAME revision_id (the api_key article's published revision) but DIFFERENT chunk_index values (0 and 1) — these correspond to the first two h2/h3 sections that MarkdownParser extracts. Plan 27-03's body for the api_key article MUST have ≥ 2 h2 sections (Pitfall 4 mitigation already pinned in 27-03). If for any reason the executor discovers only 1 h2 exists, the FIX is to add a second h2 in 27-03's body — NOT to invent a different evidence shape here.

   Both source_type :knowledge_base and trust_level :canonical are pinned by the schema enum lists (verified ArticleSuggestionEvidence lines 7-8). Do NOT use :resolved_case or :assistive for these rows — those would model conversation-derived evidence, which is not what we're shipping in this phase.

4. **Compute the evidence_digest deterministically** (D-16 + RESEARCH Code Examples — field order is load-bearing; metadata is excluded):

       defp compute_evidence_digest(evidence_snapshot) do
         evidence_snapshot
         |> Enum.map(fn e ->
           %{
             source_type:     e.source_type,
             trust_level:     e.trust_level,
             title:           e.title,
             excerpt:         e.excerpt,
             citation_target: e.citation_target,
             match_reasons:   e.match_reasons
           }
         end)
         |> Jason.encode!()
         |> then(&:crypto.hash(:sha256, &1))
         |> Base.encode16(case: :lower)
       end

   Note: the helper takes plain maps (the snapshot before cast_embed) and produces the SAME hash that the production `evidence_digest_for/1` would produce on the cast embeds, because the field-projection map is identical in shape and key ordering. Verify by reading the production function at lib/cairnloop/knowledge_automation.ex:961-976.

5. **Build the suggestion attrs (gap-driven path — do NOT set article_id / base_revision_id; validate_anchor_rules at line 144-148 rejects them):**

       proposed_markdown = """
       ## Exporting your billing receipts

       Trailmark stores monthly receipts under Settings → Billing → Receipts [1].
       Each receipt is downloadable as a PDF; multi-month bulk export is available via the API-key authenticated export flow [2].

       ## When to contact support

       If a receipt is missing or shows the wrong billing email, contact support with the receipt date and your account email.
       """

       suggestion_attrs = %{
         stable_key:         @demo_suggestion_stable_key,
         suggestion_type:    :article,              # CONTEXT spec :new_article  → actual schema enum :article
         status:             :ready,                # CONTEXT spec :ready_for_review → actual schema enum :ready
         tenant_scope:       :host_user_scoped,
         host_user_id:       "demo_operator",       # operator-scope per Pitfall 3
         entrypoint_type:    :gap_candidate,
         entrypoint_id:      gap.id,
         title:              "Exporting Trailmark billing receipts",
         operator_summary:   "Repeated customer requests for a billing-export flow surfaced through the demo_gap_billing_export gap. Hand-authored draft grounded in the canonical API-key article's published revision.",
         proposed_markdown:  proposed_markdown,
         grounding_metadata: %{"status" => "strong", "evidence_count" => length(evidence_snapshot)},
         evidence_snapshot:  evidence_snapshot,
         evidence_digest:    compute_evidence_digest(evidence_snapshot),
         generated_at:       DateTime.utc_now()
       }

   Validation gates the schema enforces (the changeset will reject otherwise):
   - grounding_metadata must be a non-empty map (validate_grounding_metadata/1).
   - tenant_scope: :host_user_scoped requires host_user_id (validate_host_scope/1) — set to "demo_operator".
   - stable_key length 8..160.
   - proposed_markdown length >= 1.
   - validate_anchor_rules for {:article, :gap_candidate} REJECTS article_id / base_revision_id — leave both unset on the suggestion. (The article_id / revision_id pair lives on the evidence rows' citation_target + metadata.destination, not on the suggestion itself.)
   - cast_embed(:evidence_snapshot, with: &ArticleSuggestionEvidence.changeset/2) runs the embedded changeset on each map; each map MUST contain source_type, trust_level, title, excerpt, citation_target, metadata. Each citation_target MUST contain article_id/revision_id/chunk_index. Each metadata MUST contain a :destination map. The two rows above satisfy all of these.

6. **Insert the suggestion idempotently** (the get_or_insert!/3 helper from plan 27-01 works for non-embedded fields, but embeds_many is cast at insert time, so we use a manual idempotency check keyed on stable_key):

       suggestion =
         case CairnloopExample.Repo.get_by(Cairnloop.KnowledgeAutomation.ArticleSuggestion,
                stable_key: @demo_suggestion_stable_key) do
           nil ->
             {:ok, s} =
               %Cairnloop.KnowledgeAutomation.ArticleSuggestion{}
               |> Cairnloop.KnowledgeAutomation.ArticleSuggestion.changeset(suggestion_attrs)
               |> CairnloopExample.Repo.insert()
             s

           existing ->
             existing
         end

7. **Companion ReviewTask via the library facade** (Critical Finding 2 / Pitfall 1; the function reads ONLY :actor_id from opts — tenant_scope and host_user_id are sourced from the loaded suggestion automatically):

       {:ok, review_task} =
         Cairnloop.KnowledgeAutomation.ensure_review_task_for_suggestion(
           suggestion.id,
           actor_id: "system"
         )

   ensure_review_task_for_suggestion/2 is idempotent (returns existing active task if one already linked, per line 128-129) so the re-run is safe.

8. **Return value:** `{suggestion, review_task}` — matches the skeleton from plan 27-01 (the run/0 main flow pattern-matches `{_sugg, _task} = build_suggestion(articles, conversations)`).

Constraints (do NOT violate):
- Spec language :ready_for_review and :new_article DO NOT EXIST in the schema enums. Use :ready and :article. The header reconciliation table in plan 27-01 documents this.
- Both evidence rows are KB-chunk-grounded (source_type :knowledge_base) — NOT conversation-grounded. The schema's validate_citation_target / validate_metadata_destination validators make conversation-only citation_targets fundamentally incompatible; staying KB-only is the strongest single-axis fix.
- citation_target on EVERY row contains article_id + revision_id + chunk_index (string or atom keys both work; we use atom keys for consistency with the surrounding seed code).
- metadata on EVERY row contains a :destination map (atom keys).
- Do NOT set article_id or base_revision_id on the suggestion itself (validate_anchor_rules rejects them for gap-driven article suggestions).
- Do NOT pass :tenant_scope or :host_user_id to ensure_review_task_for_suggestion/2 — they are ignored. Pass only :actor_id.
- Workers.GenerateArticleSuggestion is NEVER called from this builder (D-15 — LLM call, would flake or burn tokens).
- KnowledgeAutomation.suggest_article/1 is NEVER called from this builder (it enqueues the LLM worker — D-15).
- host_user_id on the suggestion is "demo_operator" (Pitfall 3 — operator-scope; otherwise the SuggestionReview queue filter hides the row). The ReviewTask inherits this automatically.
- evidence_digest field order: [source_type, trust_level, title, excerpt, citation_target, match_reasons]. Metadata is excluded (RESEARCH §Sources L715 evidence_digest_for/1 — load-bearing; future CandidateBuilder re-computation must match this hash).
- Calls ensure_review_task_for_suggestion/2 after insert — without it, SuggestionReview LiveView shows empty queue (Pitfall 1).
- No library code touched.
- Brand voice (C-08, §5.5) on every visible string: title, operator_summary, proposed_markdown sections.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tee /tmp/p27-06-compile.log; echo '---'; echo 'spec-language-leaks-into-status (should be 0):'; grep -E 'status:\s*:ready_for_review|suggestion_type:\s*:new_article' examples/cairnloop_example/priv/repo/seeds.exs | wc -l; echo 'correct-status-enum:'; grep -c 'status:\s*:ready' examples/cairnloop_example/priv/repo/seeds.exs; echo 'correct-suggestion_type-enum:'; grep -c 'suggestion_type:\s*:article' examples/cairnloop_example/priv/repo/seeds.exs; echo 'workers-generatearticlesuggestion-invocations (should be 0):'; grep -c 'Workers\.GenerateArticleSuggestion' examples/cairnloop_example/priv/repo/seeds.exs; echo 'suggest_article-invocations (should be 0):'; grep -c 'KnowledgeAutomation\.suggest_article' examples/cairnloop_example/priv/repo/seeds.exs; echo 'ensure_review_task_for_suggestion-call-present (should be 1):'; grep -c 'ensure_review_task_for_suggestion' examples/cairnloop_example/priv/repo/seeds.exs; echo 'evidence_digest-helper-defined:'; grep -c 'defp compute_evidence_digest' examples/cairnloop_example/priv/repo/seeds.exs; echo 'evidence-rows-have-destination-key (should be >= 2):'; grep -c 'destination:' examples/cairnloop_example/priv/repo/seeds.exs; echo 'evidence-rows-have-chunk_index (should be >= 2):'; grep -c 'chunk_index:' examples/cairnloop_example/priv/repo/seeds.exs; echo 'no-ignored-opts-passed-to-review-task (should be 0):'; grep -E 'ensure_review_task_for_suggestion\([^)]*tenant_scope|ensure_review_task_for_suggestion\([^)]*host_user_id' examples/cairnloop_example/priv/repo/seeds.exs | wc -l</automated>
    <human-check>Read the new build_suggestion/2 body and confirm: (a) proposed_markdown contains [1] and [2] anchors corresponding to the 2 evidence rows; (b) the evidence_digest computation field order matches the algorithm in lib/cairnloop/knowledge_automation.ex:961-976 (source_type, trust_level, title, excerpt, citation_target, match_reasons — metadata excluded); (c) BOTH evidence rows reference api_key_article's published revision via citation_target (article_id + revision_id + chunk_index) AND metadata.destination; (d) the ensure_review_task_for_suggestion call passes ONLY actor_id (not tenant_scope, not host_user_id); (e) neither article_id nor base_revision_id is set on the suggestion attrs (validate_anchor_rules would reject the insert otherwise).</human-check>
  </verify>
  <acceptance_criteria>
- cd examples/cairnloop_example && mix compile --warnings-as-errors exits 0.
- grep -E 'status:\s*:ready_for_review|suggestion_type:\s*:new_article' examples/cairnloop_example/priv/repo/seeds.exs returns 0 lines (no spec-language leak — sealed-enum reconciliation honored).
- grep -c 'Workers\.GenerateArticleSuggestion' returns 0.
- grep -c 'KnowledgeAutomation\.suggest_article' returns 0.
- grep -c 'ensure_review_task_for_suggestion' returns ≥ 1.
- grep -c 'defp compute_evidence_digest' returns 1 (the helper is defined exactly once).
- grep -c 'destination:' returns ≥ 2 (both evidence rows carry metadata.destination).
- grep -c 'chunk_index:' returns ≥ 2 (both evidence rows pin a chunk_index).
- grep -E 'ensure_review_task_for_suggestion\([^)]*(tenant_scope|host_user_id)' returns 0 (only :actor_id is passed; the other opts are ignored by the function and confuse readers).
- The suggestion's stable_key is exactly "demo:article_suggestion:billing_export:v1" (idempotency key).
- End-to-end FIX-04 assertion (Repo.count(ArticleSuggestion, status: :ready) >= 1 AND Repo.count(ReviewTask, status: :pending_review) >= 1 AND a ReviewTaskEvent with event_type :task_created exists) ships in plan 27-08's integration test.
  </acceptance_criteria>
  <done>
All acceptance_criteria conditions hold; build_suggestion/2 produces 1 suggestion + 2 KB-chunk evidence rows + 1 review_task + 1 task_created event on fresh DB; idempotent on re-run; returns {%ArticleSuggestion{}, %ReviewTask{}}.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → ArticleSuggestion schema | Spec-language vs sealed-enum mismatch (:ready_for_review / :new_article) would cause silent or loud changeset failures (Pitfall 2); evidence shape mismatch (missing citation_target keys or metadata.destination) is caught by validate_citation_target / validate_metadata_destination |
| seed script → SuggestionReview LiveView | ReviewTask is the actual data source — bypassing ensure_review_task_for_suggestion/2 produces an invisible suggestion (Critical Finding 2 / Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-16 | Tampering | sealed-enum spec-language drift into seed (would raise Ecto changeset is_invalid error or, worse, silently fail validation) | mitigate | Acceptance criterion grep gate fails the build if status: :ready_for_review or suggestion_type: :new_article appears in the file. The header reconciliation table in plan 27-01 documents the canonical mapping. |
| T-27-17 | Information Disclosure | ArticleSuggestion seeded in wrong tenant scope hides it from SuggestionReview queue (Pitfall 3) | mitigate | tenant_scope: :host_user_scoped + host_user_id: "demo_operator" hard-coded on the suggestion; ensure_review_task_for_suggestion/2 inherits the same scope from the loaded suggestion (verified lib/cairnloop/knowledge_automation.ex:135-140). |
| T-27-18 | Tampering | LLM call accidentally enqueued from seed (would flake without OPENAI_API_KEY, burn tokens with one — D-15) | mitigate | Acceptance criterion grep gates count Workers.GenerateArticleSuggestion and KnowledgeAutomation.suggest_article invocations to 0. |
| T-27-19 | Repudiation | non-deterministic evidence_digest breaks future re-computation invariant if CandidateBuilder ever re-hashes the suggestion | mitigate | The compute_evidence_digest helper mirrors the production algorithm at lib/cairnloop/knowledge_automation.ex:961-976 verbatim (field order + metadata-exclusion). |
| T-27-20 | Tampering | evidence_snapshot row missing required citation_target keys (article_id/revision_id/chunk_index) or missing metadata.destination — would raise Ecto.Changeset validation error and fail the insert | mitigate | Both seeded evidence rows are KB-chunk-shaped, pointing at api_key_article's published revision chunk_index 0 and 1; both metadata maps carry a :destination sub-map. Grep gates count destination: and chunk_index: occurrences to ≥ 2. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | No new packages. |
</threat_model>

<verification>
After this task:
1. Grep gates in <verify> all pass.
2. Compile is warnings-clean.
3. End-to-end assertions (Repo aggregates + SuggestionReview LiveView renders 1 row) ship in plan 27-08's integration test once seed-test infra exists.
</verification>

<success_criteria>
- 1 ArticleSuggestion status :ready (sealed enum) with operator-scope and citation-backed proposed_markdown is inserted.
- 2 ArticleSuggestionEvidence rows reference distinct KB chunks (chunk_index 0 and 1) of api_key_article's published revision; both rows satisfy validate_citation_target AND validate_metadata_destination.
- The companion ReviewTask + :task_created event are created via ensure_review_task_for_suggestion(suggestion.id, actor_id: "system") (without this, FIX-04 silently fails; with extra opts beyond :actor_id, the reader is misled about what the function actually consumes).
- evidence_digest is deterministic and matches the production algorithm so future CandidateBuilder re-runs don't desync the seed.
- No LLM worker enqueue. No library code modified.
- Idempotent on re-run.
</success_criteria>

<output>
Create .planning/phases/27-realistic-demo-fixtures/27-06-SUMMARY.md when done — record:
- The verbatim sealed-enum mapping used (status :ready, suggestion_type :article).
- The verbatim stable_key.
- Confirmed source_type and trust_level atoms the seeded evidence rows use (both :knowledge_base / :canonical — cite ArticleSuggestionEvidence lines 7-8).
- Confirmation that both evidence rows reference api_key_article's published revision via citation_target.article_id + .revision_id + .chunk_index, and that metadata.destination is set on both rows.
- Confirmation that the suggestion's article_id and base_revision_id are UNSET (validate_anchor_rules would otherwise reject the gap-driven article suggestion insert).
- Confirmation that ensure_review_task_for_suggestion/2 was called with ONLY :actor_id (no ignored opts).
- Note: the conversation-grounded evidence row from the prior plan iteration was DROPPED in favor of 2 KB-chunk-grounded rows because the schema's validate_citation_target requires article_id/revision_id/chunk_index on every row — conversation linkage is preserved in the demo via the seeded conversations themselves (plan 27-04), not via the suggestion's evidence.
</output>
</content>
</invoke>