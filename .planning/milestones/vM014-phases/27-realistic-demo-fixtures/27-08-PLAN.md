---
phase: 27-realistic-demo-fixtures
plan: 08
type: execute
wave: 7
depends_on:
  - 27-07
files_modified:
  - examples/cairnloop_example/test/cairnloop_example/seeds_test.exs
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
  - FIX-04
tags:
  - test
  - integration
  - elixir
  - ecto
  - oban
must_haves:
  truths:
    - "examples/cairnloop_example/test/cairnloop_example/seeds_test.exs uses CairnloopExample.DataCase with async: false (seeds touch shared Oban state)"
    - "the test runs the seed via Code.eval_file(\"priv/repo/seeds.exs\") so it exercises the real adopter path"
    - "after a single eval, the test asserts: conversations >= 16 (FIX-01 row count); :open + :resolved cohort balance (>= 12 + >= 4 by status — :new + :open + :awaiting_customer share status :open per D-03); articles >= 5 (FIX-02); revisions >= 6 with >= 1 :archived state (FIX-02 :deprecated criterion); cairnloop_chunks count > 0 after Oban drain (FIX-02 M008 self-test); gap candidates >= 3 with status :open (FIX-03); each gap has >= 1 membership row; article suggestions >= 1 with status :ready (FIX-04); a companion ReviewTask with status :pending_review exists for that suggestion (FIX-04 critical-finding 2)"
    - "message-count bound is 48 <= count <= 80 — lower bound is the plan-27-04 expected total (58: 4*2 + 4*4 + 4*3 + 4*5 + 2 internal_notes) minus headroom; upper bound is tight enough to catch a runaway-loop regression and loose enough to absorb a 1–2 message copy-edit per conversation across future polish phases"
    - "an idempotency test eval-files the seed TWICE and asserts row counts are stable (D-02)"
    - "the test is tagged @tag :requires_postgres so it skips cleanly when Postgres is unavailable on developer machines per CLAUDE.md C-04 + RESEARCH Assumption A5"
    - "the test runs green via: cd examples/cairnloop_example && mix test test/cairnloop_example/seeds_test.exs (requires Postgres on localhost:5433 with cairnloop_example_test DB per VALIDATION.md)"
  artifacts:
    - path: "examples/cairnloop_example/test/cairnloop_example/seeds_test.exs"
      provides: "DB-backed integration test asserting FIX-01..FIX-04 row counts + Oban drain + idempotency"
      contains: "use CairnloopExample.DataCase"
  key_links:
    - from: "seeds_test.exs"
      to: "examples/cairnloop_example/priv/repo/seeds.exs"
      via: "Code.eval_file (script runs in test process; inherits sandbox connection)"
      pattern: "Code.eval_file"
    - from: "seeds_test.exs"
      to: "Cairnloop.KnowledgeBase.Chunk pgvector table"
      via: "Repo.aggregate(Cairnloop.KnowledgeBase.Chunk, :count) > 0 after Oban drain"
      pattern: "KnowledgeBase.Chunk"
---

<objective>
Ship the DB-backed integration test that pins FIX-01..FIX-04 row-count thresholds + the M008 substrate self-test (cairnloop_chunks non-empty after Oban drain) + D-02 idempotency. The test runs under `cd examples/cairnloop_example && mix test` against Postgres on localhost:5433 (per VALIDATION.md). Per RESEARCH Assumption A5 + Critical Finding 3, this test CANNOT live in the library's `mix test.integration` lane — the library harness wires `Cairnloop.Repo` (test_host), not `CairnloopExample.Repo`. The example app owns its own test for the seed it owns.

Purpose: Closes the Phase 27 Wave-0 gap. Without this test, the FIX-01..FIX-04 row-count contracts ride entirely on visual inspection of `mix setup`, which is fragile and not adopter-runnable as proof. The test is tagged `:requires_postgres` so developers without Postgres can still run `mix test` headlessly per CLAUDE.md C-04.

Output: A single test file with one DataCase-shaped describe block containing four tests: (1) end-to-end seed run + row-count assertions, (2) Oban drain produces non-empty chunks, (3) idempotency (two evals produce stable counts), (4) FIX-04 ReviewTask companion exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md
@.planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md
@.planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md
@.planning/phases/27-realistic-demo-fixtures/27-VALIDATION.md
@CLAUDE.md
@examples/cairnloop_example/test/support/data_case.ex
@examples/cairnloop_example/test/test_helper.exs
@examples/cairnloop_example/priv/repo/seeds.exs

<interfaces>
<!-- CairnloopExample.DataCase (verified examples/cairnloop_example/test/support/data_case.ex):
       using do
         quote do
           alias CairnloopExample.Repo
           import Ecto
           import Ecto.Changeset
           import Ecto.Query
           import CairnloopExample.DataCase
         end
       end
       setup tags do
         CairnloopExample.DataCase.setup_sandbox(tags)
         :ok
       end
       setup_sandbox/1 :: starts an SQL sandbox owner; shared: not tags[:async] enables shared mode for async: false suites.

     test_helper.exs:
       ExUnit.start()
       Ecto.Adapters.SQL.Sandbox.mode(CairnloopExample.Repo, :manual)

     Schemas the assertions query:
       Cairnloop.Conversation                                  (FIX-01)
       Cairnloop.Message                                       (FIX-01)
       Cairnloop.KnowledgeBase.Article                         (FIX-02)
       Cairnloop.KnowledgeBase.Revision                        (FIX-02 :archived state criterion)
       Cairnloop.KnowledgeBase.Chunk                           (FIX-02 M008 self-test — pgvector chunks)
       Cairnloop.KnowledgeAutomation.GapCandidate              (FIX-03)
       Cairnloop.KnowledgeAutomation.GapCandidateMembership    (FIX-03)
       Cairnloop.KnowledgeAutomation.ArticleSuggestion         (FIX-04)
       Cairnloop.KnowledgeAutomation.ReviewTask                (FIX-04 critical finding 2)

     Seed invocation pattern (RESEARCH Wave 0 Gaps recommendation):
       Code.eval_file("priv/repo/seeds.exs")
       — runs the script in the test process; inherits the sandboxed Repo connection;
       Oban.drain_queue/1 also runs synchronously in-process and sees the same sandbox.

     Oban testing mode (examples/cairnloop_example/config/test.exs:2):
       testing: :manual — drain_queue still works (Pitfall 7), just bypasses the auto-running supervisor.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create seeds_test.exs with end-to-end + drain + idempotency + ReviewTask assertions</name>
  <files>examples/cairnloop_example/test/cairnloop_example/seeds_test.exs</files>
  <read_first>
    - examples/cairnloop_example/test/support/data_case.ex (the using/setup template the new test consumes)
    - examples/cairnloop_example/test/test_helper.exs (sandbox mode)
    - examples/cairnloop_example/priv/repo/seeds.exs (the final state after plans 27-01..27-07 — confirm the script's last line is CairnloopExample.SeedRun.run())
    - .planning/phases/27-realistic-demo-fixtures/27-CONTEXT.md D-20 (test posture — integration test in example app), D-02 (idempotency)
    - .planning/phases/27-realistic-demo-fixtures/27-RESEARCH.md (Critical Finding 3 + Assumption A5; Wave 0 Gaps; Pitfall 7 testing: :manual is fine for drain)
    - .planning/phases/27-realistic-demo-fixtures/27-PATTERNS.md (seeds_test.exs section — DataCase wiring + Code.eval_file pattern)
    - .planning/phases/27-realistic-demo-fixtures/27-VALIDATION.md (per-task verification map; this test backs every FIX-* row in that table)
    - Plan 27-04 (for the canonical message-count math: 4*2 + 4*4 + 4*3 + 4*5 + 2 internal_notes = 58 on a fresh DB)
  </read_first>
  <action>
Create `examples/cairnloop_example/test/cairnloop_example/seeds_test.exs` with:

1. **Module + DataCase wiring:**

       defmodule CairnloopExample.SeedsTest do
         use CairnloopExample.DataCase, async: false

         alias Cairnloop.Conversation
         alias Cairnloop.Message
         alias Cairnloop.KnowledgeBase.Article
         alias Cairnloop.KnowledgeBase.Revision
         alias Cairnloop.KnowledgeBase.Chunk
         alias Cairnloop.KnowledgeAutomation.ArticleSuggestion
         alias Cairnloop.KnowledgeAutomation.GapCandidate
         alias Cairnloop.KnowledgeAutomation.GapCandidateMembership
         alias Cairnloop.KnowledgeAutomation.ReviewTask
         …
       end

   `async: false` (PATTERNS.md §seeds_test.exs — seeds touch shared Oban state; serial-only).

2. **Helper to run the seed from the test process:**

       defp run_seed!() do
         seed_path = Path.expand("../../priv/repo/seeds.exs", __DIR__)
         Code.eval_file(seed_path)
         :ok
       end

   The `Path.expand` walks from `test/cairnloop_example/` up two levels to the example app root, then down to `priv/repo/seeds.exs`. Verify the path resolves by including a small `assert File.exists?(seed_path)` line at the top of the helper before calling eval.

3. **Tag the suite for selective execution:**

       @moduletag :requires_postgres

   Adopters and CI lanes that lack Postgres can `mix test --exclude requires_postgres` to skip this whole file. CLAUDE.md C-04 + RESEARCH A5 — Cairnloop.Repo / CairnloopExample.Repo may be unavailable on developer machines.

4. **Test 1: end-to-end seed run + FIX-01/FIX-02/FIX-03/FIX-04 row counts (single eval).**

       describe "priv/repo/seeds.exs" do
         test "produces FIX-01..FIX-04 row counts on a single run" do
           assert :ok == run_seed!()

           # FIX-01: 16 conversations across 4 JTBD-derived cohorts (D-03 — :new/:open/:awaiting_customer
           # all share schema status :open, so the :open aggregate must be >= 12).
           assert Repo.aggregate(Conversation, :count) >= 16
           assert Repo.aggregate(from(c in Conversation, where: c.status == :open), :count) >= 12
           assert Repo.aggregate(from(c in Conversation, where: c.status == :resolved), :count) >= 4

           # FIX-01: per-cohort message counts (plan 27-04):
           #   :new                -> 4 conv × 2 msgs = 8
           #   :open               -> 4 conv × 4 msgs = 16
           #   :awaiting_customer  -> 4 conv × 3 msgs = 12
           #   :resolved           -> 4 conv × 5 msgs = 20
           #   plus 2 :internal_note additions       = 2
           #   Expected fresh-DB total                = 58
           # The 48 lower bound absorbs a 10-message regression headroom (e.g. one cohort drops 1 message
           # per conversation). The 80 upper bound catches a runaway-loop regression while leaving room
           # for a 1–2 message copy-edit per conversation across future polish phases — 80 = 58 + 22 headroom.
           assert Repo.aggregate(Message, :count) >= 48
           assert Repo.aggregate(Message, :count) <= 80

           # FIX-02: ≥ 5 articles, ≥ 6 revisions, ≥ 1 :archived revision
           assert Repo.aggregate(Article, :count) >= 5
           assert Repo.aggregate(Revision, :count) >= 6
           assert Repo.aggregate(from(r in Revision, where: r.state == :archived), :count) >= 1

           # FIX-03: ≥ 3 GapCandidate rows with status :open; each has ≥ 1 membership
           assert Repo.aggregate(from(g in GapCandidate, where: g.status == :open), :count) >= 3
           memberships_by_gap =
             Repo.all(from m in GapCandidateMembership, group_by: m.gap_candidate_id,
                      select: {m.gap_candidate_id, count(m.id)})
           assert length(memberships_by_gap) >= 3
           assert Enum.all?(memberships_by_gap, fn {_id, n} -> n >= 1 end)

           # FIX-04: ≥ 1 ArticleSuggestion with status :ready
           assert Repo.aggregate(from(s in ArticleSuggestion, where: s.status == :ready), :count) >= 1
         end

5. **Test 2: M008 substrate self-test — chunks populated after Oban drain (FIX-02 second half).**

       test "Oban drain produces non-empty cairnloop_chunks (M008 substrate self-test)" do
         assert :ok == run_seed!()

         # The drain inside seeds.exs is synchronous; chunks should exist immediately.
         assert Repo.aggregate(Chunk, :count) > 0,
                "cairnloop_chunks is empty after seed; FIX-02 M008 self-test failed. " <>
                  "Check that build_articles/0 uses KnowledgeBase.publish_revision/1 (NOT direct %Revision{} insert) " <>
                  "and that drain_embedding_pipeline/0 runs after build_articles/0."

         # Soft sanity check: at least one chunk per article (5 articles → expect ≥ 5 chunks at minimum,
         # likely ~15+ since each article body has ≥ 2 h2 sections per Pitfall 4 mitigation in plan 27-03).
         assert Repo.aggregate(Chunk, :count) >= 5
       end

6. **Test 3: FIX-04 ReviewTask companion exists (Critical Finding 2 / Pitfall 1).**

       test "FIX-04: the seeded ArticleSuggestion has a companion ReviewTask :pending_review" do
         assert :ok == run_seed!()

         suggestion =
           Repo.one!(from s in ArticleSuggestion,
                    where: s.stable_key == "demo:article_suggestion:billing_export:v1")

         review_task =
           Repo.one(from t in ReviewTask, where: t.article_suggestion_id == ^suggestion.id)

         assert review_task, "No ReviewTask found for the seeded ArticleSuggestion. " <>
                "SuggestionReview LiveView reads from ReviewTask (NOT ArticleSuggestion), so the " <>
                "queue would render empty without this row. Plan 27-06 must call " <>
                "KnowledgeAutomation.ensure_review_task_for_suggestion/2 after the suggestion insert."

         assert review_task.status == :pending_review
       end

7. **Test 4: D-02 idempotency — running the seed TWICE produces stable row counts.**

       test "D-02 idempotency: running the seed twice produces stable row counts" do
         assert :ok == run_seed!()

         counts_after_run_1 = %{
           conversations:  Repo.aggregate(Conversation, :count),
           messages:       Repo.aggregate(Message, :count),
           articles:       Repo.aggregate(Article, :count),
           revisions:      Repo.aggregate(Revision, :count),
           gap_candidates: Repo.aggregate(GapCandidate, :count),
           memberships:    Repo.aggregate(GapCandidateMembership, :count),
           suggestions:    Repo.aggregate(ArticleSuggestion, :count),
           review_tasks:   Repo.aggregate(ReviewTask, :count)
         }

         assert :ok == run_seed!()

         counts_after_run_2 = %{
           conversations:  Repo.aggregate(Conversation, :count),
           messages:       Repo.aggregate(Message, :count),
           articles:       Repo.aggregate(Article, :count),
           revisions:      Repo.aggregate(Revision, :count),
           gap_candidates: Repo.aggregate(GapCandidate, :count),
           memberships:    Repo.aggregate(GapCandidateMembership, :count),
           suggestions:    Repo.aggregate(ArticleSuggestion, :count),
           review_tasks:   Repo.aggregate(ReviewTask, :count)
         }

         assert counts_after_run_1 == counts_after_run_2,
                "Re-running the seed produced different row counts — idempotency contract (D-02) violated.\n" <>
                  "After 1st run: #{inspect(counts_after_run_1)}\n" <>
                  "After 2nd run: #{inspect(counts_after_run_2)}"
       end

8. **Important wiring caveats:**

   - The `assert Repo.aggregate(Chunk, :count) > 0` test depends on Oban drain running in-process under the test's sandboxed connection. Per RESEARCH Pitfall 7, `testing: :manual` in `config/test.exs` does NOT break `Oban.drain_queue/1` — drain works exactly because it explicitly runs available jobs from the table. The chunks insert runs in the worker callback inside the same test process, so it sees the sandbox connection.
   - The `Code.eval_file` invocation must use an absolute path (Path.expand) since the test process's cwd is the example app root but file resolution semantics for relative paths inside `.exs` files can vary.
   - DO NOT add any `:async` flag to individual tests — the module-level `async: false` is correct for the whole file; per-test async overrides do not help here (Oban state is module-scoped).
   - DO NOT call any sandbox-allow function — the seed's helpers (build_articles, etc.) run in the same process as the test, so connection ownership is inherited automatically.
   - DO NOT install any new test dependency. ExUnit + Ecto.Adapters.SQL.Sandbox + Oban are all already present.

9. **Brand-voice / copy review reminder (out-of-scope for this plan but worth a comment):** the integration test does NOT pin the seeded message body strings (those are stylistic — plan 27-04's brand-voice review covers them). It pins ROW COUNTS and one specific stable_key (the suggestion's), keeping the test resilient to copy churn in future phases.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tee /tmp/p27-08-compile.log; echo '---'; echo 'test-file-exists:'; ls examples/cairnloop_example/test/cairnloop_example/seeds_test.exs &amp;&amp; echo OK; echo 'requires_postgres-tag:'; grep -c '@moduletag :requires_postgres' examples/cairnloop_example/test/cairnloop_example/seeds_test.exs; echo 'datacase-wiring:'; grep -c 'use CairnloopExample.DataCase, async: false' examples/cairnloop_example/test/cairnloop_example/seeds_test.exs; echo 'eval_file-call:'; grep -c 'Code.eval_file' examples/cairnloop_example/test/cairnloop_example/seeds_test.exs; echo 'four-test-blocks:'; grep -cE '^\s*test "' examples/cairnloop_example/test/cairnloop_example/seeds_test.exs; echo 'message-upper-bound-is-80-not-100:'; grep -c 'Repo.aggregate(Message, :count) <= 80' examples/cairnloop_example/test/cairnloop_example/seeds_test.exs; echo '---'; echo 'optional-test-run (requires Postgres on localhost:5433; skips cleanly if absent):'; cd examples/cairnloop_example &amp;&amp; mix test test/cairnloop_example/seeds_test.exs --color 2&gt;&amp;1 | tail -20 || true</automated>
    <human-check>If Postgres is available locally, run the test and confirm all 4 tests pass. If Postgres is NOT available, confirm: (a) `mix test --exclude requires_postgres` (the headless lane) does not even try to load this file's setup, (b) the headless DemoContextProvider test from plan 27-02 still runs and passes.</human-check>
  </verify>
  <acceptance_criteria>
- cd examples/cairnloop_example && mix compile --warnings-as-errors exits 0.
- examples/cairnloop_example/test/cairnloop_example/seeds_test.exs exists.
- grep -c 'use CairnloopExample.DataCase, async: false' returns 1.
- grep -c '@moduletag :requires_postgres' returns 1.
- grep -cE '^\s*test "' returns exactly 4 (the four tests listed in <action>).
- grep -c 'Code.eval_file' returns ≥ 1 (the helper invokes it; tests may call run_seed! which wraps it).
- grep -c 'Repo.aggregate(Message, :count) <= 80' returns 1 (the upper bound is tightened from the prior 100 to 80 — see truths note: expected fresh-DB count is 58; 80 leaves 22 headroom for future polish copy edits while still catching a runaway-loop regression).
- On a developer machine WITH Postgres on localhost:5433 (per VALIDATION.md): cd examples/cairnloop_example && mix test test/cairnloop_example/seeds_test.exs exits 0 (all 4 tests green) in <60 sec.
- On a developer machine WITHOUT Postgres: cd examples/cairnloop_example && mix test --exclude requires_postgres still runs green (the headless DemoContextProviderTest from plan 27-02 still passes; this file is skipped via the moduletag).
- This plan's test pins FIX-01..FIX-04 row-count contracts so future phases that touch the seed catch regressions immediately.
  </acceptance_criteria>
  <done>
All acceptance_criteria conditions hold; the integration test ships with all 4 tests green on a CI/dockerized lane; the headless test lane is unaffected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → CairnloopExample.Repo sandbox | The test owns the sandbox; the eval'd seed inherits it; Oban drain runs in-process so worker callbacks see the same connection (RESEARCH §Sandbox ownership note) |
| test process → Postgres on localhost:5433 | Required for FIX-02's "embeddings flow through pgvector" assertion; if Postgres is unavailable the test is skipped via @moduletag :requires_postgres (RESEARCH Assumption A5; CLAUDE.md C-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-23 | Tampering | sandbox-connection ownership leak (test's writes invisible to Oban worker callbacks) | mitigate | DataCase.setup_sandbox uses shared mode (shared: not async = shared: true when async: false). The seed's helpers AND the Oban drain all run in the test process; connection ownership is inherited. RESEARCH §Sandbox ownership confirms this. |
| T-27-24 | Repudiation | test passes spuriously if the seed silently no-ops on a stale DB (no aggregate assertions would catch this) | mitigate | The idempotency test (test 4) measures counts BEFORE and AFTER a second eval; this catches a regression where re-running the seed inflates counts (e.g. duplicate messages from a broken get_or_insert! branch). |
| T-27-25 | Denial of Service | test hangs because Oban drain has nothing to do (no enqueued jobs) and waits forever | mitigate | Oban.drain_queue/1 (with_recursion: true) returns synchronously with a count map even when the queue is empty (returns %{success: 0, ...}). It does not block waiting for new jobs. Confirmed via deps/oban/lib/oban.ex:920-928. |
| T-27-26 | Information Disclosure | test failure messages leak production-style data | accept | All seeded data is hard-coded demo content (RFC 2606 .example emails, fictional names). No real customer data ever reaches the test output. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | No new packages — ExUnit + DataCase + Oban already present. |
</threat_model>

<verification>
After this task:
1. mix compile --warnings-as-errors exits 0.
2. On the dockerized/CI lane (Postgres available): all 4 tests green; the FIX-01..FIX-04 contract is now CI-enforced.
3. On a Postgres-less developer machine: mix test --exclude requires_postgres skips the file cleanly; headless tests remain green.
4. The test is the FIX-* row-count contract — any future plan that breaks the seed (e.g. plan 27-07 wires builders in the wrong order, or a future phase changes the schema) trips one of the 4 tests immediately.
</verification>

<success_criteria>
- The Wave 0 gap from VALIDATION.md is closed (seeds_test.exs exists; covers FIX-01..FIX-04 + drain + idempotency + ReviewTask companion).
- The contract is CI-enforceable on the dockerized lane without forcing every developer to install Postgres locally.
- nyquist_compliant: true can now be set in VALIDATION.md frontmatter (after plan 27-08 merges).
- No library code modified.
</success_criteria>

<output>
Create .planning/phases/27-realistic-demo-fixtures/27-08-SUMMARY.md when done — record:
- The 4 test names exactly as committed.
- The total runtime of the suite on the local Postgres lane (target: < 60 sec).
- Confirmation that mix test --exclude requires_postgres still passes the headless DemoContextProviderTest from plan 27-02.
- The Path.expand resolution used inside run_seed! (and confirmation that the path resolves correctly via File.exists?).
- The actual fresh-DB message count observed (expected 58; if different, document the drift and the rationale).
- Update .planning/phases/27-realistic-demo-fixtures/27-VALIDATION.md by editing nyquist_compliant from false to true and wave_0_complete from false to true in the frontmatter, AND fill in the per-task verification map with the actual task IDs.
</output>
</content>
</invoke>