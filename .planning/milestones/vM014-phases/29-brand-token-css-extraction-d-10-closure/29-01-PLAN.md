---
phase: 29-brand-token-css-extraction-d-10-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/assets/css/app.css
autonomous: true
requirements:
  - BRAND-01
tags:
  - css
  - tailwind-v4
  - brand-tokens

must_haves:
  truths:
    - "examples/cairnloop_example/assets/css/app.css imports the canonical 15 primitive + 14 semantic tokens from prompts/cairnloop.css verbatim."
    - "The Tailwind @theme block contains all 15 --color-cl-* primitive tokens (not just the prior 4)."
    - "The :root block contains all 15 --cl-color-* primitive mirrors + 14 --cl-<semantic> tokens + typography + radius + shadow."
    - "[data-theme=\"dark\"] override block is present immediately after :root inside @layer base, with all 14 semantic overrides."
    - "--cl-on-primary: var(--cl-primary-text); alias is present in :root (Pitfall 3 — keeps sealed render code unchanged)."
    - "All daisyUI plugin blocks, @source directives, @custom-variant blocks, and the LiveView wrapper rule are preserved byte-for-byte outside the replaced range."
  artifacts:
    - path: "examples/cairnloop_example/assets/css/app.css"
      provides: "Canonical brand tokens for the example app's Tailwind compile"
      contains: "--color-cl-fault-clay"
      contains_also:
        - "--cl-color-fault-clay"
        - "--cl-danger: var(--cl-color-fault-clay)"
        - "--cl-on-primary: var(--cl-primary-text)"
        - "[data-theme=\"dark\"]"
        - "@plugin \"../vendor/daisyui\""
  key_links:
    - from: "examples/cairnloop_example/assets/css/app.css"
      to: "prompts/cairnloop.css"
      via: "verbatim copy of :root + dark override blocks (per D-04, D-06, D-07)"
      pattern: "--cl-color-path-copper: #A94F30"
    - from: "examples/cairnloop_example/assets/css/app.css @theme block"
      to: "Tailwind v4 utility class generation (bg-cl-*, text-cl-*, border-cl-*)"
      via: "--color-cl-* namespace inside @theme directive"
      pattern: "--color-cl-basalt: #18211F"
---

<objective>
BRAND-01 closure: replace the 4-token + 6-raw-`--cl-*` placeholder in
`examples/cairnloop_example/assets/css/app.css` with the canonical brand token
block from `prompts/cairnloop.css` (15 primitive + 14 semantic + typography +
radius + shadow), expand the Tailwind `@theme` block to all 15 primitive colors,
add the `[data-theme="dark"]` override block, and add the additive
`--cl-on-primary: var(--cl-primary-text);` alias.

This plan stands alone — it makes Plans 02 + 03 possible by defining every token
those plans drop the fallback from. The example app's `app.css` becomes the single
source of truth for cascade values.

Purpose: Close half of D-10 (the "land the tokens" half). The "drop the fallbacks"
half is Plan 02. Split here keeps Plan 01 a single-file CSS edit (no Elixir code
churn) and lets Plan 02 trust that every `--cl-<token>` referenced in render code
resolves through this file.

Output: The updated `app.css` file.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-brand-token-css-extraction-d-10-closure/29-CONTEXT.md
@.planning/phases/29-brand-token-css-extraction-d-10-closure/29-RESEARCH.md
@.planning/phases/29-brand-token-css-extraction-d-10-closure/29-PATTERNS.md
@prompts/cairnloop.css
@examples/cairnloop_example/assets/css/app.css

<interfaces>
<!-- Canonical token contract that Plan 02 + Plan 03 will rely on. -->
<!-- All values come verbatim from prompts/cairnloop.css. Executor MUST NOT invent values. -->

Tailwind v4 @theme namespace (generates utility classes):
- 15 primitive colors: --color-cl-basalt, --color-cl-moss-ink, --color-cl-trailpaper,
  --color-cl-warm-stone, --color-cl-granite, --color-cl-slate-lichen, --color-cl-path-copper,
  --color-cl-copper-glow, --color-cl-lichen, --color-cl-deep-lichen, --color-cl-glacier-mist,
  --color-cl-waypoint-blue, --color-cl-heather, --color-cl-ember, --color-cl-fault-clay

CSS custom properties (in :root inside @layer base):
- 15 primitive mirrors: --cl-color-basalt … --cl-color-fault-clay
- 14 semantic: --cl-bg, --cl-surface, --cl-surface-raised, --cl-text, --cl-text-muted,
  --cl-border, --cl-primary, --cl-primary-text, --cl-success, --cl-info, --cl-ai,
  --cl-warning, --cl-danger, --cl-focus
- 1 alias (additive, per Pitfall 3): --cl-on-primary: var(--cl-primary-text)
- 3 typography: --cl-font-sans, --cl-font-display, --cl-font-mono
- 4 radius/shadow: --cl-radius-sm, --cl-radius-md, --cl-radius-lg, --cl-shadow-raised

[data-theme="dark"] sibling block (14 semantic overrides only):
- --cl-bg through --cl-focus (no primitive overrides, no typography overrides)

Preserve byte-for-byte (everything OUTSIDE the @theme + @layer base replacement):
- Line 1-5: header + @import "tailwindcss" source(none);
- Line 24-26: @source directives
- Line 28-30: @plugin "../vendor/heroicons" + comments
- Line 32-76: @plugin "../vendor/daisyui" + dark daisyUI theme plugin
- Line 78-111: light daisyUI theme plugin
- Line 113-122: @custom-variant blocks + LiveView wrapper rule
- Line 124: trailing comment
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace @theme stub + :root stub in app.css with canonical block + dark overrides + on-primary alias</name>
  <files>examples/cairnloop_example/assets/css/app.css</files>
  <read_first>
    - examples/cairnloop_example/assets/css/app.css (lines 1-26 are the replacement target; lines 28-124 must be preserved untouched)
    - prompts/cairnloop.css (lines 1-59 — verbatim source for the :root block and dark overrides)
    - .planning/phases/29-brand-token-css-extraction-d-10-closure/29-CONTEXT.md (D-04 through D-07 specify the exact landing rules)
    - .planning/phases/29-brand-token-css-extraction-d-10-closure/29-RESEARCH.md (Example A shows the full replacement block; Pitfalls 3 and 4 explain the on-primary alias and the replace-don't-append rule)
    - .planning/phases/29-brand-token-css-extraction-d-10-closure/29-PATTERNS.md (the "examples/cairnloop_example/assets/css/app.css" section shows the exact byte range and replacement structure)
  </read_first>
  <action>
    Replace lines 6-22 of `examples/cairnloop_example/assets/css/app.css` (the existing 4-entry `@theme { … }` block AND the 4-token + 2-semantic `@layer base { :root { … } }` block) with the canonical structure described below. Everything outside this byte range (lines 1-5 header, lines 24-124 from `@source` onward through the daisyUI plugins, `@custom-variant` blocks, LiveView wrapper rule, and trailing comment) MUST stay byte-for-byte identical.

    The replacement structure is, in order:

    1. A `@theme { … }` block containing EXACTLY the 15 primitive color tokens with the `--color-cl-` namespace, in the same order they appear in `prompts/cairnloop.css` lines 3-17 (basalt, moss-ink, trailpaper, warm-stone, granite, slate-lichen, path-copper, copper-glow, lichen, deep-lichen, glacier-mist, waypoint-blue, heather, ember, fault-clay), each with its hex value from `prompts/cairnloop.css`. NO semantic tokens in `@theme` (per D-05). NO typography, radius, or shadow in `@theme`.

    2. An `@layer base { … }` block containing two siblings:
       a. `:root { … }` with: the 15 primitive `--cl-color-*` mirrors (same names and values as `prompts/cairnloop.css` lines 3-17), then the 14 semantic tokens (`--cl-bg`, `--cl-surface`, `--cl-surface-raised`, `--cl-text`, `--cl-text-muted`, `--cl-border`, `--cl-primary`, `--cl-primary-text`, `--cl-success`, `--cl-info`, `--cl-ai`, `--cl-warning`, `--cl-danger`, `--cl-focus`) verbatim from `prompts/cairnloop.css` lines 19-32, then the additive alias `--cl-on-primary: var(--cl-primary-text);` (per Pitfall 3 — closes the gap where 4 sealed render sites reference `--cl-on-primary` which has no canonical definition; alias keeps sealed code untouched), then the 3 typography tokens (`--cl-font-sans`, `--cl-font-display`, `--cl-font-mono`) verbatim from `prompts/cairnloop.css` lines 34-36, then the 4 radius/shadow tokens (`--cl-radius-sm`, `--cl-radius-md`, `--cl-radius-lg`, `--cl-shadow-raised`) verbatim from `prompts/cairnloop.css` lines 38-41.
       b. `[data-theme="dark"] { … }` with the 14 semantic overrides verbatim from `prompts/cairnloop.css` lines 44-59 (per D-07: dark overrides go inside the same `@layer base` block, as a sibling to `:root`).

    Do NOT add any other tokens. Do NOT change the existing `@source` directives, daisyUI plugins, `@custom-variant` blocks, or LiveView wrapper rule. Do NOT add a `.cl-button-primary`, `.cl-card`, `.cl-route-active`, or `.cl-focusable` rule (those classes live only in `prompts/cairnloop.css` as documentation; the example app uses inline styles).

    Use the Edit tool to perform this replacement — read the current contents lines 6-22 exactly and write the new block in their place.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop && grep -c '^  --color-cl-' examples/cairnloop_example/assets/css/app.css | awk '$1 == 15 {exit 0} {exit 1}' && grep -q -- '--cl-on-primary: var(--cl-primary-text);' examples/cairnloop_example/assets/css/app.css && grep -q -- '\[data-theme="dark"\]' examples/cairnloop_example/assets/css/app.css && grep -q -- '--cl-color-fault-clay: #B54C36;' examples/cairnloop_example/assets/css/app.css && grep -q -- '@plugin "../vendor/daisyui"' examples/cairnloop_example/assets/css/app.css && grep -q -- '@custom-variant phx-click-loading' examples/cairnloop_example/assets/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  --color-cl-' examples/cairnloop_example/assets/css/app.css` returns `15` (exactly 15 primitive color entries inside the `@theme` block, each on its own line indented two spaces).
    - `grep -c '^    --cl-color-' examples/cairnloop_example/assets/css/app.css` returns `15` (exactly 15 primitive color mirrors inside `:root`, each on its own line indented four spaces).
    - File contains `--cl-danger: var(--cl-color-fault-clay);` (semantic token resolves to primitive — matches `prompts/cairnloop.css` line 31).
    - File contains `--cl-on-primary: var(--cl-primary-text);` (Pitfall 3 alias).
    - File contains `[data-theme="dark"] {` and the dark block contains `--cl-danger: #E18C7D;` (matches `prompts/cairnloop.css` line 57).
    - File contains `--cl-font-sans: "Atkinson Hyperlegible Next"` (typography token preserved).
    - File contains `--cl-shadow-raised: 0 1px 2px rgba(24, 33, 31, 0.08)` (shadow token preserved).
    - File still contains `@plugin "../vendor/daisyui"` (daisyUI light AND dark plugins preserved).
    - File still contains `@custom-variant phx-click-loading` (LiveView custom variants preserved).
    - File still contains `@source "../../lib/cairnloop_example_web";` (Tailwind source directive preserved).
    - File still contains `[data-phx-session], [data-phx-teleported-src] { display: contents }` (LiveView wrapper rule preserved).
    - Running `mix compile --warnings-as-errors` from project root exits 0 (no Elixir compile regression — though this file is pure CSS, this guards against accidental cross-file edits).
    - `grep -E 'var\(--cl-[a-z-]+, #' examples/cairnloop_example/assets/css/app.css` returns nothing (the canonical block itself uses bare `var(--cl-color-*)` references, not hex fallbacks — verifies no hex fallback strings introduced in app.css).
  </acceptance_criteria>
  <done>app.css contains the canonical 15-primitive `@theme`, 15-primitive + 14-semantic + 1-alias + 3-typography + 4-radius/shadow `:root`, and 14-semantic `[data-theme="dark"]` block, with all surrounding daisyUI / LiveView / Tailwind plumbing preserved byte-for-byte. `mix compile --warnings-as-errors` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase is a CSS / inline-style refactor. No new trust boundaries are introduced.
The canonical token values land via a verbatim copy from `prompts/cairnloop.css` (a
checked-in source artifact) into an in-tree asset file. No user input, no runtime
configuration, no network traffic, no persisted data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-V14 | Tampering | `app.css` configuration drift between `prompts/cairnloop.css` and the example app | mitigate | Verbatim copy per D-04; values cross-checked against `prompts/cairnloop.css` lines 3-59 in acceptance_criteria; ExUnit gate test in Plan 02 catches re-introduction of hex fallbacks in render code. |
| T-29-01-CSS | Spoofing | Visual spoofing via brand-token misalignment (e.g., success-colored danger button) | accept | Out of scope here — `app.css` only defines tokens; render-site usage is governed by Plan 02 and existing never-color-alone tests (e.g., `bulk_recovery_live_test.exs:264-267` checks SVG + heading + token). |
| T-29-01-SC | Tampering | Supply chain (npm/pip/cargo installs) | n/a | No new packages installed; no install surface to audit. |

ASVS L1 evaluation: V2/V3/V4/V5/V6/V7 do not apply (no auth, session, access control,
input validation, crypto, or error handling surface). V14 Configuration applies in
the "brand-token configuration file" sense and is addressed by the verbatim-copy
mitigation above.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` from project root exits 0.
- `grep -c '^  --color-cl-' examples/cairnloop_example/assets/css/app.css` returns `15`.
- `grep -c '^    --cl-color-' examples/cairnloop_example/assets/css/app.css` returns `15`.
- `grep -q -- '\[data-theme="dark"\]' examples/cairnloop_example/assets/css/app.css` succeeds.
- `grep -q -- '--cl-on-primary: var(--cl-primary-text);' examples/cairnloop_example/assets/css/app.css` succeeds.
- `grep -q -- '@plugin "../vendor/daisyui"' examples/cairnloop_example/assets/css/app.css` succeeds (daisyUI plugins preserved).
- `grep -q -- '@custom-variant phx-click-loading' examples/cairnloop_example/assets/css/app.css` succeeds (LiveView variants preserved).
- `grep -E 'var\(--cl-[a-z-]+, #' examples/cairnloop_example/assets/css/app.css` returns nothing.
</verification>

<success_criteria>
BRAND-01 satisfied: `examples/cairnloop_example/assets/css/app.css` contains the
canonical 15 `@theme` primitives, the canonical `:root` block (15 primitives +
14 semantic + 1 `--cl-on-primary` alias + typography + radius/shadow), and the
`[data-theme="dark"]` overrides. All daisyUI / Tailwind / LiveView plumbing is
preserved byte-for-byte. Build is warnings-clean.
</success_criteria>

<output>
Create `.planning/phases/29-brand-token-css-extraction-d-10-closure/29-01-SUMMARY.md` when done.
</output>
