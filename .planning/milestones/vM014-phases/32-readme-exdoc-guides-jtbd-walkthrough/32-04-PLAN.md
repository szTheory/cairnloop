---
phase: 32-readme-exdoc-guides-jtbd-walkthrough
plan: 04
type: execute
wave: 2
depends_on: ["32-01", "32-02", "32-03"]
files_modified:
  - mix.exs
autonomous: true
requirements: [DOC-03]
must_haves:
  truths:
    - "`mix docs` renders the four guides under a 'Guides' sidebar group with no missing-file or assets errors."
    - "`mix hex.build` lists all four guides AND the LICENSE file in the tarball contents."
    - "The existing groups_for_modules (6 groups), main:'readme', and all package: keys are preserved."
  artifacts:
    - path: "mix.exs"
      provides: "ExDoc extras + groups_for_extras + package files including guides — guides ship to HexDocs and the Hex tarball"
      contains: "groups_for_extras"
  key_links:
    - from: "mix.exs docs: extras"
      to: "the four guides/*.md files"
      via: "extras list referencing guides paths"
      pattern: "guides/01-quickstart.md"
    - from: "mix.exs package: files"
      to: "the published Hex tarball"
      via: ":files key listing guides + LICENSE"
      pattern: "files:.*guides"
---

<objective>
Wire `mix.exs` so the four guides publish to HexDocs and ship in the Hex tarball: add them to
`docs: extras`, add a `groups_for_extras` "Guides" group, add a commented `:assets` line, and add a
`package: :files` key that includes `guides` and the correctly-cased `LICENSE`. Then verify with the
two DB-free gate commands (`mix docs`, `mix hex.build`).

Purpose: The load-bearing distribution edit — without `:files` listing `guides`, the guides render
locally but vanish from the published tarball and HexDocs. (DOC-03)
Output: Edited `mix.exs`; verified rendering + tarball contents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-readme-exdoc-guides-jtbd-walkthrough/32-CONTEXT.md
@.planning/phases/32-readme-exdoc-guides-jtbd-walkthrough/32-PATTERNS.md

<interfaces>
<!-- Authoritative facts — extracted from mix.exs + Hex/ExDoc docs. EXTEND in place; never rewrite. -->

CURRENT mix.exs `docs:` block (lines 26-37) — EXTEND, do not replace:
  - main: "readme"  (KEEP)
  - extras: ["README.md", "CHANGELOG.md"]  (REPLACE with the guides-first list below)
  - groups_for_modules: [ Governance, "Knowledge Base", Retrieval, MCP, Web, Core ]  (KEEP all 6 unchanged)

TARGET `docs:` block (D-05): set extras to (guides BEFORE README/CHANGELOG; tuple form with titles
is recommended-discretion, plain strings equally valid):
  extras: [
    {"guides/01-quickstart.md", title: "Quickstart"},
    {"guides/02-jtbd-walkthrough.md", title: "JTBD Walkthrough"},
    {"guides/03-host-integration.md", title: "Host Integration"},
    {"guides/04-troubleshooting.md", title: "Troubleshooting"},
    "README.md",
    "CHANGELOG.md"
  ],
Add a `groups_for_extras` key:
  groups_for_extras: ["Guides": ~r/^guides\//],
Add a commented assets line (D-02 — keep COMMENTED; uncommenting without guides/assets/ makes mix docs raise):
  # assets: "guides/assets"  # uncomment once PNG screenshots are captured
Keep `groups_for_modules` and `main: "readme"` exactly as-is.

CURRENT mix.exs `package:` block (lines 17-25) — ADD a `:files` key; do NOT remove name/licenses/links/maintainers:
TARGET `package:` (D-05/D-11):
  files: ~w(lib priv guides mix.exs README.md LICENSE CHANGELOG.md),

⚠ CRITICAL — LICENSE casing (pattern-mapper correction, overrides CONTEXT D-05 / RESEARCH which said
"LICENSE.md"): the on-disk file is `LICENSE` with NO extension `[VERIFIED: ls LICENSE* → "LICENSE"]`.
An explicit `:files` list is an exact match (no glob), so listing `LICENSE.md` would SILENTLY DROP the
license from the published tarball. Use `LICENSE` (no extension).

VERIFICATION (all DB-free — runnable without Postgres):
  - `mix docs` — must exit 0, render a "Guides" sidebar group with the four entries, and produce NO
    "File not found" / missing-extra / missing-assets errors. (Requires the four guide files + README
    + CHANGELOG to exist — guaranteed by depends_on 32-01/32-02/32-03.)
  - `mix hex.build` — must list all four `guides/0N-*.md` files AND `LICENSE` in the included files.
  - `mix compile --warnings-as-errors` — mix.exs must still parse/compile clean (CLAUDE.md mandate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add guides to docs: extras + groups_for_extras + commented :assets, and package: :files</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (CURRENT `docs:` block lines 26-37 and `package:` block lines 17-25 — extend in place)
  </read_first>
  <action>
    Edit the `docs:` block: replace `extras: ["README.md", "CHANGELOG.md"]` with the guides-first
    extras list (four guide tuples with `title:` metadata, then `"README.md"`, then `"CHANGELOG.md"` —
    see interfaces for exact shape). Add a `groups_for_extras: ["Guides": ~r/^guides\//]` key. Add a
    commented `# assets: "guides/assets"  # uncomment once PNG screenshots are captured` line (KEEP it
    commented). Preserve `main: "readme"` and the entire `groups_for_modules:` list (all 6 groups)
    byte-for-byte. Edit the `package:` block: add `files: ~w(lib priv guides mix.exs README.md LICENSE CHANGELOG.md)` —
    use `LICENSE` with NO extension (the on-disk file has no extension; `LICENSE.md` would silently drop
    it from the tarball). Do NOT remove or reorder any existing `package:` keys (`name`, `licenses`,
    `links`, `maintainers`).
  </action>
  <verify>
    <automated>grep -q "guides/01-quickstart.md" mix.exs && grep -q "guides/02-jtbd-walkthrough.md" mix.exs && grep -q "guides/03-host-integration.md" mix.exs && grep -q "guides/04-troubleshooting.md" mix.exs && grep -q "groups_for_extras" mix.exs && grep -Eq "files:\s*~w\(.*\bguides\b.*\)" mix.exs && grep -Eq "files:\s*~w\(.*\bLICENSE\b.*\)" mix.exs && ! grep -Eq "files:\s*~w\(.*LICENSE\.md.*\)" mix.exs && grep -Eq "^\s*#\s*assets:\s*\"guides/assets\"" mix.exs && grep -q 'main: "readme"' mix.exs && grep -q "groups_for_modules" mix.exs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs `extras` lists all four `guides/0N-*.md` files and still lists `README.md` and `CHANGELOG.md`.
    - mix.exs contains `groups_for_extras` mapping `"Guides"` to `~r/^guides\//`.
    - mix.exs `package:` has a `files:` key whose `~w(...)` list contains the bare token `guides` and the bare token `LICENSE`.
    - `grep -E "files:\s*~w\(.*LICENSE\.md.*\)" mix.exs` returns nothing (NOT `LICENSE.md`).
    - mix.exs contains a COMMENTED `# assets: "guides/assets"` line (the `assets:` key is not active).
    - `main: "readme"` and `groups_for_modules` are both still present unchanged.
  </acceptance_criteria>
  <done>mix.exs docs: + package: extended in place: guides in extras, Guides group, commented assets, and `:files` listing `guides` + `LICENSE` (no extension).</done>
</task>

<task type="auto">
  <name>Task 2: Verify mix docs renders + mix hex.build ships the guides + LICENSE</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (post-edit state)
    - .planning/phases/32-readme-exdoc-guides-jtbd-walkthrough/32-RESEARCH.md (Validation Architecture — the DB-free gate commands)
  </read_first>
  <action>
    Run the three DB-free gate commands and confirm each passes; fix the mix.exs edit if any fails
    (do NOT touch lib/ source). (1) `mix compile --warnings-as-errors` — mix.exs must parse clean.
    (2) `mix docs` — must exit 0 and emit no "File not found" / missing-extra / missing-assets errors;
    the four guides render under a "Guides" sidebar group. (3) `mix hex.build` — inspect the printed
    included-files list and confirm `guides/01-quickstart.md` through `guides/04-troubleshooting.md`
    AND `LICENSE` appear. If `mix docs` raises about `guides/assets`, confirm the `:assets` line is
    still commented. If `mix hex.build` omits `guides` or `LICENSE`, fix the `:files` list. Record the
    relevant command output (the hex.build file list and the docs exit status) in the SUMMARY.
  </action>
  <verify>
    <automated>mix docs >/tmp/32-04-docs.log 2>&1 && grep -q "guides/01-quickstart" <(mix hex.build --unpack 2>/dev/null || mix hex.build 2>&1) || mix hex.build 2>&1 | grep -Eq "guides/0[1-4]-"</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 (mix.exs parses clean).
    - `mix docs` exits 0 with no "File not found", missing-extra, or missing-assets error in its output, and the generated `doc/` output includes pages for the four guides (e.g. `doc/01-quickstart.html` exists, or the docs log lists the four extras under a Guides group).
    - `mix hex.build` output lists all four `guides/0N-*.md` files in the included files.
    - `mix hex.build` output lists `LICENSE` in the included files (proves the casing fix).
  </acceptance_criteria>
  <done>`mix docs` renders the four guides under a Guides group with no errors and `mix hex.build` confirms the guides + LICENSE ship in the tarball — DOC-03 "visible on Hex.pm after the next release" proven locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Config-only edit to mix.exs; no executable runtime surface added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Information Disclosure | package :files list | mitigate | `:files` lists only intended artifacts (lib, priv, guides, mix.exs, README, LICENSE, CHANGELOG) — no test/secret files shipped in the tarball. |

No blocking threats — config edit with nil attack surface.
</threat_model>

<verification>
- mix.exs extended in place (extras + groups_for_extras + commented assets + package :files), preserving main:"readme" and the 6 module groups.
- `:files` lists `guides` and `LICENSE` (no extension); not `LICENSE.md`.
- `mix compile --warnings-as-errors` clean; `mix docs` renders the Guides group with no errors; `mix hex.build` lists the four guides + LICENSE.
</verification>

<success_criteria>
The guides ship in the published Hex tarball and surface in the ExDoc navigation under a "Guides" group (DOC-03), verified by `mix docs` and `mix hex.build` — both DB-free.
</success_criteria>

<output>
Create `.planning/phases/32-readme-exdoc-guides-jtbd-walkthrough/32-04-SUMMARY.md` when done.
</output>
