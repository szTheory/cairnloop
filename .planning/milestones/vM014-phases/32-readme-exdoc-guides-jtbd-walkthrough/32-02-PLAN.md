---
phase: 32-readme-exdoc-guides-jtbd-walkthrough
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/02-jtbd-walkthrough.md
  - guides/03-host-integration.md
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "An adopter can read guides/02-jtbd-walkthrough.md and follow all nine JTBD stages in the locked golden-path order, with screen-region descriptions and code references."
    - "An adopter can read guides/03-host-integration.md and implement all four host behaviours with the FULL current callback sets."
    - "The walkthrough ends with the bounded SCREENSHOTS TODO block and commits no PNG references."
  artifacts:
    - path: "guides/02-jtbd-walkthrough.md"
      provides: "Nine-stage JTBD walkthrough matching golden_path_test.exs, prose + screen-region descriptions, bounded screenshot TODO"
      min_lines: 60
      contains: "<!-- SCREENSHOTS:"
    - path: "guides/03-host-integration.md"
      provides: "Four host behaviour contracts (full callbacks) + relocated telemetry section"
      min_lines: 60
      contains: "on_outbound_triggered"
  key_links:
    - from: "guides/02-jtbd-walkthrough.md"
      to: "test/integration/golden_path_test.exs stage sequence"
      via: "nine stage headings in golden-path order"
      pattern: "[Bb]ulk recovery"
    - from: "guides/03-host-integration.md"
      to: "Cairnloop.Notifier three callbacks"
      via: "documented on_conversation_resolved/on_sla_breach/on_outbound_triggered"
      pattern: "on_outbound_triggered"
---

<objective>
Create the two content-heavy ExDoc guides: `guides/02-jtbd-walkthrough.md` (prose walkthrough of
all nine JTBD stages in the locked golden-path order, with a bounded screenshot TODO block) and
`guides/03-host-integration.md` (the four host behaviour contracts with their full current callback
sets, plus the telemetry section relocated from the README).

Purpose: Adopters understand the live demo flow and can wire the four host integration points
correctly. (DOC-02)
Output: Two new Markdown files under the root-level `guides/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-readme-exdoc-guides-jtbd-walkthrough/32-CONTEXT.md
@.planning/phases/32-readme-exdoc-guides-jtbd-walkthrough/32-PATTERNS.md

<interfaces>
<!-- Authoritative facts the executor needs — extracted from source. Do NOT paraphrase from memory. -->

JTBD nine-stage sequence (D-08 — copy stage NAMES and ORDER from test/integration/golden_path_test.exs
@moduledoc lines 6-15; use the test ONLY for sequence + behaviour/event names, NOT for routes):
  1. Seed (conversation + customer message)
  2. Inbox sees the conversation
  3. ConversationLive + cmd+k search + citation chip
  4. Approve AI draft
  5. Tool proposal approve (via Governance facade)
  6. ToolExecutionWorker :success
  7. Resolve (via Chat.resolve_conversation/2)
  8. Outbound.trigger/2 from sidebar (trigger_recovery_follow_up)
  9. Bulk recovery (InboxLive multi-select → confirm_bulk_send → BulkEnvelope row)

ROUTE CONVENTION (Pitfall 1): use shipped routes `/support` (inbox) and `/support/:id`
(conversation). NEVER `/inbox` or `/governance/:id`.

Bounded screenshot TODO block — copy VERBATIM at the END of 02-jtbd-walkthrough.md (D-01):
  <!-- SCREENSHOTS: boot the example app (`cd examples/cairnloop_example && mix setup && mix phx.server`),
       navigate each JTBD stage, capture PNGs to guides/assets/, update the image references above.
       See guides/02-jtbd-walkthrough.md for the labeled regions. -->
Commit NO actual PNG references and do NOT create guides/assets/ (D-02).

Host behaviour callbacks — document the FULL current set (Pitfall 4: the README's old Notifier
example shows only 2 callbacks; the real behaviour has 3). VERIFIED signatures:
  - Cairnloop.ContextProvider:  get_context(actor_id :: String.t(), opts :: keyword()) :: {:ok, map()} | {:error, term()}
  - Cairnloop.Notifier (THREE): on_conversation_resolved(conversation, metadata) :: :ok | any()
                                on_sla_breach(conversation, sla, metadata) :: :ok | {:error, term()} | any()
                                on_outbound_triggered(message, conversation) :: :ok | {:error, term()} | any()
  - Cairnloop.AutomationPolicy: decide(proposal :: map(), opts :: map()) :: :allow | :draft_only | :require_approval | :deny
  - Cairnloop.SLAPolicyProvider: get_active_policies() :: {:ok, list(map())} | {:error, term()}
                                 set_policy(priority :: atom(), attrs :: map()) :: {:ok, map()} | {:error, term()}

SECURITY (D-09 / research Security Domain): the AutomationPolicy headline example MUST default to
`:require_approval`, NOT `:allow` — matching the project's HITL/approval-gated-only invariant.

Adopter-implementation order for the four behaviours (D-09): ContextProvider (always required) →
Notifier (required for outbound) → AutomationPolicy (governs AI drafting) → SLAPolicyProvider.

Telemetry relocation (D-09): the README currently carries (lines 54-94) the dual-emission telemetry
prose with `[:cairnloop, :conversation, :resolve, :stop]` span and `[:cairnloop, :conversation, :resolved]`
domain-event examples. MOVE that prose+code into a "Telemetry (observability only)" subsection of
03-host-integration.md. Per arch posture: telemetry is observability only, never a UI/display source.
The actual deletion of these blocks from README happens in plan 32-03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write guides/02-jtbd-walkthrough.md</name>
  <files>guides/02-jtbd-walkthrough.md</files>
  <read_first>
    - test/integration/golden_path_test.exs (canonical 9-stage sequence — @moduledoc + inline `# Stage N:` comments; stage NAMES and ORDER only)
    - docs/cairnloop-jtbd-and-user-flows.md (prose tone + flow depth — internal source, NOT exposed in ExDoc)
    - examples/cairnloop_example/lib/cairnloop_example_web/router.ex (confirm shipped /support, /support/:id, /chat routes)
    - prompts/cairnloop_brand_book.md (calm, reason-forward copy register)
  </read_first>
  <action>
    Write the JTBD walkthrough with an H1 title "JTBD Walkthrough" and one `##` section per stage,
    in this exact order (matching golden_path_test.exs, D-08): (1) Seed — a customer message arrives
    and creates a conversation; (2) Inbox sees it — operator opens `/support` and the inbox shows the
    seeded conversations across all status states (include a screen-region description like "The
    inbox shows 12-16 conversations across all status states — new (blue), open, awaiting customer,
    resolved"); (3) Conversation workspace — operator opens `/support/:id`, uses cmd+k search and sees
    a citation chip; (4) Approve AI draft; (5) Governed tool proposal approve (note this goes through
    the `Cairnloop.Governance` facade); (6) ToolExecutionWorker reaches `:success`; (7) Resolve the
    conversation; (8) Outbound trigger from the sidebar (the recovery follow-up); (9) Bulk recovery —
    InboxLive multi-select → confirm → a `BulkEnvelope` row is created with per-recipient outbound
    jobs. For each stage write prose + a screen-region description; include code references to the
    relevant module/facade names where useful (e.g. `Cairnloop.Governance`, `Cairnloop.Outbound.trigger/2`,
    `BulkEnvelope`) but do NOT invent API surface. Use ONLY shipped routes `/support` and `/support/:id`
    (and `/chat` for the customer side) — never `/inbox` or `/governance/:id`. Open with a one-line
    note that routes are mounted under the path passed to `cairnloop_dashboard/2` and this guide
    assumes `/support`. Commit NO PNG image references. End the file with the verbatim bounded
    `<!-- SCREENSHOTS: ... -->` block (see interfaces) as the last content in the file. Do NOT create
    a `guides/assets/` directory.
  </action>
  <verify>
    <automated>test -f guides/02-jtbd-walkthrough.md && grep -q "<!-- SCREENSHOTS:" guides/02-jtbd-walkthrough.md && grep -qi "bulk recovery" guides/02-jtbd-walkthrough.md && grep -q "/support" guides/02-jtbd-walkthrough.md && ! grep -Eq "/inbox|/governance/" guides/02-jtbd-walkthrough.md && ! grep -Eq "!\[[^]]*\]\([^)]+\.png" guides/02-jtbd-walkthrough.md && test ! -d guides/assets && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `guides/02-jtbd-walkthrough.md` exists, begins with an H1, and has a `##` heading for each of the nine stages in golden-path order (Seed → Inbox → Conversation/search/citation → Approve draft → Tool proposal → ToolExecutionWorker success → Resolve → Outbound → Bulk recovery).
    - File contains the literal `<!-- SCREENSHOTS:` bounded TODO block as trailing content.
    - File contains `/support` and `bulk recovery` (case-insensitive).
    - `grep -E "/inbox|/governance/" guides/02-jtbd-walkthrough.md` returns nothing.
    - `grep -E '!\[[^]]*\]\([^)]+\.png' guides/02-jtbd-walkthrough.md` returns nothing (no committed PNG references).
    - `guides/assets/` directory does not exist.
  </acceptance_criteria>
  <done>Walkthrough exists covering all nine stages in golden-path order with screen-region descriptions, shipped routes, no PNG refs, and the bounded screenshot TODO block.</done>
</task>

<task type="auto">
  <name>Task 2: Write guides/03-host-integration.md</name>
  <files>guides/03-host-integration.md</files>
  <read_first>
    - lib/cairnloop/context_provider.ex (moduledoc + get_context/2 callback)
    - lib/cairnloop/notifier.ex (THREE callbacks: on_conversation_resolved/2, on_sla_breach/3, on_outbound_triggered/2)
    - lib/cairnloop/automation_policy.ex (decide/2 → :allow | :draft_only | :require_approval | :deny)
    - lib/cairnloop/sla_policy_provider.ex (get_active_policies/0, set_policy/2)
    - README.md (the telemetry + Notifier sections lines 50-137 that MOVE here — D-09)
    - prompts/cairnloop_brand_book.md (copy register)
  </read_first>
  <action>
    Write the host integration guide with an H1 title "Host Integration" and one `##` section per
    behaviour, in adopter-implementation order (D-09): (1) ContextProvider — document `get_context/2`
    with its `{:ok, map()} | {:error, term()}` return; carry the moduledoc prose that the returned map
    is recursively rendered as categorized UI sections ("Zero-Config UI") and that the callback must
    return tagged tuples and never raise, so the dashboard degrades to "Context Unavailable" rather
    than crashing; show a `MyApp.CairnloopContext` example. (2) Notifier — document ALL THREE callbacks
    (`on_conversation_resolved/2`, `on_sla_breach/3`, `on_outbound_triggered/2`) — do NOT inherit the
    README's stale 2-callback example; mention the generator escape hatch `mix cairnloop.gen.notifier`
    which scaffolds the module and injects `config :cairnloop, :notifier, MyApp.CairnloopNotifier`;
    show the `config :cairnloop, :notifier, MyApp.CairnloopNotifier` config line. (3) AutomationPolicy —
    document `decide/2` returning one of `:allow | :draft_only | :require_approval | :deny`; the headline
    example MUST default to `:require_approval` (project HITL/approval-gated-only invariant) — do NOT
    show `:allow` as the recommended posture. (4) SLAPolicyProvider — document `get_active_policies/0`
    and `set_policy/2` with their tagged-tuple returns. Then add a `## Telemetry (observability only)`
    subsection: move the README's dual-emission prose and the two `:telemetry.attach` examples here —
    the `[:cairnloop, :conversation, :resolve, :stop]` span example and the
    `[:cairnloop, :conversation, :resolved]` domain-event example — and state per arch posture that
    telemetry is observability only, never a UI/display source. All example modules use placeholder
    names (`MyApp.*`); no real secrets/tokens. Keep prose calm and reason-forward per the brand book.
  </action>
  <verify>
    <automated>test -f guides/03-host-integration.md && grep -q "get_context" guides/03-host-integration.md && grep -q "on_outbound_triggered" guides/03-host-integration.md && grep -q "require_approval" guides/03-host-integration.md && grep -q "set_policy" guides/03-host-integration.md && grep -q ":telemetry" guides/03-host-integration.md && ! grep -q "decide(_proposal, _opts), do: :allow" guides/03-host-integration.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `guides/03-host-integration.md` exists, begins with an H1, and has a section per behaviour in order: ContextProvider → Notifier → AutomationPolicy → SLAPolicyProvider.
    - File contains `get_context`, all three Notifier callbacks (`on_conversation_resolved`, `on_sla_breach`, `on_outbound_triggered`), `decide`, `get_active_policies`, and `set_policy`.
    - File contains `require_approval` and does NOT present `:allow` as the AutomationPolicy default (e.g. no `decide(_proposal, _opts), do: :allow`).
    - File contains a Telemetry subsection with `:telemetry` and the `[:cairnloop, :conversation, :resolved]` event name.
    - File contains `mix cairnloop.gen.notifier` and the `config :cairnloop, :notifier` config line.
  </acceptance_criteria>
  <done>Host integration guide exists with the full callback set for all four behaviours, the relocated telemetry section, and the secure `:require_approval` default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan. No executable surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02 | Information Disclosure | guide code examples | mitigate | Placeholders only (`MyApp.*`) — no real secrets/tokens/hostnames. |
| T-32-03 | Tampering (downstream) | AutomationPolicy example | mitigate | Headline example defaults to `:require_approval`, not `:allow` — mirror the project's secure HITL default. |

No blocking threats — documentation phase with nil attack surface.
</threat_model>

<verification>
- Both guide files exist.
- 02-jtbd-walkthrough.md: nine stages in order, shipped routes only, bounded screenshot TODO present, no PNG refs, no `guides/assets/`.
- 03-host-integration.md: full callback sets for all four behaviours, telemetry section relocated, `:require_approval` default.
</verification>

<success_criteria>
The two content-heavy guides exist, grounded verbatim in the behaviour modules and the golden-path test. (DOC-02 — completes the four-guide set together with plan 32-01; ExDoc rendering verified in plan 32-04.)
</success_criteria>

<output>
Create `.planning/phases/32-readme-exdoc-guides-jtbd-walkthrough/32-02-SUMMARY.md` when done.
</output>
