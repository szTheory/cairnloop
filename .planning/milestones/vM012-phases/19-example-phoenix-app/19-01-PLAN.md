---
phase: 19
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/cairnloop_example/mix.exs
  - examples/cairnloop_example/config/config.exs
  - examples/cairnloop_example/lib/cairnloop_example/application.ex
  - examples/cairnloop_example/priv/repo/seeds.exs
  - examples/cairnloop_example/lib/cairnloop_example_web/router.ex
  - examples/cairnloop_example/lib/cairnloop_example_web/live/chat_live.ex
  - examples/cairnloop_example/assets/css/app.css
  - examples/cairnloop_example/README.md
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
must_haves:
  truths:
    - "Example app boots with a single mix setup command"
    - "Example app demonstrates draft/approval/KB flow in the browser"
    - "README documents host app integration"
    - "App requires published cairnloop ~> 0.1 hex dependency"
  artifacts:
    - path: "examples/cairnloop_example/mix.exs"
      provides: "Dependency configuration"
    - path: "examples/cairnloop_example/priv/repo/seeds.exs"
      provides: "Database seeding for demo"
    - path: "examples/cairnloop_example/README.md"
      provides: "Documentation"
    - path: "examples/cairnloop_example/lib/cairnloop_example_web/router.ex"
      provides: "Demo routes"
  key_links:
    - from: "examples/cairnloop_example/mix.exs"
      to: "cairnloop hex dependency"
      via: "mix deps"
      pattern: "{:cairnloop, \"~> 0.1"
    - from: "examples/cairnloop_example/lib/cairnloop_example_web/router.ex"
      to: "Cairnloop.Router"
      via: "cairnloop_dashboard"
      pattern: "cairnloop_dashboard"
---

<objective>
Generate the Phoenix example app, configure dependencies, establish the data layer (migrations and seeds), and implement the frontend demo flow.

Purpose: Provides a fully functional, self-contained Phoenix application to demonstrate Cairnloop's capabilities and documents the setup process for host applications.
Output: A bootstrapped Phoenix application in `examples/cairnloop_example/` with configured Ecto, Oban, pgvector, UI, and Cairnloop integrated.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-example-phoenix-app/19-RESEARCH.md
@.planning/phases/19-example-phoenix-app/19-PATTERNS.md
@.planning/phases/19-example-phoenix-app/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate App & Dependencies</name>
  <files>examples/cairnloop_example/mix.exs, examples/cairnloop_example/config/config.exs, examples/cairnloop_example/lib/cairnloop_example/application.ex</files>
  <action>
    1. Run `mix phx.new examples/cairnloop_example --no-mailer --no-gettext --no-dashboard` from the project root.
    2. Edit `examples/cairnloop_example/mix.exs` to include `{:cairnloop, "~> 0.1.0"}`, `{:oban, "~> 2.17"}`, `{:pgvector, "~> 0.3.1"}`, and `{:igniter, "~> 0.5"}`. Ensure `cairnloop` is a Hex dep (not path).
    3. Configure Oban in `config/config.exs` with `repo: CairnloopExample.Repo` and add `Oban` to the supervision tree in `application.ex`.
    4. Set `config :cairnloop, :repo, CairnloopExample.Repo` and `config :cairnloop, :tools, []` in `config.exs`.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example && mix deps.get && mix deps.tree | grep cairnloop</automated>
  </verify>
  <done>App generated and mix deps.get succeeds, showing cairnloop as a dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Database Setup, Generators & Seeding</name>
  <files>examples/cairnloop_example/priv/repo/seeds.exs</files>
  <action>
    1. Create a leading migration file in `examples/cairnloop_example/priv/repo/migrations/` (e.g. `20240101000000_add_vector_extension.exs`) that executes `CREATE EXTENSION IF NOT EXISTS vector;`.
    2. From within `examples/cairnloop_example`, run `mix oban.install`, `mix cairnloop.install`, and `mix cairnloop.add_draft_table`.
    3. Update `priv/repo/seeds.exs` to insert a dummy `Cairnloop.Conversation` (status: :open), a `Cairnloop.Message` (role: :user, content: "I need help..."), and a `Cairnloop.KnowledgeBase.Article`.
  </action>
  <verify>
    <automated>cd examples/cairnloop_example && mix setup</automated>
  </verify>
  <done>mix setup runs without errors and the database is populated with the vector extension, Oban tables, Cairnloop tables, and seed data.</done>
</task>

<task type="auto">
  <name>Task 3: UI Integration and Documentation</name>
  <files>examples/cairnloop_example/lib/cairnloop_example_web/router.ex, examples/cairnloop_example/lib/cairnloop_example_web/live/chat_live.ex, examples/cairnloop_example/assets/css/app.css, examples/cairnloop_example/README.md</files>
  <action>
    1. Update `router.ex` to `import Cairnloop.Router` and mount `cairnloop_dashboard("/support", host_user_id: "demo_operator")` inside a scope.
    2. Create a basic `ChatLive` view mounted at `/chat` to simulate the customer interface (using Phoenix core components).
    3. Add Cairnloop brand CSS tokens from 19-PATTERNS.md to `app.css`.
    4. Overwrite `README.md` to document the setup (`mix setup`), routing, and integration steps (deps, Oban, pgvector, igniter tasks).
  </action>
  <verify>
    <automated>cd examples/cairnloop_example && mix compile</automated>
  </verify>
  <done>Routes are accessible, CSS is present, and README is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo DB | Local postgres instance created for the demo app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | Seeds | accept | Demo environment is local/ephemeral. |
| T-19-02 | Spoofing | Operator Dashboard | accept | Demo environment requires no auth for simplicity. |
</threat_model>

<verification>
Verify `mix setup` executes Ecto setup properly and dependency is satisfied. Ensure UI boots.
</verification>

<success_criteria>
App bootstraps, dependencies resolve, database schema is fully established, and documentation is provided.
</success_criteria>

<output>
After completion, create `.planning/phases/19-example-phoenix-app/19-01-SUMMARY.md`
</output>