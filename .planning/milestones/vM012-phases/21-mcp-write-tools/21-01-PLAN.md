---
phase: 21-mcp-write-tools
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - test/cairnloop/web/mcp/router_test.exs
  - lib/cairnloop/web/mcp/router.ex
autonomous: true
requirements:
  - ACT-02
  - ACT-03
must_haves:
  truths:
    - An authenticated MCP client calling tools/call with a valid governed-tool name receives a success response with proposal_id and pending_approval text
    - Duplicate calls return the same success block without error
    - Unauthenticated requests get 401
    - Invalid arguments return standard JSON-RPC error -32602
    - Execution is delegated to Governance.propose/3 and Tool.run/3 is never called synchronously
  artifacts:
    - path: lib/cairnloop/web/mcp/router.ex
      provides: JSON-RPC tools/call handler mapping to Governance
    - path: test/cairnloop/web/mcp/router_test.exs
      provides: Integration tests for MCP router write functionality
  key_links:
    - from: lib/cairnloop/web/mcp/router.ex
      to: Cairnloop.Governance
      via: Cairnloop.Governance.propose/3
      pattern: Governance\.propose
---

<objective>
Implement the MCP `tools/call` JSON-RPC handler to route write-capable tool invocations through Cairnloop's governance proposal pipeline.

Purpose: Allow remote MCP clients to trigger actions while strictly enforcing the async approval flow and unique idempotency constraint. No direct tool execution is permitted.
Output: A fully tested `tools/call` handler in the MCP router.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/21-mcp-write-tools/DECISION.md
@.planning/phases/21-mcp-write-tools/PATTERNS.md
@.planning/phases/21-mcp-write-tools/RESEARCH.md

<interfaces>
From `lib/cairnloop/governance.ex`:
```elixir
@spec propose(String.t(), String.t(), map()) ::
        {:ok, ToolProposal.t()}
        | {:error, Ecto.Changeset.t()}
        | {:blocked, outcome :: atom(), reason :: any()}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update MCP Router tests for DB integration</name>
  <files>test/cairnloop/web/mcp/router_test.exs</files>
  <behavior>
    - Test 1: `tools/call` with valid token and valid governed tool name returns `200 OK` and a JSON-RPC success block containing `isError: false` and text with the `proposal_id` and `pending_approval` status.
    - Test 2: `tools/call` identically duplicated returns the exact same success block without error.
    - Test 3: `tools/call` with missing token returns `401 Unauthorized`.
    - Test 4: `tools/call` with invalid arguments (e.g. missing required params for the tool) returns JSON-RPC error `-32602`.
    - Test 5: `tools/call` with an unsupported tool name returns JSON-RPC error `-32601`.
  </behavior>
  <action>
    Change the test module to `use Cairnloop.ConnCase, async: true` (instead of `use ExUnit.Case`) so that it can interact with the database, which is required for `Governance.propose/3`.

    In the `setup` block (or per-test), use `Cairnloop.MCP.issue_token(%{name: "Test Token"})` to generate a valid token. When building the test `conn`, ensure you pass this raw token in the `authorization: Bearer <token>` header so `AuthPlug` succeeds.

    Write the failing test cases as defined in the behavior block. Use a simple known tool (e.g., `Cairnloop.Tools.InternalNote`) for the valid payload tests. Ensure all responses are structurally validated per the MCP JSON-RPC 2.0 specs.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/mcp/router_test.exs</automated>
  </verify>
  <done>Failing test suite committed for `tools/call` behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tools/call handler in Router</name>
  <files>lib/cairnloop/web/mcp/router.ex</files>
  <action>
    Replace the stubbed `tools/call` method in `Cairnloop.Web.MCP.Router` with the full implementation calling `Governance.propose/3` (reference analog: `lib/cairnloop/web/conversation_live.ex` as defined in `PATTERNS.md`).

    Extract the `mcp_token` from `conn.assigns`. If missing, fallback to 401 Unauthorized (this logic already exists in the file).
    If present, prepare the context:
    ```elixir
    context = %{
      origin: :mcp,
      mcp_token_id: token.id,
      mcp_token_name: token.name,
      tool_params: params["arguments"] || %{}
    }
    ```
    Call `Cairnloop.Governance.propose(params["name"], "mcp_token:#{token.id}", context)`.

    Match the returns as detailed in `.planning/phases/21-mcp-write-tools/PATTERNS.md`:
    - `{:ok, proposal}` -> Call `json_result/3` with `content: [%{type: "text", text: "Proposal created... ID: #{proposal.id} Status: pending_approval..."}], isError: false`.
    - `{:blocked, :unsupported, _}` -> Call `json_error/4` with `-32601`.
    - `{:blocked, :needs_input, changeset}` -> Call `json_error/4` with `-32602` and format the changeset errors into a string message. Add a private `format_changeset_errors/1` helper using `Ecto.Changeset.traverse_errors`.
    - `{:blocked, outcome, reason}` -> Call `json_result/3` with `content: [%{type: "text", text: "Action blocked..."}], isError: true`.
    - `{:error, _changeset}` -> Call `json_error/4` with `-32603`.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/mcp/router_test.exs</automated>
  </verify>
  <done>Router test suite passes green, all JSON-RPC constraints and error formats met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → MCP Router | Remote, untrusted input crossing into the JSON-RPC interface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Spoofing | `tools/call` | mitigate | Relies on `AuthPlug` and Token record validation; `mcp_token` identity strictly appended with prefix to avoid masquerading as local user. |
| T-21-02 | Tampering | `tools/call` | mitigate | Idempotency and de-duplication managed fully by `Governance.propose/3` and unique DB constraints. |
| T-21-03 | Denial of Service | `tools/call` | mitigate | The `tool_name` parameter is passed directly as a string to `Governance.propose/3`. No atoms are dynamically generated from user input. |
</threat_model>

<verification>
- `mix test test/cairnloop/web/mcp/router_test.exs` passes 100%.
- Router contains no synchronous calls to `Tool.run/3`.
</verification>

<success_criteria>
An authenticated MCP client can successfully submit a valid `tools/call` JSON-RPC method and immediately receive a `proposal_id` with `isError: false`, mapping cleanly to the async Governance pipeline without any custom execution logic in the router.
</success_criteria>

<output>
After completion, create `.planning/phases/21-mcp-write-tools/21-mcp-write-tools-01-SUMMARY.md`
</output>