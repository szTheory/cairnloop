---
phase: "20-mcp-oauth-seam"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "priv/repo/migrations/*_create_cairnloop_mcp_tokens.exs"
  - "lib/cairnloop/mcp/token.ex"
  - "lib/cairnloop/mcp.ex"
  - "test/cairnloop/mcp_test.exs"
autonomous: true
requirements:
  - "MCP-02"
  - "MCP-03"
must_haves:
  truths:
    - "MCP tokens can be issued and stored securely as SHA-256 hashes"
  artifacts:
    - path: "lib/cairnloop/mcp/token.ex"
      provides: "Token schema"
      min_lines: 30
    - path: "lib/cairnloop/mcp.ex"
      provides: "Token lifecycle context API"
      exports: ["issue_token", "validate_token", "revoke_token"]
  key_links:
    - from: "lib/cairnloop/mcp.ex"
      to: "lib/cairnloop/mcp/token.ex"
      via: "Ecto queries"
      pattern: "Token"
---

<objective>
Implement the core Ecto models, migrations, and context logic for the MCP token lifecycle, securing stored tokens via hashing.

Purpose: Provide durable storage for MCP access tokens based on Ecto, ensuring hashes rather than plain text are stored in the database.
Output: Migration file, `Token` Ecto schema, and `Cairnloop.MCP` context functions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-mcp-oauth-seam/20-PATTERNS.md
@.planning/phases/20-mcp-oauth-seam/20-RESEARCH.md
@.planning/phases/20-mcp-oauth-seam/DISCUSS.md
</context>

<tasks>

<task type="auto">
  <name>1. Ecto Migration</name>
  <files>priv/repo/migrations/[timestamp]_create_cairnloop_mcp_tokens.exs</files>
  <action>
    Generate Ecto migration for `cairnloop_mcp_tokens`.
    Reference the exact pattern from `20-PATTERNS.md` (Analog: `priv/repo/migrations/20260524000000_add_tool_proposals_and_action_events.exs`).
    Fields required: `id` (binary_id, primary key), `name` (string, null: false), `token_hash` (binary, null: false), `expires_at` (utc_datetime_usec), `revoked_at` (utc_datetime_usec).
    Add timestamps (`type: :utc_datetime_usec`).
    Create a unique index on `token_hash`.
  </action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>Database table `cairnloop_mcp_tokens` exists with correct columns and index.</done>
</task>

<task type="auto" tdd="true">
  <name>2. Token Schema</name>
  <files>lib/cairnloop/mcp/token.ex</files>
  <behavior>
    - Schema maps to `cairnloop_mcp_tokens`
    - Changeset validates required fields and unique token hash
  </behavior>
  <action>
    Create `Cairnloop.MCP.Token` Ecto schema.
    Reference the exact schema and changeset pattern from `20-PATTERNS.md` (Analog: `lib/cairnloop/governance/tool_proposal.ex`).
    Set `@primary_key {:id, :binary_id, autogenerate: true}` and `@foreign_key_type :binary_id`.
    Define fields matching the migration.
    Add `changeset/2` function casting `[:name, :token_hash, :expires_at, :revoked_at]`, requiring `[:name, :token_hash]`, and setting `unique_constraint(:token_hash)`.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>Schema module compiles and correctly maps to the database fields.</done>
</task>

<task type="auto" tdd="true">
  <name>3. Context API (Token Lifecycle)</name>
  <files>lib/cairnloop/mcp.ex, test/cairnloop/mcp_test.exs</files>
  <behavior>
    - `issue_token/1` returns securely generated and hashed token
    - `validate_token/1` correctly retrieves valid tokens and rejects invalid/revoked
    - `revoke_token/1` updates `revoked_at`
  </behavior>
  <action>
    Implement `Cairnloop.MCP` context.
    Reference the Context Structure and Repo Integration patterns from `20-PATTERNS.md` (Analog: `lib/cairnloop/governance.ex` and Shared Patterns). Use `defp repo` to dynamically fetch the repo config.
    `issue_token(attrs)`: Use `:crypto.strong_rand_bytes(32) |> Base.url_encode64(padding: false)` for token string. Hash it via `:crypto.hash(:sha256, raw_token)`. Insert using Token changeset and return `{:ok, token_record, raw_token}`.
    `validate_token(raw_token)`: Hash input, query Repo for active (unrevoked, unexpired) record. Return `{:ok, token_record}` or `{:error, :unauthorized}`.
    `revoke_token(token_record)`: Update `revoked_at` to current time.
    Write ExUnit tests covering these functions.
  </action>
  <verify>
    <automated>mix test test/cairnloop/mcp_test.exs</automated>
  </verify>
  <done>Context functions handle secure generation and hashing; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted MCP clients submitting JSON-RPC requests with Bearer tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | `Cairnloop.MCP` DB | mitigate | Store only SHA-256 hashes of tokens in Ecto, never plain text. |
| T-20-02 | Information Disclosure | Token Validation | mitigate | Query by hash to prevent timing attacks on token string comparison. |
| T-20-03 | Spoofing | `AuthPlug` | mitigate | Cryptographically secure token generation via `:crypto.strong_rand_bytes(32)`. |
</threat_model>

<verification>
See VALIDATION.md
</verification>

<success_criteria>
1. Token Ecto schema and migration are committed.
2. Context provides secure token lifecycle.
3. All ExUnit tests pass successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/20-mcp-oauth-seam/20-01-SUMMARY.md`
</output>