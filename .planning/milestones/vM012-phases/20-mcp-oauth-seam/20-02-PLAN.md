---
phase: "20-mcp-oauth-seam"
plan: "02"
type: "execute"
wave: 2
depends_on: ["20-01"]
files_modified:
  - "lib/cairnloop/web/mcp/auth_plug.ex"
  - "lib/cairnloop/web/mcp/well_known_plug.ex"
  - "test/cairnloop/web/mcp/auth_plug_test.exs"
  - "test/cairnloop/web/mcp/well_known_plug_test.exs"
  - "lib/cairnloop/web/mcp/router.ex"
  - "lib/cairnloop/web/mcp/tool_projector.ex"
autonomous: true
requirements:
  - "MCP-02"
  - "MCP-03"
must_haves:
  truths:
    - "AuthPlug extracts and validates Bearer tokens without halting"
    - "WellKnownPlug serves RFC 9728 metadata"
    - "Router enforces 401 Unauthorized for write methods if token is invalid/missing"
    - "Protocol version is bumped to 2025-11-05"
  artifacts:
    - path: "lib/cairnloop/web/mcp/auth_plug.ex"
      provides: "Token validation plug"
    - path: "lib/cairnloop/web/mcp/well_known_plug.ex"
      provides: "RFC 9728 endpoint"
    - path: "lib/cairnloop/web/mcp/router.ex"
      provides: "401 enforcement for tools/call and protocol version update"
    - path: "lib/cairnloop/web/mcp/tool_projector.ex"
      provides: "Protocol version update"
  key_links:
    - from: "lib/cairnloop/web/mcp/auth_plug.ex"
      to: "lib/cairnloop/mcp.ex"
      via: "Cairnloop.MCP.validate_token/1"
      pattern: "Cairnloop\\.MCP\\.validate_token"
    - from: "lib/cairnloop/web/mcp/router.ex"
      to: "lib/cairnloop/web/mcp/auth_plug.ex"
      via: "conn.assigns.mcp_token check for write methods"
      pattern: "assigns\\.mcp_token"
---

<objective>
Implement the web plugs and router integration to enforce MCP OAuth Bearer authentication and bump protocol version.

Purpose: Provide a stateless-to-the-network but database-backed authentication layer for MCP, exposing RFC 9728 metadata, and bump protocol for requirement coverage.
Output: `AuthPlug`, `WellKnownPlug`, updated router with auth enforcement, and protocol bump.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-mcp-oauth-seam/20-01-SUMMARY.md
@.planning/phases/20-mcp-oauth-seam/20-PATTERNS.md
@.planning/phases/20-mcp-oauth-seam/20-RESEARCH.md
</context>

<interfaces>
From lib/cairnloop/mcp.ex:
```elixir
defmodule Cairnloop.MCP do
  def issue_token(attrs \\ %{})
  def validate_token(raw_token)
  def revoke_token(token)
end
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>1. AuthPlug and WellKnownPlug</name>
  <files>lib/cairnloop/web/mcp/auth_plug.ex, lib/cairnloop/web/mcp/well_known_plug.ex, test/cairnloop/web/mcp/auth_plug_test.exs, test/cairnloop/web/mcp/well_known_plug_test.exs</files>
  <behavior>
    - `AuthPlug` assigns valid token to `conn.assigns.mcp_token` without halting on failure
    - `WellKnownPlug` returns RFC 9728 JSON config and halts
  </behavior>
  <action>
    Create `Cairnloop.Web.MCP.AuthPlug` and `Cairnloop.Web.MCP.WellKnownPlug`.
    Reference the Plug Construction pattern from `20-PATTERNS.md` (Analog: `lib/cairnloop/web/mcp/router.ex` and Shared Patterns). Use `@behaviour Plug` and `def init(opts), do: opts`.
    For `AuthPlug`: Extract `Authorization: Bearer <token>` via `Plug.Conn.get_req_header/2`. Call `Cairnloop.MCP.validate_token/1`. If valid, assign `:mcp_token` to `conn`. Do NOT `halt(conn)` on invalid/missing tokens; simply leave `assigns.mcp_token` empty.
    For `WellKnownPlug`: Fetch `:mcp_authorization_servers` from application env, send JSON `%{authorization_servers: servers}` (status 200), and halt.
    Write tests for both plugs.
  </action>
  <verify>
    <automated>mix test test/cairnloop/web/mcp/auth_plug_test.exs test/cairnloop/web/mcp/well_known_plug_test.exs</automated>
  </verify>
  <done>Plugs conform to research patterns and pass tests.</done>
</task>

<task type="auto" tdd="true">
  <name>2. Router Integration and Protocol Version Bump</name>
  <files>lib/cairnloop/web/mcp/router.ex, lib/cairnloop/web/mcp/tool_projector.ex</files>
  <behavior>
    - MCP router rejects write methods (`tools/call`) with 401 if token is missing
    - Protocol version is updated to "2025-11-05"
  </behavior>
  <action>
    Update `Cairnloop.Web.MCP.Router.call/2` (or equivalent method dispatcher).
    Reference error formatting from `20-PATTERNS.md` if applicable.
    Ensure `AuthPlug` runs before the JSON body is consumed for routing, but do not double-parse.
    For write methods like `"tools/call"`, explicitly check `conn.assigns.mcp_token`. If nil, return HTTP 401 with header `WWW-Authenticate: Bearer` (or point to RFC 9728 endpoint if fully structured) and short-circuit the response without invoking the tool.
    CRITICAL: Bump the `@protocol_version` (or equivalent constant) from "2025-03-26" to "2025-11-05" in both `lib/cairnloop/web/mcp/router.ex` and `lib/cairnloop/web/mcp/tool_projector.ex` to fulfill ROADMAP requirements.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>Router enforces 401 strictly for write-capable tool calls and protocol version is bumped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted MCP clients submitting JSON-RPC requests with Bearer tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Elevation of Privilege | `Router` | mitigate | Explicit method-based check (`tools/call`) for `conn.assigns.mcp_token`. |
</threat_model>

<verification>
See VALIDATION.md
</verification>

<success_criteria>
1. Plugs behave non-haltingly for auth and standardly for well-known metadata.
2. Router strictly guards `tools/call` with HTTP 401.
3. Protocol version is bumped in relevant files.
4. All ExUnit tests pass successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/20-mcp-oauth-seam/20-02-SUMMARY.md`
</output>