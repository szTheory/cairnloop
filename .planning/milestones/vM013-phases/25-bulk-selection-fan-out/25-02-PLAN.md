---
phase: 25-bulk-selection-fan-out
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - lib/cairnloop/outbound.ex
  - lib/cairnloop/workers/outbound_worker.ex
  - test/cairnloop/outbound_test.exs
  - test/cairnloop/workers/outbound_worker_test.exs
autonomous: true
requirements: [BULK-02, BULK-03]

must_haves:
  truths:
    - "`Cairnloop.Outbound.trigger/2` STAYS SEALED at its public surface — only an additive optional opt `:bulk_envelope_id` (default `nil`) is added (D-12)."
    - "A new `Cairnloop.Outbound.bulk_trigger/2` exists that: validates cohort against `max_batch_size` (hard fail-closed per D-09), snapshots the rendered template body on a single `BulkEnvelope` row (D-13), and fans out `trigger/2` for each recipient inside one `Ecto.Multi` (no transaction nesting, per research Open Question 1)."
    - "`bulk_trigger/2` returns `{:error, :batch_too_large}` when `length(conversation_ids) > max_batch_size()` AND persists a `BulkEnvelope` row with `status: :refused_cap_exceeded` (research Open Question 5)."
    - "Per-recipient `OutboundWorker` jobs declare Oban uniqueness with `unique: [period: :infinity, fields: [:worker, :args], keys: [:conversation_id, :template_id, :bulk_envelope_id]]` (D-11). Phase 24 single-conversation callers pass `bulk_envelope_id: nil` and participate in the same dedup — confirmed acceptable per research Open Question 2."
    - "`max_batch_size()` reads from `Application.get_env(:cairnloop, :max_batch_size, 25)` (D-09 + research Open Question 3 — direct env access, no Config module)."
    - "Telemetry for bulk emits `[:cairnloop, :outbound, :bulk, :triggered]` with enum-only labels (`outcome: :submitted | :refused_cap_exceeded`); `template_id` and recipient identifiers are NEVER in labels (D-B)."
  artifacts:
    - path: "lib/cairnloop/outbound.ex"
      provides: "`bulk_trigger/2` public function, a private `build_trigger_multi/2` shared helper (research Open Question 1), `max_batch_size/0` private helper, and an additive `:bulk_envelope_id` opt threaded through `trigger/2` into `Message.metadata` and `OutboundWorker.new/2` args."
      contains: "def bulk_trigger("
    - path: "lib/cairnloop/workers/outbound_worker.ex"
      provides: "Updated `use Oban.Worker` declaration with `unique:` clause keyed on `(conversation_id, template_id, bulk_envelope_id)`."
      contains: "unique: [period: :infinity, fields: [:worker, :args], keys: [:conversation_id, :template_id, :bulk_envelope_id]]"
    - path: "test/cairnloop/outbound_test.exs"
      provides: "Headless `MockRepo`-driven tests for `bulk_trigger/2`: happy path (envelope + N triggers via Multi.merge), cap refusal both at `cap` and `cap+1`, snapshot persistence of `rendered_body`, `bulk_envelope_id` threading into worker args, telemetry emit with enum-only labels."
    - path: "test/cairnloop/workers/outbound_worker_test.exs"
      provides: "Asserts `OutboundWorker.__opts__()` (or `OutboundWorker.new/2` constructed job) carries the new `unique:` clause; asserts the worker's `perform/1` reads `bulk_envelope_id` from args without crashing when nil (Phase 24 backwards-compat)."
  key_links:
    - from: "lib/cairnloop/outbound.ex (bulk_trigger/2)"
      to: "lib/cairnloop/outbound/bulk_envelope.ex"
      via: "Ecto.Multi.insert/3 of a BulkEnvelope changeset before merging per-recipient multis"
      pattern: "Ecto\\.Multi\\.insert\\(.*BulkEnvelope"
    - from: "lib/cairnloop/outbound.ex (trigger/2 additive opt)"
      to: "lib/cairnloop/workers/outbound_worker.ex (Oban unique: keys)"
      via: "OutboundWorker.new/2 args carrying conversation_id, template_id, bulk_envelope_id"
      pattern: "\"bulk_envelope_id\" =>"
    - from: "lib/cairnloop/outbound.ex (max_batch_size/0)"
      to: "Application env :max_batch_size"
      via: "Application.get_env/3 with default 25"
      pattern: "Application\\.get_env\\(:cairnloop, :max_batch_size, 25\\)"
---

<objective>
Land the `Cairnloop.Outbound` envelope side of Phase 25: a sealed-primitive-preserving `bulk_trigger/2` plus the Oban-level idempotency guard that backs D-11. This is the seam between InboxLive (Plan 03) and the durable + delivery layers (Phase 22/23 sealed code paths).

Purpose: Plan 03 needs a callable `Outbound.bulk_trigger(ids, opts)` that already enforces the cap (defense-in-depth — research Pitfall 4), snapshots the rendered body (T-25-03 mitigation), and writes the audit envelope (OBS-02 shape Phase 26 reads). The sealed `trigger/2` cannot change shape (D-12 / project "seal completed phases"), so the per-recipient multi-building logic is extracted into a private helper that both `trigger/2` and `bulk_trigger/2` reuse.

Output: One new public function (`bulk_trigger/2`), one private shared helper (`build_trigger_multi/2`), one private config helper (`max_batch_size/0`), one additive opt threaded through `trigger/2`, and an Oban `unique:` declaration on `OutboundWorker`. No LiveView changes; no UI yet — that's Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md
@.planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md
@.planning/phases/25-bulk-selection-fan-out/25-VALIDATION.md
@.planning/phases/25-bulk-selection-fan-out/25-01-SUMMARY.md
@CLAUDE.md
@lib/cairnloop/outbound.ex
@lib/cairnloop/outbound/bulk_envelope.ex
@lib/cairnloop/workers/outbound_worker.ex
@lib/cairnloop/workers/approval_resume_worker.ex
@lib/cairnloop/telemetry.ex
@test/cairnloop/outbound_test.exs
@test/cairnloop/workers/outbound_worker_test.exs

<interfaces>
<!-- Contracts the executor needs. Extracted from in-repo code. -->

From lib/cairnloop/outbound.ex (current sealed `trigger/2` shape — DO NOT change the public signature, only ADD optional keys):
```
def trigger(conversation_id, opts) do
  template_id  = Keyword.fetch!(opts, :template_id)
  content      = Keyword.get(opts, :content, "Outbound message using template: #{template_id}")
  schedule_in  = Keyword.get(opts, :schedule_in)
  actor        = Keyword.get(opts, :actor)
  auditor      = Keyword.get(opts, :auditor, Application.get_env(:cairnloop, :auditor, Cairnloop.Auditor.NoOp))
  # ...
end
```

After this plan adds the additive `:bulk_envelope_id` opt, the signature stays `trigger(conversation_id, opts)` — `bulk_envelope_id` is consumed via `Keyword.get(opts, :bulk_envelope_id)`.

From lib/cairnloop/workers/approval_resume_worker.ex:36 (the template to copy for Oban `unique:`):
```
use Oban.Worker,
  queue: :default,
  unique: [period: :infinity, fields: [:worker, :args], keys: [:approval_id]]
```

From lib/cairnloop/workers/outbound_worker.ex (current — line 1):
```
use Oban.Worker, queue: :default
```
Target after this plan:
```
use Oban.Worker,
  queue: :default,
  unique: [period: :infinity, fields: [:worker, :args],
           keys: [:conversation_id, :template_id, :bulk_envelope_id]]
```

From lib/cairnloop/telemetry.ex (the existing `Cairnloop.Telemetry.span/3` and `Cairnloop.Telemetry.execute/3` helpers — bulk_trigger should wrap its multi in `Cairnloop.Telemetry.span([:outbound, :bulk, :triggered], meta, fn -> ... end)` mirroring the existing `trigger/2` shape).

From test/cairnloop/outbound_test.exs (the MockRepo pattern that supports `Ecto.Multi.merge/2` already — lines 31-38). The new tests can reuse this MockRepo without modification for the happy-path tests.

Decisions resolved (so the executor doesn't have to re-litigate):
- **OQ1 (private helper):** Extract `build_trigger_multi/2` private function that returns an `Ecto.Multi` WITHOUT running it. Both `trigger/2` and `bulk_trigger/2` use it. Keeps `trigger/2`'s public shape sealed and avoids transaction nesting.
- **OQ2 (Phase 24 dedup):** Phase 24 callers DO participate — `bulk_envelope_id: nil` is treated by Oban as a valid dedup-key value. Two same-template Phase 24 recoveries for the same conversation in quick succession will be deduped, which is the desired behavior per D-11 ("at-most-once delivery").
- **OQ3 (config location):** Direct `Application.get_env(:cairnloop, :max_batch_size, 25)` — mirrors `:outbound_recovery_template_id` in `conversation_live.ex:1744`. No `Cairnloop.Outbound.Config` module.
- **OQ5 (persist refusals):** YES — `bulk_trigger/2` writes a `BulkEnvelope` row with `status: :refused_cap_exceeded` and `refused_reason: "batch_size <length> exceeds cap <cap>"` before returning `{:error, :batch_too_large}`. Mirrors `Governance.propose_blocked` pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Oban `unique:` to OutboundWorker + bulk_envelope_id-aware perform</name>
  <files>lib/cairnloop/workers/outbound_worker.ex, test/cairnloop/workers/outbound_worker_test.exs</files>
  <read_first>
    - lib/cairnloop/workers/outbound_worker.ex (current 39-line file)
    - lib/cairnloop/workers/approval_resume_worker.ex (lines 32–40 — `unique:` shape to mirror)
    - lib/cairnloop/workers/tool_execution_worker.ex (lines 36–42 — second instance of the same pattern, for cross-check)
    - test/cairnloop/workers/outbound_worker_test.exs (entire file — MockRepo + MockNotifier patterns)
    - .planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md § "Pattern 3: Per-recipient Oban uniqueness" (lines 231–252) — load-bearing
    - .planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md § "Batch Safety Rails" (D-11)
  </read_first>
  <behavior>
    - Test 1: An `OutboundWorker.new/2` job constructed with `%{"message_id" => 1, "conversation_id" => 10, "template_id" => "recovery_v1", "bulk_envelope_id" => nil}` is a valid `Oban.Job` struct.
    - Test 2: `OutboundWorker.__opts__()` (or the equivalent Oban metadata accessor — `OutboundWorker.unique()` is exposed by Oban 2.17) returns a keyword list containing `[period: :infinity, fields: [:worker, :args], keys: [:conversation_id, :template_id, :bulk_envelope_id]]`. If `__opts__` is not stable across Oban versions, fall back to constructing two jobs with identical keys and asserting that Oban-side deduplication metadata is present on the second job (Oban 2.17 surfaces a `:conflict?` field on rejected unique inserts — best to test with `Oban.Testing` if available, otherwise inspect the worker module's compile-time attribute via `OutboundWorker.__info__(:attributes)`).
    - Test 3: `OutboundWorker.perform(%Oban.Job{args: %{"message_id" => 1, "bulk_envelope_id" => nil}})` succeeds (Phase 24 backwards-compat — no crash on nil envelope id).
    - Test 4: `OutboundWorker.perform(%Oban.Job{args: %{"message_id" => 1, "bulk_envelope_id" => "uuid-string"}})` succeeds and (optionally — only if the executor decides to thread bulk_envelope_id into the persisted status payload) `Message.metadata["bulk_envelope_id"]` is preserved. v1 minimum: perform does NOT crash on the new arg.
  </behavior>
  <action>
    Modify `lib/cairnloop/workers/outbound_worker.ex`:

    1. Replace `use Oban.Worker, queue: :default` with the multi-line form including the `unique:` clause from research § "Pattern 3":
       `unique: [period: :infinity, fields: [:worker, :args], keys: [:conversation_id, :template_id, :bulk_envelope_id]]`.
    2. `perform/1` must NOT crash when `args` includes `"bulk_envelope_id"` (string or nil) — it doesn't currently use that arg, and v1 does not require it to do anything with it. Just ensure the existing pattern match on `%{"message_id" => message_id}` still works whether or not `bulk_envelope_id` is present; this should be true by default because `=` is a partial match on map shape, not a strict equality.
    3. Add a module docstring explaining the unique-keys policy (D-11, research Pattern 3) and citing that Phase 24 callers pass `bulk_envelope_id: nil` and participate in the same dedup (research Open Question 2).
    4. No behavior change to `update_message_status/2`.

    Modify `test/cairnloop/workers/outbound_worker_test.exs`:

    - Existing tests must remain green (Phase 24 callers don't pass `bulk_envelope_id`; the worker still runs).
    - Add a new `describe "Oban unique policy (Phase 25 D-11)"` block holding the four behavior tests above. Use `OutboundWorker.__info__(:attributes)` or `OutboundWorker.new(%{...}, []).meta` to inspect the unique-keys clause — pick whichever Oban 2.17 surfaces cleanly. If a clean introspection is not available, use `Code.Typespec.fetch_types/1`-style introspection on the worker module's `@unique` attribute, OR (cleanest) write a regression test that simply asserts the source file contains the `keys: [:conversation_id, :template_id, :bulk_envelope_id]` substring (less ideal but defensible — note as a "structural test until Oban exposes a stable introspection API" in a comment).
    - Add a regression test that `OutboundWorker.perform/1` with `args: %{"message_id" => 1, "bulk_envelope_id" => "some-uuid"}` returns `{:ok, _}` (Phase 25 forward-compat).
    - Add a regression test that `OutboundWorker.perform/1` with `args: %{"message_id" => 1}` (no `bulk_envelope_id` key) STILL returns `{:ok, _}` (Phase 24 backwards-compat — the test that pins Phase 24 behavior under the new declaration).
    - All tests headless against MockRepo.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tail -10 &amp;&amp; mix test test/cairnloop/workers/outbound_worker_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/workers/outbound_worker_test.exs` exits 0; both existing Phase 24 tests AND the new Phase 25 dedup tests are green.
    - `grep -c "unique: \[period: :infinity, fields: \[:worker, :args\], keys: \[:conversation_id, :template_id, :bulk_envelope_id\]\]" lib/cairnloop/workers/outbound_worker.ex` returns 1.
    - `grep -c "use Oban.Worker" lib/cairnloop/workers/outbound_worker.ex` returns 1 (no duplication).
    - `grep -c "D-11" lib/cairnloop/workers/outbound_worker.ex` returns at least 1 (decision-ID traceability in the module docstring).
    - `mix compile --warnings-as-errors` exits 0 (D-15).
    - Phase 24 backwards-compat regression test passes (no crash when `bulk_envelope_id` absent from args).
  </acceptance_criteria>
  <done>OutboundWorker declares Oban uniqueness keyed on the three-tuple from D-11; existing Phase 24 callers continue to work because nil is treated as a valid key value; tests cover both old and new shapes; warnings-clean build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract build_trigger_multi/2 + thread :bulk_envelope_id additive opt</name>
  <files>lib/cairnloop/outbound.ex, test/cairnloop/outbound_test.exs</files>
  <read_first>
    - lib/cairnloop/outbound.ex (full 65-line file — sealed `trigger/2` shape)
    - test/cairnloop/outbound_test.exs (full file — MockRepo handles `Ecto.Multi.merge/2`; the existing five `describe "trigger/2"` tests must all remain green)
    - .planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md § "Pattern 1: Sealed-Primitive Additive Opt" (lines 165–179) and § "Pattern 2: One `Ecto.Multi` for Envelope + Fan-out" (lines 181–229) — load-bearing
    - .planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md § "Architecture & Re-use" (D-12 sealed, D-13 envelope)
  </read_first>
  <behavior>
    - Test 1: `Outbound.trigger(1, template_id: "recovery_v1")` (no `bulk_envelope_id`) STILL produces a `:message` and `:delivery_job` in results, with `args == %{"message_id" => 999}` OR with `bulk_envelope_id => nil` threaded through — the executor's call: as long as Phase 24 contract (existing test on line 53–64) doesn't regress, the new shape is acceptable.
    - Test 2: `Outbound.trigger(1, template_id: "recovery_v1", bulk_envelope_id: "envelope-uuid")` produces a `:delivery_job` whose `args` map includes `"bulk_envelope_id" => "envelope-uuid"`, `"conversation_id" => 1`, `"template_id" => "recovery_v1"` (these are required by the Task 1 Oban `unique:` keys).
    - Test 3: The message inserted by `trigger/2` with a `bulk_envelope_id` opt has `metadata["bulk_envelope_id"] == "envelope-uuid"` (so the conversation timeline can correlate per-recipient cards back to the envelope row — D-A consistency).
    - Test 4: Calling `trigger/2` without `:bulk_envelope_id` leaves `metadata["bulk_envelope_id"]` either absent OR `nil` — the executor's call; both shapes are acceptable as long as it doesn't break the existing Phase 24 metadata expectations.
  </behavior>
  <action>
    Modify `lib/cairnloop/outbound.ex`:

    1. Extract a private `build_trigger_multi/2` function. Signature: `build_trigger_multi(conversation_id, opts) :: Ecto.Multi.t()`. It returns the per-recipient `Ecto.Multi` WITHOUT running it — i.e., the `Ecto.Multi.new() |> Ecto.Multi.insert(:message, ...) |> Ecto.Multi.merge(fn %{message: ...} -> ... end)` chain currently inlined in `trigger/2`'s body, PLUS the auditor step. (The auditor step needs to be parameterizable — see step 3 below.)
    2. Add `:bulk_envelope_id` as an additive optional key: `bulk_envelope_id = Keyword.get(opts, :bulk_envelope_id)`. Thread it into:
       - `Message.changeset/2` metadata under the `"bulk_envelope_id"` string key (alongside `"template_id"` and `"status"`).
       - `OutboundWorker.new/2` args under `"bulk_envelope_id"`, `"conversation_id"`, `"template_id"` (the latter two are needed for Task 1's Oban `unique:` keys).
    3. Rewrite `trigger/2` to call `build_trigger_multi(conversation_id, opts)`, wrap the result in `Cairnloop.Telemetry.span([:outbound, :triggered], meta, fn -> ... end)`, and `repo().transaction/1` the multi. The PUBLIC signature `trigger(conversation_id, opts)` MUST NOT change — only what's inside the body. The auditor step `auditor.audit(:outbound_trigger, actor, %{conversation_id, template_id})` MUST still appear with the same action name `:outbound_trigger` (test on line 100–114 pins this).
    4. Add a module-level `defp max_batch_size, do: Application.get_env(:cairnloop, :max_batch_size, 25)` (will be used in Task 3 — adding it here keeps the next task's diff smaller and tightens the module docstring's invariant list).
    5. Update the module docstring to enumerate the additive opt (`:bulk_envelope_id`) and the sealing invariant (D-12).

    Extend `test/cairnloop/outbound_test.exs`:

    - Existing five tests in `describe "trigger/2"` MUST remain green (Phase 24 / 23 / 22 regression gate).
    - Add a new `describe "trigger/2 with :bulk_envelope_id (Phase 25 additive opt)"` block covering Test 2 + Test 3 + Test 4 above.
    - Use the existing MockRepo (it already supports `Ecto.Multi.merge`).
    - Headless per D-16.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tail -10 &amp;&amp; mix test test/cairnloop/outbound_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/outbound_test.exs` exits 0; all five existing Phase 22/23 tests green; the three (or more) new Phase 25 tests green.
    - `grep -c "defp build_trigger_multi(" lib/cairnloop/outbound.ex` returns 1.
    - `grep -c "Keyword.get(opts, :bulk_envelope_id)" lib/cairnloop/outbound.ex` returns at least 1.
    - `grep -c "\"bulk_envelope_id\" =>" lib/cairnloop/outbound.ex` returns at least 1 (the worker args build).
    - The public function `def trigger(conversation_id, opts) do` is still present and is the only public `trigger/2` definition: `grep -c "^  def trigger(conversation_id, opts) do" lib/cairnloop/outbound.ex` returns 1.
    - `grep -c "Application.get_env(:cairnloop, :max_batch_size, 25)" lib/cairnloop/outbound.ex` returns 1.
    - `grep -c "D-12\\|D-13" lib/cairnloop/outbound.ex` returns at least 1 (decision-ID traceability).
    - `mix compile --warnings-as-errors` exits 0 (D-15).
  </acceptance_criteria>
  <done>Private helper extracted; additive opt threaded through; sealed `trigger/2` public shape preserved; Phase 22–24 regression tests still green; `max_batch_size/0` helper exists for Task 3's consumer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Outbound.bulk_trigger/2 with cap, snapshot, and fan-out</name>
  <files>lib/cairnloop/outbound.ex, test/cairnloop/outbound_test.exs</files>
  <read_first>
    - lib/cairnloop/outbound.ex (as just modified by Task 2 — `build_trigger_multi/2` and `max_batch_size/0` are now available)
    - lib/cairnloop/outbound/bulk_envelope.ex (schema from Plan 01)
    - lib/cairnloop/governance.ex § `propose_blocked` (lines 405–548) — load-bearing analog for "persist the refusal as a durable row, return `{:error, …}` to the caller" pattern
    - lib/cairnloop/telemetry.ex (entire — `Cairnloop.Telemetry.span/3` shape)
    - .planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md § "Pattern 2: One `Ecto.Multi` for Envelope + Fan-out" (lines 181–229) — load-bearing for `Ecto.Multi.merge` shape
    - .planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md § "Pitfall 4: Cap check only on the LiveView side" (lines 362–366) — defense-in-depth invariant
    - .planning/phases/25-bulk-selection-fan-out/25-RESEARCH.md § "Pitfall 5: Telemetry label leak — `template_id` is high-cardinality" (lines 368–372) — enum-only labels (D-B)
    - .planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md § "Architecture & Re-use" (D-13, D-14)
    - .planning/phases/25-bulk-selection-fan-out/25-CONTEXT.md § "Batch Safety Rails" (D-09, D-10, D-11)
  </read_first>
  <behavior>
    - Test 1 (happy path, headless): `Outbound.bulk_trigger([1, 2, 3], template_id: "recovery_v1", rendered_body: "Hi, ...", actor: "agent_1")` returns `{:ok, results}` where `results.envelope` is a `BulkEnvelope` struct with `count: 3`, `recipient_conversation_ids: [1, 2, 3]`, `rendered_body: "Hi, ..."`, `template_id: "recovery_v1"`, `status: :submitted`, and a UUID-shaped `:id`; and `results` ALSO contains the per-recipient `:message_1`, `:message_2`, `:message_3` keys (one per conversation, scoped by id) inserted via the merged multi. The exact key-naming convention (e.g., `:"message_#{cid}"` vs `:"trigger_#{cid}"`) is the executor's call — document it in the docstring.
    - Test 2 (cap refusal at cap+1): `Outbound.bulk_trigger(Enum.to_list(1..26), template_id: "recovery_v1", rendered_body: "Hi", actor: "agent_1")` returns `{:error, :batch_too_large}`. A `BulkEnvelope` row IS still inserted in the MockRepo, with `status: :refused_cap_exceeded`, `count: 26`, `refused_reason: "batch_size 26 exceeds cap 25"` (or equivalent), AND NO per-recipient triggers are inserted (`results` keys do not include any `:message_*`).
    - Test 3 (cap-boundary OK at exactly cap=25): `Outbound.bulk_trigger(Enum.to_list(1..25), ...)` returns `{:ok, results}` with `status: :submitted` and 25 per-recipient inserts. (Sample at `cap` and `cap+1` only — Nyquist.)
    - Test 4 (snapshot persistence): The `BulkEnvelope.rendered_body` column on the inserted row is exactly the string passed in as `:rendered_body`; it is NOT re-rendered, re-resolved, or re-templated. Changing `Application.put_env(:cairnloop, :outbound_recovery_template_id, "v2")` between the `bulk_trigger/2` call and an inspection of the envelope row does NOT change `envelope.rendered_body` (mirrors research Pitfall 2 regression test).
    - Test 5 (envelope id threaded to triggers): Each per-recipient `:delivery_job` in the merged results carries `args["bulk_envelope_id"]` equal to the envelope's UUID. (Cross-references Task 2's threading.)
    - Test 6 (configurable cap via env): `Application.put_env(:cairnloop, :max_batch_size, 3)` (with `on_exit` cleanup) causes `bulk_trigger([1, 2, 3, 4], ...)` to refuse with `{:error, :batch_too_large}` AND `bulk_trigger([1, 2, 3], ...)` to succeed.
    - Test 7 (telemetry — enum-only labels per D-B): `:telemetry.attach/4` on `[:cairnloop, :outbound, :bulk, :triggered, :stop]`; emit a happy path; assert the metadata map contains `outcome: :submitted` and `count: 3` and does NOT contain `template_id`, `conversation_id`, `actor`, or any recipient identifier as a label.
    - Test 8 (telemetry — refusal): The same telemetry event on a cap-exceeded refusal emits `outcome: :refused_cap_exceeded`.
  </behavior>
  <action>
    In `lib/cairnloop/outbound.ex`, ADD (do not modify the public `trigger/2` signature) a new public function `bulk_trigger/2`:

    Signature: `def bulk_trigger(conversation_ids, opts) when is_list(conversation_ids)`.

    Required opts: `:template_id` (string), `:rendered_body` (string — the pre-rendered template body, snapshotted at confirmation time per D-13 / CLAUDE.md). The caller (Plan 03's InboxLive) is responsible for rendering — `bulk_trigger/2` does NOT call the template engine. This keeps D-13's "snapshot at decision time" strictly enforceable: the caller renders ONCE and passes the body in; the envelope persists exactly that string.

    Optional opts: `:actor` (string), `:auditor` (module, default same as `trigger/2`).

    Behavior:

    1. **Cap check (D-09, defense-in-depth — Pitfall 4):** If `length(conversation_ids) > max_batch_size()`, build an envelope changeset with `status: :refused_cap_exceeded`, `refused_reason: "batch_size #{length(conversation_ids)} exceeds cap #{max_batch_size()}"`, `recipient_conversation_ids: conversation_ids`, `count: length(conversation_ids)`, `requested_at: DateTime.utc_now()`, `requested_by: actor`, then `repo().insert/1` it (the audit story for refused attempts — research Open Question 5), emit `[:cairnloop, :outbound, :bulk, :triggered]` telemetry with `outcome: :refused_cap_exceeded` and `count: length(...)`, and return `{:error, :batch_too_large}`.

    2. **Happy path (count ≤ cap):** Generate `envelope_id = Ecto.UUID.generate()`. Build `envelope_attrs`. Build an `Ecto.Multi`:
       - Step 1: `Ecto.Multi.insert(:envelope, BulkEnvelope.changeset(%BulkEnvelope{}, envelope_attrs))`.
       - Step 2: `Ecto.Multi.merge(fn %{envelope: env} -> Enum.reduce(conversation_ids, Ecto.Multi.new(), fn cid, acc -> Ecto.Multi.merge(acc, fn _ -> build_trigger_multi(cid, opts ++ [bulk_envelope_id: env.id]) end) end) end)`.
       - Step 3: `auditor.audit(multi, :bulk_outbound_trigger, actor, %{bulk_envelope_id: envelope_id, count: length(conversation_ids), template_id: template_id})` — note `template_id` IS allowed in the AUDIT metadata (durable Ecto row, not a telemetry label); it just MUST NOT appear in the `:telemetry` event labels.
       - Wrap the whole `repo().transaction(multi)` call in `Cairnloop.Telemetry.span([:outbound, :bulk, :triggered], %{outcome: :submitted, count: length(conversation_ids)}, fn -> ... end)`. Inside the span the metadata for the `:stop` event must be enum-only — D-B.

    3. **Multi key collision:** Per-recipient multi keys MUST be unique across the merged multi. The existing `trigger/2` uses `:message` and `:delivery_job` keys — calling N copies in a single merged multi would collide. Disambiguate by passing a per-recipient prefix (e.g., make `build_trigger_multi/2` accept an optional `:multi_key_prefix` opt that defaults to `nil`; when present, it builds steps named `:"message_#{prefix}"` and `:"delivery_job_#{prefix}"`). The cleanest implementation: in `bulk_trigger/2`'s merge function, pass `multi_key_prefix: cid` for each recipient. Update `build_trigger_multi/2` accordingly (this is an internal change — `trigger/2`'s observable behavior, including the key names `:message` and `:delivery_job`, MUST remain the same, so when `multi_key_prefix == nil` the keys stay `:message` and `:delivery_job` exactly as the Phase 22/23 tests expect).

    4. **No telemetry label leakage:** the metadata map passed to `Cairnloop.Telemetry.span/3` must contain ONLY `outcome` and `count`. `template_id` lives in the durable `BulkEnvelope` row + the auditor metadata, but NOT in `:telemetry` labels (D-B + research Pitfall 5 + Open Question A4).

    5. **Module docstring:** Add a `@doc` for `bulk_trigger/2` explicitly stating: caller pre-renders the template body and passes it as `:rendered_body`; the function never re-resolves the template (CLAUDE.md / T-25-03 mitigation); refused-cap attempts persist a `BulkEnvelope` row with `status: :refused_cap_exceeded` (research Open Question 5); cite D-09, D-11, D-13.

    Extend `test/cairnloop/outbound_test.exs`:

    - Add a new `describe "bulk_trigger/2"` block holding Tests 1–8.
    - Use the existing MockRepo (already supports `Ecto.Multi.merge`). Extend it ONLY if needed to assert on the envelope insert and on per-recipient `:message_*`/`:delivery_job_*` keys (the executor's call: it may be cleanest to give MockRepo a way to capture each insert into a process-dictionary list for assertion).
    - Use `setup` + `on_exit` to clean up any `Application.put_env(:cairnloop, :max_batch_size, ...)` between tests (research Pitfall 7).
    - Use `:telemetry.attach/4` + `:telemetry.detach/1` per test for the telemetry assertions; mirror the existing pattern at line 80–98 of `outbound_test.exs`.

    6. **Integration tests for Postgres-only invariants (D-16 / CLAUDE.md compliance — REPO-UNAVAILABLE):** The headless MockRepo tests above prove contract and snapshot semantics but cannot faithfully prove (a) `Ecto.Multi` atomicity (rollback when the envelope insert fails after per-recipient changesets are built) or (b) Oban actually rejecting a duplicate insert under the `unique:` clause from Task 1. Per CLAUDE.md: "tests that genuinely require a Postgres round-trip should be written but marked with a `# REPO-UNAVAILABLE` note where they can't run here." Author exactly two such tests:

       - In `test/cairnloop/outbound_test.exs`, add a `describe "bulk_trigger/2 — Postgres integration"` block tagged `@tag :integration` (so `mix test` excludes it; `mix test.integration` runs it). Inside, one test: `# REPO-UNAVAILABLE — requires Cairnloop.Repo + Postgres; runs on a DB-available host via mix test.integration. Asserts BulkEnvelope row + N Message rows are inserted atomically; force an FK violation by passing a non-existent conversation_id and assert the envelope row is rolled back (count_after == count_before).`
       - In `test/cairnloop/workers/outbound_worker_test.exs`, add a `describe "Oban unique: dedup under bulk envelope"` block tagged `@tag :integration`. Inside, one test: `# REPO-UNAVAILABLE — requires Cairnloop.Repo + Postgres + Oban tables. Asserts that two consecutive Oban.insert calls with identical args {conversation_id, template_id, bulk_envelope_id} succeed once and are deduped on the second (Oban returns the existing job, no second row in oban_jobs).`

       Both tests MUST carry the literal comment `# REPO-UNAVAILABLE` on the line above the test name so a grep can confirm coverage. The tests do NOT need to pass in this workspace (D-16 caveat) — they must pass on a Postgres-available host.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/cairnloop &amp;&amp; mix compile --warnings-as-errors 2&gt;&amp;1 | tail -10 &amp;&amp; mix test test/cairnloop/outbound_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/cairnloop/outbound_test.exs` exits 0 with all eight new `bulk_trigger/2` tests green AND all Task-2 and original Phase 22/23 tests still green (full file passes).
    - `grep -c "def bulk_trigger(conversation_ids, opts) when is_list" lib/cairnloop/outbound.ex` returns 1.
    - `grep -c "{:error, :batch_too_large}" lib/cairnloop/outbound.ex` returns at least 1.
    - `grep -c ":refused_cap_exceeded" lib/cairnloop/outbound.ex` returns at least 2 (one for the changeset attrs, one for the telemetry outcome).
    - `grep -c "Ecto.Multi.insert(:envelope" lib/cairnloop/outbound.ex` returns 1.
    - `grep -c "Cairnloop.Telemetry.span(\[:outbound, :bulk, :triggered\]" lib/cairnloop/outbound.ex` returns 1.
    - `grep -c "D-09\\|D-11\\|D-13" lib/cairnloop/outbound.ex` returns at least 2.
    - The telemetry metadata grep gate (D-B enforcement): `grep -E "Cairnloop\\.Telemetry\\.span\\(\\[:outbound, :bulk" lib/cairnloop/outbound.ex | grep -v '^#' | grep -c "template_id" ` returns 0 (the span call site does NOT pass `template_id` in its meta map).
    - `mix compile --warnings-as-errors` exits 0 (D-15).
    - REPO-UNAVAILABLE integration coverage authored (D-16 / CLAUDE.md): `grep -c "# REPO-UNAVAILABLE" test/cairnloop/outbound_test.exs` returns at least 1 AND `grep -c "# REPO-UNAVAILABLE" test/cairnloop/workers/outbound_worker_test.exs` returns at least 1; the two `@tag :integration` test names must include the strings `atomically` (in `outbound_test.exs`) and `unique` (in `outbound_worker_test.exs`).
  </acceptance_criteria>
  <done>`bulk_trigger/2` exists with cap, snapshot, fan-out, refusal-persistence, and enum-only telemetry; all tests green; warnings-clean build; `trigger/2`'s public shape STILL unchanged (D-12 verified by the regression block).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Any non-LiveView caller (MCP, console, future tools) → `bulk_trigger/2` | The envelope MUST enforce the cap regardless of caller — UI cap-check is a usability feature, not a security one (research Pitfall 4). |
| Confirmation-time render → worker run | Worker must NOT re-render the template; `BulkEnvelope.rendered_body` is the snapshot truth (CLAUDE.md). |
| `OutboundWorker` Oban-job args | Args carry `conversation_id`, `template_id`, `bulk_envelope_id` for dedup keys — these MUST be present on every enqueue (Phase 24 callers pass `bulk_envelope_id: nil`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering / DoS | `Outbound.bulk_trigger/2` envelope | mitigate | Task 3: hard cap enforced before any insert; oversized batches return `{:error, :batch_too_large}` AND persist a `:refused_cap_exceeded` row for audit. LiveView (Plan 03) adds the second layer of defense-in-depth. |
| T-25-02 | Repudiation / Tampering | `OutboundWorker` Oban dedup | mitigate | Task 1: `unique: [period: :infinity, fields: [:worker, :args], keys: [:conversation_id, :template_id, :bulk_envelope_id]]` — the same operator clicking "Confirm send" twice in quick succession (or an Oban retry burst) produces ONE delivery per recipient per envelope. Phase 24 single-conversation callers also participate (research OQ2 — locked decision). |
| T-25-03 | Tampering | `BulkEnvelope.rendered_body` | mitigate | Task 3: `bulk_trigger/2` accepts `:rendered_body` as a caller-provided string and persists it directly — no template re-resolution inside the function. The worker (sealed Phase 22/23) reads from message metadata, not from `Application.get_env(:cairnloop, :outbound_recovery_template_id)`, so a config flip mid-flight does not change what was approved. |
| T-25-04 | Tampering | (web layer bypass — out of scope for this plan) | accept (defer to Plan 03) | This plan only affects backend; Plan 03 owns the `InboxLive` callsite and the `grep -c "Conversation \\| where" lib/cairnloop/web/inbox_live.ex == 0` assertion. |
| T-25-05 | Information Disclosure | `:telemetry` event labels for bulk | mitigate | Task 3: `Cairnloop.Telemetry.span([:outbound, :bulk, :triggered], %{outcome: ..., count: ...}, ...)` — enum-only labels per D-B; `template_id`, `actor`, recipient identifiers stay in durable audit (Ecto + auditor) but NEVER in telemetry. Acceptance criterion includes a `grep` gate against `template_id` appearing inside the span call site. |
| T-25-SC | Tampering | New package installs | accept | This plan adds ZERO new packages. All dependencies (`ecto_sql`, `oban`, `jason`) are already locked and vetted. |
</threat_model>

<verification>
**Plan-wide checks before declaring done:**

1. `mix compile --warnings-as-errors` exits 0 (D-15).
2. `mix test test/cairnloop/outbound_test.exs test/cairnloop/workers/outbound_worker_test.exs` exits 0. The full `mix test` suite is green except for the pre-existing `Automation.DraftTest` baseline failure (memory note: cairnloop-baseline-draft-test-failure).
3. `grep -c "^  def trigger(conversation_id, opts) do" lib/cairnloop/outbound.ex` returns 1 — the sealed public signature is intact (D-12).
4. No file outside `files_modified` has been touched: `git diff --stat` shows ONLY `lib/cairnloop/outbound.ex`, `lib/cairnloop/workers/outbound_worker.ex`, `test/cairnloop/outbound_test.exs`, `test/cairnloop/workers/outbound_worker_test.exs`.
5. `lib/cairnloop/outbound.ex` does NOT import or reference any web-layer / LiveView module — `bulk_trigger/2` is purely a domain function (the rendered body is provided by the caller, not constructed here).
</verification>

<success_criteria>
- `Outbound.bulk_trigger/2` exists; cap enforced; envelope row persists for both success and refusal; rendered_body is exactly what the caller passed.
- `OutboundWorker` declares Oban uniqueness keyed on `(conversation_id, template_id, bulk_envelope_id)`; Phase 24 backwards-compat tests still pass with `bulk_envelope_id: nil`.
- Telemetry labels are enum-only (`outcome`, `count`) — no high-cardinality fields leak (D-B).
- The private `build_trigger_multi/2` helper exists and is the shared seam between `trigger/2` and `bulk_trigger/2` (research OQ1).
- Full headless suite is green (excluding the documented baseline failure).
- Scope: only the four declared files were modified — no LiveView changes (those land in Plan 03).
</success_criteria>

<output>
Create `.planning/phases/25-bulk-selection-fan-out/25-02-SUMMARY.md` when done, recording:
- The exact `bulk_trigger/2` signature landed (including any opts the executor added beyond the planned set).
- The disambiguation strategy used for per-recipient multi keys (which prefix scheme — e.g., `:"message_#{cid}"` or `:"trigger_#{cid}"`).
- Confirmation that the sealed `trigger/2` public signature `def trigger(conversation_id, opts)` is unchanged.
- Confirmation that the existing Phase 22/23/24 tests in `outbound_test.exs` and `outbound_worker_test.exs` all still pass.
- Telemetry event name + label vocabulary landed (so Phase 26 can read these in OBS-01).
- Any deviation from the research's recommended `Ecto.Multi.merge` shape (and why).
</output>
