{
  "name": "cairnloop-demo-screenshots",
  "private": true,
  "version": "0.0.0",
  "description": "Deterministic Playwright capture of the Cairnloop demo for docs + website. Non-gating; not wired into the Elixir build.",
  "type": "module",
  "scripts": {
    "capture": "playwright install chromium --with-deps && node capture.mjs",
    "capture:no-install": "node capture.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
