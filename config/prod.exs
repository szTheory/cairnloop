import Config

# Cairnloop is a host-owned library — the host app owns all production configuration.
# This stub exists only so the env-split `import_config "#{config_env()}.exs"` in
# config.exs resolves under MIX_ENV=prod. The published package ships no prod config.
